# Agent Memory — plan-critic

> Index of durable plan-review knowledge. Recurring-pattern tracker + stable recipes.
> Per `.claude/rules/agent-memory.md`: keep < 200 lines / < 25 KB. No session logs — git holds history.

## Recurring Issues Tracker

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---|---|---|---|---|
| UI-component-reuse plans (Study Mode flashcard tab reusing `AnswerOptions` app/_components/answer-options.tsx) assume `correctOptionId`+`disabled` reveals the correct option — WRONG: the green-correct highlight is gated on `showResult = lockedSelection != null && correctOptionId != null` (L66; lockedSelection = the `selectedOptionId` prop, L59). The plan's `selectedOptionId=null` → showResult=FALSE → NOTHING highlighted, flashcard renders blank; `disabled=true` also blocks any click-to-select so it never becomes non-null. FIX: pass `selectedOptionId = correctOptionId` (renders the key green via isCorrect, L77/38-39; submit button auto-hidden when showResult L113; `isExam` must stay falsy). 2nd pattern same plan: reusing `getRandomQuestionIds`→`_filtered_question_pool` (mig 20260614000001 L36-92) for a type-narrowed RPC MISSES that the pool has NO `question_type` clause → a MC-only `get_study_questions` (WHERE `question_type='multiple_choice'`) SILENTLY DROPS non-MC IDs → runner returns < requested `count` for mixed-type subjects (dormant now [no non-MC prod data], live once RT/short_answer/dialog_fill/ordering import). FIX: filter type at ID stage or over-fetch+document. 3rd: new files mirroring `quiz-config-form.tsx` (already 151 ln >§1 150-cap) / `use-quiz-start.ts` (83 ln >§1 80-hook-cap) inherit the over-limit → budget split upfront. POSITIVE (confirmed correct): new arbitrary-client-ID answer-exposure RPC REQUIRES `deleted_at IS NULL` (NOT §15 immutable-col carve-out — that needs session-frozen IDs); plan's guard set (auth-null/active-user+org/soft-delete/search_path/GRANT) clones get_quiz_questions mig 136 correctly; 42702 alias note correct; `correctOptionId` as own RETURNS TABLE col matches the separate `AnswerOptions` prop (L19); `useFlaggedQuestions` useEffect-fetch (use-flagged-questions.ts:14) is established session-runner precedent, not a new no-useEffect violation. Verdict: REVISE (0 CRIT, 2 ISSUE = reveal-mechanic + mixed-type drop; 1 SUGG line-budget). First seen: Study Mode plan. | 2026-06-26 | 1 | 2026-06-26 | WATCHING — UI-reuse contract (AnswerOptions showResult gate) + ID-pool type-filter gap |
| RPC-signature-change plans (RETURNS TABLE DROP+recreate, e.g. VFR-RT Phase 2 `get_quiz_questions` +question_type/dialog_template/blanks_safe) claim "caller = X ONLY" but MISS the generated `packages/db/src/types.ts` `Database['public']['Functions']['<fn>']` registry (`get_quiz_questions` @ types.ts:1429-1445 still lists OLD 12 cols). NOT a runtime/typecheck break: app's `rpc<TResult>` helper (`apps/web/lib/supabase-rpc.ts:13-20`) casts via `supabase as unknown as {rpc}` to a CALLER-SUPPLIED generic, never consults the registry → stale entry = doc-drift, not contract break (WHY such plans "work" untouched). FIX: add `packages/db/src/types.ts` to Files-affected; regenerate or flag doc-updater. ALSO this plan family: (a) N5-style validator-deferral plans justify "no UI produces non-MC yet" while the SAME task widens the type (`DraftAnswer` types.ts:88) — verify the actual answer-Map PRODUCER not the type: `use-answer-handler.ts:50` exposes only `handleSelectAnswer(optionId)` writing MC-only `{selectedOptionId,responseTimeMs}` (L56-59,90-93); `handleTextAnswer` lands Phase 3.2 → deferral SAFE but restate basis as "sole producer MC-only until 3.2". (b) integration plans assert RPC strip-output but OMIT the TS MAPPER pass-through assertion (load-session-questions.ts:54-62) where N1 "silently inaccessible" risk lives — name updating existing `*.test.ts` (new fields surface + MC→null); under-spec §7 ≥2-distinct-seed output-contract for new graders (`check_non_mc_answer`: 2 short_answer canonicals + per-blank `{index,is_correct,canonical}` length). (c) N2 internal-helper deviation acceptable on merits BUT binding spec note `tasks.md` N2 needs SPEC UPDATE before impl per deviation rule, not just a plan flag (security lens covers the GRANT/PUBLIC hazard separately — see next row). (d) **SQL-mechanics LATERAL trap (final-decision round, REVISED plan): the existing `get_quiz_questions` shuffles MC options via CROSS JOIN LATERAL `jsonb_array_elements(q.options)` + GROUP BY (mig 20260327000059 L50-61). `questions.options` is NOT NULL DEFAULT `'[]'::jsonb` for short_answer/dialog_fill (mig 20260610000100 L85-89) → `jsonb_array_elements('[]')` yields ZERO rows → the CROSS JOIN ELIMINATES the entire non-MC question row before GROUP BY → every short_answer/dialog_fill question SILENTLY VANISHES from the result (passes tsc/biome/db reset; runtime "no questions found").** Plan 2.1's "MC options shuffled (already present)" wording INVITES keeping the LATERAL — contradicts its own "verbatim mig 105" cite, which uses a CORRELATED SUBQUERY in `CASE WHEN question_type='multiple_choice' THEN (SELECT jsonb_agg(...) FROM jsonb_array_elements(q.options) opt) ELSE NULL END` (mig 20260611000100 L134-140, NO LATERAL/GROUP BY, preserves non-MC rows). FIX: plan must EXPLICITLY say replace LATERAL+GROUP BY with the mig-105 subquery form + drop GROUP BY; 2.4 must assert a mixed-type session returns ALL questions incl non-MC rows (current 2.4 checks column-stripping only — would miss a dropped row). RULE: when a plan extends an aggregating RPC to NEW question types, check whether the existing shape uses row-multiplying LATERAL/CROSS JOIN that drops rows for empty/NULL arrays. Verdicts: round-1 APPROVED (0 CRIT, 3 ISSUE non-blocking); FINAL-DECISION round REVISE (0 CRIT, 1 ISSUE = LATERAL trap — blocking). First seen: VFR-RT Training Phase 2. | 2026-06-21 | 2 | 2026-06-21 | RULE CANDIDATE (2) — LATERAL/CROSS-JOIN row-drop on empty array when extending aggregating RPC to new types |
| Data-layer-CHECK + regex-hardening answer-leak fix plans (#951 dialog_fill `}`-in-value breaks `[^}]*` strip → partial key leaks in student `dialog_template`) are SOUND when the CHECK and the strip are evaluated TOGETHER, but the verdict turns on a coordination invariant the plan must STATE: every input the hardened RPC strip would leave partial-key residue on MUST be REJECTED by the template CHECK (since the strip is defense-in-depth behind the constraint). VERIFIED EMPIRICALLY for #951: (1) hardened strip `\{\{(\d+)\|(?:[^}]|\}(?!\}))*\}\}`→`{{\1}}` is provably ≥ old `[^}]*` form on brace-in-value/synonym, identical on clean data, newline-safe (no `.`), ARE supports `(?!…)` lookahead; (2) it still leaves residue on adversarial `{{1|a}}b}}` (→`{{1}}b}} `) — but the template CHECK `regexp_replace(dialog_template,'\{\{\d+\|[^{}|]*\}\}','','g') !~ '[{}]'` REJECTS that exact case, so it can never be stored; (3) brute-search over 399 token-fragment combos found ZERO cases where CHECK passes yet RPC output retains a `{{\d+\|` token-pipe → the CHECK+strip pair is complete for the template surface. Delimiter analysis: `;` inside a value is NOT a leak (whole token strips to `{{n}}`) — correctly noted, covered by blanks_config CHECK only as authoring hygiene. blanks_safe emits index-only → blanks_config canonical/synonym values are NOT a direct student leak surface; the blanks_config CHECK is belt-and-suspenders for the template-mirrors-blanks authoring contract, not a second leak path (plan over-states it slightly as a leak guard — harmless). IMMUTABLE-helper-in-CHECK needs NO `search_path` (precedent `normalize_answer` mig 101 is IMMUTABLE PARALLEL SAFE, no search_path, pg_catalog built-ins only) — plan Risk-note correct; recommend mirroring `IMMUTABLE PARALLEL SAFE`. Guard-set re-emit VERIFIED only-regex-changes vs CURRENT bodies: get_quiz_questions mig 118 (auth-null L60, active-user+org gate `users u…deleted_at IS NULL` L72-73, org+deleted_at+active read scope L119-121, search_path L55, GRANT L125) and get_vfr_rt_exam_questions mig 105 (auth-null L71, active-user/org gate L77-82, §15 frozen-config carve-out + session-ownership `student_id=v_caller AND mode AND deleted_at` L92-95, config-shape guard L101-104, caller-org filter L157, search_path L63, GRANT L162) — all guards must survive; both are the LATEST CREATE-chain defs (no later re-emit). Verdict round-1 (general lens): APPROVED (0 CRIT/ISSUE; 1 SUGG). **Round-2 SQL-MECHANICS lens (this review) surfaced 2 ISSUE the round-1 lens missed → REVISE:** (A) **22023-not-23514 raise:** helper iterating synonyms via `jsonb_array_elements_text(b->'synonyms')` RAISES `22023 cannot extract elements from a scalar` if `synonyms` is present-but-not-an-array (NO constraint enforces array shape — mig 094 L94-110 only checks `jsonb_array_length(blanks_config)>0`, NOT element shape; the shape `{index,canonical,synonyms[]}` is a COMMENT L78 only). A CHECK that RAISES aborts INSERT with 22023, NOT the 23514 the tests assert. Missing-key path is safe (`b->'synonyms'`=SQL NULL→SRF 0 rows; `b->>'canonical'`=NULL→`NULL ~ regex`=NULL→not-matched). FIX: guard `jsonb_typeof(b->'synonyms')='array'` (mirror mig 105 L101-102 config-shape guard) or coalesce to `'[]'::jsonb`. (B) **FALSE PRECEDENT:** plan Validation/Patterns claims "matches the IMMUTABLE-helper-in-CHECK idiom" — VERIFIED ZERO table CHECK constraints in supabase/migrations/ call a user-defined fn (all use built-ins: jsonb_array_length/IN/comparisons — mig 094, mig 20260610000300 L47-51/98-102, mig 20260619000100 L104). The function-calling CHECK-like clauses are all RLS `WITH CHECK` (is_admin()/subqueries) — a DIFFERENT feature that PERMITS subqueries/fns; a table CHECK does not. This plan introduces the FIRST user-fn-in-table-CHECK (legal in PG, but the pattern-consistency claim is false → don't cite as validation; justify on merits: CHECK can't hold the SRF directly so a scalar IMMUTABLE wrapper is REQUIRED). SUGGESTIONs (non-blocking): helper search_path optional (not SECURITY DEFINER; precedent normalize_answer mig 101 omits it — consistent to omit, lean add since reachable from every dialog_fill write); state CHECK⊇strip-residue coordination invariant; note template-CHECK allows `;` but blanks-CHECK forbids it (asymmetry, document why). Empirics RE-CONFIRMED this round: hardened strip ≥ old + clean-identical + newline-safe + `(?!…)` ARE-supported; template CHECK `regexp_replace(...,'\{\{\d+\|[^{}|]*\}\}','','g') !~ '[{}]'` rejects every leak shape (}-in-value, |-in-value, value-ends-}, stray-{) with NO false-positive on clean multi-blank/multi-syn/multiline/empty-value/space templates; bracket class `[{}|;]`/`[{}]` literal (no PG-ARE escaping bug). Guard-set re-emit only-regex-change verified vs LATEST defs (mig 118 / mig 105). CREATE OR REPLACE (no DROP) correct (same signature). **Round-3 STABILITY (Opus, final sample) surfaced 2 NEW test-plan ISSUEs the prior SQL-mechanics lens missed → REVISE:** (C) **DDL-in-integration-test is INFEASIBLE in this harness, not merely "heavy":** the optional regex-hardening test "lifts the template CHECK via DROP/ADD CONSTRAINT in try/finally" — but the `packages/db` integration harness imports ONLY `@supabase/supabase-js` (setup.ts:1; NO `pg` driver in package.json), so all DB access is via PostgREST, which CANNOT issue `ALTER TABLE … DROP CONSTRAINT`. There is NO generic exec-SQL RPC in any migration and NO precedent for DDL in any `__integration__` test (grep = zero). The test as written cannot exist without adding a security-hostile arbitrary-SQL SECURITY DEFINER RPC or a new direct-pg dependency — neither scoped. The §7 error-accumulator/restore shape is moot because the test is unbuildable. (Had it been buildable: forks pool + NO db-reset-between-files + shared local DB → a crash between DROP and the finally re-ADD leaves prod-shared `questions` missing its guard for every later test file in that worker = real blast radius.) FIX: CUT the optional test (plan's own fallback) — rely on the 3 constraint-rejection 23514 tests + regex-covered-by-construction. The hardened regex IS exercised on clean data by the EXISTING happy-path strip tests (rpc-get-quiz-questions L428-466 / rpc-vfr-rt-questions L206-235; gqq accepts arbitrary `p_question_ids` so reachable, vfr is session-derived L47 so needs a frozen config). (D) **plan.md count-literal instruction is WRONG:** plan says bump "current-phase header + 'Integration tests:' line" — but the ONLY current-state count literal is the line-5 header (`197`); the THREE `**Integration tests:**` lines (35/51/83) are ALL HISTORICAL phase-completion records (#864/#837/Phase-A frozen "Suite now N" snapshots) which the doc-updater rule + plan-critic mem L134 forbid editing. Bump line-5 `197`→`197+N` ONLY; do NOT touch lines 35/51/83. ISSUE-1 (22023 shape guard) and false-precedent: BOTH resolved in revised plan (§ mig125 ISSUE-1-fix + Validation/Patterns "Do NOT cite a pre-existing idiom"). Regex lines RE-VERIFIED vs LATEST defs: get_quiz_questions L104 (`20260621000100`, =mig 118, arbitrary p_question_ids, org-scoped), get_vfr_rt_exam_questions L143 (`20260611000100`, =mig 105, session-derived). **Round-4 STABILITY (Opus, this review — re-ran ALL claims on LIVE local pg `supabase_db_lmsplusv2`): ISSUE-1 + false-precedent CONFIRMED resolved (empirical proofs: unguarded `jsonb_array_elements_text('"oops"')` RAISES 22023; guarded helper returns clean bools for non-array/missing/empty; real fn-in-CHECK aborts delimiter INSERT with SQLSTATE 23514 caught via `WHEN check_violation`; grep ALL supabase CHECKs = ZERO user-fn precedent; `(?!…)` ARE-supported; hardened strip byte-identical-on-clean + recovers brace-in-value; template CHECK rejects adversarial `{{1|a}}b}}` residue + pipe-in-value, permits `;`; Postgres does NOT enforce fn-immutability in CHECK so IMMUTABLE marking harmless; numbering 125/126/127+ts correct next-seq vs latest 124/000300). BUT round-3's ISSUE-C + ISSUE-D were NOT fixed in the revised plan — STILL PRESENT verbatim → NOT a clean stability round → REVISE. (C) plan L39 still proposes the "optional DDL-in-test (DROP/ADD CONSTRAINT in try/finally)" — RE-VERIFIED INFEASIBLE: setup.ts imports ONLY `@supabase/supabase-js`, package.json has NO pg/postgres driver, `grep ALTER TABLE|DROP CONSTRAINT|exec_sql packages/db/src/__integration__/`=ZERO; PostgREST can't issue DDL → unbuildable. FIX: CUT it (plan's own fallback); hardened regex already exercised on clean data by existing happy-path strip tests. (D) plan L35 still says bump "current-phase header + 'Integration tests:' line" — but the ONLY current-state literal is the line-5 header (`197`); the 3 `**Integration tests:**` lines (35/51/83) are HISTORICAL phase-completion snapshots (#864-161/#837-154/PhaseA-136) the doc-updater rule + this tracker forbid editing → bump line-5 `197`→`197+N` ONLY. Round-4 verdict: REVISE (0 CRIT, 2 ISSUE = C+D residual; both ≤ trivial plan-text edits).** **Round-5 STABILITY (Opus, FINAL confirmation): C+D BOTH NOW APPLIED → CLEAN, 0 APPLY → APPROVED.** (C) plan L39 now "No DDL-lifting regex test — DECIDED CUT (all 3 stability critics)" with full PostgREST-only-harness rationale; coverage re-grounded on existing clean-data strip tests + covered-by-construction behind template CHECK + 3 new 23514 tests. (D) plan L35 now targets ONLY line-5 header `integration suite now 197`→`200` (+3, arithmetic correct) + explicitly forbids touching frozen snapshots L35/51/83 — VERIFIED vs live docs/plan.md: L5=sole current-state literal `197`, L35=`Suite now 161`(#864), L51=`Suite now 154`(#837), L83=`136…at delivery`(PhaseA), all correctly excluded; plan hedges "verify exact literal at commit time." Core unchanged/re-confirmed (hardened strip, template CHECK, synonyms jsonb_typeof='array'→23514, faithful CREATE OR REPLACE mig 118/105 regex-line-only, false-precedent corrected L47, IMMUTABLE PARALLEL SAFE no-search_path on merits). R4(core)+R5(this) = 2 consecutive clean stability samples on security path. First seen: #951. **Recurrence (PR #998 VFR RT ordering, 2026-06-26): plan adds IMMUTABLE `is_valid_ordering_items(jsonb)` to mig 134 `questions_question_type_columns_check` ordering branch. UPDATE: user-fn-in-CHECK NOW HAS a merged repo precedent — mig 125 `dialog_fill_blanks_delimiter_free` (LANGUAGE sql IMMUTABLE PARALLEL SAFE, jsonb-arg-only, in `questions_dialog_fill_*` CHECKs); cite it, drop the old "first/no-precedent" framing. SRF-CHECK 22023 STILL applies: `is_valid_ordering_items` must wrap its `jsonb_array_elements` arg in `CASE WHEN jsonb_typeof(p)='array' THEN p ELSE '[]'` (mirror mig 125 L73-88) → non-array returns FALSE (clean 23514) not a 22023 abort (AND-order vs the sibling `jsonb_array_length>=2` clause not contractual); helper CREATE must precede ADD CONSTRAINT in-file. 2nd finding (mig 139 perm-guard #466): count(*)=N AND count(DISTINCT selected_option)=N pre-loop guard is SAFE vs legit submits — VERIFIED `OrderingInput.handleSubmit` submits `order.map(id)` over the FULL N-item sortable list (arrayMove preserves length, no "unplaced" concept) + existing 3-of-5 partial-CREDIT test submits a full perm `[0,1,2,4,3]` (count=5=N passes); partial CREDIT ≠ partial SUBMISSION. Place guard AFTER _batch_questions build (~L190) BEFORE FOR loop (L192) so replay(L103-132)+expiry(L135-150) early-returns unaffected; selectedOptionId→selected_option mapping confirmed (action L46). Both migs unmerged → edit BOTH byte-identical mirrors (134≡20260625000100, 139≡20260625000600) + require `db reset --no-seed`+functional INSERT/RPC execution tests (deferred-validation SQL §5).** | 2026-06-23 | 6 | 2026-06-26 | RULE CANDIDATE (2) — (1) user-fn-in-CHECK NOW has precedent mig 125 (cite it); (2) SRF-CHECK-helper 22023 total-function guard; (3) DDL-in-`__integration__`-test infeasible; (4) plan.md count: bump line-5 header only |
| VFR-RT Phase 4 report-rendering plans (per-question grouping of per-blank dialog_fill rows; item-level `correct_count`). 3 ISSUE the general lens caught (logic/data-flow review): (1) **N7 pagination/builder slice-then-group**: plan says "paginate on DISTINCT question_id (pages-of-questions)" but CURRENT `quiz-report-questions.ts:43-69` paginates ANSWER ROWS (`count:'exact'` over `quiz_session_answers`, `.range(from,to)` ordered `answered_at,id`). If only display-grouping changes, a 9-blank dialog at a `PAGE_SIZE=10` boundary delivers 3 blanks pg1 + 6 pg2 → builder emits SAME questionId twice (dup React key + wrong fraction). NO PostgREST `SELECT DISTINCT question_id LIMIT/OFFSET` primitive → plan MUST name concrete shape (fetch all session rows, dedupe distinct qid list in TS, slice list to page, filter rows to page-slice qids → safe ∵ N bounded). Admin sibling `admin-quiz-report.ts:150-211` has identical row-level `.range()` — same fix both callers. (2) **field repurpose silent-units-drift**: `QuizReportSummary.answeredCount` repurposed row-count→distinct-questions WITHOUT rename; `result-summary.tsx:37` skipped uses it, `:74`/`:103` Correct denominator was it (plan swaps to new `answeredItems`). Rename→`answeredQuestions` OR record grep proving only 3 consumers; specify NEW `answeredItems` fallback (mirror existing `quiz-report.ts:112` `?? total_questions`). (3) **sibling audit `complete_quiz_session` (mig 104:77-90)**: its `correct_count`=`count FILTER(is_correct)` over ALL rows = ALREADY item-level (no change needed for D1) BUT its `score_percentage`=`v_correct/count(*)` is ROW-level, DIVERGES from Decision-47 per-question partial-credit (`batch_submit` mig 130:277-291). MITIGATING: `completeQuiz` action (calls it) has ZERO app callers (study runner uses `batchSubmitQuiz` exclusively) → dead for non-MC, latent not live. Plan must discharge on-record (state dead + defer, or align), not leave as open Q (security.md §12). VERIFIED INDEPENDENT: mig 130 `v_correct_credit`(score) and `v_correct_count` are separate SELECT-list exprs over same join → changing `v_correct_count`→`sum(correct_rows)` does NOT touch score% (plan's "score unchanged" HOLDS). SCENARIOS (a/b/c) all coherent: item-level Correct + question-level Skipped is intentional mixed-unit (D1b), no numerator>denom / negative skipped. §2 summary-fetch bounded-N OK but PostgREST 1000-row cap caveat. §3 builder must SORT blanks by blank_index (fetch orders `answered_at,id` NOT blank order) + skipped (zero-row) Qs absent from breakdown by design. Verdict: REVISE (0 CRIT, 3 ISSUE). **CONTRACT/TEST/TYPE lens (2nd coverage round, this review) CONFIRMS the 3 above + adds 3 the general lens MISSED: (4) STALE-TEST-NAME-AFTER-SEMANTICS-CHANGE: the integration re-derivations are arithmetically CORRECT (rpc-batch-submit-quiz L502 0→2, L524 1→3, L541 MC 1==1, L668 expired-replay-0 unchanged — verified vs fixtures) BUT plan treats them as NUMBER-ONLY; each carries a stale TEST TITLE/COMMENT encoding the OLD "fully-correct questions" contract (L512 title "counts correct_count as fully-correct questions only", L483 comment "correct_count=0 (question not fully correct)") → §7/stale-comment-after-rename: must RENAME+rewrite, not renumber. (5) SINGLE-MOCK-2nd-RPC: adding `get_report_answer_keys` 2nd `.rpc()` breaks the single call-order `mockRpc` in quiz-report-questions.test.ts (L13 single fn, `mockResolvedValueOnce` L163/179/286 feed-first-caller, `toHaveBeenCalledWith` L264, `not.toHaveBeenCalled` L250) → convert to name-dispatching mockImplementation + per-name assertions; plan's "new RPC mock" one-liner under-specs this. (6) DISCRIMINATED-UNION FLAT-FIELD breakage ENUMERATED: report-question-row.tsx L28 (`question.options.some`+`selectedOptionId`), L97-101 (OptionsList `options`/`correctOptionId`/`selectedOptionId`) → tsc rejects on SA/DF variants unless narrowed per questionType; AND admin `.select()` admin-quiz-report.ts:149 OMITS `response_text,blank_index,question_type` (plan adds them only to student L65) → builder mis-discriminates the admin feed; plan's "confirm it compiles" insufficient. RECONCILED w/ general lens on the mig-104 sibling: they verified `completeQuiz` action has ZERO app callers (study runner uses batchSubmitQuiz exclusively) → score%-divergence is LATENT not live → ISSUE (discharge on-record), NOT CRIT; my initial CRIT framing stands DOWN given that caller-path evidence. result-summary.test.tsx:10-25 makeSummary lacks `answeredItems` + L55 `'7/10'` Correct-fraction re-derive vs answeredItems; quiz-report.test.ts:121 `{count:2,data:null}` mock shape breaks under head-count→rows-fetch+Set. Net Phase 4 verdict across both lenses: REVISE (0 CRIT, 6 ISSUE — 3 general + 3 contract/test/type).** **STABILITY round (Opus, this review — FULLY-REVISED plan, all ~11 coverage findings incorporated): core design VERIFIED CORRECT + self-consistent vs source (mig 130 L280 one-liner `count FILTER fully-correct`→`coalesce(sum(coalesce(g.correct_rows,0)),0)::int` independent of L279 v_correct_credit/score% + L278 v_answered [separate SELECT-list exprs same join] → "score unchanged" HOLDS; replay L98-134 returns stored v_correct_count verbatim=consistent-by-construction; complete_quiz_session mig 104 L79-80 already `count FILTER(is_correct)` item-based→correctly UNCHANGED; new RPC `(question_id,question_type,blank_index,answer_key)` carries SA-1row+DF-Nrow, `q.question_type AS question_type` qualifier avoids 42702, guard set mirrors get_report_correct_options mig 114 exactly [auth-null/active-user/owner/ended_at/§15-carveout/search_path/GRANT]; blanks_config per-blank shape `{index,canonical,synonyms[]}` confirmed via check_non_mc_answer mig 119 L226-231; admin builder-default questionType→MC consistent w/ student-only .select() add; pages-of-questions slice-distinct-ids→fetch-rows-by-id-IN DOES prevent dialog-straddle; header triad correctCount(items)/answeredItems(items)+totalQuestions−answeredQuestions(distinct) units-coherent; QuestionBreakdown index `(page-1)*pageSize+i` L28 stays correct ∵ pages=pageSize-questions now). BUT REVISE — 3 NEW ISSUEs coverage MISSED, all test-impact-LIST completeness (NOT design): (A) **rename+union breaks 3 MORE .test files plan never lists**: `report-card.test.tsx` L16-25 typed `QuizReportSummary` literal `answeredCount:3`+NO `answeredItems` (2 tsc errs) + `QuizReportQuestion` MC-literals L30+ (need union discriminant); `question-breakdown.test.tsx` (QuizReportQuestion literals); `admin-quiz-report.test.ts` (renamed field). ALSO `result-summary.test.tsx` L62/L68 skipped-case `answeredCount` overrides — plan listed only L10-25 makeSummary+L55. Validation §5 "rename scoped to QuizReportSummary ONLY" right about SOURCE (grep: `summary.answeredCount` consumed only result-summary.tsx; every other repo `answeredCount`=unrelated type) but TEST surface wider. (B) **report integration tests omitted from impact list**: `quiz-report.integration.test.ts`+`quiz-report-questions.integration.test.ts` exercise the real changed-contract fns; new `.rpc('get_report_answer_keys')` site in quiz-report-questions.ts triggers code-style §7 HARD new-app-query-site integration-test rule → plan must state co-located app-tier assertion vs only mig-tier rpc-report-answer-keys.integration.test.ts. (C) **pagination ORDER BY column doesn't exist on queried table**: plan says "fetch DISTINCT question_id ORDERED by question_number" but `question_number` is a `questions` col NOT on `quiz_session_answers` (current code orders `answered_at,id` L67-68) → `SELECT DISTINCT question_id … ORDER BY question_number` over qsa-alone won't compile; needs JOIN-to-questions OR keep answered_at order. SUGGESTIONs: blanks_config key is `b->>'index'` NOT `blank_index`; refresh mig 130 stale comments L45/L259-261 ("fully-correct question count"→"correct items"). Stability verdict: REVISE (0 CRIT, 3 ISSUE = test-list A+B + ordering-col C; 2 SUGG) — NOT clean, re-run after revisions. First seen: VFR-RT Phase 4 (#697). **VFR-RT Phase 5 (`ordering` q-type, NEW occurrence — same dialog_fill-clone report family) round-1 CONTRACT/PATTERN lens REVISE (0 CRIT, 5 ISSUE, 3 SUGG): core design SOUND + verified (per-position rows = dialog_fill clone; item-id-in-`selected_option`+slot-in-`blank_index` wire shape consistent w/ `fanOutAnswer` quiz-submit.ts:26-42 dialog_fill expansion; pagination by DISTINCT question_id [`groupAnswersByQuestion`+`buildDistinctQuestionOrder`] handles multi-row free). 5 ISSUE all INCOMPLETE union/validator widening sweep, NOT architecture: (1) `buildAnswerKeyMap` (quiz-report-helpers.ts:26-42) routes ONLY `question_type==='dialog_fill'` into per-index `canonicalByIndex`; a new `'ordering'` falls into the `else` short_answer branch (L39-41) → each slot row OVERWRITES → final entry `{type:'short_answer',canonical:<last slot>}` = ALL slot keys lost but last. FIX: widen `AnswerKeyEntry` (report-question-builder.ts:37-39, only SA|DF today) + add ordering branch to collapser + `buildOrdering` reads `key?.type==='ordering'`. (2) plan widens ONLY localStorage validators (`quiz-session-validators.ts`) — MISSES the SERVER draft Zod `SaveDraftInput` (draft-schema.ts): answer `.refine` L48-53 requires exactly-one-of {selectedOptionId,responseText,blankAnswers} (no `order`) → ordering draft rejected; `feedback` `z.discriminatedUnion('questionType',[mc,sa,df])` L61-108 THROWS on `'ordering'` discriminant → save-draft is a LIVE study-mode path (quick_quiz). (3) `fanOutAnswer` (quiz-submit.ts:26-42) discriminates blankAnswers→responseText→MC-default; `order`-carrying DraftAnswer matches neither → falls to MC default L41 → malformed entry no `selected_option`/`blank_index`; the exactly-one-shape invariant must widen in LOCKSTEP across `fanOutAnswer`+`isValidDraftAnswer`(validators L33-41)+draft-schema refine. (4) `report-question-row.tsx:32` drives row icon+red-bg from single `question.isCorrect` (no ordering-awareness) → plan must STATE `buildOrdering` sets `isCorrect=correctCount===totalItems` (dialog_fill parity report-question-builder.ts:165) else partials mislabeled. (5) `load-session-questions.ts` has TWO hardcoded `question_type` union literals (L14 raw + L27 mapped) — BOTH widen; name the mapped `Question` field + the `OrderingInput` prop wiring in quiz-main-panel.tsx new branch. SUGG: admin path (admin-quiz-report.ts row-`.range()`+omitted cols) intentionally MC-only under documented #991 — state so; dnd-kit in package.json L24-26 but ZERO existing imports (first use) → extract `use-ordering-input.ts` hook for ≤150-line cap + co-located hook test (§7); restate exam-mode guard quiz-main-panel.tsx:24-30 blocks non-MC → ordering study-only intentional. Verified dnd-kit present, `quiz-main-panel` dispatch, `CheckNonMcAnswerResult`/`AnswerFeedback` unions need ordering variant.** | 2026-06-24 | 3 | 2026-06-25 | RULE CANDIDATE (3) — DIALOG_FILL-CLONE NEW-Q-TYPE checklist: when a plan adds a non-MC q-type by "cloning dialog_fill", it MUST widen ALL of: (a) `buildAnswerKeyMap` collapser + `AnswerKeyEntry` union [non-DF types silently collapse to short_answer→last-row-wins]; (b) BOTH localStorage validators AND server `draft-schema.ts` Zod (refine one-of-N + feedback discriminatedUnion THROWS on new discriminant); (c) `fanOutAnswer` dispatch branch before MC-default + the one-of-N invariant in lockstep across all 3 validators; (d) `buildX` sets binary `isCorrect=correct===total` ∵ row-renderer is single-boolean; (e) BOTH `question_type` literals in load-session-questions.ts (L14+L27). Plus row-paginate-then-group dup-key; field-repurpose units drift; dead-RPC sibling score% (latent); stale-test-name; single-mock-2nd-RPC; admin-feed MC-only #991; pagination ORDER BY col must exist on queried table |
| Plans gate a NEW directly-callable SECURITY DEFINER RPC/helper "internal-only" by merely OMITTING `GRANT EXECUTE TO authenticated` — FALSE PREMISE. Postgres `CREATE FUNCTION` grants EXECUTE to **PUBLIC by default**; `authenticated` inherits via PUBLIC, so an un-granted fn is still callable via PostgREST RPC. To make it internal-only you MUST `REVOKE EXECUTE ... FROM PUBLIC`. VERIFIED vs tree (vfr-rt Phase 2): `grep REVOKE.*EXECUTE.*FROM PUBLIC supabase/migrations/`=**ZERO** — no REVOKE-from-PUBLIC precedent exists. Every directly-callable SECURITY DEFINER RPC is EXPLICITLY `GRANT ... TO authenticated` (check_quiz_answer L127, batch_submit_quiz L306, submit_vfr_rt L300, even pure helper normalize_answer L48 — which is SECURITY INVOKER so harmless). Only un-granted SECURITY DEFINER fns in repo are TRIGGER fns (`sanitize_question_options`, `stamp_last_active` — RETURNS TRIGGER, not RPC-callable) or CREATE-OR-REPLACE inheriting prior grant. CRITICAL when the helper takes a `p_student_id`/`p_session_id` PARAM and writes immutable answer tables (quiz_session_answers/student_responses) with auth/owner/mode guards left ONLY in the dispatcher → cross-user write/forge primitive. FIX (preferred): inline per-type grading in the dispatcher (matches submit_vfr_rt's single-fn CASE-on-type L160-207, zero new surface) OR `REVOKE EXECUTE FROM PUBLIC`+re-derive auth.uid()/ownership in-helper. The §11 sibling-guard rule assumes guards run; a PUBLIC-callable un-guarded helper defeats it. First seen: vfr-rt Phase 2 (3 grade_record_* helpers). | 2026-06-21 | 1 | 2026-06-21 | WATCHING (CRITICAL when param-driven write to immutable tables) |
| Plans for a NEW key-REVEALING practice grader RPC (returns canonical/explanation post-answer) under-enumerate the §15 ordering + per-type correctness guards that double as answer-leak guards: membership-in-frozen-`config.question_ids` RAISE MUST precede the keyless `questions` read (check_quiz_answer L88-91 before L101-107) since the read pulls answer-key cols (canonical_answer/accepted_synonyms/blanks_config); the `answer_type_mismatch` (submit_vfr_rt L175/L185), `invalid_blank_index` (`^\d{1,4}$`+blanks_config membership L188-198), and missing-key RAISE (check_quiz_answer L109) guards must be explicitly listed or an unguarded path leaks the wrong-type canonical. check_quiz_answer does NOT org-scope its questions read — OK ONLY because membership checked first + ID set server-trusted; a new revealer must keep that exact invariant + add itself to `docs/security.md §15` example list. First seen: vfr-rt Phase 2 (`check_non_mc_answer`). | 2026-06-21 | 1 | 2026-06-21 | WATCHING |
| ELP oral-exam (async-grade) plans — 2 durable patterns. (1) **Service-role-webhook grade forgery via a Server-Action invoke helper:** the app-layer trigger that POSTs `{record:{id,audio_path}}`+`ELP_WEBHOOK_SECRET` to the grading Edge Function (which trusts payload `audio_path`, writes to `p_response_id=record.id` via REVOKE-gated grader) must be a PLAIN server module (NO `'use server'`), reachable ONLY through the ownership-validated submit action which forwards the SERVER-BUILT path (re-validated by the RPC's `invalid_audio_path` seg[2]=auth.uid/seg[3]=session guard, mig 151:163-166) + the RPC-returned responseId. A `'use server'` slip re-exposes audio-substitution self-cheat + cross-student overwrite. VERIFIED closed in revised Slice-1 plan (helper=`lib/elp/trigger-scoring.ts` plain; grader REVOKE+`auth.uid() IS NOT NULL→forbidden` mig 152:54-56,184-188 preserved). (2) **Poll-vs-entry session-scope split:** a status poll on `getOralExamSession(id)` must scope `deleted_at IS NULL` ONLY (grader sets `ended_at` on finalize mig 152:172 → an ended_at-filter hides the `graded` transition, poll spins forever) — CORRECT in plan; BUT the ENTRY-page Resume/Start gating needs the OPPOSITE (`ended_at IS NULL` = active only) or a graded/ended session shows "Resume" for a finished exam. These are TWO distinct `.from('oral_exam_sessions')` sites w/ opposite filters → the entry lookup is a SECOND new query site needing its own §7-HARD integration test (plan budgeted only one → ISSUE). Aux: getOralExamSession "throw" posture diverges from sibling `oral-exam-report.ts:83` null-return (poll wrapper must catch); no `@tanstack/react-query` dep so setInterval-poll useEffect (interval-lifecycle only, server-action fetch) is defensible §6 but a header comment ≠ mechanical code-reviewer suppression (SUGG). Slice-1 verdict: REVISE (0 CRIT [forgery confirmed closed], 1 ISSUE=entry-scope+§7 test, 2 SUGG). First seen: ELP §1 Interview Slice-1. | 2026-07-02 | 1 | 2026-07-02 | WATCHING — webhook-invoke-not-a-Server-Action + poll(deleted_at)/entry(ended_at) scope split |
| Mechanical static-analysis guard plans (e.g. #925 Phase 3 soft-delete-column guard, `scripts/check-soft-delete-guard.mjs`) under-specify SCAN SCOPE + leave splitter terminators redundant. Default glob `apps/web/**/*.{ts,tsx}` PULLS IN `apps/web/scripts/` (8 seed/import scripts: seed-eval/seed-e2e/import-questions/seed-vfr-rt-training-eval) which densely access forbidden tables AND carry many `.is('deleted_at')` — exclusion list (`*.test`, `__integration__`, `e2e/`, `integration-support/`, node_modules, .next) OMITS `scripts/`. They start green only by the splitter's `.from(`-to-`.from(` adjacency luck (e.g. seed-eval.ts:248-251 `question_banks…deleted_at` separated from easa_subjects/topics L258-302 by intervening `.from(`), NOT by exclusion → a future seed-script edit false-positives on throwaway service-role code where `.is('deleted_at')` is meaningless. FIX: add `scripts/` to exclusions (≤2 LOC). VERIFIED EMPIRICALLY: reproduced the plan's exact splitter (re.split before each `.from(`/`.rpc(` + `;`, comment-strip) over full tree → 0 violations (green-start claim HOLDS incl exam-config L16-23 false-positive shape + the harder GDPR shape `collect-user-data-queries.ts` where forbidden student_responses/audit_events/quiz_session_answers chains sit one line below soft-deletable quiz_sessions/question_comments `.is('deleted_at')` chains — `.from(`-terminator separates correctly). Codebase is SEMICOLON-FREE (ASI) → the `;` terminator never fires; the `.from(`/`.rpc(` terminator does 100% of work; the "newline-joined boundary" terminator is vague + the only over-split (false-negative) risk → drop it, minimal-correct rule = "chain runs from one `.from(`/`.rpc(` to the next". Literal-quote-only matcher: `.from(\`backtick\`)` + `.from(variable)` evade (confirmed 0 such forbidden accesses today; seed-vfr-rt-training-eval.ts:215 `db.from(table)` = no forbidden table) — accepted FN per R1, name it. Test gap: plan's FP case (line 35) is exam-config (soft-deletable AFTER forbidden); the harder REVERSE shape (soft-deletable `.is('deleted_at')` chain immediately ABOVE forbidden `.from`, no `;`) is the real GDPR risk — add that test. Verdict APPROVED (green-start validated; 1 ISSUE scripts/-exclusion non-blocking ≤2 LOC; 3 SUGGESTIONs). ROUND-2 (revised PHASE3-PLAN.md) stability check: scripts/ ISSUE RESOLVED (plan now excludes `scripts/`+`apps/web/scripts/`); guard relocated root-`scripts/`→`.claude/hooks/` (tracked+biome-linted; existing `guard-bash.js`/`review-gate.js` there pass biome clean=path viable). EMPIRICALLY RE-VERIFIED vs tree: (1) biome 2.5.0 `noRestrictedImports.options.paths` is EXACT-MATCH — probed: `@repo/db/test-helpers` flagged, `@repo/db/server` AND prefix `@repo/db/test-helpers-extra` NOT → repo's ~30 prod `@repo/db/{server,client,types,schema}` imports safe; (2) proposed override `**/lib/integration-support/**` empirically exempts `harness.ts` (the lone importer, NOT a `.test.ts`) while a prod file with same import IS flagged — deep-merge override confirmed; (3) green-start re-confirmed on profile.ts (organizations `.is` L70 vs student_responses L110, `.from`-separated) + collect-user-data-queries.ts (4 soft-delete `.is` all in quiz_sessions/question_comments chains, forbidden student_responses/audit_events/quiz_session_answers carry none). Residual SUGGESTIONs only: plan prose undercounts FP-candidate file surface (~8 files contain both tokens, not "3 hits"); `node --test` is a NEW CI runner (precedent is bash) — exercise once; consider 1-line lefthook-comment doc note (Phase-4 doc deferral otherwise sound). VERDICT round-2: APPROVED, 0 CRITICAL/ISSUE. First seen: #925 Phase 3 mechanical-guards plan. | 2026-06-20 | 1 | 2026-06-20 | RESOLVED-WATCH (round-2 clean; scripts/-ISSUE fixed, biome+override empirically verified) |
| New test-tier plans (e.g. app-layer integration `*.integration.test.ts` in `apps/web`) under-specify TWO ingestion vectors: (1) the EXISTING unit `vitest.config.ts` `include: ['**/*.test.{ts,tsx}']` matches `*.integration.test.ts` (the `.integration.` infix doesn't exempt it) → unit run picks them up + fails (no DB); (2) `apps/web/tsconfig.json` `include: ['**/*.ts']` makes the pre-commit `pnpm check-types` type-check them against the APP tsconfig — there's no `tsconfig.integration.json`/`check-types:integration` like `@repo/db` has. A bare "exclude from unit config" line addresses neither fully. Plans must add precise `exclude: ['**/*.integration.test.ts']` AND a separate integration tsconfig+script, verified locally. RESOLVED for #925 round-2: revised plan added (a) unit-vitest exclude, (b) base-tsconfig exclude, (c) `tsconfig.integration.json`+`check-types:integration`, (d) `test:integration` — all closed; `pnpm check-types`=`turbo run check-types`→app `tsc --noEmit` (no `-p`, base tsconfig), integration type-check is CI-only (matches @repo/db). Import-safety verified: `setup.ts`/`seed.ts`/`cleanup.ts`/`vfr-rt-helpers.ts` import only `@supabase/supabase-js` (NO vitest) — a `@repo/db` `./test-helpers` re-export is vitest-free. `@supabase/ssr` resolves via pnpm-symlinked `packages/db/node_modules` (NOT app/root); spike proved it works WITHOUT an explicit alias. `next/headers` (via `@repo/db/server`) is the ONLY load-bearing mock in the read-path graph; `next/cache`/`next/navigation` mocks are defensive. Helper inventory present: `getAuthenticatedClient`/`createTestUser`/`createTestOrg`/`getAdminClient` (setup.ts), `seedReferenceData`/`seedQuestions` (seed.ts), `cleanupTestData`/`cleanupReferenceData` (cleanup.ts). NOTE: `getRtSubjectData` hard-codes `code='RT'` — its OWN test must coexist with the eval RT seed (the spike's afterEach collision); unique-suffix discipline applies to NEW seeded subjects, not this fixed lookup. First seen: #925 app-integration-tier plan. | 2026-06-20 | 1 | 2026-06-20 | RESOLVED-WATCH (round-2 fixes verified vs files) |
| Integration-test "negative"/exclusion assertions are VACUOUS when the row is hidden by RLS itself, not by the helper's filter. The student-facing read runs under real RLS, so any condition the RLS `USING` clause ALSO enforces (e.g. `exam_configs.student_select` USING `enabled=true AND deleted_at IS NULL`) makes the helper's own `.eq('enabled',true)` filter untestable from the student side — the disabled/deleted row is invisible regardless of helper correctness, so the test passes even if the helper drops the filter. Plan-critic must, for each negative assertion in an under-RLS integration test, check whether the excluded state is RLS-hidden; if so the assertion proves RLS not the helper → flag CRITICAL, recommend removing or reading via admin client (which changes what's under test). Distinct from code-style §7 vacuity (empty-collection): this is RLS-shadowing of the helper filter. First seen: #925 Phase 1 exam-subjects `enabled:false` negative (`supabase/migrations/20260411000001_exam_configs.sql:99-105`). Related: mastery RPC denominator is org-wide-not-session-scoped, so progress `totalQuestions:N` needs an explicit "exactly N active Qs in org under subject" invariant + the second-student RLS-isolation must reuse the SAME question pool (else denominator drifts) AND pin B's correct-count so `not 5` is non-vacuous. | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| App-tier mutation-action integration plans under-cover the ACTION CONTRACT (the tier's only zero-overlap-with-DB job): (a) success cases assert SOME but not ALL return fields — esp. omit the null-vs-string branches of nullable pass-through cols (`explanationText`/`explanationImageUrl`), so seed ≥2 fixtures (one with explanation, one without); (b) NO true start→submit→complete lifecycle test asserting final score reflects answers (code-style §7 lifecycle rule) — per-action isolation is necessary-not-sufficient; (c) optional coalesced fields (`passed:??null`/`expired:??false` in batch-submit.ts:73-74) listed in shape but not pinned — assert `passed===null,expired===false` on quiz-mode to prove the `??` wiring; (d) multi-token `String.includes` error maps tested with only 1 token — start-exam.ts:51-65 maps 3 (`already in progress`/`no exam configuration`/`not enough active questions`); start-internal-exam `mapRpcError` iterates 10-entry START_INTERNAL_EXAM_ERROR_MESSAGES but plans exercise only `code_not_found` — several siblings (`code_voided`,`code_expired`,`code_already_used`) raise WITHOUT full success seeding, so the map is cheaply more-than-single-entry testable. Rule: for each mutation action, enumerate EVERY `return {success:false}` branch + EVERY mapped token + EVERY success field (incl. null variants) before judging coverage complete; exact-string assertions (the friendly strings ARE the contract). CAVEAT on (a): `seedQuestions` (`packages/db/src/__integration__/seed.ts:108-128`) HARDCODES `explanation_text:\`Explanation for question N\`` for EVERY question and NEVER sets `explanation_image_url` (always NULL) — there is NO param to vary either. So "seed ≥2 variants (one with explanation, one without)" is INFEASIBLE via seedQuestions: every seeded Q has non-null text + null image, making the null-vs-string assertion VACUOUS (both fixtures identical). The plan MUST specify an explicit admin `UPDATE questions SET explanation_text=NULL` / `SET explanation_image_url='https://…'` on specific seeded ids — precedent `rpc-batch-submit-quiz.integration.test.ts:87-94` (seedQuestions leaves image NULL → admin UPDATE to populate) and `rpc-vfr-rt-results.integration.test.ts:127` (per-index null/populated). Flag as ISSUE whenever a plan claims explanation variants "seeded" without naming the admin-UPDATE step. First seen: #925 Phase 2 mutation-lifecycle tier; re-flagged round-2 (revised plan adopted the under-spec'd "seed ≥2 variants" verbatim, still infeasible). ALSO (round-1 #925 P2, distinct mechanisms): (e) the SUCCESS path of `startExamSession`/`startInternalExam` is UNREACHABLE with Phase-1 fixtures — `start_exam_session`/`start_internal_exam_session` (latest def `20260606000003`) require an `exam_configs` row (org-scoped, mode internal/mock, `enabled=true`, `deleted_at IS NULL` → else `exam_config_required`) + `exam_config_distributions` + a sufficient active question pool + (internal) a non-consumed/non-voided/non-expired `internal_exam_codes` row matching the student. Harness (`test-helpers.ts:12-19`) seeds ONLY org/user/subject/topic/questions/quick-quiz-sessions — NO exam-config/code seeder. "Bogus code" reaches only `code_not_found`. Plans must DECIDE per file: negative-only (admin-insert code + cleanup) vs a new ≥30-LOC `seedExamConfig`/`seedInternalExamCode` fixture (enumerate every NOT NULL col on exam_configs+internal_exam_codes+distribution + cleanup FK order); precedent admin-insert shape `rpc-record-internal-exam-code-emailed.integration.test.ts:64-86` (code `EML${suffix}${idx}`, never fixed). (f) FILE-NAME COLLISION: quiz/actions has TWO `start*.ts` sources (`start.ts`=startQuizSession, `start-exam.ts`=startExamSession), each with its own `*.test.ts`; §7 one-file-per-source means integration tests mirror exactly (`start.integration.test.ts` vs `start-exam.integration.test.ts`) — a plan naming both off "start" collides. Pin the exact source→test-file map. | 2026-06-20 | 2 | 2026-06-20 | WATCHING |
| Integration fixture flat admin INSERTs omit NOT NULL / UNIQUE columns the schema requires (vs going through the RPC that fills them). #925 Phase 1 exam-subjects flat insert into `exam_configs` omitted `organization_id` (NOT NULL) and ignored `UNIQUE(organization_id,subject_id)`; the student read also requires `organization_id = caller's org` for visibility. Plans specifying a "flat admin insert" must enumerate every NOT NULL column + unique-key tuple from the latest CREATE TABLE. `exam_config_distributions` (`packages/db/migrations/038_exam_configs.sql:26-33`): NOT NULL `exam_config_id,topic_id,question_count(>0)`, nullable `subtopic_id`, `UNIQUE(exam_config_id,topic_id,subtopic_id)`; for start_exam success seeding the per-dist `question_count` must SUM to `exam_configs.total_questions` AND that many `status='active'` questions must exist per (topic[,subtopic]) or the RPC raises. (mirror `supabase/migrations/20260411000001`.) | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| Rule/docs-promotion plans (#925 Phase 4) hard-code learner COUNTS + offender INVENTORIES into the rule prose to be inserted, but the figures drift from the LIVE learner tracker between plan-draft and execution. VERIFIED #925 P4: (1) §5 cast-in-tests cited "count=3 (#818+#845+03cb2fa7)" but MEMORY.md:59 reads **count=4** (4th = PR #930 `665c0bdf` `sessionId as string` in fixtures.ts, archive:395) → undercount, promotion still valid (4≥2), fix the number+commit-set; (2) §5 offender list cited "~48 across **6** files" but `grep -rln 'as unknown as' packages/db/src/__integration__/rpc-*.integration.test.ts`=**8** files (the named 6 + `rpc-vfr-rt-constraint-regression` + `rpc-start-session`; 51 raw matches) → deferred-issue offender list built off the plan's 6-enum silently lingers 2 files (the exact agent-learner § Sweep-On-Promotion failure mode) → regenerate file:line list FRESH at exec, don't trust plan parenthetical. CONTRAST — §7(d) non-vacuous count=2 RULE CANDIDATE→§7 promotion is VALID (main-tracker MEMORY.md:66 shows count=2 RULE CANDIDATE→PROPOSED PROMOTION), BUT the plan's COMMIT CITATION for it is wrong. RECONCILED 2026-06-21 (2nd review round, this PHASE4-PLAN.md): the plan cites §7(d) as "count=2 — `21d86dc5` + `ba6a378e`"; the binding source (tracker-archive.md:417) credits `21d86dc5` + fix `4e462cf0` — TWO SUB-MECHANISMS in the SAME #925 Phase-1 cycle (A=RLS-shadows-helper, B=one-sided isolation), NOT two independent commits. `ba6a378e` IS a real commit ("cover session-dependent read queries") but is a DIFFERENT Phase-1 test commit and appears NOWHERE in learner memory as this pattern's source. NOTE: a PRIOR round's tracker note here mis-asserted "`ba6a378e` is the legit count=2 promotion commit" — that was itself a stale-citation error (the failure mode validating itself); corrected now. So even my own contrast-claim drifted → ALWAYS `git log -1 <hash>` + grep `.claude/agent-memory/learner/` for the hash before trusting ANY commit attribution, mine or the plan's. Anchors/table-names/issue-refs(#926/#933 OPEN)/Decision-46(45 latest)/Biome v2.4.16→2.5.0(pkg `^2.5.0`)/§7 0-offender sweep ALL re-verified accurate this round. §5(b) count=4 accurate but commit-set cites `665c0bdf` where curation log records the squash `f4c76c83`/PR#930. RULE: for every learner-count or commit/offender citation the plan bakes into inserted prose, re-derive from the LIVE main tracker AND `git log`-verify each hash at review time — never trust the plan's self-cited figure OR a prior tracker note. Verdicts both rounds: APPROVED (0 CRIT; round-2 = 1 ISSUE §7(d) `ba6a378e` miscitation non-blocking + 4 SUGG incl. §5(b) commit-set, plan.md head-`---`-line-6 vs pre-PhaseA-line-40 omits #864/#837 sections, "integration test" §7 terminology overload vs line-631 jsdom usage, steering /automerge flag-wording). | 2026-06-21 | 2 | 2026-06-21 | RULE CANDIDATE (2) — re-derive every baked-in learner count + git-verify every cited hash at exec |
| #925-family plans cite a migration as the "latest" RPC body but the CREATE OR REPLACE chain has moved — the body governs the assertable error tokens/answer-key source. #925 Phase 2 cited mig 042 for `start_exam_session`; LATEST is **mig 088** (unique_violation→'already in progress' EXCEPTION wrap, org-scoped overdue auto-complete, role cache). Functionally equiv for the 3 planned tokens (all present in 088) so the plan's asserted tokens are still REACHABLE — citation-hygiene only, not a blocking error. SEPARATELY the MC answer key moved to `questions.correct_option_id` (mig 111) and every grader was re-pointed: `batch_submit_quiz`=**112b** reads `q.correct_option_id` (still validates `selected_option ∈ options[]`), `submit_quiz_answer`=**112** ('session not found' RAISE for cross-user, line 72), `check_quiz_answer`=**117**, `complete_quiz_session`=**104**, `start_internal_exam_session`=**087** (`code_not_found` reachable). `seedQuestions` sets `correct_option_id:'b'`+options a/b/c/d ⇒ 'b'=correct/'a'=wrong success seeding valid vs the LATEST graders; the mig-042 options-JSONB-scan body is superseded. Rule: trace the chain, cite the latest, reason answer-correctness vs the latest grader. | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| Tooling/guard-script plans place new committed scripts under root `/scripts/` — but `.gitignore:48 /scripts/` (root-anchored) makes the ENTIRE dir untracked ("Throwaway operational scripts" comment; explicitly does NOT cover tracked `apps/web/scripts/`). `git add scripts/foo.mjs` silently no-ops → script never commits → CI/lefthook `node scripts/foo.mjs` crashes with module-not-found on clean checkout. Plans adding committed tooling must `git check-ignore` the target path FIRST; recommend `.claude/hooks/` (already tracked, houses `run-security-auditor.sh`+`.test.sh` — also the location-pattern precedent). RELATED: plans cite LOCAL `node --version` (v24) to justify `node:test`/`node --test`, but CI pins `NODE_VERSION:'22'` (ci.yml:17, used by setup-node in lint job) — always verify the CI runtime, not local; state the floor (Node 20+ for node:test). biome IGNORES `/scripts/` via `useIgnoreFile:true`+gitignore, so a guard there is also unlinted — another reason to relocate to a tracked+linted path. First seen: #925 Phase 3 mechanical-guards plan (`scripts/check-soft-delete-guard.mjs`). | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| biome `overrides` DEEP-MERGE rule objects (later override wins per-rule); they do NOT replace the whole `linter.rules`. A new override setting only `style.noRestrictedImports:off` for exempt globs CANNOT re-enable `noExplicitAny`/`useConst` in those files — proof: existing test-override (biome.json:57-71) sets only 2 rules off yet root rules still apply to tests. So plan-worry "new override might re-enable other rules" is UNFOUNDED — a narrow per-glob OFF override is sufficient and safe. Separately verify the rule's GROUP name (`noRestrictedImports` group moved nursery→style across versions) against the pinned biome binary (2.5.0) — wrong group → config-load error breaks all of `pnpm lint`; smoke test gates this. First seen: #925 Phase 3 (test-helpers import guard). | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| Dual-client pattern for app-layer integration tests (#925): the code-under-test (Server Action) reads the **cookie-jar** session seated by `signInAs(email,pw)` via the mocked `next/headers`; the FIXTURES seed via a **separate** `studentClient` from `getAuthenticatedClient()`. Both must point at the SAME user; a cross-user test seeds as A's studentClient then `signInAs(B)` before invoking the action (B's submit on A's session → RPC RAISE → action 'Failed to submit answer'). A new `seedOpenSession()` is feasible exactly like `seedCompletedSession` (drive `start_quiz_session` via studentClient, return {sessionId,questionIds}); subject/topic non-null ⇒ questionIds MUST be under that subject+topic or RPC raises invalid_question_ids (fixtures.ts CONTRACT). Plans must NOT assume action+fixture share one client. Confirmed by `quiz-report.integration.test.ts` (studentAClient seeds, signInAs(emailA) runs the helper). | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| Rules-change proposals that claim to "keep existing caps" while actually replacing the numeric values (e.g. plan-critic 1-round cap → 4-round ceiling, impl-critic 2-round cap → 4). The proposal text says "KEEPS that ceiling" but changes the numbers. Plan-critic must diff proposed numeric values against the binding text in agent-critic.md and agent-workflow.md. Deeper issue: for impl-critic the artifact mutates every fix round — "consecutive clean" on a moving target is incoherent; floor logic only works on stable artifacts (plan-critic). Protocol proposals that apply the same floor to both gates conflate two different situations. First seen: multi-round-critic-protocol DRAFT (agent-critic.md:17,18,28; agent-workflow.md:105,221). | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| Process-change proposals that introduce multi-round review loops risk learner double-counting: within-gate re-finding of the same issue across rounds of the same gate on the same plan/diff should count as ONE learner occurrence, not N. The count≥2 promotion threshold (agent-learner.md line 13) requires 2 different commits/sessions; repeated round findings on the same plan inflate counts artificially. Any protocol adding review rounds must explicitly restrict learner Count increments to cross-session, cross-diff occurrences. First seen: multi-round-critic-protocol DRAFT open question #5. | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| Plans that generalize an MC-only pipeline to new question types omit the `DraftAnswer` / `AnswerFeedback` type widening AND fail to list `loadSessionQuestions` (the RPC caller) in "Files to change". `DraftAnswer` is `{selectedOptionId,responseTimeMs}` only; `loadSessionQuestions` hard-codes `QuizQuestionRow` to MC columns and silently discards any new RPC output columns. Also: answer recording is batch (`batch_submit_quiz` at session end), not per-question — plans that say "extend the per-question Server Action" for recording are wrong. Round 1 correctly fixed in vfr-rt-training spec (Phase 2.3 lists batch_submit_quiz + DraftAnswer). But: the LATEST `batch_submit_quiz` body (mig 112b/20260619000250) has a HARD `selected_option IS NULL OR ''` RAISE on every answer + an MC option-membership validation loop — so even widening `DraftAnswer` won't help until the migration is extended to branch on question_type. The spec's Phase 2.3 wording "extend batch_submit_quiz to accept and persist response_text/blank_index rows" is correct, but the implementer must know the HARD non-null guard on line 175 must be made conditional, not just additive. Phase 5/6 adding ordering/diagram_label both hit the same guard — spec should call it out explicitly per type. | 2026-06-20 | 3 | 2026-06-20 | WATCHING |
| New-type answer storage shape ambiguity: when a new question type requires per-item recording (e.g. `diagram_label` per-zone correctness), the spec must decide BEFORE implementation whether to use (A) single JSON row in response_text (blank_index=NULL) or (B) multiple rows per item (blank_index=zone_index, like dialog_fill). Choice (B) inflates the quiz_session_answers count used for report pagination (count of rows ≠ count of questions), breaking PAGE_SIZE logic. First seen: vfr-rt-training spec Phase 6 `diagram_label`. Plans must specify the storage shape explicitly, not leave it to the implementer. **CONFIRMED 2026-06-21 in the SCORE path too (vfr-rt Phase 2 batch_submit_quiz dispatcher plan):** `batch_submit_quiz` (mig 20260619000250 L239-250) computes `v_answered`/`v_correct_count` via raw `count(*)`/`count(*) FILTER(is_correct)` over quiz_session_answers, then `v_score = v_correct_count / v_answered` (practice) or `/ v_total` (exam). Per-blank dialog_fill rows (choice B) → N rows per question → `count(*)` ≠ question count → score_percentage + answered_count both wrong; passes `db reset` + every MC/short-only test, breaks only on a multi-blank dialog_fill practice session. The CORRECT precedent is `submit_vfr_rt_exam_answers` (mig 20260619000300 L225+) which deliberately uses a `session_questions` CTE for "per-part scores from persisted rows" precisely because raw count() can't be used with per-blank rows. RULE EXTENDED: any plan adding a per-blank/per-item type to an EXISTING grader must also specify the SCORE-aggregation change (DISTINCT question_id, or CTE) + define per-question correctness for partial dialog_fill, AND add a multi-blank-session score integration test — not just the storage shape. Also re-confirmed two sibling batch-grader mechanics for the same plan: (a) non-MC INSERTs must pin `ON CONFLICT (session_id, question_id, blank_index) DO NOTHING` on BOTH quiz_session_answers AND student_responses (the old 2-col UNIQUE was DROPPED in mig 095/20260610000300 → 42P10 if copy-pasting it); (b) the dispatcher's `_batch_questions` TEMP TABLE (L155-164) projects ONLY MC cols → can't supply a `questions`-rowtype/question_type to per-type helpers; plan must widen the projection or switch to a full-row `SELECT q INTO`. Regex strip `\{\{(\d+)\|[^}]*\}\}`→`{{\1}}` is execution-PROVEN in mig 105 (no 2201B; `[^}]` is normal ARE class not POSIX bracket shorthand) — "copy verbatim" is correct de-risking. DROP-not-CREATE-OR-REPLACE for RETURNS TABLE col-add confirmed (mig 059 header + mig 105). | 2026-06-20 | 2 | 2026-06-21 | RULE CANDIDATE (2) — per-blank/per-item type added to existing grader must specify score-aggregation change + multi-item score test, not just storage shape |
| Report query SELECT string not updated alongside AnswerRow type widening: plans that add response_text/blank_index to the TypeScript AnswerRow type but don't update the actual `.select('question_id, selected_option_id, ...')` SQL string in quiz-report-questions.ts leave response_text always undefined at runtime (TypeScript sees the type, runtime gets undefined). First seen: vfr-rt-training spec task 4.2 — names `AnswerRow +=` but doesn't name the line-65 SELECT string change. Plans must name the exact SELECT columns to add, not just the type. | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| RETURNS TABLE extension requires DROP+CREATE, not CREATE OR REPLACE. Confirmed in `20260327000059` comment: "Must DROP first because CREATE OR REPLACE cannot change return type." Plans that say "CREATE OR REPLACE" to add columns to a RETURNS TABLE will fail at migration apply time with Postgres error. First seen: vfr-rt-training spec G1. | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| Session-storage validator whitelist not widened when adding new exam mode to `exam-modes.ts`. `quiz-session-validators.ts` line 48 hard-checks `v === 'mock_exam' \|\| v === 'internal_exam'` for `examMode`. Adding `vfr_rt_exam` to EXAM_MODES/QuizMode without also updating this validator means sessions with `examMode:'vfr_rt_exam'` fail the storage round-trip and lose exam state on tab refresh. Plans that change exam-modes.ts must include quiz-session-validators.ts in "Files affected". First seen: #697 Phase B plan. | 2026-06-19 | 1 | 2026-06-19 | WATCHING |
| Rebase/re-timestamp plans for security PRs with inline `mig NNN` cross-refs inside migration SQL comments: the plan's "targeted edits" scope must enumerate the SQL-file-level comments too (e.g. "Migration 115: … mig 110 … batch_submit_quiz 110b, report RPCs 112/113"), not just docs/ and .claude/rules/. And the `Depends on YYYYMMDD*` timestamp refs inside migration bodies also need re-stamping when files are re-timestamped. Plans that list only docs/ cross-refs miss this. First seen: #856 rebase plan. | 2026-06-19 | 1 | 2026-06-19 | WATCHING |
| Plans that re-number migrations +2 AND add a new spec (count N→N+1) must update spec-count literals in THREE places in tech.md (lines 85, 146, 183) AND in decisions.md — but the PR branch's tech.md may already be stale vs master (branch shows 41, master shows 44). The plan must specify which starting value to use (master's current, not branch's stale). #856 rebase plan: plan says 44→45 but PR856 branch tech.md says 41 (stale from branch cut) while master says 44. | 2026-06-19 | 1 | 2026-06-19 | WATCHING |
| Multi-query test mocks: single `mockFrom`/`mockRpc` keyed by name can't distinguish N sequential calls to the SAME endpoint with different results (e.g. page-fetch then probe). Plan must scope call-order dispatch (e.g. `mockRpc.mockResolvedValueOnce(…).mockResolvedValueOnce(…)`) as in-scope; and must name which specific existing test cases will BREAK (not just "verify/keep"). | 2026-04-11 | 6 | 2026-05-31 | WATCHING |
| UI-sweep plans that add text-swap ("-ing" variants) to raw `<button>` elements must enumerate all test files that query those buttons by accessible name (getByRole/name). If the button's accessible name changes under loading (e.g. "Submit Answer" → "Submitting…"), every test asserting the pre-loading name with the element in its loading state will fail. Plans must list these tests under "Files affected" and specify updated assertions. First seen: #533 quiz-controls.test.tsx line 210 (`/submit answer/i` when submitting=true). | 2026-06-14 | 1 | 2026-06-14 | WATCHING |
| UI-sweep plans that propose spinner/text to synchronous navigation buttons (sessionStorage + router.push, no async gap) will produce a visual flash that disappears instantly on navigation. These buttons have no async work to show loading for. Resume buttons in draft-card.tsx and resume-draft-banner.tsx are synchronous — no loading state exists or should be added. Plans must distinguish sync-nav vs async-action buttons before scoping spinner additions. First seen: #533. | 2026-06-14 | 1 | 2026-06-14 | WATCHING |
| Multi-query test mocks: single `mockFrom` keyed by table name can't distinguish N parallel `.from()` calls, two sequential calls to the SAME table with different filters, or count-head vs range-data calls. Plan must scope `mockImplementation`/`mockFromSequence`/call-order dispatch as in-scope. dashboard.ts getTotalAnswered+getQuestionsToday are BOTH `student_responses` count-head reads in Promise.all — any error test in dashboard.test.ts that sets `student_responses` to `{ count: null, error }` necessarily errors BOTH, not just the target. Plan's proposed mitigation (assert on prefix match `/Failed to fetch/`) is correct — flag this only as SUGGESTION to change assertion rationale in plan. | 2026-04-11 | 6 | 2026-06-01 | WATCHING |
| Test-update scope underspecification: plans say "test update" for a file but don't name the specific assertion that breaks, the fixture `PROPS` object that needs a new required prop, or the new test cases needed for new UI. Implementer over/under-mocks. Recurs: query→RPC refactors (count 7); feature adding a required prop to a shared component (internal-exam-code-email: `issue-code-form.test.tsx:105`, `issued-code-panel.test.tsx` PROPS, new button tests). | 2026-04-11 | 8 | 2026-06-18 | WATCHING |
| SECURITY DEFINER RPC plans omit project SQL conventions: `is_admin()` over inline role lookup; `AND deleted_at IS NULL` on user/session lookups (§9); cached `v_admin_role` variable (mig-087 pattern) — not an inline subquery in the audit INSERT. `actor_role` in audit INSERT must come from the authz-time SELECT; inline re-query NULLs on mid-call soft-delete of admin row. | 2026-04-11 | 5 | 2026-06-18 | WATCHING |
| Sibling-file audit missed: defensive pattern (error destructure, null guard) added to one action/query file, identical pattern in sibling in same folder untouched. (CLAUDE.md sibling-audit rule.) | 2026-04-11 | 2 | 2026-05-22 | RULE CANDIDATE → CLAUDE.md sibling-audit |
| Plans referencing a constraint/mode value cite mig 001 but a later migration widened/renamed it. Must trace the FULL migration chain (grep `ALTER TABLE`, `ON CONFLICT (`, constraint names) before depending on a constraint's shape. | 2026-05-08 | 3 | 2026-05-28 | WATCHING |
| Red-team fixture INSERT into `student_responses` omits `selected_option_id` value and/or misstates column type. Actual schema: `TEXT NOT NULL CHECK(IN 'a','b','c','d')` — not uuid, not nullable. Must include a value; service-role INSERT bypasses FORCE RLS. | 2026-05-31 | 2 | 2026-05-31 | WATCHING |
| `get_student_streak` always returns exactly one `{current_streak:0,best_streak:0}` row for anon/cross-org (COALESCE scalar subqueries with no FROM). Generic "error!=null \|\| length===0" assertion pattern fails for this RPC; needs a tailored `current_streak===0 && best_streak===0` check. Requirements doc for #673 correctly specifies the {0,0} shape throughout — positive signal. | 2026-05-31 | 1 | 2026-05-31 | WATCHING |
| Cross-org non-vacuity gap: plans assert "victim org HAS a config/resource" to prove org-scoping, but omit the complementary assertion that "attacker org DOES NOT have a config/resource." Without the second assertion, if the attacker org accidentally acquired the resource (e.g. prior test run, idempotent seed), the rejection could be for the wrong reason. Both assertions are needed: (a) victim org has the resource, (b) attacker org does not. #825 DN2 (cross-org start_vfr_rt_exam_session). | 2026-06-14 | 1 | 2026-06-14 | WATCHING |
| Red-team spec scaffold plans that need different callers for different test cases (e.g. IDOR uses attacker, pre-completion gate uses owner/victim) fail to enumerate ALL required client fixtures. Plan must list each client in the "scaffold" description when more than one authenticated user is needed — mirrors rpc-complete-empty-exam.spec.ts pattern. #825 rpc-vfr-rt-results.spec.ts needs both victimClient (DB2) and attackerClient (DB3). | 2026-06-14 | 1 | 2026-06-14 | WATCHING |

## Durable plan-review checks

### SQL / migrations
- **Trace the full migration chain, not just mig 001.** `quiz_sessions.mode` CHECK widened by mig 058 (added `internal_exam`) and given the canonical name `quiz_sessions_mode_check` — use named `DROP/ADD CONSTRAINT`, not the DO-block predicate-text lookup (that pattern is only for unnamed constraints). `quiz_sessions.deleted_at` added by mig 023 (not in initial schema). Always grep all migrations for `ALTER TABLE` on the target before reasoning about its current shape.
- **CREATE OR REPLACE FUNCTION: trace to the latest definition before flagging a missing pattern.** A later migration may already add the guard/clause. (Pre-Flag Verification — also in `semantic-reviewer.md` / `implementation-critic.md`.)
- **Widening a UNIQUE constraint breaks existing `ON CONFLICT (col-list) DO NOTHING`.** Postgres needs an exact matching unique index. Grep `ON CONFLICT (` across all migrations and include a companion migration rewriting every living RPC's clause (missed for `batch_submit_quiz`/`submit_quiz_answer` on mig 084).
- **`STABLE`/`IMMUTABLE` + `EXECUTE` is a hard error.** Dynamic-SQL RPCs (`RETURN QUERY EXECUTE`) must be `VOLATILE` (or omit the keyword). Model: `get_session_reports`, not `get_admin_student_stats`.
- **New `questions` columns must satisfy existing NOT NULL columns.** `questions.options` is `JSONB NOT NULL`, no DEFAULT — new types (short_answer, dialog_fill) need nullable/DEFAULT/explicit `options:'[]'` or inserts fail silently.
- **New exam modes with answer-bearing question types need a server-side projection RPC.** `get_quiz_questions()` only strips MC correct-option markers; any new correct-answer column (canonical_answer, blanks_config canonical, accepted_synonyms) needs a `get_*_questions` RPC that strips it — never let students SELECT it via PostgREST.
- **`smart_review` passes `p_subject_id = NULL`.** `q.subject_id = NULL` is always NULL in Postgres → 0 rows. Subject-scoped WHERE/COUNT must be `(p_subject_id IS NULL OR q.subject_id = p_subject_id)`.

### Pagination / truncation (PostgREST max_rows = 1000)
- `.limit(N)` with N > 1000 is NOT a safeguard — max_rows=1000 overrides it. Treat as unbounded (e.g. `dashboard-stats.ts` `.limit(10000)`/`.limit(5000)`).
- Truncation audits partitioned by directory miss colocated `queries.ts` in admin sub-routes and the cross-cutting GDPR export layer (`lib/gdpr/collect-user-data.ts`). Scope audits by file-ownership, name the colocated files.
- `.order('id')` tiebreak fails on tables without an `id` column — `flagged_questions`/`active_flagged_questions` use composite PK `(student_id, question_id)`; use `flagged_at`. Verify each target table has the column.
- `fetchAllRows<T>` contract: `data` is always `T[]`, never null. Error-path mocks use `{ data: [], error: {...} }`, not `{ data: null }`. Joins in the `getPage` select need an explicit cast to `PromiseLike<PageResult<T>>`.

### Extraction / file-split
- Removing one function from an over-limit utility file may leave the parent still over the 200-line cap — verify post-extraction line count or scope a second split.
- Query-helper modules extracted from `'use server'` files must NOT carry `'use server'` (they take a `supabase` param; the directive turns exports into Server Actions and breaks runtime calls). Precedent: `collect-user-data-queries.ts`.
- Adding a column to a `.select()` string must also update any local row-type alias used in an `as SomeRow[]` cast (TS doesn't enforce the cast against runtime; missing field is invisible until referenced).
- Replacing one `Promise.all` arm with a helper returning an unwrapped array changes the destructuring (`{ data }` → bare binding) — state it explicitly.

### Component / UI
- Migrating a hand-rolled overlay to a controlled Dialog: removing the mount guard breaks `useState(props…)` initializers (run once at mount) → stale form on reopen. Keep the guard or add `key={entityId}` to force remount.
- Rewriting a component to a new UI lib: the co-located `.test.tsx` mocks the OLD lib by module path — the entire mock + assertions must be rewritten; list the test file under "Files to change".
- `nav-items.ts` icon name is a CLOSED union; `NavIcon` switch silently renders nothing for unknown names. New nav entries reuse an existing icon or add to BOTH the union AND the switch. `sidebar-nav.test.tsx` asserts exact named items — list it as an affected caller.

### Red-team / E2E specs
- A spec's *pass condition* may be a specific error code — hardening that changes the error string (e.g. `23502` → `invalid_question_ids`) breaks the assertion. Check per spec; prefer open `expect(error).not.toBeNull()`.
- Specs fetching question IDs with only `deleted_at IS NULL` (not `status='active'`) feed invalid IDs to new active-status validation. Add `.eq('status','active')` or document seeded-active assumption.
- NUL-byte (`\x00`) injection payloads are rejected by PostgREST (400) before the RPC runs — message won't match the RPC's `RAISE` string; needs a separate assertion branch.
- audit-completeness: `actor_id` differs per event type — admin events (`internal_exam.code_issued/voided`) bind `v_admin_id`; student events bind `v_student_id`. Bind `expected_actor` to the right role or get 0 rows.
- Name the specific RPC per event: `exam.started`/`exam.completed` come from `start_exam_session`+`batch_submit_quiz(mode='mock_exam')`, NOT `start_internal_exam_session`.

### Validation discipline
- Never leave "needs verification" open in a validated plan — resolve before review or the implementer guesses.
- Dual-deletion-path tables (CASCADE + direct admin RLS DELETE, e.g. `exam_config_distributions`): soft-delete-matrix rows must document BOTH paths.
- `userEvent.type('150')` emits 3 change events; `.at(-1)` captures only the final clamped value — that (not "Math.max so exact") is the correct rationale for `toBe(max)`.

### Agent-tooling plans (native subagent memory)
- A Phase-0 spike "confirm the agent reads/writes MEMORY.md" is a false positive if the agent body also has an explicit Read/Write of that file — strip the body instruction first to isolate native-injection engagement.
- Reference matrices (e.g. red-team `attack-surface.md`) must not be renamed to `MEMORY.md` where the curation nudge can prune active GAP rows; keep as a topic file with a no-delete header and update operational references (insights.md) in the same commit.
- After `memory: project` is added, remove body-level "Read your memory file at start" instructions or the agent double-loads (test-writer, code-reviewer, plan-critic, implementation-critic, security-auditor all had these).

| Red-team spec for aggregation RPCs: instructor fixture missing from seed.ts (only 'student' and 'admin' roles exported). Plan that references `seedInstructor` or assumes the instructor helper exists will fail at runtime. Must add the fixture as part of the spec. | 2026-05-31 | 2 | 2026-05-31 | WATCHING — #673 design correctly adds seedRedTeamInstructor + upsertUser role widening; resolved for this PR |
| Red-team plans label specs with wrong/colliding Vector ID. BE/BF/BG/BH/BI are occupied by FIXED, unrelated vectors (draft-limit, audit-spoofing, batch_submit soft-delete, soft-deleted-user JWT, draft TOCTOU) — existing rows' Notes reference spec files by filename only, no label cross-refs, so re-lettering the spec self-labels never requires editing the existing rows. Plan must (a) verify each proposed label against attack-surface.md, (b) explicitly state which occupied rows require no edits. #326 plan correctly confirmed this. Also: spec-count in tech.md (×3) + decisions.md must be checked when registering specs; registering already-on-disk specs does NOT increment the count. | 2026-05-31 | 4 | 2026-06-09 | WATCHING |

### Return-type contract for incremental additions to query functions
When a plan adds a new code path to a function that returns a discriminated-union result type (e.g. `{ ok:false; error:string } | { ok:true; ... }`), verify every new `return` statement satisfies ALL fields of the matching union arm. Plans consistently specify `{ ok:false }` for new error paths but omit the mandatory `error: string` field — the TypeScript build fails. Always check the full type definition before specifying return values.
| Vector-label collision sweep must include ALL spec files in apps/web/e2e/redteam/, not just the two being renamed. Labels like "BJ" appear in BOTH the start-internal-exam spec (to be renamed) AND server-action-unauthenticated.spec.ts (#603 "soft-deleted user gate" sub-describe, lines 405/407/432/434/435) as an INDEPENDENT, non-colliding usage. Plans must grep all spec files before scoping the rename. The server-action-unauthenticated.spec.ts "BJ" is a different usage (#603 soft-delete gate, NOT the start_internal_exam_session unauth vector) — it must be noted as "leave untouched" in the plan, not silently omitted. | 2026-06-09 | 1 | 2026-06-09 | WATCHING |
| Red-team spec additions that cover a new RPC code-path consistently omit adding a vector row to attack-surface.md. Plans say "add a test case to existing spec" but do not say "add/update vector row in attack-surface.md." This is a recurring pattern: new coverage is added but the authoritative gap-tracker is not updated. Must include attack-surface.md in "Files affected" for any red-team spec addition. Also: when a plan routes Vectors L/M to a new dedicated spec file instead of the attack-surface.md canonical files (server-action-unauthenticated.spec.ts / rpc-cross-tenant.spec.ts), the matrix rows must be updated to reflect the new file choice — or the rows remain showing as GAP. A10-batch confirms the pattern: plan adds error-handling-disclosure.spec.ts with no attack-surface.md in Files affected — the A10 gap row must be updated from GAP→COVERED. | 2026-06-01 | 4 | 2026-06-06 | WATCHING |

### Red-team batching plans (organization plans, not implementation)
- "One spec file = one PR" principle must be verified against the *issue bodies*, not assumed from the PR's headline. Multiple issues in a single PR may each touch a different spec file (e.g. #557 requires both a new rpc-complete-empty-exam.spec.ts AND extending server-action-unauthenticated.spec.ts for Vector AN). Check each issue body for target spec file(s).
- A batch labeled "new file only" is only parallel-safe if NONE of its constituent issues also touch shared existing files. A single issue that spans 3 existing files (e.g. #517 covers AG in server-action-unauthenticated + AH/AK in rpc-cross-tenant + AI in session-replay) makes the whole PR conflict-prone.
- Issues with overlapping scope (e.g. #545 covers AG+AH; #517 also covers AG+AH) must be noted with a deduplication callout — both issues close from a single test implementation, and the plan must say which test file owns the implementation.
- Feature issues (e.g. #379, labeled `feature`, requiring new SECURITY DEFINER RPCs and Server Action wiring) cannot be "split out" from a red-team PR without production code — they require a migration + production code change + manual eval. Grouping them in a spec-only batch without noting production code is a false "automatable" classification.
- Attack-surface.md Vector O ("quiz-report leaks raw correct boolean server-side") is labeled CRITICAL in the matrix, not "possibly-CRITICAL". Plans that understate it need correction. The defense (buildReportQuestions strips the field in TypeScript) exists but is asserted only at the unit test layer — no E2E spec yet. It IS spec-only (no production fix needed), but the severity label must match the matrix.
- #551 (batch_submit_quiz soft-deleted question SQL integration test) has an undecided home: issue says "Playwright e2e/redteam/ pattern OR new integration/ Vitest suite hitting local Supabase Docker". The redteam CI workflow does not trigger on a new Vitest integration file — the plan must choose and state which, and if Vitest, note that the redteam.yml will NOT run it. RESOLVED in this PR: plan chose Vitest integration (packages/db/src/__integration__/rpc-batch-submit-quiz.integration.test.ts) and noted CI exclusion. Correct choice.
- batch_submit_quiz `RETURNS jsonb` — result is `{ results: [...], total_questions, answered_count, ... }`. Plans that write `data.length>=1` intending to test the results array are wrong; correct is `data.results.length>=1`. Plans must spell out `data.results[N].field` for all batch_submit_quiz assertions. Also: the `p_answers` element JSON key is `selected_option` (not `selected_option_id`) — the table column is `selected_option_id` but the JSON key the RPC reads via `v_answer->>'selected_option'` is `selected_option`.

| Two-dir migration mirror drift: supabase/migrations can have a newer TIMESTAMP migration that CREATE OR REPLACEs a function without a matching NNN_ packages/db mirror. The result is the same RUNTIME body (supabase controls production) but packages/db's highest NNN file is stale. The plan must identify WHICH directory's latest is binding and reproduce THAT body verbatim. Specifically: `20260521000001_start_quiz_session_null_guard.sql` (supabase) has the NULL guard but has NO packages/db mirror — packages/db 081 is stale. The new 086 migration must base on the supabase latest, not 081. Plans must always grep both dirs and compare bodies when supabase timestamp > packages/db highest NNN. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |
| Sibling-function inline-role-subquery audit: when a plan fixes the inline `(SELECT u.role…)` pattern in one function, sibling SECURITY DEFINER functions with the SAME pattern must be enumerated. The plan's sibling-grep instruction is correct but leaves discovery to the implementer — leaving start_exam_session (054, line 141) unmentioned. This is the same "inline role subquery in audit INSERT" pattern targeted in #734. Fix for start_exam_session is ≤10 lines (add `v_role text` to DECLARE, extend the existing SELECT INTO, replace inline). Plans should name the known offender rather than delegate discovery. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |
| Column GRANT plans on users table: the `users` table has NO explicit column-level GRANT in any migration. `authenticated` holds table-wide UPDATE via Supabase's default schema grants (same mechanism that quiz_sessions used before mig 20260605000001). The REVOKE UPDATE then GRANT UPDATE (full_name) is the correct and non-vacuous shape — mirroring quiz_sessions precedent. The plan's "expected: full_name, possibly updated_at / avatar / locale" is wrong: the users CREATE TABLE has NO updated_at, NO avatar, NO locale columns. The correct re-GRANT is `full_name` only (the only column written by authenticated users via `settings/actions.ts updateDisplayName`). email / deleted_by / last_active_at are only written by service-role or SECURITY DEFINER RPCs. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |
| FK-into-global-table audit: plans that claim "X is the ONLY FK into table Y" must grep ALL migrations for `REFERENCES Y`. For easa_subjects/topics/subtopics: FKs exist in questions (NOT NULL), quiz_sessions (NULLABLE, no ON DELETE clause), and exam_configs (NOT NULL). A plan that only cites questions misses quiz_sessions and exam_configs. However, for test-hermeticity helpers, the practical impact is zero because (a) quiz_sessions.{subject_id,topic_id} are nullable so an easa deletion that left those null would only cause a FK violation on the non-null subjects/topics deleted last, and more importantly (b) cleanupTestData already deletes quiz_sessions before cleanupReferenceData runs — so the FK is satisfied by run order regardless. The plan's stated rationale is wrong but the design is still safe; flag as SUGGESTION not ISSUE. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |

| Spec-count updates that only update one line in a steering doc while siblings carry the same stale number. tech.md has THREE occurrences of "18 adversarial Playwright specs" (lines 85, 146, 183). Plans that update only line 146 leave two stale counts behind. Always grep the full steering doc for the exact number string before scoping line edits. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |

| Red-team plans that exercise `record_auth_event` assert `/not authorized/i` for the admin-only event-type gate, which matches mig 093 line 66 (`RAISE EXCEPTION 'not authorized'`). BUT the anon auth check uses lowercase `'not authenticated'` (mig 093 line 42) while `get_session_reports` uses capitalized `'Not authenticated'` (mig 091 line 48). Plans must read the exact RAISE string in the LATEST def of EACH RPC; do not assume the same casing across different RPCs. CQ assertion in server-action-unauthenticated.spec.ts must match `/not authenticated/i` (lowercase) for record_auth_event and `/not authenticated/i` (capitalized N) for get_session_reports — both match case-insensitively so the plan's assertions are technically safe, but the inconsistency is a risk if a case-sensitive match is accidentally used. | 2026-06-07 | 1 | 2026-06-07 | WATCHING |
| #781 cookie-rewrite approach for @supabase/ssr: `getUser()` (called in proxy.ts) routes through `_useSession` → `__loadSession` which reads the session from the SSR cookie storage and refreshes only when `expires_at * 1000 - Date.now() < EXPIRY_MARGIN_MS` (= 90 000 ms). The Playwright cookie-rewrite approach (set `expires_at` to a past unix ts) DOES trigger a refresh on the next `getUser()` call because `hasExpired=true` at `__loadSession:2354`. The approach is sound. The fallback (jwt_expiry lowering in config.toml) is a valid secondary option but should not be necessary. Flag: supabase/config.toml's jwt_expiry is a local-dev setting only — CI Supabase in docker uses its own config; lowering it affects the entire CI run, not just the redteam project. Plans proposing this fallback must document the CI blast radius. | 2026-06-07 | 1 | 2026-06-07 | WATCHING |

| E2E un-skip plans for AlertDialogAction with `isPending` text-flip: `AlertDialogAction` is a plain `<Button>` (not a `AlertDialogPrimitive.Close` wrapper). Clicking it fires `onClick` but does NOT dismiss the dialog — the dialog stays open while `isPending=true` (button says "Deleting…") and closes only after the Server Action resolves and the parent re-renders. `name:'Delete'` safely matches the pre-transition text only; the button is disabled during transition so a second click can't race. The dialog stays visible during the transition — plan's `toHaveCount(0)` assertion is only safe AFTER the toast is visible (which is already gated first). Plan was correct on all assumptions; positive signal. | 2026-06-09 | 1 | 2026-06-09 | WATCHING |
| Red-team spec-split plans that classify tests as "probe-only/no cleanup" without reading the test body. Confirmed in #796: rpc-cross-tenant BY tests (lines 191-252) INSERT egmont fixture rows (internal_exam_codes + quiz_sessions) and set describe-scoped cleanup vars — these are NOT probe-only. A plan that routes BY to "isolation file (no afterAll needed)" leaves the reports file's afterAll holding stale ID variables it can't reach, and the isolation file missing cleanup it needs. Before labeling any test "probe-only", read the test body for adminClient.from(...).insert/upsert calls. | 2026-06-09 | 1 | 2026-06-09 | WATCHING |
| Spec-split plans that propose attack-surface.md Spec-File column updates without checking whether the spec-label in the TEST matches the Vector ID in the MATRIX. In #796: rpc-cross-tenant.spec.ts tests are labeled "Q (#276)" and "X (#384)" but attack-surface.md Vector Q = question_comments unauth (server-action-unauthenticated.spec.ts) and Vector X = user-consents-isolation.spec.ts. The cross-tenant tests are supplementary coverage, not the primary-spec row. Updating the matrix Spec-File to the new split file would overwrite the correct primary-spec entry with a secondary one. Always grep attack-surface.md for the Vector ID AND confirm the description matches the test before adding Spec-File update instructions. | 2026-06-09 | 1 | 2026-06-09 | WATCHING |
| File-split plans with "possibly" on the key helper extraction underestimate whether the primary file drops under the target ceiling. In #796: audit-completeness.spec.ts (704L) splits off ~126L of CT tests → 578L remaining, still over 500. The plan's "possibly audit-assertions.ts" for 2 helpers saves only ~42L → 536L, still over 500. ALL 6 spec-local helpers (150L total) must move to pass. "Possibly" on a helper whose extraction is the ONLY path to the target must be hardened to "definitely". | 2026-06-09 | 1 | 2026-06-09 | WATCHING |

## Positive signals (what good plans got right)
- #326 agent-health.yml false-positive fix: plan verified all 5 edits against actual code before recommending. Attack-surface path EDIT 1 exactly correct (only topics/ copy exists). Check 5 removal scope complete (all 4 SEC_SECTION references enumerated: lines 172, 175, 211, 228). Check 3 awk fields verified against actual 6-column learner table ($2=Issue Type, $3=Count, $5=Status). sed stem `s/(ing|s)$//` confirmed safe — only "testing"→"test" changes match status; "next.js"→"next.j" is a harmless substring (still matches "Next.js" in coderabbit.yaml). One SUGGESTION: EDIT 5's `sed -E 's/(ing|s)$//'` applied to "next.js" yields "next.j" — plan should note this is a benign substring match, not a break.
- Cast-guard sweep (#677): plan correctly identified all 3 unguarded `rpc<T[]>` sites and verified 7 already-guarded siblings. Line numbers exact. Test scope correct. Only minor issue: cited `check-consent.ts` at wrong path (`lib/queries/` vs actual `lib/consent/`) and `load-session-questions.ts:50` uses `!data?.length` guard (not `Array.isArray`) — a different-but-safe pattern, not a reference-compliant sibling.
- #673 tasks.md: line numbers (seedRedTeamAdmin:107, upsertUser:339, VICTIM_EMAIL:4-6, getAdminClient:25), file paths, and ordering all correct. Design faithfulness high — no contradictions on the substantive algorithmic decisions (round-robin, single-snapshot dates, sentinel idempotency, 'a' literal, streak offsets, no-cleanup rationale). Key issue: attack-surface.md status-flip (BW1-3/BX1-7/CA/CB → COVERED + new BX7 row) and upsertUser admin-param omission in Task 1 description body (not caught by the task's _Leverage reference to line 107). Also: victim creds sourcing underspecified in Task 5 prompt (should name VICTIM_EMAIL/VICTIM_PASSWORD or seedRedTeamStudent() call explicitly).
- #789 recordAuthEvent extraction: plan correctly read all 4 action files and matched console.error prefix strings verbatim. RPC arg-shape claim (no p_metadata when absent) verified against all 4 exact-match test assertions. Vi.mock scope analysis for the sibling split was accurate (module-scope mocking applies to transitive imports). Migration 093 signature matched on all 3 params (p_event_type, p_resource_id, p_metadata DEFAULT '{}'). One ISSUE found: wrong file path for settings/actions.ts (plan says app/settings/actions.ts but actual is app/app/settings/actions.ts).
- #367 un-skip soft-delete E2E test: all six assumptions verified correctly. AlertDialogAction role=alertdialog confirmed via Base UI handle.js. Hermiticity reasoning for restoreSeededQuestionsState exact. Text truncation (90 chars) correctly cleared for 53-char marker text. Toast regex /Deleted/ correct. No red-team trigger paths touched. deleted_at IS NULL query filter in getQuestionsList confirmed. APPROVED clean with one SUGGESTION (add an explicit timeout before toHaveCount(0)).
- #792/#793 hygiene PR: all five verified facts were accurate — CU/CV/CW unused in matrix, line-3 BL/BM/BN is #611 score-forgery family, spec describe-block label is purely cosmetic (no assertion depends on it), archive has exactly 3× each duplicate per the intentional section structure, code-style.md §7 does contain the RPC output contract rule at line 735. Plan correctly scoped archive as read-only. APPROVED clean.
- #326 attack-surface matrix registration: spec-count stays 39 (files already on disk); existing BE/BF/BG/BH/BI rows reference specs by filename only → no edits needed; Vector Z correctly kept GAP (orthogonal authenticated vs unauthenticated paths); unlabeled tests in rpc-issue-internal-exam-code correctly identified as intentionally unlabeled. 2 ISSUEs (spec-count scoping + occupied-row disambiguation) → REVISE. **STALE-BRANCH MISS (corrected at execution, not by the critic): the plan was drafted against a pre-#802 branch where CW was the matrix max, so it allocated CX–DC — but #802 had already merged CX–DG to master. Caught only when reading the live header during execution; re-allocated to DH–DM. Lesson: branch off master FIRST, then verify the matrix — Explore/critic run against a stale working tree produce stale max-ID/collision findings.**

| Idempotent-resume RPC race-handler: when wrapping an INSERT in BEGIN...EXCEPTION WHEN unique_violation, a function that is idempotent-resume (returns existing session on race) must do a re-read + RETURN in the handler — NOT mirror the siblings' raise-only shape. start_internal_exam_session (mig 070) and start_exam_session (mig 088) both just RAISE 'active_session_exists' because they are not idempotent. start_vfr_rt_exam_session IS idempotent — the handler must re-read via a SELECT identical to step 5 and return the winner's session. Plans that say "mirror whichever sibling fits best" without explicitly noting this divergence will produce a raise-only handler that breaks idempotency on concurrent first-calls. | 2026-06-10 | 1 | 2026-06-10 | WATCHING |

| docs/plan.md Phase-A delivery records vs current-state claims: line 5 is a current-state count ("136 integration tests") — correct bump target. Line 34 is a Phase A delivery record ("136 new SQL tests … Phase A delivered") — bumping it corrupts the historical record. Plans that say "bump lines 5+34" must be corrected to "bump line 5 only." Historical phase-completion entries must not be edited (agent-doc-updater.md rule). | 2026-06-11 | 1 | 2026-06-11 | WATCHING |

| Probe-only-to-seed transitions in red-team specs: when a plan converts a probe-only spec (no seeding, no afterAll) to a seeding spec, it must: (a) update the file header comment to remove "no seeding" / "probe-only" claims, (b) test.afterAll soft-delete for the new seeded rows. Plans that update the header are architecturally sound. Established pattern: rpc-cross-tenant-isolation.spec.ts #818. | 2026-06-13 | 1 | 2026-06-13 | WATCHING |
| Prop-threading plans through intermediate state-holder components must enumerate ALL four file changes: (a) the action/source file returning the new field, (b) the form/trigger component updating its onCallback type, (c) the intermediate state-holder updating its state type and callback lambda, (d) the consumer component adding the prop. Plans that mark the state-holder as "to be confirmed" leave the implementer without a full change list. The test for the form component asserts the exact onCallback shape — if that shape changes, the test breaks. First seen: send-email-code plan (codes-tab.tsx IssuedCode type; issue-code-form.test.tsx line 105 asserts onIssued({code,expiresAt}) which breaks when codeId is added). | 2026-06-18 | 1 | 2026-06-18 | WATCHING |

## Topic pointers
- [tracker-archive](topics/tracker-archive.md) — relocated verbatim: older single-occurrence (count=1) tracker rows + older positive signals (moved 2026-06-07 to stay under the 25 KB injection cap).
- [pr-836-report-ui](topics/pr-836-report-ui.md) — VFR-RT student-report/flagging UI facts (#697 Phase B/C, #836): report-page structure, `useFlaggedQuestions` is server-result-driven not optimistic, `active_flagged_questions` security_invoker view.
