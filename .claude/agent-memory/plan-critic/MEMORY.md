# Agent Memory — plan-critic

> Index of durable plan-review knowledge. Recurring-pattern tracker + stable recipes.
> Per `.claude/rules/agent-memory.md`: keep < 200 lines / < 25 KB. No session logs — git holds history.
> Rows are never deleted, only state-transitioned. Detail lives in `topics/` — keep rows to ONE line.

## Recurring Issues Tracker

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---|---|---|---|---|
| Cold count=1 rows (routing/redirect-target, PR3 admin-report cluster, CR-fixup-sweep cluster, #1165 RPC-scoping cluster) relocated — see [tracker-archive § Relocated 2026-09-13](topics/tracker-archive.md). | — | — | 2026-09-13 | ARCHIVED |
| Hook-addition plans miss `docs/plan.md:1045` (pre-commit enumeration) even when they DO catch `docs/decisions.md:47`, `CLAUDE.md:251`. The three surfaces are not the same text but they all enumerate the same command list. **UPDATE 2026-09-14:** After PR #1274 all three surfaces now say "the command list is DATA in `.claude/pipeline.json`" rather than enumerating. A plan adding a new pre-commit guard now only needs to update `pipeline.json` — the prose surfaces already self-invalidated by pointing to it. Count reset to WATCHING on the new condition: plans that miss `pipeline.json` when adding a lefthook command. | 2026-09-14 | 1 | 2026-09-14 | WATCHING (resolved shape) |
| Escape-hatch "marker on the removed line" design: a removed line is content being DELETED — the marker cannot be added to it in the same single commit (requires a two-commit workflow). Plans should say "marker visible in the diff" (matching the spec) and let the implementer decide whether it goes on a `-` or `+` line. | 2026-09-14 | 1 | 2026-09-14 | WATCHING |
| A `React.cache()` plan counts ALL call sites as beneficiaries. `cache` is a literal passthrough with no async dispatcher (`react.react-server.development.js:578`), so Server-Action callers with ONE call each gain nothing — split callers into RSC-render vs action before claiming the win. **rev2 mechanism:** the corrected plan then counted the win PER FILE (files with ≥2 calls), which structurally cannot see a cross-file fan-out — `/app/admin/internal-exams` is 5 calls spread one-per-module. Count per RENDER TREE (route), never per file. | 2026-08-20 | 2 | 2026-08-20 | RULE CANDIDATE (2) |
| Plans state a call-site census by grepping FILES containing the identifier and reporting it as CALL EXPRESSIONS (or vice-versa) — the two numbers get swapped and both land in the Validation section as fact. Re-derive each with its own command. **rev2:** the same plan fixed the file-vs-expression swap and shipped a NEW wrong number in its place ("24 of 38 destructure"; actual 35 of 38, 3 bare) — a corrected census is not a verified one. | 2026-08-20 | 2 | 2026-08-20 | RULE CANDIDATE (2) |
| A shared error/not-found component gets ONE recovery link chosen for the anonymous case — and the plan's claim that siblings link role-appropriately is itself unread: `admin/error.tsx:31` links `/app/dashboard` (STUDENT), while the plan cited it as "matching `/app/admin/dashboard`". Enumerate the LAYOUT CONTEXTS the component renders in, and OPEN every sibling cited as precedent. | 2026-08-20 | 2 | 2026-08-20 | RULE CANDIDATE (2) |
| UI-reuse & question-type-CLONE plans miss the reveal-mechanic + shared touch-points. ([details](topics/vfr-rt-question-type-clone-plans.md)). | 2026-06-26 | 2 | 2026-07-02 | WATCHING (2) |
| RPC-signature-change plans (RETURNS TABLE DROP+recreate) miss types.ts drift, LATERAL row-drop, multi-key fan-out key-split. ([details](topics/vfr-rt-question-type-clone-plans.md)). | 2026-06-21 | 3 | 2026-07-02 | RULE CANDIDATE (3) |
| Data-layer-CHECK + regex-hardening answer-leak plans (#951): 22023 traps, DDL-in-test infeasibility, plan.md count-literal. ([details](topics/regex-hardening-answer-leak-951.md)). | 2026-06-23 | 6 | 2026-06-26 | RULE CANDIDATE (2) |
| VFR-RT Phase 4/5 report-rendering: pagination/builder, field-repurpose, sibling-audit, DIALOG_FILL-CLONE checklist. ([details](topics/vfr-rt-phase4-report-rendering.md)). | 2026-06-24 | 3 | 2026-06-25 | RULE CANDIDATE (3) |
| Plans gate a new SECURITY DEFINER RPC "internal-only" by omitting GRANT — false premise; REVOKE is required. ([details](topics/security-definer-rpc-plan-lessons.md)). | 2026-06-21 | 2 | 2026-07-02 | WATCHING (2) |
| App-tier mutation-action integration plans under-cover the action contract (null-vs-string, lifecycle, coalesced fields, error maps). ([details](topics/925-integration-tier-plan-lessons.md)). | 2026-06-20 | 2 | 2026-06-20 | WATCHING |
| Rule/docs-promotion plans hard-code learner counts + offender inventories that drift — re-derive at exec, git-verify hashes. ([details](topics/rule-promotion-count-drift.md)). | 2026-06-21 | 3 | 2026-07-03 | RULE CANDIDATE (3) |
| MC-only→new-question-type pipeline plans omit DraftAnswer/AnswerFeedback widening + the loadSessionQuestions caller. ([details](topics/new-question-type-storage-and-pipeline.md)). | 2026-06-20 | 3 | 2026-06-20 | WATCHING (3) |
| New-type answer storage-shape ambiguity hits report pagination AND score-aggregation — specify both. ([details](topics/new-question-type-storage-and-pipeline.md)). | 2026-06-20 | 2 | 2026-06-21 | RULE CANDIDATE (2) |
| Multi-query test mocks: one mockFrom/mockRpc keyed by name can't distinguish N sequential calls — need call-order dispatch. ([details](topics/test-mock-scoping-lessons.md)). | 2026-04-11 | 6 | 2026-05-31 | WATCHING (6) |
| Multi-query test mocks: one mockFrom can't distinguish N parallel calls to a table, or count-head vs range-data. ([details](topics/test-mock-scoping-lessons.md)). **rev7:** swapping a `head:true` count for `fetchAllRows` turns 1 `from()` into 1+N (count + pages), shifting EVERY positional `mockFromSequence` in the describe — and the displaced users-lookup `{data:{...}}` gets spread by `all.push(...data)`, so tests THROW rather than fail. | 2026-04-11 | 7 | 2026-08-24 | WATCHING (7) |
| Test-update scope underspecified: "test update" without naming the assertion/fixture/prop that breaks. ([details](topics/test-mock-scoping-lessons.md)). **rev9:** "student reports list tests" named no file — actual blast radius was `session-table.test.tsx:71`, `session-card.test.tsx:64`, `reports-list.test.tsx:126`, `reports-content.test.tsx`, `reports.test.ts`, `reports.integration.test.ts`, plus an unnamed `attempts-table.test.tsx:97` (`'14/16'`). | 2026-04-11 | 9 | 2026-08-24 | WATCHING (9) |
| New `.from()`/`.rpc()` site in `apps/web/lib/queries/**` planned with a unit test only — `code-style.md` §7 "New Supabase Query Sites Require an Integration Test" is HARD and was not applied to the plan's new shared helper. Check the rule whenever a plan adds a query MODULE, not just when it adds an RPC. | 2026-08-24 | 1 | 2026-08-24 | WATCHING |
| SECURITY DEFINER RPC plans omit project SQL conventions: is_admin(), soft-delete filters, cached v_admin_role. ([details](topics/security-definer-rpc-plan-lessons.md)). | 2026-04-11 | 5 | 2026-06-18 | WATCHING (5) |
| Sibling-file audit missed: defensive pattern added to one action/query file, identical sibling untouched. **rev3:** a 4-surface sweep of `correct/total` list rows missed the 5th — `internal-exam/_components/my-reports-tab.tsx:112` renders `answeredCount/totalQuestions` where `list_my_internal_exam_history` (`20260521000003:51`) computes `count(*)` over `quiz_session_answers` = item-level. The plan's own "internal exams are not MC-only" argument mandates the STUDENT mirror too. | 2026-04-11 | 3 | 2026-08-24 | RULE CANDIDATE → CLAUDE.md sibling-audit |
| Plans citing a constraint/mode value must trace the FULL migration chain — a later one may have widened it. ([details](topics/migration-and-schema-audit-lessons.md)). | 2026-05-08 | 3 | 2026-05-28 | WATCHING (3) |
| Red-team fixture INSERTs into student_responses need selected_option_id (TEXT NOT NULL CHECK a/b/c/d). ([details](topics/redteam-spec-organization-lessons.md)). | 2026-05-31 | 2 | 2026-05-31 | WATCHING (2) |
| Red-team specs for aggregation RPCs need an instructor fixture not in seed.ts by default. ([details](topics/redteam-spec-organization-lessons.md)). | 2026-05-31 | 2 | 2026-05-31 | WATCHING |
| Red-team plans must verify Vector IDs against the LIVE attack-surface.md matrix + tech.md/decisions.md spec-counts. ([details](topics/redteam-spec-organization-lessons.md)). | 2026-05-31 | 4 | 2026-06-09 | WATCHING (4) |
| Red-team spec additions omit the attack-surface.md vector row / GAP→COVERED flip. ([details](topics/redteam-spec-organization-lessons.md)). | 2026-06-01 | 4 | 2026-06-06 | WATCHING (4) |
| Two-dir migration mirror drift — identify the authoritative dir before reproducing a body. ([details](topics/migration-and-schema-audit-lessons.md)). | 2026-06-06 | 1 | 2026-07-11 | RESOLVED (packages/db/migrations frozen 2026-07-11; supabase/ sole truth — #1111) |
| A mechanic-rename plan changes only the PRIMARY definition line (e.g. agent-critic.md:26 "Reset on finding") but leaves the CONCEPT NAME and derived wording throughout the same section (lines 20-21 "Consecutive-clean floor", line 37 "do not reset the clean counter", lines 48/78/90/91 "consecutive-clean floor") — readers see contradictory instructions in the same file. Change the concept name and every wording derived from it in the SAME commit. | 2026-09-06 | 1 | 2026-09-06 | WATCHING |
| This Claude Code shell's `grep` is an ALIASED wrapper (`ARGV0=ugrep ... -G --ignore-files --hidden`) that silently respects `.gitignore` on a plain recursive `grep -r`. Reproducing a plan's fs-walk-vs-`git ls-files` divergence claim (e.g. `.next/` build artifacts matching a pattern) with the bare `grep` command reads FALSE-CLEAN — use `/usr/bin/grep` to reproduce the real developer/CI behavior before disputing a count. | 2026-09-13 | 1 | 2026-09-13 | WATCHING |
| Guard-found-path plans conflate "guard flagged path X" with "X should be corrected in prose" — but historical narrative and exclusion-logic comments that contain a non-existent path are NOT stale claims; they need a waiver marker, not a prose correction. Worse, a plan can name a WRONG "real path" substitute (e.g. `lib/typescript.js` → `packages/typescript-config` when the sentence describes what TypeScript npm dropped from its own package). Always read the sentence's SUBJECT before classifying a flagged path as fixable. | 2026-09-16 | 1 | 2026-09-16 | WATCHING |
| Plans with a two-section structure ("Files to change / MIRRORS" + "Additions from commands/agents/skills/specs map / COMMANDS") — checking ONLY the MIRRORS table produces false-positive "missing line" findings. The COMMANDS section is explicitly additive and may cover lines absent from MIRRORS (e.g. autonomerge.md:19 only in COMMANDS, not MIRRORS). Always combine both sections before flagging a missing line. | 2026-09-17 | 1 | 2026-09-17 | WATCHING |
| Behavior-change plans omit the UNIT TEST that pins the OLD behavior being removed. When Item N changes a fallback/sentinel value, grep the test file for a test asserting the old value and verify it is in the "Files affected" list. (2026-09-19: Item 2 removed the `'00000000-...'` sentinel fallback; seed-unauth-fixtures.test.ts line 140 specifically asserts `result.knownSessionId === '00000000-...'` and was not listed.) | 2026-09-19 | 1 | 2026-09-19 | WATCHING |
| GRANT-gated RPC plans conflate the GRANT-denial path (42501, fired before body) with the body's RAISE path (custom code, fired only when GRANT passes). Sibling tests that assert a RAISE message all have GRANT to anon; a no-GRANT function returns 42501 and its body's RAISE is unreachable for anon. Always check GRANT set before recommending a "tighten assertion" change. (2026-09-19: `submit_quiz_answer` GRANT is `authenticated`-only; plan said "assert specific rejection code" without naming it.) | 2026-09-19 | 1 | 2026-09-19 | WATCHING |
| Motivating-collision tables list a pair where one file's suffix already has a semantic DISCRIMINATOR suffix (e.g. `-calc`, `-v`, `-img`) so the assembled DB key is `SCts-calc` vs `SCts` — different strings, no collision. Check the FULL suffix expression, not just the base string. (2026-09-18: rpc-calc-mode-pool `SC${ts}-calc` vs rpc-vfr-rt-constraint-regression `SC${ts}` — plan listed as collision #3.) | 2026-09-18 | 1 | 2026-09-18 | WATCHING |
| "Files affected" section claims FILE X imports HELPER Y from a testkit, but X imports from the guard directly — only N-1 of the N listed files actually use the testkit helper. Verify every import in "Files affected" by grepping the actual file. (2026-09-18: `check-retracted-phrase.test.mjs` listed as importing `run()` from the testkit; it imports from the guard itself and has zero spawn calls.) | 2026-09-18 | 1 | 2026-09-18 | WATCHING |
| Mutation-harness plans assert a cross-suite expectRed (mutation in suite A also reddens suite B) without verifying the CALL PATH. A mutation to `main()`-only code (e.g. `fromIndex`) cannot redden tests that call `evaluate()` directly — those tests supply their own `readFile` and never enter `main()`. Verify: does the test spawn the guard as a subprocess, or call an exported function directly? Only subprocess tests can reach `main()`-path mutations. | 2026-09-18 | 1 | 2026-09-18 | WATCHING |
| Mutation-harness claim encoding plans miss the SUPERSET reddening of a sibling test that checks the SAME mechanism. When encoding a "remove the push" mutation on an `if (allowed === undefined)` branch, BOTH "flags a new over-limit file" (asserts length AND why) AND the retitled "emptied baseline" test (asserts length only) go red — yet the plan only mentioned the retitled test in expectRed. **rev2 (check-retracted-phrase, 2026-09-18):** breaking `message: git([...])` at L615 (claim 1 anchor) reds BOTH claim 1's test AND claim 5's test — both rely on per-commit messages carrying waivers; there is no narrower L615 mutation. The plan says "run the harness, take the observed set," which handles it, but the implied single-title expectRed is wrong. Always check whether ANY other test also relies on the SAME code path before assuming a single-title expectRed. | 2026-09-18 | 2 | 2026-09-18 | WATCHING (2) |
| parseSuite/GROUP: attribution plans specify "attaches to NEXT test()" but GROUP: markers appear INSIDE test bodies in .repo.test.mjs files — the "NEXT test" rule misattributes them to test N+1, causing MUTATION: claims to appear "not-linked" when they ARE linked. Always grep for `// GROUP:` indented inside test bodies (`.repo.test.mjs` style) before specifying an attribution direction. **Also:** `docs/decisions.md` restates `--coverage` formula semantics (at the `Decision 67` block) and is a missed mirror whenever code-style.md §7's coverage description changes. | 2026-09-18 | 1 | 2026-09-18 | WATCHING |

- P8 shared-diff-parser plan (2026-09-22): TWO ISSUES. (1) `pipeline.json` `guards` unchanged but MUST add `.repo.test.mjs` to suites — every sibling `.repo.test.mjs` (prose-claims, prose-paths) carries CONTROL: markers and is registered in pipeline.json; without registration controls.test.mjs never verifies the new repo test's controls (stays "green" by ignorance, not correctness). (2) `run-security-auditor.sh` L40 has TWO git diff calls (`primary || fallback`); plan mentions "L40, L55" implying two total but the fallback `git diff HEAD` at L40 also needs `--no-textconv --no-ext-diff`; control test only covers primary path.
- CR-local retirement plan (2026-09-19): ONE ISSUE `automerge.md:30,:51` (dead rate-limit text) missed; plan only lists :28,:31. TWO SUGGESTIONS: `fullpush.md:159` table row; `internal-exam-code-email/tasks.md` (all refs in [x] rows). REVISE.
- cl.8 measurement plan (2026-09-19): APPROVED. SUGGESTION: "55" survives in corpus as Decision 55 across three files — plan's "no fresh count" instruction mitigates.
- P1b mutation harness --jobs plan (2026-09-22): APPROVED. All architectural claims verified: spawnSync serializes worktree add/remove (event loop blocks); staged/scope test suites spawn throwaway repos in tmpdir(), never touch main repo; git ls-tree --name-only returns full relative paths (.claude/hooks/x) allowing {basename,path} reconstruction; pipeline.test.mjs does NOT assert ci.yml suite list or grading command string (no update needed). SUGGESTIONS only: (1) `jobs` threading through `modeUpdateExpected` → `modeRun` is implicit — plan says --jobs allowed with --update-expected but doesn't trace the parameter chain; (2) 4 specific mutation IDs that will FAULT on re-anchor: drop-fault-isolation, touchesstaged-no-import-hop, touchesstaged-no-named-paths, staged-empty-scope-exits-zero (plan says "re-anchor moved code" without listing IDs); (3) spawnSync worktree-add blocks event loop during concurrent spawnSuite, delaying OS pipe drain — safe given typical <10KB suite output vs 64KB pipe buffer.
- Plan B (code-review skill, 2026-09-19): ONE ISSUE — two ACTIVE specs enumerate 5-member round-1 roster (vfr-rt-slovenia-mock-exam/tasks.md:261, redteam-quiz-session-bugs/tasks.md:35); not in plan's edit list. REVISE.
- Integration-fixture-isolation plan (2026-09-18): APPROVED — all 7 questions clean.
- Vacuous-negatives plan (2026-09-19): ONE ISSUE — quiz_session_answers resolution path unspecified in RISK 1. SUGGESTION: code-style.md §7 overlap note.
- --update-expected plan (2026-09-19): ONE ISSUE — insertion "before `:709`" is INSIDE the catch block (line 709 = `return 'fault'`). Three SUGGESTIONS: line refs 495/538/752 each off. All other claims verified.
- round-8 residual fixes plan (2026-09-20, `fix/learner-tracker-termination`): APPROVED. All claims verified: agent-memory.md grep=0 for "archiv" ✓; insights.md:21 baselined at `@8bda443a9b689bc8` ✓; 8091d3b4 message "17 of the 18 rows" ✓; RESOLVED-WATCH counts origin/master=20 / 8091d3b4=21 / HEAD=23 ✓; "only MEMORY.md is injected" confirmed by agent-memory.md ✓; agent-memory.md not in prose-claims baseline ✓; no missed mirrors ✓. ONE SUGGESTION: tasks.md:141-145 says "verdict needed" for RESOLVED-WATCH archiving — verdict already in insights.md:21 and plan adds it to agent-memory.md; background note is stale but non-blocking.
- P2 guard controls plan (2026-09-22, `guards/controls` from `ci/claude-review`): APPROVED. All 7 CONTEXT checks verified: (1) guard set matches exactly (13 entries — wired from lefthook+settings+ci.yml after P2 additions = registry minus P3-P6); (2) no P3-P8 imports in taken files; (3) parseSuite body stays ~23 lines after hunk, scanControls is 10-line separate function; (4) TOP_LEVEL_KEYS single-key add sufficient; (5) check-test-title-leakage snapshot compatible — guard .mjs identical at snapshot vs master (empty diff); (6) §7/D79 first-bullet-only clean of P3-P8 refs; (7) ONE SUGGESTION only: after correcting "all seven reviewers" → "the round-1 roster", the text still reads "…every round" which implies full roster every round — on master (D74/77) rounds 2+ run only code-reviewer + semantic-reviewer.
- P7 deletion-reviewer plan (2026-09-22, `docs/deletion-reviewer`): ONE ISSUE — corpus-codification/tasks.md (ACTIVE spec) has three stale "six"/"SIX" roster references at lines 249, 270, 578 not in plan's edit list; plan R6 only ticks the P7 checkbox. Same pattern as Plan B (active-spec-misses count = 2, now RULE CANDIDATE). REVISE.

## Positive signals
- See [tracker-archive § Relocated positive signals](topics/tracker-archive.md) — 6 plans that verified assumptions correctly.
- GROUP: marker plan for check-file-size-guard + check-retracted-phrase (2026-09-18, branch `chore/link-guard-claims`): all 5 assertions (A-E) verified clean. `target` anchors unaffected; `expectRed` titles portable across file splits; `ownerFor` forward-lookahead correctly handles claim→GROUP→test() stacking; all 9 suites stay under 500 lines after markers except cli.test.mjs (addressed by plan's split); ci.yml/lefthook/limits.json/run-mutations.test.mjs all correctly excluded from blast radius. One SUGGESTION: plan stated "19 not-linked claims" for check-retracted-phrase; actual count is ~11-12 (65 total claims, 53 markers → 11 unmarked tests, max 12 claims). Non-blocking — plan's own `--coverage → 0 DANGLING` step self-corrects. APPROVED.
- corpus-codification slice 2 mutation-harness + R0-VALUE plan (2026-09-14): all 7 targeted risk areas verified clean. CWD-relative limits.json claim confirmed (`.test.mjs:30`), worktree protocol reading confirmed (`test-writer.md:85/96/105`), ratchet key design sound, stage choice sound, ci.yml explicit-step requirement confirmed (`ci.yml:70/126`), docs/plan.md + decisions.md + steering/tech.md already defer to pipeline.json (no stale enumeration to update). A well-validated plan with no false assumptions found.
- #1298 pre-push gate plan (2026-09-17): large corpus-wide pipeline change across 40+ files. D1-D11 decisions all covered. Three deleted exemptions (D3/D4/D5) verified against all referencing files. round/count thresholds all covered. Rule-Mirror Sync table rows all covered. One SUGGESTION only: agent-workflow.md:424 Delegation Protocol "Post-commit agent integration" subsection not in edit list; plan's R1 provides Retracted-ok: mitigation. All "gaps" from prior session analysis were false positives caused by checking MIRRORS section only, missing COMMANDS additions (new pattern logged above).
- Decision 74 CR-local round-1-only plan (2026-09-17): 35-site / 17-file sweep. All sites verified at HEAD. No missed sites found across .coderabbit.yaml, .claude/skills/**, .claude/agents/*, and all command files. Unchecked task counts verified for all 5 named specs (backlog-burndown 30, corpus-codification 22, redteam-quiz-session-bugs 14, study-mode 1, vfr-rt-slovenia-mock-exam 14 — all live). automerge.md:30 (CR-local rate-limit sentence) correctly NOT in change list — instruction stays valid for round 1. ADR-annotation convention for Decision 73 confirmed via existing annotations. APPROVED.

## Topic pointers
- [cr-fixup-sweep-axis-1185](topics/cr-fixup-sweep-axis-1185.md) — sweep by code STRUCTURE not identifier; diff prior art's DISPOSITION not shape; `instanceof Error`; `typeof` collapses null/array/object; positional `tsc` ignores tsconfig.
- [durable-review-checks](topics/durable-review-checks.md) — per-domain checklists: SQL/migrations, pagination, extraction/file-split, component/UI, red-team specs, validation discipline, return contracts.
- [1165-rpc-scoping-and-rls-narrowing](topics/1165-rpc-scoping-and-rls-narrowing.md) — RPC arg-change enumeration, §15 INSERT-path carve-outs, caller-keyed answer strips, RLS `FOR ALL`→`FOR SELECT` semantics.
- [extraction-and-guard-plan-gaps](topics/extraction-and-guard-plan-gaps.md) — `_hooks/` test omissions, snap/clamp `onClick`, over-cap hook growth, re-entry ref placement, async-component test relocation.
- [pr3-admin-report-non-mc](topics/pr3-admin-report-non-mc.md) — full text of the 2026-08-24 rows: surface-kind sibling classification, unverified "mirror", fixture-blind test claims, matcher-blind break claims, embedded-aggregate vs `fetchAllRows`, RLS-narrower embed silent 0.
- [tracker-archive](topics/tracker-archive.md) — cold count=1 rows relocated in dated sections (grep `^## Relocated` for the current set; latest 2026-09-13) + positive signals. **Check here before concluding a pattern is new**; re-promote a row if it recurs.
- [derived-content-ids-20260817](topics/derived-content-ids-20260817.md) — derivation specified for only one input domain; ids derived from cosmetic constants; "tests hardcode the values" claims that grep disproves.
- [typo-tolerance-split-20260815](topics/typo-tolerance-split-20260815.md) — corpus-simulated tolerance thresholds, GRANT-precedent verification, levenshtein 255 limit, migration-split partial application, prod-write row keying.
- [pr-836-report-ui](topics/pr-836-report-ui.md) — VFR-RT report/flagging UI facts: `useFlaggedQuestions` is server-result-driven, `active_flagged_questions` security_invoker view.
