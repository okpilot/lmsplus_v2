# Agent Memory — plan-critic

> Index of durable plan-review knowledge. Recurring-pattern tracker + stable recipes.
> Per `.claude/rules/agent-memory.md`: keep < 200 lines / < 25 KB. No session logs — git holds history.

## Recurring Issues Tracker

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---|---|---|---|---|
| Multi-query test mocks: single `mockFrom`/`mockRpc` keyed by name can't distinguish N sequential calls to the SAME endpoint with different results (e.g. page-fetch then probe). Plan must scope call-order dispatch (e.g. `mockRpc.mockResolvedValueOnce(…).mockResolvedValueOnce(…)`) as in-scope; and must name which specific existing test cases will BREAK (not just "verify/keep"). | 2026-04-11 | 6 | 2026-05-31 | WATCHING |
| Multi-query test mocks: single `mockFrom` keyed by table name can't distinguish N parallel `.from()` calls, two sequential calls to the SAME table with different filters, or count-head vs range-data calls. Plan must scope `mockImplementation`/`mockFromSequence`/call-order dispatch as in-scope. dashboard.ts getTotalAnswered+getQuestionsToday are BOTH `student_responses` count-head reads in Promise.all — any error test in dashboard.test.ts that sets `student_responses` to `{ count: null, error }` necessarily errors BOTH, not just the target. Plan's proposed mitigation (assert on prefix match `/Failed to fetch/`) is correct — flag this only as SUGGESTION to change assertion rationale in plan. | 2026-04-11 | 6 | 2026-06-01 | WATCHING |
| Test-rewrite plans for query→RPC (or Server-Action delegation) refactors underspecify scope: must enumerate which test blocks survive unchanged, which collapse (dead paths), which change *assertions* (not just mocks). Vague "rewrite N tests" leaves implementer to over/under-mock. | 2026-04-11 | 7 | 2026-05-29 | WATCHING |
| SECURITY DEFINER RPC plans omit project SQL conventions: `is_admin()` over inline role lookup; `AND deleted_at IS NULL` on user/session lookups (§9); §9-omission needs inline comment citing rule + precedent. | 2026-04-11 | 4 | 2026-05-27 | WATCHING |
| Sibling-file audit missed: defensive pattern (error destructure, null guard) added to one action/query file, identical pattern in sibling in same folder untouched. (CLAUDE.md sibling-audit rule.) | 2026-04-11 | 2 | 2026-05-22 | RULE CANDIDATE → CLAUDE.md sibling-audit |
| Plans referencing a constraint/mode value cite mig 001 but a later migration widened/renamed it. Must trace the FULL migration chain (grep `ALTER TABLE`, `ON CONFLICT (`, constraint names) before depending on a constraint's shape. | 2026-05-08 | 3 | 2026-05-28 | WATCHING |
| Red-team fixture INSERT into `student_responses` omits `selected_option_id` value and/or misstates column type. Actual schema: `TEXT NOT NULL CHECK(IN 'a','b','c','d')` — not uuid, not nullable. Must include a value; service-role INSERT bypasses FORCE RLS. | 2026-05-31 | 2 | 2026-05-31 | WATCHING |
| `get_student_streak` always returns exactly one `{current_streak:0,best_streak:0}` row for anon/cross-org (COALESCE scalar subqueries with no FROM). Generic "error!=null \|\| length===0" assertion pattern fails for this RPC; needs a tailored `current_streak===0 && best_streak===0` check. Requirements doc for #673 correctly specifies the {0,0} shape throughout — positive signal. | 2026-05-31 | 1 | 2026-05-31 | WATCHING |

## Durable plan-review checks

### SQL / migrations
- **Trace the full migration chain, not just mig 001.** `quiz_sessions.mode` CHECK widened by mig 058 (added `internal_exam`) and given the canonical name `quiz_sessions_mode_check` — use named `DROP/ADD CONSTRAINT`, not the DO-block predicate-text lookup (that pattern is only for unnamed constraints). `quiz_sessions.deleted_at` added by mig 023 (not in initial schema). Always grep all migrations for `ALTER TABLE` on the target before reasoning about its current shape.
- **CREATE OR REPLACE FUNCTION: trace to the latest definition before flagging a missing pattern.** A later migration may already add the guard/clause. (Pre-Flag Verification — also in `semantic-reviewer.md` / `implementation-critic.md`.)
- **Widening a UNIQUE constraint breaks existing `ON CONFLICT (col-list) DO NOTHING`.** Postgres needs an exact matching unique index. Grep `ON CONFLICT (` across all migrations and include a companion migration rewriting every living RPC's clause (missed for `batch_submit_quiz`/`submit_quiz_answer` on mig 084).
- **`STABLE`/`IMMUTABLE` + `EXECUTE` is a hard error.** Dynamic-SQL RPCs (`RETURN QUERY EXECUTE`) must be `VOLATILE` (or omit the keyword). Model: `get_session_reports`, not `get_admin_student_stats`.
- **New `questions` columns must satisfy existing NOT NULL columns.** `questions.options` is `JSONB NOT NULL`, no DEFAULT — new types (short_answer, dialog_fill) need nullable/DEFAULT/explicit `options:'[]'` or inserts fail silently.
- **New exam modes with answer-bearing question types need a server-side projection RPC.** `get_quiz_questions()` only strips MC correct-option markers; any new correct-answer column (canonical_answer, blanks_config canonical, accepted_synonyms) needs a `get_*_questions` RPC that strips it — never let students SELECT it via PostgREST.
- **`smart_review` passes `p_subject_id = NULL`.** `q.subject_id = NULL` is always NULL in Postgres → 0 rows. Subject-scoped WHERE/COUNT must be `(p_subject_id IS NULL OR q.subject_id = p_subject_id)`.

### Pagination / truncation (PostgREST max_rows = 1000)
- `.limit(N)` with N > 1000 is NOT a safeguard — max_rows=1000 overrides it. Treat as unbounded (e.g. `dashboard-stats.ts` `.limit(10000)`/`.limit(5000)`).
- Truncation audits partitioned by directory miss colocated `queries.ts` in admin sub-routes and the cross-cutting GDPR export layer (`lib/gdpr/collect-user-data.ts`). Scope audits by file-ownership, name the colocated files.
- `.order('id')` tiebreak fails on tables without an `id` column — `flagged_questions`/`active_flagged_questions` use composite PK `(student_id, question_id)`; use `flagged_at`. Verify each target table has the column.
- `fetchAllRows<T>` contract: `data` is always `T[]`, never null. Error-path mocks use `{ data: [], error: {...} }`, not `{ data: null }`. Joins in the `getPage` select need an explicit cast to `PromiseLike<PageResult<T>>`.

### Extraction / file-split
- Removing one function from an over-limit utility file may leave the parent still over the 200-line cap — verify post-extraction line count or scope a second split.
- Query-helper modules extracted from `'use server'` files must NOT carry `'use server'` (they take a `supabase` param; the directive turns exports into Server Actions and breaks runtime calls). Precedent: `collect-user-data-queries.ts`.
- Adding a column to a `.select()` string must also update any local row-type alias used in an `as SomeRow[]` cast (TS doesn't enforce the cast against runtime; missing field is invisible until referenced).
- Replacing one `Promise.all` arm with a helper returning an unwrapped array changes the destructuring (`{ data }` → bare binding) — state it explicitly.

### Component / UI
- Migrating a hand-rolled overlay to a controlled Dialog: removing the mount guard breaks `useState(props…)` initializers (run once at mount) → stale form on reopen. Keep the guard or add `key={entityId}` to force remount.
- Rewriting a component to a new UI lib: the co-located `.test.tsx` mocks the OLD lib by module path — the entire mock + assertions must be rewritten; list the test file under "Files to change".
- `nav-items.ts` icon name is a CLOSED union; `NavIcon` switch silently renders nothing for unknown names. New nav entries reuse an existing icon or add to BOTH the union AND the switch. `sidebar-nav.test.tsx` asserts exact named items — list it as an affected caller.

### Red-team / E2E specs
- A spec's *pass condition* may be a specific error code — hardening that changes the error string (e.g. `23502` → `invalid_question_ids`) breaks the assertion. Check per spec; prefer open `expect(error).not.toBeNull()`.
- Specs fetching question IDs with only `deleted_at IS NULL` (not `status='active'`) feed invalid IDs to new active-status validation. Add `.eq('status','active')` or document seeded-active assumption.
- NUL-byte (`\x00`) injection payloads are rejected by PostgREST (400) before the RPC runs — message won't match the RPC's `RAISE` string; needs a separate assertion branch.
- audit-completeness: `actor_id` differs per event type — admin events (`internal_exam.code_issued/voided`) bind `v_admin_id`; student events bind `v_student_id`. Bind `expected_actor` to the right role or get 0 rows.
- Name the specific RPC per event: `exam.started`/`exam.completed` come from `start_exam_session`+`batch_submit_quiz(mode='mock_exam')`, NOT `start_internal_exam_session`.

### Validation discipline
- Never leave "needs verification" open in a validated plan — resolve before review or the implementer guesses.
- Dual-deletion-path tables (CASCADE + direct admin RLS DELETE, e.g. `exam_config_distributions`): soft-delete-matrix rows must document BOTH paths.
- `userEvent.type('150')` emits 3 change events; `.at(-1)` captures only the final clamped value — that (not "Math.max so exact") is the correct rationale for `toBe(max)`.

### Agent-tooling plans (native subagent memory)
- A Phase-0 spike "confirm the agent reads/writes MEMORY.md" is a false positive if the agent body also has an explicit Read/Write of that file — strip the body instruction first to isolate native-injection engagement.
- Reference matrices (e.g. red-team `attack-surface.md`) must not be renamed to `MEMORY.md` where the curation nudge can prune active GAP rows; keep as a topic file with a no-delete header and update operational references (insights.md) in the same commit.
- After `memory: project` is added, remove body-level "Read your memory file at start" instructions or the agent double-loads (test-writer, code-reviewer, plan-critic, implementation-critic, security-auditor all had these).

| Red-team spec for aggregation RPCs: instructor fixture missing from seed.ts (only 'student' and 'admin' roles exported). Plan that references `seedInstructor` or assumes the instructor helper exists will fail at runtime. Must add the fixture as part of the spec. | 2026-05-31 | 2 | 2026-05-31 | WATCHING — #673 design correctly adds seedRedTeamInstructor + upsertUser role widening; resolved for this PR |
| Red-team plans label specs with wrong/colliding Vector ID. BE/BF/BG/BH/BI are occupied by FIXED, unrelated vectors (draft-limit, audit-spoofing, batch_submit soft-delete, soft-deleted-user JWT, draft TOCTOU) — existing rows' Notes reference spec files by filename only, no label cross-refs, so re-lettering the spec self-labels never requires editing the existing rows. Plan must (a) verify each proposed label against attack-surface.md, (b) explicitly state which occupied rows require no edits. #326 plan correctly confirmed this. Also: spec-count in tech.md (×3) + decisions.md must be checked when registering specs; registering already-on-disk specs does NOT increment the count. | 2026-05-31 | 4 | 2026-06-09 | WATCHING |

### Return-type contract for incremental additions to query functions
When a plan adds a new code path to a function that returns a discriminated-union result type (e.g. `{ ok:false; error:string } | { ok:true; ... }`), verify every new `return` statement satisfies ALL fields of the matching union arm. Plans consistently specify `{ ok:false }` for new error paths but omit the mandatory `error: string` field — the TypeScript build fails. Always check the full type definition before specifying return values.
| Vector-label collision sweep must include ALL spec files in apps/web/e2e/redteam/, not just the two being renamed. Labels like "BJ" appear in BOTH the start-internal-exam spec (to be renamed) AND server-action-unauthenticated.spec.ts (#603 "soft-deleted user gate" sub-describe, lines 405/407/432/434/435) as an INDEPENDENT, non-colliding usage. Plans must grep all spec files before scoping the rename. The server-action-unauthenticated.spec.ts "BJ" is a different usage (#603 soft-delete gate, NOT the start_internal_exam_session unauth vector) — it must be noted as "leave untouched" in the plan, not silently omitted. | 2026-06-09 | 1 | 2026-06-09 | WATCHING |
| Red-team spec additions that cover a new RPC code-path consistently omit adding a vector row to attack-surface.md. Plans say "add a test case to existing spec" but do not say "add/update vector row in attack-surface.md." This is a recurring pattern: new coverage is added but the authoritative gap-tracker is not updated. Must include attack-surface.md in "Files affected" for any red-team spec addition. Also: when a plan routes Vectors L/M to a new dedicated spec file instead of the attack-surface.md canonical files (server-action-unauthenticated.spec.ts / rpc-cross-tenant.spec.ts), the matrix rows must be updated to reflect the new file choice — or the rows remain showing as GAP. A10-batch confirms the pattern: plan adds error-handling-disclosure.spec.ts with no attack-surface.md in Files affected — the A10 gap row must be updated from GAP→COVERED. | 2026-06-01 | 4 | 2026-06-06 | WATCHING |

### Red-team batching plans (organization plans, not implementation)
- "One spec file = one PR" principle must be verified against the *issue bodies*, not assumed from the PR's headline. Multiple issues in a single PR may each touch a different spec file (e.g. #557 requires both a new rpc-complete-empty-exam.spec.ts AND extending server-action-unauthenticated.spec.ts for Vector AN). Check each issue body for target spec file(s).
- A batch labeled "new file only" is only parallel-safe if NONE of its constituent issues also touch shared existing files. A single issue that spans 3 existing files (e.g. #517 covers AG in server-action-unauthenticated + AH/AK in rpc-cross-tenant + AI in session-replay) makes the whole PR conflict-prone.
- Issues with overlapping scope (e.g. #545 covers AG+AH; #517 also covers AG+AH) must be noted with a deduplication callout — both issues close from a single test implementation, and the plan must say which test file owns the implementation.
- Feature issues (e.g. #379, labeled `feature`, requiring new SECURITY DEFINER RPCs and Server Action wiring) cannot be "split out" from a red-team PR without production code — they require a migration + production code change + manual eval. Grouping them in a spec-only batch without noting production code is a false "automatable" classification.
- Attack-surface.md Vector O ("quiz-report leaks raw correct boolean server-side") is labeled CRITICAL in the matrix, not "possibly-CRITICAL". Plans that understate it need correction. The defense (buildReportQuestions strips the field in TypeScript) exists but is asserted only at the unit test layer — no E2E spec yet. It IS spec-only (no production fix needed), but the severity label must match the matrix.
- #551 (batch_submit_quiz soft-deleted question SQL integration test) has an undecided home: issue says "Playwright e2e/redteam/ pattern OR new integration/ Vitest suite hitting local Supabase Docker". The redteam CI workflow does not trigger on a new Vitest integration file — the plan must choose and state which, and if Vitest, note that the redteam.yml will NOT run it. RESOLVED in this PR: plan chose Vitest integration (packages/db/src/__integration__/rpc-batch-submit-quiz.integration.test.ts) and noted CI exclusion. Correct choice.
- batch_submit_quiz `RETURNS jsonb` — result is `{ results: [...], total_questions, answered_count, ... }`. Plans that write `data.length>=1` intending to test the results array are wrong; correct is `data.results.length>=1`. Plans must spell out `data.results[N].field` for all batch_submit_quiz assertions. Also: the `p_answers` element JSON key is `selected_option` (not `selected_option_id`) — the table column is `selected_option_id` but the JSON key the RPC reads via `v_answer->>'selected_option'` is `selected_option`.

| Two-dir migration mirror drift: supabase/migrations can have a newer TIMESTAMP migration that CREATE OR REPLACEs a function without a matching NNN_ packages/db mirror. The result is the same RUNTIME body (supabase controls production) but packages/db's highest NNN file is stale. The plan must identify WHICH directory's latest is binding and reproduce THAT body verbatim. Specifically: `20260521000001_start_quiz_session_null_guard.sql` (supabase) has the NULL guard but has NO packages/db mirror — packages/db 081 is stale. The new 086 migration must base on the supabase latest, not 081. Plans must always grep both dirs and compare bodies when supabase timestamp > packages/db highest NNN. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |
| Sibling-function inline-role-subquery audit: when a plan fixes the inline `(SELECT u.role…)` pattern in one function, sibling SECURITY DEFINER functions with the SAME pattern must be enumerated. The plan's sibling-grep instruction is correct but leaves discovery to the implementer — leaving start_exam_session (054, line 141) unmentioned. This is the same "inline role subquery in audit INSERT" pattern targeted in #734. Fix for start_exam_session is ≤10 lines (add `v_role text` to DECLARE, extend the existing SELECT INTO, replace inline). Plans should name the known offender rather than delegate discovery. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |
| Column GRANT plans on users table: the `users` table has NO explicit column-level GRANT in any migration. `authenticated` holds table-wide UPDATE via Supabase's default schema grants (same mechanism that quiz_sessions used before mig 20260605000001). The REVOKE UPDATE then GRANT UPDATE (full_name) is the correct and non-vacuous shape — mirroring quiz_sessions precedent. The plan's "expected: full_name, possibly updated_at / avatar / locale" is wrong: the users CREATE TABLE has NO updated_at, NO avatar, NO locale columns. The correct re-GRANT is `full_name` only (the only column written by authenticated users via `settings/actions.ts updateDisplayName`). email / deleted_by / last_active_at are only written by service-role or SECURITY DEFINER RPCs. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |
| FK-into-global-table audit: plans that claim "X is the ONLY FK into table Y" must grep ALL migrations for `REFERENCES Y`. For easa_subjects/topics/subtopics: FKs exist in questions (NOT NULL), quiz_sessions (NULLABLE, no ON DELETE clause), and exam_configs (NOT NULL). A plan that only cites questions misses quiz_sessions and exam_configs. However, for test-hermeticity helpers, the practical impact is zero because (a) quiz_sessions.{subject_id,topic_id} are nullable so an easa deletion that left those null would only cause a FK violation on the non-null subjects/topics deleted last, and more importantly (b) cleanupTestData already deletes quiz_sessions before cleanupReferenceData runs — so the FK is satisfied by run order regardless. The plan's stated rationale is wrong but the design is still safe; flag as SUGGESTION not ISSUE. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |

| Spec-count updates that only update one line in a steering doc while siblings carry the same stale number. tech.md has THREE occurrences of "18 adversarial Playwright specs" (lines 85, 146, 183). Plans that update only line 146 leave two stale counts behind. Always grep the full steering doc for the exact number string before scoping line edits. | 2026-06-06 | 1 | 2026-06-06 | WATCHING |

| Red-team plans that exercise `record_auth_event` assert `/not authorized/i` for the admin-only event-type gate, which matches mig 093 line 66 (`RAISE EXCEPTION 'not authorized'`). BUT the anon auth check uses lowercase `'not authenticated'` (mig 093 line 42) while `get_session_reports` uses capitalized `'Not authenticated'` (mig 091 line 48). Plans must read the exact RAISE string in the LATEST def of EACH RPC; do not assume the same casing across different RPCs. CQ assertion in server-action-unauthenticated.spec.ts must match `/not authenticated/i` (lowercase) for record_auth_event and `/not authenticated/i` (capitalized N) for get_session_reports — both match case-insensitively so the plan's assertions are technically safe, but the inconsistency is a risk if a case-sensitive match is accidentally used. | 2026-06-07 | 1 | 2026-06-07 | WATCHING |
| #781 cookie-rewrite approach for @supabase/ssr: `getUser()` (called in proxy.ts) routes through `_useSession` → `__loadSession` which reads the session from the SSR cookie storage and refreshes only when `expires_at * 1000 - Date.now() < EXPIRY_MARGIN_MS` (= 90 000 ms). The Playwright cookie-rewrite approach (set `expires_at` to a past unix ts) DOES trigger a refresh on the next `getUser()` call because `hasExpired=true` at `__loadSession:2354`. The approach is sound. The fallback (jwt_expiry lowering in config.toml) is a valid secondary option but should not be necessary. Flag: supabase/config.toml's jwt_expiry is a local-dev setting only — CI Supabase in docker uses its own config; lowering it affects the entire CI run, not just the redteam project. Plans proposing this fallback must document the CI blast radius. | 2026-06-07 | 1 | 2026-06-07 | WATCHING |

| E2E un-skip plans for AlertDialogAction with `isPending` text-flip: `AlertDialogAction` is a plain `<Button>` (not a `AlertDialogPrimitive.Close` wrapper). Clicking it fires `onClick` but does NOT dismiss the dialog — the dialog stays open while `isPending=true` (button says "Deleting…") and closes only after the Server Action resolves and the parent re-renders. `name:'Delete'` safely matches the pre-transition text only; the button is disabled during transition so a second click can't race. The dialog stays visible during the transition — plan's `toHaveCount(0)` assertion is only safe AFTER the toast is visible (which is already gated first). Plan was correct on all assumptions; positive signal. | 2026-06-09 | 1 | 2026-06-09 | WATCHING |
| Red-team spec-split plans that classify tests as "probe-only/no cleanup" without reading the test body. Confirmed in #796: rpc-cross-tenant BY tests (lines 191-252) INSERT egmont fixture rows (internal_exam_codes + quiz_sessions) and set describe-scoped cleanup vars — these are NOT probe-only. A plan that routes BY to "isolation file (no afterAll needed)" leaves the reports file's afterAll holding stale ID variables it can't reach, and the isolation file missing cleanup it needs. Before labeling any test "probe-only", read the test body for adminClient.from(...).insert/upsert calls. | 2026-06-09 | 1 | 2026-06-09 | WATCHING |
| Spec-split plans that propose attack-surface.md Spec-File column updates without checking whether the spec-label in the TEST matches the Vector ID in the MATRIX. In #796: rpc-cross-tenant.spec.ts tests are labeled "Q (#276)" and "X (#384)" but attack-surface.md Vector Q = question_comments unauth (server-action-unauthenticated.spec.ts) and Vector X = user-consents-isolation.spec.ts. The cross-tenant tests are supplementary coverage, not the primary-spec row. Updating the matrix Spec-File to the new split file would overwrite the correct primary-spec entry with a secondary one. Always grep attack-surface.md for the Vector ID AND confirm the description matches the test before adding Spec-File update instructions. | 2026-06-09 | 1 | 2026-06-09 | WATCHING |
| File-split plans with "possibly" on the key helper extraction underestimate whether the primary file drops under the target ceiling. In #796: audit-completeness.spec.ts (704L) splits off ~126L of CT tests → 578L remaining, still over 500. The plan's "possibly audit-assertions.ts" for 2 helpers saves only ~42L → 536L, still over 500. ALL 6 spec-local helpers (150L total) must move to pass. "Possibly" on a helper whose extraction is the ONLY path to the target must be hardened to "definitely". | 2026-06-09 | 1 | 2026-06-09 | WATCHING |

## Positive signals (what good plans got right)
- #326 agent-health.yml false-positive fix: plan verified all 5 edits against actual code before recommending. Attack-surface path EDIT 1 exactly correct (only topics/ copy exists). Check 5 removal scope complete (all 4 SEC_SECTION references enumerated: lines 172, 175, 211, 228). Check 3 awk fields verified against actual 6-column learner table ($2=Issue Type, $3=Count, $5=Status). sed stem `s/(ing|s)$//` confirmed safe — only "testing"→"test" changes match status; "next.js"→"next.j" is a harmless substring (still matches "Next.js" in coderabbit.yaml). One SUGGESTION: EDIT 5's `sed -E 's/(ing|s)$//'` applied to "next.js" yields "next.j" — plan should note this is a benign substring match, not a break.
- Cast-guard sweep (#677): plan correctly identified all 3 unguarded `rpc<T[]>` sites and verified 7 already-guarded siblings. Line numbers exact. Test scope correct. Only minor issue: cited `check-consent.ts` at wrong path (`lib/queries/` vs actual `lib/consent/`) and `load-session-questions.ts:50` uses `!data?.length` guard (not `Array.isArray`) — a different-but-safe pattern, not a reference-compliant sibling.
- #673 tasks.md: line numbers (seedRedTeamAdmin:107, upsertUser:339, VICTIM_EMAIL:4-6, getAdminClient:25), file paths, and ordering all correct. Design faithfulness high — no contradictions on the substantive algorithmic decisions (round-robin, single-snapshot dates, sentinel idempotency, 'a' literal, streak offsets, no-cleanup rationale). Key issue: attack-surface.md status-flip (BW1-3/BX1-7/CA/CB → COVERED + new BX7 row) and upsertUser admin-param omission in Task 1 description body (not caught by the task's _Leverage reference to line 107). Also: victim creds sourcing underspecified in Task 5 prompt (should name VICTIM_EMAIL/VICTIM_PASSWORD or seedRedTeamStudent() call explicitly).
- #789 recordAuthEvent extraction: plan correctly read all 4 action files and matched console.error prefix strings verbatim. RPC arg-shape claim (no p_metadata when absent) verified against all 4 exact-match test assertions. Vi.mock scope analysis for the sibling split was accurate (module-scope mocking applies to transitive imports). Migration 093 signature matched on all 3 params (p_event_type, p_resource_id, p_metadata DEFAULT '{}'). One ISSUE found: wrong file path for settings/actions.ts (plan says app/settings/actions.ts but actual is app/app/settings/actions.ts).
- #367 un-skip soft-delete E2E test: all six assumptions verified correctly. AlertDialogAction role=alertdialog confirmed via Base UI handle.js. Hermiticity reasoning for restoreSeededQuestionsState exact. Text truncation (90 chars) correctly cleared for 53-char marker text. Toast regex /Deleted/ correct. No red-team trigger paths touched. deleted_at IS NULL query filter in getQuestionsList confirmed. APPROVED clean with one SUGGESTION (add an explicit timeout before toHaveCount(0)).
- #792/#793 hygiene PR: all five verified facts were accurate — CU/CV/CW unused in matrix, line-3 BL/BM/BN is #611 score-forgery family, spec describe-block label is purely cosmetic (no assertion depends on it), archive has exactly 3× each duplicate per the intentional section structure, code-style.md §7 does contain the RPC output contract rule at line 735. Plan correctly scoped archive as read-only. APPROVED clean.
- #326 attack-surface matrix registration: spec-count stays 39 (files already on disk); existing BE/BF/BG/BH/BI rows reference specs by filename only → no edits needed; Vector Z correctly kept GAP (orthogonal authenticated vs unauthenticated paths); unlabeled tests in rpc-issue-internal-exam-code correctly identified as intentionally unlabeled. 2 ISSUEs (spec-count scoping + occupied-row disambiguation) → REVISE. **STALE-BRANCH MISS (corrected at execution, not by the critic): the plan was drafted against a pre-#802 branch where CW was the matrix max, so it allocated CX–DC — but #802 had already merged CX–DG to master. Caught only when reading the live header during execution; re-allocated to DH–DM. Lesson: branch off master FIRST, then verify the matrix — Explore/critic run against a stale working tree produce stale max-ID/collision findings.**

| Idempotent-resume RPC race-handler: when wrapping an INSERT in BEGIN...EXCEPTION WHEN unique_violation, a function that is idempotent-resume (returns existing session on race) must do a re-read + RETURN in the handler — NOT mirror the siblings' raise-only shape. start_internal_exam_session (mig 070) and start_exam_session (mig 088) both just RAISE 'active_session_exists' because they are not idempotent. start_vfr_rt_exam_session IS idempotent — the handler must re-read via a SELECT identical to step 5 and return the winner's session. Plans that say "mirror whichever sibling fits best" without explicitly noting this divergence will produce a raise-only handler that breaks idempotency on concurrent first-calls. | 2026-06-10 | 1 | 2026-06-10 | WATCHING |

## Topic pointers
- [tracker-archive](topics/tracker-archive.md) — relocated verbatim: older single-occurrence (count=1) tracker rows + older positive signals (moved 2026-06-07 to stay under the 25 KB injection cap).
