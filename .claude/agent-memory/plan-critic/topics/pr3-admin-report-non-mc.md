# W1 PR 3 — admin report correctness for non-MC sessions (#991 + #990), 2026-08-24

> Full text of the tracker rows first logged during the plan-critic rounds on `fix/admin-report-non-mc`.
> `MEMORY.md` carries one-line summaries; the mechanism and evidence live here.

| Pattern | Status |
|---|---|
| A plan mirrors a SINGLE-SESSION DETAIL component onto a LIST row and calls it "matching the convention" — the three existing session-LIST surfaces (`reports/_components/session-table.tsx:96`, `session-card.tsx:34`, `admin/internal-exams/_components/attempts-table.tsx:126`) all render `correct/total` with NO mode branch. Classify the sibling by SURFACE KIND (list vs detail), not by the identifier it renders. |  WATCHING |
| A plan proposes a "verbatim mirror" of a sibling without checking the SIBLING is correct — `result-summary.tsx`'s `isExam ? totalQuestions` branch rendered VFR RT exams as e.g. 29/25 (removed 2026-08-24) (item-level `correct_count` from `20260815000300_submit_vfr_rt_fuzzy.sql:310`, question-level denominator). Verify the prior art before copying it; "mirrors X" is a claim about X. |  RESOLVED-WATCH (rev adopted the fix as its §C) |
| A plan names N test files as "asserting the behaviour we're changing" when NONE of them do — `result-summary.test.tsx` / `report-card.test.tsx` default `mode:'quick_quiz'` and `report-view.test.tsx` MOCKS the child, so `result-summary.tsx:76`'s `isExam` ternary has ZERO coverage. Inverted-claim tell: the plan budgets test UPDATES where the real need is new coverage (§7). Open each cited test and check the FIXTURE selects the branch, not just that the identifier appears. |  WATCHING |
| A plan claims a row-shape test breaks when a type gains a field, without reading the MATCHER — `attempts-queries.test.ts:85` is `toMatchObject` (tolerates extras → new field lands unasserted/vacuous) while `students/[id]/queries.test.ts:234` is `toEqual` (really breaks). Grep `toEqual(` vs `toMatchObject(` before claiming either. |  WATCHING |
| A plan's chosen REMEDY contradicts the explicit acceptance criteria of the issues it closes — #991/#990 both say "MC-only sessions keep their current behavior unchanged"; the plan moves MC from `15/25` to `15/20` and to engagement framing. Re-read each closed issue's AC against the final display, not against its Problem section. |  WATCHING |
| A plan calls a rendered expression a CRITICAL defect without grepping docs for a deliberate decision — `docs/plan.md:1437` states the exam header is intentionally `{correct items} / {totalQuestions}`. Neither cited nor scheduled for update, so the change would silently falsify a design record. Grep `docs/plan.md` + `decisions.md` for the surface before classifying it as a bug. |  WATCHING |
| A plan adds a per-row aggregate over a child table to a PAGINATED list via one `.in(ids)` + group-in-TS, without checking PostgREST `max_rows=1000` (`supabase/config.toml:18`) — 25 sessions x ~40 answer rows silently truncates, shrinking the very denominator the fix exists to correct. Repo answers: `fetchAllRows` (`lib/supabase-paginate.ts:15`) or an RPC (`get_admin_dashboard_students`, database.md:2626, which replaced exactly this TS-merge shape after #682/#668). |  RESOLVED-WATCH (rev2 adopted `fetchAllRows`; see the ORDER-BY + error-vs-zero rows below, which the switch introduced) |
| A plan adopts `fetchAllRows` but specifies only the PAGE select, omitting the deterministic `.order()` its contract demands (`supabase-paginate.ts:10` "deterministic total order"; both existing callers add `.order('id')`). Unordered `.range()` paging can repeat and OMIT rows across pages — a Set-based distinct count absorbs the repeats and silently UNDERCOUNTS the omissions, reproducing the truncation defect the switch was made to fix. Specify count-callback AND page-order, not just the columns. |  RESOLVED (rev3 dropped `fetchAllRows` on lists for a PostgREST embedded aggregate `child!fk(count)` — measured: immune to `max_rows` AND to an explicit embedded `limit`, and NOT gated by `db-aggregates-enabled` (`sum()` returns PGRST123 on the same instance where `(count)` works), so no client-side paging exists to mis-order) |
| A plan replaces a `?? fallback` with a Map lookup and never says what callers render on MISS — `map.get(id) ?? 0` conflates "answered zero" with "query errored", printing `0/N` for every row. Make the error a render decision (`—`), not a zero. |  RESOLVED-WATCH (rev3's `—` rule covers it; the embed errors the whole select rather than degrading, so the miss branch is unreachable) |
| A revision that REPLACES a design leaves the superseded design's REQUIREMENTS standing as if still binding — rev3 dropped the `.in()`-based helper for an embedded aggregate, but kept "must import `adminClient` directly because `OffsetChainBuilder` has no `.in()`" (true of the type, irrelevant to the new design) and a test-hole mechanism phrased around "the helper". After a redesign, re-derive every downstream instruction; a still-true CLAIM is not a still-live REQUIREMENT. |  WATCHING |
| A plan asserts "the generated types may not model X" and prescribes a cast/guard on that premise, without running tsc — postgrest-js 2.112 types `child!fk(count)` exactly as `{count:number}[]`. Also: an RLS-narrower CHILD table makes an embedded aggregate return a silent `0` (not an error, not `[]`) when the outer row is visible but the child rows are not — probe with a real JWT per role before claiming the count is the caller's own. |  WATCHING |
| A plan adds a COLUMN to a table component and lists the header + cell edits but not the empty-state `colSpan` (`attempts-table.tsx:56` `colSpan={7}`). Grep `colSpan` in the touched component whenever the column count changes. |  WATCHING |
| A plan justifies changing a display expression by pointing at a compensating stat on the same card WITHOUT opening the component — `result-summary.tsx:85` renders Skipped under `{!isExam && ...}`, so the exam branch (the only branch being changed) has no such stat, and mobile (`:97-114`) has none at all. Open the JSX and confirm the compensating control renders in the branch you are changing. |  WATCHING |
| A plan cites a migration as PRECEDENT for adding a computation that the same migration DELETED, with a measured rationale — `20260606000007_get_session_reports_drop_unused_answered_count.sql:1-12` removed this exact per-session answered count (~12 ms/page, EXPLAIN ANALYZE, #471) as unused; the plan re-adds it on the same student-facing surface as extra client round-trips. Read the migration's HEADER, not just its effect on the signature. |  WATCHING |
