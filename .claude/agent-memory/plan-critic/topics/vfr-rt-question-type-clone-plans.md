---
name: vfr-rt-question-type-clone-plans
description: Plan-review lessons for plans that clone an existing question type (dialog_fill/ordering) to a new one, or reuse quiz UI components for a new mode.
metadata:
  type: project
---

## VFR-RT question-type CLONE plan lessons

Relocated verbatim from plan-critic MEMORY.md (curated to stay under the 25 KB native-injection cap). Plan-review lessons for plans that clone an existing question type (dialog_fill/ordering) to a new one, or reuse quiz UI components for a new mode.

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---|---|---|---|---|
| UI-component-reuse plans (Study Mode flashcard tab reusing `AnswerOptions` app/_components/answer-options.tsx) assume `correctOptionId`+`disabled` reveals the correct option — WRONG: the green-correct highlight is gated on `showResult = lockedSelection != null && correctOptionId != null` (L66; lockedSelection = the `selectedOptionId` prop, L59). The plan's `selectedOptionId=null` → showResult=FALSE → NOTHING highlighted, flashcard renders blank; `disabled=true` also blocks any click-to-select so it never becomes non-null. FIX: pass `selectedOptionId = correctOptionId` (renders the key green via isCorrect, L77/38-39; submit button auto-hidden when showResult L113; `isExam` must stay falsy). 2nd pattern same plan: reusing `getRandomQuestionIds`→`_filtered_question_pool` (mig 20260614000001 L36-92) for a type-narrowed RPC MISSES that the pool has NO `question_type` clause → a MC-only `get_study_questions` (WHERE `question_type='multiple_choice'`) SILENTLY DROPS non-MC IDs → runner returns < requested `count` for mixed-type subjects (dormant now [no non-MC prod data], live once RT/short_answer/dialog_fill/ordering import). FIX: filter type at ID stage or over-fetch+document. 3rd: new files mirroring `quiz-config-form.tsx` (already 151 ln >§1 150-cap) / `use-quiz-start.ts` (83 ln >§1 80-hook-cap) inherit the over-limit → budget split upfront. POSITIVE (confirmed correct): new arbitrary-client-ID answer-exposure RPC REQUIRES `deleted_at IS NULL` (NOT §15 immutable-col carve-out — that needs session-frozen IDs); plan's guard set (auth-null/active-user+org/soft-delete/search_path/GRANT) clones get_quiz_questions mig 136 correctly; 42702 alias note correct; `correctOptionId` as own RETURNS TABLE col matches the separate `AnswerOptions` prop (L19); `useFlaggedQuestions` useEffect-fetch (use-flagged-questions.ts:14) is established session-runner precedent, not a new no-useEffect violation. Verdict: REVISE (0 CRIT, 2 ISSUE = reveal-mechanic + mixed-type drop; 1 SUGG line-budget). First seen: Study Mode plan. **Recurrence (Phase 6 diagram_label clone of ordering PR #998, 2026-07-02): question-type-CLONE plans systematically OMIT 3 shared touch-points the source PR changed** — (1) `apps/web/app/app/_types/session.ts` `QuestionType` union + SessionQuestion delivery field (imported by ~10 session-path files: session-answer-block, active-session, use-session-state, session-operations — the new literal is unassignable there → tsc break; loader `load-session-questions.ts` uses its OWN inline union L19/L33 so plan's loader item does NOT cover this); (2) `apps/web/app/app/quiz/actions/load-draft-helpers.ts` `toFeedbackEntry` needs the new `case` — without it a DB-draft resume returns null and `toFeedbackRecord` discards the WHOLE draft's feedback (also semantic-reviewer sibling-validator-parity count=3); (3) the fail-closed default-branch TEST that used the NEW type as its "unknown/unsupported" fixture flips to rendering the real control once wired (`quiz-main-panel.test.tsx:528` hardcodes `question_type:'diagram_label'` expecting the "not yet supported" alert) → must swap fixture to a still-unwired type; plan listed no test update. 4th: `answer-input.tsx` already 137 ln → adding a per-type sub-component (~27 ln like `OrderingAnswer` L45-71) + `case` breaches the 150 component cap; plan lists it as a wire-point but budgets NO §1 same-commit extraction. VERIFIED-CORRECT in this plan: DROP FUNCTION for both get_quiz_questions + check_non_mc_answer (ordering migs 145 L19 / 146 L20 precedent; current check_non_mc_answer is 5-arg `(uuid,uuid,text,jsonb,jsonb)` so "DROP 5-arg CREATE 6-arg" is right — DROP must name the exact current sig); report `never` check (report-answer-body.tsx:49) forces the branch; dnd-kit deps ALREADY installed P5 (package.json needs no change) — diagram needs @dnd-kit/core useDroppable+useDraggable (clone DndContext+PointerSensor/TouchSensor/KeyboardSensor L57-60 from ordering-input.tsx) NOT @dnd-kit/sortable; REVOKE-by-grant-omission discipline sound. dnd under-spec: collisionDetection should be pointerWithin/rectIntersection for drop-zones, not ordering's closestCenter. | 2026-06-26 | 2 | 2026-07-02 | WATCHING — question-type-clone plans omit _types/session.ts + load-draft-helpers.ts + fail-closed-test-fixture flip + answer-input.tsx cap |
| Drag-drop-question-type CLONE plans (VFR-RT Phase 6 `diagram_label` cloning `ordering` migs 143–149 / 5 integration files) systematically UNDER-SPECIFY the execution-only SQL + UNDER-ENUMERATE the CHECK-rejection/REVOKE/type tests that the clone SOURCE already spends ~25 of 41 tests on. Recurring gap set: (1) new-type validator in a table CHECK (`is_valid_diagram_config`) — must jsonb_typeof-guard EVERY scalar before `->>`/`::numeric` cast (numeric coord `(e->>'x')::numeric` on non-number → 22P02 abort NOT clean 23514) + CASE-wrap EVERY `jsonb_array_elements/length` over EACH array (diagram has 3: zones/labels/answer vs ordering's 1) for totality (non-array → 22023 abort NOT 23514); plan saying "CASE-wrapped (per ordering CR)" generically is insufficient — name the coord-cast trap. (2) plan test list omits the CHECK-REJECTION suite the source has 9 of (`rpc-get-quiz-questions-ordering` L275-485: dup-id/blank-text/non-string/whitespace/missing-key/extra-key-smuggle/non-array) → enumerate diagram analogs incl. non-number-coord→23514, out-of-range→23514, answer.zone∉zones, answer.label∉labels, coverage≠#zones. (3) omits the answer-key COLUMN-REVOKE-by-omission test (source L224-273: student `.select('id,<keycol>')`→42501 WITH positive-control `.select('id')` row-visible + admin-reads-key non-vacuity) — the actual answer-exposure surface. (4) omits the `_grade_record_*` REVOKE tests (source `rpc-batch-submit-quiz-ordering` L349-415: authenticated+anon, signature-valid payload + admin positive-control so PGRST202=REVOKE not arg-drift). (5) grader must RAISE on a submitted id absent from config (unresolved label→text NULL→`*_answer_shape_check` branch-2 violation at execution — same class as mig 120 23502-NULL-is_correct bug; source mig 147 L66-69). (6) check_non_mc plan says "non-whitelist mode" SINGULAR but source rejects BOTH mock_exam AND internal_exam separately (L252-318) + has 5 `answer_type_mismatch` + a non-array-payload→mismatch-code≠22023 guard (L391-436). (7) **BEHAVIORAL INVERSION trap: diagram ALLOWS partial submission + distractors; ordering's batch self-defence REQUIRES a complete permutation (`count(*)=N AND count(DISTINCT sel)=N`, mig 148 L203-214, "omits a slot" reject L329) — cloning it verbatim either wrongly rejects partial diagrams (functional break) or lets forgeries through. Correct diagram guard = `zone_ids⊆config ∧ distinct-zone ∧ distinct-label`, NO `=#zones`.** Distractor test set the plan omits: partial-accepted, distractor-on-zone=wrong-BUT-persists-row (distractor IS in labels→resolves, unlike garbage id), leftover-chip-no-row, double-placed-label-reject, forged-zone∉config-reject. (8) 4 RPCs change signature/RETURNS → generated `packages/db/src/types.ts` Functions registry stale (doc-drift not runtime break — `rpc<TResult>` caller-generic; SAME as next row's finding) → add to Files-affected. (9) label shuffle IS a real leak guard here (if seed authors labels[i]↔zones[i], unshuffled labels leak the mapping positionally) — NOT cosmetic like it'd seem; assert non-vacuous shuffle + de-correlate seed. Doc-count: bump ONLY plan.md line-5 header literal (was `311`), frozen snapshots L35/51/83 excluded. Next Decision # correct (52; dnd-kit already Decision 50, per-zone storage is a spec deviation like ordering N7→Decision 51). Verdict: REVISE (0 CRIT, 8 ISSUE = coord-22P02-spec + CHECK-reject-tests + col-REVOKE-test + grader-REVOKE-tests + distractor/partial-inversion + check_non_mc-modes/mismatch + unresolved-label-guard + types.ts; 4 SUGG). First seen: diagram_label. | 2026-07-02 | 1 | 2026-07-02 | WATCHING — drag-drop-type clone under-enumerates execution-only SQL + CHECK-reject/REVOKE/distractor tests; behavioral-inversion (partial-submit vs complete-permutation) self-defence trap |
| RPC-signature-change plans (RETURNS TABLE DROP+recreate, e.g. VFR-RT Phase 2 `get_quiz_questions` +question_type/dialog_template/blanks_safe) claim "caller = X ONLY" but MISS the generated `packages/db/src/types.ts` `Database['public']['Functions']['<fn>']` registry (`get_quiz_questions` @ types.ts:1429-1445 still lists OLD 12 cols). NOT a runtime/typecheck break: app's `rpc<TResult>` helper (`apps/web/lib/supabase-rpc.ts:13-20`) casts via `supabase as unknown as {rpc}` to a CALLER-SUPPLIED generic, never consults the registry → stale entry = doc-drift, not contract break (WHY such plans "work" untouched). FIX: add `packages/db/src/types.ts` to Files-affected; regenerate or flag doc-updater. ALSO this plan family: (a) N5-style validator-deferral plans justify "no UI produces non-MC yet" while the SAME task widens the type (`DraftAnswer` types.ts:88) — verify the actual answer-Map PRODUCER not the type: `use-answer-handler.ts:50` exposes only `handleSelectAnswer(optionId)` writing MC-only `{selectedOptionId,responseTimeMs}` (L56-59,90-93); `handleTextAnswer` lands Phase 3.2 → deferral SAFE but restate basis as "sole producer MC-only until 3.2". (b) integration plans assert RPC strip-output but OMIT the TS MAPPER pass-through assertion (load-session-questions.ts:54-62) where N1 "silently inaccessible" risk lives — name updating existing `*.test.ts` (new fields surface + MC→null); under-spec §7 ≥2-distinct-seed output-contract for new graders (`check_non_mc_answer`: 2 short_answer canonicals + per-blank `{index,is_correct,canonical}` length). (c) N2 internal-helper deviation acceptable on merits BUT binding spec note `tasks.md` N2 needs SPEC UPDATE before impl per deviation rule, not just a plan flag (security lens covers the GRANT/PUBLIC hazard separately — see next row). (d) **SQL-mechanics LATERAL trap (final-decision round, REVISED plan): the existing `get_quiz_questions` shuffles MC options via CROSS JOIN LATERAL `jsonb_array_elements(q.options)` + GROUP BY (mig 20260327000059 L50-61). `questions.options` is NOT NULL DEFAULT `'[]'::jsonb` for short_answer/dialog_fill (mig 20260610000100 L85-89) → `jsonb_array_elements('[]')` yields ZERO rows → the CROSS JOIN ELIMINATES the entire non-MC question row before GROUP BY → every short_answer/dialog_fill question SILENTLY VANISHES from the result (passes tsc/biome/db reset; runtime "no questions found").** Plan 2.1's "MC options shuffled (already present)" wording INVITES keeping the LATERAL — contradicts its own "verbatim mig 105" cite, which uses a CORRELATED SUBQUERY in `CASE WHEN question_type='multiple_choice' THEN (SELECT jsonb_agg(...) FROM jsonb_array_elements(q.options) opt) ELSE NULL END` (mig 20260611000100 L134-140, NO LATERAL/GROUP BY, preserves non-MC rows). FIX: plan must EXPLICITLY say replace LATERAL+GROUP BY with the mig-105 subquery form + drop GROUP BY; 2.4 must assert a mixed-type session returns ALL questions incl non-MC rows (current 2.4 checks column-stripping only — would miss a dropped row). RULE: when a plan extends an aggregating RPC to NEW question types, check whether the existing shape uses row-multiplying LATERAL/CROSS JOIN that drops rows for empty/NULL arrays. Verdicts: round-1 APPROVED (0 CRIT, 3 ISSUE non-blocking); FINAL-DECISION round REVISE (0 CRIT, 1 ISSUE = LATERAL trap — blocking). First seen: VFR-RT Training Phase 2. **Recurrence (Phase 6 diagram_label, 2026-07-02, SECURITY lens): clones ordering migs 143–149. Column-grant omission VERIFIED sufficient for diagram_config (base grant 20260610000100 L130-154 is a COLUMN-LIST grant not table-level; a later ADD COLUMN is auto-gated by omission — ordering_items precedent + no later blanket `GRANT SELECT ON questions`; only an explicit `GRANT SELECT (col)` like has_calculations mig 20260611000300 re-opens one → plan's "do NOT add diagram_config to GRANT SELECT" is exactly right). get_quiz_questions projection drops `answer` (sound, mirrors mig 145 shuffle). Grader REVOKE FROM PUBLIC, anon, authenticated present + necessary (mig 147 L96 pattern; FROM PUBLIC alone insufficient on Supabase). Loosened self-defence (distractors, partial allowed) has NO over-score exploit GIVEN all 4 invariants — zones⊆config + distinct zone_id + distinct label_id + distinct blank_index (dup guard mig 148 L167-176) — credit=correct_rows/#zones ≤1.0 (LEAST cap) and is_correct is server-computed. NEW TRAP for multi-key fan-out clones (ISSUE): diagram fans out 3 client fields per zone (zone_ordinal→blank_index, zone_id→answer lookup, label_id) vs ordering's 2 (slot, item_id). If zone_ordinal is client-supplied INDEPENDENT of zone_id, the (question_id,blank_index) dup guard and the "no dup zone" self-defence protect DIFFERENT keys, AND get_report_answer_keys (clone of mig 149) keys answer_key by CANONICAL config zone index while stored rows carry client ordinal → report shows wrong/misaligned answer keys. FIX: derive blank_index = canonical index of zone_id in config.zones SERVER-SIDE (don't trust a client ordinal). Also: grader clone must RAISE on unknown label_id/zone_id + validate ordinal range (mirror mig 147 L48/L59-69) — plan only said "resolves label_id→text". Also: p_mapping answer_type_mismatch PARITY — each existing branch (short/dialog/ordering, mig 146 L151/L178/L256) must add `AND p_mapping IS NULL`. SEED opaque-id invariant (like ordering): zone_id and label_id must NOT share a naming scheme or the pairing leaks even with `answer` omitted (soft invariant, not guard-enforced). Verdict: REVISE (0 CRIT, 3 ISSUE = ordinal↔zone_id correspondence/report-align + grader-reject-unknown + p_mapping parity; 2 SUGG).** | 2026-06-21 | 3 | 2026-07-02 | RULE CANDIDATE (3) — (1) LATERAL row-drop on new types; (2) multi-key fan-out clone: derive positional blank_index server-side from the identity key, never trust an independent client ordinal (dup-guard/self-defence key-split + report misalignment) |
| Report query SELECT string not updated alongside AnswerRow type widening: plans that add response_text/blank_index to the TypeScript AnswerRow type but don't update the actual `.select('question_id, selected_option_id, ...')` SQL string in quiz-report-questions.ts leave response_text always undefined at runtime (TypeScript sees the type, runtime gets undefined). First seen: vfr-rt-training spec task 4.2 — names `AnswerRow +=` but doesn't name the line-65 SELECT string change. Plans must name the exact SELECT columns to add, not just the type. | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| RETURNS TABLE extension requires DROP+CREATE, not CREATE OR REPLACE. Confirmed in `20260327000059` comment: "Must DROP first because CREATE OR REPLACE cannot change return type." Plans that say "CREATE OR REPLACE" to add columns to a RETURNS TABLE will fail at migration apply time with Postgres error. First seen: vfr-rt-training spec G1. | 2026-06-20 | 1 | 2026-06-20 | WATCHING |
| Session-storage validator whitelist not widened when adding new exam mode to `exam-modes.ts`. `quiz-session-validators.ts` line 48 hard-checks `v === 'mock_exam' \|\| v === 'internal_exam'` for `examMode`. Adding `vfr_rt_exam` to EXAM_MODES/QuizMode without also updating this validator means sessions with `examMode:'vfr_rt_exam'` fail the storage round-trip and lose exam state on tab refresh. Plans that change exam-modes.ts must include quiz-session-validators.ts in "Files affected". First seen: #697 Phase B plan. | 2026-06-19 | 1 | 2026-06-19 | WATCHING |
