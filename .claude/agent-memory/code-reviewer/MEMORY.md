# Code Reviewer — Memory

> Native subagent memory index. Tracker first, durable knowledge second, topic pointers last.
> Update rows/bullets IN PLACE. No session logs — git holds history (`git log -p`).

## Recurring Issues Tracker

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---------|-----------|-------|-----------|---------------------|
| Function > 30 lines in Server Action | 2026-03-20 (d93f924) | 2 | 2026-03-20 (6520962) | PROMOTED → code-style.md §3 (getFilteredCount 58L; toggleFlag 52L — both 2-branch toggle/conditional shape, fixed by extracting named helpers) |
| New hook/utility file shipped without co-located test | 2026-03-21 (8771aa2) | 4 | 2026-03-23 (b104ae4) | PROMOTED → code-style.md §7 (use-comments.ts, use-flagged-questions.ts, score-color.ts, reports-utils.ts) |
| Hook file > 80-line limit | 2026-03-21 (8771aa2) | 4 | 2026-04-27 (c656868) | PROMOTED → code-style.md §1 (use-comments 84L; use-quiz-config 110L; use-quiz-state 121L; use-session-bootstrap 99L). Fix = extract orchestration/derivation helper |
| React component > 150-line limit | 2026-03-21 (8771aa2) | 5 | 2026-06-14 (20de7c29) | PROMOTED → code-style.md §1 (comments-tab 155L; finish-quiz-dialog 243L; quiz-session 176/204L→155→161L; quiz-controls 152L). Fix = extract sub-component/helper component. quiz-session.tsx worsened again: ~155→161L (+6L comment block + prop change for #886 fix). finish-quiz-dialog.tsx 311→323L (aca9ba8f, +12 net from pendingAction threading + DialogFooter expansion — pre-existing violation, not worsened in intent; see durable note on file-level vs export limit; SPLIT CANDIDATE). |
| Feature modes (study/exam, draft/live) conditionally wired into one hook/component | 2026-04-13 (exam PR2) | 2 | 2026-04-26 (34194aa) | RULE CANDIDATE — extract mode-specific logic to its own hook/component once a file passes 120L (hook) or trends toward 200L (component). Single component switching render on an `isExam` prop is acceptable at small scale |
| Server Action file > 100-line limit | 2026-03-20 (d93f924) | 2 | 2026-06-07 (e9c2a36b) | RESOLVED (toggle-student-status.ts split to 55L ✓ via e9c2a36b — mutations extracted to toggle-student-status-mutations.ts 78L, both under limit). lookup.ts 112L still WATCHING. |
| Utility function > 30 lines (single query RPC) | 2026-05-31 (c879a259) | 8 | 2026-06-20 (5c836041) | WATCHING (getSessionReports 47→64L; forceTokenRefresh 44L; insertShortAnswerQuestion/insertDialogFillQuestion/getRtRefs 32-33L; insertMcQuestion 35L — all E2E/integration infra, non-blocking structural exception. c993b7de (Phase C): getVfrRtInProgress 46L, buildVfrRtPayload 37L, useVfrRtAnswers 39L — single-concern sequential/dispatch, same structural shape; WARNING, non-blocking). 5c836041 (C.5): getVfrRtResults 75L — 2-RPC graceful-degradation chain + Map merge; same single-concern structural shape; WARNING, non-blocking. cleanupReferenceData resolved. |
| Component deep nesting > 3 levels | 2026-03-27 (75ffa51) | 1 | 2026-03-27 | WATCHING (ConsentForm: form→div→div→Checkbox+span→a, repeated 3×; fix = extract ConsentCheckbox sub-component) |
| Utility file > 200-line limit | 2026-04-27 (c656868) | 2 | 2026-06-06 (ade08109) | RESOLVED (setup.ts) — split into setup.ts 89L / seed.ts 134L / cleanup.ts 85L (ade08109, closes #778). quiz-session-storage.ts 251L still WATCHING. new helpers/cleanup.ts 195L + helpers/audit-helpers.ts 197L are both under 200L (b95360f0). |
| E2E Playwright hermiticity (hard-delete without restoration) | 2026-04-30 (from §7 testing rule) | 2 | 2026-04-30 (admin-students, admin-questions pre-mig 083) | RESOLVED — distributions-rls.spec + report.spec + admin-dashboard.spec (all 2026-06-04, ad19626) apply hard-delete cascade correctly with `.beforeEach/.afterEach + createdIds.clear()` pattern; no recurrence since rule promotion. session-race-condition.spec (PR-4, 2026-06-06) newly hermetic with afterEach soft-delete + `createdSessionIds` array (session-replay was already hermetic on master) |
| `waitForTimeout` in E2E specs | 2026-06-06 (test/isolation-hygiene) | 1 | 2026-06-06 | WATCHING (exam-recovery.spec.ts: waitForTimeout(300) replaced with `waitFor`+attribute assertion — fix already applied in this commit, tracking for recurrence) |

> Count increments only on a **distinct** mechanism. Rows transition state, never deleted.
> Four "watching" count=1 rows have not recurred — that non-recurrence is itself a positive signal.
> Commit 3260e2d9 (2026-06-10): docs-only spec update — no code pattern violations. All migration timestamps verified against actual files.
> Commit 76a9b03e (squash of pre-rebase 5f561ddf, 2026-06-10): VFR RT Phase A — 14 migrations + 4 integration tests. Clean. Three SECURITY DEFINER migrations at 299-300L (095c=300, 100=299, 102=299) — within cap or at exact limit, exception confirmed. Integration test files at 223/598/673/769L — all within 500L test-file ceiling. No pattern violations.
> Commit f8898771 (2026-06-10): Phase A CR hardening — comment/CHECK additions to 4 migs + mirror pairs + docs. Clean. 095c 300→304L (SECURITY DEFINER exception applies; 4 added lines are load-bearing §15 rationale comments). No pattern violations.
> Commits fcef799b + e179093b (2026-06-10): mig 094 doc sync + constraint-regression integration tests. Clean. Test file 389L (within 500L ceiling). Three new it() titles behavior-first, pass all §7 disallowed-title checks. Hermiticity via beforeAll/afterAll + cleanupTestData pattern. No pattern violations.
> Commit aed96dcb (2026-06-10): #831 org-filter fix — mig 099b 117L (clean, within 300L cap), docs/attack-surface 1-line updates (exempt). rpc-vfr-rt-start.integration.test.ts 769→823L: worsens pre-existing 500L test-file ceiling violation (+54L for non-vacuous cross-org isolation tests). WARNING logged, non-blocking. File added to watch list below.
> Commit b13d2c3a (2026-06-10): CR-local fixes — race guard (mig 096 unique index + mig 099 EXCEPTION handler), mig 100 comment additions, 3 integration-test files. Clean. mig 099 254→285L (within 300L cap). mig 100 300→302L: 2-line overage is purely load-bearing comments on SECURITY DEFINER function — same exception as 095c/f8898771; do NOT trim. rpc-vfr-rt-start.integration.test.ts 866→922L: worsens pre-existing 500L ceiling violation; memory watch row updated below. No new pattern violations.
> Commit c461dc28 (2026-06-10): CR round-2 — comment reflows (migs 095c/100 both land at exactly 300L ✓); 42501 error.code assert replaces message-heuristic (correct, matches suite pattern); `if (orgId)` guards on 3 afterAll cleanups in constraint-regression test (pre-existing 580L, not worsened). rpc-vfr-rt-start.integration.test.ts 922→916L (improving). rpc-vfr-rt-constraint-regression.integration.test.ts 580L pre-existing — add to watch list. No new pattern violations.
> Commit 37106063 (2026-06-10): #838 legacy-RPC whitelist + results active-user gate — 4 migs (095b 161L ✓, 095c 300L ✓, 103 182L ✓, 104 97L ✓) + mirror pairs all within caps. Three test files worsened pre-existing >500L violations: rpc-vfr-rt-start 916→964L (+5, bracket pin), rpc-vfr-rt-submit 676→732L (+56, ×3 whitelist tests), rpc-vfr-rt-results 605→636L (+31, soft-deleted-caller test) — all non-blocking (test-file exemption), watch rows updated. Test naming behavior-first ✓. Hermiticity: whitelist tests use inline forceEndSession cleanup; soft-deleted-caller uses try/finally restore (family pattern). No new pattern violations.
> Commit 50c81b94 (2026-06-10): post-commit gap-fill — 1 new integration test (soft-deleted-caller path for complete_quiz_session, try/finally restore hermiticity ✓) + 2 doc-line fixes (database.md footer 094-103→094-104, plan.md count 132→133). rpc-complete-session.integration.test.ts 193→249L (within 500L ceiling). Clean.
> Commit fce9a871 (2026-06-10): cloud-CR fixes — mig 095b/104 pairs (active-user gate, whitelist narrowed, FOR UPDATE): 095b 161→180L ✓, 104 97→116L ✓, both within 300L cap. rpc-submit-answer 230→309L ✓ (within 500L ceiling). rpc-vfr-rt-submit 732→736L (+4, §5 zero-row observability fix on forceEndSession — load-bearing). rpc-vfr-rt-constraint-regression 580→605L (+25, single positive-control it() block). Both pre-existing watch files worsened slightly but not by new describe blocks; watch continues — flag on next describe-block addition. No new pattern violations.
> Commit ae27a7bc (2026-06-11): #833 mig 105 (153L ✓, new file, within 300L cap) + types.ts types update (clean) + rpc-vfr-rt-start.integration.test.ts 964→1196L (+232L, 9 new it() blocks for session-derived signature). Test-file ceiling pre-existing violation worsened (+232L, now 139% over 500L). 9 new it() titles behavior-first ✓, no disallowed-title patterns. `as unknown as` casts in test body are permitted null-injection/RPC-shape pattern. No BLOCKING issues. WARNING: rpc-vfr-rt-start.integration.test.ts 1196L — split candidate now; see watch list below.
> Commit d5d4084e (2026-06-11): #840 mig 106 (192L ✓, new file, within 300L cap) + rpc-vfr-rt-results.integration.test.ts 636→712L (+76L, 2 new it() blocks). Test-file ceiling pre-existing violation worsened (+76L, now 42% over 500L). 2 new it() titles behavior-first ✓. No production code changes. No BLOCKING issues. WARNING: rpc-vfr-rt-results.integration.test.ts 712L — growing; flag on next describe-block addition.
> Commit 1309878c (2026-06-11): post-commit review fixes — VOLATILE annotation drop (mig 105 both mirrors, 153→156L ✓), exam_configs delete added to cleanupTestData (packages/db/src/__integration__/cleanup.ts 109→120L ✓, well within 200L cap — distinct from e2e helpers/cleanup.ts 195L watch), docs/database.md claim rewords (2 lines). All clean. No pattern violations. Note: packages/db/src/__integration__/cleanup.ts and apps/web/e2e/redteam/helpers/cleanup.ts are distinct files; only the e2e one is on the 200L watch.
> Commit cbb2b68f (2026-06-11): test mock queue + FK-order assertions updated for exam_configs delete (7 lines added / 8 removed, cleanup.test.ts 309L ✓). Clean.
> Commit 86b97b21 (2026-06-13): #851 Vector DX — rpc-cross-tenant-isolation.spec.ts 305→374L (within 500L ceiling ✓). WARNING: `as boolean` cast on line 349 lacks a runtime guard (before?.has_calculations could be null even when the row exists; `expect(before).not.toBeNull()` guards the row but not the column). Not BLOCKING — pre-existing row-null assertion makes the undefined-cast unreachable in practice, but structurally unsound. Pattern count=1, WATCHING.
> Commit 2f33bedb (2026-06-13): #818 non-vacuous cross-tenant reads + within-time submit contract. rpc-cross-tenant-isolation.spec.ts 374→427L (+53L, new beforeAll/afterAll seed block + 3 rewritten tests — within 500L ceiling ✓). audit-completeness.spec.ts 388→424L (+36L, new `expectWithinTimeSubmitContract` helper + 2 call sites — within 500L ceiling ✓). `expectWithinTimeSubmitContract` is 22L (within 30L function cap ✓). Both mutations (insert/update) destructure `{ data, error }` ✓. Runtime guard on `as { ... }` cast ✓ (§5 cast-guard pattern). Test names behavior-first ✓. Hermiticity: seeded victim session soft-deleted in afterAll with `.select('id')` zero-row observability ✓. All checks passed — clean commit.
> Commit 9f581a54 (2026-06-13): #849 flag-idor non-vacuity — flag-idor.spec.ts 99→146L (within 500L ceiling ✓). All checks passed. Non-vacuity assertions (attacker sees ≥1 own flag before asserting 0 victim flags), base-table `.is('deleted_at', null)` guard correctly explained, afterAll soft-deletes both seeded flags via `.in(student_id, [victim, attacker])`. No pattern violations.
> Commit 79562e76 (2026-06-13): #845 integration test for get_vfr_rt_exam_results per-question answers array — rpc-vfr-rt-results.integration.test.ts 712→777L (+65L, 1 new it() block). Test-file ceiling pre-existing violation worsened (+65L, now 55% over 500L). Consistent growth trend (636→712→777). Test name behavior-first ✓. `as unknown as` RPC-shape cast ✓ (established pattern). `!` non-null assertions all guarded by preceding expect().toBeDefined() ✓. docs/plan.md both count occurrences updated (151→152) ✓. Clean commit.
> Commit 1a068277 (2026-06-14): #825 VFR RT guard/unauth/cross-org E2E — 2 new spec files (rpc-vfr-rt-submit 144L ✓, rpc-vfr-rt-results 148L ✓, both well under 500L ceiling) + server-action-unauthenticated.spec.ts 509→569L (+60L, 5 new unauth probes) + rpc-cross-tenant-isolation.spec.ts 427→468L (+41L, 1 new DN2 test). All files within 500L ceiling ✓. Test names behavior-first ✓. `{ data, error }` destructured on every admin `.insert()`, `.update()`, `.select()` call ✓. Hermiticity: afterEach soft-delete via `.in('id', Array.from(createdSessionIds))` + `.select('id')` zero-row observability + try/finally `clear()` ✓. No `any` types, no non-null assertions, no nesting >3 levels. All checks passed — clean commit.
> Commit 8be57963 (2026-06-14): #844 split rpc-vfr-rt-start 1335L — pure behaviour-preserving split into vfr-rt-helpers.ts (171L ✓, new shared seed helper, within 200L utility cap) + rpc-vfr-rt-start.integration.test.ts (1335→551L, within 500L test-file ceiling ✓) + rpc-vfr-rt-questions.integration.test.ts (new, 654L, within 500L ceiling ✓). Pre-existing rpc-vfr-rt-start watch row resolved. Not a barrel file (real implementations, no re-exports). No `any`, no non-null assertions unguarded. WARNING: 4 seed helper functions in vfr-rt-helpers.ts (33L/32L/35L/33L) slightly exceed the 30L function cap — all are single-concern sequential DB inserts with no branching logic, structurally equivalent to the known-structural E2E infra exception in memory (helpers/cleanup.ts, helpers/audit-helpers.ts); non-blocking. attack-surface.md DO row repointed to questions file ✓. Positive: rpc-vfr-rt-start 1196L watch row now RESOLVED by this split.
> Commit 619ad3d3 (2026-06-14): #533 LoadingButton component — loading-button.tsx 35L ✓, loading-button.test.tsx 69L ✓. All checks passed. Named export PascalCase ✓, file kebab-case ✓, co-located test ships in same commit ✓, 7 behavior-first it() titles ✓, no `any`, no nesting >2 levels, no useEffect, no barrel. Positive: `aria-hidden` on spinner + `loadingText` fallback + `does-not-fire-onClick-while-loading` test are a complete behavioral contract.
> Commit a7793c5e (2026-06-14): #533 loading feedback to answer-submit controls — 5 components + 2 test files. finish-quiz-dialog.tsx 302→307L (pre-existing violation worsened by +5, non-blocking — known split candidate). All other files within limits. No `any`, no useEffect, no barrel, no non-null assertions. Positive: `aria-busy={submitting || undefined}` (avoids false in DOM); `aria-hidden` on all 5 spinners; behavior-first test names; tests updated in same commit as production change.
> Commit 03582906 (2026-06-14): #533 quiz regression fix — quiz-main-panel.tsx `disabled` revert (+1/-1), answer-options.tsx submit-disable fix (+1/-1), use-answer-handler.ts comment block (+4/0). use-answer-handler.ts 112→116L: worsens pre-existing 80L hook cap violation by +4 comment lines (non-blocking, WARNING). No new pattern violations.
> Commit 2e0465b4 (2026-06-14): #533 LoadingButton app-wide sweep — 16 submit buttons migrated across 14 component files + 2 bespoke inline-spinner buttons in quiz-config-form + consent-form canSubmit cleanup. All 14 changed components within 150L component cap (max: code-entry-modal 166→172L — worsens pre-existing violation by +6, WARNING). quiz-config-form.tsx 140→149L (within 150L cap ✓). No `any`, no useEffect-for-data, no barrel files, no non-null assertions. Bespoke spinner pattern in quiz-config-form correctly uses `aria-busy` + `aria-hidden` on Loader2. Positive: `disabled` cleanly decoupled from `loading` across all 16 sites (no accidental double-disable).
> Commit 20de7c29 (2026-06-14): #886 mobile Submit Answer RPC in-flight fix — quiz-session.tsx ~155→161L (component cap 150L, worsened by +6 comment block + prop change; WARNING, tracker row count→5). quiz-controls.tsx 131L (within 150L cap, WATCH). quiz-session.test.tsx 776→816L (pre-existing >500L test-file ceiling violation worsened by +40L for regression test — non-blocking, test-file exemption). Positive: `resolveCheck` closure-assignment non-null assertion is correct TypeScript pattern; `waitFor()` replaces any direct assertion on async state; test name is behavior-first.
> Commit aca9ba8f (2026-06-14): action-scoped finish-dialog loading — finish-quiz-dialog.tsx 307→323L (+16L net: +12 in DialogFooter sub-component + 4 prop/type additions; pre-existing violation worsened; SPLIT CANDIDATE). use-quiz-submit.ts 90→103L (+13L, hook now exceeds 80L hook cap — pre-existing file is NOT a hook by naming; it is a hook by function but exports as module; per code-style.md §1 hook limit applies to use*.ts files — 103L is BLOCKING vs 80L hook cap). use-quiz-state.ts 85→86L (+1L, worsens pre-existing 80L hook cap violation — non-blocking, single pass-through line). use-exam-state.ts 77→78L (+1L, at exactly 80L hook cap — WATCH). finish-quiz-dialog.test.tsx 597→667L (+70L, within 500L test-file ceiling ✓). use-quiz-submit.test.ts 259→308L (+49L, within 500L ceiling ✓). All 9 new it() titles behavior-first ✓. No `any`, no non-null assertions, no useEffect-for-data, no barrel.
> Commit 4cecbe8c (2026-06-14): CR review fixes — per-action aria-busy on ConfirmPanel + in-flight counter in use-answer-handler + loading-button test coverage. finish-quiz-dialog.tsx 323→330L (+7, pre-existing violation worsened — SPLIT CANDIDATE). use-answer-handler.ts 116→121L (+5, pre-existing 80L hook cap violation worsened by outer try/finally wrapper; WARNING, non-blocking). finish-quiz-dialog.test.tsx 667→703L (within 500L ceiling ✓). loading-button.test.tsx 91→98L (within 500L ceiling ✓). handleSelectAnswer 55L (pre-existing >30L function cap violation — nesting depth 3 ✓, within limit). New it() title behavior-first ✓. No `any`, no non-null assertions, no useEffect-for-data, no barrel. Positive: outer try/finally guarantees counter decrement on all exit paths (RPC error, success, checkpoint throw); `Math.max(0, n - 1)` prevents counter going negative on unexpected re-entry.
> Commit 328cec34 (2026-06-14): #865 duration formatter extraction — new format-duration.ts (18L ✓) + format-duration.test.ts (30L ✓, 6 behavior-first cases) shipped together. report-question-row.tsx 150→144L (removes inline formatResponseTime, now below cap — watch row resolved). result-summary.tsx 123→116L (inline formatter extracted to shared util). report-question-row.test.tsx 398L ✓. No BLOCKING, no WARNINGs. Positive: new util + co-located test in same commit (rule-compliance on promoted pattern); both components reduced below their prior cap-proximity.
> Commit 5d15fbac (2026-06-14): #863 ZoomableImage rewrite — dialog overlay → new-tab anchor. zoomable-image.tsx 34L (was larger with dialog; now a minimal Server Component ✓). zoomable-image.test.tsx 32L ✓. `'use client'` correctly removed (no useState/useEffect remains). Test names behavior-first ✓. biome-ignore scoped and justified ✓. No BLOCKING, no WARNINGs. Positive: component simplification reduced complexity and eliminated a client boundary.
> Commit 2ba93a14 (2026-06-14): #864 size-limit remediation split — question-filters.tsx 181→83L ✓, question-filter-toggles.tsx new 106L ✓, use-quiz-config.ts 94→65L ✓, use-quiz-config-state.ts new 57L ✓, use-filtered-count-sync.ts new 39L ✓. All prior BLOCKING violations resolved. §7 test-coverage verdict: three new files are pure extractions of logic exercised at the orchestration layer (use-quiz-config.test.ts 50 cases covers calcMode/imageMode/handleSubjectChange/refetch paths; question-filters.test.tsx 28 cases covers FilterToggle rendering + toggle behavior via the consuming component); no separate test files required. useEffect in use-filtered-count-sync calls fc.refetch (a useCallback-stable Server Action trigger), not a direct fetch/Supabase call — correctly exempt from the data-fetching useEffect anti-pattern. No `any`, no barrel, naming correct. Clean commit.
> Commit cdba3ea1 (2026-06-14): #862 keyboard shortcuts + legend — new files: quiz-key-actions.ts 54L ✓ (+ .test.ts 224L ✓), use-quiz-keyboard.ts 78L ✓ (+ .test.ts 293L ✓), use-quiz-timer.ts 28L ✓ (no dedicated test — 28L pure extraction, timer behavior covered transitively by quiz-session.test.tsx via 8 timer/expiry assertions ✓), keyboard-legend.tsx 65L ✓ (+ .test.tsx 86L ✓), quiz-session-footer.tsx 47L ✓. quiz-session.tsx 204→150L (at exact cap ✓, reduced by extraction). answer-options.tsx 107→118L ✓. No `any`, no non-null assertions, no barrel, naming correct. useEffect in use-quiz-keyboard is a window event listener (window.addEventListener/removeEventListener) — exempt from data-fetching anti-pattern. useEffect in use-quiz-keyboard that resets highlightedIndex on currentIndex change is a state-sync effect, not data-fetching — exempt. Clean commit.
> Commit 170c5b6c (2026-06-14): #862 q-shortcut addendum — quiz-key-actions.ts 54→55L ✓, keyboard-legend.tsx 82→83L ✓, use-quiz-keyboard.ts 84L (unchanged in production, JSDoc only). Test files 234L/306L/111L all within 500L ceiling. Test titles behavior-first ✓. Exam-mode suppression negative assertion is non-vacuous (positive path asserted in prior test). No BLOCKING, no WARNINGs. Clean commit.
> Commits 08bac19f + 8727a879 + d34f4c03 (2026-06-14): CR-local round 3 a11y + test hygiene — question-filter-toggles.tsx 106→113L ✓ (+7 lines: onFocus/onBlur, aria-label expansion, aria-hidden on tooltip span — all additive, well under 150L cap). Test files: 3 title-paraphrasing comments dropped, 1 test renamed behavior-first, 1 boundary assertion added — all correct §7 patterns. attack-surface.md DY row added (exempt, agent-memory file). Clean — no BLOCKING, no WARNINGs.
> Commit 427b0932 (2026-06-19): email exam code via Resend — all new files within limits (resend.ts 44L ✓, internal-exam-code.ts 65L ✓, email-queries.ts 78L ✓, send-code-email.ts 58L ✓, issued-code-panel.tsx 67→99L ✓, mig 110 85L ✓, integration test 260L ✓). Two WARNINGs: (1) internalExamCodeEmail() in internal-exam-code.ts lines 26–65 = 39L (>30L cap) — bulk of the overage is the HTML template string literal; no branching logic, single concern, non-blocking. (2) getInternalExamCodeForEmail() in email-queries.ts lines 42–78 = 36L (>30L cap) — single-concern query + mapping, structurally similar to known-structural exception; non-blocking. All new utility files ship co-located tests ✓. useEffect in issued-code-panel is a state-reset (setCopied/setSent), not data-fetching — exempt ✓. Test names behavior-first ✓. No `any`, no barrel, no non-null assertions unguarded.
> Commit 5ff40d42 (2026-06-19): #869 batch_submit_quiz idempotent-replay output contract — session-replay.spec.ts 468→577L (within 500L test-file ceiling ✓). WARNING: `submitAndReplay()` helper function defined inside the test() callback is 46L (>30L cap) — it is a two-RPC sequential fixture helper with no branching logic beyond the RPC calls; structurally similar to the established E2E infra exception (forceTokenRefresh 44L, seed helpers 33–35L). Non-blocking. `buildGradedAnswers()` 29L ✓ (within 30L cap). Test name behavior-first ✓. Hermiticity: new sessions added to `createdSessionIds` ✓. `as BatchPayload` casts are RPC-shape pattern ✓ (established precedent). No `any`, no barrel, no non-null assertions. Positive: two distinct-grade fixtures (100 vs 0) make the non-vacuity assertion non-gameable by a hardcoded-constant regression.
> Commit df54f793 (2026-06-19): #902 redteam coverage for record_internal_exam_code_emailed — rpc-record-internal-exam-code-emailed.spec.ts new 346L (within 500L ceiling ✓). server-action-unauthenticated.spec.ts 569→581L (+12, within 500L ceiling ✓). audit-completeness.spec.ts 424→455L (+31, within 500L ceiling ✓). Tech.md ×3 + decisions.md count bumps 43→44 (docs-only, exempt). WARNING: seedCode() in new spec lines 54–91 = 38L (>30L function cap) — multi-field conditional object construction for consumed/voided/expired variants; single-concern, no branching across the function body; structurally equivalent to the known-structural E2E seed helper exception (vfr-rt-helpers.ts 33–35L each, logged in tracker); non-blocking. Test names behavior-first ✓ (5 new titles, DZ + audit-completeness title ✓). Hermiticity: per-step error-accumulator + .select('id') soft-delete in afterEach + try/finally clear() on both createdCodeIds + createdSessionIds — matches multi-step cleanup rule (code-style.md §7). Non-vacuous negative assertions: victim code existence proven via service-role read before attack ✓. No `any`, no non-null assertions, no barrel, no API-route mutations.
> Commit c993b7de (2026-06-20): #697 Phase C task C.2 VFR-RT in-progress exam runner — 15 files, all new except mc-renderer.tsx (+11/-4) and vfr-rt-exam.ts (+83/-1). All file limits clean. Three WARNINGs: (1) getVfrRtInProgress 46L (>30L cap) — single-concern auth+query+RPC chain, structurally identical to getSessionReports 47–64L pattern, non-blocking. (2) buildVfrRtPayload 37L (>30L cap) — 3-branch type dispatch, no nesting >3, single-concern, non-blocking. (3) useVfrRtAnswers 39L (>30L cap) — 2 useEffects + 3 useCallbacks + return, no complex branching, non-blocking. All three are first occurrences in this commit; established structural exception applies. No `any`, no barrel, no non-null assertions, no useEffect-for-data-fetching (both useEffects are localStorage state-sync — approved §6 SSR guard pattern). Both utils ship co-located tests ✓. Hook ships co-located test ✓. Test names behavior-first ✓ (34 it() titles). Positive: discriminated-union return type on getVfrRtInProgress is clean; submittedRef one-shot gate prevents double-submit race.
> Commits 44d2b0af + 5c836041 (2026-06-20): #697 Phase C task C.5 VFR-RT results page — 11 new files (9 production + 2 test), 946 lines added. All file limits clean: page 23L ✓, components 31–90L ✓, vfr-rt-results.ts 141L ✓ (utility cap 200L), test files 43/199/205L ✓. No `any`, no `'use client'` (all new components correctly Server Components), no barrel, naming correct. One WARNING: getVfrRtResults() spans lines 67–141 = 75L (>30L function cap) — 2 sequential RPC calls with graceful-degradation branch + Map-based merge + return; structurally similar to getVfrRtInProgress 46L and getSessionReports 47–64L (tracker row count→8 on "Utility function > 30 lines"); non-blocking. NUMERIC coercion: part1_pct/part2_pct/part3_pct/correct_count/total_questions all wrapped in Number() ✓. Wire shape typed honestly (number | string) ✓. `rpc<ResultsJson>` cast guarded with `Array.isArray(resultsData.questions)` ✓. Test names behavior-first ✓ (23 it() titles). vfr-rt-exam.ts 275→139L (results types split out — not in this diff; confirmed within 200L cap ✓). Positive: ResultsJson wire type annotates string-numeric columns; graceful degradation pattern (106 results + 105 display fallback) matches VFR-RT phase architectural pattern; per-type sub-components (short/mc/dialog) correctly delegated from review-row.
> Commit f402852d (2026-06-19): #856 sibling-grant addendum — packages/db mig 112 + supabase mirror (20260619000200) each +7 lines: 5-line comment + 1 GRANT line + 1 blank separator. Both files now 183L (within 300L SQL migration cap ✓). Mirror pair byte-identical confirmed (diff returned empty). No TypeScript/component/hook/utility/test files changed. No BLOCKING, no WARNINGs. Positive: additive GRANT with load-bearing rationale comment (belt-and-suspenders vs DROP+CREATE regression) matches sibling pattern established by 112b/113/114/117.
> Commit cb924697 (2026-06-19): VFR-RT Phase C slice 1 — 5 new presentational components/util + 5 co-located tests. All files within caps (max: dialog-fill-renderer.tsx 93L ✓). No `any`, no useEffect, no barrel, naming correct. `'use client'` correctly absent on part-progress.tsx (no browser APIs) and parse-dialog-display.ts (pure util). Module-level stateful regex BLANK_RE guarded by `lastIndex = 0` reset in parseSegments ✓. All 28 it() titles behavior-first ✓. No BLOCKING, no WARNINGs. Positive: non-vacuous canonical-strip security test (parser→renderer chain asserts answer text never reaches DOM); `SpeakerBadge` sub-component cleanly extracted; `useId()` for radio group name prevents DOM collisions across multiple McRenderer instances.
> Commit 0f8bdb72 (2026-06-19): VFR-RT Phase C task C.1 — briefing/landing page. All 5 new files within caps (page 35L ✓, briefing 82L ✓, vfr-rt-exam.ts 57L ✓, tests 73L/130L ✓). No `any`, no barrel, no useEffect-for-data. `'use client'` correctly scoped to briefing only; page stays Server Component ✓. Co-located tests ship in same commit ✓. 8 it() titles behavior-first ✓. Router push assertion checks URL ✓. WARNING: `getActiveVfrRtSession` is 33L (>30L function cap) — single-concern auth+query+map, no branching, structurally similar to known query-helper exception; non-blocking.

## Durable knowledge

- **Scope is the commit diff only.** Don't flag size violations whose over-limit lines are all pre-existing (no `+` lines in the hunk) — only flag what the commit introduced or worsened. Note worsened pre-existing violations but classify them as such (e.g. quiz-session-storage 226→251).
- **Test files are exempt from line limits up to a 500-line ceiling.** Most quiz test files run 150–760 lines and are fine. (use-session-bootstrap.test.ts ~685L is pre-existing tracking, not a fresh flag.) Exemption applies to `.test.ts`/`.test.tsx` files only — shared test-infra helpers like `setup.ts` or `helpers/*.ts` are utility files subject to the 200-line cap.
- **Documented Server Action exception:** a file >100 lines holding 3+ focused exported functions (each ≤30L) plus private helpers is acceptable (e.g. flag.ts 118L: toggleFlag 32L, getFlaggedIds 29L + flagQuestion/unflagQuestion helpers).
- **`useEffect` for ResizeObserver, click-outside listeners, timer cleanup, and hydration guards is NOT a data-fetching violation** — do not flag. Only `useEffect` that fetches data is the anti-pattern.
- **Migrations:** 300-line cap. Comment-trimming to hit the cap is acceptable when the rationale lives in `docs/database.md` + commit message; never trim load-bearing decision comments (lock rationale, soft-delete-skip justification, grace-period notes). SECURITY DEFINER functions get a documented exception (e.g. migration 047; mig 087 at 360L covers two full function bodies — same exception, confirmed a94850bb).
- **Page files:** 80-line cap, composition only — consistently honored (dashboard/page 42L, report/page 46/59L).
- **Borderline watch files trending toward limits:** quiz-session.tsx and use-quiz-state.ts (toward 200L/120L); session-table.tsx ExamBadge inline styling; result-summary.tsx desktop+mobile in one file (89L→116L after 328cec34 — still under 150L cap, but growing; flag on next significant growth). report-question-row.tsx 150→144L (328cec34 — inline formatter extracted, cap-proximity resolved ✓). Flag on next growth, not now. collect-user-data.test.ts 550L (c3ac3300, #684) — 10% over the 500-line test-file ceiling; flag on next growth. rpc-vfr-rt-start.integration.test.ts 1335→551L (8be57963, #844 SPLIT — RESOLVED ✓). rpc-vfr-rt-questions.integration.test.ts 654L (new from split, within 500L ceiling ✓ — no watch needed yet). rpc-vfr-rt-submit.integration.test.ts 736L (fce9a871, +4 from 732L — §5 zero-row observability fix) — 47% over 500L ceiling; split candidate when next describe block added. Flag on next growth. rpc-vfr-rt-results.integration.test.ts 802L (b1e33f03, +25L from 777L — formatter-only: useLiteralKeys conversions, no new describe blocks) — 60% over 500L ceiling; consistent growth (636→712→777→802 across 3 commits); flag on next describe-block addition, consider split. rpc-vfr-rt-constraint-regression.integration.test.ts 605L (fce9a871, +25 from 580L — single positive-control it() block) — 21% over 500L ceiling; split candidate when next describe block is added. Flag on next growth. audit-completeness.spec.ts 592→697L→394L, rpc-cross-tenant.spec.ts 672→757L (renamed/split into rpc-cross-tenant-isolation.spec.ts 305L + rpc-cross-tenant-reports.spec.ts 441L), server-action-unauthenticated.spec.ts 504→527L→499L — all three resolved by b95360f0 (#796). server-action-unauthenticated.spec.ts 499→509→569L (1a068277, +5 VFR RT unauth probes) — still well within 500L ceiling ✓. **New watch:** helpers/cleanup.ts cleanupFixtures() is 137L (>30L) and helpers/audit-helpers.ts buildAnswersForSession() is 46L (>30L) — both are E2E infra helpers with single-concern sequential logic; do NOT flag, document as known-structural and watch for growth. helpers/cleanup.ts 195L (near 200L cap) — flag on next growth. **question-filters.tsx 146→181L (e2ecd265, #864 imageMode toggle addition) — BLOCKING violation introduced by this commit (+36L from adding IMAGE_TOGGLES const + handleImageToggle + JSX render block); fix = extract FilterToggle sub-component + constants to sibling file. use-quiz-config.ts 77→94L (e2ecd265, #864) — BLOCKING hook cap violation (+17L); fix = extract state declarations to useQuizConfigState helper. Both need splits.**
- **Stable positive patterns** (acknowledge, don't re-litigate): behavior-first test naming; `vi.hoisted` + `vi.mock` + `vi.resetAllMocks()` mocking; `Readonly<Props>` immutability wrappers; early-fallback ternary (`== null ? fallback : value`); `new URL(url, base).searchParams` for URL-param assertions; responsive tree split (separate desktop/mobile components, not inline branches); component extraction + shared placement + all-import-site updates executed correctly (39b372e6); immutable-table hermiticity via delta assertion + `actor_id` + `gte(created_at, ...)` scoping instead of afterEach cleanup (c2758326 rate-limiting audit_events pattern); redteam specs consistently hermetic via soft-delete in `afterEach` + `try/finally` for transfer/restore + `.select('id')` zero-row observability (PR-6, test/redteam-quiz-session); read-only RPC probes (non-existent UUIDs, no DML) correctly require no afterEach — hermiticity by construction (9a728afd error-handling-disclosure.spec.ts). Actor-liveness beforeAll pre-check (rpc-record-auth-event.spec.ts, b004f206) — asserts all RPC callers have `deleted_at IS NULL` before tests run, preventing gate-2 pre-empting gate-3 assertions. New-dimension threading pattern (e2ecd265, #864 imageMode): adding a new tri-state filter dimension mirrors the existing CalcMode pattern end-to-end (type → Zod schema → RPC param → query helper → action → hook → component) with co-located tests in every file touched — clean commit, no violations.
- **Immutable-table E2E pattern:** when a test produces rows in `audit_events` (append-only by security.md rule 5), hermiticity is achieved via a pre/post delta assertion scoped by `actor_id` + `created_at >= testStart` — no afterEach cleanup is needed or possible. Do not flag absence of afterEach on tests whose only writes go to immutable tables.
- **`auth.getUser()` / auth SDK calls destructuring only `{ data }` is correct.** §5 destructure rule applies to `.from()` table queries (`.select()`, `.insert()`, `.update()`, `.delete()`). Auth SDK calls (auth.getUser, auth.signIn, etc.) return `{ data, error }` but the `error` field encodes auth-layer errors, not DB errors; destructuring only `{ data }` and null-checking `data.user` is the established redteam pattern — do not flag.
- **`null as unknown as T` in integration tests is a permitted null-injection pattern.** Test files pass `null as unknown as string[]` to RPC callers to exercise null-guard branches. This is not the §5 "type casting unvalidated external data" violation — that rule targets `req.body`, `formData`, `JSON.parse()` in production code.
- **code-reviewer scope is style/structure only.** Logic, security, RLS, error-handling correctness belong to semantic-reviewer — don't overlap.
- **Bash hook files in `.claude/hooks/` are infrastructure, not TypeScript.** File-size limits (Server Action 100L, hook 80L, utility 200L) do not apply. The relevant check is consistency with sibling scripts and internal function length. `run-security-auditor.sh` at 168L is acceptable — a single cohesive pre-push gate with a fallback branch. Flag on next growth only if functions exceed ~40L.
- **Agent prompt / parser contract drift is a WARNING.** When a hook's verdict parser is updated, check that the `TMPFILE` agent prompt still instructs the agent to emit the token format the parser expects. Mismatch produces unnecessary fragility even if the parser is defensive (f7ec4a11: parser now accepts `BLOCKED` anywhere on a line, but prompt still says "last line MUST be exactly: BLOCKED").
