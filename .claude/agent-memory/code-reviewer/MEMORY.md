# Code Reviewer — Memory

> Native subagent memory index. Tracker first, durable knowledge second, topic pointers last.
> Update rows/bullets IN PLACE. No session logs — git holds history (`git log -p`).

## Recurring Issues Tracker

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---------|-----------|-------|-----------|---------------------|
| Function > 30 lines in Server Action | 2026-03-20 (d93f924) | 2 | 2026-03-20 (6520962) | PROMOTED → code-style.md §3 (getFilteredCount 58L, toggleFlag 52L; both fixed) |
| New hook/utility file shipped without co-located test | 2026-03-21 (8771aa2) | 6 | 2026-08-30 (d2d3bdb3) | PROMOTED → code-style.md §7. 6th instance: admin-report-helpers.ts (6 exported query helpers, extracted from admin-quiz-report.ts) shipped with no admin-report-helpers.test.ts — covered only indirectly via admin-quiz-report.test.ts (adminClient is the only mock; the helpers module runs for real) — RESOLVED same PR (11cadfc2): admin-report-helpers.test.ts added, 23 tests, 3 mutation-verified. Two of six recurrences are extraction commits specifically (load-session-diagram-guards.ts, admin-report-helpers.ts) — the same-commit-extraction discipline (§1) is not carrying a same-commit-test discipline (§7) with it. RULE CANDIDATE: mechanical guard (extend check-soft-delete-guard.mjs sibling or new hook) flagging a new file under lib/queries, lib/, _hooks/, _utils/ with no matching .test.ts in the same commit. POSITIVE (still holding pre-8771aa2 instances): guard-bash.js stdin rewrite (5923087a) + session-bootstrap-load.ts .catch() (cb690c4f) + mc-content.ts (fefef96e) all shipped with co-located test. ANOTHER POSITIVE (f644ca53, review-follow-up to c90caf61): chainable-rpc-client.ts — extracted from two near-duplicate test-local mocks (supabase-rpc.test.ts's makeChainableClient + admin-report-helpers.test.ts's fakeChainableAuthClient) — shipped with chainable-rpc-client.test.ts (108L, 8 tests) in the SAME commit, matching the lib/test-support/mock-router.ts precedent. Second data point for the same-commit-extraction-carries-same-commit-test pattern the RULE CANDIDATE below still wants mechanized. |
| Hook file > 80-line limit | 2026-03-21 (8771aa2) | 4 | 2026-04-27 (c656868) | PROMOTED → code-style.md §1. Fix = extract orchestration helper. use-study-config.ts 80L exactly at cap — WATCH. RESOLVED: use-dialog-fill-input.ts 87L→56L (9e651aaf — extracted deriveBlankIndices/toBlankResults/buildSubmitPayload/toSeedValues + DialogBlank/BlankResult types to new dialog-fill-helpers.ts, same commit as the fix — good same-commit-extraction precedent for §1). |
| React component > 150-line limit | 2026-03-21 (8771aa2) | 6 | 2026-07-02 (03aae12d) | PROMOTED → code-style.md §1. BLOCKING: rwy-2709-lh-pattern.tsx 155L (fix: extract RunwayBody). SPLIT CANDIDATE: select.tsx 193L, alert-dialog.tsx 168L (shadcn wrappers; annotation-expansion; split work separate). WATCH: quiz-config-form.tsx 150L exactly at cap, quiz-main-panel.tsx 125L. RESOLVED: quiz-session.tsx 161L→132L (eec31df2 — QuizSessionProps extracted to session-types.ts). |
| Feature modes (study/exam) wired into one hook/component | 2026-04-13 (exam PR2) | 2 | 2026-04-26 (34194aa) | RULE CANDIDATE — extract mode logic once file passes 120L (hook) or 200L (component). |
| Server Action file > 100-line limit | 2026-03-20 (d93f924) | 2 | 2026-06-07 (e9c2a36b) | RESOLVED. WATCHING: lookup.ts 112L; batch-submit.ts 100L exactly at cap. |
| Utility function > 30 lines | 2026-05-31 (c879a259) | 22 | 2026-09-02 (7c9c9177) | WATCHING. Full per-commit history → [file-size-watch-log](topics/file-size-watch-log.md). Recurring mechanism: a correctness-guard fix pushes a function over cap; extractable part is usually the COMMENT first, then the closure. Still-open (fn / lines / fix): getSessionReports 60L; buildReportQuestions 65L; startOralExam 44L; getOralExamSession 31L; buildHandleSubmit 47L; buildFinishDialogHandlers 37L; assembleQuizState 36L; useOrderingInput 46L; useFinishQuizDialog 59L; checkNonMcAnswer 82L; requireAdmin 41L (mostly comments); handleSave 42L; handleRecoveryResume 30L/buildRecoveryResume 32L (factory exception); fetchAllRpcRows 42L. |
| Deep nesting > 3 levels | 2026-03-27 (75ffa51) | 3 | 2026-07-10 (df045384) | WATCHING (ConsentForm JSX 4-deep; ensureOtherOrgBank cb150e26 3-branch conditional at indent 4 — fix = early-return chain; topic-tree-actions.ts df045384: nested startTransition pushed setTopics to indent 4 — fix = extract applyTopics helper). |
| Utility file > 200-line limit | 2026-04-27 (c656868) | 7 | 2026-08-30 (464fd213) | WATCHING. SPLIT CANDIDATE: quiz-session-storage.ts 279L; quiz-submit.ts 239L. RECONCILED 2026-08-31: the quiz-report-questions.ts 213L figure was stale — measured at aec5dd24 = 177L (well under cap; drop it as a split candidate). WATCH: check-non-mc-answer-helpers.ts 200L at cap; seed-users.ts 198L; helpers/cleanup.ts 195L. BLOCKING: question-filters.tsx 181L (fix: extract FilterToggle + constants); use-quiz-config.ts 94L hook cap (fix: extract useQuizConfigState). RESOLVED: admin-quiz-report.ts 272L (464fd213, over cap) → 200L exactly (d2d3bdb3, #991 review follow-up) — the two near-identical `fetchAllRows` count+page blocks plus the session-guard and page-question/RPC fetches extracted to new admin-report-helpers.ts (171L, 6 exported helpers, each ≤29L). getAdminQuizReportSummary 87L→68L, getAdminQuizReportQuestions 156L→100L — both still far over the 30L func cap (pre-existing, not worsened by this commit — not re-flagged). New file admin-report-helpers.ts shipped with no co-located test — RESOLVED same PR (11cadfc2); see the missing-test tracker row above. Full history → file-size-watch-log. |
| E2E Playwright hermiticity (hard-delete without restoration) | 2026-04-30 (from §7) | 2 | 2026-04-30 | RESOLVED — afterEach soft-delete pattern established; no recurrence. |
| `waitForTimeout` in E2E specs | 2026-06-06 (test/isolation-hygiene) | 1 | 2026-06-06 | WATCHING (exam-recovery.spec.ts — fixed in same commit; tracking for recurrence). |
| E2E spec > 500-line limit | 2026-08-20 (b67beccf) | 2 | 2026-09-01 (4341ab8f) | RULE CANDIDATE (2). Full history → [file-size-watch-log](topics/file-size-watch-log.md). Mechanism: additive `describe`/`beforeAll` growth on files already near cap. rpc-admin-report-answer-keys-idor.spec.ts had 3 consecutive same-mechanism growths (511→518→546L) with no split yet — next touch should trigger the split (extract the shared precondition-check helper it duplicates with its sibling spec) rather than a 4th WARNING. |
| §10 false "always" claim in mechanic-rewrite commits | 2026-09-06 (43b1b9a5) | 1 | 2026-09-07 (1bf29e9d) | RESOLVED (d58572c8). CLEAN: 1bf29e9d — fullpush.md third version verified accurate (CORE agents / red-team ordering both confirmed against agent-workflow.md:733); wrap-tolerant class sweep for per-APPLY→per-ROUND wording found 0 live remaining instances (one hit in .claude/agent-memory/ is a historical quote, excluded). All four amended files updated consistently. No new false claims. agent-critic.md + docs/decisions.md added "CR-local has always used extend-by-one … never had the defect" — FALSE (crlocal used a consecutive-clean counter 2026-06-18→2026-06-23, ddf5f647→765914d7). Fixed in d58572c8: both files now describe CR-local as PRECEDENT — same defect, same fix, five days apart. SHAs and quotes verified by this reviewer. Follow-up 93df2283 corrected D61's "for the same reason" — the two rationales differ (CR-local: cloud is authoritative gate; D61: floor arithmetically unreachable); verified against 765914d7 text. docs/decisions.md footer not bumped to 2026-09-07 in that commit — noted as WARNING. |
| Ambiguous "reset" scope in mechanic-rewrite prose | 2026-09-07 (092fab98) | 1 | 2026-09-07 (092fab98) | RESOLVED (4efa42a7). Line 31 now reads "does NOT reset the round counter M" — objects are explicit (baseline vs. M counter). Collision removed, not relocated. |
| Class-sweep "complete" overclaim — surviving instance outside documented exceptions | 2026-09-07 (4a9769fc) | 1 | 2026-09-07 (4a9769fc) | WATCHING. fullpush.md:14 still reads "it runs after all four report" (about the learner). Defensible in context (same sentence qualifies "skipped entirely on a reduced cycle") but structurally identical to the two Class A instances fixed in this commit. Not one of the two documented exceptions (agent-learner.md trigger; agent-workflow.md diagram line). Fix: "after every core agent reports". |
| §10 compliance-ratio/quote drift in a newly-authored data file's own comments | 2026-09-09 (7ca1f522) | 1 | 2026-09-09 (7ca1f522) | WATCHING. `.claude/limits.json`'s test-file rule `note` claims "502 of 553 comply; the 51 that do not" — measured (guard's own `countLines`, `evaluate()`): 553 pre-commit / 554 post-commit total, 50 over-limit both before and after (matches the 50 `kind: 'test file'` baseline entries exactly, guard exits 0 clean) — true figures are "503 of 553 comply; the 50 that do not". Off by one on both numbers, in the file whose entire thesis is replacing drifted hand-counted prose with data. Separately, `check-file-size-guard.mjs`'s `globToRe` JSDoc quotes `resume-helpers.ts` as carrying the line "No `'use server'` — these are pure transforms" — it actually reads "…these are invoked by the action"; only the neighboring `load-draft-helpers.ts` has the quoted wording verbatim. Also `packages/db/src/types.ts` cited as "1807-line" — actual 1806 by both `wc -l` and the guard's own `countLines()`. All three confirmed by direct execution, not inference. None affect the guard's PASS/FAIL logic (all 28 tests green, guard exits 0) — comment-only. |

> Count increments only on a **distinct** mechanism. Rows transition state, never deleted.

## Durable knowledge

- **Scope is the commit diff only.** Flag size violations only when the commit introduced or worsened them (`+` lines in the hunk). Note pre-existing violations but don't flag them.
- **Test files** are exempt from line limits up to 500L (`.test.ts`/`.test.tsx`/`.spec.ts`). Shared test-infra helpers (`setup.ts`, `helpers/*.ts`, `seed.ts`) are utility files subject to the 200L cap.
- **Server Action exception:** file >100L holding 3+ focused exports (each ≤30L) + private helpers is acceptable.
- **`useEffect` exceptions:** ResizeObserver, click-outside, timer cleanup, and hydration guards are NOT data-fetching violations. Only flag `useEffect` that fetches data.
- **§5 mutations in test `finally` restore blocks:** `await admin.from(...).update(...)` without `{ error }` destructuring is a WARNING — silent restore failure corrupts next test run.
- **Migrations:** 300L cap. Single-function SECURITY DEFINER RPCs may exceed — documented exception in code-style.md §1 (mig 087 360L, mig 129 330L, mig 130 329L, mig 160 351L `submit_vfr_rt_exam_answers`). Header self-annotates the exception; reviewer confirms and passes.
- **Page files:** 80L cap, composition only — consistently honored.
- **Borderline watch files** — full history (per-file growth chains, SPLIT CANDIDATE list, resolved items) → [file-size-watch-log](topics/file-size-watch-log.md), updated 2026-09-09. E2E/infra helpers are structural exceptions — do NOT flag. Live BLOCKING-adjacent: rwy-2709-layout.ts 194L (200L cap, 6L headroom); `.claude/pipeline.test.mjs` 300L (over 200L utility cap technically, WARNING only). Live SPLIT CANDIDATEs (3rd+ consecutive growth): students/[id]/queries.test.ts 586L; attempts-queries.test.ts 564L; admin-quiz-report.test.ts 1158L; ~12 other over-500L test files (list in topic file). As of 7ca1f522 these are all `.claude/limits.json` `baseline` entries now — the ratchet mechanically enforces "may only shrink", so this bullet is a pre-commit early-warning aid, not the enforcement mechanism.
- **`auth.getUser()` / auth SDK:** destructuring only `{ data }` is correct — §5 destructure rule applies to `.from()` table queries only.
- **`null as unknown as T` in integration tests** is a permitted null-injection pattern (not a §5 violation — that rule targets `req.body`, `formData`, `JSON.parse()` in prod code).
- **`unknown`-typed wire-shape + `typeof` runtime guards** (study-queries.ts, 14a8b9c5): the correct §5 cast-guard pattern for nullable `RETURNS TABLE` fields. Acknowledge, don't re-litigate.
- **code-reviewer scope is style/structure only.** Logic, security, RLS belong to semantic-reviewer.
- **Rules/memory-only commits (`.claude/rules/*.md`, `.claude/agent-memory/**`, no code) — §1-9 are N/A; only §10 (Comment Accuracy) applies.** Verify every cited commit SHA, count, and "instance N" narrative before clearing, using the artifact checks in `agent-workflow.md` § Finding Validation (`git show --stat <sha> -- <path>` + `git grep`) — READ that section for the exact form rather than working from this line: it carries a `git log -1` trap and a merge-commit flag, and this copy has already drifted twice by restating the mechanics instead of pointing at them — these commits routinely cite prior commits and tracker counts as evidence (e.g. `b177a3d2`, a learner-row promotion citing `20a14793`/`ab737599` and a count=5 history; all five instances traced and confirmed accurate against `cross-agent-lessons.md`/`tracker-archive.md` and git history). Also re-check §10 rule 3 (partial-edit staleness) with a repo-wide grep of the retracted status-cell text when a tracker row transitions state. Second clean instance: `c787b0d2` (applies b177a3d2's own findings) — cited footer line numbers (:833, :110 vs. a stale :746/:32 from doc-updater's prior report) verified against `git show b177a3d2:<path> | grep -n`, the `agent-workflow.md:819` CONSTRAINTS citation verified by line number, the new doc-updater.md section's backward-reference to `agent-doc-updater.md`'s NEVER list verified to exist, and all four edited files' trailing bytes confirmed `0a` — those four checks clean. NOT a clean-commit verdict: `c787b0d2` shipped two false claims OUTSIDE what this pass checked (a ".claude/rules/agent-doc-updater.md is orchestrator-facing and does not reach you" claim, and "Three recorded instances" when the tracker held two), both caught by semantic-reviewer post-commit and fixed in the follow-up. Report what was checked, never a scope you did not cover.
- **Hook files in `.claude/hooks/`** and **operational scripts in `apps/web/scripts/`** are infrastructure — file-size limits and style rules (function length, nesting, missing tests) do not apply. Neither ships in the application bundle; `apps/web/scripts` is named in `apps/web/tsconfig.json`'s `exclude` array, and the `.mjs` hooks sit outside any TS project. Matches suppression #8 in `agent-code-reviewer.md`. **#1219 fixed in `1335c73d`** (`Closes #1219`; the issue closes on MERGE — `gh issue view` still reports OPEN until then). `apps/web/tsconfig.scripts.json` covers the directory and is chained into `pnpm check-types`, so the earlier "no tsconfig coverage at all" caveat is RETRACTED. The suppression's structural rationale still holds (scripts do not ship in the bundle). Keep reporting genuine findings there at INFO — coverage is narrow: only `import-questions.ts` and `cleanup-orphaned-draft-sessions.ts` type against `Database`, so a wrong column name still compiles elsewhere. WATCH (re-derived with `wc -l` 2026-08-18, not re-typed): `diagram-content.ts` 255L and `mc-content.ts` 228L both over the 200L utility cap; `dialog-fill-content.test.ts` 522L over the 500L test cap and `mc-content.test.ts` 503L (489+14, 6cf8e1fc) now over it — both scripts-exempt; `import-vfr-rt-content.ts` nearly 2kL with `main()`/`insertAll`/`planReplaceAll` all long.
- **Agent prompt / parser contract drift** is a WARNING — verify the agent prompt emits the token format the hook's verdict parser expects.
- **`Readonly<Props>` rule** (code-style.md §5+§8, WARNING). `Readonly<Readonly<...>>` double-wrap is harmless — WARNING not BLOCKING. Shadcn wrapper annotation expansion worsens line counts by 2-4L per component; note as pre-existing, split work is separate.
- **Platform-behavior claims in agent definitions** (1b4ee552, 2026-09-06): §1-9 N/A for rules/agents/hooks-only commits. §10 applies: watch for platform claims (e.g., `tools:` vs `memory:` interaction) stated as established fact when only verifiable at next-session restart. The correct pattern: "per the official subagent docs — confirm at next session restart." If such a claim is wrong and goes unflagged, downstream agents silently lose capabilities (e.g., 8 tracker writes breaking with no error). Follow-up 29cd8d0f hedged agent-memory.md but left agent-workflow.md:743 stating the same memory-write claim as established fact — inconsistency between two files edited in the same commit (WARNING, not fixed yet).

## Topic pointers

- [commit-review-log](topics/commit-review-log.md) — per-commit review notes (read on demand).
- [file-size-watch-log](topics/file-size-watch-log.md) — full chronological file-size growth history (read on demand).
