# Code Reviewer — Memory

> Native subagent memory index. Tracker first, durable knowledge second, topic pointers last.
> Update rows/bullets IN PLACE. No session logs — git holds history (`git log -p`).

## Recurring Issues Tracker

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---------|-----------|-------|-----------|---------------------|
| Function > 30 lines in Server Action | 2026-03-20 (d93f924) | 2 | 2026-03-20 (6520962) | PROMOTED → code-style.md §3 (getFilteredCount 58L; toggleFlag 52L — both 2-branch toggle/conditional shape, fixed by extracting named helpers) |
| New hook/utility file shipped without co-located test | 2026-03-21 (8771aa2) | 4 | 2026-03-23 (b104ae4) | PROMOTED → code-style.md §7 (use-comments.ts, use-flagged-questions.ts, score-color.ts, reports-utils.ts) |
| Hook file > 80-line limit | 2026-03-21 (8771aa2) | 5 | 2026-06-13 (1b5e6679) | PROMOTED → code-style.md §1 (use-comments 84L; use-quiz-config 110L; use-quiz-state 121L; use-session-bootstrap 99L; use-question-form-state 103L). Fix = extract orchestration/derivation helper |
| React component > 150-line limit | 2026-03-21 (8771aa2) | 5 | 2026-06-13 (1b5e6679) | PROMOTED → code-style.md §1 (comments-tab 155L; finish-quiz-dialog 243L; quiz-session 176/204L; quiz-controls 152L; question-form-fields 158L). Fix = extract sub-component/helper component |
| Feature modes (study/exam, draft/live) conditionally wired into one hook/component | 2026-04-13 (exam PR2) | 2 | 2026-04-26 (34194aa) | RULE CANDIDATE — extract mode-specific logic to its own hook/component once a file passes 120L (hook) or trends toward 200L (component). Single component switching render on an `isExam` prop is acceptable at small scale |
| Server Action file > 100-line limit | 2026-03-20 (d93f924) | 2 | 2026-06-07 (e9c2a36b) | RESOLVED (toggle-student-status.ts split to 55L ✓ via e9c2a36b — mutations extracted to toggle-student-status-mutations.ts 78L, both under limit). lookup.ts 112L still WATCHING. |
| Utility function > 30 lines (single query RPC) | 2026-05-31 (c879a259) | 2 | 2026-06-07 (b004f206) | WATCHING (getSessionReports 47→64L; forceTokenRefresh 44L in e2e/redteam/helpers/force-token-refresh.ts — E2E seam helper, not production util. Fix = extract clearStaleChunks + rebuildCookies helpers). cleanupReferenceData WARNING resolved d9548f62: refactored 48L→18L loop |
| Component deep nesting > 3 levels | 2026-03-27 (75ffa51) | 1 | 2026-03-27 | WATCHING (ConsentForm: form→div→div→Checkbox+span→a, repeated 3×; fix = extract ConsentCheckbox sub-component) |
| Utility file > 200-line limit | 2026-04-27 (c656868) | 2 | 2026-06-06 (ade08109) | RESOLVED (setup.ts) — split into setup.ts 89L / seed.ts 134L / cleanup.ts 85L (ade08109, closes #778). quiz-session-storage.ts 251L still WATCHING. new helpers/cleanup.ts 195L + helpers/audit-helpers.ts 197L are both under 200L (b95360f0). |
| E2E Playwright hermiticity (hard-delete without restoration) | 2026-04-30 (from §7 testing rule) | 2 | 2026-04-30 (admin-students, admin-questions pre-mig 083) | RESOLVED — distributions-rls.spec + report.spec + admin-dashboard.spec (all 2026-06-04, ad19626) apply hard-delete cascade correctly with `.beforeEach/.afterEach + createdIds.clear()` pattern; no recurrence since rule promotion. session-race-condition.spec (PR-4, 2026-06-06) newly hermetic with afterEach soft-delete + `createdSessionIds` array (session-replay was already hermetic on master) |
| `waitForTimeout` in E2E specs | 2026-06-06 (test/isolation-hygiene) | 1 | 2026-06-06 | WATCHING (exam-recovery.spec.ts: waitForTimeout(300) replaced with `waitFor`+attribute assertion — fix already applied in this commit, tracking for recurrence) |

> Count increments only on a **distinct** mechanism. Rows transition state, never deleted.
> Four "watching" count=1 rows have not recurred — that non-recurrence is itself a positive signal.
> Commit 3260e2d9 (2026-06-10): docs-only spec update — no code pattern violations. All migration timestamps verified against actual files.
> Commit 76a9b03e (squash of pre-rebase 5f561ddf, 2026-06-10): VFR RT Phase A — 14 migrations + 4 integration tests. Clean. Three SECURITY DEFINER migrations at 299-300L (095c=300, 100=299, 102=299) — within cap or at exact limit, exception confirmed. Integration test files at 223/598/673/769L — all within 500L test-file ceiling. No pattern violations.
> Commit f8898771 (2026-06-10): Phase A CR hardening — comment/CHECK additions to 4 migs + mirror pairs + docs. Clean. 095c 300→304L (SECURITY DEFINER exception applies; 4 added lines are load-bearing §15 rationale comments). No pattern violations.
> Commits fcef799b + e179093b (2026-06-10): mig 094 doc sync + constraint-regression integration tests. Clean. Test file 389L (within 500L ceiling). Three new it() titles behavior-first, pass all §7 disallowed-title checks. Hermiticity via beforeAll/afterAll + cleanupTestData pattern. No pattern violations.
> Commit aed96dcb (2026-06-10): #831 org-filter fix — mig 099b 117L (clean, within 300L cap), docs/attack-surface 1-line updates (exempt). rpc-vfr-rt-start.integration.test.ts 769→823L: worsens pre-existing 500L test-file ceiling violation (+54L for non-vacuous cross-org isolation tests). WARNING logged, non-blocking. File added to watch list below.
> Commit b13d2c3a (2026-06-10): CR-local fixes — race guard (mig 096 unique index + mig 099 EXCEPTION handler), mig 100 comment additions, 3 integration-test files. Clean. mig 099 254→285L (within 300L cap). mig 100 300→302L: 2-line overage is purely load-bearing comments on SECURITY DEFINER function — same exception as 095c/f8898771; do NOT trim. rpc-vfr-rt-start.integration.test.ts 866→922L: worsens pre-existing 500L ceiling violation; memory watch row updated below. No new pattern violations.
> Commit c461dc28 (2026-06-10): CR round-2 — comment reflows (migs 095c/100 both land at exactly 300L ✓); 42501 error.code assert replaces message-heuristic (correct, matches suite pattern); `if (orgId)` guards on 3 afterAll cleanups in constraint-regression test (pre-existing 580L, not worsened). rpc-vfr-rt-start.integration.test.ts 922→916L (improving). rpc-vfr-rt-constraint-regression.integration.test.ts 580L pre-existing — add to watch list. No new pattern violations.
> Commit 37106063 (2026-06-10): #838 legacy-RPC whitelist + results active-user gate — 4 migs (095b 161L ✓, 095c 300L ✓, 103 182L ✓, 104 97L ✓) + mirror pairs all within caps. Three test files worsened pre-existing >500L violations: rpc-vfr-rt-start 916→964L (+5, bracket pin), rpc-vfr-rt-submit 676→732L (+56, ×3 whitelist tests), rpc-vfr-rt-results 605→636L (+31, soft-deleted-caller test) — all non-blocking (test-file exemption), watch rows updated. Test naming behavior-first ✓. Hermiticity: whitelist tests use inline forceEndSession cleanup; soft-deleted-caller uses try/finally restore (family pattern). No new pattern violations.
> Commit 50c81b94 (2026-06-10): post-commit gap-fill — 1 new integration test (soft-deleted-caller path for complete_quiz_session, try/finally restore hermiticity ✓) + 2 doc-line fixes (database.md footer 094-103→094-104, plan.md count 132→133). rpc-complete-session.integration.test.ts 193→249L (within 500L ceiling). Clean.
> Commit fce9a871 (2026-06-10): cloud-CR fixes — mig 095b/104 pairs (active-user gate, whitelist narrowed, FOR UPDATE): 095b 161→180L ✓, 104 97→116L ✓, both within 300L cap. rpc-submit-answer 230→309L ✓ (within 500L ceiling). rpc-vfr-rt-submit 732→736L (+4, §5 zero-row observability fix on forceEndSession — load-bearing). rpc-vfr-rt-constraint-regression 580→605L (+25, single positive-control it() block). Both pre-existing watch files worsened slightly but not by new describe blocks; watch continues — flag on next describe-block addition. No new pattern violations.
> Commit ae27a7bc (2026-06-11): #833 mig 105 (153L ✓, new file, within 300L cap) + types.ts types update (clean) + rpc-vfr-rt-start.integration.test.ts 964→1196L (+232L, 9 new it() blocks for session-derived signature). Test-file ceiling pre-existing violation worsened (+232L, now 139% over 500L). 9 new it() titles behavior-first ✓, no disallowed-title patterns. `as unknown as` casts in test body are permitted null-injection/RPC-shape pattern. No BLOCKING issues. WARNING: rpc-vfr-rt-start.integration.test.ts 1196L — split candidate now; see watch list below.
> Commit d5d4084e (2026-06-11): #840 mig 106 (192L ✓, new file, within 300L cap) + rpc-vfr-rt-results.integration.test.ts 636→712L (+76L, 2 new it() blocks). Test-file ceiling pre-existing violation worsened (+76L, now 42% over 500L). 2 new it() titles behavior-first ✓. No production code changes. No BLOCKING issues. WARNING: rpc-vfr-rt-results.integration.test.ts 712L — growing; flag on next describe-block addition.
> Commit 1309878c (2026-06-11): post-commit review fixes — VOLATILE annotation drop (mig 105 both mirrors, 153→156L ✓), exam_configs delete added to cleanupTestData (packages/db/src/__integration__/cleanup.ts 109→120L ✓, well within 200L cap — distinct from e2e helpers/cleanup.ts 195L watch), docs/database.md claim rewords (2 lines). All clean. No pattern violations. Note: packages/db/src/__integration__/cleanup.ts and apps/web/e2e/redteam/helpers/cleanup.ts are distinct files; only the e2e one is on the 200L watch.
> Commit cbb2b68f (2026-06-11): test mock queue + FK-order assertions updated for exam_configs delete (7 lines added / 8 removed, cleanup.test.ts 309L ✓). Clean.
> Commit 09372624 (2026-06-11): chore — .coderabbit.yaml §15 prose update + agent memory curation + scheduled_tasks.lock churn. No TypeScript/React source files. All changes are config/infra/memory — exempt from code-style.md limits. Clean.
> Commit 26015581 (2026-06-11): docs(rules) — 2 prose lines in .claude/rules/security.md + CLAUDE.md (§15 carve-out enumeration + pre-commit gate correction). Markdown rules files only — exempt from all code-style.md limits. Clean.
> Commit d656695a (2026-06-11): CR round-1 — config-shape guard added to migs 105/106 + both mirror pairs (6L + 7L each, 162L ✓ and 199L ✓, both within 300L cap). 1 new integration test (foreign-org filter pin, 139 lines added). rpc-vfr-rt-start.integration.test.ts 1196→1335L (+139L, single it() block). Test-file ceiling pre-existing violation worsened (+139L, now 167% over 500L ceiling). Split urgency elevated — see watch list below. plan.md count 144→145 (single-line update, clean). No new pattern violations.
> Commit 6dddade2 (2026-06-11): docs(db) — database.md 3-line error-code additions + mig 106 comment block (both mirrors, SQL body unchanged). mig 106 199→203L (within 300L cap). All changes are comment/prose only — exempt from all code-style.md structural checks. Clean.
> Commit 27bdf862 (2026-06-11): fix(reports) #834 — hours-tier to formatDuration (result-summary.tsx) + new formatDurationMinutes (reports-utils.ts) + render-site wiring (session-card.tsx, session-table.tsx) + boundary tests. All files within limits: result-summary.tsx 121L (component cap 150L), reports-utils.ts 21L, session-card.tsx 40L, session-table.tsx 114L. New formatDurationMinutes is 2L; modified formatDuration is 7L. Tests ship co-located (§7 complied). No pattern violations. Clean.
> Commit 66f1ddc2 (2026-06-11): docs(reports) — 2-line comment correction in reports-utils.ts (sibling-formatter branch descriptions). No code changes. reports-utils.ts stays at 21L. Clean.
> Commit 2474b554 (2026-06-11): feat(reports) #835 — new options-list.tsx 48L ✓ + co-located test 73L ✓; report-question-row.tsx 125→116L (net shrink, now 116L ✓); query files within caps (quiz-report.ts 119→120L ✓, quiz-report-questions.ts 122→124L ✓, admin-quiz-report.ts 210→212L ✓). All it() titles behavior-first ✓. No any/non-null/useEffect/barrel violations. report-question-row.tsx watch updated: 117→116L (shrunk, 34L headroom to 150L cap). Clean.
> Commit 50b58c78 (2026-06-11): test(reports) — null selectedOptionId + query fixture completions. report-question-row.test.tsx +5L (334L ✓, within 500L ceiling). admin-quiz-report.test.ts +2L (509L — 2% over 500L ceiling, pre-existing, not worsened materially). quiz-report-questions.test.ts +2L (453L ✓). New it() title behavior-first ✓ ("shows 'Not answered' when selectedOptionId is null (VFR RT text-answer row)" — parenthetical context is role annotation, not impl-detail leakage). Clean.
> Commit 7faac459 (2026-06-11): feat(reports) #836 — report-flag-context.tsx new 68L ✓ (hook/context cap 80L, 12L headroom); report-question-row.tsx 116→143L (+27L from flag button block — 7L under 150L component cap, 7L headroom; watch row updated); flagged-questions.ts new 35L ✓ (utility cap 200L); page files internal-exam/report 51L ✓ and quiz/report 48L ✓. Test files: report-flag-context.test.tsx new 118L ✓; report-question-row.test.tsx 334→378L ✓; flagged-questions.test.ts new 101L ✓ — all within 500L ceiling. toggle() callback 24L ✓. No any/barrel/useEffect-data-fetch violations. getFlaggedQuestionIds: auth.getUser() destructures `{ data: { user }, error: authError }` correctly (established auth SDK pattern, durable knowledge §5). Table SELECT destructures `{ data, error }` ✓. r.question_id `as string` cast justified by comment ✓. Tests ship co-located ✓, behavior-first naming ✓, vi.hoisted pattern ✓. Clean.
> Branch feat/837-has-calculations-filter (2026-06-11, top commit 03058032): #837 calc-filter feature — 46 files, 2 commits. All source files within limits: bulk-update-calculations.ts 36L ✓ (SA cap 100L), question-filter-selects.tsx 179L ✓ (component cap 150L — note: 25L above the previous pre-branch 154L; pre-branch master was already 154L, all 23 new lines are additive JSX, not net new over 150). Actually: question-filter-selects.tsx 179L pre-branch was 154L — WORSENED 154→179L, still within 150L cap? NO — 154L was already 3% over 150L cap on master. New lines pushed it to 179L — 19% over cap. FLAG: question-filter-selects.tsx 179L BLOCKING (pre-existing 154L cap violation worsened by +25L). use-quiz-config.ts 95L, use-filtered-count.ts 83L (both hook cap 80L) — hook files: use-quiz-config.ts 85→95L (pre-branch 85L, this branch +10L); use-filtered-count.ts 79→83L (pre-branch 79L, this branch +4L) — both worsened pre-existing violations. question-form-fields.tsx 167L (component cap 150L, pre-branch 150L, +16L new lines — BLOCKING, worsened 150→167L). question-table.tsx 156L (pre-branch 151L, +5L — BLOCKING, worsened 151→156L). use-quiz-start.ts 87L (pre-branch 85L, +4L — hook cap 80L, worsened). quiz-lib/queries/quiz.ts 263L (pre-branch 259L, +4L, utility 200L cap — pre-existing worsened). All test files within 500L ceiling: integration test 183L ✓, unit tests 183-611L ✓. No any/barrel/useEffect-data-fetch/non-null/naming violations introduced. useEffect in use-quiz-config.ts was pre-existing pattern (refetch call); this branch only added calcMode to dep array. Positive: vi.hoisted + buildChain pattern, behavior-first it() titles, Readonly<Props>, co-located tests for all new source files.
> Commit 6265713c (2026-06-11): feat(admin) UI polish + calc filter as toggles (#853 branch) — 23 files, net -72L. All source files within caps: question-filters.tsx (quiz) 132→148L ✓ (within 150L component cap, 2L headroom), question-filter-selects.tsx (admin) 111→94L ✓ (pre-branch 179L violation RESOLVED by this commit — difficulty filter dropped), question-table.tsx 128→133L ✓, question-form-fields.tsx 148→148L unchanged (pre-existing 148L; 2L added and 2L removed, no worsening), page.tsx 69→63L ✓ (well within 80L page cap). FilterToggle is an intra-file component (not a new file) — new-util-without-test rule does not apply. All functions well within 30L (handleCalcToggle 1L, handleToggle 11L, FilterToggle body 8L). No any/barrel/useEffect-data-fetch/non-null/naming violations. Test files 132→175L (quiz filters, within 500L ✓) and 191→177L (question-table, shrunk ✓). it() titles behavior-first ✓. Clean.
> Commit db0b303e (2026-06-11): fix(e2e) drop obsolete "filters by difficulty" admin spec (#853 branch) — pure 10-line deletion. No additions. describe/afterEach hermetic structure intact. Clean.
> Commit 0cdc01bf (2026-06-12): feat(db) #823 P0 — 14 SQL migration files (7 canonical + 7 supabase mirrors), relocating MC answer key to REVOKE-gated correct_option_id column. All within 300L cap: 109=134L, 110=152L, 110b=300L (AT cap, SECURITY DEFINER exception applies — do NOT trim), 111=290L, 112=106L, 113=172L, 114=89L. Naming: 110b suffix + 000250 interleaved timestamp follow established 095b/095c → 000400/000450 precedent. SQL-only commit; no TS/React rules apply. Clean.
> Commit 1b5e6679 (2026-06-13): feat(questions) #823 P1 — app/schema/admin/seeds/integration-tests consuming correct_option_id. Two BLOCKING size violations: question-form-fields.tsx 148→158L (+10L, component cap 150L, 8L over), use-question-form-state.ts 93→103L (+10L, hook cap 80L, 23L over). New files all within caps: get-correct-option.ts 31L ✓ (SA cap 100L) + co-located test 66L ✓, trigger-sanitize 203L ✓, mc-check 167L ✓, authoring-fields 240L ✓ — all within 500L test ceiling. VFR RT test files: rpc-vfr-rt-start 1335→1337L (+2), rpc-vfr-rt-results 712→714L (+2), rpc-vfr-rt-constraint-regression 605→613L (+8) — pre-existing ceiling violations, minor seed additions (non-new describe blocks). useEffect in use-question-form-state.ts is form-reset-on-close (NOT data-fetch) — exempt. fetch in question-form-dialog.tsx is event-driven startTransition in onOpenChange (NOT useEffect) — exempt per task constraints. Positive: behavior-first test naming ✓, vi.hoisted pattern ✓, Readonly<Props> ✓, co-located tests for new SA ✓, { data, error } destructure on RPC call ✓.
> Commit b8bbbec5 (2026-06-13): refactor(questions) #823 editor refactor — resolves both P1 BLOCKINGs: question-form-fields.tsx 158→144L (extracts LabeledTextarea+AnswerKeyField — clean ✓), use-question-form-state.ts 103→44L (extracts build-initial-form-state.ts 65L — clean ✓). New files: answer-key-field.tsx 30L ✓ + test 62L ✓; labeled-textarea.tsx 34L ✓ (no test — pure wrapper, presentation-only, acceptable); build-initial-form-state.ts 65L (new _hooks/ utility, no co-located test — WARNING §7; indirectly covered by use-question-form-state.test.ts but no direct test file). question-form-dialog.tsx 124→148L (+24L — within 150L cap, 2L headroom; watch). VFR RT test files: rpc-vfr-rt-start 1337L (+0, seed additions from 1b5e6679), rpc-vfr-rt-submit 738L, rpc-vfr-rt-results 714L, rpc-vfr-rt-constraint-regression 613L — all pre-existing ceiling violations, not worsened by this commit. Positive: EMPTY_OPTIONS constant moved to build-initial-form-state.ts (correct colocation); Readonly<Props> on new components ✓; setter-handler builder pattern clean.
> Commit a943b17a (2026-06-13): docs(823) + Vector EA red-team spec — rpc-correct-option-id-isolation.spec.ts 130L ✓ (well within 500L ceiling). Spec is read-only hermetic (no afterEach state needed). Non-vacuous positive control (service-role read first, then student 42501) ✓. No TS/React production code changes. Docs-only commit plus spec — exempt from code-style.md limits. Clean.
> Commits 558755b7+3f80c7ca+8e8d2155 (2026-06-13): #823 P0 fix-cycle — chore(deps) esbuild pin (config/lock, exempt) + fix(db) mig 109/20260612000100 DO-block strengthen (139→151L each, well within 300L cap, SQL-only) + docs(db) 5 RPC body example syncs (docs-only, exempt). Clean.
> Commit 57642974 (2026-06-13): test(redteam) #823 — correct_option_id added to 3 MC seed inserts in injection-xss.spec.ts (336L ✓), rpc-issue-internal-exam-code.spec.ts (294L ✓), rpc-question-membership.spec.ts (234L ✓). 3 lines added, 0 removed. Pure fixture data — no logic, no new functions. All files within 500L test-file ceiling. Clean.
> Commits 27b14ec3+54c3a7ef (2026-06-13): #823 CR hardening — (a) SQL: mig 115 90→122L ✓ (new check_quiz_answer hardening: active-user gate + practice-mode guard + NULL config check; 300L cap), mig 110b/supabase-250 300→305L: WORSENED 5L (§15 carve-out comment block). mig 115/supabase-700 identical 90→122L ✓. import-schema.ts 5-line uniqueness refine ✓. (b) TS: new use-correct-option-loader.ts 62L ✓ (hook cap 80L, 18L headroom) + co-located test 133L ✓; question-form-dialog.tsx 157→125L (shrunk, well within 150L cap ✓). handleOpenChange 26L ✓, useCorrectOptionLoader body 33L ✓. rpc-check-answer integration test 184→287L ✓ (within 500L ceiling, +103L for exam-mode-oracle + soft-deleted-caller tests). Test naming behavior-first ✓. try/finally hermiticity on admin-inserted exam session ✓. No any/barrel/useEffect-data-fetch violations. WARNING: mig 110b/supabase-250 now 305L — 5L over the 300L cap, introduced by 5-line §15 rationale comment block. SECURITY DEFINER exception has been invoked repeatedly on this file; comments are load-bearing; do NOT trim. Flag if any further addition occurs.

## Durable knowledge

- **Scope is the commit diff only.** Don't flag size violations whose over-limit lines are all pre-existing (no `+` lines in the hunk) — only flag what the commit introduced or worsened. Note worsened pre-existing violations but classify them as such (e.g. quiz-session-storage 226→251).
- **Test files are exempt from line limits up to a 500-line ceiling.** Most quiz test files run 150–760 lines and are fine. (use-session-bootstrap.test.ts ~685L is pre-existing tracking, not a fresh flag.) Exemption applies to `.test.ts`/`.test.tsx` files only — shared test-infra helpers like `setup.ts` or `helpers/*.ts` are utility files subject to the 200-line cap.
- **Documented Server Action exception:** a file >100 lines holding 3+ focused exported functions (each ≤30L) plus private helpers is acceptable (e.g. flag.ts 118L: toggleFlag 32L, getFlaggedIds 29L + flagQuestion/unflagQuestion helpers).
- **`useEffect` for ResizeObserver, click-outside listeners, timer cleanup, and hydration guards is NOT a data-fetching violation** — do not flag. Only `useEffect` that fetches data is the anti-pattern.
- **Migrations:** 300-line cap. Comment-trimming to hit the cap is acceptable when the rationale lives in `docs/database.md` + commit message; never trim load-bearing decision comments (lock rationale, soft-delete-skip justification, grace-period notes). SECURITY DEFINER functions get a documented exception (e.g. migration 047; mig 087 at 360L covers two full function bodies — same exception, confirmed a94850bb).
- **Page files:** 80-line cap, composition only — consistently honored (dashboard/page 42L, report/page 46/59L).
- **Borderline watch files trending toward limits:** quiz-session.tsx and use-quiz-state.ts (toward 200L/120L); session-table.tsx ExamBadge inline styling; report-question-row.tsx (143L after 7faac459 — 7L headroom to 150L cap; flag on next growth); result-summary.tsx desktop+mobile in one file (121L after 27bdf862, +32L from hours-tier addition). Component cap is 150L — 29L headroom; flag on next growth. report-flag-context.tsx 68L (12L headroom to 80L hook/context cap; watch on next growth). question-form-dialog.tsx 125L (54c3a7ef, shrunk 148→125L after hook extraction — 25L headroom to 150L cap; no longer a watch concern). collect-user-data.test.ts 550L (c3ac3300, #684) — 10% over the 500-line test-file ceiling; flag on next growth. rpc-vfr-rt-start.integration.test.ts 1337L (1b5e6679, stable in b8bbbec5 — 167% over 500L ceiling; split is overdue. Suggested split: session-gate negative tests (8 it() blocks) → rpc-vfr-rt-questions-gate.integration.test.ts. Flag NOW — next growth should be blocked until split occurs. rpc-vfr-rt-submit.integration.test.ts 738L (stable after b8bbbec5) — 48% over 500L ceiling; split candidate when next describe block added. Flag on next growth. rpc-vfr-rt-results.integration.test.ts 714L (stable) — 43% over 500L ceiling; flag on next growth. rpc-vfr-rt-constraint-regression.integration.test.ts 613L (stable) — 23% over 500L ceiling; split candidate when next describe block is added. Flag on next growth. audit-completeness.spec.ts 592→697L→394L, rpc-cross-tenant.spec.ts 672→757L (renamed/split into rpc-cross-tenant-isolation.spec.ts 305L + rpc-cross-tenant-reports.spec.ts 441L), server-action-unauthenticated.spec.ts 504→527L→499L — all three resolved by b95360f0 (#796). **New watch:** helpers/cleanup.ts cleanupFixtures() is 137L (>30L) and helpers/audit-helpers.ts buildAnswersForSession() is 46L (>30L) — both are E2E infra helpers with single-concern sequential logic; do NOT flag, document as known-structural and watch for growth. helpers/cleanup.ts 195L (near 200L cap) — flag on next growth. mig 110b (batch_submit_quiz) = 305L (5L over 300L cap after 27b14ec3 §15 comment block — SECURITY DEFINER exception and load-bearing comments; do NOT trim; flag any further addition). mig 111 (submit_vfr_rt_exam_answers) = 290L, 10L headroom — flag on next growth.
- **Stable positive patterns** (acknowledge, don't re-litigate): behavior-first test naming; `vi.hoisted` + `vi.mock` + `vi.resetAllMocks()` mocking; `Readonly<Props>` immutability wrappers; early-fallback ternary (`== null ? fallback : value`); `new URL(url, base).searchParams` for URL-param assertions; responsive tree split (separate desktop/mobile components, not inline branches); component extraction + shared placement + all-import-site updates executed correctly (39b372e6); immutable-table hermiticity via delta assertion + `actor_id` + `gte(created_at, ...)` scoping instead of afterEach cleanup (c2758326 rate-limiting audit_events pattern); redteam specs consistently hermetic via soft-delete in `afterEach` + `try/finally` for transfer/restore + `.select('id')` zero-row observability (PR-6, test/redteam-quiz-session); read-only RPC probes (non-existent UUIDs, no DML) correctly require no afterEach — hermiticity by construction (9a728afd error-handling-disclosure.spec.ts). Actor-liveness beforeAll pre-check (rpc-record-auth-event.spec.ts, b004f206) — asserts all RPC callers have `deleted_at IS NULL` before tests run, preventing gate-2 pre-empting gate-3 assertions.
- **Immutable-table E2E pattern:** when a test produces rows in `audit_events` (append-only by security.md rule 5), hermiticity is achieved via a pre/post delta assertion scoped by `actor_id` + `created_at >= testStart` — no afterEach cleanup is needed or possible. Do not flag absence of afterEach on tests whose only writes go to immutable tables.
- **`auth.getUser()` / auth SDK calls destructuring only `{ data }` is correct.** §5 destructure rule applies to `.from()` table queries (`.select()`, `.insert()`, `.update()`, `.delete()`). Auth SDK calls (auth.getUser, auth.signIn, etc.) return `{ data, error }` but the `error` field encodes auth-layer errors, not DB errors; destructuring only `{ data }` and null-checking `data.user` is the established redteam pattern — do not flag.
- **`null as unknown as T` in integration tests is a permitted null-injection pattern.** Test files pass `null as unknown as string[]` to RPC callers to exercise null-guard branches. This is not the §5 "type casting unvalidated external data" violation — that rule targets `req.body`, `formData`, `JSON.parse()` in production code.
- **code-reviewer scope is style/structure only.** Logic, security, RLS, error-handling correctness belong to semantic-reviewer — don't overlap.
- **Bash hook files in `.claude/hooks/` are infrastructure, not TypeScript.** File-size limits (Server Action 100L, hook 80L, utility 200L) do not apply. The relevant check is consistency with sibling scripts and internal function length. `run-security-auditor.sh` at 168L is acceptable — a single cohesive pre-push gate with a fallback branch. Flag on next growth only if functions exceed ~40L.
- **Agent prompt / parser contract drift is a WARNING.** When a hook's verdict parser is updated, check that the `TMPFILE` agent prompt still instructs the agent to emit the token format the parser expects. Mismatch produces unnecessary fragility even if the parser is defensive (f7ec4a11: parser now accepts `BLOCKED` anywhere on a line, but prompt still says "last line MUST be exactly: BLOCKED").
