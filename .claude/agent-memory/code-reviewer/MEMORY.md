# Code Reviewer — Memory

> Native subagent memory index. Tracker first, durable knowledge second, topic pointers last.
> Update rows/bullets IN PLACE. No session logs — git holds history (`git log -p`).

## Recurring Issues Tracker

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---------|-----------|-------|-----------|---------------------|
| Function > 30 lines in Server Action | 2026-03-20 (d93f924) | 2 | 2026-03-20 (6520962) | PROMOTED → code-style.md §3 (getFilteredCount 58L; toggleFlag 52L — both 2-branch toggle/conditional shape, fixed by extracting named helpers) |
| New hook/utility file shipped without co-located test | 2026-03-21 (8771aa2) | 4 | 2026-03-23 (b104ae4) | PROMOTED → code-style.md §7 (use-comments.ts, use-flagged-questions.ts, score-color.ts, reports-utils.ts) |
| Hook file > 80-line limit | 2026-03-21 (8771aa2) | 4 | 2026-04-27 (c656868) | PROMOTED → code-style.md §1 (use-comments 84L; use-quiz-config 110L; use-quiz-state 121L; use-session-bootstrap 99L). Fix = extract orchestration/derivation helper |
| React component > 150-line limit | 2026-03-21 (8771aa2) | 6 | 2026-06-26 (599b8bc9) | PROMOTED → code-style.md §1 (comments-tab 155L; finish-quiz-dialog 243L; quiz-session 176/204L→155→161L; quiz-controls 152L). Fix = extract sub-component/helper component. quiz-session.tsx worsened again: ~155→161L (+6L comment block + prop change for #886 fix). finish-quiz-dialog.tsx 311→323L (aca9ba8f, +12 net from pendingAction threading + DialogFooter expansion — pre-existing violation, not worsened in intent; see durable note on file-level vs export limit; SPLIT CANDIDATE). quiz-config-form.tsx 151→169L (599b8bc9, +18L discovery mode early-return — BLOCKING; pre-existing 1L-over worsened to 19L over; SPLIT CANDIDATE: extract DiscoveryModePanel or StudyExamConfigPanel). **RESOLVED cc5b7409: quiz-config-form.tsx 169→150 (DiscoveryModePanel + StartButton extracted; exactly at cap — WATCH, flag on next growth).** |
| Feature modes (study/exam, draft/live) conditionally wired into one hook/component | 2026-04-13 (exam PR2) | 2 | 2026-04-26 (34194aa) | RULE CANDIDATE — extract mode-specific logic to its own hook/component once a file passes 120L (hook) or trends toward 200L (component). Single component switching render on an `isExam` prop is acceptable at small scale |
| Server Action file > 100-line limit | 2026-03-20 (d93f924) | 2 | 2026-06-07 (e9c2a36b) | RESOLVED (toggle-student-status.ts split to 55L ✓ via e9c2a36b — mutations extracted to toggle-student-status-mutations.ts 78L, both under limit). lookup.ts 112L still WATCHING. |
| Utility function > 30 lines (single query RPC) | 2026-05-31 (c879a259) | 10 | 2026-06-29 (#1011) | WATCHING (getSessionReports 47→64L; forceTokenRefresh 44L; insertShortAnswerQuestion 33L / insertDialogFillQuestion 32L / getRtRefs 33L / insertMcQuestion 35L — seed helpers, structural exception; seedCompletedSession 62L — 3-RPC chain, structural exception. submitQuizSession 43d7cb52 ~28→47L, 3-branch type fan-out. buildAnswerHandlers dbcd3c9e 47L (3-handler factory); isValidFeedbackEntry 42L (3-branch switch); AnswerInput 59L (3-type renderer). NEW: buildReportQuestions e92d90ee 65L (3-branch discriminated-union projector, single concern — WARNING, non-blocking). getQuizReportQuestions e92d90ee 160L (multi-RPC orchestrator, pre-existing but worsened 120L→160L — WARNING). cleanupReferenceData resolved d9548f62. handleStart dd493371 40L (3-error-path orchestrator: startStudy call + sessionStorage write try/catch + outer catch — WARNING, fix = extract writeHandoff helper). #1011: startStudy study.ts pre-existing 36L worsened to 54L (+discovery session block); getActivePracticeSession new 44L — both WARNING, fix = extract named helpers.) |
| Deep nesting > 3 levels | 2026-03-27 (75ffa51) | 2 | 2026-06-26 (cb150e26) | WATCHING (ConsentForm JSX: form→div→div→Checkbox+span→a; ensureOtherOrgBank spec helper cb150e26: `if(existing)→if(deleted_at)→if(restoreErr)` puts throw at indent level 4; fix = early-return flat chain). Two distinct mechanisms (JSX tree depth vs conditional chain) — rule already in §3. |
| Utility file > 200-line limit | 2026-04-27 (c656868) | 5 | 2026-06-27 (dd493371) | WATCHING (setup.ts resolved ade08109; quiz-session-storage.ts 267→276→279L (2ae3a94d +8L; dd493371 +3L comment expansion explaining active-session/handoff validator divergence — pre-existing 79L-over, comments-only growth; SPLIT CANDIDATE; flag on next growth); quiz-submit.ts 240L SPLIT CANDIDATE. quiz-report-questions.ts 126→204→213L (e92d90ee→dd871c00, +9L fetchAllRows callsite expansion — pre-existing violation worsened: 4L over cap → 13L over; SPLIT CANDIDATE; flag on next growth). quiz-report.ts 171→180L (dd871c00, +9L fetchAllRows callsite — within 200L utility cap ✓, WATCH). admin-quiz-report.ts 214→221→226L (e2ff0b08, +5L net comment-only — pre-existing violation, comments-only growth; SPLIT CANDIDATE). quiz-subject-queries.ts 207→206L (599b8bc9, −1L comment trim — pre-existing violation IMPROVED but still 6L over; SPLIT CANDIDATE; flag on next growth). types.ts 246→252L (599b8bc9, +6L SessionMode type + comment — pre-existing 46L-over violation worsened; BLOCKING; fix = split session-contract types to session-types.ts). **RESOLVED cc5b7409: types.ts 252→185 (session-type cluster moved to session-types.ts 75L; both within 200L cap ✓).** |
| E2E Playwright hermiticity (hard-delete without restoration) | 2026-04-30 (from §7 testing rule) | 2 | 2026-04-30 (admin-students, admin-questions pre-mig 083) | RESOLVED — distributions-rls.spec + report.spec + admin-dashboard.spec (all 2026-06-04, ad19626) apply hard-delete cascade correctly with `.beforeEach/.afterEach + createdIds.clear()` pattern; no recurrence since rule promotion. session-race-condition.spec (PR-4, 2026-06-06) newly hermetic with afterEach soft-delete + `createdSessionIds` array (session-replay was already hermetic on master) |
| `waitForTimeout` in E2E specs | 2026-06-06 (test/isolation-hygiene) | 1 | 2026-06-06 | WATCHING (exam-recovery.spec.ts: waitForTimeout(300) replaced with `waitFor`+attribute assertion — fix already applied in this commit, tracking for recurrence) |

> Count increments only on a **distinct** mechanism. Rows transition state, never deleted.
> Four "watching" count=1 rows have not recurred — that non-recurrence is itself a positive signal.

## Durable knowledge

- **Scope is the commit diff only.** Don't flag size violations whose over-limit lines are all pre-existing (no `+` lines in the hunk) — only flag what the commit introduced or worsened. Note worsened pre-existing violations but classify them as such (e.g. quiz-session-storage 226→251).
- **Test files are exempt from line limits up to a 500-line ceiling.** Most quiz test files run 150–760 lines and are fine. (use-session-bootstrap.test.ts ~685L is pre-existing tracking, not a fresh flag.) Exemption applies to `.test.ts`/`.test.tsx` files only — shared test-infra helpers like `setup.ts` or `helpers/*.ts` are utility files subject to the 200-line cap.
- **Documented Server Action exception:** a file >100 lines holding 3+ focused exported functions (each ≤30L) plus private helpers is acceptable (e.g. flag.ts 118L: toggleFlag 32L, getFlaggedIds 29L + flagQuestion/unflagQuestion helpers).
- **`useEffect` for ResizeObserver, click-outside listeners, timer cleanup, and hydration guards is NOT a data-fetching violation** — do not flag. Only `useEffect` that fetches data is the anti-pattern.
- **Migrations:** 300-line cap. Comment-trimming to hit the cap is acceptable when the rationale lives in `docs/database.md` + commit message; never trim load-bearing decision comments (lock rationale, soft-delete-skip justification, grace-period notes). SECURITY DEFINER functions get a documented exception (e.g. migration 047; mig 087 at 360L covers two full function bodies — same exception, confirmed a94850bb).
- **Page files:** 80-line cap, composition only — consistently honored (dashboard/page 42L, report/page 46/59L).
- **Borderline watch files trending toward limits:** quiz-session.tsx and use-quiz-state.ts (toward 200L/120L); end-discovery.ts 35L (right at orchestrator exception ceiling — no flag, no room to grow); page.tsx 79L (1L under 80L page cap — watch for next addition); session-table.tsx ExamBadge inline styling; result-summary.tsx desktop+mobile in one file (89L→116L after 328cec34 — still under 150L cap, but growing; flag on next significant growth). report-question-row.tsx 150→144L (328cec34 — inline formatter extracted, cap-proximity resolved ✓). Flag on next growth, not now. report-question-builder.ts 153→165→178L (8a474d63, 99f2421b — isQuestionAnswered moved in + buildDialogFill expanded for config-driven blank rendering; still under 200L utility cap ✓ — WATCH, flag on next growth). collect-user-data.test.ts 550L (c3ac3300, #684) — 10% over the 500-line test-file ceiling; flag on next growth. report-question-builder.test.ts 573→637L (99f2421b +64L omitted-blank regression + sentinel init tests — pre-existing over-500 violation, no new describe-block; flag on next describe-block addition). rpc-vfr-rt-start.integration.test.ts 1335→551→570L (8be57963 #844 SPLIT; +19L from 34098801 #921 parity rows — SPLIT CANDIDATE; flag on next describe-block addition). rpc-vfr-rt-questions.integration.test.ts 654L (new from split, within 500L ceiling ✓ — no watch needed yet). rpc-vfr-rt-submit.integration.test.ts 736→775→842L (1eaf04cc +39L §938; 2e9f7a25 +67L #839 replay test — pre-existing violation worsened; test-file exemption; no new describe-block so watch condition not triggered; SPLIT CANDIDATE). Flag on next describe-block addition. rpc-vfr-rt-results.integration.test.ts 802→838L (1eaf04cc, +36L — §938 cast-guard sweep + 3 missing error checks; pre-existing violation, consistent growth 636→712→777→802→838; SPLIT CANDIDATE — flag on next describe-block addition). **rpc-vfr-rt-constraint-regression.integration.test.ts 605→869L (070db842, +256L — mig 125 delimiter-guard describe block added; BLOCKING — 74% over 500L test-file ceiling; split the mig-125 block to rpc-vfr-rt-mig125-delimiter-guard.integration.test.ts). Resolved to 613L on master (split occurred). Worsened to 621L by 84e826bb (+8L fixture comment expansion for mig 131 dialog_fill retarget — no new describe block, split-candidate tracking continues; flag on next describe-block addition).** audit-completeness.spec.ts 592→697L→394L, rpc-cross-tenant.spec.ts 672→757L (renamed/split into rpc-cross-tenant-isolation.spec.ts 305L + rpc-cross-tenant-reports.spec.ts 441L), server-action-unauthenticated.spec.ts 504→527L→499L — all three resolved by b95360f0 (#796). server-action-unauthenticated.spec.ts 499→509→569→599L (1a068277 +5 VFR RT unauth probes; 1911d79d +1 EM probe — still under 500L ceiling ✓, SPLIT CANDIDATE growing). **New watch:** helpers/cleanup.ts cleanupFixtures() is 137L (>30L) and helpers/audit-helpers.ts buildAnswersForSession() is 46L (>30L) — both are E2E infra helpers with single-concern sequential logic; do NOT flag, document as known-structural and watch for growth. helpers/cleanup.ts 195L (near 200L cap) — flag on next growth. **question-filters.tsx 146→181L (e2ecd265, #864 imageMode toggle addition) — BLOCKING violation introduced by this commit (+36L from adding IMAGE_TOGGLES const + handleImageToggle + JSX render block); fix = extract FilterToggle sub-component + constants to sibling file. use-quiz-config.ts 77→94L (e2ecd265, #864) — BLOCKING hook cap violation (+17L); fix = extract state declarations to useQuizConfigState helper. Both need splits.** quiz-main-panel.tsx 65→116→125L (dbcd3c9e +51L; 7259b1ba +9L exam-mode guard — still under 150L cap, growing; WATCH). quiz-session-validators.ts 57→105→116→117L (dbcd3c9e+56678b99+e3f3f800, +60L total — discriminated-union switch + helpers + parity guard; WATCH, still under 200L cap); answer-handler-helpers.ts 56→111L (dbcd3c9e, +55L — 3-handler factory + type definitions added; WATCH, still under 200L cap); use-answer-handler.ts 126→113L (dbcd3c9e, −13L — IMPROVED below hook cap; disclosed pre-existing violation resolved). **draft.ts 134→148L→41L (7259b1ba worsened; 01236624 RESOLVED — Zod schema extracted to draft-schema.ts 123L→142L (e3f3f800, +19L blanks superRefine + .min(1)/.max(50) guards — still under 200L utility cap ✓). BLOCKING cleared.** check-non-mc-answer-helpers.ts 157→162L (e3f3f800, +5L verifySessionMembership PGRST116 split — still under 200L utility cap ✓). quiz-session-validators.test.ts 732→744L (e3f3f800, +12L empty-blanks guard test — pre-existing over-500L violation, no new describe block, split-candidate tracking continues; flag on next describe-block addition). quiz-session-storage.test.ts 1181→1203L (2ae3a94d, +22L mode-guard tests — pre-existing over-500L violation, no new describe block; flag on next describe-block addition). **quiz-report-questions.ts 126→204→213L (e92d90ee→dd871c00, +9L fetchAllRows callsite — pre-existing 4L-over violation now 13L over; SPLIT CANDIDATE). quiz-report.ts 171→180L (dd871c00, +9L fetchAllRows — within 200L cap ✓, WATCH). admin-quiz-report.ts 214→221→226→231L (e92d90ee +7L comments; ab60a54c +5L page<1 guard — pre-existing violation; SPLIT CANDIDATE). quiz-report-questions.integration.test.ts 431→465L (3ad6d11e, +34L sentinel init fix — within 500L ceiling ✓). quiz-report-questions.test.ts 573→639L (dd871c00, +66L fetchAllRows mock rewiring + new page-error test — pre-existing over-500L violation, no new describe-block; SPLIT CANDIDATE, flag on next describe-block addition). quiz-report.test.ts 182→226L (dd871c00, +44L fetchAllRows mock rewiring + new page-error test — within 500L ceiling ✓). **Study Mode feature (89b7a9f8 original; 599b8bc9 UI relocation to Discovery segment): page.tsx 76→70L (599b8bc9, −6L studyContent removed — well under 80L cap ✓). quiz-tabs.tsx 132→118L (599b8bc9, 3rd tab removed — 32L headroom ✓). quiz-config-form.tsx 151→169L→150L (599b8bc9 BLOCKING; cc5b7409 RESOLVED — DiscoveryModePanel + StartButton extracted; exactly at 150L cap; WATCH, flag on next growth). session-types.ts 75L (cc5b7409, new — session/start runtime type cluster; well within 200L utility cap ✓). discovery-mode-panel.tsx 29L + start-button.tsx 27→34L (cc5b7409 new; 4dcb31c3 +7L loadingLabel prop — well under 150L cap ✓, co-located tests shipped). study-config-form.tsx 113→97→104→99L (cb150e26 −16L; 599b8bc9 +7L; 4dcb31c3 −5L StartButton reuse — comfortable 51L headroom ✓). study-runner.tsx + use-study-runner.ts DELETED dd493371 (Discovery rework — reuses real quiz runner). use-study-config.ts 79→80L (dd493371, +1L userId/subjects params — exactly at 80L hook cap; WATCH, flag on next growth). use-study-start.ts ~44→67→66L (cb150e26; dd493371 −1L refactor — 14L headroom ✓). build-discovery-handoff.ts 62L NEW (utility in _hooks/ dir; under 200L cap ✓, co-located test shipped). quiz-finish-dialog-host.tsx 46L NEW (under 150L cap ✓, co-located test shipped). get-study-questions-eo.spec.ts 430→494L→503L→505L→575L (1833ace9; 14a8b9c5 +70L EO7 draft-status test + EO5 full-contract assertions + EO6 self-contained preflight — pre-existing 500L ceiling violation worsened from 5L-over to 75L-over; no new describe-block; SPLIT CANDIDATE; flag on next describe-block addition). study-queries.integration.test.ts 319→327L (14a8b9c5, +8L — finally-accumulator threading; within 500L ceiling ✓).**
- **`unknown`-typed wire-shape fields + `typeof` runtime guards pattern (study-queries.ts, 14a8b9c5):** typing every nullable `RETURNS TABLE` field as `unknown` in the internal wire-shape type (not `string | null`), then coercing in a mapper with `typeof v === 'string' ? v : null/''`, is the correct cast-guard pattern for §5. Paired with `Array.isArray(data)` + `.filter(r => ... typeof r.id === 'string')` row-level guard before `.map(toStudyQuestion)`. Acknowledge, don't re-litigate.
- **EO6 cleanup accumulator after try block (get-study-questions-eo.spec.ts, 14a8b9c5):** `let cleanupError: string | null = null` declared before try, populated inside finally, thrown via `if (cleanupError) throw new Error(...)` AFTER the try/finally — this is the correct Biome `noUnsafeFinally`-compliant pattern for single-step cleanups where a leaked row would corrupt later tests. Acknowledge, don't re-litigate.
- **Stable positive patterns** (acknowledge, don't re-litigate): behavior-first test naming; `vi.hoisted` + `vi.mock` + `vi.resetAllMocks()` mocking; `Readonly<Props>` immutability wrappers; early-fallback ternary (`== null ? fallback : value`); `new URL(url, base).searchParams` for URL-param assertions; responsive tree split (separate desktop/mobile components, not inline branches); component extraction + shared placement + all-import-site updates executed correctly (39b372e6); immutable-table hermiticity via delta assertion + `actor_id` + `gte(created_at, ...)` scoping instead of afterEach cleanup (c2758326 rate-limiting audit_events pattern); redteam specs consistently hermetic via soft-delete in `afterEach` + `try/finally` for transfer/restore + `.select('id')` zero-row observability (PR-6, test/redteam-quiz-session); read-only RPC probes (non-existent UUIDs, no DML) correctly require no afterEach — hermiticity by construction (9a728afd error-handling-disclosure.spec.ts). EN4 soft-delete/restore try/finally pattern with noUnsafeFinally compliance (rpc-report-answer-keys.spec.ts, 1911d79d): assertions live outside finally, restoreError variable accumulates infra failures checked last — so a restore failure never masks the security proof; best-effort single-step afterAll for FK-blocked auth deletes logs with console.error, does not throw. Actor-liveness beforeAll pre-check (rpc-record-auth-event.spec.ts, b004f206) — asserts all RPC callers have `deleted_at IS NULL` before tests run, preventing gate-2 pre-empting gate-3 assertions. New-dimension threading pattern (e2ecd265, #864 imageMode): adding a new tri-state filter dimension mirrors the existing CalcMode pattern end-to-end (type → Zod schema → RPC param → query helper → action → hook → component) with co-located tests in every file touched — clean commit, no violations. Pure-helper extraction pattern (8a474d63): moving domain logic out of a .tsx component module into the co-responsible lib/queries utility + relocating its tests to the new home simultaneously is the correct mechanics — test co-location and single-importer repoint executed cleanly. Dual-extraction fix pattern (cc5b7409): simultaneously resolving a component BLOCKING (extract sub-components) + a utility BLOCKING (extract type cluster to sibling file) in one commit, with co-located tests for new components + one-directional import from sibling type file, is the correct mechanics — no circular deps, 12 importer sites all import-only updates.
- **Immutable-table E2E pattern:** when a test produces rows in `audit_events` (append-only by security.md rule 5), hermiticity is achieved via a pre/post delta assertion scoped by `actor_id` + `created_at >= testStart` — no afterEach cleanup is needed or possible. Do not flag absence of afterEach on tests whose only writes go to immutable tables.
- **`auth.getUser()` / auth SDK calls destructuring only `{ data }` is correct.** §5 destructure rule applies to `.from()` table queries (`.select()`, `.insert()`, `.update()`, `.delete()`). Auth SDK calls (auth.getUser, auth.signIn, etc.) return `{ data, error }` but the `error` field encodes auth-layer errors, not DB errors; destructuring only `{ data }` and null-checking `data.user` is the established redteam pattern — do not flag.
- **`null as unknown as T` in integration tests is a permitted null-injection pattern.** Test files pass `null as unknown as string[]` to RPC callers to exercise null-guard branches. This is not the §5 "type casting unvalidated external data" violation — that rule targets `req.body`, `formData`, `JSON.parse()` in production code.
- **code-reviewer scope is style/structure only.** Logic, security, RLS, error-handling correctness belong to semantic-reviewer — don't overlap.
- **Bash hook files in `.claude/hooks/` are infrastructure, not TypeScript.** File-size limits (Server Action 100L, hook 80L, utility 200L) do not apply. The relevant check is consistency with sibling scripts and internal function length. `run-security-auditor.sh` at 168L is acceptable — a single cohesive pre-push gate with a fallback branch. Flag on next growth only if functions exceed ~40L.
- **Agent prompt / parser contract drift is a WARNING.** When a hook's verdict parser is updated, check that the `TMPFILE` agent prompt still instructs the agent to emit the token format the parser expects. Mismatch produces unnecessary fragility even if the parser is defensive (f7ec4a11: parser now accepts `BLOCKED` anywhere on a line, but prompt still says "last line MUST be exactly: BLOCKED").
- **`Readonly<Props>` rule is now formal (code-style.md §5 + §8, WARNING, #1027 / PR #1029).** CR mirror covers four path blocks: `page.tsx` + `layout.tsx` + `_components/*.tsx` + `apps/web/components/**/*.tsx` (Sonar S6759 remains the comprehensive `.tsx` backstop). ~25+ pre-existing offenders (inline-object AND named-type props, e.g. `app/page.tsx`) tracked in #1027 — sweep pending, NOT fixed in the promotion PR. Severity is WARNING (cosmetic), never BLOCKING. Also listed as item 15 in this agent's own WARNINGS checklist.

## Topic pointers

- [commit-review-log](topics/commit-review-log.md) — per-commit review notes (relocated #948; read on demand).
