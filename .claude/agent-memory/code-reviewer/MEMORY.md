# Code Reviewer — Memory

> Native subagent memory index. Tracker first, durable knowledge second, topic pointers last.
> Update rows/bullets IN PLACE. No session logs — git holds history (`git log -p`).

## Recurring Issues Tracker

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---------|-----------|-------|-----------|---------------------|
| Function > 30 lines in Server Action | 2026-03-20 (d93f924) | 2 | 2026-03-20 (6520962) | PROMOTED → code-style.md §3 (getFilteredCount 58L; toggleFlag 52L — both 2-branch toggle/conditional shape, fixed by extracting named helpers) |
| New hook/utility file shipped without co-located test | 2026-03-21 (8771aa2) | 5 | 2026-07-02 (37fc1127) | PROMOTED → code-style.md §7 (use-comments.ts, use-flagged-questions.ts, score-color.ts, reports-utils.ts). **RECURRENCE POST-PROMOTION**: load-session-diagram-guards.ts (90L, new, extracted from load-session-questions.ts to clear its 200L cap) shipped with NO co-located test — guards (isDiagramConfig/toDiagramConfigRow) covered only indirectly via load-session-questions.test.ts. Notably the commit's own sibling extractions in the same feature (report-question-answered.ts, check-non-mc-answer-helpers.ts) DID ship co-located tests — so the rule is known and usually followed, just missed here. WARNING, non-blocking per severity table, but signals the promoted rule isn't 100% sticking — consider a mechanical guard (lefthook check) if a 6th instance appears. |
| Hook file > 80-line limit | 2026-03-21 (8771aa2) | 4 | 2026-04-27 (c656868) | PROMOTED → code-style.md §1 (use-comments 84L; use-quiz-config 110L; use-quiz-state 121L; use-session-bootstrap 99L). Fix = extract orchestration/derivation helper |
| React component > 150-line limit | 2026-03-21 (8771aa2) | 6 | 2026-07-02 (03aae12d) | PROMOTED → code-style.md §1 (comments-tab 155L; finish-quiz-dialog 243L; quiz-session 176/204L→155→161L; quiz-controls 152L). Fix = extract sub-component/helper component. quiz-session.tsx worsened again: ~155→161L (+6L comment block + prop change for #886 fix). finish-quiz-dialog.tsx 311→323L→333L (aca9ba8f; 03aae12d +3L Readonly annotation expansion — pre-existing SPLIT CANDIDATE annotation-only growth; SPLIT CANDIDATE). quiz-config-form.tsx 151→169L→150L (599b8bc9 BLOCKING; cc5b7409 RESOLVED — DiscoveryModePanel + StartButton extracted; exactly at cap — WATCH, flag on next growth). **NEW 03aae12d (#1027 Readonly sweep): select.tsx 189→193L (+4L annotation multi-line formatting — pre-existing 39L-over violation worsened by annotation expansion; SPLIT CANDIDATE). alert-dialog.tsx 162→168L (+6L — pre-existing 12L-over worsened by annotation expansion; SPLIT CANDIDATE). Both are components/ui/ shadcn wrapper files; split work is separate from the Readonly sweep.** **NEW 6d56309b (Phase 6 diagram_label manual-eval redesign): rwy-2709-lh-pattern.tsx 126→155L — CROSSED cap this commit (+29L: RunwayBody component with dashed centerline + piano-key threshold markings, replacing a plain rect); BLOCKING; fix = extract RunwayBody (+ its `runway` const/helpers) to a sibling file (e.g. rwy-2709-runway-body.tsx). diagram-label-input.tsx at 150L (confirmed exactly at cap after net-zero keyboard-a11y fix 2ea9bbd — closestCenter swap; WATCH, flag on next growth).** |
| Feature modes (study/exam, draft/live) conditionally wired into one hook/component | 2026-04-13 (exam PR2) | 2 | 2026-04-26 (34194aa) | RULE CANDIDATE — extract mode-specific logic to its own hook/component once a file passes 120L (hook) or trends toward 200L (component). Single component switching render on an `isExam` prop is acceptable at small scale |
| Server Action file > 100-line limit | 2026-03-20 (d93f924) | 2 | 2026-06-07 (e9c2a36b) | RESOLVED (toggle-student-status.ts split to 55L ✓ via e9c2a36b — mutations extracted to toggle-student-status-mutations.ts 78L, both under limit). lookup.ts 112L still WATCHING. batch-submit.ts 96→100L (98371b8f, CR-round-3 comment expansion) — exactly at cap, zero headroom; WATCH, flag on next growth. |
| Utility function > 30 lines (single query RPC) | 2026-05-31 (c879a259) | 12 | 2026-07-02 (53dddf15) | WATCHING (getSessionReports 47→64L; forceTokenRefresh 44L; insertShortAnswerQuestion 33L / insertDialogFillQuestion 32L / getRtRefs 33L / insertMcQuestion 35L — seed helpers, structural exception; seedCompletedSession 62L — 3-RPC chain, structural exception. submitQuizSession 43d7cb52 ~28→47L, 3-branch type fan-out. buildAnswerHandlers dbcd3c9e 47L (3-handler factory); isValidFeedbackEntry 42L (3-branch switch); AnswerInput 59L (3-type renderer). buildReportQuestions e92d90ee 65L (3-branch discriminated-union projector). getQuizReportQuestions e92d90ee 160L. cleanupReferenceData resolved d9548f62. handleStart dd493371 40L. startStudy 54L; getActivePracticeSession 44L. NEW 53dddf15 ELP Slice-1c-A: startOralExam (start-oral-exam.ts) 44L — SA-orchestrator exception is 35L ceiling; 9L over (Zod try/catch 6L + main try 35L + outer catch 3L); fix = extract `validateMode` or inline error-message ternary to named helper. getOralExamSession (oral-exam-session.ts) 31L — 1L over; fix = extract `toResponses(raw: unknown)` private helper alongside toSections/toSummary.) |
| Deep nesting > 3 levels | 2026-03-27 (75ffa51) | 2 | 2026-06-26 (cb150e26) | WATCHING (ConsentForm JSX: form→div→div→Checkbox+span→a; ensureOtherOrgBank spec helper cb150e26: `if(existing)→if(deleted_at)→if(restoreErr)` puts throw at indent level 4; fix = early-return flat chain). Two distinct mechanisms (JSX tree depth vs conditional chain) — rule already in §3. |
| Utility file > 200-line limit | 2026-04-27 (c656868) | 6 | 2026-07-02 (d74f3e0e) | WATCHING (setup.ts resolved ade08109; quiz-session-storage.ts 267→276→279L (2ae3a94d +8L; dd493371 +3L comment expansion explaining active-session/handoff validator divergence — pre-existing 79L-over, comments-only growth; SPLIT CANDIDATE; flag on next growth); quiz-submit.ts 279→239L IMPROVED (d74f3e0e, −40L — fan-out logic extracted to quiz-submit-fanout.ts; still watch for next growth). quiz-report-questions.ts 126→204→213L (e92d90ee→dd871c00, +9L fetchAllRows callsite expansion — pre-existing violation worsened: 4L over cap → 13L over; SPLIT CANDIDATE; flag on next growth). admin-quiz-report.ts 214→221→226L (e2ff0b08, +5L net comment-only — pre-existing violation, comments-only growth; SPLIT CANDIDATE). quiz-subject-queries.ts 207→206L (599b8bc9, −1L comment trim — pre-existing violation IMPROVED but still 6L over; SPLIT CANDIDATE; flag on next growth). types.ts 246→252L (599b8bc9, +6L SessionMode type + comment — pre-existing 46L-over violation worsened; BLOCKING; fix = split session-contract types to session-types.ts). **RESOLVED cc5b7409: types.ts 252→185 (session-type cluster moved to session-types.ts 75L; both within 200L cap ✓).** helpers/seed.ts 666→670L→**RESOLVED bf85518f (#1047)** — split into 6 sibling modules (seed-core 81L, seed-users 198L, seed-quiz 170L, seed-responses 144L, seed-consent 54L, seed-markers 25L); all under 200L cap ✓; no barrel; direct imports repointed across 48 spec importers. seed-users.ts at 198L — 2L headroom, WATCH. **RESOLVED 6d56309b (Phase 6 diagram_label fixup, same PR as d74f3e0e): quiz-report.ts 214→199L (DiagramZoneResult + diagram union arm extracted to new quiz-report-diagram-types.ts 23L) and report-question-builder.ts 208→184L (isQuestionAnswered extracted to new report-question-answered.ts 25L) — both back under the 200L cap, both extraction targets are real single-purpose modules (not barrel re-exports), one-directional imports (quiz-report.ts → quiz-report-diagram-types.ts; report-question-answered.ts → quiz-report.ts), no circularity. Positive signal: BLOCKING → RESOLVED in the very next commit on the same branch. check-non-mc-answer-helpers.ts 157→200L exactly at cap — WATCH.** |
| E2E Playwright hermiticity (hard-delete without restoration) | 2026-04-30 (from §7 testing rule) | 2 | 2026-04-30 (admin-students, admin-questions pre-mig 083) | RESOLVED — distributions-rls.spec + report.spec + admin-dashboard.spec (all 2026-06-04, ad19626) apply hard-delete cascade correctly with `.beforeEach/.afterEach + createdIds.clear()` pattern; no recurrence since rule promotion. session-race-condition.spec (PR-4, 2026-06-06) newly hermetic with afterEach soft-delete + `createdSessionIds` array (session-replay was already hermetic on master) |
| `waitForTimeout` in E2E specs | 2026-06-06 (test/isolation-hygiene) | 1 | 2026-06-06 | WATCHING (exam-recovery.spec.ts: waitForTimeout(300) replaced with `waitFor`+attribute assertion — fix already applied in this commit, tracking for recurrence) |

> Count increments only on a **distinct** mechanism. Rows transition state, never deleted.
> Four "watching" count=1 rows have not recurred — that non-recurrence is itself a positive signal.

## Durable knowledge

- **Scope is the commit diff only.** Don't flag size violations whose over-limit lines are all pre-existing (no `+` lines in the hunk) — only flag what the commit introduced or worsened. Note worsened pre-existing violations but classify them as such (e.g. quiz-session-storage 226→251).
- **Test files are exempt from line limits up to a 500-line ceiling.** Most quiz test files run 150–760 lines and are fine. (use-session-bootstrap.test.ts ~685L is pre-existing tracking, not a fresh flag.) Exemption applies to `.test.ts`/`.test.tsx` files only — shared test-infra helpers like `setup.ts` or `helpers/*.ts` are utility files subject to the 200-line cap.
- **Documented Server Action exception:** a file >100 lines holding 3+ focused exported functions (each ≤30L) plus private helpers is acceptable (e.g. flag.ts 118L: toggleFlag 32L, getFlaggedIds 29L + flagQuestion/unflagQuestion helpers).
- **`useEffect` for ResizeObserver, click-outside listeners, timer cleanup, and hydration guards is NOT a data-fetching violation** — do not flag. Only `useEffect` that fetches data is the anti-pattern.
- **§5 mutations in integration test `finally` restore blocks** — `await admin.from(...).update(...)` without `{ error }` destructuring inside a `finally` is a §5 violation: a silent restore failure leaves corrupted state for the next test run. Flag it (WARNING). First seen: 9e54fcf5 resume.integration.test.ts. **RESOLVED 1e757201**: the `questions.update()` restore block now properly destructures `{ error: restoreErr }` and logs on failure.
- **Migrations:** 300-line cap. Comment-trimming to hit the cap is acceptable when the rationale lives in `docs/database.md` + commit message; never trim load-bearing decision comments (lock rationale, soft-delete-skip justification, grace-period notes). SECURITY DEFINER functions get a documented exception (e.g. migration 047; mig 087 at 360L covers two full function bodies — same exception, confirmed a94850bb).
- **Page files:** 80-line cap, composition only — consistently honored (dashboard/page 42L, report/page 46/59L).
- **Borderline watch files trending toward limits** — full per-commit history → [topics/file-size-watch-log.md](topics/file-size-watch-log.md). Current still-relevant state: **BLOCKING**: question-filters.tsx 181L (fix: extract FilterToggle sub-component + constants); use-quiz-config.ts 94L hook cap (fix: extract useQuizConfigState helper). **SPLIT CANDIDATE**: report-question-builder.test.ts 637L; rpc-vfr-rt-start.integration.test.ts 570L; rpc-vfr-rt-submit.integration.test.ts 842L; rpc-vfr-rt-results.integration.test.ts 838L; rpc-vfr-rt-constraint-regression.integration.test.ts 621L; server-action-unauthenticated.spec.ts 614L; quiz-main-panel.test.tsx 706L; quiz-session-validators.test.ts 744L; admin-quiz-report.ts 231L; quiz-report-questions.ts 213L; quiz-report-questions.test.ts 639L; rpc-report-answer-keys.spec.ts 610L; rpc-check-non-mc-answer-diagram.integration.test.ts 550L. **WATCH (near cap/growing)**: quiz-session.tsx (toward 200L); page.tsx (quiz report) 79L; session-table.tsx ExamBadge inline styling; result-summary.tsx 116L; collect-user-data.test.ts 550L; quiz-session-storage.test.ts 1203L; quiz-main-panel.tsx 125L; answer-handler-helpers.ts 111L; quiz-report.ts 180L; helpers/cleanup.ts 195L; quiz-config-form.tsx 150L (exactly at cap); use-study-config.ts 80L (exactly at hook cap); get-study-questions-eo.spec.ts 416L; rpc-get-quiz-questions-ordering.integration.test.ts 486L. **CROSSED cap**: audit-completeness.spec.ts 583L (WARNING; fix: extract VFR-RT describe to audit-vfr-rt.spec.ts sibling). **Structural exceptions (E2E infra helpers — do NOT flag)**: helpers/audit-helpers.ts; helpers/get-study-questions-eo-setup.ts 386L + ensureEoSoftDelStudent 75L; helpers/seed-vfr-rt-pool.ts 382L + cleanupVfrRtPool ~36L. **RESOLVED / no longer live-tracked** (full detail in topic file): use-quiz-state.ts, report-question-row.tsx, report-question-builder.ts (6d56309b), rpc-cross-tenant*.spec.ts (split), draft.ts/draft-schema.ts, use-answer-handler.ts, quiz-session-validators.ts, quiz-tabs.tsx, session-types.ts, discovery-mode-panel.tsx, start-button.tsx, study-config-form.tsx, study-runner.tsx/use-study-runner.ts (deleted), use-study-start.ts, build-discovery-handoff.ts, quiz-finish-dialog-host.tsx, study-queries.integration.test.ts, quiz-report.test.ts, quiz-report-questions.integration.test.ts, page.tsx (Discovery) 70L, check-non-mc-answer-helpers.ts 162L, end-discovery.ts 35L (static, no room to grow).
- **`unknown`-typed wire-shape fields + `typeof` runtime guards pattern (study-queries.ts, 14a8b9c5):** typing every nullable `RETURNS TABLE` field as `unknown` in the internal wire-shape type (not `string | null`), then coercing in a mapper with `typeof v === 'string' ? v : null/''`, is the correct cast-guard pattern for §5. Paired with `Array.isArray(data)` + `.filter(r => ... typeof r.id === 'string')` row-level guard before `.map(toStudyQuestion)`. Acknowledge, don't re-litigate.
- **EO6 cleanup accumulator after try block (get-study-questions-eo.spec.ts, 14a8b9c5):** `let cleanupError: string | null = null` declared before try, populated inside finally, thrown via `if (cleanupError) throw new Error(...)` AFTER the try/finally — this is the correct Biome `noUnsafeFinally`-compliant pattern for single-step cleanups where a leaked row would corrupt later tests. Acknowledge, don't re-litigate.
- **Stable positive patterns** (acknowledge, don't re-litigate): behavior-first test naming; `vi.hoisted` + `vi.mock` + `vi.resetAllMocks()` mocking; `Readonly<Props>` immutability wrappers; early-fallback ternary (`== null ? fallback : value`); `new URL(url, base).searchParams` for URL-param assertions; responsive tree split (separate desktop/mobile components, not inline branches); component extraction + shared placement + all-import-site updates executed correctly (39b372e6); immutable-table hermiticity via delta assertion + `actor_id` + `gte(created_at, ...)` scoping instead of afterEach cleanup (c2758326 rate-limiting audit_events pattern); redteam specs consistently hermetic via soft-delete in `afterEach` + `try/finally` for transfer/restore + `.select('id')` zero-row observability (PR-6, test/redteam-quiz-session); read-only RPC probes (non-existent UUIDs, no DML) correctly require no afterEach — hermiticity by construction (9a728afd error-handling-disclosure.spec.ts). EN4 soft-delete/restore try/finally pattern with noUnsafeFinally compliance (rpc-report-answer-keys.spec.ts, 1911d79d): assertions live outside finally, restoreError variable accumulates infra failures checked last — so a restore failure never masks the security proof; best-effort single-step afterAll for FK-blocked auth deletes logs with console.error, does not throw. Actor-liveness beforeAll pre-check (rpc-record-auth-event.spec.ts, b004f206) — asserts all RPC callers have `deleted_at IS NULL` before tests run, preventing gate-2 pre-empting gate-3 assertions. New-dimension threading pattern (e2ecd265, #864 imageMode): adding a new tri-state filter dimension mirrors the existing CalcMode pattern end-to-end (type → Zod schema → RPC param → query helper → action → hook → component) with co-located tests in every file touched — clean commit, no violations. Pure-helper extraction pattern (8a474d63): moving domain logic out of a .tsx component module into the co-responsible lib/queries utility + relocating its tests to the new home simultaneously is the correct mechanics — test co-location and single-importer repoint executed cleanly. Dual-extraction fix pattern (cc5b7409): simultaneously resolving a component BLOCKING (extract sub-components) + a utility BLOCKING (extract type cluster to sibling file) in one commit, with co-located tests for new components + one-directional import from sibling type file, is the correct mechanics — no circular deps, 12 importer sites all import-only updates. Marker-based pre-sweep pattern (877da444, rpc-report-answer-keys.spec.ts): replacing in-memory `createdQuestionIds` Set with `.like('question_number', EN_MARKER_LIKE)` pre-sweep in beforeAll + marker-scoped afterAll cleanup is the correct crash-resilient hermiticity pattern — a killed process empties the Set but leaves orphan rows; the marker-based cleanup survives and handles both cases. All mutations correctly destructure `{ error }`; no `any` types; nesting within limits.
- **Immutable-table E2E pattern:** when a test produces rows in `audit_events` (append-only by security.md rule 5), hermiticity is achieved via a pre/post delta assertion scoped by `actor_id` + `created_at >= testStart` — no afterEach cleanup is needed or possible. Do not flag absence of afterEach on tests whose only writes go to immutable tables.
- **`auth.getUser()` / auth SDK calls destructuring only `{ data }` is correct.** §5 destructure rule applies to `.from()` table queries (`.select()`, `.insert()`, `.update()`, `.delete()`). Auth SDK calls (auth.getUser, auth.signIn, etc.) return `{ data, error }` but the `error` field encodes auth-layer errors, not DB errors; destructuring only `{ data }` and null-checking `data.user` is the established redteam pattern — do not flag.
- **`null as unknown as T` in integration tests is a permitted null-injection pattern.** Test files pass `null as unknown as string[]` to RPC callers to exercise null-guard branches. This is not the §5 "type casting unvalidated external data" violation — that rule targets `req.body`, `formData`, `JSON.parse()` in production code.
- **code-reviewer scope is style/structure only.** Logic, security, RLS, error-handling correctness belong to semantic-reviewer — don't overlap.
- **Bash hook files in `.claude/hooks/` are infrastructure, not TypeScript.** File-size limits (Server Action 100L, hook 80L, utility 200L) do not apply. The relevant check is consistency with sibling scripts and internal function length. `run-security-auditor.sh` at 168L is acceptable — a single cohesive pre-push gate with a fallback branch. Flag on next growth only if functions exceed ~40L.
- **Agent prompt / parser contract drift is a WARNING.** When a hook's verdict parser is updated, check that the `TMPFILE` agent prompt still instructs the agent to emit the token format the parser expects. Mismatch produces unnecessary fragility even if the parser is defensive (f7ec4a11: parser now accepts `BLOCKED` anywhere on a line, but prompt still says "last line MUST be exactly: BLOCKED").
- **`Readonly<Props>` rule is now formal (code-style.md §5 + §8, WARNING, #1027 / PR #1029).** CR mirror covers four path blocks: `page.tsx` + `layout.tsx` + `_components/*.tsx` + `apps/web/components/**/*.tsx` (Sonar S6759 remains the comprehensive `.tsx` backstop). Sweep (PR #1050, 03aae12d): 142+ `.tsx` files updated — annotation-only, all clean except three **double-wrap** cases where the named type was already `Readonly<{...}>`: `kpi-cards.tsx` (CardProps + Props), `result-summary.tsx` (Props), `admin-report-card.tsx` (Props). `Readonly<Readonly<...>>` is harmless (TS collapses it); the param-site wrapper is still what Sonar S6759 checks. Report double-wraps as WARNING only, never BLOCKING. Severity is WARNING (cosmetic), never BLOCKING. Also listed as item 15 in this agent's own WARNINGS checklist.
- **Readonly sweep on components/ui/ shadcn wrappers:** complex intersection types (`Foo.Props & { extraProp }`) expand to multiple lines when wrapped in `Readonly<>`, adding 2-4 lines per component. For files already over the 150L component limit, this worsens the count by annotation-only lines. Do not call these BLOCKING; note as pre-existing violations worsened by annotation expansion; split work is separate. (First seen 03aae12d #1027 sweep: select.tsx 189→193L, alert-dialog.tsx 162→168L.)

## Topic pointers

- [commit-review-log](topics/commit-review-log.md) — per-commit review notes (relocated #948; read on demand).
- [file-size-watch-log](topics/file-size-watch-log.md) — full chronological file-size growth history behind the "Utility file > 200-line limit" tracker row and the "Borderline watch files" bullet (relocated 2026-07-04; read on demand).
