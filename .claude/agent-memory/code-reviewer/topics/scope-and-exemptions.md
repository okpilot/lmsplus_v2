# Scope and exemptions — the long-form entries

Split out of `MEMORY.md` on 2026-09-14 when that file reached 23,447 bytes against the 25 KB
injection cap. Injection truncates at 200 lines OR 25 KB, whichever is smaller, and it is
SILENT — content past the cap is simply invisible. Nothing here was edited in the move.

- **Borderline watch files** — full history (per-file growth chains, SPLIT CANDIDATE list, resolved items) → [file-size-watch-log](topics/file-size-watch-log.md), updated 2026-09-09. E2E/infra helpers are structural exceptions — do NOT flag. Live BLOCKING-adjacent: rwy-2709-layout.ts 194L (200L cap, 6L headroom); `.claude/pipeline.test.mjs` 443L — test-file rule, cap 500, compliant. Live SPLIT CANDIDATEs (3rd+ consecutive growth): students/[id]/queries.test.ts 586L; attempts-queries.test.ts 564L; admin-quiz-report.test.ts 1158L; ~12 other over-500L test files (list in topic file). As of 7ca1f522 these are all `.claude/limits.json` `baseline` entries now — the ratchet mechanically enforces an EXACT baseline match (a growth needs an argument in the PR body), so this bullet is a pre-commit early-warning aid, not the enforcement mechanism.

- **Rules/memory-only commits (`.claude/rules/*.md`, `.claude/agent-memory/**`, no code) — §1-9 are N/A; only §10 (Comment Accuracy) applies.** Verify every cited commit SHA, count, and "instance N" narrative before clearing, using the artifact checks in `agent-workflow.md` § Finding Validation (`git show --stat <sha> -- <path>` + `git grep`) — READ that section for the exact form rather than working from this line: it carries a `git log -1` trap and a merge-commit flag, and this copy has already drifted twice by restating the mechanics instead of pointing at them — these commits routinely cite prior commits and tracker counts as evidence (e.g. `b177a3d2`, a learner-row promotion citing `20a14793`/`ab737599` and a count=5 history; all five instances traced and confirmed accurate against `cross-agent-lessons.md`/`tracker-archive.md` and git history). Also re-check §10 rule 3 (partial-edit staleness) with a repo-wide grep of the retracted status-cell text when a tracker row transitions state. Second clean instance: `c787b0d2` (applies b177a3d2's own findings) — cited footer line numbers (:833, :110 vs. a stale :746/:32 from doc-updater's prior report) verified against `git show b177a3d2:<path> | grep -n`, the `agent-workflow.md:819` CONSTRAINTS citation verified by line number, the new doc-updater.md section's backward-reference to `agent-doc-updater.md`'s NEVER list verified to exist, and all four edited files' trailing bytes confirmed `0a` — those four checks clean. NOT a clean-commit verdict: `c787b0d2` shipped two false claims OUTSIDE what this pass checked (a ".claude/rules/agent-doc-updater.md is orchestrator-facing and does not reach you" claim, and "Three recorded instances" when the tracker held two), both caught by semantic-reviewer post-commit and fixed in the follow-up. Report what was checked, never a scope you did not cover. Same applies to a hooks-only + docs commit (no `.ts`/`.tsx`): `24c2bd70` (`.claude/hooks/check-file-size-guard{,.test,.update.test}.mjs` + `docs/decisions.md`) — §1-9 N/A (no TS/component code), §10 fully checked and CLEAN: mutation-verified both new tests (revert `declaresUseServer`→old regex reddens exactly the one new test; revert the `LIMITS`-derived extension loop→hardcoded list reddens exactly the one updated test), re-ran `--stats` with old vs. new `declaresUseServer` over the live repo and diffed byte-identical (confirms the file's own "no tracked file trips it today" and the commit message's "51 over, zero differing paths"), and re-derived decisions.md's two populations from scratch (11 total / 7 no-directive / 4 directive under `actions/`; 4 directive files outside `actions/`, 1 over cap) — all matched exactly.

- **Hook files in `.claude/hooks/`** and **operational scripts in `apps/web/scripts/`** are infrastructure — file-size limits and style rules (function length, nesting, missing tests) do not apply. Neither ships in the application bundle; `apps/web/scripts` is named in `apps/web/tsconfig.json`'s `exclude` array, and the `.mjs` hooks sit outside any TS project. Matches suppression #8 in `agent-code-reviewer.md`. **#1219 fixed in `1335c73d`** (`Closes #1219`; the issue closes on MERGE — `gh issue view` still reports OPEN until then). `apps/web/tsconfig.scripts.json` covers the directory and is chained into `pnpm check-types`, so the earlier "no tsconfig coverage at all" caveat is RETRACTED. The suppression's structural rationale still holds (scripts do not ship in the bundle). Keep reporting genuine findings there at INFO — coverage is narrow: only `import-questions.ts` and `cleanup-orphaned-draft-sessions.ts` type against `Database`, so a wrong column name still compiles elsewhere. WATCH (re-derived with `wc -l` 2026-08-18, not re-typed): `diagram-content.ts` 255L and `mc-content.ts` 228L both over the 200L utility cap; `dialog-fill-content.test.ts` 522L over the 500L test cap and `mc-content.test.ts` 503L (489+14, 6cf8e1fc) now over it — both scripts-exempt; `import-vfr-rt-content.ts` nearly 2kL with `main()`/`insertAll`/`planReplaceAll` all long.

- **Platform-behavior claims in agent definitions** (1b4ee552, 2026-09-06): §1-9 N/A for rules/agents/hooks-only commits. §10 applies: watch for platform claims (e.g., `tools:` vs `memory:` interaction) stated as established fact when only verifiable at next-session restart. The correct pattern: "per the official subagent docs — confirm at next session restart." If such a claim is wrong and goes unflagged, downstream agents silently lose capabilities (e.g., 8 tracker writes breaking with no error). Follow-up 29cd8d0f hedged agent-memory.md but left agent-workflow.md:743 stating the same memory-write claim as established fact — inconsistency between two files edited in the same commit (WARNING, not fixed yet).

- **MUTATION comment accuracy** (§7 rule promoted from count=4 violations — 714da90e, 2026-09-14): two new MUTATION comments added and verified. Both name directly reachable mechanisms: Test 1 targets the `message: git(['log', '-1', '--format=%B', sha])` line in `main()`; Test 2 targets the per-commit iteration pattern (vs. concatenation). The rule is: name only mechanisms the fixture can reach, not aspirational semantics. CLEAN instance. Second CLEAN instance e8e3e20b (2026-09-14): four MUTATION comments in new check-retracted-phrase.base.test.mjs — all four verified reachable against production check-retracted-phrase.mjs: (1) per-commit message passing in --base dispatch `main()`, (2) cross-commit waiver concatenation in the for-unit loop, (3) `grepScope` returning `['--cached']`, (4) `*/tasks.md` glob passed to ls-tree in `completedSpecDirs`. Same-commit split also correct: base.test.mjs 182L + repo.test.mjs 438L both under 500L cap; CI step wired; 2 tests moved + 2 new = net +2 (54 total, was 52). Third CLEAN instance e97ce5a4 (2026-09-14): one MUTATION comment in new merge-commit test — names `--no-merges` in the rev-list enumeration (`check-retracted-phrase.mjs` line ~521); fixture direction verified (trunk merges PR, not the reverse — the first attempt used the wrong direction and passed with `--no-merges` removed). 55 tests, all passing.
