# Semantic Reviewer — Tracker Archive

> Terminal-state tracker rows (PROMOTED / RESOLVED / RESOLVED-WATCH / FALSE POSITIVE) relocated VERBATIM from MEMORY.md to keep the auto-injected index under the 25 KB / 200-line budget. Rows are retained, not deleted — counts and statuses are unchanged. See `.claude/rules/agent-memory.md`.

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---------|-----------|-------|-----------|---------------------|
| Multi-permissive RLS SELECT table read inside a per-caller RPC over-scopes unless body adds explicit `WHERE owner_col = auth.uid()` | 2026-05-26 (#540/BW3) | 3+ | 2026-05-29 (get_student_profile_stats) | PROMOTED → security.md §11 / docs/security.md §3. Confirmed count=2 across two tables (student_responses, quiz_sessions); appears in SQL comment of every fn touching those tables |
| Supabase query `{ data }` destructured without `{ error }` (non-mutation reads) — silent null fallback hides missing table / unrun migration | 2026 (dashboard-stats) | 7 | 2026-06-06 (PR-4 test/redteam-sweeps #722 #624) | PROMOTED → code-style.md §5 (new SELECT subsection + §8 checklist bullet); .coderabbit.yaml synced. Repo-wide sweep: 11 read sites + 1 observability site fixed. PR-4 extends sweep to 16 integration + E2E files (read-after-seed `.single()` sites + seed helpers). New pattern to watch: Server Action wrapping a now-throwing query helper with no try/catch — throws propagate to client as React error boundary activation (acceptable but behavioral change from silent []). Flag on next occurrence. |
| PostgREST serializes NUMERIC/bigint aggregate columns as strings — `row.n` is a string, needs `Number()` coercion | 2026 (PR #680 quiz.ts) | 4 | 2026-05-31 (reports.ts score_percentage NUMERIC, c5df103b) | RESOLVED — c5df103b coerced score_percentage (NUMERIC) with null-preserving `=== null ? null : Number(r.score_percentage)`; widened RpcRow field to `number \| string \| null`; added fractional-string + null tests. Prior assessment "already number \| null → acceptable" was wrong — PostgREST serializes fractional NUMERIC as strings too. NOTE: types.ts manually types score_percentage as `number` (non-nullable) in get_session_reports Returns block — the Postgres schema is `NUMERIC` (nullable), so types.ts is a type lie (nullability drift). The BIGINT coercion is correct in reports.ts RpcRow; types.ts drift is pre-existing, not introduced by #471. WATCHING: every RPC that returns a NUMERIC computed column must be audited for the same gap. |
| PostgREST serializes NUMERIC/bigint aggregate columns as strings — `row.n` is a string, needs `Number()` coercion | 2026 (PR #680 quiz.ts) | 3 | 2026-06-01 (#717 sweep) | PROMOTED → code-style.md §5 (Number() coercion rule). Repo sweep done for 16 explicit sites. Missed: `weakestSubject.avgMastery` in `getDashboardKpis` — cast as `{ avgMastery: number }` without `Number()`, NUMERIC survives in JSONB as string; JS relational operators happen to coerce so no visible break today, but type lie and no string-wire test. |
| Fire-and-forget Server Action + immediate `router.push` to a page that re-triggers the cleared guard = race window (~200–800ms) | 2026-04 (b9829c0) | 2 | 2026-04-14 (ce67aee) | RESOLVED — `await discardQuiz(...).catch(err => console.error(...))` before redirect. Standard fix |
| Paginated read test suites cover the getCount error path but not the getPage error path (caller-level propagation) | 2026-05-27 (PR #681) | 2 | 2026-06-01 (#699) | PROMOTED → code-style.md §7 "Paginated Fetch Needs a Caller-Level Page-Error Test" + .coderabbit.yaml synced. Sweep: 7 fetchUser* pass-throughs in collect-user-data-queries.test.ts; comment-queries + students-queries were already covered (#668 #7). |
| E2E sentinel idempotency guard uses strict `count === 8` — a partial prior run that left 1–7 rows bypasses the guard and inserts another full batch (duplicate dates → still harmless for DISTINCT-based RPCs, but the guard is brittle; `count >= 8` or a `count > 0` bail-out would be more robust) | 2026-05-31 (PR #673) | 1 | 2026-05-31 | RESOLVED — fixed to `(count ?? 0) >= 8` in commit 52e7020e (#673 PR-level sweep confirmed) |
| Cross-org mastery test `every(correct === 0)` is vacuously true when the cross-org org has zero questions (empty array returns true for `.every()`) — assert `data !== null` or add a positive control that proves data is non-empty when the subject org has questions | 2026-05-31 (PR #673) | 1 | 2026-05-31 | RESOLVED — fixed in commit f2cb9a5d: `expect(data ?? []).toHaveLength(0)` added before the every() guard; vacuity clearly documented in comment (the non-vacuous self-scope proof delegates to BW3 in dashboard-stats-rpc-isolation.spec.ts) |
| Red-team unauth SELECT test for a table (e.g. flagged_questions, question_comments) with no dedicated seed → 0-row result is vacuous on fresh/empty DB (RLS vs absence-of-data indistinguishable). flag-idor.spec.ts correctly addresses this for flagged_questions; question_comments unauth read test has no seed and remains vacuous. | 2026-05-31 (#314) | 1 | 2026-05-31 | RESOLVED — commit b365f1e1 seeds victim-owned comment + flag via admin client in beforeAll (server-action-unauthenticated.spec.ts), hermetic soft-delete in afterAll. Both tests now non-vacuous. |
| `typeof d.passed === 'boolean'` asserted on a `quick_quiz` session — but `quiz_sessions.passed` is `BOOLEAN NULL` (schema comment: "NULL for study mode or incomplete sessions"), and `batch_submit_quiz` never sets `v_passed` for non-exam modes, so `d.passed` is JS `null` → `typeof null === 'object'`, not `'boolean'`. Assertion fails on every `quick_quiz` #257 run. | 2026-06-04 (088a69ea #743) | 1 | 2026-06-04 | RESOLVED — 74ac8e91 replaced `typeof d.passed === 'boolean'` with `expect(d.passed).toBeNull()` and widened `BatchScalars.passed` to `boolean \| null`. Cross-response equality retained as `d2.passed === d1.passed` (null===null). Contract verified against mig 20260601000001 lines 245-253. |
| `vi.mock(factory)` for a module that is ONLY ever `await import()`-ed lazily (e.g. dynamic import inside `register()`) + `vi.resetModules()` in `afterEach` only — claimed: first test sees a stale cached module so the factory isn't re-invoked and the spy reads count=0. | 2026-06-06 (test/isolation-hygiene) | 1 | 2026-06-06 | FALSE POSITIVE — disproven by running instrumentation test 1 in isolation: it PASSES alone, so the factory DID run during test 1. A `vi.mock` factory runs lazily at FIRST import resolution; for a dynamically-imported mocked module there is no file-load import, so the first resolution happens inside the triggering test → factory runs → spy count=1. No stale cache exists for test 1. (Moving `resetModules` to `beforeEach` is a harmless robustness nicety but is NOT required and the vacuity claim is wrong for lazily-imported mocks.) |
| E2E advisory-lock BI test: admin-seeded pre-condition rows could be inflated by prior-test state if `afterEach` for the preceding test fails — `preBurstRows.length !== 19` guard catches and fails loudly, preventing a vacuous assertion but causing a spurious failure. Fix: explicit pre-test cleanup before re-seeding. | 2026-06-06 (quiz-draft-injection BI, PR-7) | 1 | 2026-06-06 | RESOLVED — explicit pre-seed cleanup added in PR test/redteam-hardening-batch #770 commit |
| Integration test `cleanupTestData` does not delete `easa_subjects`/`easa_topics` — reference data accumulates across multiple integration test runs. Upsert-on-conflict makes this harmless today (same code always upserts the same row), but non-idempotent test helpers using `seedReferenceData` + `cleanupTestData` are not fully hermetic. Pre-existing pattern across all integration tests, not introduced by this PR. | 2026-06-06 (#551 review) | 1 | 2026-06-06 | RESOLVED (#775, PR pending) — cleanupReferenceData helper added; 7 suites clean their seeded easa_* rows in afterAll |
| E2E try/finally restore with pre-try assertions that mutate shared state: `expect()` inside `try` throws on failure (Playwright), so `finally` runs but the post-finally `expect(restoreError)` is never reached — restore failure is silently dropped when a prior assertion fails. Separate concern: soft-delete that precedes the `try` block is not wrapped → if that `expect()` throws, question stays soft-deleted permanently, poisoning downstream specs. | 2026-06-04 (35eb5d84 #256) | 2 | 2026-06-04 (81b708be amended) | RESOLVED (primary) — soft-delete moved inside `try` in 81b708be; finally now always restores. Residual: post-finally `expect(restoreError)` unreachable when `try` throws — restore failure silently dropped on prior-assertion failure. Accepted pattern in this codebase (hermiticity is served by the finally running; the post-finally assertion guards the happy path). WATCHING for recurrence. |
