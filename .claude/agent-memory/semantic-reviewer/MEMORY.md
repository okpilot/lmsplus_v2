# Semantic Reviewer — Memory

> Native subagent memory index. Tracker first, durable knowledge second, topic pointers last.
> Update rows/bullets IN PLACE. No session logs — git holds history (pre-migration body: `git show 2e87c3e6:.claude/agent-memory/semantic-reviewer/patterns.md`; `git log` after).
> Scope: logic / security / RLS / query-correctness at CodeRabbit depth. Style & file-size belong to code-reviewer — don't overlap.

## Recurring Issues Tracker

> Only patterns that recurred ≥2× as distinct mechanisms are tracked. Count increments on a distinct occurrence, never a re-mention. Rows transition state, never deleted.
> Terminal-state rows (PROMOTED/RESOLVED/RESOLVED-WATCH/FALSE POSITIVE) are relocated verbatim to [tracker-archive](topics/tracker-archive.md) — retained, still counted, not deleted. Live rows below are the active (WATCHING / RULE CANDIDATE) set.

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---------|-----------|-------|-----------|---------------------|
| Client terminal path (submit/discard/timeout) redirects + clears localStorage but never calls a Server Action to end/soft-delete the DB session → orphaned `quiz_sessions` row with `ended_at IS NULL` blocks next `start_exam_session` | 2026-04 (b9829c0) | 3 | 2026-04-13 (PR #523) | RULE CANDIDATE — every client-side terminal path for a session with a DB row MUST call a Server Action to end/delete it. Zero-answer exam timeout was the 3rd instance |
| Event-handler refetch bypasses useEffect guard with stale empty-array topic IDs — handler calling `fc.refetch` directly with render-time `allTopicIds` sends `[]` (= match nothing in RPC) when topics haven't loaded yet; `useEffect` would have guarded via `allTopicIds.length === 0` return. Pattern: dual-path design (handler + effect both trigger refetch) when only effect carries the guard. Secondary consequence: when the handler carries the guard AND calls refetch, the effect ALSO fires after the next render (same dep change), causing two network calls per user action. Not data-corrupting (filterGeneration counter handles race) but wasteful. | 2026-06-11 (feat/837 #837) | 2 | 2026-06-11 | WATCHING (count 2 — flagged twice across commits in #837 cycle) — when a handler bypasses the useEffect path to call refetch directly, verify it carries the same guard conditions the effect uses. Empty non-null arrays to RPCs that use `= ANY(p_arr)` match nothing and produce zero-pool results. Also watch for double-refetch: handler fires + effect fires on same state change. Fix: handler sets state only; effect is the sole fetch trigger. |
| Stale test comment describes a `?? []`/guard/code path the refactor removed — passes for the wrong reason, invites wrong re-addition | 2026-04 (PR #523) | 7 | 2026-06-11 (ca8d8a18 #834) | WATCHING — when removing a fallback/guard, grep co-located `.test.ts` comments for it. 4th instance: `reconstructCookieValue([], BASE_KEY)` test title says "found auth cookie name(s) but no value" but passes an empty array — the throw fires via chunks.length===0, not the described guard. 5th instance: rpc-issue-internal-exam-code.spec.ts L249 title "mirror of BI" — BI was the old vector ID, now DM. 6th instance: session-table.test.tsx L77 title "displays duration in minutes" — fixed in fix/834 branch: renamed to "displays duration in minutes when under one hour". 7th instance: session-card.test.tsx "displays em dash when scorePercentage is null" — inaccurate narrative comment "The subject-name em dash and the score em dash — at least one must be present" described neither the fixture (subjectName='Navigation', non-null) nor the intent (score em dash specifically); dropped in ca8d8a18. POSITIVE SIGNAL: pattern caught and fixed on same commit. |
| Test mock string drift: renaming a Server Action's user-visible error string; test mocks the action (not the RPC) and echoes its own stale mock value → passes silently | 2026-04-26 (use-exam-start) | 2 | 2026-04 | WATCHING — on string rename, grep `.test.ts` for old string, update mock values + assertions |
| Prop added to a type/Props but never forwarded to the consuming component (drives icon/aria-label/conditional render) | 2026 (answeredIds) | 3 | 2026 (learningObjective) | WATCHING — when a new prop drives rendering, verify ≥1 call site passes real data |
| Server Action called AFTER `router.push` — App Router revalidation triggered by the SA response cancels the pending soft navigation, stranding the user (#568). Fix: SA must always fire BEFORE push; push must be the last statement in the success branch | 2026-06-01 (#568) | 1 | 2026-06-01 | WATCHING — audit every `router.push`/`router.replace` call site: confirm no Server Action call appears after it in the same synchronous execution path |
| Unstable function reference (plain fn, not `useCallback`/ref) in a `useEffect`/`useMemo` dep array re-runs effect unexpectedly | 2026 (PR #514) | 2 | 2026-04-13 (ea3d8d7) | WATCHING — wrap in `useCallback` or capture in a ref (ref-capture is the standard for callback props in interval/timeout hooks) |
| Client timer initialized at mount shows more time than server has (mount latency: nav+fetch+render) | 2026 | 2 | 2026-04 | WATCHING — fix = return `started_at` from start RPC, thread through SessionData. Escalate to ISSUE on next occurrence |
| Storage converter (`toSessionData`) drops new fields added to the target type but not the source type | 2026 | 2 | 2026-04 (PR #523) | WATCHING — when adding a field to SessionData, audit ALL its producers (`readSessionHandoff`, `toSessionData`) |
| Idempotency test seeded with a value that coincides with the post-regression hardcoded fallback — passes silently on regression to any *other* hardcoded value. Fix: seed TWO distinct scores (one above pass_mark, one below); any single hardcoded value then fails at least one assertion. | 2026-06-04 (921d0c0c AQ) | 1 | 2026-06-04 | WATCHING — when writing an idempotent-path test that seeds a specific stored value, use at least two seed variants (e.g., 75/true + 50/false); catching a regression to a different hardcoded literal requires the test to distinguish re-read from any constant |
| Cross-user/org isolation test: `data.length === 0` / `not.toContain(id)` is vacuous when the attacker has zero rows of their own — empty array satisfies the assertion trivially; need the attacker to have at least one own row so that "0 victim rows" proves RLS rejection, not a silent empty table. Applies to both cross-org (BZ1) and cross-student (flag-idor.spec.ts) isolation specs. | 2026-06-04 (ad19626c BZ1) | 2 | 2026-06-11 (feat/836 flag-idor.spec.ts) | WATCHING — before asserting `data.length === 0` or `not.toContain(victimId)` in a cross-user isolation test, seed ≥1 row owned by the attacker so the zero-result proves RLS rejection, not absence of data. flag-idor.spec.ts L65/74 assert attacker sees 0 victim flags without first asserting the attacker has their own visible flags. |
| `.single()` used in E2E audit row metadata read — throws PGRST116 on 0-row or HTTP 406 on 2+ rows; the helper `expectAuditRow`/`expectCompletionMetadata` in the same file already use `data?.[0]` or length-check. `.single()` in a manual audit fetch is inconsistent and flaky under sub-ms clock skew (testStart set after DB write). | 2026-06-04 (14de1723 #728) | 1 | 2026-06-04 | WATCHING — use `.maybeSingle()` or `data?.[0]` in E2E audit row reads; reserve `.single()` only when the test logic requires exactly-one semantics and that can be proven. |
| Cross-org isolation test: seed query filters by subject_id but test call passes p_topic_id; RPC's topic_id JOIN guard fires first — test enters error branch for wrong reason (topic mismatch, not org isolation). Isolation proof requires seed query and RPC call to use matching filters. | 2026-06-04 (7b1cf5b3) | 1 | 2026-06-04 | WATCHING — when a cross-org RPC probe passes both p_subject_id and p_topic_id, the question seed must also filter by topic_id to ensure the error is org-scoped and not an unrelated validation rejection |
| NULL-propagation through normalize_answer(NULL) in SECURITY DEFINER grader: canonical_answer IS NULL (schema-allowed, nullable TEXT column) → normalize_answer(NULL) = NULL → NULL comparison with student norm → boolean expression evaluates to NULL → 23502 NULL constraint violation on is_correct NOT NULL column. Same path for dialog_fill blanks_config entry with missing 'canonical' key. Fix: COALESCE(normalize_answer(v_canonical), '') in the grader OR add a NOT NULL DB constraint on canonical_answer for short_answer questions. | 2026-06-10 (76a9b03e VFR RT) | 1 | 2026-06-10 | WATCHING — when a SECURITY DEFINER grader reads a nullable column and passes it to an IMMUTABLE normalizer, the NOT NULL check on the is_correct column is a runtime trap, not caught at test time (integration tests always seed non-NULL canonicals) |
| Multi-guard RPC test proves only the guard whose fixture satisfies all pre-conditions — a guard that requires a threshold (e.g., overdue timer) is not proven when the fixture is below the threshold; the test's "no auto-complete" assertion passes vacuously for the wrong reason. | 2026-06-06 (#572 BE test) | 1 | 2026-06-06 | WATCHING — when a test exercises two guards (e.g. stale-session overdue lookup + duplicate-active EXISTS), verify the fixture independently satisfies each guard's pre-conditions. A non-overdue fixture session cannot prove the overdue-session org filter regardless of the assertion passing. |
| Two sequential "observation" tests share a dedup window for the same user: test 1 opens the window (writes row), test 2 fires within 60s → all calls rate-limited → delta=0, not 1. `expect(delta).toBe(1)` fails reliably when the tests run back-to-back in CI. Root: test 2's `delta===1` assumption requires the window to be expired, but test ordering guarantees it is NOT expired. | 2026-06-06 (rate-limiting.spec.ts #298) | 1 | 2026-06-06 | WATCHING — when a test exercises a time-window dedup guard, a sibling test that shares the same user+event_type+window MUST either (a) wait for the window to expire, (b) use a dedicated user for each test, or (c) assert delta>=0 and explain why |
| `cleanupTestData` omits `exam_configs` — tests that INSERT exam_configs in `beforeAll` then rely on `cleanupTestData` to clean up: `exam_configs` has `organization_id FK NO ACTION`, so the org delete silently fails (swallowed by `deleteOrLog`), orphaning the row. Tests pass because each run uses a fresh org + suffix — but rows accumulate across runs. | 2026-06-11 (ae27a7bc #833/#840) | 1 | 2026-06-11 | RESOLVED — fixed in 1309878c: `exam_configs` delete inserted before `organizations` in `cleanupTestData`; 144/144 integration tests green. |
| E2E try seed outside `try` block: org-transfer + fixture INSERT outside `try` → if transfer fails, `finally` does not run → fixture row left undeleted + user org stays changed, poisoning downstream specs. Seen in rpc-cross-tenant.spec.ts BE test (PR-7). afterAll multi-section cleanup (CL3 + prior vectors) also lacks a try/finally wrapper — first cleanup throw skips subsequent deletes. | 2026-06-06 (rpc-cross-tenant BE, PR-7) | 2 | 2026-06-09 (b95360f0 #796) | WATCHING — move any state-mutating setup (admin INSERT + UPDATE) that must be compensated into the `try` block so `finally` always runs after both the mutation and any assertion failure. In `afterAll` with multiple independent cleanup blocks, each block must be wrapped in its own try/catch (or accumulate errors like the void spec's afterEach), so first cleanup failure doesn't skip subsequent blocks. CONFIRMED GOOD: b95360f0 re-verified this is fixed in the new rpc-cross-tenant-reports.spec.ts BE test (inline try/finally preserved correctly). |
| Trigger body inheriting SECURITY DEFINER context omits `AND deleted_at IS NULL` on `UPDATE users` — inconsistent with prior stamp site (mig 20260406000004 line 77) and with docs/security.md §9 (every write in a SECURITY DEFINER context must explicitly filter soft-deleted rows on soft-deletable tables; RLS is not applied) | 2026-06-06 (mig 092, #532) | 1 | 2026-06-06 | WATCHING — when a trigger function's UPDATE targets a soft-deletable table (users, quiz_sessions) and executes as postgres via a SECURITY DEFINER RPC, add `AND deleted_at IS NULL` to the WHERE clause. The omission is benign today (the enclosing RPC's active-user gate rejects soft-deleted callers before the trigger fires), but the explicit filter is required for consistency with the pattern and future-proofing. |
| `ON CONFLICT (cols) WHERE predicate DO NOTHING` on a non-UNIQUE index — PostgreSQL requires a unique index or constraint matching the column list + predicate exactly; a plain `CREATE INDEX` cannot serve as an ON CONFLICT target and the statement fails at runtime with "there is no unique or exclusion constraint matching the ON CONFLICT specification". Caught on mig 085 (`idx_user_consents_lookup` is `CREATE INDEX` not `CREATE UNIQUE INDEX`). Fix: change the index creation to `CREATE UNIQUE INDEX`. | 2026-06-06 (mig 085 #386, a94850bb) | 1 | 2026-06-06 | WATCHING — when wiring ON CONFLICT to a partial index, always verify the index is UNIQUE before the migration applies. Grep for `CREATE INDEX` (not `CREATE UNIQUE INDEX`) on the target table. |
| Attack-surface.md trigger error message drift — trigger RAISE EXCEPTION text in migration body diverges from the string documented in `attack-surface.md`. Mig 089 AJ: trigger raises `'exam_config reactivation must go through upsert_exam_config'` but attack-surface.md row says `'exam_config_reactivation_not_allowed'`. Also: AJ row says `(no spec)` but a spec was added in same commit. | 2026-06-06 (mig 089 #755, a94850bb) | 1 | 2026-06-06 | WATCHING — after adding a trigger RAISE EXCEPTION, grep attack-surface.md + docs/security.md for the error string and update. |
| .coderabbit.yaml rule text omits multi-step requirement from docs/security.md — when the canonical rule has 3 numbered sub-requirements (a/b/c), the CR rule text collapses them, dropping the cross-reference requirement (c). Seen: §15 carve-out rule omits "must cross-reference docs/database.md §3" from the CR YAML. | 2026-06-06 (#552, 9a728afd) | 1 | 2026-06-11 (09372624) | RESOLVED — chore commit 09372624 restated the §15 CR rule with all three sub-requirements (a)(b)(c) including the docs/database.md §3 cross-reference. Verified in review of fix/833-840 branch. |
| Skill-doc bucket list for status tokens omits real tokens present in the source file — skill lists named posture buckets but misses tokens actually used in attack-surface.md (e.g. `ASSESSED LOW`, `ASSESSED NON-ISSUE`, `FULLY COVERED`, `DB-CAPPED`, `INTENTIONAL`, `COVERED AT INTEGRATION LAYER`). A future /redteam run buckets these as `unknown` or silently skips them. | 2026-06-08 (#114, 8cdc114a) | 1 | 2026-06-08 | WATCHING — when a skill lists a closed token-list for a data-driven field, verify every distinct token in the source file is covered by at least one bucket label. Omitted tokens lead to silent mis-bucketing. |
| attack-surface.md durable-lesson gap note not updated when the fix lands — a "flag for Phase E assessment" note in the durable lessons section persists after the gap was fixed in a subsequent migration; the DO row in the matrix is updated but the lesson note still describes the pre-fix state, creating contradictory coverage claims in the same file. Seen: line 174 CY3 note for the org-filter gap, fixed by mig 099b but lesson note not purged. | 2026-06-10 (aed96dcb PR #831) | 1 | 2026-06-10 | WATCHING — when a fix commit addresses a gap flagged in attack-surface.md durable lessons, grep the lessons section for the vector name or gap description and remove/update the note alongside the matrix row update. |

## Durable knowledge

### Column-relocation / answer-key migration patterns (updated 2026-06-13)
- **App-layer write paths must be updated atomically with DB migrations that add a non-nullable key column.** When a DB migration (a) strips an answer key from a JSONB column, (b) moves it to a new column, and (c) triggers the strip on all future writes — the app-layer Server Actions, seed scripts, integration tests, and E2E test fixtures that read `options[].correct` to derive the answer key MUST be updated in the same deploy. Shipping only the DB migration half breaks: integration test seeding (`correct_option_id = NULL` → RPC raises), E2E specs that find the correct option via `options.find(o => o.correct)`, and every new insert/update from the admin editor until task #3 lands.
- **Sibling RPCs that read the same field all need updating — count them exhaustively.** PR #823 updated 6 RPCs but missed `check_quiz_answer` (7th). The missed RPC reads `(opt->>'correct')::boolean` from options JSONB; after the sanitize trigger strips `correct`, this always returns NULL → raises on every answer. The unit test mocks the RPC and never exercises the SQL body, so the test suite passes with the breakage present. **Checklist pattern:** when relocating a field used by multiple SECURITY DEFINER RPCs, grep ALL migrations for `->>'correct'` (or the field name), trace each to its latest definition, and list them explicitly in the plan before marking the migration task complete.
- **E2E / integration tests that derive expected values from `options[].correct` are coupling tests to the old storage format.** After stripping, the source of truth is `correct_option_id` (the column). Tests must read the column (service-role, via `get_question_authoring_fields`, or from the RPC response) — not derive it from options JSONB.
- **Seed scripts reading `o.correct` from their own static fixture array (not the DB) to derive `correct_option_id` on INSERT are correct**: they derive the value before writing, pass it in the INSERT payload, and the sanitize trigger strips `correct` from the stored JSONB. This is the intended split: static fixture data carries `correct` as the authoring source; the DB column is the runtime source. Confirmed in seed-eval.ts and seed-e2e.ts (#823 commit 1b5e6679).
- **open-then-fetch sequencing on REVOKE-gated column in admin edit dialog**: when an admin opens the edit dialog, the dialog opens immediately (`setOpen(true)`) and simultaneously fires `getCorrectOption` via `startTransition`. During the fetch, `isPending=true` disables ALL form inputs including the radio (via `disabled={isPending}` propagated to `OptionEditor`). This means there is no window where the admin can submit with `correctOptionId=''` because the submit button is also `disabled={isPending}`. Zod on the server also catches empty-string (not in enum). Pattern works correctly.

### Security / RLS / SECURITY DEFINER (highest-value catches)
- **Column-level GRANT pattern for privilege reduction:** when a table's RLS UPDATE policy scopes rows but not columns, the fix is REVOKE the table-level UPDATE then GRANT UPDATE(<safe-col-list>) excluding sensitive columns. Postgres's `SECURITY DEFINER` RPCs run as `postgres` owner and are not subject to the `authenticated` column grant — so scoring/completion RPCs are unaffected. `anon` role gets no explicit table UPDATE grant in this schema, so no `anon` side-channel risk. Verify the column list against all ADD COLUMN migrations when reviewing such patches. Precedent: mig 20260521000004 (internal_exam_codes) + mig 20260605000001 (quiz_sessions).
- **SECURITY DEFINER bypasses RLS** — every SELECT in the body needs manual `AND deleted_at IS NULL` on soft-deletable tables; audit-event INSERT subqueries (actor/session/question FK lookups) need the filter independently of outer guards (security.md §9, §10). Narrow exception: reads by IDs from an immutable write-once column (`quiz_sessions.config.question_ids`). **Cached-role pattern (mig 078, mig 084, 093):** capturing `organization_id + role` in a single deleted_at-filtered read at auth time and reusing the cached variable in later audit INSERTs is §10-compliant — the filter is present at the capture site; no re-query needed. Verify the capture SELECT still includes `AND deleted_at IS NULL`. **Resource-lookup removal exception (mig 093, e3732955):** when a resource existence/org-scope SELECT cannot carry `AND deleted_at IS NULL` because the resource is the just-soft-deleted subject (user.deactivated audit fires after the deactivation), the correct fix is to REMOVE the resource SELECT entirely rather than add a §9-non-compliant filter. The audit row's `organization_id` is always the actor's own org (from the §9-compliant actor lookup), so a bogus/cross-org resource_id is only self-referential log noise — no cross-org read or write. Document the reasoning in a SQL comment. Confirmed: p_resource_id is `UUID NULL, no FK` in audit_events — soft-deleted or cross-org UUIDs are valid label values. Integration test must assert `organization_id === actor's org` to pin the invariant.
- **Multi-permissive SELECT trap:** Postgres ORs permissive policies; a per-caller RPC must self-scope `WHERE owner = auth.uid()` (see tracker). Admin RPCs behind `is_admin()` are exempt.
- **RPCs must self-defend** — Zod at the Server Action boundary is not enough; the RPC is callable directly by any authenticated user. Validate JSONB array length, sum invariants, and UUID element validity inside the function (raw element produces internal `invalid_text_representation`, not a structured `RAISE`).
- **Correct-answer exposure:** student-facing reads must go through `get_quiz_questions()`/report builders that strip `correct` from options. Watch new Server Actions querying `questions.options` directly.
- **Auth-then-validate order:** parse Zod input is fine to throw, but auth check (`auth.uid()`) must gate before any DB work; flag `Schema.parse(input)` placed before the auth check.
- **`.single()` → `.maybeSingle()`** for user/profile lookups where soft-delete yields 0 rows (`.single()` throws PGRST116 as a service error).
- **Trace `CREATE OR REPLACE FUNCTION` to the latest definition** before flagging a missing-pattern finding on a Postgres function (per agent-critic.md Pre-Flag rule).

### Query / data correctness
- **Window-function totals:** `count(*) OVER()` runs before LIMIT → correct full-set total for single-query pagination; but 0-row (out-of-range) page yields no total — caller must treat `totalCount:0` as "maybe out of range".
- **Denominator/numerator mismatch:** widening a read (active-only → non-deleted) for retention UX can push a ratio >100% if the denominator stays narrow — cap with `Math.min(pct,100)` or align (first seen #540).
- **Filter consistency:** count query and page query must carry identical filters + deterministic ordering with a unique tiebreaker (`.order(natural).order('id')`). A single `fetchAllRows` abstraction makes this structural.
- **Zero-row no-op:** DELETE/UPDATE via RLS returns 200 + 0 rows when RLS blocks or filter misses — chain `.select('id')` and check length (code-style.md §5).
- **`as unknown as T` needs a runtime guard** (`Array.isArray`/`typeof`) before use, e.g. `config.question_ids.includes()` throws on a non-array (code-style.md §5).
- **PostgREST embed:** use `!fk_column` (errors loudly) not `:` alias (returns null on FK-resolution failure).
- **camelCase/snake_case JSONB key drift** between TS `.rpc()` payload and SQL `->>'key'` reads is invisible to mocked-client unit tests — needs integration/SQL-parse coverage; prefer snake_case both sides.
- **TOCTOU on COUNT-then-INSERT** (no serializable isolation) is an accepted repo-wide trade-off — document, don't flag as a bug.
- **types.ts nullability:** manually authored RPC `Returns` entries must mirror Postgres nullability — `NUMERIC/TEXT/TIMESTAMPTZ` without NOT NULL → `T | null`.

### React / Next.js correctness
- **Re-throw redirect errors:** any catch wrapping `redirect()`/`notFound()` must `isRedirectError(error)` re-throw (Server Components AND client-component Server Action callers — project has `rethrow-redirect.ts`). Bare `catch{}` turns a redirect into a 500/stale render.
- **Stale-closure on locks/Maps:** deleting from a `lockedRef` in a catch before `setAnswers` propagates opens a double-submit window — release via `useEffect` keyed on the `answers` prop, not synchronously.
- **`new Map()` / `new Set()` in a render return** = new ref each render → downstream dep-array thrash; use a stable `useRef`.
- **Two-layer guard:** HTML `disabled` is not a behavioral contract — `onClick` handlers must also `if (!disabled)` when they manage local state and can be invoked programmatically.
- **`'server-only'` — DO NOT flag its absence (FALSE POSITIVE, #789).** `server-only` is used 0× in this repo; `*/queries.ts` + sibling modules import `adminClient` with no such guard. The earlier "required" claim was wrong — recommending it contradicts the codebase + adds an unused dep.
- **Server Component query helpers** (called via Suspense, not `'use server'`) throwing `error.message` is NOT a client-exposure gap (propagates to error.tsx→Sentry, never reaches DOM) — but a `console.error` before the throw is still wanted for log correlation. Confirmed intentional at count=2; do NOT flag as ISSUE.

### Test-quality catches (semantic, not style)
- **PostgREST NUMERIC(5,2) integer-value serialization:** for integer-valued NUMERIC cells (e.g. `score_percentage = 0` after UPDATE ... SET score_percentage = 0), PostgREST serializes as JSON number `0`, not string `"0.00"`. Playwright `toEqual(0)` passes without `Number()`. Non-integer values (e.g. `66.67`) serialize as strings and need `Number()`. Rule: if the test's DB value is provably always integer (e.g. the expiry branch hardcodes `0`), `toEqual(0)` is safe; if non-integer values are possible in the same column, use `Number()`. Confirmed by rpc-complete-overdue-exam.spec.ts CI passing with `toEqual(0)` direct PostgREST read.
- **Vacuous re-run test:** to verify an effect guard, the dep value passed must actually differ from the initial — same value silently skips the effect, making the assertion vacuous.
- **jsdom flushes `useEffect` in `act()`** — any property depending on an effect appears correct even if the effect fires too late for a real browser; lock-release-after-failure tests pass in jsdom regardless.
- **Mocked-client unit tests can't catch JSONB-key drift or PostgREST string-serialization** — call out the integration-test gap.
- **`vi.importActual` partial mock** (spread real module, override only the fn) keeps exported constants (e.g. `COMMENT_SELECT`) live from source — prefer over full mock with a hardcoded constant string (drift vector).
- **Proxy-based `buildChain` absorbs `.limit()` silently** — `get-active-exam-session.test.ts` + `get-active-internal-exam-session.test.ts` use a `Proxy` that returns itself on any method call. Adding `.limit()` to production code never breaks these tests, but also means no assertion is possible that the cap value is applied. If adding a call-chain assertion to these files, first replace `buildChain` with a per-method `vi.fn()` chain (the `load-draft.test.ts` pattern). Flagged as SUGGESTION on PR #701 (2026-05-30).
- **GoTrue `ban_duration: 'none'` omits `banned_until` from response (undefined, not null):** The SDK `User.banned_until` is typed `string | undefined` (optional field). After an unban, GoTrue omits the field entirely; the JS SDK surfaces `undefined`. The `?? null` coercion maps this to `null` for `.toBeNull()` assertions. A past-date leftover string would also fail the assertion — that failure is correct regression detection behavior. Confirmed PR #372 (ccc1cdf5).
- **Phase-2 skip guard on library contract:** When a phase-2 step is skipped via `if (phase1Result.data.length > 0)`, ensure the skip also covers phase-1 error (not just empty). For `fetchAllRows` callers the two coincide (error → data=[]), but the intent must be explicit with `if (ids.length > 0 && !phase1Result.error)` so a future non-`fetchAllRows` caller doesn't silently send phase-2 with stale/null data. First seen: #684 `collectUserData` session IDs → answers phase-2.
- **`data ?? null` assertion idiom on `RETURNS TABLE` RPCs:** For RPC error paths, `data` is always `null` from PostgREST — `data ?? null` equals `null` and `toBeNull()` passes correctly. But `?? null` is borrowed from scalar-return RPCs where `data: null` on both success and error. On a TABLE-returning RPC's success path `data` would be an array, not null — `?? null` would mask that regression silently. Prefer bare `expect(data).toBeNull()` on error-path assertions for TABLE-returning RPCs. First flagged PR f02031fa #384 (check_consent_status).
- **`expect(error).toBeNull()` on RLS-isolated reads is correct:** PostgREST returns `{ data: [], error: null }` when RLS filters all rows — not an error. Adding `expect(error).toBeNull()` to cross-tenant reads (cross-org, cross-student) distinguishes "RLS blocked → empty array, no error" from "transport/schema failure → error non-null". This is the right assertion pattern on isolation proofs.
- **Empty-array `.in('col', [])` in cleanup helpers:** PostgREST short-circuits `IN ()` to 0 rows without error (Supabase-specific behavior), so it is harmless, but a guard `if (!ids?.length) skip` is more explicit and avoids the implicit behavior. Flag as SUGGESTION when seen in cleanup/setup helpers.

### Security-gate (bash hook) patterns
- **Agent prompt instruction vs parser contract drift:** when the verdict parser is made more permissive than the agent's in-prompt instruction (e.g. parser now accepts BLOCKED at any line start, but prompt still says "last line MUST be exactly: BLOCKED"), the prompt drifts stale. Not a security bug (parser is the real gate; prompt is guidance), but flag as SUGGESTION so the agent's guidance matches reality. First seen: fix/832 (f7ec4a11).
- **`^[[:space:]]*BLOCKED` over-matches prefixes (BLOCKEDxyz) — accepted:** the grep pattern does not enforce a word boundary after BLOCKED. For a security auditor agent whose output is structured and domain-constrained, "BLOCKEDxyz at line start" is not a realistic agent output. Over-blocking (false positive) is the safe failure direction for a push gate. Do not flag this as ISSUE; SUGGESTION only.
- **Critical ordering in verdict parsers:** the BLOCKED check must execute BEFORE the APPROVED check in any multi-token verdict parser. If APPROVED comes first, an adversarial transcript with APPROVED-then-BLOCKED would approve. Flag as ISSUE if the check order is inverted. First verified correct: fix/832.
- **Committed test suite (7 cases) missing APPROVED-before-BLOCKED ordering test:** the ordering guarantee is structural (BLOCKED runs first in parse_verdict), but a test explicitly proving BLOCKED wins over a prior APPROVED token is the highest-value missing case. Working-tree additions (cases 8-12) added it post-commit — RESOLVED in 7e803759 (case 9 now pins this). Flag as SUGGESTION when a security-gate test suite tests the individual tokens but not their ordering precedence.

### Process
- **PR-level sweep** (`git diff master...HEAD`) adds value on multi-commit extraction PRs (confirms earlier SUGGESTIONs landed, filter consistency holds cross-commit) but finds nothing extra on single-module refactors.
- **Attack-surface.md commit-ordering stale-state:** when a PR has 2 commits — (1) feature + attack-surface doc written at commit 1, (2) integration test added at commit 2 — the attack-surface rows written at commit 1 may say "GAP" for vectors the commit-2 integration test already covers. The PR-level sweep catches this: the integration test covers the guards, but the doc rows never transitioned to "COVERED AT INTEGRATION LAYER". Fix by updating the doc rows in the same commit that adds the tests. Watch for recurrence (#379, CK/CL/CM/CO).
- **Treat ISSUE as a current gap** regardless of current callers — no "safe today"/"latent" deferral (agent-semantic-reviewer.md).
- **Builder-function dead param:** when extracting inline handlers to `build*` functions that pass all current state as params, a param that was never used in the original (e.g. `setSession` in `buildResumeHandler` — original didn't call it either) should be removed rather than forward-carried. It's noise, and callers passing it incorrectly won't be caught by the type system since `SetState<T>` is structurally identical across all setters. Flag as SUGGESTION on first occurrence; ISSUE if it could mask a missing call.
- **Security doc partial-unique-index threat description:** when documenting an AJ-style "admin bypasses RPC" threat involving a partial unique index, verify whether the index itself closes the threat. If the index covers `WHERE deleted_at IS NULL`, an UPDATE that clears `deleted_at` on a row while a conflicting active row already exists is rejected by the index — the threat vector is narrower than "allows two active rows". Distinguish: (a) index prevents the data-integrity violation entirely vs. (b) only the UX/audit path is bypassed. Mischaracterizing (a) as if the index doesn't help overstates severity.
- **`ensure*` E2E helper unconditional password reset (#593): WON'T-FIX** — GoTrue exposes no hash to compare; a conditional sign-in probe costs ~the same as the idempotent write it would replace, while adding a branch to 4 helpers. The reset is idempotent, does not emit `record_login`/auth events, and Playwright setup runs workers=1 (no lock contention). Do not re-flag.
- **`let refs` uninitialized-ref in afterAll cleanup (#775 pattern):** when `refs` is a describe-scoped `let` assigned in `beforeAll`, a beforeAll throw leaves it `undefined`. afterAll still runs (vitest semantics) and `cleanupReferenceData({ refs: [refs] })` → `.map(r => r.subtopicId)` throws TypeError on `undefined`. This matches the identical gap in `cleanupTestData` (orgId is also an unguarded `let`). Classify SUGGESTION on first occurrence; watch for pattern recurrence. The fix is a null guard: `refs: [refs].filter(Boolean)` or check `if (refs)` before calling. Do not raise to ISSUE: (a) it is only exposed during beforeAll crashes (not normal runs), (b) the suite already fails loudly in that scenario, and (c) the pattern is pre-existing across all 7 suites in the same file.
- **EXCEPTION WHEN unique_violation re-read visibility (READ COMMITTED, PG sub-block):** After the loser's INSERT raises unique_violation inside a plpgsql sub-block, the sub-block savepoint is rolled back. The re-read SELECT then executes AFTER the winner has committed (the unique-index wait ends at winner commit). In READ COMMITTED, each statement takes a new snapshot — so the post-savepoint-rollback SELECT sees the winner's committed row. Visibility holds. Cross-index masking: the EXCEPTION handler is global to ALL unique_violations, but the INSERT hardcodes `mode = 'vfr_rt_exam'`; only `uq_vfr_rt_exam_session_active` (same mode predicate) can fire. The two sibling indexes (`uq_active_exam_session` WHERE mock_exam, `uq_internal_exam_session_active` WHERE internal_exam) are mode-disjoint and cannot be reached by this INSERT. Handler masking is structurally impossible. Validated correct at b13d2c3a (PR #830).
- **Soft-deleted-caller fail-closed test pattern (50c81b94, mig 104):** When a SECURITY DEFINER RPC has no explicit active-user pre-check and relies on `actor_role NOT NULL` + a `deleted_at IS NULL` subquery for fail-closed behavior, the integration test must: (1) start the session BEFORE soft-deleting the caller (JWT stays live), (2) assert `error` is non-null after the RPC call, (3) assert the session `ended_at` IS NULL to prove the transaction rolled back (not just that an error was returned), and (4) restore `deleted_at = null` AND force-end the session in a `finally` block so `afterAll` cleanup runs cleanly. The session lookup filters on `qs.deleted_at`, not the caller's `users.deleted_at` — the session is found, the audit INSERT fails, and the whole transaction rolls back. This is distinct from the "session not found" path.
- **Mechanism pin via `error?.message.toContain(column)` vs `error?.code` (cd927b08, round-3):** For a NOT NULL violation inside a SECURITY DEFINER plpgsql body, `expect(error?.message).toContain('actor_role')` pins BOTH the SQLSTATE (23502) AND the specific column — it is MORE discriminating than `error?.code.toBe('23502')` alone, because a regression where a DIFFERENT column becomes null would still fail the code assertion but for the wrong reason. `error.message` contains the column name verbatim in the Postgres 23502 format (`null value in column "actor_role" ...`). This is a valid intentional pattern for mechanism-pinning tests; do not flag it as less precise than code assertion. `error.code` is preferred for PostgREST-level constraint violations (direct table inserts, e.g. 23514 in constraint-regression tests) where no column specificity is needed.
