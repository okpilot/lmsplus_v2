# Red Team — Attack Surface Map

> Last updated: 2026-06-06 (commit 8d0a758d — record_auth_event RPC + 4 auth Server Actions; new vectors CK–CQ: A1–A5 sub-vectors + actor_id-derivation invariant + audit-completeness gap). Batch A DB-hardening (migs 085–090, PRs #386/#275/#734/#754/#755/#773): record_consent EXISTS-guard idempotency (accepted=true no-op; ON CONFLICT not used — idx_user_consents_lookup is non-unique); start_quiz_session 500-array cap + NULL mode guard; internal-exam RPCs actor_role cache (mig 087); start_exam_session unique_violation mapping + org_id index align + role cache (mig 088); exam_configs reactivation-block trigger (mig 089); users column UPDATE GRANT revoke/full_name-only (mig 090). Vectors updated: AJ→ENFORCED, AL→ENFORCED, CG→HARDENED, CF confirmed class-closed. New vector CI (array-length cap). Previous: branch fix/redteam-specs-green-and-required (HEAD): consent-gate.spec.ts Vector V/Z cookie-assert fix (decodeURIComponent) — attack unchanged, COVERED. quiz-draft-injection.spec.ts Vector BI TOCTOU assertion unchanged — afterEach/pre-seed/count queries converted from soft-delete to hard-delete (quiz_drafts has no deleted_at column; prior soft-delete was always returning a DB error that cleanup code swallowed) — COVERED. redteam.yml path filter removed so "Red Team Specs" can be a required status check on all PRs (coverage semantics unchanged). All three changes are spec infrastructure fixes with zero attack-surface change. 243 passed, 1 skipped, 0 failed locally. PR #741 (01a73368): fix(security): close quiz_sessions score-forgery via column GRANT (#611) — mig 20260605000001 REVOKEs blanket UPDATE on quiz_sessions and re-GRANTs 11 non-scoring columns; Vectors BL/BM/BN (score column forgery) and ended_at forge mapped to new quiz-session-score-forgery.spec.ts (PASSING, 6 tests). quiz-session-config-injection.spec.ts header updated to note scoring columns now 42501 (not trigger path). session-race-condition.spec.ts comment updated. Two new candidate gap vectors added: CG (users.role forge via trigger not column GRANT — trigger defence assessed sufficient) and CH (quiz_sessions score-forgery via SECURITY DEFINER completion RPCs vs. non-SECURITY-DEFINER path — assessed non-issue: completion RPCs are postgres-owned). PR #738 (cb861b4): rpc-comment-idor.spec.ts — Vector P GAP→PASSING; DELETE RLS USING semantics confirmed: cross-user DELETE is a 0-row no-op / 200 OK, NOT 42501 — 42501 is the INSERT/UPDATE WITH CHECK path only. PR #739 (ad19626): BY1/BZ1 GAP→COVERED (rpc-admin-dashboard-students.spec.ts); L/M/N GAP→COVERED (rpc-report.spec.ts); O GAP→COVERED/PASSING (report-question-builder.test.ts Vitest unit, not Playwright — correct home for app-layer answer-key-leak vectors); CE GAP→COVERED (exam-config-distributions-rls.spec.ts). PR #740 (14de172): audit-event-forgery.spec.ts — AA GAP→COVERED/PASSING (cross-user audit SELECT via actor_id probe, #398); audit-completeness.spec.ts — BT2 GAP→COVERED (#728, complete_empty exam.completed metadata locked); #297 (flagged_questions WITH CHECK deleted_at) closed obsolete (mig 050 intentionally removed the guard); #623 (cross-org draft injection) deferred — needs cross-org question seeding. PR #750 (#384/#517): user-consents-isolation.spec.ts — Vectors X/Y (user_consents RLS owner-scoping + direct-INSERT block) COVERED/PASSING; AJ/AL exam-mode residual vectors re-documented in docs/security.md §11 with corrected framing — AL race is constraint-enforced (uq_active_exam_session, mig 20260411000006; only error-mapping pending, #754), AJ uniqueness holds via uq_exam_configs_org_subject_active (bypass is controlled-flow only, trigger tracked #755). AJ row below refined GAP→LOW. Lessons in Durable Lessons.

## Vector-to-Spec Mapping

| ID | Vector | Priority | Spec File | Status | Notes |
|----|--------|----------|-----------|--------|-------|
| A | submit_quiz_answer accepts foreign questionId | HIGH | rpc-question-membership.spec.ts | FIXED (migration 033) | Added question membership check to RPC |
| B | fetch-stats without auth | MEDIUM | server-action-unauthenticated.spec.ts | PASSING | RPCs require auth.uid() |
| C | Draft with foreign question_ids | MEDIUM | quiz-draft-injection.spec.ts | TBD | RLS may not check question ownership in drafts |
| D | start_quiz_session with cross-org subjectId | MEDIUM | rpc-cross-tenant.spec.ts | PASSING | RLS filters cross-org data |
| E | Topic lookups without auth | MEDIUM | server-action-unauthenticated.spec.ts | PASSING | Covered by unauthenticated spec |
| F | Student INSERT into audit_events | MEDIUM | audit-event-forgery.spec.ts | FIXED (migration 034) | INSERT policy replaced with WITH CHECK (false) |
| G | Concurrent discard + complete | LOW | session-race-condition.spec.ts | PASSING | FOR UPDATE lock prevents race |
| H | PKCE code forwarding | LOW | pkce-state.spec.ts | PASSING | Supabase validates PKCE server-side |
| J | batch_submit on completed session | LOW | session-replay.spec.ts | PASSING | RPC checks session status. #257 pins the concurrent-no-double-insert invariant (two batch_submit via Promise.all → FOR UPDATE serialises one scorer + one idempotent caller, quiz_session_answers count stays 3). #256 pins a mid-session soft-deleted question still scored (§15 write-once config.question_ids carve-out, count===3). #551 adds SQL-layer coverage of the same carve-out via packages/db rpc-batch-submit-quiz.integration.test.ts (Vitest, runs under test:integration). |
| K | Rapid-fire Server Action calls | MEDIUM | rate-limiting.spec.ts | DOCUMENTED GAP | No rate limiting implemented |
| L | get_report_correct_options called without auth | HIGH | rpc-report.spec.ts | COVERED (commit ad19626) — "L: an unauthenticated caller is rejected": anon client calls RPC with a completed session_id; asserts error not null + message ~ /not authenticated/i | RPC auth.uid() guard pinned by rpc-report.spec.ts |
| M | get_report_correct_options called with foreign session_id (cross-tenant) | HIGH | rpc-report.spec.ts | COVERED (commit ad19626) — "M: the attacker cannot read the victim's completed session (IDOR)": attacker calls RPC with victim's completed session_id; asserts error not null + message ~ /session not found, not owned, or not completed/i | Session-ownership guard pinned |
| N | get_report_correct_options called on active (not completed) session | HIGH | rpc-report.spec.ts | COVERED (commit ad19626) — "N: the owner cannot read an active (not-yet-completed) session": victim calls RPC with their own active (ended_at IS NULL) session_id; asserts error not null + message ~ /session not found, not owned, or not completed/i | ended_at IS NOT NULL guard pinned |
| O | quiz-report questions SELECT leaks raw correct boolean server-side | CRITICAL | report-question-builder.test.ts (Vitest unit) | COVERED/PASSING (commit ad19626) — "never leaks a `correct` field on report options, even if the source carries one": passes a leaky option shape ({id,text,correct}), asserts the returned options contain only {id,text} keys. NOTE: this vector is permanently homed in the Vitest unit test — a PostgREST-level Playwright spec cannot reach the app-layer buildReportQuestions builder. The Vitest unit is the correct and permanent home for this class of app-layer answer-key-leak vector. | Defense is buildReportQuestions projection; no DB-layer gate. Vitest unit is the only spec layer that can reach this code path. |
| P | deleteComment called by non-owner student (IDOR via RLS) | HIGH | rpc-comment-idor.spec.ts | PASSING (commit cb861b4) — "Vector P: a different student cannot delete the victim's comment (IDOR)": attacker calls DELETE on victim's comment_id; asserts error===null AND deleted.length===0 (0-row no-op, 200 OK — NOT 42501); admin re-reads and confirms comment survives. Positive: owner can delete own comment (deleted.length===1). NOTE: 42501 is raised only by INSERT/UPDATE WITH CHECK violations — DELETE USING failures are silent no-ops per PostgREST (code-style.md §5). Issue #711's original "42501" acceptance was incorrect for the DELETE path. | RLS policy question_comments_delete_own uses USING(user_id = auth.uid()); question_comments is a hard-delete table (mig 049) — afterAll uses hard DELETE, not soft-delete |
| Q | question_comments table read/write without auth (unauthenticated) | HIGH | server-action-unauthenticated.spec.ts | PASSING (#314) — unauthenticated SELECT returns 0 rows + INSERT rejected by RLS WITH CHECK | server-action-unauthenticated.spec.ts now verifies this: an anon SELECT on question_comments returns 0 rows ("sees 0 rows from question_comments"), and an anon INSERT is rejected by RLS WITH CHECK (user_id = auth.uid()) because auth.uid() is NULL ("cannot insert into question_comments"). SELECT policy requires EXISTS(SELECT 1 FROM users WHERE id = auth.uid()); INSERT WITH CHECK enforces user_id = auth.uid(). |
| R | createComment/getComments called without auth via Server Action layer | HIGH | server-action-unauthenticated.spec.ts | COVERED (unit) — getComments silent-success pinned by comments.test.ts:83; Server Actions cannot be invoked from client-level red-team specs, so E2E table-layer is covered via Vector Q | getComments does NOT return an error for unauthenticated callers; it silently succeeds with empty array — a spec should confirm no data leaks and that the silent-success behaviour is intentional |
| S | toggleFlag called with a foreign student's questionId (IDOR) | MEDIUM | flag-idor.spec.ts | PASSING (#314) — table-layer RLS isolation verified: attacker reads 0 victim flags and cannot insert a flag as the victim | toggleFlag hard-codes user.id from auth token for student_id filter; RLS WITH CHECK also binds student_id = auth.uid(); low risk but no spec verifies attacker cannot read or corrupt another student's flag state. toggleFlag/getFlaggedIds bind student_id = auth.uid() (foreign student_id not expressible at the Server-Action layer); flag-idor.spec.ts exercises the only reachable layer (direct PostgREST table access under RLS). |
| T | getFlaggedIds called without auth | MEDIUM | server-action-unauthenticated.spec.ts | PASSING (#314) — flagged_questions unauthenticated SELECT returns 0 rows (server-action-unauthenticated.spec.ts); getFlaggedIds silent-success unit-covered by flag.test.ts:238/246 | getFlaggedIds returns success:true+[] when unauthed (line 87 flag.ts); silent success not tested |
| U | deleteComment hard-delete bypasses soft-delete policy for deleted_at column | MEDIUM | (no spec) | GAP — table has deleted_at column but DELETE policy does not require deleted_at IS NULL; admin could hard-delete a comment that was already soft-deleted, which is consistent, but no spec checks the interplay | Low risk; document gap for completeness |
| V | Consent gate bypass via forged/missing __consent cookie | HIGH | (no spec) | GAP — proxy.ts consent gate reads cookie value only; no spec verifies that a user with no DB consent record cannot bypass gate by manually crafting a valid-looking cookie value | New vector from migration 057 + proxy.ts change |
| W | Unauthenticated access to check_consent_status and record_consent RPCs | HIGH | server-action-unauthenticated.spec.ts | COVERED (commit f02031fa, #384) — "check_consent_status rejects unauthenticated callers" + "record_consent rejects unauthenticated callers": anon calls each RPC; both SECURITY DEFINER `_uid IS NULL → RAISE 'Not authenticated'` (capital N, matched /not authenticated/i); assert error not null + data null. | New RPCs from migration 057 |
| X | Cross-tenant consent record read (user_consents SELECT cross-uid) | HIGH | user-consents-isolation.spec.ts | COVERED (commit fba24e5a, #384/PR #750) — "Vector X": admin seeds a victim consent row (non-vacuity), attacker probes `.eq('user_id', victimUserId)` → error null + 0 rows (RLS user_consents_select_own USING (user_id = auth.uid())). Plus a positive control proving the policy SCOPES to the owner (attacker reads own SELF_VERSION row, never the victim's) rather than blocking all reads. | New table from migration 057 |
| Y | Student direct INSERT into user_consents bypassing record_consent RPC | HIGH | user-consents-isolation.spec.ts | COVERED (commit fba24e5a, #384/PR #750) — "Vector Y": attacker direct `.insert()` (own user_id) → error.code 42501 (RLS user_consents_no_direct_insert WITH CHECK (false)); admin confirms no forged row landed. 42501 is the INSERT/UPDATE WITH CHECK path (not DELETE USING, which is a 0-row no-op). | New table from migration 057 |
| Z | Consent cookie injected without completing actual consent flow (login-complete bypass) | HIGH | pkce-state.spec.ts | GAP — an authenticated user who navigates directly to /auth/login-complete (e.g., by replaying the URL) can trigger consent-cookie issuance without going through /consent page if they already have DB consent records; the inverse gap is also untested: a user with a crafted cookie but no DB record who reaches /app/* after the proxy allows them through because cookie matches expected format | New auth flow from login-complete/route.ts change |
| AA | Student reads another student's audit events via audit_read_own policy (cross-user SELECT) | HIGH | audit-event-forgery.spec.ts | COVERED/PASSING (commit 14de172, #398) — "AA: a student cannot read another student audit events via actor_id probe": seeds a victim-owned audit row (idempotent by marker; audit_events is append-only), attacker queries .select('id').eq('actor_id', victimUserId), asserts leaked.length===0. Non-vacuity confirmed: victimEventId is asserted truthy before the probe. | Migration 059; closed #398 |
| AB | Student probes audit_events for events where actor_id = their uid but metadata contains sensitive data about other users | MEDIUM | audit-event-forgery.spec.ts | ASSESSED LOW RISK — all event types written with student actor_id (quiz_session.started, quiz_session.completed, quiz_session.batch_submitted, student.login) contain only the student's own session/score data in metadata. No cross-user data in student-authored events confirmed by grep of all RPC inserts. No spec change required; document here for completeness. | Migration 059 impact analysis |
| AC | get_admin_dashboard_students called by an authenticated non-admin student or instructor (privilege escalation) | HIGH | server-action-unauthenticated.spec.ts | GAP — spec does not test this RPC at all; RPC has auth.uid() + is_admin() guard but no spec exercises the "authenticated but not admin" path; carries over from dropped get_admin_student_stats (mig 20260408000007); also covers the instructor role (role='instructor' triggers is_admin()=false → 'forbidden') | Migration 20260527000001 (replaces 20260408000007) |
| AD | get_admin_dashboard_students intentionally includes soft-deleted users — SECURITY DEFINER soft-delete rule exception | MEDIUM | server-action-unauthenticated.spec.ts | INTENTIONAL — migration 20260527000001 explicitly does NOT filter deleted_at IS NULL on the users roster; admin dashboard must show inactive students (p_status filter controls visibility; deleted_at in return set for UI badge). Admin caller's own profile requires deleted_at IS NULL (org resolution). questions/sessions sub-queries all retain deleted_at IS NULL. Only the user roster JOIN is intentionally unpinned. Exception documented in migration comment (§9 exception note). No spec change needed for the intentional omission but the exception persists from mig 20260408000007. | Migration 20260527000001 (replaces 20260408000007) |
| AE | Student who knows an admin's user-ID probes getStudentDetail (IDOR) using a non-student UUID | MEDIUM | (no spec) | GAP — getStudentDetail now uses .eq('role', 'student') instead of .is('deleted_at', null); this is an improvement because it gates on role type, but no spec verifies that querying with an admin UUID returns null rather than the admin's record. The adminClient (service role) is used so RLS does not apply — only the explicit .eq('organization_id') + .eq('role', 'student') guards prevent cross-role data access. | queries.ts change in apps/web/app/app/admin/dashboard/students/[id]/ |
| AF | requireAdmin redirect pattern — Server Action caller catches and re-raises NEXT_REDIRECT (bypass via error interception) | LOW | (no spec) | ASSESSED LOW RISK — Next.js redirect() throws a special error that is designed to propagate up the call stack and is caught by the framework, not by user code. Any caller that catches it must explicitly check isRedirectError() and re-throw. All current callers (queries.ts files) do not wrap requireAdmin in try/catch — the redirect propagates correctly. Low risk; no new spec required but pattern documented. | require-admin.ts change |
| AG | start_exam_session called by unauthenticated user or student from wrong org | HIGH | server-action-unauthenticated.spec.ts | COVERED (unauth, commit f02031fa, #545) — "start_exam_session rejects unauthenticated callers": anon calls RPC with a REAL knownSubjectId; SECURITY DEFINER `auth.uid() IS NULL → RAISE 'not authenticated'` fires before exam_config lookup; assert error not null + ~/not authenticated/i + data null. Cross-tenant p_subject_id probe is Vector AH (now COVERED in Hub B). | Migration 20260411000003/000005 |
| AH | start_exam_session called with cross-org p_subject_id (cross-tenant exam config probe) | HIGH | rpc-cross-tenant.spec.ts | COVERED (commit a17fdc0f, #545) — "cross-org student cannot start an exam for an egmont subject": crossOrgClient.rpc('start_exam_session', {p_subject_id: examSubjectId}) → error not null + ~/no exam configuration found/i + data null. Non-vacuous: ensureExamConfig seeds an enabled egmont config + distribution in beforeAll and examSubjectId has egmont questions → proves org-scoping (exam_configs lookup filtered to caller's v_org_id), not global absence. | Migration 20260411000003/000005 |
| AI | batch_submit_quiz submitted after time limit (time bypass) | MEDIUM | batch-submit-time-limit.spec.ts | COVERED (commit b9993191, #546, PR #746) — "AI: an overdue exam submission is force-expired and scored zero": admin direct-inserts an attacker-owned mock_exam session (quick_quiz has NULL time_limit_seconds → branch unreachable) with started_at backdated 120s past the 60s+30s grace; batch_submit_quiz returns expired:true, answered_count=0, correct_count=0, score_percentage=0, passed=false, results=[]; DB row shows ended_at set + score 0; quiz_session_answers count=0 (no scoring before the gate). Non-vacuous: an in-time control submission (started_at −5s) scores normally (answered_count=1, no expired key), proving expiry — not a universal rejection — zeroes the overdue case. `expired:true` is emitted ONLY by the past-grace branch (mig 20260601000001 L99-125), so the assertion uniquely pins that path. Hermetic afterEach soft-delete (try/finally + zero-row observability). NOT separately covered: the internal_exam mode exercises the same mode-agnostic branch — low-priority extension. | Migration 20260411000005 (enforcement); latest def mig 20260601000001 |
| AJ | Admin reactivates soft-deleted exam_config via direct UPDATE (bypasses upsert_exam_config controlled de-duplication path) | LOW | exam-config-reactivation-guard.spec.ts | ENFORCED (mig 089, #755) — trigger trg_block_exam_config_reactivation raises 'exam_config reactivation must go through upsert_exam_config' when OLD.deleted_at IS NOT NULL AND NEW.deleted_at IS NULL (unconditional — no role exemption). Direct reactivation attempts now fail at the DB layer before the uniqueness index is even tested. upsert_exam_config (SECURITY DEFINER, postgres owner) is the only sanctioned reactivation path; it never clears deleted_at (its UPDATE branch touches only non-soft-delete columns), so the trigger condition is never met on the sanctioned path — and a direct postgres-level UPDATE clearing deleted_at would also be blocked. The data-integrity invariant (uq_exam_configs_org_subject_active) remains as defense-in-depth. Covered by exam-config-reactivation-guard.spec.ts (blocked-reactivation + positive upsert control). | Migration 20260411000007; trigger mig 20260606000005 (#755) |
| AK | student reads exam_config_distributions directly (exam paper structure leak) | HIGH | rpc-cross-tenant.spec.ts | COVERED (commit a17fdc0f, #517) — "cross-org student cannot read exam_config_distributions": admin confirms examConfigId has ≥1 distribution row (non-vacuity), then crossOrgClient.from('exam_config_distributions').select('id').eq('exam_config_id', examConfigId) → error null + 0 rows. Pins the admin-only SELECT policy (is_admin() AND same org, latest mig 20260601000002) / no-student-policy asymmetry at the DB layer — a non-admin student sees none of the distribution rows. | Migration 20260411000001 |
| AL | student calls start_exam_session twice for the same subject concurrently (duplicate exam session) | LOW | session-race-condition.spec.ts | ENFORCED (mig 088, #754) — start_exam_session now catches unique_violation (23505) from uq_active_exam_session and maps it to the friendly domain error 'an exam session is already in progress for this subject'. The index column list was widened from (student_id, subject_id) to (student_id, organization_id, subject_id) aligning with uq_internal_exam_session_active. Defense stack: sequential IF EXISTS RAISE guard (mig 20260411000005) + DB-level partial unique index + error mapping (mig 088). Both the data-integrity invariant and the user-facing error surface are now fully closed. | Migration 20260411000005/000006; hardening mig 20260606000004 (#754) |
| AM | student mutates quiz_sessions.config.question_ids on active session before batch_submit (config injection) | HIGH | quiz-session-config-injection.spec.ts | FIXED (mig 079 / 20260502000001_quiz_sessions_immutable_columns) — BEFORE UPDATE OF trigger `quiz_sessions_protect_immutable_columns` blocks authenticated mutations to config, total_questions, mode, time_limit_seconds, started_at, organization_id, student_id, subject_id, topic_id. Service-role exempted via `current_role = 'service_role'` so SECURITY DEFINER completion paths still work. quiz-session-config-injection.spec.ts exercises 4 sub-vectors (config injection, started_at backdating, total_questions tampering, mode escalation), PLUS the #615 parametrized loop covering the remaining 6 frozen columns (time_limit_seconds, organization_id, student_id, subject_id, topic_id, created_at) — all 10 trigger-protected columns now individually exercised, each re-verifying byte-identity of the full frozen set via assertSessionStillLocked(). Implicitly resolves Vector BD's started_at concern at the DB layer. | Migration 20260502000001 — see red-team review 2026-05-02 / 2026-06-04 (#615) |
| AN | complete_empty_exam_session called without authentication | HIGH | server-action-unauthenticated.spec.ts | COVERED (commit f02031fa, #557) — "complete_empty_exam_session rejects unauthenticated callers": anon calls RPC with a real knownSessionId; SECURITY DEFINER `auth.uid() IS NULL → RAISE 'not authenticated'` fires before ownership lookup; assert error not null + ~/not authenticated/i + data null. Fulfils the deferral from rpc-complete-empty-exam.spec.ts header comment. | Commit 375bde5 |
| AO | complete_empty_exam_session called with a session_id owned by a different student (IDOR / session hijack) | HIGH | rpc-complete-empty-exam.spec.ts | COVERED (commit 921d0c0) — "AO: a different student cannot complete a foreign session (IDOR)": attacker calls RPC with victim's session_id; asserts error=/session not found or not accessible/i AND victim's session ended_at remains NULL. | Commit 375bde5; spec commit 921d0c0 |
| AP | complete_empty_exam_session called on a study/quick_quiz session to force-complete it as exam (mode confusion) | HIGH | rpc-complete-empty-exam.spec.ts | COVERED (commit 921d0c0) — "AP: the owner cannot complete a non-exam (quick_quiz) session": owner calls RPC with mode='quick_quiz' session; asserts error=/session is not an exam/i. Mode guard in mig 20260429000008 accepts mock_exam and internal_exam; rejects all other modes. | Commit 375bde5; spec commit 921d0c0 |
| AQ | complete_empty_exam_session called twice on a session that already has a real score (idempotent path overwrite) | MEDIUM | rpc-complete-empty-exam.spec.ts | COVERED (commit 921d0c0) — "AQ: idempotent replay returns the real stored score, never clobbers it to 0": seeds a session with score_percentage=75/passed=true/correct_count=3 (already ended), calls RPC, asserts result.score_percentage===75 AND result.passed===true AND DB row unchanged (still 75/true). NOTE: the AQ description in the original gap entry (commit 375bde5) said "returns a hardcoded 0/false payload" — this was the bug in mig 20260427000002. It was FIXED in mig 20260428000003 which added a SELECT INTO v_correct_count/v_score/v_passed/v_answered before the idempotent RETURN. The spec now exercises and pins the corrected re-read behaviour. Mirrors BA (complete_overdue idempotency is also correct). | Commit 375bde5 (original bug); mig 20260428000003 (fix); spec commit 921d0c0 |
| AR | complete_empty_exam_session called on a soft-deleted session | LOW | rpc-complete-empty-exam.spec.ts | COVERED (commit 921d0c0) — "AR: the owner cannot complete a soft-deleted session": admin soft-deletes the session then owner calls RPC; asserts error=/session not found or not accessible/i (deleted_at IS NULL in FOR UPDATE query blocks it). Mirrors BB (complete_overdue soft-deleted session). | Commit 375bde5; spec commit 921d0c0 |
| AS | audit_events row written by complete_empty_exam_session uses subquery for actor_role (non-atomic role read) | LOW | audit-event-forgery.spec.ts | ASSESSED LOW — the RPC writes actor_role via (SELECT role FROM users WHERE id = v_student_id AND deleted_at IS NULL) — now fixed in migration 051/20260428000001; the soft-delete gap from the original 049 migration is closed. The non-atomic race window for role changes is unchanged (sub-millisecond, requires admin action) — still no spec required for that residual; pattern noted for future RPCs to cache role in a DECLARE variable | Commit 375bde5; FIXED commit b4e5b7e |
| AT | Cross-org session enumeration via getActiveExamSession | HIGH | server-action-unauthenticated.spec.ts | COVERED — RLS students_select_sessions (USING student_id = auth.uid()) blocks cross-user reads; explicit .eq('student_id', user.id) filter adds redundant defence; no new spec required | Commit f22cb87 |
| AU | localStorage tampering — student edits localStorage to inject forged answers for a session they do not own | MEDIUM | session-replay.spec.ts | COVERED — batch_submit_quiz SECURITY DEFINER ownership check (qs.student_id = v_student_id) is the authoritative gate; localStorage is cosmetic; forged sessionId fails at RPC layer; existing session-replay.spec.ts exercises the ownership path; no new spec required | Commit f22cb87 |
| AV | SessionStorage handoff injection — forged handoff for another student's sessionId lets attacker resume foreign session | MEDIUM | session-replay.spec.ts | COVERED — first server RPC call enforces student_id ownership; mechanically identical to Vector AO; session-replay.spec.ts covers this path; no new spec required | Commit f22cb87 |
| AW | Resume race — two tabs both call getActiveExamSession, both render banners, both click Resume simultaneously | LOW | session-race-condition.spec.ts | ASSESSED LOW — getActiveExamSession is read-only; start_exam_session IF EXISTS guard prevents duplicate session creation; localStorage last-write-wins is cosmetic; no new DB write path; no spec required | Commit f22cb87 |
| AX | complete_overdue_exam_session called by a different student (IDOR — foreign session_id) | HIGH | rpc-complete-overdue-exam.spec.ts | COVERED (commit 64339b28) — "AX: a different student cannot complete a foreign overdue session (IDOR)": attacker calls RPC with victim's session_id, asserts error=/session not found or not accessible/i AND victim's session ended_at remains NULL | Commit 5cbbfe8; spec commit 64339b28 |
| AY | complete_overdue_exam_session called on a non-overdue still-running session | HIGH | rpc-complete-overdue-exam.spec.ts | COVERED (commit 64339b28) — "AY: the owner cannot complete a session that is not yet overdue": owner calls with started_at = now()-5s (under 30s grace), asserts error=/session is not overdue/i | Commit 5cbbfe8; spec commit 64339b28 |
| AZ | complete_overdue_exam_session called on a study-mode / quick_quiz session | HIGH | rpc-complete-overdue-exam.spec.ts | COVERED (commit 64339b28) — "AZ: the owner cannot complete a non-exam (quick_quiz) session": owner calls with mode='quick_quiz' overdue session, asserts error=/session is not an exam/i (mode guard accepts only mock_exam / internal_exam per mig 20260429000008) | Commit 5cbbfe8; spec commit 64339b28 |
| BA | complete_overdue_exam_session called twice — idempotent path returns real stored score now (FIXED in this RPC vs AQ) | LOW | session-replay.spec.ts | ASSESSED IMPROVED — unlike AQ (complete_empty idempotency bug), this RPC's idempotent branch re-reads v_correct_count/v_score/v_passed/v_answered from the DB via a second SELECT on quiz_sessions + quiz_session_answers (lines ~75-81 of migration). Returns the real score, not a hardcoded 0. No new gap vs AQ; session-replay.spec.ts does not cover this cross-RPC path but the defect present in AQ does NOT exist here. Document for completeness. | Commit 5cbbfe8 |
| BB | complete_overdue_exam_session called on a soft-deleted session | LOW | rpc-complete-overdue-exam.spec.ts | COVERED (commit 64339b28) — "BB: the owner cannot complete a soft-deleted session": admin soft-deletes the session then owner calls RPC, asserts error=/session not found or not accessible/i (deleted_at IS NULL in FOR UPDATE query blocks it). Mirrors AR (complete_empty soft-delete). | Commit 5cbbfe8; spec commit 64339b28 |
| BC | start_exam_session auto-complete path — concurrent double-completion on same overdue session | MEDIUM | rpc-complete-overdue-exam.spec.ts | COVERED (commit 64339b28) — "BC: concurrent completion on the same overdue session ends it exactly once": Promise.all of two victimClient.rpc calls on the same overdue session_id; asserts both calls succeed with no error, both return session_id, and DB row shows ended_at set exactly once (passed=false, score_percentage=0). FOR UPDATE serialises callers; second hits idempotent path. | Commit 5cbbfe8; spec commit 64339b28 |
| BD | start_exam_session returns started_at — client computes remaining time from server field; forged started_at via direct UPDATE on quiz_sessions shifts deadline client-side only | MEDIUM | session-replay.spec.ts | ASSESSED LOW for server enforcement — batch_submit_quiz and complete_overdue_exam_session enforce deadline using DB-stored started_at, not a client-supplied value. A student who mutates quiz_sessions.started_at via RLS UPDATE (Vector AM attack surface) would shift their own deadline on the server. This is a higher-severity implication of Vector AM, not a new standalone vector. No additional spec beyond AM's recommendation. Document here to cross-reference. | Commit 5cbbfe8 |

| BE | enforce_draft_limit trigger — search_path injection (pre-fix) | LOW | quiz-draft-injection.spec.ts | FIXED (commit 33ebcc6) | Trigger now has SET search_path = public; no spec needed — advisor fix, not an exploitable open gap |
| BF | start_quiz_session audit actor_role subquery — soft-deleted user spoofing (pre-fix) | MEDIUM | audit-event-forgery.spec.ts | FIXED (commit 33ebcc6) | deleted_at IS NULL added to actor_role subquery; closes security.md §10 pattern for this RPC; no spec asserted absence of filter |
| BG | batch_submit_quiz idempotent replay JOIN on questions — scoring soft-deleted question via replay (pre-fix) | MEDIUM | session-replay.spec.ts | FIXED (commit 33ebcc6) | AND q.deleted_at IS NULL added to idempotent replay JOIN only; bulk-fetch temp table intentionally untouched (security.md §15 write-once carve-out). #256 positively pins the carve-out from the scoring side: a question soft-deleted mid-session (after start, before batch_submit) is still scored on the active-session first-submit path. The test is non-vacuous (code-style.md §5): it first asserts the soft-delete affected a row (`delData?.length === 1`) before asserting `quiz_session_answers` count === 3, confirming the bulk-fetch reads config.question_ids with no deleted_at filter by design. |
| BH | batch_submit_quiz replay path callable by soft-deleted user with valid JWT — leaks completed quiz report to inactive caller (pre-fix) | HIGH | session-replay.spec.ts | FIXED (commit 3407658) | Top-level IF NOT EXISTS gate added after auth.uid() check raises 'user not found or inactive'. Closes a vector that survived 33ebcc6: lower scattered actor_role filters did not block the idempotent replay branch which short-circuits before any user JOIN. No spec asserted the absence of this gate; gap remains unfilled. |
| BI | enforce_draft_limit TOCTOU — two concurrent inserts both read count=19 and bypass the 20-draft cap (pre-fix) | LOW | quiz-draft-injection.spec.ts | FIXED (commit 3407658) | pg_advisory_xact_lock(hashtext(NEW.student_id::text)) added before the COUNT(*) read serialises per-student inserts within a transaction. Existing spec exercises foreign-question_id injection, not concurrency. No spec asserted the cap held under parallel inserts. |
| BJ | start_quiz_session refactor — TOCTOU between SELECT INTO v_org_id/role and audit-row INSERT (theoretical) | LOW | (no spec) | ASSESSED LOW | The locals capture (v_org_id, v_role) at function top is read inside one transaction; the subsequent INSERT INTO audit_events writes the captured locals, not a re-read. A student soft-deleted between the SELECT INTO and the INSERT would already be past the IF NOT FOUND gate, but the audit row would still record their pre-deletion role. Window is the function body (sub-millisecond) and requires admin action mid-call. Mechanically identical to the residual in Vector AS. No spec required. |
| BK | batch_submit_quiz TOCTOU tx-abort closed — user soft-deleted between gate and audit INSERT no longer aborts tx | LOW | session-replay.spec.ts | PARTIAL — concurrent no-double-insert COVERED (#257); mid-call soft-delete TOCTOU remains spec-inaccessible (sub-ms window) | #257 pins the FOR UPDATE serialisation invariant (concurrent batch_submit → exactly one scorer, no double answer-insert). The inner mid-call soft-delete sub-vector is still not Playwright-exercisable. Prior IF NOT EXISTS gate + scattered subquery for actor_role meant a mid-call soft-delete would null the subquery and abort the tx, losing all answer writes. Mig 078 caches role in v_actor_role via SELECT INTO + IF NOT FOUND at function top; both audit INSERTs (timeout + completion paths) use v_actor_role. The fix means a mid-call soft-delete no longer aborts the tx. Existing session-replay.spec.ts does not simulate the mid-call soft-delete scenario. Full spec coverage requires: complete quiz, admin-soft-delete the user after the gate fires but before audit INSERT — verifiable only if the guard closure is tested (call succeeds even when user was deleted mid-call). In practice the window is sub-millisecond; the important spec is the outer BH scenario (soft-deleted before the call). BH remains FIXED; BK is the inner-TOCTOU complement. |
| BL | start_quiz_session: smuggle cross-org/soft-deleted/draft/wrong-subject question UUIDs | HIGH | (no E2E spec) | COVERED AT INTEGRATION LAYER (mig 20260506000001 + rpc-start-session.integration.test.ts) | All 5 sub-vectors (cross-org, soft-deleted, inactive/draft, wrong-subject, non-existent UUID) are blocked by the new count-verification guard and covered by 5 Vitest integration tests. No E2E (Playwright) spec exercises these; see Recommendation 1. Vector D in rpc-cross-tenant.spec.ts passes a foreign-org question list — the spec's open assertion (expect(error).not.toBeNull() OR questionCount==0) still passes post-hardening because the RPC now raises 'invalid_question_ids'. The spec logic handles both the old (0-question session) and new (explicit error) outcomes. |
| BM | start_quiz_session: empty/null p_question_ids array (was 23502 NOT NULL violation, now 'no_questions_provided') | MEDIUM | server-action-unauthenticated.spec.ts | COVERED AT INTEGRATION LAYER; E2E spec passes a 1-element array (not empty) | The unauthenticated spec passes a valid 1-element array to start_quiz_session — it tests auth, not the empty-array path. The integration test explicitly covers []. No E2E spec exercises the empty-array path. Low priority for a dedicated E2E spec since the fix is purely server-side input validation with no UI path to trigger it. |
| BN | start_quiz_session: rate-limiting spec now uses status='active' filter — aligns with new RPC contract | LOW | rate-limiting.spec.ts | PASSING | The rate-limiting spec's beforeAll now filters questions with .eq('status', 'active') before passing IDs to start_quiz_session. Without this fix the spec would have started failing post-hardening when inactive questions were inadvertently passed. No new coverage gap; this was a fixture correctness fix, not an attack vector change. |
| BO | start_internal_exam_session.p_code — SQL fragment / control-character injection (OWASP A05) | HIGH | injection-sql.spec.ts | COVERED (commit c45330f) | 8 DB_LAYER payloads asserted to receive 'code_not_found'; 2 TRANSPORT_LAYER payloads (NUL byte, raw CRLF) asserted `error !== null`. RPC uses parameterised query; payloads cannot alter SQL structure. Closes A05 coverage for this parameter. |
| BP | void_internal_exam_code.p_reason — SQL fragment / whitespace / control-character injection (OWASP A05) | HIGH | injection-sql.spec.ts | FULLY COVERED (commit d13dc55) | 8 DB_LAYER payloads stored as plain text (no side effects); 5 WHITESPACE payloads covering all 6 POSIX [[:space:]] class members (0x20 spaces-only, 0x09 tabs-only, 0x0A newlines-only, 0x0D cr-only, 0x20+0x09+0x0A+0x0D+0x0C+0x0B mixed-whitespace) rejected as 'invalid_reason'; 1 TRANSPORT_LAYER payload (NUL) rejected before RPC. Guard is POSIX `p_reason ~ '^[[:space:]]*$'` (migration 20260507000001). All 5 whitespace tests pass locally. ERROR_MESSAGES map in void-code.ts now maps invalid_reason → user-facing string; Vitest case added. No remaining coverage gap. |
| BQ | submit_quiz_answer.p_selected_option — SQL fragment / control-character injection (OWASP A05) | HIGH | injection-sql.spec.ts | COVERED (commit c45330f) | 8 DB_LAYER payloads rejected as option-not-member-of-question (guard 5); 2 TRANSPORT_LAYER payloads rejected before persistence. Fixture creates a live practice session so payloads reach guard 5, not guard 3 (question-membership). Closes A05 coverage for this parameter. |
| BR | Admin-authored XSS payloads rendered in student contexts — full_name, question_text, explanation_text (OWASP A05) | HIGH | injection-xss.spec.ts | COVERED (commit c45330f) | 5 payloads each for student full_name (admin list), question_text (quiz), explanation_text (feedback panel). DOM assertions: no <script>, no on* attributes, no javascript: href, window.__pwned canary unset. Covers React sanitization and react-markdown + remark-gfm regression. |
| BS | Security headers on /, /app/dashboard, /auth/login — 7 headers × 3 paths (OWASP A02) | HIGH | header-validation.spec.ts | COVERED (commit c45330f / ef53aa3); 4xx/5xx header path COVERED at code layer (commit 5f1295c — `applySecurityHeaders` applied to 403 and 503 branches); E2E spec coverage for 4xx/5xx tracked in issue #637 | 21 assertions across Content-Security-Policy (structural, per response class: routed=full CSP, redirect/middleware=`default-src 'none'; frame-ancestors 'none'` — intentionally reduced, no scripts execute on 3xx/4xx/5xx), Strict-Transport-Security (max-age ≥ 6mo), X-Frame-Options (DENY — tightened from SAMEORIGIN per issue #631; security.md code block corrected to DENY in commit 17ec581), X-Content-Type-Options, Referrer-Policy, Permissions-Policy. Redirect responses from Edge Middleware now carry headers via proxy.ts (commit ef53aa3). 4xx/5xx responses use identical `applySecurityHeaders()` helper — same 7-header posture as redirects. Distinction between routed CSP and middleware CSP now documented in docs/security.md §6 (commit 17ec581). Issue #637 tracks E2E spec addition (needs authenticated non-admin fixture). |
| BT | audit_events positive emission — all 10 live event_type literals spec-asserted (OWASP A09) | MEDIUM | audit-completeness.spec.ts | COVERED (commit c45330f + 3790db7 cycle); exam.completed + internal_exam.completed metadata-key schema locked (mig 082 / commits e621f9ba+9091ad60, #570/#571) | quiz_session.batch_submitted, exam.started, exam.completed, exam.expired, internal_exam.code_issued, internal_exam.code_voided, internal_exam.started, internal_exam.completed, internal_exam.expired, student.login. Actor-id split: 8 by student, 2 by admin. Time-expired events use service-role started_at backdating (91s past 60s limit + 30s grace). internal_exam.completed (9th literal) and student.login (10th) were added after initial c45330f — both now covered. Migration 082 renames batch_submit_quiz completion-audit metadata keys 'answered'→'answered_count', 'correct'→'correct_count'; `expectCompletionMetadata` helper added to audit-completeness.spec.ts asserts (a) answered_count present, (b) correct_count present, (c) 'answered' absent, (d) 'correct' absent — for BOTH exam.completed and internal_exam.completed. The `complete_empty_exam_session` emitter of exam.completed (zero-answer in-grace path) is NOT yet covered by this helper — see BT2. |
| BT2 | audit_events metadata-key schema for complete_empty_exam_session's exam.completed emitter (zero-answer in-grace finish) | LOW | audit-completeness.spec.ts | COVERED (commit 14de172, #728) — "writes exam.completed with locked metadata on complete_empty_exam_session within grace": starts a real exam session, calls complete_empty_exam_session RPC, calls expectCompletionMetadata (asserts answered_count + correct_count keys present, legacy 'answered'/'correct' absent), then additionally asserts metadata.reason === 'completed with no answers'. This is a separate test case from the batch_submit_quiz emitter — locks the complete_empty-specific reason key. Verified vs migs 20260429000008 + 20260428000005. | Closes #728; sub-vector of BT |
| BU | start_quiz_session.p_mode — mode confusion injection (mock_exam / internal_exam bypass) (OWASP A05) | HIGH | start-quiz-session-mode-confusion.spec.ts | FIXED (migration 081 / commit 5b4223b, closes #629) | Hard whitelist `p_mode IN ('smart_review','quick_quiz')` raised before active-user gate (timing-attack defense). Spec asserts both `mock_exam` and `internal_exam` raise `mode_not_allowed` AND positive no-insert assertion (admin count=0 scoped to attacker_id + post-test `created_at`). Integration coverage: `packages/db/src/__integration__/rpc-start-session.integration.test.ts` (commit fc5d47e) — 2 cases against local Postgres. mock_exam sessions remain creatable via `start_exam_session` (mig 040); internal_exam via `start_internal_exam_session` (mig 058). |
| BV | upsert_exam_config.p_distributions — admin JSONB parameter injection (OWASP A05) | MEDIUM | upsert-exam-config-injection.spec.ts | COVERED (commit f2c52459, #633, PR #747) — 5 cases as authenticated admin: SQL-injection string as topic_id → 22P02 (uuid cast rejects as data, never executes); non-UUID topic_id → 22P02; non-integer question_count → 22P02 (int cast, fires before FK); negative question_count (real topic) → 23514 (CHECK question_count > 0); missing topic_id key → 23502 (NOT NULL). Non-vacuous: SQLSTATE ≠ P0001 proves admin passed auth.uid() + is_admin() and rejection came from the distribution INSERT, not the gate. No cleanup: single-tx RPC rolls back atomically on any INSERT failure, leaving egmont seed config untouched. Pins the DB-layer guard; the Server Action Zod schema is unit-tested in schema.test.ts (unreachable from PostgREST). | mig 20260411000008; DB-layer defense-in-depth behind Zod |
| BV2 | upsert_exam_config called by authenticated non-admin (student/instructor) — privilege escalation | MEDIUM | upsert-exam-config-injection.spec.ts | COVERED (commit 4bdb533b, PR #747) — "rejects an authenticated non-admin caller": a seeded student calls upsert_exam_config with a WELL-FORMED payload and is rejected with 'admin access required' (P0001, message-match). Well-formed payload isolates the rejection cause to the role guard, not input shape. Non-vacuous: the student is authenticated (passes auth.uid()), so this pins the is_admin role guard specifically — distinct from the unauthenticated path (Hub A / server-action-unauthenticated.spec.ts). Role guard fires before any write → nothing persisted, no cleanup. Surfaced during the #633 BV review and folded into the same PR. | mig 20260411000008 (IF v_role != 'admin' RAISE) |
| BW1 | get_student_mastery_stats() called by unauthenticated client — empty-set guarantee | MEDIUM | server-action-unauthenticated.spec.ts | COVERED (#673 — commit 5e4d426) — "get_student_mastery_stats returns an empty set for an unauthenticated caller": asserts error===null, data.length===0 | Migration 20260521000005 |
| BW2 | get_student_mastery_stats() called by cross-org student — correct-count leak via RPC | HIGH | rpc-cross-tenant.spec.ts | COVERED (#673) — "cross-org student sees no egmont questions or correct counts from get_student_mastery_stats": asserts data.length===0 (no egmont question reaches the cross-org caller's denominator via tenant_isolation) AND every(correct===0), after seedVictimResponses(). The earlier vacuity weakness (bare every() on an empty array) was fixed by adding the length===0 assertion; the non-vacuous numerator self-scope proof lives in BW3 (dashboard-stats-rpc-isolation.spec.ts, populated denominator). | Migration 20260521000005 |
| BW3 | get_student_mastery_stats() called by instructor/admin — self-scope regression guard (instructors_read_students policy) | MEDIUM | dashboard-stats-rpc-isolation.spec.ts | COVERED (#673 — commit 5e4d426) — "egmont instructor with no responses sees denominator rows but zero correct counts": asserts length>0 (non-vacuous) AND every(correct===0). Strongest assertion of the BW/BX class. | Migration 20260521000005 |
| BX1 | get_student_streak() called by unauthenticated client — single {0,0} row guarantee | MEDIUM | server-action-unauthenticated.spec.ts | COVERED (#673 — commit 5e4d426) — "get_student_streak returns a single zeroed row for an unauthenticated caller": asserts data.length===1, current_streak===0, best_streak===0 | Migration 20260521000006 |
| BX2 | get_student_last_practiced() called by unauthenticated client — empty-set guarantee | MEDIUM | server-action-unauthenticated.spec.ts | COVERED (#673 — commit 5e4d426) — "get_student_last_practiced returns an empty set for an unauthenticated caller": asserts data.length===0 | Migration 20260521000006 |
| BX3 | get_student_streak() called by instructor/admin — self-scope regression guard | MEDIUM | dashboard-stats-rpc-isolation.spec.ts | COVERED (#673 — commit 5e4d426) — "egmont instructor with no responses sees a single {0,0} streak row": asserts data.length===1, current_streak===0, best_streak===0 | Migration 20260521000006 |
| BX4 | get_student_last_practiced() called by instructor/admin — self-scope regression guard | MEDIUM | dashboard-stats-rpc-isolation.spec.ts | COVERED (#673 — commit 5e4d426) — "egmont instructor with no responses sees an empty last-practiced array": asserts data.length===0 | Migration 20260521000006 |
| BX5 | get_student_streak() called by cross-org student — no cross-org streak | HIGH | rpc-cross-tenant.spec.ts | COVERED (#673 — commit 5e4d426) — "cross-org student sees zero streak counts from get_student_streak": asserts data.length===1, current_streak===0, best_streak===0 after seedVictimResponses() | Migration 20260521000006 |
| BX6 | get_student_last_practiced() called by cross-org student — no other-org subject_id leak | HIGH | rpc-cross-tenant.spec.ts | COVERED (#673 — commit 5e4d426) — "cross-org student sees no practice history from egmont victim in get_student_last_practiced": asserts data.length===0 after seedVictimResponses() | Migration 20260521000006 |
| BX7 | get_student_streak() gaps-and-islands correctness — seeded runs produce expected current=3/best=5 | MEDIUM | dashboard-stats-rpc-isolation.spec.ts | COVERED (#673 — commit 5e4d426) — "egmont victim streak reflects seeded runs: current = 3 days, best = 5 days": offsets [0,1,2] → current run; gap at [3,4,5]; offsets [6,7,8,9,10] → best run; noon-UTC single-snapshot prevents UTC-midnight drift; asserts data.length===1, current_streak===3, best_streak===5 | Migration 20260521000006 |
| BY1 | get_admin_dashboard_students called by authenticated non-admin (student or instructor) — must raise 'forbidden' (privilege escalation) | HIGH | rpc-admin-dashboard-students.spec.ts | COVERED (commit ad19626) — "BY1: an unauthenticated caller is rejected" (error ~ /not authenticated/i), "BY1: a student caller is forbidden" (error ~ /forbidden/i), "BY1: an instructor caller is forbidden — only is_admin() gates the RPC" (error ~ /forbidden/i). All three sub-paths covered. | Migration 20260527000001 |
| BZ1 | get_admin_dashboard_students called by admin of org-A with a forged/manipulated org scope — must not return org-B students (cross-org admin isolation) | HIGH | rpc-admin-dashboard-students.spec.ts | COVERED (commit ad19626) — "BZ1: a cross-org admin sees only their own org's students, never the egmont victim": cross-org admin calls RPC, asserts returned rows contain no victimUserId. Positive: egmont admin confirms victimUserId is present. Org derived from auth.uid() — no parameter override path. | Migration 20260527000001 |
| CA | get_random_question_ids / get_filtered_question_counts called by unauthenticated client — empty-set guarantee | MEDIUM | server-action-unauthenticated.spec.ts | COVERED (unauth, commit f02031fa, #689) — "get_random_question_ids returns an empty set for an unauthenticated caller" + "get_filtered_question_counts ...": both SECURITY INVOKER run as anon; `_filtered_question_pool` reads questions under tenant_isolation RLS → empty for anon (real knownSubjectId with active questions proves RLS blocks, not table emptiness); assert error null + data.length===0. Anon retains Postgres PUBLIC-default EXECUTE (no REVOKE in mig tree). Cross-org probe (Vector CB) now COVERED in Hub B. (Prior #673/5e4d426 covered the older get_question_counts, a different RPC.) | Migration 20260528000001 |
| CB | get_random_question_ids / get_filtered_question_counts called cross-org (cross-org subject_id probe + cross-student filter isolation) | HIGH | rpc-cross-tenant.spec.ts | PARTIAL — org-scope COVERED (commit a17fdc0f, #690): "cross-org student sees no egmont questions from get_random_question_ids" (egmontQuestionIds.length>0 non-vacuity, then error null + 0 rows) + "...no egmont counts from get_filtered_question_counts" (same). Both SECURITY INVOKER + tenant_isolation RLS. Prior #673/5e4d426 covered the older get_question_counts. RESIDUAL GAP: cross-student filter isolation via fsrs_cards / active_flagged_questions in _filtered_question_pool (per-student weighting boundary, not the org boundary). | Migration 20260528000001 |
| CC | _filtered_question_pool called directly by an authenticated student with p_count absent (wrapper bypassed) — direct call returns raw question IDs (only id/topic_id/subtopic_id columns) up to the PostgREST 1000-row default cap. No security gap at the column level (no correct_option_id, explanation, question_text in the return set). The 1000-row cap is a footgun (noted in the function comment) but not a security issue. GRANT EXECUTE to authenticated is intentional. | LOW | (no spec) | ASSESSED LOW RISK — return set contains only id, topic_id, subtopic_id. No answer-column exposure. Org-scoping via tenant_isolation RLS still applies. Cross-student filter isolation via explicit student_id = auth.uid() still applies. The only risk is a student calling directly and getting a 1000-row-capped sample instead of the full pool — a usability footgun, not a security vector. No spec required; documented for completeness. | Migration 20260528000001 |
| CD | void_internal_exam_code — consumed code whose linked quiz_session was soft-deleted before void; RPC must raise session_state_changed and NOT void the code | HIGH | rpc-void-internal-exam-code.spec.ts | COVERED (#638 — mig 084 / 20260601000003, branch fix/db-softdelete-failfast-518-638, commit bf3a957e + hermetic afterEach in 655faa88) — test "voiding a consumed code whose session was soft-deleted raises session_state_changed (Vector CD)": seeds session + code, service-role soft-deletes the session, calls void RPC as admin, asserts error.message ~ /session_state_changed/i AND voided_at on code row remains NULL. 6/6 passing locally. | Prior to mig 084 both IF FOUND AND ... branches were gated on FOUND; if the SELECT ... FOR UPDATE on quiz_sessions returned no rows (session soft-deleted or cross-org) the code was silently voided. Fix: explicit IF NOT FOUND THEN RAISE session_state_changed immediately after the FOR UPDATE, redundant FOUND guards removed. Mirrors the existing ROW_COUNT=0 guard but fires on the no-row case. spec also added hermetic afterEach (soft-delete of created internal_exam_codes + quiz_sessions) per code-style.md §7. |
| CE | admin reads exam_config_distributions for a soft-deleted parent exam_config via direct PostgREST (RLS missing deleted_at filter — pre-mig 083) | HIGH | exam-config-distributions-rls.spec.ts | COVERED (commit ad19626) — "CE: distributions are visible while the parent is active, hidden after soft-delete": admin reads distributions before and after service-role soft-delete of the parent config; asserts before.length > 0, after.length === 0 (SELECT policy); parity: INSERT referencing soft-deleted parent is rejected (42501 RLS WITH CHECK violation). Non-vacuous positive assertion included. afterEach hard-deletes the throwaway config (ON DELETE CASCADE removes its distributions). | Migration 083 / 20260601000002. exam_config_distributions has no deleted_at column; hard-delete of the test config is the correct cleanup path (not soft-delete + cascade). |
| CF | void_internal_exam_code — admin soft-deleted after authorization but before audit INSERT still produces a valid committed void (cached-role pattern side-effect) | LOW | (no spec) | ASSESSED LOW — intentional fix behaviour, not an exploitable path. Pre-fix: admin soft-deleted mid-call caused NOT NULL abort, silently rolling back an authorized void. Post-fix (mig 084/085): void is committed with the cached pre-deletion role. Audit row is accurate (role from deleted_at-filtered authz read). No new attack surface: (a) soft-deleted-before-authz still raises 'not an admin'; (b) the window is sub-millisecond and requires a concurrent admin action; (c) the void was already authorized. Mirrors residual TOCTOU in BJ/AS. No spec required. **Cached-role class fully closed (mig 087, #734):** start_internal_exam_session + issue_internal_exam_code now also cache actor_role via a top-level SELECT INTO gate (mirrors mig 076/078/084 pattern). mig 088 (#754) extends the same pattern to start_exam_session. All audit-writing RPCs in the exam/internal-exam lifecycle now cache role at function entry rather than using a scalar subquery at INSERT time. | Commit f0750992. Mig 076 (start_quiz_session), 078 (batch_submit_quiz), 084 (void_internal_exam_code), 087 (start_internal_exam_session + issue_internal_exam_code), 088 (start_exam_session). |
| BL | quiz_sessions score forgery via direct UPDATE — correct_count column | HIGH | quiz-session-score-forgery.spec.ts | COVERED (commit 01a73368, #611) — "Vector BL: a student cannot forge correct_count via direct UPDATE": asserts error.code==='42501' (permission denied for column correct_count). Defense: mig 20260605000001 REVOKE+GRANT. | Migration 20260605000001; closes #611 |
| BM | quiz_sessions score forgery via direct UPDATE — score_percentage column | HIGH | quiz-session-score-forgery.spec.ts | COVERED (commit 01a73368, #611) — "Vector BM: a student cannot forge score_percentage via direct UPDATE": asserts error.code==='42501'. | Migration 20260605000001 |
| BN | quiz_sessions score forgery via direct UPDATE — passed column | HIGH | quiz-session-score-forgery.spec.ts | COVERED (commit 01a73368, #611) — "Vector BN: a student cannot forge passed via direct UPDATE": asserts error.code==='42501'. | Migration 20260605000001 |
| BO-ended_at | quiz_sessions self-completion via direct UPDATE — ended_at column | HIGH | quiz-session-score-forgery.spec.ts | COVERED (commit 01a73368, #611) — "a student cannot self-complete by forging ended_at via direct UPDATE": asserts error.code==='42501'. Also covered by no-mutation re-read test (correct_count=0, score_percentage=null, passed=null, ended_at=null after all forge attempts) and positive-control discard test (deleted_at still writable — 42501s are column-specific, not table-wide REVOKE). | Migration 20260605000001 |
| CG | users sensitive-column forge (role/organization_id/deleted_at) via direct UPDATE — trigger vs. column GRANT | MEDIUM | users-role-forge.spec.ts | HARDENED (mig 090, #773) — mig 090 (`20260606000006`) adds REVOKE UPDATE ON users FROM authenticated + GRANT UPDATE (full_name) ON users TO authenticated. A direct `UPDATE users SET role='admin'` now returns 42501 (permission denied for column) before the trigger or RLS fires. users-role-forge.spec.ts asserts the 42501 error where observable. Defense stack: privilege layer (42501 before query executes) → trigger trg_protect_users_sensitive_columns (mig 041) → RLS (mig 020 + 056). Mirrors the quiz_sessions column-GRANT pattern (mig 20260605000001 / #611). | Mig 20260326000056 (RLS) + mig 20260316000041 (trigger) + mig 20260606000006 (column GRANT). |
| CH | quiz_sessions score forgery via SECURITY DEFINER bypass — calling completion RPC with crafted correct_count input | LOW | (no spec) | ASSESSED NON-ISSUE — batch_submit_quiz / complete_overdue_exam_session / complete_empty_exam_session are SECURITY DEFINER (postgres-owned), so they run outside authenticated column grants. However, none of these RPCs accept correct_count / score_percentage / passed as parameters — scores are computed server-side from quiz_session_answers. A student cannot supply a forged score as an RPC parameter. The column GRANT restriction closes the direct-UPDATE path; the RPC path has no score input surface to exploit. | Migration 20260605000001. |
| CI | start_quiz_session.p_question_ids array-length resource exhaustion — caller passes arbitrarily large array to force a full-table scan on questions | MEDIUM | start-quiz-session-mode-confusion.spec.ts | DB-CAPPED (mig 086, #275) — start_quiz_session raises `too_many_questions` when `array_length(p_question_ids, 1) > 500`. Cap enforced at the DB layer before any question lookup. Server Action Zod schema enforces the same cap so a direct RPC caller cannot bypass it. start-quiz-session-mode-confusion.spec.ts covers the array-cap path. | Mig 20260606000002 / #275. Mirrors the get_random_question_ids LIMIT LEAST(p_count, 500) cap (mig 20260528000001). |
| CJ | OWASP A10:2025 — error-path information disclosure via SECURITY DEFINER RPCs and Server Actions | HIGH | error-handling-disclosure.spec.ts | COVERED (commit 9a728afd, #634) — 5 probes (start_internal_exam_session, complete_overdue_exam_session, complete_empty_exam_session, void_internal_exam_code, get_report_correct_options): authenticated attacker calls each RPC with a well-formed-but-nonexistent identifier (crypto.randomUUID()); asserts error not null + data null (non-vacuous RAISE fires) + message matches the clean domain token + message matches none of the DISCLOSURE_PATTERNS (relation/column/function does not exist, search_path, pg_*, SQLSTATE, CONTEXT:, at character N, DSN params, postgres:// URL). `permission denied` (42501) intentionally excluded from disclosure regex — it is the expected clean authorization verdict. No afterEach cleanup required — probes are read-only (RAISE fires before any DML). Server Action layer: bulkUpdateStatus and softDeleteQuestion raw error.message leak FIXED (console.error server-side + generic return string). DEFERRED sub-vectors: (a) malformed-JSON transport 400 (Next.js framework-layer, not RPC-layer — no SECURITY DEFINER code runs; deferred, low priority); (b) DB-connection 500 / pgbouncer errors (requires infrastructure-level failure injection beyond Playwright's scope; infrastructure-team concern). | Closes #634. Two Server Action fixes: bulk-update-status.ts + soft-delete-question.ts (code-style.md §5 sanitize-error-messages rule). |
| CK | record_auth_event — student forges admin event (user.created / user.password_reset / user.deactivated) as self | HIGH | rpc-record-auth-event.integration.test.ts | COVERED AT INTEGRATION LAYER (#379, commit 0962112c) — test (b) calls all three admin-only event_types as a student → asserts /not authorized/i (v_role <> 'admin' gate). E2E Playwright still open (#788). | Migration 093 / 20260606000009 |
| CL | record_auth_event — student records self-service event for a different user (IDOR) | HIGH | rpc-record-auth-event.integration.test.ts | COVERED AT INTEGRATION LAYER (#379) — test (c): student calls user.password_changed with another user's id → /self event resource must be the actor/i. E2E open (#788). | Migration 093 |
| CM | record_auth_event — caller passes arbitrary/unlisted event_type (whitelist bypass) | HIGH | rpc-record-auth-event.integration.test.ts | COVERED AT INTEGRATION LAYER (#379) — test (d): unlisted event_type → /unsupported event_type/i (whitelist ELSE). E2E open (#788). | Migration 093 |
| CN | record_auth_event — unauthenticated caller (anon) | HIGH | server-action-unauthenticated.spec.ts | GAP — Hub A deferral (#788). A4 sub-vector: anon calls RPC → auth.uid() IS NULL → RAISE 'not authenticated'. Guard is the standard SECURITY DEFINER auth.uid() null-check shared by all RPCs. | Migration 093 |
| CO | record_auth_event — cross-org resource_id in an admin event | LOW | rpc-record-auth-event.integration.test.ts | ASSESSED NON-ISSUE / BEHAVIOR CHANGED (#379, CR-local Major) — the resource org-scope SELECT was REMOVED: it would need AND deleted_at IS NULL per §9, which would reject the user.deactivated audit (target already soft-deleted). A cross-org resource_id is now ACCEPTED but logged under the ADMIN's OWN org (organization_id = actor's org) — no cross-org read or write, only self-referential log noise. Pinned by integration test (f). | Migration 093 |
| CP | record_auth_event — actor_id derivable from caller input (cannot forge foreign actor_id) | HIGH | rpc-record-auth-event.integration.test.ts | COVERED AT INTEGRATION LAYER (#379) — test (e): admin records with p_resource_id=studentId; asserts audit row actor_id===adminUserId (auth.uid()-derived, never p_resource_id). | Migration 093 |
| CQ | audit-completeness: 4 new event_type literals not yet in audit-completeness.spec.ts | MEDIUM | audit-completeness.spec.ts | GAP — user.password_changed, user.password_reset, user.deactivated, user.created are written by the 4 Server Actions but no Playwright E2E spec asserts these rows land in audit_events. audit-completeness.spec.ts covers 10 existing event types; these 4 are not in the list. | Migration 093 |

## Consent Gate — Seeding Note (added 2026-03-27)

All specs that use `seedRedTeamUsers()` and then exercise RPC calls or browser navigation to `/app/*` routes will now be blocked by the consent gate in proxy.ts. The gate checks the `__consent` cookie against `CURRENT_TOS_VERSION:CURRENT_PRIVACY_VERSION` (currently `v1.0:v1.0`).

**Specs that call RPCs directly via `createAuthenticatedClient` are NOT affected** — they bypass the Next.js proxy entirely and call Supabase directly. These specs (rpc-cross-tenant, rpc-question-membership, session-replay, session-race-condition, audit-event-forgery, server-action-unauthenticated) do not go through the proxy and therefore are unaffected by the cookie gate.

**Specs that use a real browser page (Playwright `page`) ARE affected** — pkce-state.spec.ts navigates to `/app/dashboard` in one test case. If the PKCE-failed user somehow obtains a valid session (which should not happen, but the spec verifies), the consent gate would redirect them to `/consent` rather than staying on `/app/dashboard`. The existing assertion `expect(page).not.toHaveURL(/\/app\/dashboard/)` still passes because `/consent` is also not `/app/dashboard`, so the spec remains valid without modification.

**Seeding consent records for future browser-based specs:** any new spec that logs a user in via the browser (not via `createAuthenticatedClient`) and then navigates to `/app/*` must either (a) seed consent records via admin client and set the cookie, or (b) complete the consent flow as part of test setup. The `seed.ts` helper does not currently seed consent records.

## Files to Watch

These files contain the attack surface. Changes here should trigger a red-team review:

- `supabase/migrations/*.sql` — RLS policies, RPCs, table definitions
- `packages/db/src/schema.ts` — Zod schemas for input validation
- `apps/web/app/app/quiz/actions/*.ts` — Server Actions (auth checks, input validation)
- `apps/web/proxy.ts` — session middleware
- `apps/web/app/auth/callback/route.ts` — PKCE callback
- `apps/web/app/auth/login-complete/route.ts` — consent check + cookie issuance
- `apps/web/lib/consent/versions.ts` — consent version constants (bump triggers re-consent)
- `apps/web/lib/consent/check-consent.ts` — checkConsentStatus + buildConsentCookieValue
- `apps/web/lib/queries/quiz-report.ts` — post-session report query (answer key access path)

## Lessons Learned

2026-03-16 — review of branch fix/45-remove-answer-keys-from-test:
- The migration sequence (032→033→034) shows iterative hardening: the final RPC correctly derives question IDs from quiz_session_answers rather than accepting them as a parameter, eliminating arbitrary question-ID probing. This is the right pattern.
- quiz-report.ts performs a direct .select('options') on the questions table (line 88). The `correct` boolean is filtered in TypeScript inside buildReportQuestions, not at the DB layer. A future refactor that changes the mapping logic could silently re-expose answer keys. A red-team spec should assert the report payload never contains `correct` fields.
- New RPCs require matching unauthenticated and cross-tenant test cases on every addition — these are not covered by default by the existing generic specs.

2026-03-27 — review of migration 059 (audit_read_own policy — GDPR Article 15 student self-read):
- The new SELECT policy `audit_read_own` uses `USING (actor_id = auth.uid())`. This is correctly scoped — a student can only see rows where they are the actor. They cannot enumerate other students' events by probing UUIDs.
- No event type in the system writes to audit_events with a student's actor_id AND contains data about a different user in metadata. All student-authored events (quiz_session.started, quiz_session.completed, quiz_session.batch_submitted, student.login) contain only session/score data for the student themselves. Admin-authored events (question.created, question.edited, question.deleted) use the admin's actor_id and are not reachable by the new policy.
- The policy does NOT conflict with audit_read_instructors. Postgres evaluates permissive policies with OR — instructors now match BOTH policies (their own events via audit_read_own PLUS all org events via audit_read_instructors). This is correct and additive; instructors do not lose access.
- The immutability policies (audit_no_update, audit_no_delete, audit_no_direct_insert) are unchanged and unaffected. Adding a SELECT policy cannot widen INSERT/UPDATE/DELETE surface.
- GAP IDENTIFIED: audit-event-forgery.spec.ts tests INSERT/UPDATE/DELETE forgery (Vector F) but has no test case for cross-user SELECT after the new policy is added. A student should attempt to SELECT with a filter against a known other user's actor_id and get 0 rows. This is now Vector AA.
- The server-action-unauthenticated.spec.ts test at line 136 ("unauthenticated client sees 0 rows from audit_events") remains valid and still passes — the new policy requires auth.uid() IS NOT NULL implicitly (auth.uid() returns null for unauthed clients, and null = null is false in SQL USING clauses).
- collect-user-data.ts audit_events query at line 63-67 now correctly returns data when called via the RLS-scoped client (exportMyData path), resolving the GDPR Article 15 compliance gap flagged as Issue 1 in the semantic-reviewer session log from 2026-03-27.

2026-03-27 — review of GDPR consent gate (migration 057 + proxy.ts + login-complete/route.ts):
- The consent gate in proxy.ts is cookie-only — it does not re-check the DB on every request. This is intentional (performance) but means a forged cookie with the correct format (`v1.0:v1.0`) bypasses the gate entirely. The DB is only consulted at login-complete time. A spec should confirm that an attacker who knows the cookie format and value cannot gain access to /app/* routes without a valid auth session — the auth session check in proxy.ts (lines 39-43 in the diff) still runs before the consent check, so unauthenticated requests remain blocked. The consent gate only applies to authenticated users.
- Two new RPCs (record_consent, check_consent_status) follow the correct SECURITY DEFINER + auth.uid() + SET search_path = public pattern. Neither accepts user_id as a parameter — both derive identity from auth.uid(). This is the correct IDOR-prevention pattern, matching the flag.ts precedent.
- user_consents table correctly mirrors the audit_events append-only pattern: INSERT only via RPC (WITH CHECK false on direct INSERT), no UPDATE policy, no DELETE policy. This is consistent with the immutable table rules.
- The cookie value is `${TOS_VERSION}:${PRIVACY_VERSION}` — currently `v1.0:v1.0`. This is a predictable format. If an attacker knows the current version strings (which are in client-side JS as they're imported into proxy.ts which runs as middleware), they can forge the cookie. The only protection against this is that the auth session check runs first. A spec should pin this dependency explicitly.
- pkce-state.spec.ts test "does not leak session after failed PKCE exchange" navigates to /app/dashboard after a failed exchange. With the consent gate active, even a user who somehow obtained a session but has no consent record would be redirected to /consent, not /app/dashboard. The existing assertion `not.toHaveURL(/\/app\/dashboard/)` remains correct either way.

2026-04-11 — review of exam-mode PR1 (migrations 20260411000001–000007):

**Migration 20260411000007 (partial unique index on exam_configs):**
- Replaces full UNIQUE(organization_id, subject_id) constraint with partial unique index WHERE deleted_at IS NULL.
- This change is purely additive in terms of correctness: uniqueness now applies only to active rows. Soft-deleted configs no longer block re-creation for the same org+subject pair.
- Attack surface change: the uniqueness enforcement is now conditional. The DB still prevents two simultaneous active configs (index enforces it). An admin cannot clear deleted_at on a soft-deleted config while an active one exists — the partial index would raise a unique violation. This is correct.
- The student_select_exam_configs RLS policy already had AND deleted_at IS NULL (migration 000001), so students were never able to see soft-deleted configs. The partial index aligns the uniqueness constraint with the existing visibility policy.
- No existing spec covers this table. New gaps AG, AH, AJ, AK, AL documented above.

**New RPC start_exam_session (migrations 20260411000003, 000005):**
- Follows correct SECURITY DEFINER + auth.uid() + SET search_path = public pattern.
- Org-scoped exam_configs lookup (ec.organization_id = v_org_id) prevents cross-tenant config access.
- deleted_at IS NULL filter on both users and exam_configs lookups — SECURITY DEFINER soft-delete rule satisfied.
- Duplicate active session guard (IF EXISTS ... RAISE EXCEPTION) prevents race-condition exam creation.
- No spec exercises this RPC in any red-team scenario. Gaps AG and AH are the primary concerns.

**Modified RPC batch_submit_quiz (migration 20260411000005):**
- Adds server-side time limit enforcement (now() > started_at + (time_limit + 30s)). Grace period of 30s is intentional.
- Adds mock_exam mode guard requiring all questions answered.
- Idempotent replay path (v_ended_at IS NOT NULL) is unchanged — session-replay.spec.ts (Vector J) still covers this.
- New attack surface: time bypass (submit after deadline). Gap AI documented.

2026-04-08 — review of admin hardening batch (migration 20260408000007, require-admin.ts, getStudentDetail):

**Migration 20260408000007 (get_admin_student_stats — include inactive students):**
- The change removes `AND u.deleted_at IS NULL` from the final `FROM users` JOIN in the RPC. This is intentional: the admin dashboard must show stats for inactive (soft-deleted) students.
- The security-relevant guards are all intact: (a) auth.uid() IS NULL check, (b) is_admin() check, (c) the admin's own profile still requires `deleted_at IS NULL` for org resolution (line 33), (d) questions/sessions sub-queries all retain `AND deleted_at IS NULL`.
- The `docs/security.md` SECURITY DEFINER soft-delete rule states "every SELECT inside a SECURITY DEFINER function must explicitly filter deleted_at IS NULL on all soft-deletable tables." The new RPC intentionally violates this rule on the final user JOIN. This is the correct business decision — the rule was written for student-facing RPCs that should not see deleted data. Admin RPCs may deliberately include it. The rule in docs/security.md should be annotated with this admin exception to prevent the auditor from flagging future intentional occurrences.
- The RPC `get_admin_student_stats` has never been tested in any red-team spec. Given migration 060007 widened the result set, this is a new gap (Vector AC).

**require-admin.ts (redirect pattern):**
- Changed from `throw new Error('Not authenticated')` / `throw new Error('Forbidden: admin role required')` to `redirect('/auth/login')` / `redirect('/app')`.
- `redirect()` in Next.js throws a NEXT_REDIRECT error that propagates through the framework. This is the standard Next.js Server Action/Server Component pattern — it is not a bypass risk.
- The existing unit tests (`require-admin.test.ts`) now assert the redirect error shape, confirming the framework will catch and handle it correctly.
- No bypass potential identified. The auth check (supabase.auth.getUser()) and role check (profile.role !== 'admin') still run in the correct order before any redirect.

**getStudentDetail role guard (.eq('role', 'student') replacing .is('deleted_at', null)):**
- The old filter excluded soft-deleted students from the detail page. The new filter replaces it with a role check, allowing soft-deleted students to be fetched (their `deletedAt` field is now returned and displayed in the UI).
- The `adminClient` (service role) is used — RLS does not apply. The three explicit guards are: (a) requireAdmin() call for auth+admin check, (b) .eq('organization_id', organizationId) for org scope, (c) .eq('role', 'student') to prevent cross-role access.
- Gap: no spec verifies that probing with an admin UUID returns null (Vector AE).

2026-04-27 — review of commits 8a36a92/16ee419/791528c (batch_submit_quiz deleted_at carve-out + security doc update):

- batch_submit_quiz migration 20260427000001 drops the `AND q.deleted_at IS NULL` predicate from the bulk-fetch temp table. This is a deliberate revert of a regression introduced in 20260413000002. The historical-scoring contract (docs/database.md §3) requires that soft-deleted questions remain scoreable for sessions that were started before the question was retired.
- The security argument rests on two invariants: (1) quiz_sessions.config.question_ids is written once at session start and never mutated by a student; (2) the per-answer `IF NOT (v_question_id = ANY(v_session_question_ids)) THEN RAISE` guard prevents injection of IDs not in the session at scan time.
- Invariant (1) is NOT enforced at the DB layer. The students_update_sessions RLS WITH CHECK is `student_id = auth.uid()` — it allows the student to UPDATE any column including config while ended_at IS NULL. No generated-column constraint, trigger, or column-level policy restricts config mutations. This is Vector AM (HIGH gap).
- The docs/security.md carve-out (commits 16ee419/791528c) says the exception requires an "immutable, write-once column." The column is not actually immutable at the DB layer. The carve-out is a documentation assertion, not a technical guarantee.
- Specs that must be re-run before push: session-replay.spec.ts (calls batch_submit_quiz for idempotency), session-race-condition.spec.ts (calls batch_submit_quiz as part of both test setups).
- New gap Vector AM recorded in the mapping table above.

2026-04-27 — review of commit f22cb87 (getActiveExamSession Server Action + ResumeExamBanner + localStorage persistence):

- New Server Action getActiveExamSession: uses anon Supabase client (RLS-scoped, not SECURITY DEFINER). No Zod needed — zero input parameters. Auth gate via getUser() present. Error messages sanitized. .eq('student_id', user.id) explicit filter redundant with RLS students_select_sessions (USING student_id = auth.uid()) but not harmful. Cross-org enumeration (Vector AT) is blocked by both the explicit filter AND the existing RLS policy — double-defended. No new attack surface at the DB layer.
- localStorage persistence (mode='exam' tag): answers stored client-side are cosmetic only. The Server Action batch_submit_quiz reads answers from the request body at submit time; the server does not read from localStorage. A student who edits localStorage only corrupts their own resume UI. No spec needed for localStorage data itself.
- Vector AU (localStorage injection for foreign session): when the student submits, the sessionId in the request body is validated by batch_submit_quiz via qs.student_id = v_student_id (SECURITY DEFINER ownership check) and qs.organization_id = v_org_id. The RPC will raise "session not found or not accessible" for a forged sessionId belonging to another student. Mechanically identical to Vector AO/AV. No new spec needed; existing session-replay.spec.ts exercises this ownership check path.
- Vector AV (sessionStorage handoff injection): /app/quiz/session hydrates from sessionStorage or URL params, but the first RPC call (batch_submit_quiz or get_quiz_questions) enforces student_id ownership server-side. A forged handoff for another student's sessionId will fail at the first server round-trip. No new attack surface beyond what AO covers.
- Vector AW (dual-tab resume race): both tabs call getActiveExamSession and both receive the same active session row (read-only). Both render ResumeExamBanner. If both click Resume simultaneously, localStorage contention is cosmetic (last-write-wins on key per-sessionId). The exam session itself on the server is not duplicated — start_exam_session already has a sequential guard (IF EXISTS RAISE EXCEPTION) and that guard covers both tabs reading the same existing session. No new DB write path introduced by this commit.
- getActiveExamSession returns success:false with 'Not authenticated' when unauthed (explicit check line 24). Unlike getFlaggedIds/getComments which silently return empty arrays, this action returns an error. Consistent with submitEmptyExamSession pattern. No gap.
- No Zod validation gap: Server Action accepts zero parameters. Zod requirement applies to input parsing — nothing to parse here.

2026-04-27 — review of commit 5b36d7e (batch_submit_quiz migration 048 comment trim):

- Commit is a cosmetic-only change: 104 comment/blank lines removed, 32 shorter comment lines added. Every removed line begins with `--` or is blank. No SQL statement, keyword, operator, literal value, or whitespace inside any SQL expression was modified in either `packages/db/migrations/048_batch_submit_softdelete_regression.sql` or its Supabase mirror `supabase/migrations/20260427000001_batch_submit_softdelete_regression.sql`.
- The load-bearing security comments are preserved in condensed form: §3 docs reference, session-start membership-lock justification, 30s grace-period note, correct_option_id end-of-session safety note.
- SQL executed at migration time is byte-identical to commit 8a36a92. No new attack surface introduced. No existing spec behaviour changed.
- No specs need to be re-run solely because of this commit. The open gaps from the prior substantive review (AI, AM, and #545/#546/#554) remain unchanged.
- Pattern noted: comment-only migration diffs touching `batch_submit_quiz` can be confirmed safe by checking that every `+`/`-` line starts with `--` or is blank. If any `+`/`-` line contains a SQL keyword not prefixed by `--`, the diff is NOT comment-only and full re-review is required.

2026-04-27 — review of commit 375bde5 (complete_empty_exam_session RPC + submitEmptyExamSession Server Action):

- New SECURITY DEFINER RPC complete_empty_exam_session introduces a second write path on quiz_sessions.ended_at / score_percentage / passed alongside batch_submit_quiz. Pattern compliance: auth.uid() IS NULL check present, SET search_path = public present, org-scope via users.organization_id, ownership via qs.student_id = v_student_id, mode guard (mock_exam only), deleted_at IS NULL on both users and quiz_sessions lookups, FOR UPDATE lock. Technically compliant with docs/security.md SECURITY DEFINER rules.
- Idempotency path (v_ended_at IS NOT NULL short-circuit) returns a hardcoded 0/false payload rather than re-reading the stored score from the DB. If batch_submit_quiz has already written a real score and the caller then invokes complete_empty_exam_session, the client receives 0%/false while the DB holds a different value. This is Vector AQ (MEDIUM gap). The UI would show wrong results for a legitimately-completed exam that then expired client-side.
- Audit event insert uses a subquery (SELECT role FROM users WHERE id = v_student_id) for actor_role rather than caching the role in a DECLARE variable at the start. This creates a non-atomic read that is outside the FOR UPDATE lock. Risk is theoretical (sub-millisecond window, role change requires admin action). Pattern is inconsistent with other RPCs that cache all user fields in DECLARE. Noted as Vector AS (LOW, no spec required).
- Server Action submitEmptyExamSession: Zod validation present (z.uuid()), getUser() auth gate present, error strings sanitized (never returns raw RPC message), three distinct error-code branches mapped. Compliant with code-style.md § TypeScript rules.
- No existing red-team spec exercises complete_empty_exam_session. The commit opens four new gaps: AN (unauthenticated call), AO (IDOR session hijack), AP (mode confusion — call on study session), AQ (cross-RPC idempotency abuse). AR (soft-deleted session call) is LOW risk.
- Specs to create: rpc-complete-empty-exam.spec.ts covering AN/AO/AP/AQ. server-action-unauthenticated.spec.ts needs a new test case for submitEmptyExamSession unauthenticated path (AN at the action layer).
- session-replay.spec.ts does NOT need to be re-run for this commit — it exercises batch_submit_quiz only, which is unchanged.
- audit-event-forgery.spec.ts does NOT need to be re-run — exam.expired is written by the SECURITY DEFINER RPC via the existing append-only channel, not by student direct INSERT.

2026-04-27 — review of commit 6aac0f4 (Layer 1 app-layer wiring: getActiveExamSession + ExpiredExamNotice + startedAt handoff):

- getActiveExamSession: no new attack surface at the Server Action layer. Auth gate (getUser()) is present. Zero parameters — no Zod parsing needed. Client-side isExamOverdue() computation is cosmetic only: the auto-complete decision fires server-side inside the action loop, calling complete_overdue_exam_session RPC. The RPC's own overdue invariant (now() > started_at + interval) is the authoritative gate; a student cannot accelerate expiry by manipulating their clock because the check is in the DB.
- complete_overdue_exam_session called inline per-overdue-session: the N round-trips concern (prompt item 4) is not a new attack surface. At most 1 active exam session per student is possible at any given time (start_exam_session IF EXISTS guard, Vector AL). Multiple overdue sessions would require the student to have started exams in the past and never submitted — a legitimate state that drains naturally. No DoS surface.
- expiredSessionIds bucket: server returns only session IDs belonging to the authenticated student (RLS students_select_sessions enforces student_id = auth.uid()). A student cannot inject a foreign session_id into the response by API manipulation — the query is server-side with .eq('student_id', user.id). No new IDOR surface.
- startedAt cold-start handoff: server returns started_at from the RPC response (Zod-validated z.string() in StartExamRpcResultSchema). Client writes it to sessionStorage and reads it back for the countdown display. The server-side deadline enforcement (batch_submit_quiz time-limit guard, complete_overdue_exam_session overdue invariant) uses the DB-stored started_at, not the client-supplied value. A student who edits sessionStorage only corrupts their own timer display — the DB value is authoritative. No new attack surface. Cross-reference: Vector BD (started_at mutation via quiz_sessions UPDATE) remains the higher-severity path and is unchanged.
- ExpiredExamNotice → /app/quiz/report?session=<id>: report page uses getQuizReportSummary which uses anon Supabase client (RLS-scoped), queries quiz_sessions with .eq('student_id', user.id) AND .is('deleted_at', null), and then checks ended_at IS NOT NULL. All three gates must pass. Student-A cannot view Student-B's report via URL construction because the .eq('student_id', user.id) filter silently returns null → redirect('/app/quiz'). No new IDOR surface. Vector M (get_report_correct_options cross-tenant) and Vector N (report on active session) remain open but are pre-existing gaps not introduced by this commit.
- pass_mark Zod tightened to int().min(1).max(100): strictly narrower than the previous .number().min(0).max(100). Aligns with DB CHECK constraint. No new attack surface; closes a theoretical split-brain between server validation and client validation.
- No new tables, RPCs, or auth flows introduced. No new red-team vectors from this commit.

2026-04-27 — review of commit 5cbbfe8 (Layer 1 overdue enforcement: complete_overdue_exam_session + start_exam_session REPLACE):

- New SECURITY DEFINER RPC complete_overdue_exam_session is fully pattern-compliant: auth.uid() IS NULL check, SET search_path = public, org-scope via users.organization_id, ownership + soft-delete via FOR UPDATE query (qs.student_id = v_student_id AND qs.organization_id = v_org_id AND qs.deleted_at IS NULL), mode guard (mock_exam only), overdue invariant (raises 'session is not overdue' if deadline not yet passed), and audit subquery correctly applies deleted_at IS NULL per security.md rule 10 / #550. All guards present.
- Idempotency path FIXED vs Vector AQ: unlike complete_empty_exam_session (which returned hardcoded 0/false), this RPC's idempotent branch re-reads v_correct_count, v_score, v_passed, and v_answered from quiz_sessions + quiz_session_answers via a second SELECT before returning. Returns the real stored score. AQ-class defect does NOT exist here. Positive finding.
- Audit subquery deleted_at filter: (SELECT role FROM users WHERE id = v_student_id AND deleted_at IS NULL) — compliant with security.md rule 10 / #550 pattern. Explicitly noted in commit message. No gap.
- start_exam_session REPLACE: adds started_at to RETURN jsonb (server-authoritative deadline base for clients) and pre-INSERT auto-complete via PERFORM complete_overdue_exam_session(v_old_session_id). The PERFORM keyword discards the return value — old score does NOT bleed into the new session's response. New session returns only its own fields. Correct.
- start_exam_session audit subquery: (SELECT role FROM users WHERE id = v_student_id AND deleted_at IS NULL) — same pattern as complete_overdue; compliant.
- No spec exercises complete_overdue_exam_session. New gaps AX (IDOR), AY (non-overdue invariant), AZ (mode confusion), BB (soft-deleted session) recorded. BA assessed LOW-IMPROVED (idempotency correct). BC (start+complete race) and BD (started_at mutation cross-reference to AM) documented.
- Specs to re-run due to start_exam_session replacement: rpc-cross-tenant.spec.ts (Vector AH — cross-org start_exam_session) and server-action-unauthenticated.spec.ts (Vector AG — unauthenticated start_exam_session). The RPC's auth/org guards are unchanged but the function body is replaced — confirm no regression.
- Recommended new spec file: rpc-complete-overdue-exam.spec.ts covering AX/AY/AZ/BB (and optionally BC for concurrency pin).
- session-replay.spec.ts: batch_submit_quiz is NOT changed in this commit. No re-run required.
- audit-event-forgery.spec.ts: 'exam.expired' event written by SECURITY DEFINER via append-only channel. No direct INSERT surface. No re-run required.

2026-04-28 — review of commit b4e5b7e (fix complete_empty_exam_session audit subquery soft-delete):

- This commit introduces migrations 051 / 20260428000001 which replace complete_empty_exam_session via CREATE OR REPLACE. The only change is `AND deleted_at IS NULL` added to the actor_role subquery inside the INSERT INTO audit_events block. All other SQL is byte-identical to migration 049 / 20260427000002.
- The fix closes the pattern identified in security.md §10 (audit-row subqueries are independent SELECTs, outer guards do not satisfy the rule). Vector AS (LOW, non-atomic role read) is updated to reflect the soft-delete gap is now closed. The remaining residual in AS (non-atomic window for role changes) is unchanged and remains no-spec-required.
- No new tables, RPCs, RLS policies, or auth flows are introduced. The attack surface of complete_empty_exam_session is unchanged from the 375bde5 baseline — only the audit subquery gains a predicate.
- SPECS TO RE-RUN: none. The function's observable behaviour on all red-team test paths is unchanged — the subquery change affects only the actor_role value written into audit_events when a student is soft-deleted between the outer users lookup and the audit INSERT. The existing audit-event-forgery.spec.ts tests do not exercise RPCs; they test direct INSERT/UPDATE/DELETE access. No spec currently exercises the complete_empty_exam_session write path, so no spec needs re-running because of this commit. The pre-existing gaps AN, AO, AP, AQ, AR (all logged against commit 375bde5) are still open and unchanged.
- NEW VECTORS: none. This commit is a pure security hardening fix within an existing function. The vector table requires no new rows.
- COVERAGE NOTE: the tightrope scenario asked about (student soft-deleted after auth.uid() fires but before the audit INSERT) is now closed at the DB layer. A regression spec for this specific timing window is theoretically desirable but practically unwritable in Playwright without the ability to pause the PL/pgSQL function mid-execution. The outer guard (users lookup at function top) still prevents such a student from reaching the audit INSERT path in practice. No regression spec is warranted or possible.
- FALSE POSITIVE CHECK: no false positives identified. The security-auditor correctly caught the §10 violation. The fix is the minimal correct change.

2026-04-28 — review of commits 082830d–643732c (grace alignment mig 052/053, audit branching, orphan discard, rejected-promise catch):

- Migrations 052 / 053 are CREATE OR REPLACE on complete_overdue_exam_session and complete_empty_exam_session respectively. Both follow the established SECURITY DEFINER pattern (auth.uid() IS NULL check, SET search_path = public, org-scope, ownership + soft-delete in FOR UPDATE, mode guard).
- Grace alignment (mig 052): complete_overdue_exam_session and start_exam_session now use `started_at + (time_limit + 30) seconds` matching batch_submit_quiz. The change narrows the window in which Layer 1 could fire before batch_submit_quiz would reject, eliminating a divergent-threshold attack path. Existing AY (non-overdue invariant call) vector description remains correct but the specific timing boundary in any future AY spec fixture must account for the 30s grace — test setup that uses `time_limit_seconds = 60` and advances clock to `+61s` will now hit the grace window and NOT be considered overdue; the fixture needs `+91s` minimum to clear the grace. No existing red-team spec exercises complete_overdue_exam_session, so no spec fixture is currently wrong.
- Audit branching (mig 053): complete_empty_exam_session now conditionally writes either `exam.expired` (above +30s grace) or `exam.completed` (within grace) as event_type. The branching uses `v_started_at + ((v_time_limit + 30) || ' seconds')::interval` — same predicate as mig 052. This is not a label-spoofing risk: the event_type is computed server-side inside the SECURITY DEFINER function from DB-stored fields only; no client input influences the branch. The audit_events immutability policies (no UPDATE, no DELETE) are unchanged. No new vector introduced.
- Orphan discard in use-exam-start (use-exam-start.ts): when sessionStorage.setItem fails after start_exam_session succeeds, the hook now calls discardQuiz({ sessionId: result.sessionId }). The discard action gates on .eq('student_id', user.id) where user.id comes from the server-side auth session, NOT from the client. A stale or forged result.sessionId would not match the authenticated user's student_id column, producing a zero-row no-op (RLS-consistent). No IDOR escalation. The cleanup call is within the same auth context as the start call. No new vector.
- Rejected-promise catch in quiz-submit.ts: the try/catch wrapper around submitEmptyExamSession is a defensive client-side pattern. It synthesises a failure result on RSC/network errors so the subsequent `if (!result.success)` discard path runs. The catch path calls discardQuiz (same zero-row no-op protection as above). No new server-side attack surface.
- VECTOR TABLE CHANGES: AY description requires a fixture note (see above). No new rows needed. No existing vector is invalidated.
- SPECS TO RE-RUN: none strictly required. No existing red-team spec calls complete_overdue_exam_session, complete_empty_exam_session, or exercises use-exam-start / quiz-submit at the server layer. The batch_submit_quiz grace window is pre-existing (mig 047) and session-replay.spec.ts / session-race-condition.spec.ts call batch_submit with well-within-deadline fixtures — unaffected. The pre-existing gaps AX/AY/AZ/BB (complete_overdue) and AN/AO/AP/AQ/AR (complete_empty) remain open and are UNCHANGED in severity.
- FIXTURE WARNING (for future spec AY): any spec that exercises the 'session is not overdue' guard in complete_overdue_exam_session must advance the clock past `started_at + time_limit + 30s`, not just `started_at + time_limit`. The old threshold (no grace) no longer exists. Fixture using `time_limit_seconds = 60` needs the mock clock at `≥+91s` (not +61s) to trigger the overdue branch.

2026-04-30 — review of commit 33ebcc6 (security-definer-softdelete-sweep — #588/#573/#531):

- #588 (enforce_draft_limit trigger): adds SET search_path = public. This is a trigger, not a SECURITY DEFINER function, but the advisor fix is correct hygiene. No red-team spec exercises the draft_limit trigger path and no spec asserts the absence of SET search_path. No existing spec affected. No new attack surface — the trigger prevents students from exceeding their draft quota; the search_path fix closes a theoretical schema-injection vector. No spec is warranted for a trigger-level advisor fix.
- #573 (start_quiz_session audit actor_role subquery): adds AND deleted_at IS NULL to the (SELECT role FROM users ...) subquery inside INSERT INTO audit_events. Pattern compliant with security.md §10. No red-team spec reads actor_role values from audit_events — audit-event-forgery.spec.ts tests INSERT/UPDATE/DELETE forgery only. The fix does not change the RPC's observable return value or any error path. No existing spec assertion is invalidated. Vector BF recorded as FIXED.
- #531 (batch_submit_quiz idempotent replay JOIN): adds AND q.deleted_at IS NULL to the join that fetches questions for the idempotent (already-completed) replay path. The bulk-fetch temp table SELECT is intentionally untouched (security.md §15 write-once carve-out, matching prior review of commits 8a36a92/791528c). session-replay.spec.ts (Vector J) calls batch_submit_quiz on a completed session — this is exactly the idempotent replay path now tightened. The spec does not assert a specific score value or question content from the replay; it asserts the session status/score do not change. The added filter cannot cause the test to fail (it only prevents scoring of soft-deleted questions, which the test fixture does not include). Vector BG recorded as FIXED.
- SPEC RE-RUN ASSESSMENT: session-replay.spec.ts should be re-run as a regression gate on the batch_submit_quiz change (#531). It directly exercises the modified replay path. Probability of failure is low (no soft-deleted questions in the fixture) but the path is live code and re-verification is cheap.
- NO FALSE POSITIVES: none. All three fixes are in-scope hardening with no spec assertions asserting the absence of these filters.
- NEW GAPS: none introduced by these fixes. Pre-existing gaps AM (config column immutability), AI (time bypass), AX–AZ (complete_overdue), AN–AQ (complete_empty) are unchanged.

2026-05-01 — review of commit 3407658 (active-user gate + draft-limit advisory lock — follow-up to 33ebcc6):

- Migration 076 / 20260430000010 (active_user_gate_quiz_rpcs): refactors start_quiz_session to cache actor_role at function entry via SELECT INTO v_actor_role FROM users WHERE id = auth.uid() AND deleted_at IS NULL, raising IF NOT FOUND 'user not found or inactive' after the auth.uid() IS NULL check. Eliminates three redundant scattered subqueries on users for actor_role / org resolution. Lower scattered deleted_at filters preserved as defense in depth. **NOTE (commit 8da4d64):** Contains start_quiz_session only (~64 lines). batch_submit_quiz moved to new migration 078.
- Migration 078 / 20260430000012 (active_user_gate_batch_submit): refactors batch_submit_quiz to cache actor_role at function entry via SELECT INTO v_actor_role, raising IF NOT FOUND 'user not found or inactive' before any session work. Reuses v_actor_role in both timeout and completion audit INSERTs, closing the TOCTOU window where a soft-delete between gate and audit write would null the scalar subquery and abort the transaction (PR #599 CR root-cause fix). Introduces local variables v_expired_event and v_completed_event for cleaner event-type branching (mock_exam vs internal_exam paths). Split from original mig 076 because combined migration was 365 lines, exceeding the 300-line cap; mig 078 is 307 lines (7 over unavoidable for a single CREATE OR REPLACE).
- batch_submit_quiz CRITICAL closure: the idempotent replay branch (v_ended_at IS NOT NULL short-circuit) previously executed *before* any users JOIN, so a student who was soft-deleted after completing a quiz could re-fetch their stored report indefinitely with a valid JWT. The 33ebcc6 fix closed actor_role spoofing (BF) but did NOT close this replay-fast-path read. Mig 078 places the active-user gate and cached v_actor_role before the FOR UPDATE on quiz_sessions, so the replay branch now also rejects soft-deleted callers. New Vector BH recorded as FIXED.
- Migration 077 / 20260430000011 (enforce_draft_limit_advisory_lock): adds pg_advisory_xact_lock(hashtext(NEW.student_id::text)) before the COUNT(*) read inside the trigger. The lock is per-student, transaction-scoped, and prevents two concurrent INSERTs from both observing count=19 and both proceeding. Closes the read-then-write race CodeRabbit flagged. New Vector BI recorded as FIXED.
- Refactor TOCTOU analysis: the start_quiz_session locals capture (v_org_id, v_role) at function top is intentional and safe. The values are used inside the same transaction for both the session INSERT and the audit_events INSERT. A student soft-deleted between the SELECT INTO and the audit INSERT would still get an audit row written with the captured pre-deletion role — but they would have already passed the IF NOT FOUND gate, so the row is consistent with the function entry state. Window is sub-millisecond and requires admin action mid-call. New Vector BJ documented as LOW, no spec required. Mechanically identical to the residual non-atomic role read in Vector AS.
- No new tables, RLS policies, or auth flows. The three migrations (076, 077, 078) are pure SECURITY DEFINER hardening.
- SPEC RE-RUN ASSESSMENT: session-replay.spec.ts should be re-run as a regression gate on the batch_submit_quiz replay path (mig 078 adds the gate ordering before the FOR UPDATE). The existing fixture exercises an active student so the new gate is a no-op for the spec — failure probability is low, but the path is live code. quiz-draft-injection.spec.ts does NOT exercise concurrency so the advisory lock cannot affect existing assertions; re-run is optional. No existing assertion is invalidated.
- NEW SPEC RECOMMENDATIONS (file as issues, do NOT block this commit):
  1. session-replay extension: sign in as student-A, complete a quiz, soft-delete student-A via admin client, then re-call batch_submit_quiz with the completed sessionId and a valid (still-unexpired) JWT. Expected: 'user not found or inactive' error, NOT a replayed report. Closes Vector BH at the spec layer.
  2. quiz-draft-injection extension: open two parallel transactions for the same student, both insert draft #20 simultaneously, expect exactly one to succeed (advisory lock serialises). Closes Vector BI at the spec layer.
  3. server-action-unauthenticated extension: add a soft-deleted-but-authenticated test case that calls start_quiz_session and batch_submit_quiz, expecting the new top-level error string. Distinguishes the explicit gate from the old NULL-constraint-violation path.
- NO FALSE POSITIVES. The three CodeRabbit comments (active-user gate, replay-branch user-check, draft-limit race) are all real and the fixes are minimal and correct.
- LOCAL RE-RUN GUIDANCE: re-run `pnpm --filter @repo/web e2e:redteam` locally before push only if the user wants pre-push confidence on session-replay. The redteam.yml CI workflow already triggers on this push (security-paths trigger matches supabase/migrations/**), so CI coverage is automatic. Local re-run is a fast (~30s) sanity check, not strictly required for this commit.

2026-05-01 — review of commit 8da4d64 (cache actor_role in batch_submit_quiz + split mig 076):

- Migration 078 / 20260430000012 (active_user_gate_batch_submit): batch_submit_quiz is replaced via CREATE OR REPLACE. The only functional change vs the prior version in mig 076 (commit 3407658) is that the active-user gate changes from `IF NOT EXISTS (SELECT 1 FROM users ...)` to `SELECT role INTO v_actor_role FROM users WHERE id = v_student_id AND deleted_at IS NULL; IF NOT FOUND THEN RAISE`. The cached `v_actor_role` is then used in both audit INSERT sites: the timeout branch (exam.expired / internal_exam.expired) and the completion branch (exam.completed / internal_exam.completed / quiz_session.batch_submitted).
- TOCTOU TX-ABORT CLOSED: the prior implementation (commit 3407658) used `IF NOT EXISTS` as the gate but then re-read role via a scalar subquery `(SELECT role FROM users WHERE id = v_student_id AND deleted_at IS NULL)` inline at each audit INSERT. If the user was soft-deleted between the IF NOT EXISTS check and the audit INSERT, the scalar subquery returned NULL and violated audit_events.actor_role NOT NULL, aborting the entire transaction and losing all answer writes. Mig 078 eliminates this window. New vector BK documents the now-closed gap at the code layer; the spec-layer gap (no red-team test exercises this timing window) remains.
- Migration 076 is split: prior mig 076 (365 lines, over 300-line cap) held both start_quiz_session and batch_submit_quiz. After the split, mig 076 holds only start_quiz_session (~64 lines), new mig 078 holds only batch_submit_quiz (~307 lines). The modification to mig 076 (removing batch_submit_quiz from it) is safe because the PR branch is unmerged and the migration has only been applied to ephemeral CI/dev DBs reset on each run. The SQL executed for start_quiz_session is byte-identical to the prior version.
- No new tables, RLS policies, or auth flows introduced. No new attack surface.
- SPEC RE-RUN ASSESSMENT: session-replay.spec.ts must be re-run. It directly exercises batch_submit_quiz on both the active path and the idempotent replay path. The new `SELECT INTO` at the top of the function is now the first DB read (before the FOR UPDATE on quiz_sessions), so the path ordering changed — the spec's fixture (active student, non-soft-deleted) is unaffected but the live code path changed. Failure probability is low; re-run is required for regression confidence. No existing assertion is invalidated.
- NEW SPEC RECOMMENDATIONS (do NOT block push; file as issues):
  1. session-replay.spec.ts extension (BH coverage, recommended in 3407658 review, still outstanding): sign in as student-A, complete a quiz (all answers submitted), soft-delete student-A via admin client, then re-call batch_submit_quiz with the completed sessionId. Expect 'user not found or inactive' error from the new top-level gate — NOT a replayed report. Confirms BH remains FIXED after the mig 078 refactor.
  2. session-replay.spec.ts extension (BK inner-TOCTOU confirmation): call batch_submit_quiz on an active session, assert the call succeeds regardless of whether a concurrent admin soft-deletes the user after the gate fires. This is a sub-millisecond window; the practical test is to call batch_submit_quiz on an active session as a freshly-soft-deleted user and assert the call raises 'user not found or inactive' (the gate fires before the FOR UPDATE, so the call is rejected cleanly rather than aborting mid-transaction). This is the same shape as recommendation 1 above but for an active (not replay) session.
- PACKAGES/DB AND SUPABASE MIRRORS: diff of packages/db/migrations/078_active_user_gate_batch_submit.sql vs supabase/migrations/20260430000012_active_user_gate_batch_submit.sql confirmed byte-identical. Same for mig 076. No mirror drift.
- FALSE POSITIVES: none. The CodeRabbit root-cause findings that prompted this commit (inline scalar subquery for actor_role creates NULL-abort risk on mid-call soft-delete) are all correct and the fix is the minimal correct change.

2026-05-07 — review of commit c45330f (OWASP A02/A05/A09 coverage — 52 new specs, closes #108):

- Four new spec files added: injection-sql.spec.ts, injection-xss.spec.ts, header-validation.spec.ts, audit-completeness.spec.ts, plus shared helpers/payloads.ts.
- injection-sql.spec.ts covers three parameterised-text RPC inputs: start_internal_exam_session.p_code (DB_LAYER + TRANSPORT_LAYER), void_internal_exam_code.p_reason (DB_LAYER + TRANSPORT_LAYER + WHITESPACE), submit_quiz_answer.p_selected_option (DB_LAYER + TRANSPORT_LAYER). All three RPCs use parameterised queries and no SQL string interpolation is present in the SECURITY DEFINER bodies. The payloads confirm that. Vectors BO, BP, BQ closed.
- injection-xss.spec.ts covers three user-rendered fields where admin-authored content reaches student views: student full_name in the admin list, question_text in the quiz, explanation_text in the feedback panel. React's JSX escaping and react-markdown + remark-gfm sanitization are both exercised. DOM canary assertions confirm no payload execution in any path. Vector BR closed.
- header-validation.spec.ts exercises next.config.ts securityHeaders on three paths (/, /app/dashboard, /auth/login). CSP assertions are structural (default-src 'self', frame-ancestors 'none', script-src 'self', supabase.co in connect-src) to avoid exact-match brittleness across envs. HSTS asserts max-age >= 15552000 (6 months). Vector BS closed.
- audit-completeness.spec.ts covers the 8 audit event_type literals that had zero positive spec coverage: quiz_session.batch_submitted, exam.started, exam.completed, exam.expired, internal_exam.code_issued, internal_exam.code_voided, internal_exam.started, internal_exam.expired. Notably internal_exam.completed is NOT among the 8 — confirmed present in migrations but commit message's "8 currently-untested" list does not include it; see gap note below. Vector BT closed for the 8 listed literals.
- REMAINING GAP — internal_exam.completed: migration 20260429000007_extend_batch_submit_for_internal_exam.sql writes 'internal_exam.completed' when batch_submit_quiz runs on an internal_exam mode session within the time limit. audit-completeness.spec.ts does not include a test case for this literal. The 8 cases in the spec are: batch_submitted, exam.started, exam.completed, exam.expired, internal_exam.code_issued, internal_exam.code_voided, internal_exam.started, internal_exam.expired. 'internal_exam.completed' is the 9th in the codebase but was not included. This is a coverage gap at the audit-completeness layer; it should be added to the spec or tracked as a new issue. No vector row added for this yet — filing recommendation below.
- REMAINING GAPS — deferred per plan:
  - BU: start_quiz_session.p_mode injection — FIXED 2026-05-08 (migration 081 / commit 5b4223b, closes #629). Hard whitelist `p_mode IN ('smart_review','quick_quiz')` in `start_quiz_session`; `mock_exam` and `internal_exam` raise `mode_not_allowed`. Spec: `start-quiz-session-mode-confusion.spec.ts` (2 attacks + positive no-insert assertion). Integration: `rpc-start-session.integration.test.ts` (commit fc5d47e, 2 cases against local Postgres).
  - BV: upsert_exam_config.p_distributions JSONB injection — no issue yet. Admin-only surface, lower priority.
- SUPPLY CHAIN (OWASP A03:2025): no spec exists. Issue #109 tracks this. Outside scope of this commit.
- EXCEPTIONAL CONDITIONS (OWASP A10:2025): COVERED (commit 9a728afd, #634) — see Vector CJ. error-handling-disclosure.spec.ts probes 5 SECURITY DEFINER RPCs. Two Server Action raw-leak fixes landed in same commit.
- NEGATIVE AUDIT COVERAGE (no spurious writes): intentionally skipped per plan. No spec verifies that events are NOT written in error paths. Metadata-shape coupling tracked in issue #570.
- STUDENT LOGIN event (record_login): not covered by audit-completeness.spec.ts. Commit message notes 'record_login audit event (event_type literal not yet confirmed in code; out of scope)'. Confirmed by codebase grep: 'student.login' appears in migrations but the Server Action path that triggers it was not instrumented in the new spec. This is a pre-existing gap, not introduced by this commit. Should be added to the spec backlog.
- FALSE POSITIVES: none. The four new spec files test surfaces that exist in the production codebase. Fixtures confirmed against live schema. No speculative features tested.

2026-05-07 — review of commit 61883bd (void_internal_exam_code blank-reason tightening):
- This is a tightening-only migration. No new attack surface, no RLS change, no auth change, no new RPC. The function body is replaced byte-for-byte except for the single guard clause.
- The production bug (`btrim()` only trimming spaces, not tabs/newlines/CR/FF/VT) was found by the WHITESPACE_PAYLOADS loop in injection-sql.spec.ts — confirming that the payload table, even when incomplete, provides meaningful regression value.
- WHITESPACE_PAYLOADS lesson: when a whitespace guard is changed from a specific-trim function to a broader character class (`[[:space:]]`), the payload table must be audited against all members of the new class. The table only grew by the one payload that found the bug; it did not grow to cover all [[:space:]] members. The advisory recommendation is to add \n, \r, and a mixed payload before this spec runs in CI as the sole regression gate.
- The stale `why` comment on the `spaces-only` payload (`btrim() empty → invalid_reason guard fires`) must be updated when entries are added — the guard is now POSIX regex, not btrim().
- Pattern: when a new whitespace/encoding guard is written in PL/pgSQL, the payloads.ts WHITESPACE_PAYLOADS entry should enumerate all members of the Postgres character class used, not just the payload that triggered the fix.

2026-05-07 — cumulative review of branch feat/redteam-owasp-coverage-108 (18 commits, 23 files, issue #108 OWASP coverage expansion):

- Vectors BO/BP/BQ/BR/BS are FULLY COVERED and confirmed by the 115-pass/1-skip local run. No regression in existing specs.
- BP (void_internal_exam_code whitespace injection): the production bug (`btrim()` missed tabs/newlines/CR/FF/VT) was found by the WHITESPACE_PAYLOADS loop in injection-sql.spec.ts and fixed in migration 20260507000001. The fix switches the guard to POSIX `^[[:space:]]*$`. All 5 WHITESPACE_PAYLOADS (spaces-only, tabs-only, newlines-only, cr-only, mixed-whitespace) now pass. ERROR_MESSAGES map in void-code.ts updated with `invalid_reason` entry.
- BS (security headers): X-Frame-Options tightened to DENY (from SAMEORIGIN) in next.config.ts and proxy.ts. header-validation.spec.ts assertions updated to DENY. Redirect responses from Edge Middleware were previously missing all security headers — proxy.ts now injects static headers and a minimal CSP (`default-src 'none'; frame-ancestors 'none'`) on every redirect response. The spec covers this by asserting on the raw 3xx response using `maxRedirects: 0`.
- BT (audit completeness): commit c45330f initially claimed 8 literals but the audit-completeness spec in its final form covers 10: the initial 8 plus internal_exam.completed (triggered by batch_submit_quiz on an internal_exam mode session within the time limit) and student.login (record_login RPC). The rate-limit window for student.login is handled by a 60s lookback window in the test, making it idempotent across rapid re-runs.
- BK (rpc-start-internal-exam-session.spec.ts): probe scoped to testStart to avoid false positives from sibling test sessions. The spec handles the `insufficient_questions_for_exam` edge case via test.skip so the fixture limitation does not mask the actual security assertion.
- Infrastructure pattern solidified: when a spec uses a per-test code/session resource, `afterEach` must chain `.is('deleted_at', null)` and log only when `data?.length > 0` (zero-row no-op observability). The `activeCodeId`/`activeSessionId` pattern in injection-sql.spec.ts (reset-before-cleanup to prevent stale state after cleanup failure) is the reference implementation for future specs.
- Remaining confirmed gaps: BU (issue #629, p_mode injection — FIXED mig 081), BV (issue #633, JSONB injection — COVERED #747), A10:2025 (issue #634 — COVERED commit 9a728afd, Vector CJ). No new vectors introduced by this branch.

2026-05-26 — review of branch fix/540-student-mastery-stats-rpc (migration 20260521000005 — new SECURITY INVOKER RPC get_student_mastery_stats()):
- Three new vectors added: BW1 (unauthenticated call, GAP), BW2 (cross-student count leak, GAP), BW3 (instructor cross-student count via instructors_read_students policy, GAP). All three are gaps with no existing spec coverage.
- Existing specs that should be re-run: rpc-cross-tenant.spec.ts (tests student_responses direct SELECT cross-org isolation — orthogonal but the underlying table is newly read by the RPC), server-action-unauthenticated.spec.ts (does not cover get_student_mastery_stats or get_question_counts; re-run confirms existing coverage is intact but the new RPC is absent).
- get_question_counts (migration 20260520000001, master) has the same three gap classes — file companion issues alongside BW1/BW2/BW3 for that RPC too.
- Answer-data exposure confirmed absent: RPC returns only (subject_id uuid, topic_id uuid, total bigint, correct bigint). No question text, option text, explanation, or correct-answer boolean in the return schema. Security rule §1 holds.
- RLS chain confirmed sound: questions.tenant_isolation = org-scoped + deleted_at IS NULL; student_responses.students_read_responses = student_id = auth.uid(). Both policies have USING; SECURITY INVOKER executes as the authenticated caller so both policies activate. An unauthenticated caller resolves auth.uid() to NULL, both policies match zero rows, function returns empty set.
- Instructor cross-student risk (BW3): instructors_read_students policy (SELECT on student_responses WHERE organization_id = caller's org AND role IN ('instructor','admin')) is real — an instructor calling get_student_mastery_stats() via supabase-js would receive aggregated counts across ALL students in their org, not just their own responses. The RPC comment states "student_responses RLS = student_id = auth.uid()" but this ignores the instructor policy. The function is intended for students only; no role guard prevents an instructor from calling it and seeing org-wide aggregated counts. MEDIUM priority (counts, not answer data) but needs a spec.
- No RLS policy changes in this branch — the attack surface for existing vectors is unchanged.
- False positives: none. The migration comment's claim that "an unauthenticated caller resolves auth.uid() to NULL, both policies match zero rows" is correct for the student policy. The instructor policy adds a second SELECT path that does not apply to unauthenticated callers (auth.uid() IS NULL fails the subquery). Unauthenticated-empty-set claim holds.

2026-05-08 — review of commits 5b4223b / d32868b / fc5d47e (start_quiz_session p_mode whitelist, closes #629):
- Vector BU: FIXED. Migration 081 raises `mode_not_allowed` for any p_mode outside `('smart_review','quick_quiz')`. Whitelist position is after the auth check but before the active-user gate — prevents probing 'user inactive' via timing/error differences. Spec asserts both `mock_exam` and `internal_exam` rejection AND positive no-insert assertion (admin count=0 scoped to attacker_id + post-test created_at).
- Adjacent vectors unaffected: `start_internal_exam_session` is parameterless on mode (mode hardcoded to 'internal_exam' in INSERT, mig 20260430000005); no mode-confusion surface there. Audit event omission on `mode_not_allowed` rejection is intentional and consistent with how 'Not authenticated' and 'user not found or inactive' rejections also do not emit audit rows.
- Sequencing impact on remaining Group A issues: AZ (#561) and AP (#557) should expect `mode_not_allowed` as the error string for consistency with this fix. AI (#546, batch_submit time bypass) priority slightly elevated because the fake-mock_exam-via-start_quiz_session setup path is now closed; the legitimate `start_exam_session` path is still in scope.
- Remaining confirmed gaps after this commit: BV (issue #633). A10:2025 (#634) COVERED in commit 9a728afd — see Vector CJ. No new gaps introduced.

2026-06-03 — review of commit f0750992 (cache admin role in void_internal_exam_code — packages/db mig 084 + supabase mirror 20260601000003):

- Both migration files are a pure in-function hardening change: no RLS change, no new table, no new RPC, no auth flow change. The function signature is unchanged.
- Change: `SELECT u.organization_id INTO v_admin_org` extended to `SELECT u.organization_id, u.role INTO v_admin_org, v_admin_role`; the two inline scalar subqueries `(SELECT u.role FROM public.users u WHERE u.id = v_admin_id AND u.deleted_at IS NULL)` in the two `INSERT INTO audit_events` blocks are replaced by the cached `v_admin_role`.
- Vectors directly affected: **BN** (start_internal_exam_session rate-limiting fixture), **BO** (start_internal_exam_session p_code injection), **BP** (void_internal_exam_code p_reason injection), **CD** (void consumed code with soft-deleted session). All four map to `rpc-void-internal-exam-code.spec.ts` or `injection-sql.spec.ts`. None of these spec assertions depend on actor_role in audit_events or on the mid-call admin-soft-delete timing window — the specs call the RPC as a live admin, not a concurrently-soft-deleted one. No existing assertion is invalidated.
- New potential vector CF assessed: **"admin soft-deleted between authz lookup and audit INSERT still produces a valid audited void"** — i.e., is the cached-role pattern exploitable? Answer: no new attack surface. The cached role is read with `deleted_at IS NULL` at authorization time (same gate as before). If the admin is soft-deleted *before* the authz SELECT, the RPC raises 'not an admin' and no void occurs. If the admin is soft-deleted *after* the authz SELECT (between SELECT and INSERT), the prior code aborted with a NOT NULL violation, rolling back the void. The new code succeeds: the void is committed with the cached role. This is the INTENDED fix — the rollback was the bug. Security posture: the void was already authorized and the audit row is accurate (role cached from the valid pre-deletion authz read). No exploitable path opened. Assessed LOW, no spec required.
- TOCTOU residual: same sub-millisecond admin-action window as Vector BJ (start_quiz_session) and AS (complete_empty). Pattern is now documented consistently across three RPCs (start_quiz_session mig 076, batch_submit mig 078, void_internal_exam_code mig 084/085). No new spec warranted.
- Security.md §10 compliance: the authz SELECT (`u.deleted_at IS NULL`) is preserved and is now the sole read for both actor_org and actor_role. Compliant — the cached v_admin_role originates from a deleted_at-filtered read.
- packages/db and supabase mirrors confirmed byte-identical (diff shows identical hunks for both files).
- SPEC RE-RUN REQUIRED: `pnpm --filter @repo/web e2e:redteam` — specifically `rpc-void-internal-exam-code.spec.ts` (Vectors BN/CD) and `injection-sql.spec.ts` (Vectors BO/BP). The void-code spec already passes 6/6 locally per commit context. The change is purely cosmetic at the spec execution layer (same runtime observable behaviour), but confirm no regression.
- NO NEW GAPS. Vector CF assessed and closed as LOW/no-spec.

2026-06-01 — review of branch fix/db-softdelete-failfast-518-638 (migrations 083+084, commits 655faa88+bf3a957e):

- Migration 083 (exam_config_distributions RLS parent soft-delete) — all three admin policies (SELECT/INSERT/DELETE) in mig 038 joined back to exam_configs checking org + is_admin() but missing ec.deleted_at IS NULL. Distributions of a soft-deleted parent config were reachable via direct PostgREST at the policy layer. App layer masked this (queries.ts filtered client-side). Fix: DROP + CREATE all three policies, adding ec.deleted_at IS NULL inside the EXISTS subquery. sibling-audit rule applied correctly — all three policies touched together because they shared the identical gap. upsert_exam_config is SECURITY DEFINER and bypasses RLS; seed.ts uses service-role admin client, also bypasses RLS. No existing red-team spec breaks from this tightening. New vector CE added as GAP (no E2E spec verifies the RLS behaviour at the direct-PostgREST layer).
- Migration 084 (void_internal_exam_code fail-fast on missing session) — the RPC's consumed-code branch had a SELECT ... FOR UPDATE on quiz_sessions followed by two IF FOUND AND ... branches. If the session was soft-deleted or cross-org (SELECT returns no rows), FOUND was false, both branches skipped, but the code was still voided at the bottom of the function — a silent void on a phantom session. Fix: IF NOT FOUND THEN RAISE session_state_changed immediately after the lock; IF FOUND guards on subsequent branches removed as redundant. Migration 084 also converges the packages/db mirror (previous mirror 072 predated the strict blank-reason check from 20260507000001). New vector CD added as COVERED.
- Security regression check: (a) mig 083 is purely additive (narrower policies, never wider); (b) mig 084 tightens the RPC guard — old behaviour was silent void, new behaviour is loud exception. No weakening in either migration. Both packages/db mirrors confirmed byte-identical to the supabase/migrations counterparts.
- RLS seeding path confirmed safe: ensureExamConfig in seed.ts uses admin (service-role) client which bypasses RLS for both INSERT into exam_configs and INSERT into exam_config_distributions. The tightened admin_insert/admin_delete RLS policies therefore have zero impact on any existing test setup path.
- Pattern: when a child table's RLS policies join to a parent table, always verify the parent's deleted_at is filtered in every policy variant (SELECT/INSERT/DELETE). The exam_config_distributions gap is the canonical example of a child-table RLS policy that knew about the parent FK but not the parent's soft-delete status.

2026-06-04 — review of commit 921d0c0 (new rpc-complete-empty-exam.spec.ts, closes #557 AO/AP/AQ/AR):

- Vectors AO (IDOR), AP (mode confusion), AQ (idempotent replay), AR (soft-deleted session) all flipped GAP→COVERED. Soft-deleted-user guard also covered by an untitled test case in the spec.
- AQ DESCRIPTION CORRECTION: the original gap note (commit 375bde5) stated the idempotency branch "returns a hardcoded 0/false payload." This was accurate for migration 20260427000002 (the original implementation) but was already FIXED in migration 20260428000003 which added a `SELECT qs.correct_count, qs.score_percentage, qs.passed, qs.answered_count INTO v_correct_count, v_score, v_passed, v_answered FROM quiz_sessions qs WHERE qs.id = p_session_id` before the idempotent RETURN. The matrix description was stale. The new spec explicitly exercises and pins this re-read behaviour (seeds 75/true, asserts RPC returns 75/true, confirms DB row untouched). Mirrors BA (complete_overdue idempotency also re-reads real values).
- CONFIRMED DEFERRAL (AN): the unauthenticated call path for complete_empty_exam_session is intentionally deferred to Hub A (server-action-unauthenticated.spec.ts). The spec file header comment states this explicitly. The RPC's auth.uid() IS NULL check raises 'user not found or inactive'. Tracked in AN row as a gap pending Hub A work — not a miss.
- FIXTURE DETAILS: uses `pickSubjectWithQuestions` (not a hardcoded subject UUID), seeds sessions directly via admin client, afterEach soft-deletes via `.in('id', Array.from(createdSessionIds)).is('deleted_at', null)` with try/finally — matches code-style.md §7 hermetic afterEach pattern. Early-submit positive test confirms complete_empty accepts not-yet-overdue sessions (unlike complete_overdue which requires the overdue threshold to have passed).
- internal_exam positive path verified: seeds mode='internal_exam' and confirms RPC completes the session (ended_at not null, passed=false). Mode guard in mig 20260429000008 accepts both mock_exam and internal_exam.
- AP error string: asserts /session is not an exam/i, matching the error string in mig 20260429000008. This is the same string AZ uses in rpc-complete-overdue-exam.spec.ts.

2026-06-04 — review of commit 64339b28 (new rpc-complete-overdue-exam.spec.ts, closes #559/#560/#561/#562):

- Vectors AX (IDOR), AY (non-overdue invariant), AZ (mode confusion), BB (soft-deleted session) all flipped GAP→COVERED. Concurrency pin BC also COVERED. Soft-deleted-user guard (user not found or inactive) additionally covered by an untitled test case in the spec.
- internal_exam positive path is verified alongside the mock_exam positive: the spec seeds a mode='internal_exam' overdue session and confirms the RPC accepts it and sets ended_at. This is the only positive test for the internal_exam branch in any red-team spec. The AZ mode-guard test uses mode='quick_quiz' (not 'smart_review') — either non-exam mode would work; quick_quiz is the natural inverse of the exam modes.
- FIXTURE CORRECTNESS: the spec uses time_limit_seconds=60, started_at=now()-120s (2 minutes ago) for overdue sessions. This clears the 30s grace (60+30=90s < 120s) as required by the fixture warning in the 2026-04-28 commit 082830d lesson. Spec has no clock-mocking dependency — real wall-clock used. Non-overdue fixture uses started_at=now()-5s, which is well within the grace window.
- HERMETIC AFTEREACH: soft-deletes created sessions via `.in('id', Array.from(createdSessionIds)).is('deleted_at', null)`, logs only when data?.length > 0, and wraps in try/finally to clear the set on throw. This matches the code-style.md §7 hermetic afterEach pattern.
- REMAINING GAP for complete_overdue_exam_session: the unauthenticated call path (analogous to AN for complete_empty_exam_session) is intentionally deferred to the Hub A serial work on server-action-unauthenticated.spec.ts. The RPC's auth.uid() IS NULL check raises 'user not found or inactive' before any session work — same defence as all other SECURITY DEFINER functions. This is not a spec miss; it is a confirmed deferral. Tracked as the one open sub-vector for this RPC.
- audit-completeness.spec.ts: exam.expired and internal_exam.expired are already COVERED by Vector BT. complete_overdue_exam_session emits these (or exam.completed / internal_exam.completed on the idempotent / in-grace path). The positive test in rpc-complete-overdue-exam.spec.ts completes with 0 answers, so the timeout-branch fires (exam.expired / internal_exam.expired). No new metadata-key assertion is needed — BT's expectCompletionMetadata covers the batch_submit emitter; the expired events were already covered. No BT2-class gap introduced for complete_overdue.
- The mock_exam mode guard message in mig 20260429000008 is 'session is not an exam' (not 'session is not a mock exam' as in prior mig 052). Spec correctly matches /session is not an exam/i, not /mock exam/. The error string change between mig 052 and mig 20260429000008 is correctly handled; no spec brittleness.

2026-06-01 — review of branch fix/exam-audit-metadata-keys-570-571 (commits e621f9ba + 9091ad60, migration 082 / 20260601000001):

- Migration 082 is a CREATE OR REPLACE of batch_submit_quiz that changes exactly 3 lines vs mig 078: the header comment, 'answered'→'answered_count', and 'correct'→'correct_count' in the completion-event metadata block. The auth.uid() IS NULL check, active-user gate (SELECT INTO v_actor_role), ownership/org-scope/soft-delete FOR UPDATE, RLS, time-limit enforcement, all answer-write loops, and the expired-event branch are byte-identical to mig 078. No auth, RLS, or ownership logic changed.
- The supabase mirror (20260601000001) is byte-identical to packages/db/migrations/082. No mirror drift.
- Attack surface change: zero. The rename is inside the metadata JSONB that batch_submit_quiz writes to the append-only audit_events table. metadata content is never used as a security gate; it is observability data only. No consumer of the old key names ('answered', 'correct') was identified in any red-team spec, Server Action, or RPC.
- Spec updated: audit-completeness.spec.ts adds `expectCompletionMetadata` helper (lines 198–235) asserting (a) 'answered_count' present, (b) 'correct_count' present, (c) 'answered' absent, (d) 'correct' absent — called after both exam.completed and internal_exam.completed positive-emission tests. This pins the metadata schema and will catch a future CREATE OR REPLACE that silently reverts the keys.
- No other red-team spec references the old or new metadata keys. The `correct_count` references in rpc-cross-tenant.spec.ts (line 214) and rpc-void-internal-exam-code.spec.ts (line 91) are the `quiz_sessions.correct_count` database column — unrelated to audit_events metadata.
- GAP remaining: the `complete_empty_exam_session` emitter of exam.completed (zero-answer in-grace finish) is NOT covered by expectCompletionMetadata. Correct disposition: issue #728 already filed and open. Not blocking.
- Pattern: when a SECURITY DEFINER function's audit INSERT metadata keys change, audit-completeness.spec.ts is the primary spec to update (positive key assertions + negative legacy-key assertions). session-replay / session-race-condition / audit-event-forgery do NOT need re-running — they do not assert audit_events metadata content.

2026-05-28 — review of commit 36340643 (migration 20260528000001 — new SECURITY INVOKER functions _filtered_question_pool, get_random_question_ids, get_filtered_question_counts):

- Three SECURITY INVOKER (not SECURITY DEFINER) functions added. This is the correct choice: SECURITY INVOKER executes as the calling user so RLS activates automatically. No auth.uid() IS NULL explicit guard is required — the question pool returns empty when auth.uid() is NULL because the tenant_isolation RLS WHERE clause evaluates NULL = anything as false.
- tenant_isolation RLS on questions handles both org scoping and deleted_at IS NULL and status='active' — these are duplicated in the function body as defense-in-depth, which is correct hygiene.
- Cross-student filter isolation is LOAD-BEARING at student_responses (security.md §3 (Multiple Permissive SELECT Policies) — two permissive SELECT policies: students_read_responses and instructors_read_students). The explicit WHERE sr.student_id = auth.uid() is required, not optional. The commit message correctly identifies this. An instructor calling the RPC would otherwise aggregate unseen/incorrect/flagged state across org-wide students, same as BW3 class. The explicit predicate closes this exactly.
- fsrs_cards and active_flagged_questions have single applicable policies each. The explicit student_id = auth.uid() scoping there is correctly noted as defense-in-depth only.
- Answer exposure confirmed absent: return set is (id uuid, topic_id uuid, subtopic_id uuid) from _filtered_question_pool, (id uuid) from get_random_question_ids, (topic_id uuid, subtopic_id uuid, n bigint) from get_filtered_question_counts. No correct_option_id, explanation, question_text, or options JSONB in any return shape. Security rule §1 holds.
- p_filters text[] parameter: values constrained by the SQL CASE/EXISTS branches — only 'unseen', 'incorrect', 'flagged' produce any behaviour; all other values are silently ignored (fall through the OR chain with no match). No SQL injection risk — parameters are used as string literals in = ANY(...) comparisons, not as SQL identifiers or string interpolation. No parameterised-text injection spec needed for these values.
- p_count int in get_random_question_ids: GREATEST(p_count, 0) prevents negative LIMIT. A negative value would error in PostgreSQL (LIMIT must not be negative) without the GREATEST guard. The guard is correct. A caller passing p_count = 0 receives an empty set, not an error. No integer overflow risk at PostgreSQL int range.
- Server Action layer (lookup.ts + quiz.ts): both getFilteredCount and getRandomQuestionIds retain Zod validation (FilteredCountSchema / explicit opts object with typed fields) and getUser() auth gate at the Server Action layer. The RPC calls are downstream of these gates, so direct RPC calls bypass the Zod layer — the RLS + SECURITY INVOKER model provides the DB-layer security without relying on Server Action validation.
- pnpm e2e:redteam: NOT required for this commit. No existing spec exercises _filtered_question_pool, get_random_question_ids, or get_filtered_question_counts. No existing spec assertions are invalidated — the removed client-side functions (filterUnseen, filterIncorrect, filterFlagged, buildQuestionQuery, groupCounts) had no red-team spec coverage, and the RPC replacements present the same attack surface class covered by the new CA/CB gap entries.
- New gaps recorded: CA (unauthenticated call — empty-set guarantee), CB (cross-org subject_id probe + cross-student filter isolation). CC (direct _filtered_question_pool call) assessed LOW RISK, no spec required.
- Pattern: SECURITY INVOKER functions that read user-scoped tables (student_responses, fsrs_cards, flagged) must always include explicit student_id = auth.uid() predicates on tables that have instructor/admin-wider policies — the BW3/CB class. This is the same rule as security.md §3 (Multiple Permissive SELECT Policies).

2026-05-27 — review of branch fix/668-admin-dashboard-students-rpc (migration 20260527000001 — new SECURITY DEFINER get_admin_dashboard_students, drops get_admin_student_stats):

- Vectors AC and AD renamed from get_admin_student_stats → get_admin_dashboard_students. The same privilege-escalation (non-admin caller) and intentional-soft-delete-visibility surfaces carry over verbatim. The old RPC is dropped (DROP FUNCTION in the same migration); any spec that referenced it would have needed updating, but no spec ever exercised it — so no spec breakage on drop.
- New RPC is pattern-compliant: auth.uid() IS NULL → 'not authenticated'; is_admin() = false → 'forbidden'; SET search_path = public present; org derived from auth.uid() (never a parameter) — no cross-org param override path exists. Dynamic ORDER BY is injection-safe: p_sort matched against a hardcoded CASE whitelist, p_dir normalised to 'ASC'/'DESC', v_order contains only hardcoded column identifiers — no caller string in EXECUTE. Confirmed in the live local DB and in the migration comment.
- Intentional soft-delete exception (AD / §9 admin exception): the user roster intentionally includes deleted_at IS NOT NULL rows so the admin can see inactive students. This exception now applies to get_admin_dashboard_students exactly as it did to get_admin_student_stats. The migration comment documents the §9 exception explicitly. The admin caller's own org-resolution SELECT still requires deleted_at IS NULL.
- Two new coverage gaps added: BY1 (privilege escalation: unauthed / student / instructor calling the RPC) and BZ1 (cross-org admin isolation: admin of org-B cannot see org-A students). Both are HIGH priority and have no spec. No existing spec assertions are invalidated by this change — the old RPC had no spec.
- pnpm e2e:redteam: NOT required for this PR. No existing spec exercises get_admin_student_stats (the dropped RPC) or get_admin_dashboard_students (the new RPC). All 23 existing specs are orthogonal to admin roster RPCs and bypass them entirely. The drop of get_admin_student_stats cannot break any spec assertion.
- Spec files to update when writing coverage: server-action-unauthenticated.spec.ts (add cases for BY1: unauthed + student + instructor callers) and rpc-cross-tenant.spec.ts (add case for BZ1: admin of org-B confirms no org-A rows). Recommend a separate spec file rpc-admin-dashboard-students.spec.ts to group all four sub-vectors (BY1-unauthed, BY1-student, BY1-instructor, BZ1-cross-org) rather than scattering them across two existing files.
- p_sort/p_dir injection: injection-sql.spec.ts currently covers parameterised-text inputs for student RPCs (BO/BP/BQ). The p_sort and p_dir parameters of this admin RPC could be added to the injection spec as an admin-caller path, but since the whitelist + normalisation makes SQL injection structurally impossible (no caller text reaches EXECUTE), this is a MEDIUM/LOW priority addition. Not filed as a new vector — the CONTEXT note confirms it as a verified non-vector.

2026-06-04 — review of commit cb861b4 (rpc-comment-idor.spec.ts — Vector P, closes #711 and #313):

- Vector P PASSED. The cross-user DELETE attempt by the attacker produces error===null AND deleted.length===0 — a 0-row no-op returning 200 OK. The victim's comment survives (admin re-read confirms). The positive case (owner deletes own comment) passes with deleted.length===1, confirming the policy is not vacuous.
- CRITICAL RLS SEMANTICS LESSON: RLS USING clause failures on DELETE behave differently from WITH CHECK failures on INSERT/UPDATE. DELETE rows that fail USING are silently filtered out — the statement succeeds with zero affected rows and no error code. INSERT/UPDATE rows that fail WITH CHECK raise 42501 (insufficient_privilege). Issue #711's acceptance criterion stated "assert 42501" for the deleteComment IDOR test — this was WRONG for the DELETE path. The correct assertion is error===null AND deleted.length===0. Any future comment/flag/session DELETE cross-user spec must assert the 0-row no-op, not 42501.
- This distinction applies to all tables with DELETE RLS: question_comments (USING user_id=auth.uid()), flagged_questions (USING student_id=auth.uid()), and any future table. Cross-reference: flag-idor.spec.ts exercises the flagged_questions INSERT path which DOES raise 42501 (WITH CHECK failure) — that spec's assertion is correct for INSERT, but a DELETE spec on the same table must use the no-op assertion, not 42501.
- question_comments is a documented hard-delete table (mig 049). The spec's afterAll correctly uses a hard DELETE (admin service-role client) rather than a soft-delete, consistent with the table's documented exception to the soft-delete rule.

2026-06-04 — review of commit ad19626 (PR-B: combined admin-dashboard/report/distributions-RLS specs):

- BY1 and BZ1 (get_admin_dashboard_students privilege escalation and cross-org isolation) were filed as per-spec-file vectors in the 2026-05-27 lesson. The commit delivers a dedicated rpc-admin-dashboard-students.spec.ts covering all three BY1 sub-paths (unauthed, student, instructor) plus the BZ1 cross-org positive/negative pair. This is the recommended pattern from the 2026-05-27 lesson (dedicated file rather than scattering across server-action-unauthenticated + rpc-cross-tenant). Pattern confirmed: admin SECURITY DEFINER RPCs that gate on is_admin() belong in their own dedicated spec file, not scattered across Hub A/B.
- L, M, N (get_report_correct_options) are now pinned by rpc-report.spec.ts. All three guard messages are asserted exactly (/not authenticated/i, /session not found, not owned, or not completed/i). The spec uses a seedSession helper that creates both completed and active sessions via service-role INSERT, with hermetic afterEach soft-delete. This is the correct fixture pattern for RPC specs that require specific session states — avoid calling start_quiz_session/batch_submit_quiz from the spec itself (too fragile); seed the exact DB state you need.
- Vector O (correct boolean leak in report options) is permanently homed in apps/web/lib/queries/report-question-builder.test.ts (Vitest unit), NOT in a Playwright spec. The reason: buildReportQuestions is an app-layer TypeScript function. A PostgREST-level Playwright spec calls Supabase RPCs and table endpoints directly — it cannot exercise a TypeScript projection function. The Vitest unit is the only spec layer that can assert the absence of the `correct` key in the final client payload. This is a general principle: app-layer answer-key-leak vectors that rely on TypeScript projection (not DB filtering) belong in Vitest unit tests, not red-team Playwright specs. Record this as a permanent mapping decision.
- CE (exam_config_distributions parent soft-delete RLS) is now pinned by exam-config-distributions-rls.spec.ts. Key fixture note: exam_config_distributions has no deleted_at column, so afterEach hard-deletes the throwaway config (ON DELETE CASCADE removes its rows). This is a legitimate exception to the soft-delete rule — the table has no deleted_at, so hard-delete is the only cleanup path. The spec uses a free-subject strategy (find a subject with no active egmont exam_config) to avoid colliding with the partial unique index uq_exam_configs_org_subject_active. This strategy is the reference pattern for any future spec that needs to seed an exam_config.
- Spec hermiticity note for CE: the exam-config-distributions spec uses beforeEach (not beforeAll) to seed a fresh config per test. This is intentional — each test needs its own configId because the afterEach cleanup hard-deletes the config. Using beforeAll + afterAll would mean the second test inherits the first test's configId after cleanup has run. This is the correct choice for tests that mutate the seeded resource.
2026-06-04 — PR-C closure notes (#297, #623):

- #297 (flagged_questions WITH CHECK deleted_at): issue closed as obsolete. Migration 050 intentionally removed the `deleted_at IS NULL` WITH CHECK guard from the `flagged_questions_insert_own` RLS policy. The rationale: RLS on this table no longer filters on deleted_at; a student can flag a soft-deleted question (no user-visible harm) and the flag is never surfaced because the question is hidden at every query layer. The guard removal was deliberate, not a regression. The security-auditor's §9 rule (SECURITY DEFINER soft-delete filter) does NOT apply to RLS WITH CHECK — §9 covers SECURITY DEFINER function SELECTs, not RLS policies on non-SECURITY-DEFINER tables. No vector row added and no spec gap exists; the absence of the guard is the documented, correct state.
- #623 (cross-org draft injection — Vector C): confirmed DEFERRED. The spec (quiz-draft-injection.spec.ts) exercises the single-org question ownership path but cannot cover cross-org question seeding without a second org fixture. Deferred until cross-org seeding is added to the test harness. Vector C row remains as a tracked gap in the mapping table. No spec change required this PR.

2026-06-06 — #770 static vacuous-pass audit (Sweep-On-Rule-Promotion) + redteam test-hardening batch (#551/#764/#768):

- Static-audited every apps/web/e2e/redteam/**.spec.ts schema-derived assertion (`.eq`/`.is`/`.select`/`.rpc` args) against the latest migrations for the RESIDUAL vacuous-pass class — wrong-but-existing column, silently-empty filter, or vacuous negative — the class a green `e2e:redteam` run cannot catch (the runtime-error class was already swept). ~35 specs, ~400 assertions. Result: 1 real vacuity found+fixed, all other specs clean (every isolation/no-op negative already seeds the protected row or asserts the attacker's own non-empty result first per code-style.md §7).
- FIXED — quiz-draft-injection.spec.ts "attacker cannot update another student's quiz draft": the describe-level afterEach hard-deletes every victim draft (`.eq('student_id', victimUserId)`) between tests, so the victim-draft lookup found 0 rows and the test hit its early-return — the cross-user UPDATE-isolation assertion never executed. Now pre-seeds a victim-owned quiz_drafts row unconditionally (mirrors the sibling insert; afterEach still cleans up). The author's own "or seed one here if none exists" comment had never been implemented.
- #768 — rpc-cross-tenant Vector BE: org-transfer UPDATE + org-A seed moved inside `try`, `orgASessionId` declared before it; `.select('id')` length checks added (in-`try` `expect`, `finally` `console.error`) so the `finally` always restores the victim's org even if a step throws mid-test.
- #764 — Vector L positive control strengthened to assert the real `get_report_correct_options` RETURNS TABLE(question_id, correct_option_id) contract via a seeded quiz_session_answers row (was `error===null` + `Array.isArray` only). The 2-column contract is confirmed against mig 20260316231503 — the issue body's speculated 4-column shape was wrong.
- #551 (Vector J carve-out) — new packages/db `__integration__` Vitest test rpc-batch-submit-quiz.integration.test.ts pins the §15 write-once carve-out at the SQL layer (a question soft-deleted mid-session is still fetched + scored with correct_option_id + explanation fields), complementing the Playwright #256 coverage. Runs under `pnpm --filter @repo/db test:integration` (local Supabase), NOT in the redteam.yml workflow — so this SQL-layer pin is integration-CI-only.

2026-06-06 — review of commit 8d0a758d (migration 093 record_auth_event + 4 auth Server Actions, #379):

- New SECURITY DEFINER RPC record_auth_event is EXECUTE-granted to authenticated (not restricted to postgres owner). This is intentional — the function is self-defending. Forgery surface: event_type whitelist, self-service resource=actor enforcement, admin-role gate, cross-org resource guard, and actor_id derived from auth.uid() (not caller input). Five attack sub-vectors (A1–A5 from the task brief) have zero Playwright E2E coverage.
- Vitest unit tests (create-student.test.ts, reset-student-password.test.ts, toggle-student-status.test.ts, actions.test.ts) assert the mocked Server Action layer calls the RPC with the right parameters and handles the best-effort failure path. These are mocked tests and do NOT exercise the DB-layer guards (auth.uid() null-check, whitelist ELSE, role guard, resource cross-org guard). The DB-layer guards are exercised only by an integration or E2E spec against a real Supabase instance.
- The test-writer is providing DB integration tests for A1–A4 (per task brief). A5 (cross-org admin, Vector CO) is the sub-vector most likely to be missed — it requires two-org seeding. Assess whether the integration tests cover A5; if not, CO remains a gap.
- audit-completeness.spec.ts must be extended with 4 new event_type literals: user.password_changed, user.password_reset, user.deactivated, user.created. These are positive-emission tests, not forgery tests — the appropriate home is audit-completeness.spec.ts (Vector CQ), not audit-event-forgery.spec.ts.
- actor_id derivation invariant (Vector CP): the RPC writes `actor_id = v_actor_id` (= auth.uid()) and `resource_id = p_resource_id`. A spec should assert that when an admin records user.created with a new student's UUID, the audit row's actor_id is the admin's UID and resource_id is the student's UID — not the reverse. This is the canonical pattern pinned by existing audit tests (BT class) and should extend to this RPC.
- Best-effort pattern: record_auth_event call is not awaited with a hard failure path — a failed audit write logs server-side and lets the action return success. This means a spec asserting the audit row exists must tolerate transient timing (the row should exist immediately after the RPC returns, but the Server Action does not block on it). Direct PostgREST RPC calls (not via Server Action) are synchronous — E2E specs calling the RPC directly are fine.

2026-03-20 — review of branch adding migration 049 (question_comments) + comments.ts + flag.ts:
- Hard-delete on question_comments is an intentional, documented exception to the soft-delete rule. The migration comment names the rationale. The RLS DELETE policy is correctly scoped to user_id = auth.uid(). However, the absence of a spec means the IDOR protection has never been exercised end-to-end.
- getComments and getFlaggedIds both return success:true with empty data rather than an error when the caller is unauthenticated. This is a deliberate defensive pattern (no information leakage) but it creates an inconsistency with createComment/deleteComment which return error:Not authenticated. The inconsistency itself is not a vulnerability, but a spec should pin this behaviour so a future refactor does not accidentally start returning real data to unauthed callers.
- flag.ts correctly hard-codes user.id from the auth token in all DB filters — the student_id parameter is never accepted from client input. This is the correct IDOR-prevention pattern. Still needs a spec.
- New Server Action files (comments.ts, flag.ts) must be added to the server-action-unauthenticated spec's coverage list, same rule as every previous action file.
