# Learner Agent — Memory

> Cross-agent pattern synthesis + false-positive frequency tracking.
> Governed by `.claude/rules/agent-memory.md`. **Update rows IN PLACE — never append a dated session log.** History lives in git (`git log -p -- .claude/agent-memory/learner/`).
> Migrated 2026-05-29 from the 6606-line append-only `patterns.md` journal. The pre-migration body is recoverable via git.

## Issue Frequency Tracker (live — count≥2)

Curated VIEW (count≥2 rows). Full record in `topics/tracker-archive.md`; original journal in git at `2e87c3e6`. Counts drive rule-promotion (≥2) and Sweep-On-Rule-Promotion (`agent-learner.md`). Rows transition state, never deleted. Schema: `Issue Type | Count | Last Seen | Status`.


> **2026-06-07 budget curation:** 30 terminal-state rows relocated to `topics/tracker-archive.md` (§ Terminal-state rows). Active rows are terse stubs; full text in same archive (→ Active rows). Durable-knowledge bullets relocated to `topics/cross-agent-lessons.md`. No row deleted — only relocated.

| Issue Type | Count | Last Seen | Status |
|-----------|-------|-----------|--------|
| Test fixture shape mismatch (wrong/missing field in fixture object) | 2 | 2026-03-13 | RULE CANDIDATE — both caught pre-commit by type-check; … [full → topics/tracker-archive.md] |
| Bare `catch {}` without error-type narrowing | 2 | 2026-04-08 | RULE CANDIDATE → code-style.md §6 — swallowed redirect() from requireAdmin → 500; … [full → topics/tracker-archive.md] |
| Inconsistent guard between related RPCs (sibling missing guard) | 2 | 2026-03-14 | RULE CANDIDATE — auth.uid() guard + NULL correct_option guard each added to one RPC, missed sibling; … [full → topics/tracker-archive.md] |
| Partial fix applied to sibling file group (cross-cutting concern) | 9 | 2026-06-06 | RULE CANDIDATE (count 9, active) — fix applied to the instance seen, not all instances in file + siblings; … [full → topics/tracker-archive.md] |
| useTransition + manual loading state hybrid fragility | 2 | 2026-03-13 | RULE CANDIDATE — isPending + manual isLoading can both be false mid-fetch, flashing idle button; … [full → topics/tracker-archive.md] |
| Silent numeric fallback without observability logging | 2 | 2026-03-13 | RULE CANDIDATE — fallback to 0/min on empty/malformed data with no server signal; … [full → topics/tracker-archive.md] |
| Query missing student_id scope (returns wrong student's data) | 2 | 2026-03-15 | RULE CANDIDATE → security.md (propose on 3rd) — auth check ≠ ownership scoping; … [full → topics/tracker-archive.md] |
| UI event handler missing re-entry guard (double-fire) | 2 | 2026-03-16 | RULE CANDIDATE — async lock + event-propagation mechanisms; … [full → topics/tracker-archive.md] |
| UPDATE returning zero rows treated as success (silent no-op) | 2 | 2026-03-20 | RULE CANDIDATE → code-style.md §5 — ownership-scoped DELETE/UPDATE must `.select('id')` + check row count before returning … [full → topics/tracker-archive.md] |
| Error path in existing function untested (count-error branch) | 4 | 2026-04-14 | RULE CANDIDATE (count 4) — new edge/error branches added to existing tested files lack test updates in the same commit; … [full → topics/tracker-archive.md] |
| Derived value correct by coincidence (index used as count proxy) | 2 | 2026-03-13 | RULE CANDIDATE — value co-varies with metric but diverges on edge cases; … [full → topics/tracker-archive.md] |
| Auth callback guard ordering error (guards in wrong order → bypass) | 2 | 2026-03-17 | RULE CANDIDATE — session actions must precede existence/registration checks; … [full → topics/tracker-archive.md] |
| Supabase SELECT error swallowed in auth helper (distinct from mutation) | 3 | 2026-06-04 | RULE CANDIDATE → code-style.md §5 (extension) — auth-path SELECTs + seed/test-setup SELECTs must destructure `{ data, error }` and log before the guard decision (distinct mechanisms: auth guards vs. … [full → topics/tracker-archive.md] |
| Zod error message pinned to exact internal text | 2 | 2026-03-16 | RULE CANDIDATE — Zod internal messages aren't public API; … [full → topics/tracker-archive.md] |
| Missing `setSubmitting(true)` before async call in form/button handler | 2 | 2026-04-27 | RULE CANDIDATE → code-style.md (on 3rd) — set loading state before awaiting, not after; … [full → topics/tracker-archive.md] |
| Idempotent RPC returns hardcoded values instead of reading DB state | 2 | 2026-04-27 | RULE CANDIDATE — replay paths returned hardcoded pass/100% instead of querying config; … [full → topics/tracker-archive.md] |
| Code-reviewer flags file outside the commit diff scope | 2 | 2026-04-08 | RULE CANDIDATE → agent-code-reviewer.md (false positive) — agent scans full file; … [full → topics/tracker-archive.md] |
| Pre-existing file-size violation surfaced when commit adds lines to over-limit file | 3 | 2026-06-06 | RULE CANDIDATE — file at/over limit must be split in the same additive commit, not deferred; … [full → topics/tracker-archive.md] |
| Function exceeding 30-line limit in Server Action file | 3 | 2026-04-10 | RULE CANDIDATE (count 3) → code-style.md §3 — extract row-transform/business logic as named `mapXxxRow()` helper; … [full → topics/tracker-archive.md] |
| ZodError escaping Server Action via parse() without try/catch or safeParse | 2 | 2026-03-26 | RULE CANDIDATE → code-style.md — use `Schema.safeParse()` (or wrap `.parse()` in try/catch) so invalid input returns a typed … [full → topics/tracker-archive.md] |
| Hardcoded constant values in tests instead of importing source constants | 3 | 2026-05-29 | RULE CANDIDATE → test-writer/MEMORY.md — import production constants, never duplicate literal values that drift on rename; … [full → topics/tracker-archive.md] |
| Hardcoded constant in DB migration diverging from TS source-of-truth | 1 | 2026-05-30 | WATCHING — mig 20260430000011 hardcodes limit `20` while MAX_DRAFTS is the TS source; … [full → topics/tracker-archive.md] |
| Missing assertion on parameterized query bound value in test | 1 | 2026-05-30 | WATCHING — loadDrafts() `.limit()` bound not asserted; … [full → topics/tracker-archive.md] |
| CodeRabbit false-positive rate elevated on exam-mode PRs | 2 | 2026-04-14 | RULE CANDIDATE → .coderabbit.yaml — CR lacks project context (hard-delete exceptions, DB-level constraints); … [full → topics/tracker-archive.md] |
| Manual-eval bug invisible to unit tests (dual-source UI only in full app) | 2 | 2026-04-28 | RULE CANDIDATE → code-style.md §7 (extends Refresh/Reload) — dual server+client surfaces need an integration/E2E test mounting … [full → topics/tracker-archive.md] |
| Stale `why` annotations on test payloads after guard mechanism change | 2 | 2026-05-07 | RULE CANDIDATE (promotion deferred — both in same file/migration, not distinct mechanism) — payloads.ts notes drift after guard … [full → topics/tracker-archive.md] |
| Server Action ERROR_MESSAGES not synced with new RPC `RAISE EXCEPTION` literals | 2 | 2026-05-07 | RULE CANDIDATE (held for 3rd in a different RPC/Action family; … [full → topics/tracker-archive.md] |
| Doc config-tree / inventory missing items wired in settings.json | 1 | 2026-05-30 | WATCHING — docs/decisions.md config-tree + docs/setup-audit.md hooks-list omitted review-gate.js and cr-local-plan-reminder.sh (both active in settings.json); … [full → topics/tracker-archive.md] |
| Red-team spec-count prose drift across multiple doc surfaces (tech.md ×N + decisions.md) | 3 | 2026-06-09 | RULE CANDIDATE — numeric red-team spec counts in steering/tech.md (≥3 occurrences of the same literal) and docs/decisions.md go stale on every spec-addition batch; … [full → topics/tracker-archive.md] |
| Shared-reference table (easa_subjects/easa_topics) lacks organization_id column, requiring org-isolation keying on other predicates | 1 | 2026-05-31 | WATCHING — #673 plan-critic cycle: red-team specs wanted to assert subject_id ∈ org, but easa_subjects has no org_id; … [full → topics/tracker-archive.md] |
| Red-team fixture determinism: compute all backdated dates from single now-snapshot | 1 | 2026-05-31 | WATCHING — #673 design-critic: seed had fixed question dates, fixture computed answers against those → test coverage non-vacuous only if seed volumes match expectations (1 subj/20 Qs); … [full → topics/tracker-archive.md] |
| Shared-seed red-team fixture non-vacuous only against expected seed volume, not actual | 1 | 2026-05-31 | WATCHING — #673 design-critic: when test asserts an aggregate behavior (e.g., mastery ≥50%), verify the fixture's expected row count supports that assert (e.g., ≥4 responses for ≥2/4=50%); … [full → topics/tracker-archive.md] |
| Shared-reference table (easa_subjects/easa_topics) lacks organization_id column | 1 | 2026-05-31 | WATCHING — #673: easa_subjects has no org_id; org-isolation must key on other predicates. … [full → topics/tracker-archive.md] |
| Shared-seed red-team fixture non-vacuous only against expected seed volume | 1 | 2026-05-31 | WATCHING — #673: aggregate assertion (mastery ≥50%) requires verifying fixture row count supports it. … [full → topics/tracker-archive.md] |
| Spec-doc literal counts drifting from distinct-count implementations | 2 | 2026-05-31 | RULE CANDIDATE — #673 fixtures return distinct-question count (varies with seed volume), yet spec claimed fixed `correctCount:8` and `questionIds:8`; … [full → topics/tracker-archive.md] |
| Red-team RLS error-code assertions pinned to 42501 (instead of generic error non-null) | 2 | 2026-06-04 | RULE CANDIDATE (count 2) — orchestrator DEFERRED hard-rule promotion 2026-06-04. … [full → topics/tracker-archive.md] |
| E2E marker constant inlined instead of exported (code-style.md §7 compliance gap) | 1 | 2026-05-31 | WATCHING — 9976f2ca: marker string inlined in spec instead of exported from seed.ts. … [full → topics/tracker-archive.md] |
| Test asserts only negative absence on success, not that content still renders | 1 | 2026-06-01 | WATCHING — dashboard/loading.test.tsx: banner-absent asserted; tabs-present not asserted. … [full → topics/tracker-archive.md] |
| CR-local false positives on Postgres CREATE OR REPLACE migration chain | 2 | 2026-06-05 | RULE CANDIDATE → agent-coderabbit-local.md — CR reads one mig in isolation, misses forward chain. … [full → topics/tracker-archive.md] |
| Doc residual-vector claims missing DB-level constraint that exists (symmetric drift) | 2 | 2026-06-05 | RULE CANDIDATE — grep migrations for unique constraints before asserting absence in known-gap vector doc. … [full → topics/tracker-archive.md] |
| Closure variable reassigned after caller captures reference (orphaned response) | 1 | 2026-06-06 | WATCHING — #446 9a44eb34: `setAll` reassigned local `response`; caller already held original reference. … [full → topics/tracker-archive.md] |
| Multi-branch conditional structure has assertion/guard applied to one branch but not others | 1 | 2026-06-05 | WATCHING — #570/#571 (e621f9ba+9091ad60): jsonb_build_object CASE guarded mock branch, not internal_exam branch. … [full → topics/tracker-archive.md] |
| Test cleanup: throw inside finally block / multi-block afterAll without error accumulator (noUnsafeFinally) | 2 | 2026-06-07 | RULE CANDIDATE — (1) 64339b28: throw in finally; (2) 4f918ded: multi-block afterAll skipped CL3 cleanup. … [full → topics/tracker-archive.md] |
| E2E spec soft-delete+restore must occur inside try/finally (E2E hermiticity timing refinement) | 1 | 2026-06-05 | WATCHING — 89256e0d: soft-delete outside try/finally stranded deleted question, poisoned downstream specs. … [full → topics/tracker-archive.md] |
| Migration added to packages/db/migrations but not supabase/migrations (or vice versa) | 2 | 2026-06-05 | RULE CANDIDATE — mig 084 shipped to packages/db only; already in supabase/migrations same cycle. … [full → topics/tracker-archive.md] |
| Non-required CI check allowed failing runtime tests to merge silently | 1 | 2026-06-06 | WATCHING — PR #769: Red Team Specs check was non-required + path-filtered; failing tests merged. … [full → topics/tracker-archive.md] |
| Redteam spec authoring without local run before push (assumed-schema + wire-format bugs) | 1 | 2026-06-06 | WATCHING — PR #769: local e2e:redteam not run before push despite CLAUDE.md requirement. … [full → topics/tracker-archive.md] |
| ON CONFLICT clause with no supporting UNIQUE constraint (dead code or 42P10 at execution) | 2 | 2026-06-06 | RULE CANDIDATE — (1) student_responses a09c6be: ON CONFLICT silently ignored (non-unique index, dead code); … [full → topics/tracker-archive.md] |
| plpgsql function body contains deferred-validation SQL (clean migration apply ≠ execution correctness) | 2 | 2026-06-10 | RULE CANDIDATE — (1) mig 085 ON CONFLICT non-unique → 42P10; (2) mig 101 POSIX regex bracket-class → 2201B. Both clean on apply. … [full → topics/tracker-archive.md] |
| Semantic reviewer stale-baseline false positive (compared wrong predecessor migration/definition) | 2 | 2026-06-06 | RULE CANDIDATE — (1) GDPR export 7eeff14 (2026-03-27): reviewer compared against wrong migration, claimed deleted_at column absent; … [full → topics/tracker-archive.md] |
| Docs agent fabricating plausible-but-wrong details from plan/summary (not reading migration body) | 1 | 2026-06-06 | WATCHING — Batch-A ee4d5544: agent wrote 3 inaccuracies from summary without reading migration; impl-critic caught. … [full → topics/tracker-archive.md] |
| Stale local Supabase volume / in-place migration edit causing local e2e failures | 2 | 2026-06-07 | RULE CANDIDATE — (1) PR #774: leftover volume state; (2) b004f206: mig edited after first apply → db push no-op. `supabase db reset` fixes both. … [full → topics/tracker-archive.md] |
| Haiku code-reviewer false positives on Playwright E2E spec complexity (#611 cycle) | 2 | 2026-06-05 | RULE CANDIDATE (elevated FP rate on E2E scope; … [full → topics/tracker-archive.md] |
| Knip 'dead code' findings require verification against non-TS usage before deletion | 1 | 2026-05-31 | WATCHING — #325: @tailwindcss/typography flagged dead; NOT dead (prose classes in legal pages, missing @plugin). … [full → topics/tracker-archive.md] |
| Query helper promoted to throw on error, but SA caller missing catch boundary | 2 | 2026-06-01 | RULE CANDIDATE — #627 (c26ef61f + b7cf6852): throw-posture sweep missed SA callers. … [full → topics/tracker-archive.md] |
| Numeric value nested in JSON/JSONB RPC payload not coerced to `number` | 1 | 2026-06-01 | WATCHING — #717 sweep missed nested numeric fields inside jsonb payloads. … [full → topics/tracker-archive.md] |
| Dead parameter forwarded when extracting handler into new function | 1 | 2026-06-01 | WATCHING — #698 961bbc24: unused `setSession` param carried into extracted buildResumeHandler. … [full → topics/tracker-archive.md] |
| File split without moving all test branch coverage (partial test migration) | 1 | 2026-06-01 | WATCHING — #698 961bbc24: pagination edge-case tests not moved with the split function. … [full → topics/tracker-archive.md] |
| Shared constant duplicated in split files without extract-to-module pattern | 1 | 2026-06-01 | WATCHING — #698 961bbc24: `ITEMS_PER_PAGE` duplicated across both halves of split file. … [full → topics/tracker-archive.md] |
| Red-team spec field-type assertion without nullability check across RPC modes | 2 | 2026-06-06 | RULE CANDIDATE — root cause: agent assumes RPC field type/shape without reading the migration; … [full → topics/tracker-archive.md] |
| Red-team RPC output-contract assertions under-asserted (positive paths assert existence but not field values) | 3 | 2026-06-04 | RULE CANDIDATE (count 3 — already codified via PR-G/#742 §7 "RPC Output Contract") — **Occurrences:** (1) #736 complete_overdue_exam, (2) #557 complete_empty_exam, (3) PR-A #256/#257 session-replay specs. [reconciled 2026-06-07 #792: merged a duplicate live-table row recording the same mechanism; count unchanged.] … [full → topics/tracker-archive.md] |
| Shared test-infra helpers (setup.ts, helpers/*.ts) exceed 200-line utility cap (wrongly assumed under .test.ts 500-line exemption) | 2 | 2026-06-06 | RULE CANDIDATE — code-reviewer agent suppression #5 exempts `.test.ts` files up to 500 lines, but setup.ts and helpers/*.ts are NOT test files — they are shared utility files subject to the 200-line utility cap. … [full → topics/tracker-archive.md] |
| Missing negative/failure-path test when positive-path test exists (side-effect asymmetry) | 1 | 2026-06-06 | WATCHING — #379 cycle (8d0a758d): semantic-reviewer found 2 of 4 auth actions had positive audit-event tests + best-effort success tests but no 'audit-not-called-on-failure' negative test. … [full → topics/tracker-archive.md] |
| `packages/db/src/types.ts` not manually updated after RPC return-shape change | 1 | 2026-06-06 | WATCHING — #471 commit 3fd9f6e9: migration dropped `answered_count` from `get_session_reports` RETURNS TABLE; … [full → topics/tracker-archive.md] |
| Subagent "verified clean" via `pnpm test` (vitest/esbuild) but skipped `pnpm check-types` (tsc) — type error merged | 1 | 2026-06-07 | WATCHING — #789 commit 60047c2c: metadata typed `Record<string, unknown>` but RPC param is `Json`; vitest passed (esbuild strips types); tsc failed; pre-commit hook caught it. Delegation prompts must require `pnpm check-types` alongside `pnpm test`. Distinct from "Turbo cache masking errors after dep bumps" (that is a cache-bypass rule; this is about delegation protocol omitting tsc entirely). If a second subagent ships a type error that only tsc catches, elevate to RULE CANDIDATE → add `pnpm check-types` to subagent delegation template DONE WHEN criteria. |
| Semantic-reviewer asserts non-existent project convention (`import 'server-only'` in all lib/ helpers) | 1 | 2026-06-07 | WATCHING — #789 commit e9c2a36b: semantic-reviewer claimed every lib/ helper touching server clients must carry `import 'server-only'`; verified: `server-only` appears 0× in codebase; many sibling adminClient importers have no such guard. Convention does not exist. SKIPPED as FALSE POSITIVE. Distinct from row 158 in tracker-archive (that is about removing server-only to fix vitest failures; this is the reviewer inventing universal usage that never existed). If semantic-reviewer repeats this claim in a different PR, promote to FALSE POSITIVE and add note to cross-agent-lessons FP catalog. |
| Identical type union declared in N Server Action files instead of extracted to lib/ | 2 | 2026-06-07 | WATCHING (count 2) — (1) c6700161 PR #7: `QuestionFilter` duped across component+hooks → `lib/queries/quiz.ts`; (2) 19a0f1bb PR #797: `ActionResult` union byte-identical in 16 SA files → `lib/action-result.ts`. Both required large retroactive sweeps. Pattern: shared types declared locally at first use, not extracted until a sweep forces it. On 3rd commit: propose code-style.md §4 rule "extract shared SA result types to lib/ on 2nd reuse." |
| Subagent creates stray agent-memory dir in wrong cwd (apps/web/ instead of repo root) | 1 | 2026-06-07 | WATCHING — #797 cycle: plan-critic subagent cd'd into apps/web → created `apps/web/.claude/agent-memory/plan-critic/` (cleaned up). Subagents inherit cwd from invocation context, not CLAUDE.md. On 2nd occurrence: propose to delegation template CONSTRAINTS: "All file writes must use absolute paths rooted at the repo root." |
| Budget-curation duplicates live tracker rows instead of moving them (copy-not-cut) | 1 | 2026-06-07 | WATCHING — #792 dedup (b9099dd3): 2026-06-07 budget-curation relocated active rows to tracker-archive.md but left originals in the live table, creating 4 duplicate pairs. Cut-not-copy: after pasting verbatim into archive, delete the source rows from the live table in the same edit. On 2nd occurrence: add a curation checklist note to the budget-curation block. |
| Doc CI cadence understated — workflow `on:` triggers include PR/push, not just scheduled run | 1 | 2026-06-08 | WATCHING — commit 0e1fbfba (issue #109, Decision 40 Socket.dev): impl-critic found "weekly" description of CodeQL in docs/decisions.md understated (CodeQL also runs on PR/push per workflow `on:` triggers); 1 SUGGESTION (SonarCloud also runs on push). Both APPLIED before commit. Root: doc authors describe CI tools by their scheduled cadence and omit the push/PR triggers. First occurrence. On 2nd: propose a doc-authoring note: when documenting a CI tool, grep the workflow file's `on:` block for all trigger types, not just the scheduled ones. |
| Semantic-reviewer proposes rename/fix targeting a file without reading that file's purpose | 1 | 2026-06-08 | WATCHING — commit 0e1fbfba (issue #109): semantic-reviewer SUGGESTION flagged plan.md reference to `health-monitoring.yml` as stale and recommended renaming it to `agent-health.yml`. Validated as FALSE POSITIVE: `agent-health.yml` is a completely different workflow ("Agent System Health"); `health-monitoring.yml` has no git history (never existed — the plan.md reference was itself the stale artifact, deferred to issue #804). Root: reviewer proposed a destructive rename to a target file without reading that file to verify its purpose. Distinct from row 354 (asserting a universal convention) and row 332 (comparing against a stale migration baseline) — this is proposing a rename to a file the reviewer did not read. On 2nd occurrence: add to cross-agent-lessons FP catalog: "Before accepting a reviewer suggestion to rename or replace a file, read the target file and verify it is not already a different artifact." |
| Skill/agent instruction file hardcodes a closed list that duplicates a live source-of-truth file | 1 | 2026-06-08 | WATCHING — commits 8cdc114a + 4b4d71a6 (#114): `.claude/skills/redteam.md` first hardcoded an Expected Results table copied from attack-surface.md (whole point of 8cdc114a was removing it via runtime read); then the replacement re-introduced a closed 10-token status-bucket list that omitted ~19% of live Status values (ASSESSED LOW, COVERED AT INTEGRATION LAYER, FULLY COVERED, DB-CAPPED, INTENTIONAL). Both impl-critic (SKIPPED token not in attack-surface mapping) AND semantic-reviewer (bucket list incomplete) caught it in the same cycle — the pattern repeated within a single PR. Root: author mentally modelled the live source as a closed set when writing the instruction. Fixed in 4b4d71a6 by reframing to semantic judgment with representative examples instead of a closed list. On 2nd occurrence across different commits: propose an instruction-authoring note — "skill/agent files that reference a live external source (attack-surface.md, MEMORY.md, a doc table) must describe selection criteria / semantic judgment, never transcribe a closed enumeration of current values." |
| File rename/move breaks hardcoded path in `.github/workflows/**` (code-style.md §9 rename-grep scope omits workflows) | 1 | 2026-06-08 | WATCHING — commit 13dd115a (#810, issue #326): PR #704 native-subagent-memory migration moved `red-team/attack-surface.md → topics/attack-surface.md` and renamed `learner/patterns.md → learner/MEMORY.md`, but `agent-health.yml` hardcoded both old paths. Stale for ~9 days (2026-05-30 → 2026-06-08), producing 37 phantom orphan-spec findings + vacuously-green checks. Root: code-style.md §9 rename-grep checklist targets `docs/*.md`, `.claude/rules/*.md`, MEMORY.md, agent-memory files — omits `.github/workflows/**`. Not the same as the plan.md `health-monitoring.yml` stale artifact (issue #804) — that file never existed; this is a file that moved and a workflow that tracked the old path. xargs shell-parsing failure (unbalanced backtick/apostrophe in Status column) was a secondary finding — one-off, not tracked. On 2nd occurrence (another workflow path going stale after a rename): propose adding `.github/workflows/**` to the §9 rename-grep checklist in code-style.md. |
| CR-local correctness nit SKIP'd as hypothetical, then cloud CR (ASSERTIVE) re-raised it as CHANGES_REQUESTED — forced extra CI cycle | 1 | 2026-06-09 | WATCHING — PR #811 commit 22614058: stemmer `ss`-mangling nit SKIPped pre-push, cloud CR blocked auto-merge; fix was <5 lines + behavior-preserving. On 2nd: propose SKIP-bar-raise rule in agent-coderabbit-local.md for correctness nits on auto-merge branches. … [full → topics/tracker-archive.md] |
| Red-team spec self-labels vector mnemonic colliding with existing matrix ID | 3 | 2026-06-09 | RULE CANDIDATE (count 3 — propose agent-red-team.md checklist) — (1) #793: BL/BM/BN → CU/CV/CW. (2) #802: BL-BV block → CX–DG. (3) #326: branch cut against fix/367 (stale); Explore computed max=CW, allocated CX–DC — but #802 had already consumed CX–DG on master; corrected to DH–DM at execution time. Root cause stable across all 3: ID allocation done without reading the WORKING-TREE copy of attack-surface.md on current master. Propose: agent-red-team.md checklist addition: "Before allocating new vector IDs, grep the working-tree attack-surface.md (on current master, not the feature branch) for the highest existing ID and start from highest+1." Cannot edit agent def directly — propose to orchestrator. … [full → topics/tracker-archive.md] |
| Red-team matrix row description over-claims actor (admin) the test does not actually exercise | 1 | 2026-06-09 | WATCHING — #802 commit 8173a0b6: DA row described "admin-voided code" but the test seeds voided_by as a student; the RPC guard checks only voided_at presence (not who set it), so the test is valid but the description was misleading. Fixed in 8173a0b6. First occurrence. On 2nd: log to cross-agent-lessons as a doc-accuracy pattern — matrix descriptions must reflect what the test actually seeds/exercises, not the most common production path. |
| Mechanical helper extraction introduces fidelity loss (scope drop or error-contract change) | 1 | 2026-06-09 | WATCHING — #796: (1) cleanupFixtures dropped student_id scope on extraction (fixed before commit); (2) afterAll swallow→throw on extraction (ISSUE, fixed in 5e38c487). Related to "Dead parameter forwarded when extracting handler" (count=1, 2026-06-01) — same root: extraction copies block without auditing scope predicates or error-handling semantics. On 2nd: consolidate into unified RULE CANDIDATE; propose code-style.md §3 extraction checklist. … [full → topics/tracker-archive.md] |
| doc-updater rewrites historical milestone record instead of adding new entry (rule-adherence miss) | 1 | 2026-06-09 | WATCHING — #796 cycle: doc-updater rewrote plan.md "→ 39 specs" milestone heading, violating existing agent-doc-updater.md "Exclude historical milestone records" rule. Orchestrator corrected. Rule exists; no new rule needed. On 2nd: propose explicit self-audit CHECK item to agent def. … [full → topics/tracker-archive.md] |
| Plan/Explore subagents ran against stale feature branch, producing wrong max-ID for shared sequential resource | 1 | 2026-06-09 | WATCHING — #326 (commits 202c6fa8 + 6f60ba2f): Explore + plan-critic ran against fix/367 (predated PRs #802/#796/#817 on master). Concluded max ID = CW, allocated CX–DC. At execution time reading the live matrix: #802 had already consumed CX–DG; corrected to DH–DM. Root: branch was cut off current master but Explore subagent's context included the wrong working tree. Distinct from "Vector-ID renumber-on-merge" (archive row 352): that is about sibling PRs merging between branch-cut and commit; this is about Explore running on the wrong branch entirely. On 2nd occurrence: propose a CLAUDE.md or delegation-template note: "When a plan step allocates sequential IDs from a shared file (attack-surface.md, migration numbering), the Explore subagent must read the file from the current MASTER working tree — not the feature branch — immediately before allocation." |
| Vector-label rename sweep used overly narrow grep scope (missed "mirror of X" / "vs X" label forms) | 1 | 2026-06-09 | WATCHING — #326 cycle (commit 202c6fa8): rename of stale self-label BI → DM used grep scoped to "Vector BI". Post-commit semantic-reviewer found title "Stale token replay (mirror of BI)" which survived the sweep. Root: attack-surface.md cross-references an ID in multiple forms — "Vector X", "mirror of X", "vs X" — but the rename grep targeted only the canonical form. On 2nd occurrence: propose adding to agent-red-team.md (or the redteam skill): "When re-lettering a vector ID X, grep attack-surface.md for ALL label-reference forms: 'Vector X', 'mirror of X', 'vs X', '(X)', and the bare ID 'X ' — patch every form." |
| Explore/research subagent reports stale "latest" function definition (CREATE OR REPLACE chain tail missed) | 1 | 2026-06-10 | WATCHING — docs/697 cycle (commits 3260e2d9 + 903095ed): Explore subagent reported 20260430000012 as the latest batch_submit_quiz migration with "no redefinition after 078"; actual latest is 20260601000001 (mig 091). impl-critic's independent Pre-Flag Verification caught it. Distinct from "Semantic reviewer stale-baseline false positive" (that is a reviewer missing the chain) and "CR-local false positives on CREATE OR REPLACE migration chain" (that is an external CLI). This is the planning/Explore phase — before any code is written. On 2nd occurrence: propose adding to delegation template CONTEXT: "For any migration-citing spec, include the result of `ls supabase/migrations/ | grep <function_name>` so the Explore subagent reports the full chain, not the first match." |
| Quoted exact-string literal written independently in design.md and tasks.md drifts on copy (casing / wording mismatch) | 1 | 2026-06-10 | WATCHING — docs/697 cycle (commits 3260e2d9 + 903095ed): guard-message literal 'Session...' written as 'session...' in tasks.md A.9b while design.md and the actual RPC used capital S. impl-critic (round 1) caught it in design.md; the fix was applied but the string was independently written in tasks.md without the same correction — semantic-reviewer caught the surviving mismatch post-commit (903095ed). Distinct from "Spec-doc literal counts drifting" (that is about numeric derived values, not quoted string contracts). On 2nd occurrence: propose a spec-authoring note — "when writing a quoted string that must match an RPC RAISE or error literal, define it once in design.md and reference it in tasks.md rather than writing it twice." |
| Line-position evidence in multi-function SQL file misattributed to wrong function (should trace CREATE…$$ span) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A cycle (commits 5f561ddf + f452eedb): both the orchestrator's grep AND an impl-critic verification misread which function a hunk belonged to in a multi-function migration file (mig 100), because both used line-number proximity rather than tracing the enclosing CREATE FUNCTION … $$ … END; $$ span. The verbatim-copy implementer caught it by reading the function boundary. Two reviewers same root incident — counts as 1 occurrence. On 2nd occurrence across a distinct commit/migration: propose a migration-review note — "when citing a hunk in a multi-function SQL file, verify which function body the hunk falls in by scanning upward for the nearest `CREATE OR REPLACE FUNCTION` and downward for the closing `$$ LANGUAGE`; line proximity is not sufficient." |
| Doc-sync agent duplicates existing heading / RPC summary row instead of updating in place | 1 | 2026-06-10 | WATCHING — VFR RT Phase A cycle (f452eedb): doc agent run in report-only mode due to working-tree entanglement; a follow-up doc agent introduced 2 duplication ISSUEs — (a) duplicate heading in plan.md; (b) duplicate RPC summary row in database.md. Caught by impl-critic pre-commit. Root cause: agent wrote new content without reading the existing target section to check for an existing entry. Distinct from "Docs agent fabricating plausible-but-wrong details" (that is hallucination from summary; this is structural duplication from not reading the target). On 2nd occurrence: propose note to agent-doc-updater.md "DO" section — "before adding a heading or RPC summary row, grep the target file for the proposed heading/RPC name and update in place if found." |
| Multi-site formula coherence: same computation in N RPCs must share the exact expression (LEAST cap / scoring cap) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A cycle (commits 5f561ddf + f452eedb): LEAST(...,1) cap present in migs 102 and 103 but missing in mig 100's grader formula. Semantic-reviewer caught it as ISSUE (three-site coherence). Root cause: mig 100 was written first; the cap was only noticed when writing 102/103. Pattern: when a scoring/clamp expression is added to one RPC in a family, grep sibling RPCs in the same migration batch for the same computation and verify coherence. On 2nd occurrence across different feature areas: propose a migration-review note — "when adding a numeric cap or clamp expression (LEAST, GREATEST, CASE WHEN) to one RPC in a family, grep sibling RPCs for the equivalent computation and apply consistently." |
| Shared test-infra helpers (setup.ts, helpers/*.ts) exceed 200-line utility cap (wrongly under .test.ts 500-line exemption) | 2 | 2026-06-06 | RULE CANDIDATE — setup.ts/helpers/*.ts are utility files (200-line cap), not test files (500-line exemption). … [full → topics/tracker-archive.md] |
| Subagent "verified clean" via `pnpm test` but skipped `pnpm check-types` (tsc) — type error merged | 1 | 2026-06-07 | WATCHING — #789 60047c2c: vitest passed (esbuild strips types); tsc failed; hook caught it. On 2nd: add `pnpm check-types` to delegation DONE WHEN. … [full → topics/tracker-archive.md] |
| Skill/agent instruction file hardcodes a closed list duplicating a live source-of-truth file | 1 | 2026-06-08 | WATCHING — #114 8cdc114a+4b4d71a6: redteam.md status-bucket list omitted ~19% of live values; both impl-critic and semantic-reviewer caught it. On 2nd: propose instruction-authoring note. … [full → topics/tracker-archive.md] |
| File rename/move breaks hardcoded path in `.github/workflows/**` (§9 rename-grep scope omits workflows) | 1 | 2026-06-08 | WATCHING — 13dd115a: PR #704 moved attack-surface.md + MEMORY.md; agent-health.yml stale 9 days → 37 phantom findings. On 2nd: add `.github/workflows/**` to §9 checklist. … [full → topics/tracker-archive.md] |
| Quoted exact-string literal written independently in design.md and tasks.md drifts on copy (casing mismatch) | 1 | 2026-06-10 | WATCHING — 3260e2d9+903095ed: 'session...' in tasks.md vs 'Session...' in design.md + RPC. On 2nd: propose "define once, reference everywhere" spec-authoring note. … [full → topics/tracker-archive.md] |
| Line-position evidence in multi-function SQL file misattributed to wrong function (trace CREATE…$$ span) | 1 | 2026-06-10 | WATCHING — 5f561ddf+f452eedb (mig 100): line-proximity used instead of function boundary; both orchestrator + impl-critic hit it. On 2nd: propose migration-review note. … [full → topics/tracker-archive.md] |
| Multi-site formula coherence: same computation in N RPCs must share exact expression (LEAST cap) | 1 | 2026-06-10 | WATCHING — 5f561ddf+f452eedb: LEAST(,1) cap in mig 102+103, absent in mig 100; semantic-reviewer ISSUE. On 2nd: propose grep-sibling-RPCs migration-review note. … [full → topics/tracker-archive.md] |
| Doc-sync: semantically-paired RPC detail sections not co-updated (cross-ref audit catches primary, misses sibling) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A (25bc9d62 + 7c296580): cross-ref audit caught overdue section, missed empty-session peer; both needed separate fixes. … [full → topics/tracker-archive.md] |
| §15 carve-out comment dropped in CREATE OR REPLACE rewrite (security annotation lost in migration chain) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A (CR local round 1): §15 immutable-column comment in mig 075, silently dropped in mig 078 rewrite; CR local caught absence in 095c. … [full → topics/tracker-archive.md] |
| Loose error-message matching in tests (`includes('check')`) instead of asserting Postgres SQLSTATE code | 1 | 2026-06-10 | WATCHING — VFR RT Phase A (impl-critic e179093b): `errMsg.includes('check')` rejected; `.toBe('23514')` required; 2nd batch correct first time (positive signal). … [full → topics/tracker-archive.md] |
| Constraint verbatim-quote in docs not synced when constraint edited in migration (docs grep required) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A (fcef799b + 9da1e647): mig 094 + 095 CHECKs tightened; docs stale both times; both caught post-commit. 1 occurrence (same mechanism, same PR). … [full → topics/tracker-archive.md] |
| Integration-test count numeric literal in plan.md goes stale on each test-adding commit | 1 | 2026-06-10 | WATCHING — VFR RT Phase A (plan.md 115→118→121): same class as red-team spec-count drift (RULE CANDIDATE); existing rule covers red-team only. … [full → topics/tracker-archive.md] |

## Durable knowledge (cross-agent)

- This agent does cross-agent synthesis + owns **false-positive frequency tracking** — see `topics/cross-agent-lessons.md` for the FP catalog and the full rule-promotion record.
- A count reaches promotion threshold at **2 distinct mechanisms** across different commits; same-file/same-migration repeats are NOT distinct (several count=2 rows above are held below promotion for this reason — noted inline).
- On any rule promotion, schedule the **Sweep-On-Rule-Promotion** (`agent-learner.md`): fix or file issues for ALL existing offenders, not just the triggering sites (lesson from issue #573).
- The biggest recurring defect class is **partial fix to a sibling-file group** (tracker count 5) — always grep all instances of a pattern in the file AND sibling files before committing.
- *(Other bullets relocated to `topics/cross-agent-lessons.md` § Durable knowledge relocated 2026-06-07.)*

## Topic pointers

- [cross-agent-lessons](topics/cross-agent-lessons.md) — durable rule-promotion record, false-positive catalog, recurring meta-lessons.
- [tracker-archive](topics/tracker-archive.md) — full tracker record; original journal at git `2e87c3e6`. **Before adding a NEW row, grep this file first — if it exists, increment it and lift to live table.**
- [query-helper-throw-boundary](topics/query-helper-throw-boundary.md) — Server Actions must catch now-throwing query helpers at the client boundary; throw-posture safe for Server Components but crosses unsafe RPC boundary when SA returns output to client.
- [paginated-fetch-page-error-testing](topics/paginated-fetch-page-error-testing.md) — two valid test forms (real helper + mocked queries, vs. helper as dependency mock) for caller-level page-error recovery; promoted to code-style.md §7 (PR #699).
- [postgres-security-invoker-rls-pattern](topics/postgres-security-invoker-rls-pattern.md) — SECURITY INVOKER functions on RLS-protected tables return `error: null + data: []` on unauth calls (RLS gatekeeper, not GRANT denial); impl-critic false-positive suppression pattern.
