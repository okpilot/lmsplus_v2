# Learner Tracker — Lossless Full Archive

> Complete verbatim record of every Issue Frequency Tracker row backing `../MEMORY.md`.
> This file is NEVER injected or auto-curated. The live `MEMORY.md` table is a count≥2 VIEW of these rows; this archive is the source of truth. Before adding a NEW row to the live table, grep this file for the pattern first.

## Issue Frequency Tracker (full — all rows, all counts)

| Issue Type | Count | Last Seen | Status |
|-----------|-------|-----------|--------|
| Test fixture shape mismatch (wrong or missing field in fixture object) | 2 | 2026-03-13 | RULE CANDIDATE — first: missing `short` prop (2026-03-11); second: wrong SubjectOption shape in 6722e97; both caught pre-commit by type-check; test-writer patterns memory should note: always derive fixture shapes from the exported TypeScript type, not by hand |
| `possibly undefined` in test assertions | 1 | 2026-03-11 | Fixed — biome now allows `!` in test files |
| Missing vitest imports (beforeEach) | 1 | 2026-03-11 | Fixed — 3 test files missing import |
| External agent output invisible | 1 | 2026-03-11 | Fixed — Decision 20: agents now run as in-session subagents |
| Duplicate Next.js installs (Playwright) | 1 | 2026-03-11 | Fixed — excluded e2e/ from tsconfig, cast in proxy.ts |
| Pre-push hooks too slow for large diffs | 1 | 2026-03-11 | Fixed — diff cap + timeout + grep fallback |
| Hook file exceeding 80-line limit | 4 | 2026-03-13 | RULE EXISTS — 70-line watch added to code-reviewer memory (9f5a6cc); still recurring despite watch threshold; use-quiz-state.ts hit 117 lines in 741ae30 (4th occurrence); fix required hook split in 34a9352 |
| SQL score aggregation over full table (not current batch) | 1 | 2026-03-12 | Watch — batch_submit_quiz RPC; fixed in f53eccf |
| Array positional pairing instead of Map lookup (FSRS) | 1 | 2026-03-12 | RESOLVED — batch-submit.ts updateFsrsCards; fixed in f53eccf; entire FSRS TS layer removed in b41ffa8 — no active call sites remain |
| Empty-array guard missing at SQL level | 1 | 2026-03-12 | Watch — batch_submit_quiz RPC; fixed in f53eccf |
| New hook/utility file shipped without a test file | 3 | 2026-03-12 | RULE ADDED (9f5a6cc) — still recurring; rule exists but not followed at write time. See also row 58. |
| Top-level await in node16 package test file | 1 | 2026-03-12 | Watch — packages/db/src/server.test.ts; fixed with dynamic import helper |
| Bare `catch {}` without error-type narrowing | 2 | 2026-04-08 | RULE CANDIDATE — first: packages/db/src/server.ts (2026-03-12, suggestion); second: Server Component content wrapper (344ec89/a49cd88, 2026-04-08): bare catch{} swallowed redirect() exceptions from requireAdmin(), causing 500 instead of redirect; distinct from style nit — this is a functional correctness gap when auth uses throw-based control flow (Next.js redirect()); second occurrence across different commits; propose note in code-style.md Section 6 or security.md: catch blocks in Server Components must re-throw isRedirectError() before handling |
| `finally` clearing loading state during navigation | 1 | 2026-03-12 | Fixed in a269284 — only clear loading in catch/error branch |
| Supabase mutation result not destructured (error silently dropped) | 3 | 2026-03-12 | RULE EXISTS (code-style.md Section 5) — 5 new call sites found in Sprint 3 analytics cycle (db5d8ea); compliance gap, rule is clear |
| Unstable useEffect dependency (inline function prop) | 1 | 2026-03-12 | Watch — onTabChange in question-tabs.tsx; suggestion only, first occurrence |
| `vi.stubGlobal` without `vi.unstubAllGlobals` teardown in test | 1 | 2026-03-12 | Watch — use-quiz-navigation.test.ts; suggestion only, first occurrence |
| E2E test clicks UI without waiting for React state flush (race condition) | 1 | 2026-03-12 | Watch — quiz-flow.spec.ts + progress.spec.ts; fixed with progress-bar DOM gate |
| Broad E2E selector without accessible name / overly loose text match | 1 | 2026-03-12 | Watch — quiz-flow.spec.ts; fixed by scoping getByText and getByRole with name |
| Missing route entry in docs/plan.md route tree after new page added | 1 | 2026-03-12 | Watch — /app/quiz/report missing; fixed by doc-updater in same cycle |
| Concurrent session mutation without row-level lock (FOR UPDATE) | 1 | 2026-03-12 | Watch — batch_submit_quiz RPC; fixed in fe12342 with FOR UPDATE on session row |
| Partial-submission not rejected at RPC level | 1 | 2026-03-12 | Watch — batch_submit_quiz RPC; fixed in fe12342 with answer count mismatch guard |
| Import from Next.js internal module path instead of public API | 1 | 2026-03-12 | Watch — quiz-submit.ts AppRouterInstance; fixed in b312922 with ReturnType<typeof useRouter> |
| useEffect data fetching in client component (not hydration guard) | 3 | 2026-03-13 | RULE EXISTS (code-style.md Section 6) — 3rd occurrence: statistics-tab.tsx extracted to hook in 6722e97; rule exists, compliance gap |
| LANGUAGE sql instead of plpgsql for SECURITY DEFINER RPC | 1 | 2026-03-12 | Watch — Sprint 3 analytics RPCs (845923b); fixed migration 014 (86c8da4); single root introduction; plpgsql required for RAISE EXCEPTION; if second introduction occurs, add to security.md |
| ?? [] fallback applied after an explicit error guard (silent data loss path) | 1 | 2026-03-12 | Watch — subjects ?? [] in statistics-tab.tsx after null/error guard (845923b); fixed in 86c8da4; data is already known bad at that point, fallback hides the gap |
| Server Action shipped without Zod input validation | 1 | 2026-03-12 | Watch — fetch-stats.ts Server Action (845923b); fixed in 86c8da4; rule exists (security.md rule 4); compliance gap, not rule gap |
| Inconsistent guard between related RPCs (sibling RPC missing guard introduced in first) | 3 | 2026-06-14 | PROMOTED → .claude/rules/security.md rule 12 + docs/security.md §11c + .coderabbit.yaml (#859). first: auth.uid() identity guard missing from sibling analytics RPC (845923b → 7b824c2); second: NULL correct_option guard in migration 037 but missing from sibling migration 036 (83ae098 → 08abee0); third: PR #856 check_quiz_answer rewritten as a verbatim copy of its weaker older body, shipping without 3 guards submit_quiz_answer enforces (practice-mode restriction = mid-exam answer oracle, active-user gate, question_ids-null check) — CodeRabbit caught it. Sweep-on-promotion swept all 23 SECURITY DEFINER RPCs by family; found 4 legacy read-RPCs missing the active-user gate (check_quiz_answer/get_report_correct_options HIGH, get_quiz_questions/get_session_reports MED) → filed P1 #883 (migrations = manual eval, not auto-fixed). Rule: when a guard is added to one RPC in a family, audit ALL siblings in the same commit. |
| Partial fix applied to sibling file group (cross-cutting concern) | 11 | 2026-06-19 | RULE CANDIDATE (count 11, active) — first: auth error destructuring applied to 2 of 8 query files (2190dd5); second: PR4 getUser hardening missed quiz/session/page.tsx and discard.ts (83ae098); third: GDPR consent seeding updated in supabase.ts and 1 other helper but missed admin-supabase.ts ensureAdminTestUser() (consent commit, 2026-03-27), caught as CRITICAL by semantic-reviewer; fourth: e38ef8c fixed currentStreak "1 days" → "1 day" in stat-cards.tsx but left bestStreak with the same bug (same component, same line pattern) — caught as ISSUE by semantic-reviewer; fixed in ca09f34; fifth: 6954711 (2026-04-14): `clearDeploymentPin()` called in most session-terminal paths but missed one path — caught by CodeRabbit; fixed in same batch; [counts 6–10 in later curation — see git log]; eleventh: PR #856 rebase (66d09624): "(correct)" plaintext answer-key leak removed from one eval seed fixture, missed sibling seed file — caught by sibling-audit rule and fixed. Root cause consistent: fix applied to the specific instance seen rather than auditing all instances of the same call/text in the same file + sibling files. |
| Auth error from getUser() not destructured in query file | 1 | 2026-03-12 | Watch — 7 of 8 query files under apps/web/lib/queries/ missing authError destructuring (2190dd5 → 3a0d1e6 → 78cb130); distinct from mutation error pattern (that is about .insert/.update/.delete); first occurrence as a named pattern |
| Auth error from getUser() swallowed without logging | 1 | 2026-03-12 | Watch — quiz-report.ts (78cb130); auth failure path returned early with no console.error; first occurrence; silent auth failure is harder to diagnose than silent mutation failure |
| Raw internal/third-party error.message exposed through exported result type | 2 | 2026-06-19 | RULE CANDIDATE → code-style.md §5 extension — (1) 2026-03-12 load-session-questions.ts (78cb130): Supabase `error.message` returned directly to student-facing caller; (2) #901/feat/internal-exam-code-email: Resend SDK `result.error.message` returned via `SendEmailResult.error: string` exported type → semantic-reviewer ISSUE → fixed (log raw server-side, return generic `'send_failed'`). The existing §5 rule says "Postgres error strings" — this second occurrence proves the principle is source-agnostic: any third-party or internal SDK error.message must not flow through an exported result type. Rule extension: widen §5 "Sanitize Error Messages" to cover all sources (Supabase, Resend, or any third-party SDK) — not just Postgres. |
| Unconditional setState in render body (spurious re-renders) | 1 | 2026-03-13 | Watch — statistics-tab.tsx (53efbdd); `setIsLoading(false)` called unconditionally in a render-path reset block, causing a state update on every render when isLoading was already false; fixed with `if (isLoading) setIsLoading(false)` guard in b555b50; first occurrence |
| useTransition + manual loading state hybrid fragility | 2 | 2026-03-13 | RULE CANDIDATE — statistics-tab.tsx (53efbdd, f0f8d0e); semantic-reviewer flagged this suggestion twice across two consecutive commits: isPending from useTransition and manual isLoading state tracked in parallel can both be false simultaneously during a question-switch mid-fetch, briefly showing the idle "Load Statistics" button while a fetch is still in-flight; generation counter mitigates stale data but does not close the UI-state race; suggestion-level only (not fixed); second occurrence reached — propose rule clarification in code-style.md when next commit arrives in this component's area; NOT flagged in 8863926 (pure JSX presenter refactor, no state logic touched) — no further action this cycle |
| Silent numeric fallback without observability logging | 2 | 2026-03-13 | RULE CANDIDATE — first: analytics.ts (53efbdd); `boundParam` silently clamped NaN/±Infinity without console.warn; suggestion-level; second: reports.ts (33c1fa8); `answeredCount` fell back to 0 for completed sessions with no answer rows with no console.warn — fixed in d06c25b; both share same root cause: a fallback that produces a valid-looking result with no server-side signal; the fix pattern is always a console.warn before the fallback value is returned; applies to any numeric computation that falls back to 0 or a minimum when the source data is empty/malformed |
| test-writer produces TS2532 (unchecked array index) errors | 3 | 2026-03-23 | RULE IN MEMORY (test-writer/patterns.md §Array index safety, count now 3) — third occurrence in 153a975 (session-table.test.tsx): test-writer accessed array elements without guarding against undefined, triggering TS2532; fixed with non-null assertion after length assertion; test-writer patterns.md already documents this rule at count 2; pattern persists despite documented rule — test-writer generates the wrong form first and requires a fix cycle; the rule is documented but the agent does not always apply it; no additional rule change needed — rule exists; the fix cycle remains a reliable gate |
| Shared hoisted mock capture without beforeEach reset | 1 | 2026-03-13 | Watch — activity-chart.test.tsx (8863926); capturedBarChartData shared across tests without reset allowed cross-test contamination; fixed with beforeEach reset; first occurrence |
| Direct SELECT from `questions` bypassing RPC (correct-answer exposure) | 2 | 2026-03-13 | RULE EXISTS (security.md rule 1, CLAUDE.md) — 2nd occurrence: checkAnswer in feat/post-sprint-3-polish directly queried questions table exposing `correct` flag; fixed in f6ba7a0 with check_quiz_answer RPC; rule is clear, compliance gap at authoring time |
| ON CONFLICT clause with no supporting UNIQUE constraint (dead code or 42P10 at execution) | 2 | 2026-06-06 | RULE CANDIDATE — (1) student_responses (a09c6be): ON CONFLICT silently ignored, non-unique index, dead code path; fixed by adding UNIQUE constraint. (2) mig 085 record_consent (f35b2a16): ON CONFLICT (...) WHERE accepted=true against non-unique idx_user_consents_lookup → 42P10 thrown at execution time, not CREATE FUNCTION time; `supabase db reset` applied clean; structural grep confirmed clause present; bug only visible via functional execution. ON CONFLICT inference target MUST be a UNIQUE constraint or UNIQUE index. Plpgsql parser defers ON CONFLICT validation to execution — clean apply + grep is insufficient validation; function must be called. Propose: CLAUDE.md migration checklist note. |
| TOCTOU race on count-gated INSERT (read-then-write without lock) | 1 | 2026-03-13 | Watch — draft count check before inserting quiz_draft (feat/post-sprint-3-polish); concurrent requests could both pass the count check; fixed with DB trigger enforcing limit at DB level; first occurrence |
| Query missing student_id scope (returns wrong student's data) | 2 | 2026-03-15 | RULE CANDIDATE — first: getFilteredCount in feat/post-sprint-3-polish (2026-03-13); second: getQuizReport quiz_sessions query missing student_id scope (b46b0bf → 199e927, CRITICAL); both fixed; root cause: auth check (getUser) ≠ ownership scoping — queries on student-owned tables must always scope to student_id; distinct from RPC identity-guard pattern (that is SECURITY DEFINER param mismatch); propose security.md note on third occurrence |
| UI event handler missing re-entry guard (double-fire on fast interaction) | 2 | 2026-03-16 | RULE CANDIDATE — first: handleSelectAnswer async double-fire (2026-03-13, fixed with isSubmitting ref); second: finish-quiz-dialog Escape double-fire via DOM bubbling (4439640, fixed with unconditional stopPropagation); two different mechanisms (async lock vs. event propagation); common principle: any close/submit action must be audited for re-entry at authoring time |
| Server Action file exceeding 100-line limit after Biome auto-format | 1 | 2026-03-13 | Watch — draft.ts was 166 lines, split to 114+37 (6d274fa); the 114-line file still exceeds the 100-line Server Action limit; root cause: Biome expanded compact code from 97→114 lines during pre-commit format pass; authoring-time count ≠ post-format count; first occurrence of this specific mechanism (Biome expansion pushing over limit) |
| Biome auto-format expanding compact code past file-size limit | 1 | 2026-03-13 | Watch — draft.ts written at ~97 lines, Biome format expanded to 114 lines on pre-commit (6d274fa); the 100-line Server Action limit check must be done against post-format line count, not authoring-time count; first occurrence |
| Async fetch in useEffect without stale-result cancellation flag | 1 | 2026-03-13 | Watch — useEffect in draft.ts initiated fetch without cancelled flag; if the component unmounted or deps changed before the fetch resolved, setState was called on stale/unmounted context; fixed with cancelled flag pattern in fe4ffff; distinct from "useEffect data fetching in client component" (that is about banned server-action fetching — this is a permitted client fetch missing a cleanup guard); first occurrence |
| UPDATE returning zero rows treated as success (silent no-op) | 2 | 2026-03-20 | RULE CANDIDATE — first: draft update (6d274fa, 2026-03-13): draft ID did not match ownership, 0 rows updated, caller got success; second: deleteComment (6520962, 2026-03-20): DELETE with no app-layer ownership filter, wrong commentId or cross-user attempt returned `{ success: true }` silently; both fixed by selecting affected row or checking row count; fix pattern: for any DELETE or UPDATE that is ownership-scoped, add `.select('id')` (or equivalent) and check that at least one row was returned before returning success; code-style.md Section 5 covers error destructuring but not the zero-row no-op sub-case; second occurrence warrants note in code-style.md |
| Error path in existing function untested (count-error branch) | 5 | 2026-06-10 | RULE CANDIDATE (count 5) — count-error path in draft.ts (1st, 2026-03-13); users query error path in draft.ts (2nd, d06c25b); 'session not accessible' error branch in batch-submit.ts (3rd, d057128 → 45da072); zero-answer timeout path in exam session hook (4th, bdeafc3, 2026-04-14); unauthenticated + soft-deleted-caller rejection paths on get_vfr_rt_exam_questions after org-filter security fix (5th, aed96dcb → 13fa0249, 2026-06-10): test-writer found 2 missing error-path tests post-commit; all caught post-commit by test-writer; distinct from "new file without test" — this is an existing tested file with an uncovered error/edge branch added by a recent commit; pattern: new edge-case paths added to existing functions are not accompanied by test updates in the same commit |
| Async cleanup path untestable in jsdom (cancelled flag branch) | 1 | 2026-03-13 | Watch — test-writer noted that the cancelled flag cleanup path (setState skipped after unmount) is not testable in jsdom because act() flushes effects synchronously before assertions can observe the cancelled state; analogous to the pre-hydration jsdom limitation; first occurrence; do not write tests for this branch — document the constraint in code-style.md jsdom section if it recurs |
| Stale closure introduced by hook split (scalar captured in closure vs ref) | 1 | 2026-03-13 | Watch — when use-quiz-state was split in 34a9352 to fix the 80-line violation, currentIndex was captured as a scalar in a closure in handleSave instead of being accessed via useRef; fixed in df5d354; the hook split itself introduced the bug; pattern: any value read inside a callback that is defined outside a React.useCallback/useMemo with that value in its deps array is at risk of being stale; when splitting a hook, audit all callbacks in the extracted portion for captured scalar state; first occurrence |
| SQL string comparison instead of ::uuid cast in duplicate check | 1 | 2026-03-13 | Watch — batch_submit_quiz duplicate-answer check compared option IDs as text rather than casting to ::uuid, which can cause case-sensitivity and format-variation failures; fixed in 34a9352; first occurrence |
| Type cast bypassing runtime validation (`as unknown as T` hiding missing guard) | 2 | 2026-03-13 | RULE ADDED (22c3d5e) — first: batch_submit_quiz malformed config cast (306f44a); second: check-answer.ts + fetch-explanation.ts (a0d9973); rule added to code-style.md Section 5 + .coderabbit.yaml synced; 274821b applied the rule correctly via isCheckAnswerRpcResult type guard |
| rpc<T[]> consumer missing non-array type guard | 2 | 2026-05-30 | RULE EXISTS (code-style.md §5) — count=2 from #676: "CR-local catches rpc<T[]> gaps internal agents miss". #677 sweep (PR #707, 2026-05-30) added `Array.isArray` guards to the 3 unguarded TARGET sites — progress.ts (get_student_mastery_stats loop), analytics.ts getDailyActivity (:50), analytics.ts getSubjectScores (:75). Already-compliant reference siblings (check-consent.ts, load-session-questions.ts) left unchanged. No additional non-conforming sites in lib/queries (plan-critic confirmed). semantic-reviewer raised load-session-questions:50-54 as a missed site — FALSE POSITIVE (self-contradictory: claimed `.map()` reachable when `!data?.length` is true, but that path returns early; non-array shapes hit the early return; #677 issue body lists it already-compliant); false positive recorded. CR-local round on PR #707 = 0 findings (the watch-pattern's compensating reviewer found nothing — fix is complete) |
| New hook/utility file extracted without shipping tests in the same commit | 7 | 2026-03-27 | RULE EXISTS (code-style.md Section 7) — use-answer-handler.ts (306f44a), use-session-state.ts (1b38542), score-color.ts + reports-utils.ts (b104ae4, ca55c3c), collect-user-data.ts (7eeff14/0bad818 GDPR PR3); seventh recurrence; rule is clear, authoring habit is not there; code-reviewer BLOCKING catch + test-writer fix remains the reliable gate |
| Imperative ref clear in catch block leaving re-entry window | 1 | 2026-03-13 | Watch — use-answer-handler.ts (306f44a): lockedRef.current.delete() in catch block cleared the lock before React state (answers) had settled, creating a brief window where a second call could enter; fixed in a0d9973 with useEffect drain keyed on answers; then partially reverted in 5b2864f (synchronous clear restored for immediate retryability, useEffect kept as safety drain); first occurrence of this specific async-lock re-entry window pattern |
| Derived value correct by coincidence (index used as count proxy) | 2 | 2026-03-13 | RULE CANDIDATE — first: SQL score aggregation over full table (not current batch) in batch_submit_quiz (f53eccf), where position-in-array was used as a proxy for "answered"; second: answeredCount derived as currentIndex + 1 in use-session-state.ts (675104e) — correct only because index is incremented at the same moment an answer is submitted, not because it semantically tracks "answers given"; fixed in 4798fdb with dedicated useState counter incremented in handleSubmit; both share root cause: a value that co-varies with the desired metric under normal conditions but diverges on edge cases (non-linear navigation, partial completion, session restore); propose note in code-style.md or agent memory: when a metric (count, score, progress) needs to be tracked, use a dedicated state variable incremented at the domain event, not a proxy derived from a structural position |
| Unbounded numeric regex permitting int overflow (SQL input validation) | 1 | 2026-03-13 | Watch — batch_submit_quiz RPC (274821b): `^\d+$` matched response_time_ms without an upper bound, permitting integer overflow when cast to INT; fixed in 34c9b36 with `^\d{1,10}$` (10 digits caps at 9,999,999,999, safely below INT_MAX); first occurrence; applies to any SQL regex that validates before casting to a bounded numeric type — the regex digit count must be bounded to match the target type's range |
| Case-sensitive UUID/text dedup in SQL (lower() missing) | 1 | 2026-03-13 | Watch — batch_submit_quiz duplicate-answer check (274821b): dedup compared `e->>'question_id'` text values without lowercasing; fixed in 34c9b36 with `lower(e->>'question_id')`; note: UUID format from standard generators is lowercase, so this is a defense-in-depth fix rather than a known live bug; first occurrence; distinct from "SQL string comparison instead of ::uuid cast" (that is about cast vs. text — this is about case normalisation before text comparison) |
| ARIA tab role missing on button-based tab UI | 1 | 2026-03-13 | Watch — QuizTabs (46113bf): buttons used as tabs lack role="tab", aria-selected, and enclosing role="tablist"; pre-existing gap, not introduced by the TabButton extraction; flagged by semantic-reviewer as a suggestion; if a second component is flagged for missing semantic ARIA tab attributes, add a note to code-style.md Section 2 (component rules) |
| Error message not updated after control flow change eliminates its code path | 1 | 2026-03-14 | Watch — batch_submit_quiz (d057128/ce35a31): original error message 'session not found or already completed' became inaccurate after the idempotent replay path was added (completed sessions no longer reach the error branch); caught by semantic-reviewer post-commit; fixed in ce35a31 with 'session not found or not accessible'; first occurrence; the root cause: when a control flow change eliminates a code path (e.g., converting an error to a replay), any error message that referenced that path by name must be updated in the same commit |
| FOR UPDATE lock scope wider than write path (read-only replay serialization) | 1 | 2026-03-14 | Watch — batch_submit_quiz (d057128): FOR UPDATE lock on session SELECT is held even during the idempotent read-only replay path, briefly serializing replay reads; flagged by semantic-reviewer; accepted as documented trade-off (prevents TOCTOU race on concurrent new submissions); comment added in ce35a31 explaining the trade-off explicitly; first occurrence; accepted pattern with required documentation — not a violation |
| consoleSpy created without try/finally cleanup (spy leaks on test failure) | 3 | 2026-03-14 | RULE ADDED TO TEST-WRITER MEMORY — 1st: start.test.ts + check-answer.test.ts (15ad393); 2nd: draft-delete.test.ts (cb0395c, newly created file); 3rd recurrence across different commits warrants update to test-writer patterns memory; always wrap consoleSpy in try { ... } finally { consoleSpy.mockRestore() } |
| RPC missing `AND deleted_at IS NULL` guard on session fetch | 1 | 2026-03-14 | Watch — complete_quiz_session and submit_quiz_answer both fetch a session row (SELECT ... WHERE id = p_session_id) without filtering soft-deleted sessions; a deleted session can be replayed or answered; first occurrence; test-writer flagged both RPCs in a1335ff post-commit; security.md rule 6 covers "never hard DELETE" but does not explicitly require session-fetch queries to exclude soft-deleted rows; if a second RPC ships without this guard, add explicit requirement to security.md: "session-fetch queries must always include AND deleted_at IS NULL" |
| Hard DELETE in test/spec cleanup code | 1 | 2026-03-14 | Watch — session-race-condition.spec.ts (a396438/a1335ff) used hard DELETE in a `afterAll` cleanup block instead of soft-delete; test code is exempt from the application-level soft-delete rule (it is seeding/cleanup, not student data), but for security-sensitive red-team specs the pattern of writing hard DELETEs is a habit that can accidentally propagate to production code; first occurrence; acceptable in test cleanup only — document the exception if flagged again |
| Red-team spec written against wrong schema column or RPC signature | 2 | 2026-03-14 | RULE CANDIDATE (count=2; transitioned from Watch per state machine) — first: multiple specs in f278d5c used wrong column names (e.g. wrong foreign key column), wrong RPC parameter names, and wrong table names; second: a396438 + a1335ff required further alignment of RPC signatures and schema assertions; distinct from "test fixture shape mismatch" (that is TypeScript type mismatch — this is SQL/RPC API mismatch in Playwright E2E); both caught pre-merge by CI failures; root cause: red-team specs are written speculatively from memory of the schema rather than reading actual migration files; if a third spec ships with wrong schema references, add a rule to the red-team agent: always read the relevant migration file before writing DB assertions |
| Test spec encodes a security gap as a passing assertion | 1 | 2026-03-14 | Watch — session-race-condition.spec.ts (a1335ff) had a test that asserted the race condition response was acceptable/expected, effectively baking the security gap into the passing baseline rather than asserting the gap does not exist; semantic-reviewer flagged this as an ISSUE: a test that passes because it accepts wrong behavior is worse than a failing test; first occurrence; the correct form for a security spec is: assert the hardened behavior, fail if the unguarded path succeeds |
| Auth callback guard ordering error (guards run in wrong order, allowing bypass) | 2 | 2026-03-17 | RULE CANDIDATE — first (83ae098 → 08abee0, 2026-03-14): exchangeCodeForSession set session cookie before users-table check; if users check failed, signOut() was not called; second (5cc4109 → 47df5cf, 2026-03-17): recovery branch placed before profile-existence gate, allowing orphaned auth users to bypass not_registered check via password reset; both fixed in follow-up commits; root cause: multi-step auth callback branches are added without auditing the full ordering of guards; rule candidate: when any new branch is added to the auth callback, the ordering of all guards must be verified — session actions always precede existence/registration checks, and all post-session failure paths must call signOut() before redirecting |
| useMemo with empty deps used as stability guarantee (should be useRef) | 1 | 2026-03-14 | Watch — use-quiz-state.ts (1b38542 → fixed 9ea234b): initial fix used `useMemo(() => value, [])` to snapshot mount-time value; semantic-reviewer correctly flagged that useMemo is a performance hint whose result is not guaranteed stable in concurrent mode; correct tool is useRef; first occurrence; if a second component uses useMemo with empty deps to capture a mount-time constant, add a note to code-style.md Section 2 or 6 about the distinction |
| test-writer generates deprecated vi.fn generic syntax (two-arg form) | 2 | 2026-03-15 | RULE ADDED — first: use-session-state.test.ts (9ea234b, 2026-03-14); second: session-operations.test.ts (69273cf, 2026-03-15); both required orchestrator correction before type-check passed; correct form is `vi.fn<(arg: A) => R>()` (single function-type argument, Vitest v4); test-writer patterns.md updated with explicit rule and code examples; stale wrong-syntax example on line 238 also corrected |
| ZodError escaping Server Action with typed error return type (parse() without try/catch) | 1 | 2026-03-15 | Watch — checkAnswer called CheckAnswerSchema.parse(raw) without try/catch; ZodError propagated as unhandled exception instead of returning typed { success: false } response (199e927); fixed with try/catch returning typed error shape; first occurrence; distinct from "Server Action without Zod validation" (that is missing validation — this is validation present but exceptions escaping the return-type contract) |
| Supabase `.returns<T>()` causing forced intermediate type casts (`as string & keyof never`) | 1 | 2026-03-15 | Watch — PR #7 (b46b0bf) systematically removed `.returns<T>()` chains that caused `as string & keyof never` casts on query builder methods; fix pattern: drop `.returns<T>()`, execute query, cast result directly via `const typed = data as TargetType | null`; first occurrence of systematic cleanup; first occurrence of original antipattern being named |
| Non-redirect response in proxy.ts dropping refreshed session cookies | 1 | 2026-03-15 | Watch — proxy.ts admin 403 guard returned bare `new NextResponse(...)` bypassing `redirectWithCookies()` cookie-copy loop (6b49021); fixed in cebf441; any response after `getUser()` in proxy.ts must copy `response.cookies.getAll()` before returning; CRITICAL severity |
| Supabase query (SELECT) error silently swallowed in auth helper (distinct from mutation pattern) | 2 | 2026-03-15 | RULE CANDIDATE — first: getUser error ignored in Server Actions (83ae098, 2026-03-14); second: requireAdmin() profile SELECT discards `{ error }` (6b49021, 2026-03-15); both in auth-path helpers; fix: always destructure `{ data, error }` and log before guard decision; existing code-style.md rule covers mutations only — clarification extension proposed |
| Server-authoritative ordering value (sort_order) computed client-side and trusted on submission | 1 | 2026-03-15 | Watch — subject-row.tsx passed sort_order as prop to upsert Server Action; fixed in cebf441 by computing sort_order server-side from sibling count; first occurrence; applies to any derived ordering or position value that should reflect DB state at write time |
| `@ts-expect-error` for Supabase TypeScript inference depth limit on easa_* tables | 1 | 2026-03-15 | PARTIAL RESOLUTION (2026-03-16) — @supabase/ssr upgraded 0.5.0 → 0.9.0; fixed inference depth limit for quiz_drafts (draft-helpers.ts, 2 suppressions removed); easa_* tables (upsert-subject, upsert-subtopic, upsert-topic) still require suppressions on `.insert()` — TypeScript still cannot resolve the easa_* generated type chain to a non-never Insert type; suppressions are documented with JSDoc comment explaining the root cause; validated as still-needed by semantic-reviewer on 603b36c cycle (false positive rejected) |
| `.single()` used where no-row is a valid outcome (silently swallows PGRST116 error) | 1 | 2026-03-15 | Watch — seed-admin-eval.ts topic/subtopic lookups used `.single()` for rows that may legitimately not exist yet; `.single()` raises PGRST116 on zero rows and its error is swallowed if not explicitly checked; fixed in 4363a34 with `.maybeSingle()` which returns `{ data: null, error: null }` when no row is found; distinct from "Supabase mutation result not destructured" (that is about .insert/.update calls — this is about SELECT lookups where zero rows is an expected valid state); first occurrence |
| Duplicated fallback/error-handling code in same file that drifts out of sync | 1 | 2026-03-15 | Watch — security auditor script had two copies of the same grep fallback logic (timeout-fallback path and agent-failure path); they diverged silently; fixed in 4363a34 by extracting to a shared helper; first occurrence; root cause: copy-paste duplication in shell/TS scripts is harder to spot than in application code because scripts often have bespoke control flow with no shared abstraction layer |
| Migration-based consolidation (TS best-effort write moved to RPC for atomicity) | 1 | 2026-03-16 | Watch — migration 040 moved last_was_correct write from TypeScript try/catch into submit_quiz_answer RPC; first occurrence as a named pattern; prior examples: draft_count DB trigger, consecutive_correct_count in batch_submit_quiz; propose note in agent memory if a third TS best-effort post-RPC write appears |
| Behavioral gap silently fixed by migration (cross-path column population) | 1 | 2026-03-16 | Watch — migration 040 closed last_was_correct gap in single-answer mode; filter:incorrect was silently incomplete; fixed as side-effect of FSRS removal; implication: when a migration adds a write path for a column, check all query consumers that filter on that column across all write paths |
| Upstream named type used as structural approximation after library upgrade | 1 | 2026-03-16 | Watch — `Record<string, unknown>` used for `CookieOptions` in middleware.ts and server.ts; after @supabase/ssr upgrade to 0.9.0 the named type became available; fixed in 603b36c with correct import; first occurrence; apply: after any library upgrade, cross-check hand-rolled Record/structural types against the library's updated public exports |
| Zod error message pinned to exact internal text | 2 | 2026-03-16 | RULE CANDIDATE — first: cb0395c (2026-03-14) test file assertion pinned to Zod 3 internal string; second: 559bf9e Zod 3→4 migration — "Invalid uuid"→"Invalid UUID", "Required"→"Invalid input:…" changed in production source files; Zod internal messages are not public API; assert `error instanceof ZodError` or `.issues[0].code`, never `.message` text |
| Test fixtures using zeroed UUID format (`00000000-0000-0000-0000-*`) invalid under Zod 4 RFC 4122 enforcement | 1 | 2026-03-16 | Watch — 559bf9e: 27 test files required UUID constant replacements from `00000000-0000-0000-0000-*` to `00000000-0000-4000-a000-*`; Zod 4 validates version bits (nibble 13 must be 4–5) and variant bits (nibble 17 must be 8–b); also affected E2E sentinel UUIDs (5ad3c16); first occurrence; if new test file added with zeroed UUIDs, update test-writer memory with compliant constant form |
| Stale mock string in test after Server Action string rename | 1 | 2026-04-26 | Watch — 0c49a09: test file mocks `startExamSession` directly; when production Server Action error string changed from 'Exam mode is not configured' to 'Practice Exam is not configured', test mock was not updated; test continued to pass (echoed its own mock value) but no longer validated production string; pattern: when renaming user-visible strings in a Server Action, grep the `.test.ts` file for the old string and update mock values alongside assertions; first occurrence |
| Defensive fix applied for race/stale-state condition that does not materialize today | 2 | 2026-04-26 | RESOLVED (intentional hardening; per inline conclusion 'mark RESOLVED — pattern is intentional') — WATCH AND CATEGORIZE — first: 34194aa (confirmingDiscard race when timeExpired flips while confirm panel open); second: 74d557b (confirmingDiscard not reset when timeExpired flips mid-render); both fixes are defensive (the race doesn't fire today due to parent invariants, but the fix is cheap and closes the gap); semantic-reviewer flagged each as an ISSUE despite non-materialization; pattern: when a child component holds state derived from a parent invariant (e.g., canDismiss from props), mirroring that invariant explicitly in the child's render guard or effect prevents future regressions when the parent invariant is relaxed; two occurrences within same PR suggest this is a deliberate hardening pattern, not a sign of unclear spec; mark RESOLVED — pattern is intentional |
| implementation-critic false positive on adjacent JSX guards | 1 | 2026-04-26 | Watch — 34194aa: implementation-critic flagged "duplicate Discard button" but the two buttons were distinct (one conditional on event handler, one on render guard `{canDismiss && (`); critic mistook the `&&` guard block as a duplicate of the existing button; false positive; first occurrence |
| `err.errors` property access silently undefined after Zod 4 removal (was Zod 3 alias for `.issues`) | 1 | 2026-03-16 | Watch — 559bf9e: two production source files accessed `.errors` on ZodError instances; Zod 4 removed the property (not deprecated); returns undefined, silently falls through to fallback strings; caught by pre-commit tsc gate; fix: use `.issues` throughout; first occurrence |
| Turbo type-check cache masking new compile errors after dependency bumps | 3 | 2026-03-17 | RULE APPLIED (CLAUDE.md) — first: PR #211 (@supabase/ssr, @supabase/supabase-js, vitest bumps); second: c5025f6 (commitlint 20, jsdom 29, @types/node 22 bumps); third: d9de1dd (vite 7→8, @vitejs/plugin-react 5→6); CLAUDE.md rule added after 2nd occurrence: "After any dep-bump commit, run `pnpm check-types --force` (bypasses turbo cache)"; 3rd occurrence confirms the rule was followed — ISSUE was pre-empted; rule is working; no further change needed |
| rolldown-vite RC bundled in @vitejs/plugin-react@6 | 1 | 2026-03-17 | Watch — d9de1dd: @vitejs/plugin-react@6 bundles rolldown-vite (Rust-based bundler) as an internal dep; RC status means breaking changes possible in minor bumps; no action needed now; first occurrence — log and watch |
| Undefined severity level used in agent rule (label not in agent's schema) | 1 | 2026-03-16 | Watch — b32d56a: new suppression condition 9 in security-auditor.md assigned "WARNING" severity; security-auditor schema defines only CRITICAL, HIGH, MEDIUM; undefined level has no blocking/non-blocking contract; pre-push hook cannot act on it; fixed in d6e8224 with MEDIUM; first occurrence; watch for agent edits that introduce severity labels without cross-checking the agent's own severity table |
| Agent suppression condition requiring out-of-diff artifact verification | 1 | 2026-03-16 | Watch — b32d56a: suppression condition required verifying RPC's SELECT list does not expose correct answers; RPC definition lives in a migration file not present in the diff; if the migration file is inaccessible, the agent must flag as HIGH rather than suppress; fixed in d6e8224 with explicit fallback instruction: "If the migration file is not accessible, flag as HIGH"; first occurrence; watch for suppression rules that require knowledge of artifacts outside the current diff |
| Agent file DO NOT section numbering collision with main checklist numbering | 1 | 2026-03-16 | Watch — b32d56a: new DO NOT item numbered "9" collided with checklist item "9" in HIGH section; non-contiguous DO NOT numbering (1,2,3,5,6,8,9) also pre-existed; fixed in d6e8224 (DO NOT renumbered 1-7) and 0f40bd6 (duplicate HIGH block removed, renumbered 11-15); first occurrence; watch for agent edits that introduce severity labels without cross-checking the agent's own severity table |
| SECURITY DEFINER RPC input array not validated against caller-owned records | 1 | 2026-03-16 | Watch — f1f6c32/7029f4e (fix/45-remove-answer-keys-from-test): get_student_questions RPC accepted p_question_ids from caller without verifying those IDs belonged to the caller's session; fixed in 7029f4e/1f76a7b by deriving question set from session answers rather than trusting caller input; distinct from "Query missing student_id scope" (that is a missing WHERE clause on a SELECT — this is an RPC SECURITY DEFINER trusting a caller-supplied array without cross-checking ownership against session state); rule already in security.md covers auth.uid() identity check but not input array ownership validation; first occurrence |
| PostgREST BIGINT/NUMERIC serialized as JSON strings — uncoerced reads break === and arithmetic | 4 | 2026-05-31 | PROMOTED → code-style.md §5 (count 4) — PostgREST sends BIGINT (int8) and NUMERIC columns as JSON *strings*, not numbers. Reading into a `number`-typed field without `Number()` coercion produces silent bugs: broken equality checks (=== 1 fails when value is "1"), NaN on arithmetic, .toFixed() crashes. Occurrences: quiz.ts (total_time_ms, #680), profile.ts (avg_score, 2026-05-29), dashboard-stats.ts (subject_count, 2026-05-29), reports.ts (total_count, answered_count, score_percentage, this cycle). Fix pattern: coerce at read site with `Number(value)` before use; existing precedent (quiz.ts) shows the applied pattern. Promotion triggers Sweep-On-Rule-Promotion: audit all RPC result reads in lib/queries/ + lib/ for unguarded BIGINT/NUMERIC fields and apply coercion to all remaining sites (grep `int8\|numeric\|NUMERIC` in migrations to identify columns, trace to consumer call sites). |
| Red-team SELECT-0-rows assertion without seeded victim data (vacuous test) | 1 | 2026-05-31 | RESOLVED (one-off violation, pattern already established in prior specs) — #314 commit eb1953c9 added two tests: "unauthenticated client sees 0 rows from question_comments" and "...from flagged_questions" without seeding victim-owned rows in beforeAll. Semantic-reviewer flagged as ISSUE: 0-rows assertion passes for the wrong reason (table is empty, not that RLS blocked). Commit b365f1e1 immediately fixed by seeding victim-owned fixtures via admin client in beforeAll, per the established pattern used in prior specs (e.g., rpc-internal-exam-codes.spec.ts commit 31364695, which seeds both victim and attacker codes in beforeAll). Code-style.md §7 already documents E2E hermiticity (soft-delete cleanup in afterAll); fixture seeding is implicit in that pattern. Pattern existed before #314, was momentarily forgotten in eb1953c9, and restored in b365f1e1. No rule change needed. |
| Red-team RLS error-code assertions pinned to 42501 (instead of generic error non-null) | 1 | 2026-05-31 | WATCHING — #314 commits eb1953c9 + b365f1e1 introduce pinning `expect(error?.code).toBe('42501')` on RLS WITH CHECK rejection tests. Semantic-reviewer flagged the commit-1 generic assertions and suggested (SUGGESTION level) pinning specific codes instead of non-null checks on RLS errors. Commit 2 applied the fix. Prior specs use generic `expect(error).not.toBeNull()` on RLS errors (rpc-internal-exam-codes.spec.ts, audit-event-forgery.spec.ts, etc.). The new practice validates the specific mechanism (RLS WITH CHECK violation = error code 42501) rather than just presence. Pattern is sound and recommended; first occurrence of pinned error.code pattern in red-team specs. Watch for adoption on 2nd occurrence in a different spec to consider promoting to code-style.md §7 red-team guideline. |
| TypeScript type cast used as data-stripping mechanism (answer key exposure) | 1 | 2026-03-16 | Watch — f1f6c32 (fix/45-remove-answer-keys-from-test): correct field present on runtime object cast to QuestionForStudent type; TypeScript type does not exclude the field at runtime — only explicit SQL SELECT projection or object spread omitting the field strips it; directly violates security.md rule 1 (correct answers must be stripped server-side); fixed by moving answer-key stripping into the RPC SELECT list; distinct from "Type cast bypassing runtime validation" (that is about missing runtime guards on cast data — this is about using a type cast as a security boundary for data stripping, which provides zero runtime protection); first occurrence |
| RPC `.rpc()` call result not destructured for error (silent swallow on RPC failure) | 1 | 2026-03-16 | Watch — f1f6c32 (fix/45-remove-answer-keys-from-test): supabase.rpc() call result was not destructured for { error }; existing code-style.md Section 5 rule covers .insert/.update/.delete/.upsert mutations; .rpc() calls are semantically equivalent — any Supabase client call that can fail must destructure { data, error }; fixed in 7029f4e; first occurrence as a named gap in the rule's coverage; if a second .rpc() call ships without error destructuring, extend the code-style.md rule to explicitly list .rpc() alongside mutation methods |
| `NextResponse.redirect()` dropping cookies set via `cookies()` API in Route Handler | 1 | 2026-03-18 | Watch — 738eb43 (feat/174-login-redesign): verifyOtp wrote session cookies via Supabase cookies() API; NextResponse.redirect() does not carry those cookies to the browser — cookies set via the Next.js cookies() API only flow through responses built by the framework's own redirect() helper; fixed by switching to `redirect()` from `next/navigation`; applies to any Route Handler that mutates cookies (auth, session, etc.) and then redirects — always use next/navigation `redirect()` or copy cookies onto the NextResponse manually; first occurrence |
| `{{ .RedirectTo }}` in Supabase email templates passes full URL, not pathname | 1 | 2026-03-18 | Watch — 738eb43 (feat/174-login-redesign): Supabase passes the full absolute URL (e.g. `http://localhost:3000/auth/reset-password`) as the `next` query param when `{{ .RedirectTo }}` is used in email templates; if code naively appends this to a base URL, the full URL becomes a path segment and doubles the origin; fixed by extracting `.pathname` from a `new URL(next)` call before appending; applies to any code that reads a `next` param arriving from a Supabase email template redirect; first occurrence |
| Missing `setSubmitting(true)` before async call in form/button handler | 2 | 2026-04-27 | RULE CANDIDATE — first: 375bde5 handleSubmitSession async call within render was missing setSubmitting guard; semantic-reviewer ISSUE; second: same pattern likely in other exam-session handlers; PR #523 contained multiple 0-answer paths; test-writer added test 8782a18; root cause: async submission paths should always set loading state before the call, not after; pattern: any form/button handler that triggers an async operation must call `setSubmitting(true)` before awaiting; applies across all form handlers, not just exam mode; if third occurrence in different components, add to code-style.md form handler pattern or agent rules |
| Actor role lookup subquery missing `deleted_at` filter in mutation audit path | 3 | 2026-04-27 | RULE PROPOSED — #550 first surfaced in batch_submit_quiz (bdeafc3, 2026-04-14): actor_role subquery SELECT did not filter `deleted_at IS NULL`, allowing deleted user records to corrupt audit events; re-surfaced in 375bde5 for submitEmptyExamSession path (distinct RPC, same oversight); semantic-reviewer flagged for 2nd time explicitly; third crossing-reference in 8782a18 notes this as part of the pattern; root cause: SECURITY DEFINER RPCs that insert audit events must apply soft-delete filters on all related-table lookups that include user/session references; rule already in security.md covers soft-delete RPCs generally but does not specifically require audit-path subqueries to filter on soft-delete; **PROPOSED:** add to security.md rule 6 carve-out section: "Every audit_events INSERT subquery must include `AND deleted_at IS NULL` on any user/session foreign-key lookups" — document as non-negotiable audit-path hygiene; count=3, pattern confirmed across multiple RPCs and commits |
| Idempotent RPC returns hardcoded values instead of reading current DB state | 2 | 2026-04-27 | RULE CANDIDATE (count=2; transitioned from WATCH per state machine) — first: batch_submit_quiz idempotent replay (d057128, 2026-03-14) used hardcoded pass value instead of checking exam config on replay; semantic-reviewer flagged as SUGGESTION; second: submitEmptyExamSession idempotent path (375bde5) returns hardcoded `{ passPercentage: 100 }` instead of querying exam_configs; semantic-reviewer flagged as SUGGESTION in 375bde5, resolved in 8782a18 to read actual value from DB; distinct from the soft-delete pattern (that is about lookup hygiene — this is about correctness of returned values on replay); second occurrence warrants watching for a third; when an idempotent RPC returns a computed value on replay, it must read the current state from the DB, not return a cached/hardcoded estimate |
| Nesting depth 4+ in form handler (refactored to multiple smaller handlers) | 1 | 2026-04-27 | WATCH — code-reviewer NESTING WARNING on handleSubmitSession (375bde5); 4 levels deep; fixed in 8782a18 via callback extraction (hoisted validator + handler logic split); distinct from the "max 30-line function" rule (that is about line count — this is about readability via visual indent); no action needed if resolved in same cycle, but note if a third deep-nesting handler appears |
| Supabase `updateUser` returns 422 when new password matches current password | 1 | 2026-03-18 | Watch — 738eb43 (feat/174-login-redesign): E2E reset-password spec tried to reset to the same password the account already had; Supabase auth API returns HTTP 422 with no explicit error message, causing a confusing test failure; fixed by using a distinct password value in E2E tests and by handling 422 in the UI with a user-facing error message; applies to any E2E test covering the password reset flow — always reset to a value different from the current one; also applies to the reset-password UI, which should handle 422 distinctly from other errors; first occurrence |
| Open redirect via unvalidated `next` param in Route Handler | 1 | 2026-03-18 | Watch — ca5bbd5 (feat/174-login-redesign): /auth/confirm accepted any `next` query param and redirected to it without validation; a crafted link could redirect users to arbitrary internal paths (e.g. `/app/admin`); fixed by validating `next` against an explicit allowlist (only `/auth/reset-password` permitted); applies to any Route Handler or Server Action that reads a redirect target from query params or form input — always validate against an allowlist before redirecting; semantic-reviewer caught this as an ISSUE; first occurrence as a named pattern |
| Security fix requiring multiple rounds due to incomplete self-defending audit | 1 | 2026-03-16 | Watch — fix/45-remove-answer-keys-from-test (f1f6c32 → 7029f4e → 1f76a7b): branch required 3 semantic-reviewer rounds because each fix addressed the flagged issue but not the adjacent gap it exposed; round 1: correct field in runtime object; round 2: RPC not session-scoped; round 3: p_question_ids not validated against session; root cause: security fixes to SECURITY DEFINER RPCs were applied narrowly (one gap at a time) rather than with a full self-defending audit (ownership check, input validation, output projection, error handling all verified together); first occurrence; when any SECURITY DEFINER RPC is modified, audit all four axes before committing: (1) auth.uid() identity check present, (2) input arrays validated against owned records, (3) output SELECT excludes sensitive fields explicitly, (4) result destructured for error |
| Code-reviewer flags file outside the commit diff scope | 2 | 2026-04-08 | RULE CANDIDATE — first: prior cycle (exact commit not recorded, watch); second: 344ec89/a49cd88 (2026-04-08): code-reviewer flagged getStudentSessions at 61 lines — function was pre-existing, not in the diff; orchestrator correctly dismissed; root cause: code-reviewer scans changed files' full content rather than only the diff hunks; this causes false BLOCKING on pre-existing violations in files touched by an unrelated commit; confirmed false positive; suppression already exists in agent-code-reviewer.md for this case but the agent does not reliably apply it; at 2 occurrences, agent-code-reviewer.md rule should be checked and if it does not already cover this, add explicit note: "Only flag violations introduced or worsened by lines in the diff — do not flag pre-existing violations on lines not touched by this commit" |
| Pre-existing file size violation surfaced when a new commit adds lines to an already-over-limit file | 2 | 2026-04-11 | RULE CANDIDATE — first: d93f924 (2026-03-20): lookup.ts exceeded the 100-line limit before the commit; new commit added lines, making violation visible; fixed by extracting helpers; second: cb318f4 (2026-04-11, feat/exam-mode-admin-config): upsert.ts was at 102 lines (already 2 over the 100-line Server Action limit); a legitimate 5-line error-check addition pushed it to 106 lines; code-reviewer caught it; the file was already over the limit before the new commit touched it; distinct from "Biome auto-format expanding compact code past file-size limit" (that is Biome pushing an at-limit file over; this is a file already over the limit before the commit); root cause: file was never split when it first exceeded the limit; the fix pattern is always to split the file when it crosses the limit, not to defer; second occurrence across different commits — RULE CANDIDATE: when a Server Action file is at or above its 100-line limit, any additive change must be accompanied by a split in the same commit, not deferred |
| Function exceeding 30-line limit in Server Action file | 3 | 2026-04-10 | RULE CANDIDATE (count 3 — action required) — first: getFilteredCount in lookup.ts (d93f924, 2026-03-20): 58 lines, fixed by extracting buildQuestionQuery helper; second: toggleFlag in flag.ts (6520962, 2026-03-20): 52 lines, fixed by extracting unflagQuestion/flagQuestion helpers; third: mapRpcRow logic inline in reports.ts (9370e7d/9711338, 2026-04-10): 63-line function, fixed by extracting mapRpcRow helper; all in non-Server-Action utility files but same violation mechanism (single function doing row-mapping + business logic without extraction); root cause: data-mapping or row-transform logic written inline in the reporting function rather than extracted as a named helper; pattern: any reporting/analytics function combining query + row mapping in a single body is a candidate to exceed 30 lines — extract the row transform as a named `mapXxxRow()` helper at authoring time; third occurrence across different commits — action required; proposed note in code-style.md Section 3 already implied by existing rule; no new rule needed — existing BLOCKING catch + extract pattern is working |
| Shared-reference table (easa_subjects / easa_topics) lacks organization_id column, requiring org-isolation keying on other predicates | 1 | 2026-05-31 | WATCHING — #673 plan-critic cycle (4fb5d2a4, 2026-05-31): red-team regression specs for aggregation RPCs wanted to assert "mastery of subject X in org Y" as a test constraint; easa_subjects table lacks org_id column — subject IDs are not scoped to an organization; current data model has subject_id ∈ [1..N] across all orgs without org_id partitioning; org isolation via student response rows (student_responses.student_id → student.org_id) and aggregate scope (all responses across all subjects of caller's students); if a second incident recurs on a different reference table or org-isolation predicate, pattern may indicate missing data-model clarification for org-scoped reference tables |
| Red-team fixture determinism: compute all backdated answer dates from single now() snapshot | 1 | 2026-05-31 | WATCHING — #673 design-critic pass (4fb5d2a4, 2026-05-31): seed questions had fixed creation dates (e.g. date_asked = -90 days); fixture computed student response dates relative to those fixed points; plan-critic recommended parametric backdating: compute all answer timestamps by adding elapsed milliseconds to `now()` snapshot taken at spec start, so coverage is deterministic regardless of when the spec runs or what questions are in the seed; first occurrence; if second spec ships with non-parametric fixture dates or a spec's coverage drifts due to seed-timing assumptions, revisit fixture-date pattern |
| Shared-seed red-team fixture coverage non-vacuous only against expected seed volume | 1 | 2026-05-31 | WATCHING — #673 design-critic pass (4fb5d2a4, 2026-05-31): test assertion "mastery ≥ 50%" on a 2-question aggregate requires ≥1 correct of ≥2 answers to be mathematically non-vacuous; plan-critic noted that if the seed has only 1 question in the subject, the test covers a vacuous case (can't achieve 2 answers); recommends parametric verification of fixture row counts at spec-load time to validate assertions are non-vacuous on actual seed state; first occurrence; if spec coverage drifts due to silent seed-state assumptions, revisit fixture-validation pattern |
| Read-then-write race on flag/state mutation (UPDATE predicate not atomic) | 1 | 2026-03-20 | Watch — toggleFlag in flag.ts (6520962, 2026-03-20): SELECT to check current flag state followed by UPDATE or UPSERT; rapid toggle-toggle from two tabs could leave flag in wrong state; fixed by adding .is('deleted_at', null) to the UPDATE predicate to make the unflag atomic, plus row-count check; distinct from "TOCTOU race on count-gated INSERT" (that is an INSERT gate — this is a state-change UPDATE that should be conditional on current column state); fix pattern: for any UPDATE that modifies a boolean/state column, include the expected current column value in the WHERE/predicate to make the operation atomic; first occurrence — log and watch |
| Return discriminant (error field) threaded through hooks but never rendered in UI | 1 | 2026-03-20 | Watch — d93f924 (fix: resolve 3 tech-debt issues): authError was passed as a return value through multiple hook layers but never consumed in the component tree; users received no feedback on auth failures; fixed in 5a68fa3 by adding a session-expired message and disabling the Start Quiz button when authError is set; distinct from "Auth error from getUser() swallowed without logging" (that is an error not destructured at all — this is an error correctly threaded through hooks but dropped at the UI boundary); root cause: when adding a new error discriminant to a hook's return type, there is no mechanical check that the caller actually renders it; first occurrence — log and watch |
| Node Current release (non-LTS) pinned in CI configuration | 1 | 2026-03-20 | Watch — d93f924 (fix: resolve 3 tech-debt issues): CI was configured with Node 25, which is a Current (odd-numbered) release, not an LTS release; Node Current releases receive fewer months of support and are not production-stable; fixed in 5a68fa3 by reverting CI to Node 22 LTS; applies to any CI matrix or .nvmrc update — always use an even-numbered Node version (LTS); first occurrence — log and watch |
| Silent error drop on non-critical secondary query | 1 | 2026-03-21 | Watch — 29b441f/4e217d1 (feat/fix quiz results redesign #178): `getEasaSubjects()` error was ignored; the function returned an empty array and the component rendered with no subject labels, with no server-side log; fixed in 4e217d1 by adding `console.error` on the error path; distinct from "Supabase mutation result not destructured" (that is a missing `{ error }` destructure on .insert/.update — this is a SELECT error on a non-critical enrichment query that is destructured but the error branch silently falls through without logging); first occurrence — log and watch |
| Hardcoded hex colors in SVG component bypassing oklch design token system | 1 | 2026-03-21 | Watch — 29b441f (feat/quiz results redesign #178): `score-ring.tsx` SVG used hardcoded hex values (#3b82f6, #22c55e, #f59e0b, #ef4444) rather than CSS custom properties from the oklch theme; browser cannot retheme or dark-mode override these values; flagged by semantic-reviewer as a SUGGESTION; a threshold comment documenting the hex values as intentional for SVG compatibility was added in 4e217d1 as a mitigation; root cause: SVG stroke/fill do not inherit CSS vars when set as SVG attributes (only when set as CSS properties via `style=`); applies to any SVG component with color-bearing attributes — prefer `style={{ stroke: 'var(--color-...)' }}` or `className` with Tailwind; first occurrence — log and watch |
| test-writer generates vi.mock factory referencing mock variable not declared via vi.hoisted | 1 | 2026-03-20 | Watch — 7c5b6ca/f846d96 (fix: resolve 4 maintenance issues): test-writer generated subject-row.test.tsx with `mockToastSuccess` and `mockToastError` referenced inside `vi.mock('sonner', ...)` factory but declared as plain `const` above the factory rather than via `vi.hoisted()`; vi.mock factories are hoisted to the top of the file by Vitest at compile time, so any variable referenced inside them must also be hoisted via `vi.hoisted()` — otherwise the variable is `undefined` at factory execution time; the bug was caught by the test run, fixed before commit; distinct from "Shared hoisted mock capture without beforeEach reset" (that is about a hoisted variable not being reset — this is about a non-hoisted variable being used inside a factory); test-writer patterns.md should note: every mock variable referenced inside a vi.mock() factory MUST be declared via vi.hoisted(), not as a plain const; first occurrence — log and watch |
| E2E helper function copied from sibling loses return value contract | 1 | 2026-03-20 | Watch — 7c5b6ca/f846d96 (fix: resolve 4 maintenance issues / test: add subject-row tests): ensureLoginTestUser was modeled after ensureTestUser but was not updated to return { orgId, userId }; semantic-reviewer flagged the asymmetry as a suggestion; fixed in f846d96 by adding the return value; root cause: when a helper is added by copying a sibling, the copy may omit return values or contracts that the original developed over time but that the new function did not have at the time it was written; first occurrence — log and watch |
| Semantic-reviewer ISSUE on intentional staged delivery or intentional design decision | 1 | 2026-03-20 | Watch — bc1725a/3aa5a6b/fd40227 (feat/fix quiz: PR #177): two ISSUEs flagged by semantic-reviewer were false positives — (1) flaggedIds empty array flagged as a behavior gap but intentionally staged empty until PR 5 wires the data source; (2) current color on answered-but-not-visited questions flagged as "hides feedback" but intentional per Paper Design spec; both confirmed by orchestrator as intentional design decisions and dismissed; root cause: semantic-reviewer lacks context about multi-PR delivery plans and external design specs; mitigation: document intentional gaps (staged delivery, design-spec-driven choices) in a commit message note or inline code comment so the reviewer has in-diff context; first occurrence — log and watch |
| Test mock type becoming stale after production type refactor | 1 | 2026-03-20 | Watch — fd40227 (fix/quiz PR #177): a test mock used a stale type shape that no longer matched the production interface after a refactor in an adjacent commit; flagged by semantic-reviewer as a SUGGESTION; fixed in fd40227 before commit; distinct from "test fixture shape mismatch" (that is a wrong field in a fixture object used as test data — this is the type annotation on a mock itself being stale); first occurrence — log and watch |
| Misleading test name (test name contradicts actual assertion body) | 1 | 2026-03-21 | Watch — ca55c3c (fix/reports #179): a test name described a different behavior than the assertion it contained; semantic-reviewer flagged as an ISSUE (misleading passing tests give false confidence); fixed by aligning the name to the assertion; distinct from "implementation-named test" (that is a style issue — this is an active contradiction between name and assertion); first occurrence — log and watch; if it recurs, add note to test-writer patterns memory: verify each test name accurately describes the assertion's postcondition |
| Multiple anchor tags per table row pointing to same destination | 1 | 2026-03-21 | Watch — ca55c3c (fix/reports #179): session history table row rendered two <a> tags (text link + icon link) both pointing to the same detail URL; screen readers and keyboard users hit the same destination twice; fixed by consolidating to single link with aria-label; first occurrence — if a second component renders duplicate same-destination links in a repeated row, add accessibility note to code-style.md Section 2 |
| stopPropagation on nested Link inside row onClick without explanatory comment | 1 | 2026-03-23 | Watch — 281b05f (fix/reports): `<Link onClick={(e) => e.stopPropagation()}>` inside a `<tr onClick={navigate}>` lacked an inline comment explaining the call prevents the row-level navigation from firing twice; semantic-reviewer flagged as SUGGESTION; not fixed in this cycle (comment-only improvement); first occurrence — if a second component ships stopPropagation inside a delegated-click container without a comment, add note to code-style.md Section 2: stopPropagation in nested interactive elements must include a comment explaining which parent handler it blocks and why |
| Hook file exceeding 80-line limit | 5 | 2026-03-23 | RULE EXISTS — use-quiz-config.ts at 110 lines (57ec870/0bc21e6); 5th occurrence; fixed by extracting calcFilteredAvailable helper to topic-tree-helpers.ts; pattern persists despite 70-line watch threshold; root cause: hooks grow incrementally through feature additions, each addition individually small but cumulatively breaching the limit; no new rule needed — existing rule and watch threshold are correct; the post-commit code-reviewer gate remains the reliable catch |
| Async callback (refetch) not wrapped in useCallback causing redundant server calls | 1 | 2026-03-23 | Watch — use-quiz-config.ts (57ec870/0bc21e6): `fc.refetch` passed as a useEffect dependency without useCallback wrapping; new function reference on every render triggered unnecessary refetch calls; fixed by wrapping refetch in useCallback with [fc] dependency; semantic-reviewer caught as ISSUE; distinct from "Unstable useEffect dependency (inline function prop)" (that is a prop passed down — this is a hook method reference captured in useEffect deps); first occurrence — log and watch |
| ZodError escaping Server Action with typed error return type (parse() without try/catch or safeParse) | 2 | 2026-03-26 | RULE CANDIDATE — first: checkAnswer in 199e927 (2026-03-15): ZodError propagated as unhandled exception instead of returning typed { success: false }; second: settings actions.ts in 552bb2f (2026-03-26): `parse()` used instead of `safeParse()` — unhandled ZodError escapes the return-type contract on invalid input; both fixed by switching to try/catch-wrapped parse or safeParse with explicit error-path return; second occurrence across different commits — RULE CANDIDATE: Server Actions must use `Schema.safeParse()` (or wrap `.parse()` in try/catch) so that invalid input returns a typed error response rather than throwing an unhandled exception that breaks the caller's return-type contract |
| Sensitive auth operation (password change) implemented as direct client-side Supabase call instead of Server Action | 1 | 2026-03-26 | Watch — 552bb2f (feat/settings PR #368): `change-password-form.tsx` called `supabase.auth.updateUser()` directly from the client component; password changes must flow through Server Actions so the audit trail (audit_events insert) can be recorded server-side before the auth mutation commits; semantic-reviewer caught as CRITICAL; fixed in d9e1d10 by moving the call to a `changePassword` Server Action that inserts an audit event first; distinct from "Server-authoritative ordering value computed client-side" (that is a data value — this is a security-sensitive auth operation); first occurrence — log and watch; if a second auth mutation (email change, MFA toggle) ships as a client-side call, add a rule note: all auth.updateUser/admin calls must live in Server Actions, not client components |
| Soft-delete guard missing on org lookup in Server Action (SECURITY DEFINER exemption does not apply here) | 1 | 2026-03-26 | Watch — 552bb2f (feat/settings PR #368): profile update Server Action queried the `organizations` table with `.eq('id', orgId)` but no `.is('deleted_at', null)` filter; a deactivated org's data would still resolve, allowing students of a soft-deleted org to update their profiles; semantic-reviewer caught as ISSUE; fixed in d9e1d10 by adding the soft-delete filter; distinct from "RPC missing AND deleted_at IS NULL guard on session fetch" (that is inside a SECURITY DEFINER RPC — this is a direct Supabase client query in a Server Action where RLS is active but the row-level filter on the org's deleted_at is not enforced by the student's own RLS policy); first occurrence — log and watch; if a second Server Action ships an org/user lookup without a soft-delete filter, add a note to code-style.md Section 5: ownership-scoping queries on soft-deletable tables must include `.is('deleted_at', null)` |
| Hardcoded cookie/constant values in tests instead of importing source constants | 2 | 2026-03-27 | RULE CANDIDATE — first: proxy.test.ts (consent commit, 2026-03-27): hardcoded cookie name and value as string literals instead of importing from production module; second: actions.ts used hardcoded 'v1.0' string literal for CURRENT_ANALYTICS_VERSION instead of importing the constant (CodeRabbit PR #385 cycle, 227c976); and E2E helper supabase.ts used hardcoded 'v1.0' for TOS/privacy versions instead of importing CURRENT_TOS_VERSION and CURRENT_PRIVACY_VERSION (same cycle); root cause: when a constant is defined in a production module, test authors duplicate the literal value rather than importing the exported constant; when the constant is renamed or the value changes, the test continues to pass while silently testing stale data; both caught by semantic-reviewer as ISSUE-level findings; second occurrence across different commits — RULE CANDIDATE: test files must import production constants rather than duplicating literal values; add note to test-writer patterns memory |
| GRANT EXECUTE missing after CREATE OR REPLACE on SECURITY DEFINER RPC in migration | 1 | 2026-03-27 | Watch — migration 058 (44e305f/d0e5aed): `record_consent()` RPC was replaced with CREATE OR REPLACE but GRANT EXECUTE to authenticated was not re-stated; Postgres revokes execute rights when a function's owner changes via CREATE OR REPLACE; any authenticated call to the RPC would fail with permission denied; caught as ISSUE by semantic-reviewer post-commit; fixed in d0e5aed by appending GRANT EXECUTE after CREATE OR REPLACE; first occurrence — log and watch; if a second migration ships CREATE OR REPLACE on a SECURITY DEFINER function without a GRANT EXECUTE statement, add a rule note to security.md: "Every CREATE OR REPLACE of a SECURITY DEFINER function must be followed by GRANT EXECUTE ON FUNCTION ... TO authenticated" |
| E2E seed helper missing version filter (stale consent rows survive version bump) | 1 | 2026-03-27 | Watch (carry-forward suggestion — 4th consecutive cycle) — `ensureConsentRecords` in `apps/web/e2e/helpers/supabase.ts` checks for the existence of consent rows without filtering by `document_version`; after a version bump, the helper skips re-seeding because old-version rows are already present, leaving test users with stale consent records that fail the gate; flagged by semantic-reviewer as a SUGGESTION in cycles 227c976, b35a7c1, 44e305f (now 3 suggestion cycles + this 4th); not yet addressed; semantic-reviewer has flagged this 4 consecutive cycles; first entry into learner frequency table; this pattern crosses the "watch and log" threshold due to repetition frequency — GitHub issue should be created to track |
| Deep JSX nesting from repeated pattern (3x repetition causing 5-level nest) | 1 | 2026-03-27 | Watch — consent commit (2026-03-27): consent-form.tsx repeated a checkbox+label+description JSX pattern 3 times; each repetition added nesting depth, pushing the component to 5 levels (limit 3); caught as BLOCKING by code-reviewer; fixed by extracting ConsentCheckbox sub-component; distinct from "extract at 3 repetitions" rule (Section 2 applies at any nesting level — this is the specific case where the 3-repetition trigger coincides with a nesting-depth violation); first occurrence of this specific mechanism (repetition causing nesting violation) as a named pattern — log and watch; the existing "extract at 3 repetitions" rule in code-style.md Section 2 is the correct gate; no additional rule needed |
| RLS SELECT policy missing for student role on table read via RLS-scoped client in multi-client feature | 1 | 2026-03-27 | Watch — GDPR data export (7eeff14/0bad818): collect-user-data.ts used the anon/authenticated Supabase client to fetch audit_events for a student's own export; audit_events had no SELECT RLS policy for the authenticated role (admin-only by design), so the query silently returned empty data; caught as a valid ISSUE by semantic-reviewer post-commit; fixed in 0bad818 by adding a SELECT RLS policy scoped to auth.uid(); root cause: when a feature adds a code path that reads a table via the RLS-scoped client (not adminClient), each table in that path must be audited for an appropriate SELECT policy for the authenticated role — the fact that another path in the same feature uses adminClient does not guarantee the student-client path is covered; distinct from "Query missing student_id scope" (that is a missing WHERE clause — this is a missing RLS policy); distinct from "GRANT EXECUTE missing on SECURITY DEFINER RPC" (that is a function permission — this is a table row-level policy); Supabase returns 200 OK with empty data when RLS blocks a SELECT, making the gap silent at runtime (no error to destructure); first occurrence — log and watch |
| Semantic reviewer false positive due to stale-baseline comparison (wrong migration version) | 2 | 2026-06-06 | RULE CANDIDATE — (1) GDPR export (7eeff14, 2026-03-27): reviewer claimed quiz_sessions had no deleted_at column, missed earlier migration 023; (2) Batch-A fix re-review (f35b2a16, 2026-06-06): reviewer compared record_consent against mig 057 (ORIGINAL) instead of latest predecessor 20260327000058 which had already narrowed the document_type whitelist; reviewer also inverted the NOT IN logic direction (claimed dropped value "would pass INSERT" when NOT IN rejects it). Both cases: reviewer did not trace the CREATE OR REPLACE chain or CHECK constraint chain to the latest definition. Same Pre-Flag Verification failure mode as CR-local (tracker row). Mitigation: before acting on any semantic-reviewer finding about schema state, grep migrations for the specific table/function to find the LATEST definition — do not accept a finding based on an older migration alone. |
| Decorative icon in interactive element missing aria-hidden | 1 | 2026-03-27 | Watch — report-question-row.tsx (539222e/10263b3): ChevronUp/ChevronDown lucide icons inside a button lacked aria-hidden; screen readers would announce "chevron up" alongside "Hide explanation" button text, creating redundant noise; caught by semantic-reviewer as a SUGGESTION; fixed in 10263b3 by adding aria-hidden to both icons; distinct from "ARIA tab role missing on button-based tab UI" (that is about missing structural ARIA roles — this is about decorative icons polluting the accessible name of their interactive parent); first occurrence — log and watch; if a second component ships decorative icons without aria-hidden inside buttons or links, add a note to code-style.md Section 2: decorative icons (lucide or SVG) inside interactive elements must always have aria-hidden |
| Generic alt text for contextual image (alt="Explanation illustration") | 1 | 2026-03-27 | Watch — report-question-row.tsx (539222e): ZoomableImage for explanation diagrams used alt="Explanation illustration" regardless of question content; the alt text should describe the specific diagram or be derived from the question context; caught by semantic-reviewer as a SUGGESTION; not fixed in this cycle (context-specific alt text would require passing description data through the data layer); first occurrence — log and watch; the fix path would be: add an explanation_image_alt column to the questions schema and propagate it through the quiz-report query |
| New test file shipped without vi.resetAllMocks() in beforeEach | 2 | 2026-03-27 | PROMOTED → test-writer/patterns.md (rule exists; compliance gap at authoring time) — first: report-question-row.test.tsx (539222e/10263b3): test file written with mocks but missing beforeEach(() => { vi.resetAllMocks() }), allowing mock state to bleed between tests; caught by semantic-reviewer as a SUGGESTION; fixed in 10263b3 by adding beforeEach with the call; second occurrence — the rule exists in test-writer/patterns.md (§ Mock patterns) and the agent-test-writer.md rule file mandates vi.resetAllMocks() in beforeEach; the pattern persists at authoring time; the semantic-reviewer catch is the reliable gate; no additional rule change needed — rule is documented, compliance gap at generation time; distinct from "Shared hoisted mock capture without beforeEach reset" (that is a specific hoisted variable needing its own reset — this is the absence of the global vi.resetAllMocks() call entirely) |
| Supabase error mock shape wrong: `{data: [], error}` instead of `{data: null, error}` | 1 | 2026-03-28 | Watch — GDPR collect-user-data.test.ts (09e1be1): test mock helper returned `{ data: [], error: someError }` when simulating a Supabase query failure; real Supabase client always returns `{ data: null, error }` on failure — never `{ data: [], error }`; the wrong shape meant error-path tests were exercising code with a non-production input shape, making fallback assertions like `?? []` pass for the wrong reason; fixed in 09e1be1 by changing all error-path mock entries to `data: errors.xError ? null : actualData`; root cause: test authors write `data: []` as an intuitive "empty" value without checking the actual Supabase contract; the correct rule: a Supabase mock that returns a non-null error MUST set data to null, not to an empty array; first occurrence — log and watch; if a second test file ships with this pattern, update test-writer patterns.md with an explicit rule under the mock shape section |
| Lockfile orphaned stale branch (pre-existing unrelated version branches from prior dep bumps) | 1 | 2026-05-27 | Watch — e351aca0 (fix(deps)): pnpm install --no-frozen-lockfile during postcss override regeneration pruned a stale vite@8.0.13 branch (rolldown@1.0.1 bindings, react-dom@19.2.4) that was not involved in any recent commit — pre-existing orphan from a prior dep-bump or lock file merge; root cause: lockfile merges or abandoned branches from rollbacks leave stale version branches that do not appear in any active dependency path; when regenerating a lockfile, a clean `pnpm install` (not --frozen-lockfile) prunes these automatically; pattern: if lockfile-orphan drift recurs on future dep commits (especially near merges or after branch rebases), it may indicate a process gap where contributors are not regenerating the lockfile cleanly or are committing partial-updated locks; first occurrence — log and watch for recurrence; not a rule-change candidate yet (count=1) |
| jsdom PointerEvent gaps (setPointerCapture not implemented, pageX not in PointerEventInit) | 1 | 2026-03-29 | Watch — use-drag-scroll.test.ts (ca09f34): test-writer initially generated PointerEvent dispatches without `pointerId` in the init dict, causing `setPointerCapture` to throw "not implemented"; and used `pageX` directly in `new PointerEvent(...)` init without `Object.defineProperty` (jsdom does not map pageX from init dict); fixed by: (1) always including `pointerId: 1` in PointerEvent init, (2) setting `pageX` via `Object.defineProperty(event, 'pageX', { value: N })` after construction; rule now in test-writer/patterns.md (§ Dispatching PointerEvents in jsdom); first occurrence in learner table — log and watch; if a second test file ships with the same jsdom PointerEvent gaps, no additional rule change needed (test-writer memory already updated) |
| test-writer generates tests requiring jsdom compatibility fixes before they pass | 3 | 2026-03-29 | RULE IN MEMORY (test-writer/patterns.md) — first (153a975): TS2532 unchecked array index; second (various): deprecated vi.fn generic syntax; third (ca09f34): PointerEvent jsdom gaps (setPointerCapture, pageX); all required a fix cycle before tests could be committed; the pattern is consistent: test-writer generates logically correct tests but hits jsdom API limitations or TypeScript strictness constraints that are not obvious at generation time; the fix cycle remains the reliable gate; test-writer/patterns.md is the correct place to document these constraints (already updated for each occurrence); no code-style.md rule change needed |
| Redundant `afterEach(cleanup)` in test files (stale boilerplate copy-paste) | 1 | 2026-04-08 | Watch — c84a079: 6 admin test files contained `afterEach(cleanup)` from `@testing-library/react`; Testing Library v13+ registers cleanup automatically via a globally registered afterEach hook; the explicit call is idempotent but dead code that inflates boilerplate; root cause: copy-paste propagation of a stale snippet from an older test-writer pattern; first occurrence — log and watch; if it recurs in test-writer output, add note to test-writer/patterns.md: "Do not add afterEach(cleanup) — v13+ cleanup is automatic" |
| Base UI component inline `style` prop conflicting with internal style injection | 1 | 2026-04-09 | Watch — 995de26: CollapsibleContent received an inline `style={{ height: ... }}` prop to drive the collapse animation; Base UI injects its own `--collapsible-panel-height` CSS variable via the same style attribute, so the inline prop overrode it and broke the open/close animation; caught as ISSUE by semantic-reviewer and implementation-critic; fixed in 4459a5d by removing the inline style and using only Tailwind `data-[state=open]:*` classes; first occurrence — log and watch; applies to any Base UI component that drives state-dependent animation via CSS variable injection (Collapsible, Accordion, Dialog) — prefer Tailwind variants over inline styles on these components |
| Test assumes component renders children unconditionally but real component starts collapsed | 1 | 2026-04-09 | Watch — 995de26/4459a5d: test clicked a row inside CollapsibleContent without first clicking the trigger to open the panel; the panel starts closed and children are not rendered until it is opened; assertion on the row content silently passed in the mock (which always rendered children) but would fail against the real component; caught as ISSUE by semantic-reviewer; fixed in 4459a5d by adding a trigger click before the row interaction; first occurrence — log and watch; applies to any test of a component that contains interactive children inside a conditionally-rendered container (Collapsible, Accordion, Dialog, Select) — always open the container before asserting on or interacting with its contents |
| `require()` inside `vi.hoisted()` factory producing unresolvable type namespace (`R.ReactNode`) | 1 | 2026-04-09 | Watch — 4459a5d: test-writer used `require('react')` inside a `vi.hoisted()` factory to access React types, producing `R.ReactNode` reference errors (TypeScript cannot resolve the namespace from a runtime require() call); fixed by replacing with a top-level import and using `React.ReactNode` directly; distinct from "vi.mock factory referencing non-hoisted variable" (that is a variable undefined at hoist time — this is a module required inside the factory producing an unresolvable type namespace at compile time); first occurrence — log and watch; if recurs, update test-writer patterns.md: "Do not use require() inside vi.hoisted() factories — use top-level import and reference the type directly" |
| Unreachable SQL aggregate fallback (`?? value` applied to COUNT(*) which never returns NULL) | 1 | 2026-04-10 | Watch — 9370e7d/9711338: `?? r.total_questions` fallback applied to a COUNT(*) alias in an RPC result row; SQL COUNT(*) aggregates always return an integer (0 for empty sets), never NULL — the fallback was therefore dead code that could never execute; caught as ISSUE by implementation-critic pre-commit; fixed in 9711338 by removing the fallback; distinct from "Silent numeric fallback without observability logging" (that is a fallback that runs silently — this is a fallback that cannot run at all); first occurrence — log and watch; applies to any TS fallback applied to a SQL COUNT(*), COUNT(x), SUM(x) COALESCE-wrapped, or other aggregate that is guaranteed non-null by the SQL spec or explicit COALESCE in the query |
| UI interactive element changed without a corresponding test update | 1 | 2026-04-10 | Watch — 9370e7d: a sort button was added to the subject selector UI but no test covered the new sort interaction; test-writer caught the gap post-commit and added 3 tests (sort button presence, initial state, re-sort behavior); distinct from "New hook/utility file extracted without shipping tests" (that is a new file — this is an existing component gaining a new interactive element with no test update); root cause: the component had existing tests, so the author assumed coverage was sufficient; the correct rule: whenever a new interactive element (button, toggle, input) is added to a component that has a test file, the test file must be updated in the same commit; first occurrence — log and watch |
| Rule documentation self-claim drift (governance doc claims wrong deployed state) | 1 | 2026-05-30 | Watch — 4eaa696b (docs: apply 3 PR-sweep SUGGESTIONs): agent-memory.md claimed the 4-col `Issue Type|Count|Last Seen|Status` form was "the deployed schema across the MEMORY.md files", but learner is the only agent using 4-col; the other 5 trackers (code-reviewer, semantic-reviewer, test-writer, plan-critic, implementation-critic) use 5-col forms or other variations; the inaccuracy was self-corrected via PR #704 semantic-reviewer sweep (SUGGESTION level); root cause: governance rule text written to describe a single agent's schema but stated as universal across all agents without verification; distinct from "Undefined severity level used in agent rule" (that was a schema definition missing from the agent's severity table — this is a rule file making a false claim about what has been deployed elsewhere); semantic-reviewer flagged as SUGGESTION; no rule change needed; first occurrence — log and watch for recurrence on future governance doc edits |
| `server-only` import removed due to Vitest/jsdom resolution failure (test environment incompatibility) | 1 | 2026-04-11 | Watch — f5c1070/cb318f4 (feat/exam-mode-admin-config): replace-distributions.ts had a `server-only` guard added for correctness (Server Action should not be importable in client modules); the guard was removed in cb318f4 after Vitest could not resolve the `server-only` package in jsdom — the import threw at test-runner time, blocking all tests in the file; root cause: `server-only` is a runtime Next.js guard that relies on the Next.js build system to throw at bundle time; Vitest does not emulate Next.js's module resolver, so the package resolution fails entirely in the jsdom test environment; mitigation already in use for other modules: mock the module in vitest.setup.ts via `vi.mock('server-only', () => ({}))` — this was not applied here; first occurrence as a named pattern — log and watch; if a second Server Action with `server-only` has its guard removed to fix test failures, add a note to test-writer/patterns.md: "If a test imports a Server Action that imports `server-only`, add `vi.mock('server-only', () => ({}))` to the test file or vitest setup rather than removing the guard" |
| Combined `if (error \|\| !data)` guard in Server Action logs `undefined` on the `!data` branch | 1 | 2026-04-11 | Watch — f5c1070 (feat/exam-mode-admin-config): a guard written as `if (error \|\| !data) { console.error('[fn] error:', error.message) }` causes `error.message` to be `undefined` when the branch is entered because `!data` is true but `error` is null (valid Supabase state: query succeeded with zero rows); semantic-reviewer caught as ISSUE; fixed in cb318f4 by splitting into two guards: `if (error) { console.error(..., error.message) }` followed by `if (!data) { console.error(..., 'no data returned') }`; root cause: combining two semantically distinct failure modes (DB error vs. empty result) into a single guard creates an error log that always accesses `error.message` even when error is null; distinct from "Supabase mutation result not destructured" (that is about missing destructure entirely — this is a destructured result where the error-logging assumes a non-null error in a branch where error may be null); first occurrence — log and watch; the correct pattern: separate `if (error)` and `if (!data)` guards with distinct log messages appropriate to each failure mode |
| CodeRabbit false positive rate elevated on exam-mode PRs | 2 | 2026-04-14 | RULE CANDIDATE — first: PR #516 (2026-04-11): 4 of 12 findings (33%) false positives — immutable table warnings on ephemeral distributions table, shadcn Dialog size prop, num-field type annotation, start_exam_session guard already present; second: PR #523 (2026-04-14): 6 of 9 findings (67%) false positives — DB constraint prevents zero-count (CodeRabbit missed array guard existed at DB level), client guard prevents empty arrays (missed existing guard), exam sessions never recovered from localStorage (flagged missing recovery logic that is intentionally absent); root cause consistent: CodeRabbit lacks project-specific context — explicit hard-delete exceptions, documented intentional decisions, DB-level constraints that make application guards redundant; second occurrence confirms pattern — per agent rules, consider adding project-specific suppression notes to .coderabbit.yaml via coderabbit-sync agent for these exception categories |
| `userEvent.clear()` + `userEvent.type()` pattern unreliable for controlled NumField components — use `fireEvent.change()` | 1 | 2026-04-11 | Watch — 80cbd7c (feat/exam-mode-admin-config): test-writer initially generated NumField interaction tests using `userEvent.clear()` followed by `userEvent.type()` to simulate clearing and retyping a value in a controlled number input; the pattern failed because userEvent v14 dispatches a sequence of keyboard events and intermediate states can conflict with React's controlled-input reconciliation, causing the final value to diverge from the expected string; fixed by switching to `fireEvent.change(input, { target: { value: '...' } })` which directly sets the value and fires a single change event, matching how React controlled inputs process synthetic events; distinct from "jsdom PointerEvent gaps" (that is a missing API — this is an event dispatch strategy mismatch between userEvent's keyboard simulation and controlled inputs); first occurrence — log and watch; if a second test file ships with userEvent on a controlled numeric input and fails, update test-writer/patterns.md: "For controlled number inputs, prefer fireEvent.change() over userEvent.clear() + userEvent.type() — the keyboard simulation produces intermediate states that conflict with controlled-input reconciliation" |
| Dead code (unused scaffold function) left in test file by test subagent | 1 | 2026-04-11 | Watch — 05609a7 (feat/exam-mode-admin-config): test subagent generated queries.test.ts with an unused `buildChain` scaffold function that was never called in any test; Biome's `noUnusedVariables` rule would have blocked the pre-commit hook; implementation-critic caught the dead code pre-commit and it was removed before commit; root cause: test-writer generates scaffold code (mock builders, helper wrappers) during an incremental writing process but does not always clean up intermediate scaffolding before reporting; the implementation-critic pre-commit gate is the reliable catch for this pattern; first occurrence as a named pattern — log and watch |
| TypeScript strict mode requires `!` non-null assertion on array index access in test files | 2 | 2026-04-11 | RULE IN MEMORY (test-writer/patterns.md §Array index safety) — first occurrence: 153a975 (2026-03-23, session-table.test.tsx); second occurrence: 05609a7 (2026-04-11, exam-config test files): test-writer accessed `results[0]`, `calls[0]`, and similar array index expressions without guarding against undefined, triggering TS2532 in strict mode; fixed with non-null assertion (`results[0]!`) after a `toHaveLength(N)` assertion that proves the element exists; count updated 2 → 3 in test-writer patterns (this row carries the learner count separately for cross-pattern tracking); now at 3 total occurrences; test-writer/patterns.md rule exists and has been updated twice; the agent still generates the wrong form first; the pre-commit tsc gate is the reliable catch |
| SonarCloud new-code coverage gap: test files required for ALL source files with runtime logic | 1 | 2026-04-11 | Watch — 05609a7 + 80cbd7c (feat/exam-mode-admin-config): SonarCloud new-code coverage gate required test files not only for Server Action files (actions.ts, queries.ts) but also for all React components and hooks with runtime logic added in the PR (distribution-editor.tsx, subject-config-card.tsx, config-form-dialog.tsx, page-shell.tsx, num-field.tsx); components that render conditional logic, derive computed values, or handle user events all contribute to new-code line coverage; 52 + 25 = 77 tests added across 7 new test files to satisfy the 80% new-code gate; distinct from "New hook/utility file extracted without shipping tests" (that is about new files without any test — this is about the coverage threshold requiring tests for every file with branching logic, not just the action/utility layer); first occurrence as a named pattern — log and watch; implication: when planning a multi-file feature, include test files for all React component files with conditional rendering or event handling, not just the Server Action layer |
| Semantic-reviewer false positive: `isRedirectError` re-throw rule misapplied to client-side Server Action callers | 1 | 2026-04-13 | Watch — 1395d92 (feat/exam-mode-admin-config): semantic-reviewer flagged catch blocks in a `'use client'` component calling a Server Action via `startTransition` for not re-throwing `isRedirectError()`. The rule in code-style.md Section 6 explicitly applies to Server Component catch blocks only. The five existing `rethrowRedirect` usages in the codebase are all in Server Components (dashboard content files). A client component calling a Server Action via `startTransition` cannot intercept `redirect()` as a thrown exception — Next.js routes redirect responses through the response stream, not as JavaScript exceptions in the client component call stack. The `isRedirectError` re-throw pattern is meaningless and harmful in this context. Confirmed false positive — no code change made. First occurrence as a named false positive type — log and watch. If semantic-reviewer repeats this false positive on a second `'use client'` + `startTransition` + Server Action pattern, add a Known Suppression note to `agent-code-reviewer.md`: "`isRedirectError` re-throw is not applicable in client components calling Server Actions — redirect responses do not propagate as JavaScript exceptions to the client" |
| Unnecessary `async` on a sync function (misleading return type) | 1 | 2026-04-14 | Watch — 6954711 (feat/exam-mode-student-session): a function performing no async operations was declared `async`, making its return type `Promise<T>` when actual return is synchronous `T`; CodeRabbit caught as valid finding; removed the keyword; misleads callers and causes unnecessary `await` expressions (including in tests that persist after a sync refactor); first occurrence — log and watch; if a second function ships with this pattern, propose note to code-style.md: "Only declare `async` if the function body contains `await` expressions or returns a delegated Promise" |
| Object construction per render instead of reading from existing ref | 1 | 2026-04-14 | Watch — 6954711 (feat/exam-mode-student-session): `new Map()` constructed fresh on every render path instead of reading from already-initialized `useRef`; causes unnecessary allocation per render and can silently discard accumulated state; CodeRabbit caught as valid finding; fixed by reading the existing ref; first occurrence — log and watch; if a second component constructs a `new Map()`, `new Set()`, or equivalent collection literal in the render body where a ref holds the canonical instance, propose note to code-style.md Section 6: "Collections tracking session state must be initialized in useRef, not re-constructed per render" |
| Stale `await` on now-sync function in test file | 1 | 2026-04-14 | Watch — bdeafc3 (feat/exam-mode-student-session): semantic-reviewer (SUGGESTION) noted a test file `await`s a function refactored from async to sync; `await` on a non-Promise resolves immediately so the test still passes but the signature is misleading; root cause: when a function is made sync, callers (including test files) that use `await` are not updated; first occurrence — log and watch; no rule change at count 1 |
| `rerender()` with identical prop value produces a vacuous test | 1 | 2026-04-14 | Watch — 1ebe955 (feat/exam-mode-student-session): `rerender({ seconds: 2 })` passed same value as initial render, so the `useEffect` dependency never changed, the effect never re-ran, and the test was a vacuous pass; fixed by changing to `seconds: 3`; caught by CodeRabbit (not post-commit agents — vacuous assertion is outside agent scope); first occurrence — log and watch; if second test found with same pattern, propose note to code-style.md Section 7: "`rerender()` for effect re-run tests must pass a value that differs from the previously rendered value" |
| Fire-and-forget Server Action before navigation creates race with DB guards | 1 | 2026-04-14 | Watch — b9829c0/ce67aee (feat/exam-mode-student-session): `discardQuiz()` called without `await` before `router.push()`; `start_exam_session` guard could see the old session row if the router push won the race; semantic-reviewer caught as ISSUE; fixed by awaiting the Server Action before redirecting; first occurrence — log and watch; root cause: authors omit `await` on cleanup/mutation calls before navigation because navigation feels like the critical path; any Server Action that mutates DB state which the next route reads must be awaited before navigation |
| Server Action invoked after router.push/replace cancels pending App Router soft navigation | 1 | 2026-06-01 | WATCHING — #568 (68216d56/a61fcb24): `clearDeploymentPin()` Server Action called AFTER `router.push()` in `handleSubmitSession` allowed the router push to be cancelled by the pending Server Action, stranding the user on the current page instead of advancing to the report; root cause: Next.js App Router soft navigation is in-flight until no pending Server Actions exist; calling a Server Action after push cancels the in-flight navigation; fix: fire the Server Action BEFORE router.push is the last statement (confirmed via e2e repro + instrumentation); semantic-reviewer did a same-pattern sweep (10 router.push/replace sites in quiz feature) — found no other instances — every other site already fires Server Actions before push; code-reviewer (non-blocking WARNING): handleSubmitSession grew 58→65 lines (pre-existing >30 violation, worsened by +5-line explanatory comment); impl-critic APPROVED (unit test call-order would be impl-detail leakage, e2e regression is correct level); test-writer added 2 behavior-focused tests (failure/rejection paths still clear pin) — 68 pass; first occurrence — log and watch; distinct from fire-and-forget race pattern (that is await-before-push; this is invoke-before-push order) |
| Server Action mock not configured in test — undefined default hides integration gap | 1 | 2026-04-14 | Watch — 5e2fca9 (feat/exam-mode-student-session): exam timeout tests called `discardQuiz` but the mock was not given a return value, so it resolved to `undefined`; test assertions passed vacuously (no integration gap visible) but the success/failure path was never exercised; test-writer explicitly configured `mockDiscardQuiz.mockResolvedValue({ success: true })` to make the test meaningful; first occurrence — log and watch; distinct from "test fixture shape mismatch" (that is wrong field data — this is a missing mock return value that leaves a Server Action integration point unverified); if a second test is found where a Server Action mock has no return value configured and the caller checks the result, add note to test-writer patterns.md: "Always configure Server Action mocks with an explicit return value matching the action's return type — the default undefined obscures whether the caller handles the result correctly" |
| mock-boundary-blindness (router.push asserted with .toHaveBeenCalled() only, no URL argument) | 1 | 2026-04-27 | RULE ADDED — PR #523 round 7: a wrong-redirect bug was invisible because the test asserted `expect(mockPush).toHaveBeenCalled()` without checking the destination path; the implementation routed to the wrong URL but the test passed; rule added to code-style.md §7 (Assert URL on Router-Navigation Mocks) and agent checklist (flag-router-mock-no-url). Applies from 2026-04-27 onward. Existing tests migrated as touched. |
| mode-flag-as-flag-not-flow (feature mode flag tested as a toggle, never as a full lifecycle flow) | 1 | 2026-04-27 | RULE ADDED — PR #523: `isExam` toggle tests existed but no test connected start → in-progress → timer-expiry → redirect; the lifecycle gap made the wrong-redirect bug invisible at the component and hook level; rule added to code-style.md §7 (Lifecycle Integration Test for New Feature Modes) and agent checklist (flag-mode-flag-no-lifecycle). Count: 1. |
| stateful-ui-no-reload-test (stateful UI with in-memory or localStorage state shipped without a reload/recovery test) | 1 | 2026-04-27 | RULE ADDED — PR #523 exam refresh-resume bug: no test reloaded the page mid-exam; the gap between localStorage state and server session recovery was undetected until CodeRabbit caught it in production review; rule added to code-style.md §7 (Refresh / Reload Test for Stateful UI) and agent checklist (flag-stateful-flow-no-reload). Count: 1. |
| audit-actor-subquery-soft-delete (audit_events INSERT subqueries missing deleted_at IS NULL on FK lookups) | 3 | 2026-04-27 | RULE ADDED — count bumped to 3: first (#550 batch_submit_quiz, bdeafc3 2026-04-14), second (complete_empty_exam_session 375bde5 2026-04-27), third (cross-reference in 8782a18 2026-04-27); pattern hit count=3 — promoted from RULE PROPOSED to hard rule in security.md §10. Every INSERT INTO audit_events must filter deleted_at IS NULL on all FK subquery lookups (actor_id, actor_role, session-derived columns). |
| Dual-source UI surface (new feature + pre-existing fallback writing to same key) | 1 | 2026-04-28 | Watch — 82e64de (fix/quiz): new ResumeExamBanner (server-sourced via getActiveExamSession) shipped alongside pre-existing QuizRecoveryBanner (localStorage-sourced via useQuizRecovery); both read from quiz-active-session key; when exam mode began writing mode:exam entries to that key, both surfaces rendered simultaneously. Root cause: new feature created new session type using shared storage key without suppressing legacy client-sourced surface. Fixed by filtering exam-mode entries in useQuizRecovery. First named occurrence — log and watch. Watch for: (1) other localStorage keys where multiple session types write, (2) UI surfaces rendering from shared localStorage without mode-awareness, (3) manual eval findings invisible to unit tests.
| Suppression-without-failure-path (filtering one source creates blind spot if canonical source fails) | 1 | 2026-04-28 | Watch — 82e64de (fix/quiz): suppressing exam-mode entries in useQuizRecovery did not handle getActiveExamSession() failure (DB or auth error). When canonical source failed, both ResumeExamBanner and suppressed QuizRecoveryBanner rendered empty, leaving zero recovery surfaces with no error signal. Semantic-reviewer ISSUE: "suppression introduces silent-failure path". Root cause: suppression correct in success path (two surfaces wrong), incomplete in failure path (zero surfaces with no signal also wrong). Fixed in 53b8498 with role=alert error notice when lookup fails. First named occurrence — log and watch. Pattern: any suppression of fallback surface must pair with explicit error handling on canonical source.
| Manual-eval bug invisible to unit tests (dual-source UI rendering only visible in full app context) | 2 | 2026-04-28 | count 2 — first: 2026-04-27 PR #523 round 7 (refresh-resume bug: localStorage state interacts with server component render, invisible in jsdom); second: 82e64de (dual-source banner rendering: both ResumeExamBanner + QuizRecoveryBanner render simultaneously when exam in progress, visible only in full app context with both surfaces mounted). Root cause: jsdom test environments run components in isolation; no co-located test exercises scenario where both surfaces mounted on same page with overlapping session data. Pattern recurrence actionable: for any new session-recovery or stateful feature shipping with both server-sourced and client-sourced surfaces, require integration test (or E2E test) mounting both surfaces and verifying only one renders for given session state. Distinct from "Refresh / Reload Test" (code-style.md §7, count 1) which covers page-reload; this covers dual-surface rendering in same render. |
| UPDATE in migration missing compound-key scoping (UNIQUE parent_id, code table) | 1 | 2026-05-07 | Watch — 287b98d (082 topics seed/fix): code-only scoping; CodeRabbit caught both migrations; fixed by adding subject_id join; if second seed/fix cycle has same pattern, propose guidance in database.md or code-style.md §SQL migration rules |
| CR/LF control character assumed rejected at transport (JSON encodes as escape sequences, not stripped) | 1 | 2026-05-07 | Watch — c45330f: test assumed CR/LF would be rejected by PostgREST but JSON.stringify encodes as `\r`, `\n` escape sequences; RPC must explicitly validate; if second occurrence, add security.md guidance: "Control-character validation must occur at application boundary, not transport layer" |
| Test fixture seeded with wrong actor role (misrepresents threat model) | 1 | 2026-05-07 | Watch — c45330f XSS test: malicious questions seeded with studentUserId as author (students cannot create questions). Real attack is guest form injection. Root cause: test author did not verify fixture actor matched real attack vector. If second threat-model mismatch found, add code-style.md §7 rule: "Threat-model tests must seed data via actual attack permission path" |
| Single fixture shared across negative-test loop (brittleness risk) | 1 | 2026-05-07 | Watch — c45330f: `submit_quiz_answer` fixture created once, mutated and reused across 8 negative tests (empty answer, no question, expired session, etc.). If any mutation happened to match a real value, assertion would pass vacuously. Fixed in abc745c by generating per-test fixture. If second shared-fixture negative loop found, promote to rule: "Each negative test must mutate its own fixture instance" |
| Incomplete audit event_type enumeration (missed internal_exam.completed) | 1 | 2026-05-07 | Watch — c45330f: author thought "8 untested event types" but missed internal_exam.completed from batch_submit_quiz mode='internal_exam' path. Root cause: did not enumerate ALL event_type literals across both RPC implementations. If second event-type enum gap found, add rule: "Audit coverage tests must grep ALL event_type literals in codebase, not just main path" |
| Numeric counts in steering docs (fragile, go stale fast) | 2 | 2026-06-06 | RULE CANDIDATE — **Merged with "Doc numeric spec-count drift" row below (same mechanism, same tables).** First: tech.md had "9 specs" in 3 places; count became stale when 10th spec added. Second: #553 (9a728afd batch) count "18" stale in tech.md at L85/L146/L183 AND decisions.md L481 — drifted to 36; plan-critic caught plan updating only L85 (1 of 3 locations). Lifted to live tracker. Proposal: require atomic grep-and-update of ALL spec-count occurrences (or replace with structural descriptions). |
| Stale `why` annotations on test payloads after guard mechanism change | 2 | 2026-05-07 | RULE CANDIDATE (count=2 per state machine; learner deferred promotion — both occurrences share file/migration, not distinct mechanism) — both occurrences in `apps/web/e2e/redteam/helpers/payloads.ts` WHITESPACE_PAYLOADS table; first: `spaces-only` and `tabs-only` referenced `btrim()` after migration `20260507000001` switched the guard to POSIX `^[[:space:]]*$` (semantic-reviewer 61883bd); second: `cr-only` lacked the JSON-encoding transport note that `newlines-only` had after the table grew (semantic-reviewer 5f1295c). Effective root cause: same file, same migration cycle. Promotion deferred until count=2 across **distinct** payload tables / feature areas. |
| Server Action ERROR_MESSAGES not synchronized with new RPC `RAISE EXCEPTION` literals | 3 | 2026-06-19 | RULE CANDIDATE (count=3 — threshold reached; propose rule) — (1+2) `apps/web/app/app/admin/internal-exams/actions/void-code.ts` ERROR_MESSAGES missed `invalid_reason` after mig `20260507000001` added the guard (semantic-reviewer 61883bd); same file missed `session_state_changed` from mig `20260430000006` (semantic-reviewer 5f1295c sweep). (3) VFR RT Phase B cycle (2026-06-19, commits 0dedd3c4+2bbf0252): semantic-reviewer caught incomplete error-token map in start/submit VFR RT Server Actions vs the RPC's actual RAISE set; fixed in 2bbf0252. Three distinct RPC/Action families (void-code, start-VFR-RT, submit-VFR-RT). UX gap: unhandled tokens fall through to generic message. Rule shape: "every `RAISE EXCEPTION '<code>'` in a SECURITY DEFINER RPC must have a matching `<code>: '<user message>'` entry in the calling Server Action's error-token map; both must be updated in the same commit as the migration." Propose adding to agent-semantic-reviewer.md checklist. Sweep required: grep `supabase/migrations/**/*.sql` for `RAISE EXCEPTION` literals and cross-check all `apps/web/**/actions/*.ts` ERROR_MESSAGES objects for coverage gaps. |
| Edge Middleware response-path security-header parity (3xx vs 4xx/5xx) | 1 | 2026-05-07 | WATCH — `apps/web/proxy.ts` had security headers on 3xx redirects (commit ef53aa3) but not on 4xx (403) or 5xx (503) responses returned from the same middleware function. Fix in 5f1295c extracted `applySecurityHeaders` helper and applied it to all three branches. Single-cycle finding. Promotion deferred until count=2 in different middleware functions / projects. |
| Session-rotation footgun: shared E2E test user invalidates saved auth state | 1 | 2026-05-07 | WATCH — 42ac863: `injection-xss.spec.ts` introduced `loginAs(TEST_EMAIL, TEST_PASSWORD)`, rotating Supabase refresh token. Downstream `user.json` saved by `auth.setup.ts` became stale; 54 specs failed. Fix: excluded redteam from default `pnpm e2e`. Root cause: test infrastructure (`auth.setup.ts` saves user.json once at start) assumes no mid-session token rotation; new spec pattern violated that assumption. Pattern: E2E specs reusing global-seed test user credentials must rotate via `.logout()` → `.loginAs()` path, or create dedicated test user per spec. code-style.md §7 E2E hermiticity rule covers seed-data mutation; add new subsection for shared-user session rotation once count=2 in distinct E2E suites. |
| Optional-chain result passed to filter-accepting function, degrading defensiveness | 1 | 2026-05-07 | WATCH — af8004f audit-completeness.spec.ts:347: `row?.session_id` used after non-load-bearing `toBeTruthy()` assertion. Semantically: `row` is checked, but then `?.session_id` is extracted and passed to a function that treats `undefined` as "skip the filter". A future softening of the assertion (`expect(row).toBeDefined()` → removed) would silently degrade the filter. Pattern: optional-chain results should not be passed to functions that treat falsy as "do nothing". Recommendation: semantic-reviewer should flag this on next cycle. First occurrence on watch list. |
| Dead parameter forwarded when extracting handler into new function | 1 | 2026-06-01 | WATCHING — #698/#666 (commits 961bbc24, cc04e3f9, 2026-06-01): buildResumeHandler extracted from recovery-hook.tsx; unused `setSession` param from original signature was carried forward into new function. Semantic-reviewer flagged as SUGG. Root cause: extraction mechanically copied original signature without auditing which params are actually used in the extracted body. Fix: read extracted function body and drop all unreferenced params. First occurrence; watch for 2nd occurrence across distinct commits/features — on promotion, add guidance to code-style.md §3 or CLAUDE.md refactoring discipline section. |
| File split without moving all test branch coverage (partial test migration) | 1 | 2026-06-01 | WATCHING — #698/#666 (commit 961bbc24, 2026-06-01): oversized dashboard query file split; listInternalExamAttempts moved with tests, but test-writer found gap post-commit: moved function's full branch coverage (pagination edge cases) was not in destination test file. Author selected primary test only, not all branches. Root cause: incomplete audit of all test branches exercising the function. Fix pattern: when moving a function to new file, move ALL tests exercising that function, not just happy-path. Also: pagination constants duplicated across split files (drift risk) → extract to shared module. First occurrence. Distinct mechanism #2 within same refactor task: constant-duplication-on-split. On count=2 distinct mechanisms, propose code-style.md §7 guidance. |
| Shared constant duplicated in split files without extract-to-module pattern | 1 | 2026-06-01 | WATCHING — #698/#666 (commit 961bbc24, 2026-06-01): file split resulted in pagination constants (ITEMS_PER_PAGE) duplicated across both halves. Semantic-reviewer flagged as SUGG (drifts on rename). Root cause: split did not include refactor to extract shared constants. Lesson: file splits should extract ANY constant referenced by both files to shared `constants.ts` module at split-time. Same refactoring task as "file split without moving all test branch coverage" (rows above) but distinct pattern. First occurrence; on 2nd distinct mechanism across different splits, promote to file-organization guidance. |
| O(n²) array-spread accumulation in loop (replaced with get-or-push) | 1 | 2026-05-25 | WATCH — 85d5de06: progress.ts lines 64-65 used `[...qBySubject.get(...) ?? [], q.id]` pattern in loop over all active questions, creating O(n²) profile. Fixed with `.get()` + conditional `.push()` approach (O(n)). Behavior identical (only performance improved). Distinct from "Derived value correct by coincidence" (row 59) — this is a micro-perf gap in a hot path, not a semantic correctness issue. First occurrence. Log and watch. If similar spread-in-loop patterns surface in different files/components (especially in data aggregation paths), promote to code-style.md guidance: "Use get-or-push for map accumulation in hot loops; avoid `[...array, item]` spread in iteration." |
| Test comment restating/paraphrasing the it() title (§7 enforcement gap) | 2 | 2026-06-14 | RULE CANDIDATE (count 2, promoted to live table) — (1) 85d5de06 (2026-05-25): progress.test.ts, 7 comment instances restating titles; CodeRabbit caught post-merge. (2) #890 PR feat/quiz-ux-batch-860-865 (2026-06-14): answer-options.test.tsx + quiz-key-actions.test.ts — narrative comments paraphrasing the test title body; flagged by cloud CR and CR-local. Rule already exists in code-style.md §7 ("Test Comments: Audit After Renaming") and is correct. This is a persistent enforcement gap at authoring time — new tests keep introducing paraphrase-comments above it() blocks. Distinct from the impl-leakage row (that is about title text; this is about comments above the title). Action: propose adding a single sentence to the test-writer agent's note reminding it to omit narrative comments that paraphrase the title. Not a code-style.md change (rule already exists). |
| Doc numeric spec-count drift (steering/tech.md) | 2 | 2026-06-06 | RULE CANDIDATE — **See "Numeric counts in steering docs" row above (same mechanism, merged).** First: af8004f, decisions.md L477 "9 specs" while reality was 18. Second: #553 (9a728afd batch) "18 specs" across tech.md L85/L146/L183 and decisions.md L481 while reality was 36; plan-critic caught that plan updated only 1 of 3 tech.md occurrences. Lifted to live tracker as RULE CANDIDATE. |
| Zod whitespace-only string validation (min(1) accepts spaces) | 1 | 2026-05-07 | WATCH — af8004f code-reviewer flag on `void-code.ts`: `z.string().min(1)` accepts whitespace-only input. Human-facing form fields (reason text, code) should `.trim().min(1)`. Codebase-wide grep needed to identify all sibling occurrences (candidate: ~12 similar validators in form schema files). Pattern: string validators for form input should `.trim()` before `.min(1)` to reject whitespace. Grep all `z.string().min(1)` and `z.string().max(N).min(1)` across `apps/web/**` to determine count before promoting rule. Deferred on watch list. |
| SECURITY INVOKER RPC with multiple permissive RLS SELECT policies + implicit auth scoping | 1 | 2026-05-26 | **PROMOTION CANDIDATE (count=1 real security incident, high-risk pattern) → PROMOTED to hard rule.** Red-team BW3 finding (2026-05-15, issue #669): New RPC `get_student_mastery_stats` (`SECURITY INVOKER`) aggregated `student_responses` assuming RLS alone enforced per-user scoping. Reality: `student_responses` has TWO permissive SELECT RLS policies — `students_read_responses` (student_id = auth.uid()) AND `instructors_read_students` (org + instructor/admin role). RLS combines policies via OR semantics. When an instructor/admin called the RPC, both policies applied, and the RPC returned ALL org students' responses, not the caller's own. Old client code had explicit `.eq('student_id', userId)` filter; client→RPC migration dropped it (assumed RLS subsumes it). Fix in commit 24b08c3: explicit `WHERE sr.student_id = auth.uid()` in the RPC CTE. **Promotion justified:** (1) Exploit is trivial (any instructor caller learns all students' mastery), (2) Pattern risk is HIGH on any table with dual student/admin policies (student_responses is the first, but any future soft-delete/audit table adding both policy classes will recreate the risk), (3) INVOKER functions are a new RPC category post-PR #660, so this is foundational guidance for the pattern. **Decision: Promote to hard rule immediately (count=1 justified by severity + architectural risk).** |
| RPC behavior-preserving client→INVOKER migration must replicate old code's explicit WHERE filters | 1 | 2026-05-26 | WATCH — Commit 24b08c3: `get_student_mastery_stats` RPC migration dropped the old client code's `.eq('student_id', userId)` when converting from client-side Supabase query to RPC. Root cause: migration assumed RLS policies would achieve the same scoping. Reality: RLS policies are enforced, but on a multi-policy table (student_responses), RLS alone is insufficient without the explicit filter. **Distinct from the RLS-policy row above:** that row is about the RPC's own implementation; this row is about the migration discipline. Pattern: "when migrating a client-side Supabase query (with explicit WHERE/filter chain) to a SECURITY INVOKER RPC, replicate all WHERE filters in the RPC, even if RLS is active on the destination table. RLS policy + code filter are both required for correct scoping when the table has multiple policies." First occurrence — watch. If a second migration drops a filter and regresses scope, promote to agent/semantic-reviewer checklist or security.md. |
| SQL CTE header comment describes one algorithm; code implements another (documentation drift) | 1 | 2026-05-26 | WATCH — Semantic-reviewer post-commit finding (commit 24b08c3): RPC header comment described "COUNT(DISTINCT sr.question_id)" but code used "SELECT DISTINCT q.id + COUNT(*)". Comment/code mismatch creates documentation liability (future reader misunderstands behavior; refactor risks silent corruption). semantic-reviewer flagged for clarification. Fixed by revising comment to match actual algorithm. First occurrence of algorithmic comment-drift on an RPC. Pattern: SQL CTEs with non-trivial aggregation logic should have comments explaining the algorithm IF they diverge from the header comment description. Watch for second occurrence in complex RPC queries. If pattern=2, add rule to code-style.md §5 or a new §8 (SQL code comments): "CTE comments must describe the actual algorithm in the code, not the intended algorithm. After code review or refactoring, re-read the comment and verify it matches the implementation." |
| `docs/database.md` security note understates the consequence of misapplying dual-policy design to INVOKER RPCs | 1 | 2026-05-26 | DRIFT FINDING (doc-updater + semantic-reviewer) — Commit 24b08c3 updated `docs/database.md` RPC section to note `get_student_mastery_stats`'s per-user scoping via explicit WHERE clause. However, the existing `student_responses` table security note mentioned the dual-policy design but did NOT explain that any INVOKER RPC reading that table MUST add explicit filters to avoid over-scoping. Drift caught by semantic-reviewer on PR-level sweep (comparing new RPC docs against existing table security note). Fix: expanded `student_responses` security note in `docs/database.md` to clarify "...two policies (student-scoped and admin-scoped); any INVOKER RPC aggregating this table must add explicit student_id filter to enforce narrow scoping, as RLS policies combine via OR." **Doc-updater should have surfaced this via the Cross-Reference Audit Rule** (agent-doc-updater.md § Cross-Reference Audit Rule), but the per-commit scope missed it — only the cumulative PR-level sweep caught it. First occurrence of this specific drift pattern (security property documented at table level but not applied at RPC usage sites). Watch-list item; if docs drift again on a similar pattern, consider expanding doc-updater's cross-reference audit scope or adding a semantic-reviewer checklist for RPC security notes. |
| Test fixture helper computes derived field without mirroring production transform (clamp/normalize) | 1 | 2026-05-25 | WATCH — 23cffad9 (PR #665): semantic-reviewer ISSUE — `makeSubject` fixture's `masteryPercentage` lacked the `Math.min(..., 100)` clamp production applies; fixed in 333bb1a7 by adding clamp to fixture. Root cause: fixture synthesis diverged from production shape. If a second test helper computes a derived field without matching production's transform, promote: "test fixtures computing derived values must apply the same normalization/clamp/transform as production." |
| CodeRabbit suggested fix can reverse direction (must verify authoritative source before applying) | 1 | 2026-05-27 | Watch — PR #680 doc-fix cycle (appeared in two summary tables same session — reconciled to count=1, not 2). Verify authoritative source before applying a CR suggestion; potential agent-workflow.md clarification if count≥2. |
| doc-updater applies stale-hash check to historical journal files | 1 | 2026-05-27 | Watch — PR #680 doc-fix cycle (appeared in two summary tables same session — reconciled to count=1, not 2). If count≥2, add a skip condition for `.claude/agent-memory/**` files. |

## Late-cycle rows (2026-05-28 / 2026-05-29) — reconciled into the archive post-migration

> The first extraction pass dropped the most-recent per-cycle "Frequency Table Update" blocks; these rows are restored here. Source: original `patterns.md` lines ~6149–6606 at `git show 2e87c3e6`.

| Issue Type | Count | Last Seen | Status |
|-----------|-------|-----------|--------|
| .claude/rules/security.md §11 vs docs/security.md §3 section-number mismatch | 3 | 2026-05-28 (#678/#679) | RULE PROMOTION — first 2026-05-26 (#540); cross-reference section numbers between the security.md quick-summary and docs/security.md don't align; clarify in security.md |
| Missing caller-level page-error test on pagination | 2 | 2026-05-29 (#668-7) | RULE PROMOTION → code-style.md §7 — first 2026-05-28 (#681); paginated query reads need a caller-level page-error test |
| fetchAllRows<T> with join should omit explicit generic | 1 | 2026-05-29 | Memory note — reference in next #668 cycle |
| Extracting paginated reads into -queries.ts keeps action file under size limit | 3 (within #668) | 2026-05-29 | Best practice (log only) — reference in next #668 P2 cycle |
| Plan-critic false positive on type inference | 1 | 2026-05-29 | Watch (validate first) — if count≥2, note in agent-critic.md re conservative type suggestions |
| Shared-helper pattern for paired RPCs | 1 | 2026-05-28 | Positive pattern (log only) — reference in future paired-RPC designs |
| Refactor delegation prompt allows module deletion but forbids test-file deletion | 1 | 2026-05-28 | Watch — if count=2, add a standing rule to the delegation prompt template |
| Spec-folder banner consistency (SUPERSEDED marker) | 1 | 2026-05-28 | Watch — if count=2, promote to agent-doc-updater.md rule |
| CodeRabbit-on-superseded-design context switching | 1 | 2026-05-28 | Observation — when CR flags a spec doc CRITICAL, verify spec status first |
| Internal-symbol test-title leakage (code-style.md §7) | 4 | 2026-06-14 | RULE ACTIVE (promoted 2026-04-28) — recurrence after promotion; continue monitoring, no rule change needed. (4th): #890 PR feat/quiz-ux-batch-860-865 — `'forwards calcMode to startQuizSession'` (use-quiz-start.test.ts:172), `'passes imageMode to getRandomQuestionIds'` (start.test.ts), pre-existing siblings at start.test.ts:100/115/130/157; flagged by CR-local round 3 + cloud CR. Rule clause `forwards X to <internalHelper>` is the specific recurring sub-pattern — CR rounds 3 and cloud both caught it independently. No rule change needed (rule is correct and specific); this is a low-adherence enforcement gap at authoring time. Pre-commit grep check for `it\(['"].*(forwards|passes|through|via) .*[a-z][A-Z]` in `*.test.*` would mechanically catch these (Biome cannot). Consider adding to CLAUDE.md or as a custom Lefthook grep step if count reaches 5. |
| Test mock hardcodes constant instead of importing (vi.importActual variant) | 1 | 2026-05-29 | Watch — propose rule if count=2 |
| Hardcoded constants in tests vs. importing (broader pattern; mock-definition variant) | 3 | 2026-05-29 | Documented (no rule yet) — broader variant of the count-2 "Hardcoded constant values in tests" row; this variant is a mock definition, not an assertion |
| Top-level summary updated but dependent detailed section left stale (Cross-Reference Audit Rule unapplied) | 1 | 2026-05-29 | Watch — if count≥2 across commits, propose tooling/procedural enforcement of the existing rule |
| Spec-doc literal counts drifting from distinct-count implementations | 2 | 2026-05-31 | RULE CANDIDATE — #673 fixtures return distinct-question count (varies with seed volume), yet spec claimed fixed `correctCount:8` and `questionIds:8`; first fix 74f0ffff (design.md, 2026-05-31) clarified correctCount is distinct-seeded count, not a fixed literal; second fix b105f9b3 + 3fb3bf2e (design.md + tasks.md, 2026-05-31) revised questionIds description to account for round-robin repeat path (first seed yields 8, re-derive yields >=8 due to guard at count>=8); both fixes were doc-only (impl already correct); root cause: spec doc lists literal values as constants when they should be described as dependent on seed/computation-path conditions; pattern: when a spec documents a derived/variable value as a literal number, grep the impl code for all computation paths to verify the description matches all outcomes — do not assume the literal in the spec is accurate without checking all code paths that produce the value |
| CR-local false positives on Postgres CREATE OR REPLACE migration chain | 2 | 2026-06-05 | RULE CANDIDATE → agent-coderabbit-local.md — CR-local (external CLI) flagged legitimate guards twice in #750: (a) AL `start_exam_session` "IF EXISTS … RAISE" guard missing (CR read pre-guard mig 20260411000003, never traced CREATE OR REPLACE chain to 20260411000005/20260428000004); (b) AJ migration ref as "should be 044" (CR did not know repos mirror migrations in two dirs: packages/db/044_* ≡ supabase/20260411000007_*). Pre-flag verification rule exists in agent-critic.md / semantic-reviewer.md / implementation-critic.md but CR-LOCAL (external, no ChatGPT-code instructions) doesn't follow it. Both validated as false positives. Proposal: update agent-coderabbit-local.md "Common Pitfalls" section to note "Trace CREATE OR REPLACE chains to latest definition before flagging missing guards; migrate-dir mirroring may create apparent conflicts in refs." |
| Doc bullet claimed RPC capability it lacks (audit logging) | 1 | 2026-06-05 | FALSE POSITIVE (impl-critic; count-1) — AJ bullet v1 claimed upsert_exam_config does "audit logging" (no INSERT INTO audit_events in the RPC). Impl-critic caught pre-commit; doc-only fix. Related to row on doc peer-list inconsistencies but distinct — this is a false-positive capability claim, not a misclassification. Log only, no rule change. |
| Doc residual-vector claims missing DB-level constraint that exists (symmetric drift) | 2 | 2026-06-05 | RULE CANDIDATE — when documenting an RPC's known-gap vector (no DB constraint enforcing X), grep migrations for partial unique constraints BEFORE asserting absence. **Occurrences:** (a) AL "no DB-level unique constraint on active_exam_session" — but `uq_active_exam_session` exists (mig 20260411000006). (b) AJ "clearing deleted_at allows two active exam_configs" — but `uq_exam_configs_org_subject_active` rejects 2nd with 23505 (exists, enforced). Both caught post-commit by semantic-reviewer (AL per-commit, AJ via PR-level sweep). Lesson (reinforced): pattern mirrors doc-updater cross-reference-audit rule (code-style.md §9) — when asserting the absence of a constraint, grep ALL migrations for that table+columns before committing the doc. Count=2 distinct RPCs, same mechanism. |
| Multi-branch conditional structure has assertion/guard applied to one branch but not others | 1 | 2026-06-05 | WATCHING — #570/#571 (commits e621f9ba + 9091ad60): metadata-schema assertion guarded exam.completed (mock) branch of batch_submit_quiz's SHARED jsonb_build_object CASE but not internal_exam.completed branch. Semantic-reviewer ISSUE: future revert would slip through unguarded. Fixed by extracting assertion into expectCompletionMetadata helper and applying to BOTH branches. Distinct from "partial fix to sibling file group" (applies across multiple functions/files); this is single-structure multi-branch coverage. Fix pattern: when adding assertion/guard to CASE/ternary/multi-branch, audit ALL branches, apply consistently. First occurrence; watch for recurrence. |
| Test cleanup: throw inside finally block (noUnsafeFinally) | 2 | 2026-06-05 | RULE CANDIDATE — red-team spec rpc-complete-overdue-exam.spec.ts (commit 64339b28, this cycle) had `throw new Error(...)` in afterEach cleanup's finally block; Biome noUnsafeFinally caught it; fixed to `console.error` + return. Pattern mirrors "consoleSpy cleanup try/finally" (guard against resource leaks on test failure) but applies to ANY cleanup action. Count=2 here; second distinct occurrence from another red-team spec would enable promotion. (PR-A session-replay.spec.ts was NOT a 2nd — its afterEach throw inside try, not finally; #256 restore uses capture-and-assert-after, no throw-in-finally.) Watch for recurrence in red-team specs with cleanup try/finally. |
| E2E spec soft-delete+restore must occur inside try/finally (E2E hermiticity timing refinement) | 1 | 2026-06-05 | WATCHING — commit 89256e0d (#256 soft-delete fix): question soft-delete was OUTSIDE try/finally boundary, so throw after DB commit stranded the deleted question, poisoning downstream specs. Fixed by moving delete inside try block so finally-block restore always runs. **Pattern relation:** mirrors code-style.md §7 E2E hermiticity rule (afterEach cleanup) and state-flip vacuity rule (setup completeness). **New refinement:** the DELETE/UPDATE mutating shared seed state must occur INSIDE the try block whose finally restores it — not before or after. If test body throws after mutation but before finally, cleanup doesn't run. Distinct mechanism from "hermiticity restoration exists"; this is timing/scoping of mutation relative to try/finally boundary. Watch for 2nd occurrence in distinct red-team spec — on 2nd, promote to code-style.md §7 sub-rule. |
| Migration added to packages/db/migrations but not supabase/migrations (or vice versa) | 2 | 2026-06-05 | RULE CANDIDATE — migration 084 `void_internal_exam_code_failfast_session` shipped to packages/db/migrations (commit 655faa88, 2026-06-01) while already in supabase/migrations as 20260601000003 (same cycle, earlier). Creates pre-existing divergence where one dir has the function and the other doesn't. Known prior divergence; second recurrence. On 3rd occurrence across different migrations, propose mechanical check (test/script asserting the two dirs define the same latest CREATE OR REPLACE functions, or doc audit step in migration-sync workflow). |
| Red-team RPC output-contract assertions under-asserted (positive paths assert existence not field values) | 3 | 2026-06-05 | RULE CANDIDATE (count 3 → PROMOTION JUSTIFIED) — **Occurrences:** (1) #736 complete_overdue_exam, (2) #557 complete_empty_exam, (3) PR-A #256/#257 session-replay specs. All three under-asserted positive RPC output contracts: existence checked but field values, types, bounds not validated. #256/#257 semantic-reviewer SUGGs: added typeof checks on output fields; added bounds/distinctness assertions. **Promotion ready (count 3):** add to code-style.md §7 red-team guideline: "RPC positive-path and idempotent-replay specs must assert the full output contract: documented columns present + typed; numeric fields in bounds; ≥2 distinct seed values on idempotent/replay paths (so returned value must be a fresh DB read, not a hardcoded constant); all branches on concurrent paths." This prevents N rounds of semantic-reviewer refinement per spec across remaining ~12 RPC specs in this batch. **Sweep:** applies to all remaining RPC specs in PRs 4–12. |
| Postgres SECURITY INVOKER + RLS unauth-path behavior (false positive pattern) | 1 | 2026-06-05 | FALSE POSITIVE (impl-critic, new pattern) — Hub A unauth-path specs (AG/AN/CA/W) asserted anon client cannot execute SECURITY INVOKER functions, expecting `error: { code: 42501, … }` (RLS rejection). Impl-critic raised ISSUE: "these functions are GRANT TO authenticated." **Root cause:** Postgres grants function EXECUTE to PUBLIC by default; no migration revokes it, so `anon` (public role) CAN execute SECURITY INVOKER fns — but they run with `auth.uid() = null`. RLS filters then return empty results (not a GRANT rejection, soft rejection via empty set). **Pattern is sound:** SECURITY INVOKER + RLS empty-on-unauth is correct behavior. Sibling tests on master (BW/BX specs) already prove this. False positive recorded. Do not re-raise. |
| Red-team spec field-type assertion without nullability check across RPC modes | 2 | 2026-06-06 | RULE CANDIDATE — agent assumes RPC field type/shape from memory rather than reading the migration; misses conditional nullability across execution modes. Occurrence 1 (PR #743): asserted `typeof d.passed === 'boolean'` on batch_submit_quiz; `passed` is `boolean | null` (null for quick_quiz) — typeof null === 'object', CI red. Occurrence 2 (PR #774): agent asserted "no `passed` field in quiz-mode return" — WRONG; migration 082 shows `passed` always present in the jsonb; `passed: null` is the correct study-mode contract; CR caught the missing `expect(result.passed).toBeNull()` assertion. Fix: before asserting any RPC output field's type, nullability, or presence, grep the CREATE OR REPLACE FUNCTION body for the JSON key assignment site and all conditional branches. Count=2 → RULE CANDIDATE. |
| Stale local Supabase volume causing false-failures in e2e:redteam | 1 | 2026-06-06 | WATCHING — PR #774 local validation: `pnpm --filter @repo/web e2e:redteam` failed 1 spec (rpc-issue-internal-exam-code) with duplicate-key seed collision from leftover questions in prior session ("0 inserted, 20 skipped"). Fix: `supabase db reset` + reseed → 244/0. Operational rule (on 2nd): always validate red-team on a CLEAN DB — pre-existing volume state causes false failures. First occurrence. |
| Haiku code-reviewer false positives on Playwright E2E spec complexity (#611 cycle) | 2 | 2026-06-05 | RULE CANDIDATE (elevated FP rate on E2E scope; pre-mig baseline ~9 findings/cycle, post-mig #611=2-FP-zero-valid) — (1) flagged "missing zero-row check on afterAll" when `.select('id') + data?.length>0` check was already in committed code (stale-file-read or line-number miscount). (2) Flagged "vacuous positive assertion" on `.toBe(1)` write-result, misapplying §7 non-vacuous rule (targets NEGATIVE assertions like `not.toContain()`, not positive-value assertions on deterministic writes). Semantic-reviewer independently confirmed both correct. **Pattern:** haiku struggles with Playwright E2E spec code complexity — false-positive rate on E2E specs elevated vs. unit/Server Action code. **Signal for escalation:** post-refactor cycle baseline: #705=3, #677=3, #673=0, #709=0, #372=0, #627=1, #568=0, #601=clean, **#611=2-FP-zero-valid** (median ~0.7 findings/cycle). #611 is first SUBSTANTIVE full-feature cycle post-refactor. Log only. If E2E-spec false positives recur in next substantial cycle, reassess haiku reliability on E2E scope. |

## Live-table snapshot relocated 2026-06-07 (budget curation)

> Full **verbatim** copy of every row that was in `../MEMORY.md`'s live table at the 2026-06-07
> curation, when the live `MEMORY.md` was shrunk under the 25 KB native-injection cap. The live
> table now holds terse stubs for active rows and omits terminal-state rows entirely; this section
> is their lossless source of truth. 96 rows total (30 terminal + 66 active). Never auto-injected.

### Terminal-state rows (removed from live table)

| Issue Type | Count | Last Seen | Status |
|-----------|-------|-----------|--------|
| Hook file exceeding 80-line limit | 4 | 2026-03-13 | RULE EXISTS → code-style.md §1 — still recurring despite 70-line watch; use-quiz-state.ts hit 117 lines, fixed by hook split |
| New hook/utility file shipped without a test file | 3 | 2026-03-12 | RULE ADDED → code-style.md §7 — rule exists but not followed at write time; see also count-7 row below |
| Supabase mutation result not destructured (error silently dropped) | 3 | 2026-03-12 | RULE EXISTS → code-style.md §5 — 5 new call sites in Sprint 3 analytics; compliance gap, rule is clear |
| useEffect data fetching in client component (not hydration guard) | 3 | 2026-03-13 | RULE EXISTS → code-style.md §6 — 3rd: statistics-tab.tsx extracted to hook; compliance gap |
| test-writer produces TS2532 (unchecked array index) errors | 3 | 2026-03-23 | RULE IN MEMORY → test-writer/MEMORY.md §Array index safety — agent generates wrong form first; fix cycle is the gate |
| Direct SELECT from `questions` bypassing RPC (answer exposure) | 2 | 2026-03-13 | RULE EXISTS → security.md rule 1 / CLAUDE.md — checkAnswer queried correct flag; fixed via check_quiz_answer RPC; compliance gap |
| Type cast bypassing runtime validation (`as unknown as T` no guard) | 2 | 2026-03-13 | RULE ADDED → code-style.md §5 + .coderabbit.yaml — pair `as unknown as T` with a runtime type guard |
| rpc<T[]> consumer missing non-array type guard | 2 | 2026-05-30 | RULE EXISTS → code-style.md §5 — #677 sweep (PR #707) guarded the 3 unguarded sites with `Array.isArray`: progress.ts (get_student_mastery_stats loop), analytics.ts (getDailyActivity + getSubjectScores). Already-compliant siblings (check-consent.ts, load-session-questions.ts) left unchanged. semantic-reviewer raised load-session-questions as a missed site — FALSE POSITIVE (its `!data?.length` early-return already guards non-array; #677 lists it as already-compliant). No new sites found in lib/queries |
| New hook/utility file extracted without shipping tests in same commit | 7 | 2026-03-27 | RULE EXISTS → code-style.md §7 — 7th recurrence; authoring habit absent; code-reviewer BLOCKING + test-writer is the reliable gate |
| consoleSpy created without try/finally cleanup (spy leaks on failure) | 3 | 2026-03-14 | RULE ADDED → test-writer/MEMORY.md — always wrap consoleSpy in try { } finally { consoleSpy.mockRestore() } |
| Red-team spec written against wrong schema column / RPC signature | 3 | 2026-06-06 | PROMOTED → agent-red-team.md (count 3) — 3rd: PR #769 quiz-draft-injection.spec.ts assumed quiz_drafts has deleted_at (it doesn't — hard-delete table); spec used `.is('deleted_at', null)` filter, causing CI red. Fix: hard-delete cleanup + code-style.md §7 exception documented. Rule: always grep the actual migration before writing any filter/column assertion in a red-team spec. |
| test-writer generates deprecated vi.fn generic syntax (two-arg form) | 2 | 2026-03-15 | RULE ADDED → test-writer/MEMORY.md — correct form `vi.fn<(arg: A) => R>()` (single function-type arg, Vitest v4) |
| Defensive fix for race/stale-state that does not materialize today | 2 | 2026-04-26 | RESOLVED (intentional hardening) — mirror parent invariant in child render guard/effect; deliberate hardening, not unclear spec |
| Turbo type-check cache masking new compile errors after dep bumps | 3 | 2026-03-17 | RULE APPLIED → CLAUDE.md — run `pnpm check-types --force` after dep bumps; 3rd occurrence confirms rule pre-empted the issue |
| Actor-role subquery missing `deleted_at` filter in mutation audit path | 3 | 2026-04-27 | PROMOTED → security.md §10 (count 3) — every audit_events INSERT subquery must filter deleted_at IS NULL on user/session FK lookups |
| Hook file exceeding 80-line limit | 5 | 2026-03-23 | RULE EXISTS → code-style.md §1 — use-quiz-config.ts at 110 lines; hooks grow incrementally; post-commit code-reviewer is the catch |
| Plan-critic false positive: mock chain `this` binding | 1 | 2026-05-30 | FALSE POSITIVE — claimed vi.fn().mockReturnThis() doesn't bind this on POJO method; incorrect (existing select/eq chain proves it works); rejected by orchestrator, code passed |
| New test file shipped without vi.resetAllMocks() in beforeEach | 2 | 2026-03-27 | PROMOTED → test-writer/MEMORY.md §Mock patterns — rule exists; compliance gap at authoring time; semantic-reviewer is the gate |
| test-writer generates tests needing jsdom-compat fixes before they pass | 3 | 2026-03-29 | RULE IN MEMORY → test-writer/MEMORY.md — TS2532 / vi.fn syntax / PointerEvent jsdom gaps; fix cycle is the reliable gate |
| TS strict mode requires `!` non-null assertion on array index in test files | 2 | 2026-04-11 | RULE IN MEMORY → test-writer/MEMORY.md §Array index safety — assert `arr[0]!` after `toHaveLength(N)`; pre-commit tsc is the catch |
| audit-actor-subquery-soft-delete (audit_events INSERT subqueries missing deleted_at) | 3 | 2026-04-27 | PROMOTED → security.md §10 (count 3) — same pattern as the actor-role row; INSERT INTO audit_events must filter deleted_at on FK lookups |
| security.md §11 vs docs/security.md §3 section-number mismatch | 3 | 2026-05-28 | RULE PROMOTION — cross-reference section numbers between the security.md quick-summary and docs/security.md don't align; clarify in security.md (first 2026-05-26 #540) |
| Missing caller-level page-error test on pagination | 2 | 2026-06-01 | PROMOTED → code-style.md §7 (PR #699, commits 83e41d75+cd208e4b) — paginated query reads need a caller-level page-error test; two valid forms: (1) real helper with mocked queries, (2) helper mocked as dependency (for pure pass-throughs); sweep completed on 7 fetchUser* callers in GDPR flow |
| Internal-symbol test-title leakage | 3 | 2026-05-29 | RULE ACTIVE → code-style.md §7 (promoted 2026-04-28) — recurrence after promotion; monitor, no rule change |
| State-flip E2E tests must assert precondition before postcondition (vacuous isolation/reactivation tests) | 5 | 2026-06-06 | PROMOTED → code-style.md §7 (count 5, distinct mechanisms) — **#314 (PR #712):** red-team RLS isolation specs asserted 0-row reads on unseeded tables; proved nothing except table emptiness, not RLS enforcement. Fixed by seeding protected row first. **#372 (PR #713):** reactivate E2E test asserted UI state post-action without pre-action assertion. **Hub B (76e17f8d):** cross-org isolation spec asserted `not.toContain(victim)` on possibly-empty roster. **a17fdc0f:** AH/CB/AK cross-org exam/pool/distribution tests follow the rule correctly. **PR #774 (quiz-draft-injection UPDATE-isolation):** describe-level afterEach wiped victim rows between tests, so the UPDATE-isolation lookup found 0 rows and the assertion never ran — vacuous pass from test-environment cleanup, not from table emptiness. Fixed by pre-seeding the victim row inside the test body (after afterEach boundary). Same root cause: negative/isolation assertion ran on absent data, proving nothing. Rule exists; enforcement (pre-commit semantic-reviewer) is effective. |
| Doc bullet claimed RPC capability it lacks (audit logging) | 1 | 2026-06-05 | FALSE POSITIVE (impl-critic; count-1) — AJ bullet v1 claimed upsert_exam_config does "audit logging" (no INSERT INTO audit_events in the RPC). Impl-critic caught pre-commit; doc-only fix. Related to row 111 (doc peer-list inconsistencies) but distinct — this is a false-positive capability claim, not a misclassification. Log only, no rule change. |
| Postgres SECURITY INVOKER + RLS unauth-path behavior (false positive pattern) | 1 | 2026-06-05 | FALSE POSITIVE (impl-critic, new pattern) — Hub A unauth-path specs (AG/AN/CA/W) asserted anon client cannot execute SECURITY INVOKER functions, expecting `error: { code: 42501, … }` (RLS rejection). Impl-critic raised ISSUE: "these functions are GRANT TO authenticated." **Root cause:** Postgres grants function EXECUTE to PUBLIC by default; no migration revokes it, so `anon` (public role) CAN execute SECURITY INVOKER fns — but they run with `auth.uid() = null`. RLS filters then return empty results (not a GRANT rejection, soft rejection via empty set). **Pattern is sound:** SECURITY INVOKER + RLS empty-on-unauth is correct behavior. Sibling tests on master (BW/BX specs) already prove this. Durable knowledge (new, row 109): "SECURITY INVOKER functions on RLS-protected tables return `error: null + data: []` on unauth calls (RLS is the gatekeeper, not GRANT denial)." False positive recorded. Do not re-raise. |
| console.error before throw in Server Component query helpers (semantic-reviewer suggestion, count-1 check) | 1 | 2026-06-01 | FALSE POSITIVE — semantic-reviewer (S1) suggested adding `console.error(...)` before every `throw new Error(...)` in dashboard-stats.ts helpers (getTotalAnswered, getQuestionsToday). **Pattern check:** All established Server Component query helpers (getQuestionsToday, getStreakData, applyLastPracticed, getProfileStats, etc.) throw on error WITHOUT preceding console.error. This is the **established convention** — throw propagates to app/error.xyz which logs via Sentry. Semantic-reviewer's claim of "durable knowledge rule at count=2" has **no basis** in tracker (zero count=2 console-error-before-throw pattern exists; count=2 row 40-41 in archive is auth-error DESTRUCTURING in queries files, not RPC throw sites — distinct pattern). Confirmed: throw-only is correct for this context. Semantic-reviewer false positive marked. Do not re-raise. |
| PostgREST BIGINT/NUMERIC serialized as JSON strings — uncoerced reads break === and arithmetic | 5 | 2026-06-04 | PROMOTED → code-style.md §5 (count 5) — PostgREST sends BIGINT (int8) and NUMERIC columns as JSON *strings*, not numbers. Reading into a `number`-typed field without `Number()` coercion produces silent bugs: broken equality checks (=== 1 fails when value is "1"), NaN on arithmetic, .toFixed() crashes. Occurrences: quiz.ts (total_time_ms, #680), profile.ts (avg_score, 2026-05-29), dashboard-stats.ts (subject_count, 2026-05-29), reports.ts (total_count, answered_count, score_percentage, #718), audit-metadata (score_percentage, cycle D #510/#592/#575). Fix pattern: coerce at read site with `Number(value)` before use; existing precedent (quiz.ts) shows the applied pattern. Promotion triggers Sweep-On-Rule-Promotion: audit all RPC result reads in lib/queries/ + lib/ for unguarded BIGINT/NUMERIC fields and apply coercion to all remaining sites (grep `int8\|numeric\|NUMERIC` in migrations to identify columns, trace to consumer call sites). **Positive signal:** semantic-reviewer pre-commit catch (2026-06-04 cycle D) caught the audit-metadata NUMERIC coercion BEFORE commit; rule working as designed. |
| Postgres function GRANT PUBLIC default assumed restrictive by test; SECURITY INVOKER + RLS default behavior | 1 | 2026-06-04 | FALSE POSITIVE (impl-critic, new pattern) — Hub A unauth-path specs (AG/AN/CA/W) asserted anon client cannot execute SECURITY INVOKER functions, expecting `error: { code: 42501, … }` (RLS rejection). Impl-critic raised ISSUE: "these functions are GRANT TO authenticated." **Root cause:** Postgres grants function EXECUTE to PUBLIC by default; no migration revokes it, so `anon` (public role) executes the SECURITY INVOKER fns as the calling user (null session). RLS filters then empty the result set → `error: null` + `data: []`. **Pattern is sound:** SECURITY INVOKER + RLS empty-on-unauth is the correct unauth-path behavior (no hard error, soft rejection via empty set). Sibling tests on master (BW, BX) already prove anon execution works this way; impl-critic was applying a false precondition. **Durable knowledge:** Postgres PUBLIC-default EXECUTE on SECURITY INVOKER functions is by design; RLS is the gatekeeper, not the function grant. When testing unauth access to RLS-protected queries, expect `error: null` + empty data, not a GRANT rejection. See [[postgres-security-invoker-rls-pattern]] for context. |

### Active rows (stubbed in live table — full verbatim text here)

| Issue Type | Count | Last Seen | Status |
|-----------|-------|-----------|--------|
| Test fixture shape mismatch (wrong/missing field in fixture object) | 2 | 2026-03-13 | RULE CANDIDATE — both caught pre-commit by type-check; derive fixture shapes from the exported TS type, not by hand |
| Bare `catch {}` without error-type narrowing | 2 | 2026-04-08 | RULE CANDIDATE → code-style.md §6 — swallowed redirect() from requireAdmin → 500; catch in Server Components must re-throw isRedirectError() |
| Inconsistent guard between related RPCs (sibling missing guard) | 2 | 2026-03-14 | RULE CANDIDATE — auth.uid() guard + NULL correct_option guard each added to one RPC, missed sibling; audit all siblings in same commit |
| Partial fix applied to sibling file group (cross-cutting concern) | 10 | 2026-06-14 | RULE CANDIDATE (count 10, active) — fix applied to the instance seen, not all instances in file + siblings; grep all sites before commit; cycle D #636 sweep missed 3rd `as string` cast in audit-completeness.spec.ts (impl-critic caught on review); #775 plan missed that rpc-start-session has TWO describe blocks each needing cleanup (plan-critic caught pre-execution); #553/#634 plan updated only 1 of 3 stale spec-count occurrences in tech.md (plan-critic caught pre-execution — count "18" at L85/146/183 but only L85 updated in plan); **#446 d2494a05**: anti-cache headers forwarded on pass-through response (middleware.ts fix) but NOT on redirect/error fresh-response exits in proxy.ts — semantic-reviewer caught post-commit; same root cause: security/defensive attribute applied to the instance in view, not all construction sites; **#825 1a068277**: DQ3 `expect(row).not.toBeNull()` non-vacuity guard omitted while DQ2/DR2/DR3 sibling isolation tests each had it — semantic-reviewer SUGGESTION, applied in 79133c04 |
| useTransition + manual loading state hybrid fragility | 2 | 2026-03-13 | RULE CANDIDATE — isPending + manual isLoading can both be false mid-fetch, flashing idle button; suggestion-level, not yet fixed |
| Silent numeric fallback without observability logging | 2 | 2026-03-13 | RULE CANDIDATE — fallback to 0/min on empty/malformed data with no server signal; always console.warn before the fallback value |
| Query missing student_id scope (returns wrong student's data) | 2 | 2026-03-15 | RULE CANDIDATE → security.md (propose on 3rd) — auth check ≠ ownership scoping; student-owned tables must scope to student_id |
| UI event handler missing re-entry guard (double-fire) | 2 | 2026-03-16 | RULE CANDIDATE — async lock + event-propagation mechanisms; any close/submit action must be audited for re-entry at authoring time |
| UPDATE returning zero rows treated as success (silent no-op) | 2 | 2026-03-20 | RULE CANDIDATE → code-style.md §5 — ownership-scoped DELETE/UPDATE must `.select('id')` + check row count before returning success |
| Error path in existing function untested (count-error branch) | 4 | 2026-04-14 | RULE CANDIDATE (count 4) — new edge/error branches added to existing tested files lack test updates in the same commit; caught post-commit |
| Derived value correct by coincidence (index used as count proxy) | 2 | 2026-03-13 | RULE CANDIDATE — value co-varies with metric but diverges on edge cases; use a dedicated state var incremented at the domain event |
| Auth callback guard ordering error (guards in wrong order → bypass) | 2 | 2026-03-17 | RULE CANDIDATE — session actions must precede existence/registration checks; all post-session failure paths must signOut() before redirect |
| Supabase SELECT error swallowed in auth helper (distinct from mutation) | 3 | 2026-06-04 | RULE CANDIDATE → code-style.md §5 (extension) — auth-path SELECTs + seed/test-setup SELECTs must destructure `{ data, error }` and log before the guard decision (distinct mechanisms: auth guards vs. idempotent-seed preparation) |
| Zod error message pinned to exact internal text | 2 | 2026-03-16 | RULE CANDIDATE — Zod internal messages aren't public API; assert `error instanceof ZodError` or `.issues[0].code`, never `.message` |
| Missing `setSubmitting(true)` before async call in form/button handler | 2 | 2026-04-27 | RULE CANDIDATE → code-style.md (on 3rd) — set loading state before awaiting, not after; applies to all form handlers |
| Idempotent RPC returns hardcoded values instead of reading DB state | 2 | 2026-04-27 | RULE CANDIDATE — replay paths returned hardcoded pass/100% instead of querying config; idempotent replay must read current DB state |
| Code-reviewer flags file outside the commit diff scope | 2 | 2026-04-08 | RULE CANDIDATE → agent-code-reviewer.md (false positive) — agent scans full file; only flag violations on lines in the diff |
| Pre-existing file-size violation surfaced when commit adds lines to over-limit file | 4 | 2026-06-14 | RULE CANDIDATE — file at/over limit must be split in the same additive commit, not deferred; distinct from Biome-expansion case. 3rd: toggle-student-status.ts (pre-existing ~112L) grew to 124L by adding +12L audit block; shared audit-helper extraction deferred to issue. 4th: 8a6704a8 (#885/#886): quiz-session.tsx (pre-existing 155L, over the 150-line React component cap) grew to 161L by keepMounted fix + comment; trimmed to 157L by reducing the comment — file was still over-limit after trimming, split deferred to #887 (P2/M). Count=4 strengthens RULE CANDIDATE: split is required when an over-limit file is worsened, not deferred. |
| Function exceeding 30-line limit in Server Action file | 3 | 2026-04-10 | RULE CANDIDATE (count 3) → code-style.md §3 — extract row-transform/business logic as named `mapXxxRow()` helper; existing gate working |
| ZodError escaping Server Action via parse() without try/catch or safeParse | 2 | 2026-03-26 | RULE CANDIDATE → code-style.md — use `Schema.safeParse()` (or wrap `.parse()` in try/catch) so invalid input returns a typed error |
| Hardcoded constant values in tests instead of importing source constants | 3 | 2026-05-29 | RULE CANDIDATE → test-writer/MEMORY.md — import production constants, never duplicate literal values that drift on rename; broader mock-definition variant recurred 2026-05-29 (#668) |
| Hardcoded constant in DB migration diverging from TS source-of-truth | 1 | 2026-05-30 | WATCHING — mig 20260430000011 hardcodes limit `20` while MAX_DRAFTS is the TS source; distinct from test-fixture drift; watch for recurrence in config/parameter drifts |
| Missing assertion on parameterized query bound value in test | 1 | 2026-05-30 | WATCHING — loadDrafts() `.limit()` bound not asserted; coverage gap on RPC param behavior; distinct from error-path untested (row 28) |
| CodeRabbit false-positive rate elevated on exam-mode PRs | 2 | 2026-04-14 | RULE CANDIDATE → .coderabbit.yaml — CR lacks project context (hard-delete exceptions, DB-level constraints); consider suppression notes |
| Manual-eval bug invisible to unit tests (dual-source UI only in full app) | 2 | 2026-04-28 | RULE CANDIDATE → code-style.md §7 (extends Refresh/Reload) — dual server+client surfaces need an integration/E2E test mounting both |
| Stale `why` annotations on test payloads after guard mechanism change | 2 | 2026-05-07 | RULE CANDIDATE (promotion deferred — both in same file/migration, not distinct mechanism) — payloads.ts notes drift after guard swap |
| Server Action ERROR_MESSAGES not synced with new RPC `RAISE EXCEPTION` literals | 3 | 2026-06-19 | RULE CANDIDATE (count=3 — threshold reached) — UX gap: unhandled tokens fall through to generic message. (3) VFR RT Phase B: semantic-reviewer caught incomplete token map in start/submit actions vs RPC RAISE set (2bbf0252). Propose semantic-reviewer checklist note. Sweep: grep supabase/migrations for RAISE EXCEPTION literals, cross-check all actions/*.ts ERROR_MESSAGES for gaps. |
| Doc config-tree / inventory missing items wired in settings.json | 1 | 2026-05-30 | WATCHING — docs/decisions.md config-tree + docs/setup-audit.md hooks-list omitted review-gate.js and cr-local-plan-reminder.sh (both active in settings.json); surfaced when semantic-reviewer reviewed the doc-completeness commit; distinct from "missing route entry" (that is new routes; this is pre-existing untracked items) |
| Red-team spec-count prose drift across multiple doc surfaces (tech.md ×N + decisions.md) | 4 | 2026-06-14 | RULE CANDIDATE — numeric red-team spec counts in steering/tech.md (≥3 occurrences of the same literal) and docs/decisions.md go stale on every spec-addition batch; first seen 2026-05-07 (af8004f, decisions.md "9 specs"); second: #553 (9a728afd batch) count "18" stale in tech.md at L85/L146/L183 AND decisions.md L481, drifted to 36. **Third: #796 (commits b95360f0 + 5e38c487): spec-file count 39→41 found stale across decisions.md + tech.md(3×, steering) + plan.md.** **Fourth: #825 (79133c04): spec-file count 41→43 stale across decisions.md + tech.md(3×); doc-updater caught and fixed in same cycle.** Rule candidate: when a red-team spec is added, doc-updater must grep ALL occurrences of the spec-count literal in tech.md AND decisions.md and update them all atomically. Long-term fix: replace literal counts with structural descriptions ("one spec per OWASP category") per archived guidance. Already covered by Cross-Reference Audit Rule (code-style.md §9 / agent-doc-updater.md) but that rule scopes to structural cross-references — propose explicit extension: "any grep-able numeric count that appears in multiple doc surfaces must be updated atomically (all occurrences in a single commit)." Count now=4 — rule exists (agent-doc-updater.md "Repeated numeric-literal counts" sub-rule), doc-updater following it. Persistent drift suggests committer should grep+fix in the same spec-add commit rather than relying on the post-commit agent. |
| Shared-reference table (easa_subjects/easa_topics) lacks organization_id column, requiring org-isolation keying on other predicates | 1 | 2026-05-31 | WATCHING — #673 plan-critic cycle: red-team specs wanted to assert subject_id ∈ org, but easa_subjects has no org_id; org membership keyed on easa_subjects.subject_id ∉ student_id universe; plan revised to isolation via response rows + cross-subject aggregate scope; if recurs on a different table/predicate, may indicate missing data model for org-scoped reference tables |
| Red-team fixture determinism: compute all backdated dates from single now-snapshot | 1 | 2026-05-31 | WATCHING — #673 design-critic: seed had fixed question dates, fixture computed answers against those → test coverage non-vacuous only if seed volumes match expectations (1 subj/20 Qs); critic recommended parametric backdating from a single snapshot so coverage is deterministic regardless of seed state; first occurrence |
| Shared-seed red-team fixture non-vacuous only against expected seed volume, not actual | 1 | 2026-05-31 | WATCHING — #673 design-critic: when test asserts an aggregate behavior (e.g., mastery ≥50%), verify the fixture's expected row count supports that assert (e.g., ≥4 responses for ≥2/4=50%); asserts may be vacuously true or false if seed is smaller than expected; first occurrence |
| Spec-doc literal counts drifting from distinct-count implementations | 2 | 2026-05-31 | RULE CANDIDATE — #673 fixtures return distinct-question count (varies with seed volume), yet spec claimed fixed `correctCount:8` and `questionIds:8`; first fix 74f0ffff (design.md) clarified correctCount is distinct-seeded; second fix b105f9b3 + 3fb3bf2e (design.md + tasks.md) revised questionIds to account for round-robin vs. re-derive path; **third site cd8e3537 (design.md:92, same file) — Component 4 interface comment still said "the 8 seeded rows" after b105f9b3 fixed only the Data Models section L143; same mechanism, count NOT incremented, but proves in-file sweep was incomplete.** All fixes doc-only (impl already correct); root cause: spec doc lists literal values as constants when they should be described as dependent on seed/path conditions. **Lesson (reinforced): when fixing literal-count drift, grep ALL occurrences of the literal IN THE SAME FILE (interface comments, Data Models, prose) — not just the flagged line — before considering it resolved; mirrors doc-updater cross-reference-audit rule.** |
| Red-team RLS error-code assertions pinned to 42501 (instead of generic error non-null) | 2 | 2026-06-04 | RULE CANDIDATE (count 2) — orchestrator DEFERRED hard-rule promotion 2026-06-04. This is a positive practice applied CORRECTLY twice, not a recurring DEFECT; promoting to code-style.md §7 would trigger a mandatory full-redteam-spec sweep (agent-learner.md § Sweep On Rule Promotion) for marginal, hard-to-mechanically-enforce value (CodeRabbit/biome can't verify "is this the *right* SQLSTATE"). Kept as durable GUIDANCE, NOT written to code-style.md (so coderabbit-sync is NOT triggered). **Revisit + promote** only if a redteam spec is found passing VACUOUSLY because it asserted merely `toBeNull()`/`not.toBeNull()` where a code-pin would have caught it — that turns this into a defect-prevention rule worth the sweep. Evidence: **#314 (PR #712, commits eb1953c9 + b365f1e1):** pinned RLS WITH CHECK rejection to `error?.code === '42501'`. **PR-E (commits f2c52459 + 4bdb533b):** JSONB injection spec pinned constraint/cast errors to SQLSTATE 22P02 / 23514 / 23502, proving the admin caller passed the auth gate. Durable principle (apply when authoring redteam specs, not enforced): assert the exact SQLSTATE/error code (not just `toBeNull()`) to prove (1) auth/role guards were passed, (2) rejection originated from the expected defense layer — RLS 42501, P0001 auth/role RAISE, constraint 23514/23502/23505, cast 22P02, missing-table 42P01. |
| E2E marker constant inlined instead of exported (code-style.md §7 compliance gap) | 1 | 2026-05-31 | WATCHING — commit 9976f2ca: marker string '[E2E_REDTEAM] unauth-read fixture' inlined in server-action-unauthenticated.spec.ts instead of exported from seed.ts as E2E_REDTEAM_UNAUTH_COMMENT_MARKER. Fixed same session. Rule already exists (code-style.md §7 E2E Hermiticity rule #1 — "Stable marker constant for test-created rows, exported from a shared helper module, never a magic string inlined per test"), promoted 2026-05-29 after PR #523; this is a compliance gap at spec-authoring time, not a new pattern. Log only; rule is working. |
| Test asserts only negative absence, not positive presence on success (assert banner gone, but also that content still renders) | 1 | 2026-06-01 | WATCHING — dashboard/loading.test.tsx asserted `{!loadFailed && <Banner>}` → banner absent on success, but never asserted the tabs inside the conditional still render. A future regression where `{!loadFailed && <Tabs/>}` mistakenly becomes `{loadFailed && <Tabs/>}` would pass silently (test sees no error, assumes tabs render). Complement negative-absence tests with positive-presence assertions on the guarded content. First occurrence. |
| CR-local false positives on Postgres CREATE OR REPLACE migration chain | 2 | 2026-06-05 | RULE CANDIDATE → agent-coderabbit-local.md — CR-local (external CLI) flagged legitimate guards twice in #750: (a) AL `start_exam_session` "IF EXISTS … RAISE" guard missing (CR read pre-guard mig 20260411000003, never traced CREATE OR REPLACE chain to 20260411000005/20260428000004); (b) AJ migration ref as "should be 044" (CR did not know repos mirror migrations in two dirs: packages/db/044_* ≡ supabase/20260411000007_*). Pre-flag verification rule exists in agent-critic.md / semantic-reviewer.md / implementation-critic.md but CR-LOCAL (external, no ChatGPT-code instructions) doesn't follow it. Both validated as false positives. Proposal: update agent-coderabbit-local.md "Common Pitfalls" section to note "Trace CREATE OR REPLACE chains to latest definition before flagging missing guards; migrate-dir mirroring may create apparent conflicts in refs." |
| Doc residual-vector claims missing DB-level constraint that exists (symmetric drift) | 2 | 2026-06-05 | RULE CANDIDATE — when documenting an RPC's known-gap vector (no DB constraint enforcing X), grep migrations for partial unique constraints BEFORE asserting absence. **Occurrences:** (a) AL "no DB-level unique constraint on active_exam_session" — but `uq_active_exam_session` exists (mig 20260411000006). (b) AJ "clearing deleted_at allows two active exam_configs" — but `uq_exam_configs_org_subject_active` rejects 2nd with 23505 (exists, enforced). Both caught post-commit by semantic-reviewer (AL per-commit, AJ via PR-level sweep). Lesson (reinforced): pattern mirrors doc-updater cross-reference-audit rule (row 118, code-style.md §9) — when asserting the absence of a constraint, grep ALL migrations for that table+columns before committing the doc. Count=2 distinct RPCs, same mechanism. |
| Closure variable reassigned after caller captures reference (orphaned response) | 1 | 2026-06-06 | WATCHING — #446 commit 9a44eb34: `setAll` callback reassigned the local `response` to a fresh NextResponse, but `createMiddlewareSupabaseClient` returned the original reference BEFORE the lazy `setAll` fires. Reassigned object was orphaned — silently dropping forwarded headers AND refreshed session cookies on token rotation. Fix: mutate the response in place (let → const, drop reassignment) so caller's reference receives cookies and headers. Root cause: callback that mutates a shared object via reassignment instead of mutation invalidates any caller reference captured before the callback fires. Watch for recurrence in other middleware/callback patterns where a caller captures a reference and a callback later reassigns the underlying variable. |
| Multi-branch conditional structure has assertion/guard applied to one branch but not others | 1 | 2026-06-05 | WATCHING — #570/#571 (commits e621f9ba + 9091ad60): metadata-schema assertion guarded exam.completed (mock) branch of batch_submit_quiz's SHARED jsonb_build_object CASE but not internal_exam.completed branch. Semantic-reviewer ISSUE: future revert would slip through unguarded. Fixed by extracting assertion into expectCompletionMetadata helper and applying to BOTH branches. Distinct from "partial fix to sibling file group" (row 20, applies across multiple functions/files); this is single-structure multi-branch coverage gap. Fix pattern: when adding assertion/guard to CASE/ternary/multi-branch, audit ALL branches, apply consistently. First occurrence; watch for recurrence in multi-branch assertions. |
| Test cleanup: throw inside finally block (noUnsafeFinally) / multi-block afterAll without error accumulator | 3 | 2026-06-14 | PROMOTED → code-style.md §7 "Multi-Step Cleanup Needs a Per-Step Error Accumulator" (#794) — accumulator half promoted to a hard rule; throw-in-finally half stays Biome-enforced (noUnsafeFinally), NOT duplicated per agent-learner.md. Sweep-on-promotion done: 4 e2e offenders fixed (consent.spec afterAll, exam-recovery.spec afterEach, settings.spec afterAll, exam-config-reactivation-guard.spec afterAll); 3 specs already compliant (rpc-void-internal-exam-code, rpc-start-session-smuggling, injection-xss). Three distinct cleanup-safety violations: **(1) commit 64339b28 (2026-06-05):** `throw new Error(...)` in afterEach cleanup's finally block — Biome `noUnsafeFinally` caught; fixed to `console.error` + return. **(2) commit 4f918ded (2026-06-07):** `rpc-cross-tenant.spec.ts` afterAll had multiple sequential cleanup blocks without a try/catch error-accumulator — a throw in one block skipped the rest, leaving seeded session CL3 un-cleaned (poison risk for downstream specs); semantic-reviewer flagged as ISSUE; fixed by converting to per-block try/catch with error-list accumulator (mirrors the void spec's established afterEach pattern). **(3) commit 13fa0249 (2026-06-10):** test-writer agent generated restore mutation in `finally` block using `throw` (Biome noUnsafeFinally); orchestrator corrected to `console.error` + no throw. All root causes: E2E cleanup code that can skip subsequent cleanup steps when an earlier step throws. Rule candidate (count=3): ALL afterAll/afterEach cleanup blocks in E2E specs that execute ≥2 distinct cleanup steps must use a per-block try/catch error-accumulator; finally blocks must not throw — use `console.error`. |
| E2E spec soft-delete+restore must occur inside try/finally (E2E hermiticity timing refinement) | 1 | 2026-06-05 | WATCHING — commit 89256e0d (#256 soft-delete fix): question soft-delete was OUTSIDE try/finally boundary, so throw after DB commit stranded the deleted question, poisoning downstream specs. Fixed by moving delete inside try block so finally-block restore always runs. **Pattern relation:** mirrors code-style.md §7 E2E hermiticity rule (afterEach cleanup) and row 71 "State-flip tests must assert precondition" (setup completeness). **New refinement:** the DELETE/UPDATE mutating shared seed state must occur INSIDE the try block whose finally restores it — not before or after. If test body throws after mutation but before finally, cleanup doesn't run. Distinct mechanism from "hermiticity restoration exists" (row 69 count=2); this is timing/scoping of mutation relative to try/finally boundary. Watch for 2nd occurrence in distinct red-team spec — on 2nd, promote to code-style.md §7 sub-rule. |
| Migration added to packages/db/migrations but not supabase/migrations (or vice versa) | 2 | 2026-06-05 | RULE CANDIDATE — migration 084 `void_internal_exam_code_failfast_session` shipped to packages/db/migrations (commit 655faa88, 2026-06-01) while already in supabase/migrations as 20260601000003 (same cycle, earlier). Creates pre-existing divergence where one dir has the function and the other doesn't. **Known prior divergence:** second recurrence; on 3rd occurrence across different migrations, propose mechanical check (test/script asserting the two dirs define the same latest CREATE OR REPLACE functions, or doc audit step in migration-sync workflow). |
| Non-required CI check allowed failing runtime tests to merge silently | 1 | 2026-06-06 | WATCHING — PR #769: "Red Team Specs" CI check was non-required + path-filtered; 2 RED specs merged without detection. Fix (this PR): remove path filter + make check required via branch protection. Root cause: any CI check that is optional is also invisible as a gate. Rule candidate (on 2nd): all runtime-behavior checks for security defenses must be required status checks — "optional" is equivalent to "disabled" for merge gates. First occurrence; watch for recurrence of any non-required security check allowing failures to pass. |
| Redteam spec authoring without local run before push (assumed-schema + wire-format bugs) | 1 | 2026-06-06 | WATCHING — PR #769: orchestrator did NOT run `pnpm --filter @repo/web e2e:redteam` before pushing, despite CLAUDE.md + agent-red-team.md requiring it for security-path diffs. Two runtime bugs slipped through: (1) quiz_drafts.deleted_at assumed to exist (doesn't — hard-delete table); (2) cookie assertion compared percent-encoded wire value, not the app's decoded view. Both would have been caught by a single local run. CLAUDE.md already mandates local redteam run; this is a process-compliance gap, not a missing rule. **Rule candidate (on 2nd):** if a second push skips the local run and causes CI red on the redteam check, add an explicit gate to the fullpush skill checklist ("run `pnpm --filter @repo/web e2e:redteam` locally before pushing any diff that touches e2e/redteam/**"). First occurrence; watch for recurrence. |
| ON CONFLICT clause with no supporting UNIQUE constraint (dead code or 42P10 at execution) | 2 | 2026-06-06 | RULE CANDIDATE — (1) student_responses a09c6be: ON CONFLICT silently ignored (non-unique index, dead code); (2) mig 085 record_consent f35b2a16: ON CONFLICT (...) WHERE accepted=true against non-unique idx_user_consents_lookup → 42P10 at execution time, NOT at `supabase db reset` / CREATE FUNCTION time. Both cases: clean migration apply + structural grep confirmed clause present yet the function was broken. plpgsql parser defers ON CONFLICT validation to execution. Rule: ON CONFLICT inference target MUST be a UNIQUE index/constraint; verify via grep before committing. Propose: CLAUDE.md migration checklist: "ON CONFLICT (...) requires a UNIQUE index — verify the target index has UNIQUE in its definition; `supabase db reset` applying cleanly does NOT prove ON CONFLICT will execute without error." |
| plpgsql function body contains deferred-validation SQL (clean migration apply ≠ execution correctness) | 4 | 2026-06-21 | RULE CANDIDATE (4) — Four DISTINCT deferred-validation mechanisms, all passed `supabase db reset` + tsc + biome + both Opus impl-critics + semantic-reviewer; caught ONLY by integration-test execution: (1) mig 085 record_consent (f35b2a16, 2026-06-06): ON CONFLICT against non-unique index → 42P10 at call time. (2) mig 101 normalize_answer (VFR RT Phase A, 2026-06-10): POSIX regex `[][...]` bracket-class invalid in PG ARE → 2201B at every call. (3) mig 118 get_quiz_questions (VFR RT Phase 2, 2026-06-21): `PERFORM 1 FROM users WHERE id = auth.uid()` — column `id` ambiguous against `RETURNS TABLE (id uuid, ...)` OUT param → 42702 for every authenticated caller; sibling `get_vfr_rt_exam_questions` (mig 105) already aliased `users u` + `u.id`, which is why it never hit this. (4) mig 120 batch_submit helper (VFR RT Phase 2, 2026-06-21): `normalize_answer(NULL)` returns NULL → boolean `(NULL <> '' AND ...)` evaluates to NULL → inserted into `is_correct NOT NULL` → 23502; sibling grader (mig 119) already had the `coalesce(normalize_answer(...), '')`. Sub-patterns now documented: (a) ON CONFLICT inference-target; (b) POSIX/ARE regex syntax; (c) unqualified column name in RETURNS TABLE body — always alias the table in helper reads when the function has same-named OUT params; (d) NULL propagation through helper call into NOT NULL column — coalesce at the call site, not just in siblings. code-style.md §5 already covers (a) and the broader principle; the section's example list should be extended with (c) and (d). The integration tier (#925) is the ONLY gate that caught occurrences (3) and (4). |
| Semantic reviewer stale-baseline false positive (compared wrong predecessor migration/definition) | 2 | 2026-06-06 | RULE CANDIDATE — (1) GDPR export 7eeff14 (2026-03-27): reviewer compared against wrong migration, claimed deleted_at column absent; (2) Batch-A fix re-review f35b2a16 (2026-06-06): reviewer compared record_consent against mig 057 (ORIGINAL) not latest predecessor 20260327000058; also inverted NOT IN logic direction. Both: reviewer did not trace CREATE OR REPLACE chain to latest definition — the exact Pre-Flag Verification failure our rules document for CR-local / impl-critic / plan-critic. Mitigation (same as existing rule): before acting on semantic-reviewer claim about column/constraint existence or code behavior, grep migrations for the table/function to find the LATEST definition. This is a false-positive pattern, not a defect in production code. Propose: add note to agent-semantic-reviewer.md "DO" section: "Trace CREATE OR REPLACE / ALTER TABLE chain to the latest migration before claiming a column, constraint, or guard is missing or behaves differently." |
| Docs agent fabricating plausible-but-wrong details / hallucinating non-existent files or features | 2 | 2026-06-14 | RULE CANDIDATE — (1) Batch-A cycle (ee4d5544 adjacent, 2026-06-06): docs-consolidation agent wrote 3 inaccuracies — fabricated service_role trigger exemption, inverted guard order, wrong error string; agent did not read the migration SQL before documenting it. (2) #863 batch cycle (2026-06-14): doc-updater asserted a non-existent integration test file path AND claimed plan.md contained a feature ("has-image filter integration test") that had not been implemented; orchestrator caught by verifying the file did not exist on disk, then created the test to make the claim real. Root: agent writes from chat context / task description summary, not from verified filesystem/file contents. Distinct from "Doc-sync agent duplicates existing heading" (archive row ~373, structural duplication from not reading target; this is hallucination of factual claims). Propose: agent-doc-updater.md DO section — "Before asserting that a file exists, that a feature is complete, or that plan.md reflects a given state, verify by reading the file (or ls) — do not rely on chat context or task description." |
| Stale local Supabase volume / in-place migration edit causing false-failures in e2e:redteam runs | 2 | 2026-06-14 | PROMOTED → CLAUDE.md § Local migrations + consolidated into test-writer MEMORY.md migration-apply note (#794). Two distinct stale-local-DB mechanisms: **(1) PR #774 (2026-06-06):** duplicate-key seed collision from leftover questions in prior session — `supabase db reset` + reseed fixed it. **(2) This cycle b004f206 (2026-06-07):** `record_auth_event` red-team tests failed locally with 'resource not in caller org' error — root cause: mig 093 (record_auth_event) was edited in-place AFTER its first `supabase db push` apply; the local Supabase volume recorded that version hash as already-applied, so `db push` was a no-op; `supabase db reset` re-applied all migrations from scratch and fixed it. CI (which always resets) was the source of truth; local was diverged. **Rule candidate:** before running E2E/integration specs that exercise a recently-edited migration (i.e. any migration edited after first apply), run `supabase db reset` locally to guarantee the local DB matches the repo; `supabase db push` does NOT re-apply a hash already in the migration ledger. Count=2 (distinct mechanisms: volume-leftover vs. in-place-edit-after-apply). |
| Red-team RPC output-contract assertions under-asserted (positive paths assert existence not field values) | 3 | 2026-06-05 | RULE CANDIDATE (count 3 → PROMOTION JUSTIFIED) — **Occurrences:** (1) #736 complete_overdue_exam, (2) #557 complete_empty_exam, (3) PR-A #256/#257 session-replay specs. All three under-asserted positive RPC output contracts: existence checked but field values, types, bounds not validated. #256/#257 semantic-reviewer SUGGs: added typeof checks on output fields; added bounds/distinctness assertions. **Promotion ready (count 3):** add to code-style.md §7 red-team guideline: "RPC positive-path and idempotent-replay specs must assert the full output contract: documented columns present + typed; numeric fields in bounds; ≥2 distinct seed values on idempotent/replay paths (so returned value must be a fresh DB read, not a hardcoded constant); all branches on concurrent paths." This prevents N rounds of semantic-reviewer refinement per spec across remaining ~12 RPC specs in this batch. **Sweep:** applies to all remaining RPC specs in PRs 4–12. |
| Haiku code-reviewer false positives on Playwright E2E spec complexity (#611 cycle) | 2 | 2026-06-05 | RULE CANDIDATE (elevated FP rate on E2E scope; pre-mig baseline ~9 findings/cycle, post-mig #611=2-FP-zero-valid) — (1) flagged "missing zero-row check on afterAll" when `.select('id') + data?.length>0` check was already in committed code (stale-file-read or line-number miscount). (2) Flagged "vacuous positive assertion" on `.toBe(1)` write-result, misapplying §7 non-vacuous rule (targets NEGATIVE assertions like `not.toContain()`, not positive-value assertions on deterministic writes). Semantic-reviewer independently confirmed both correct. **Pattern:** haiku struggles with Playwright E2E spec code complexity — false-positive rate on E2E specs elevated vs. unit/Server Action code. **Post-refactor cycle baseline:** #705=3, #677=3, #673=0, #709=0, #372=0, #627=1, #568=0, #601=clean, **#611=2-FP-zero-valid** (median ~0.7 findings/cycle). #611 is first SUBSTANTIVE full-feature cycle (migration + Playwright + doc) post-refactor. **Signal for escalation:** no count-N tracker row yet (single-session false-positive set); log only. If E2E-spec false positives recur in next substantial cycle, reassess haiku reliability on E2E scope (may warrant sonnet upgrade for E2E specs). |
| Knip 'dead code' findings require verification against non-TS usage before deletion | 1 | 2026-05-31 | WATCHING — #325 plan-critic caught that @tailwindcss/typography was NOT dead (legal pages use `prose` classes, but `@plugin` directive was missing from globals.css, so styling was broken, not the dep). Knip is blind to: (a) Tailwind v4 `@plugin`/CSS directives, (b) `.claude/settings.json` hook wiring, (c) lefthook.yml, (d) peer-dep-only packages, (e) npx-invoked binaries. 6+ false positives in the report, 1 broken-but-functional dep caught by pre-commit review. First knip-cleanup cycle; watch for recurrence. Distinct from "verify tooling-report findings" (no prior art). |
| Query helper promoted to throw on error, but client-triggered Server Action calling it without catch boundary | 2 | 2026-06-01 | RULE CANDIDATE — #627 (c26ef61f + b7cf6852) promoted "SELECT query helpers throw on error" (count=3) → swept all throw sites. Fix for this commit (semantic-reviewer ISSUE): lookup.ts Server Action returns now-throwing helper results directly to client. Throws escape the 'use server' boundary → app shell crash. Fix: catch at Server Action boundary (log + return []). Lesson: query-helper-throw posture is safe for Server Components (page-load, caught by app/error.xyz) but requires Server Action call-site wrapping when the SA returns helper output to client. Distinct from "console.error before throw" (that is S-component query throw convention; this is SA consumer-pattern boundary). Count incremented from 1→2 on distinct mechanism (SA consumer boundary catch pattern). |
| Numeric value nested in JSON/JSONB RPC payload not coerced to `number` | 1 | 2026-06-01 | WATCHING — #717 BIGINT/NUMERIC sweep (commits 2570c2ec + 5edee298) enumerated RETURNS TABLE columns + direct `.select()` reads but MISSED nested numeric fields inside json/jsonb RPC payloads. Example: `get_admin_dashboard_kpis` returns `{ weakestSubject: { avgMastery: <NUMERIC> } }` stringified by PostgREST; nested `avgMastery` was cast to `number` without Number() coercion. **Discovery-completeness gap, not a rule gap** — Number() rule exists (code-style.md §5); sweep checklist must ALSO scan RPC return payloads for json/jsonb structures containing numeric fields. Masked by JS `<` coercion today but a type-lie. First occurrence; watch for recurrence and promote to sweep-checklist guidance in durable knowledge if count reaches 2. |
| Dead parameter forwarded when extracting handler into new function | 1 | 2026-06-01 | WATCHING — #698/#666 (commits 961bbc24 + cc04e3f9, 2026-06-01): buildResumeHandler extracted from recovery-hook.tsx, but unused `setSession` param from original was carried forward into extracted function's signature. Semantic-reviewer flagged as SUGG (not a breaking issue, just unused param). Root cause: extraction mechanically copied the original signature without auditing which params are actually used in the extracted body. Fix: read the extracted function body and drop all params that are not referenced. First occurrence; watch for recurrence — on 2nd occurrence across different commits/features, propose note in code-style.md §3 (Function Rules) or CLAUDE.md refactoring guidance. |
| File split without moving all test branch coverage (partial test migration) | 1 | 2026-06-01 | WATCHING — #698/#666 (commits 961bbc24, 2026-06-01): oversized dashboard query file split into two files; listInternalExamAttempts moved with tests, BUT test-writer post-commit found gap: the moved function's full branch coverage (pagination edge cases) was not in the moved test file — test suite was incomplete at move-time. Root cause: author selected the primary test (happy path) but did not audit all branches (error paths, page-edge cases). Fix pattern: when moving a function to a new file, move ALL tests that exercise that function, not just the primary test. Also: pagination constants duplicated across split files (drift risk) — extract to shared module at split-time. Pattern (count=1): full test migration + extract shared constants when splitting. First occurrence; watch for recurrence. On 2nd distinct mechanism, propose code-style.md §7 guidance. |
| Shared constant duplicated in split files without extract-to-module pattern | 1 | 2026-06-01 | WATCHING — #698/#666 (commits 961bbc24, 2026-06-01): file split resulted in pagination constants (e.g., `ITEMS_PER_PAGE`) duplicated across both halves. Semantic-reviewer flagged as SUGG (drifts on rename). Root cause: split did not include refactor to extract shared constants. Lesson: file splits should extract ANY constant referenced by both files to a shared `constants.ts` module. Same instance as the test-migration finding above (same refactoring task, multiple gaps). First occurrence as "constant-extraction-on-split" pattern; distinct from "hardcoded constant in tests" (row 49, hardcoded-values-in-tests pattern is broader). Watch for recurrence. |
| Red-team spec field-type assertion without nullability check across RPC modes | 2 | 2026-06-06 | RULE CANDIDATE — root cause: agent assumes RPC field type/shape without reading the migration; writes assertions that miss conditional nullability across execution modes. **Occurrence 1 (PR #743):** asserted `typeof d.passed === 'boolean'` on batch_submit_quiz; `passed` is `boolean | null` (null for quick_quiz study mode) — typeof null === 'object', failing CI on quick_quiz path. **Occurrence 2 (PR #774):** agent asserted "there is no `passed` field in quiz-mode return" — WRONG; migration 082 shows `passed` always in the jsonb, only `v_passed` assignment is mode-gated; `passed: null` is the correct study-mode value. CR caught the missing `expect(result.passed).toBeNull()` assertion. In both cases: agent worked from memory, not from reading the migration. Fix: before asserting any RPC field's type, nullability, or presence, grep the CREATE OR REPLACE FUNCTION body in the migration for the JSON key's assignment site and all conditional branches. |
| Migration added to packages/db/migrations but not supabase/migrations (or vice versa) | 2 | 2026-06-01 | RULE CANDIDATE — migration 084 `void_internal_exam_code_failfast_session` shipped to packages/db/migrations on 2026-06-01 (commit 655faa88) while it had already been added to supabase/migrations as 20260601000003 (earlier, same cycle). Creates a pre-existing divergence where one dir has the function and the other doesn't. First occurrence: unknown prior divergence (noted in task as second recurrence). On 3rd occurrence across different migrations, propose a mechanical check (test/script asserting the two dirs define the same latest CREATE OR REPLACE functions, or a doc audit step in the migration-sync workflow). |
| Assertion/guard applied to one branch of a multi-branch (CASE/ternary) structure but not all branches | 1 | 2026-06-02 | WATCHING — #570/#571 (commits e621f9ba + 9091ad60): metadata-schema assertion guarded exam.completed (mock) branch of batch_submit_quiz's SHARED jsonb_build_object CASE but not internal_exam.completed branch. Semantic-reviewer ISSUE: future partial revert would slip through unguarded. Fixed by refactoring assertion into expectCompletionMetadata helper and applying to BOTH branches. Distinct from "partial fix to sibling file group" (row 20, applies to multiple functions/files across codebase); this is single-structure multi-branch coverage. Root cause: assertion written against one branch during test development, other branch not audited. Fix pattern: when adding an assertion/guard to a CASE/ternary/multi-branch conditional structure, audit ALL branches and apply consistently. First occurrence; watch for recurrence in multi-branch assertions/guards. |
| Red-team spec: soft-delete + restore must occur inside try/finally (E2E hermiticity refinement) | 1 | 2026-06-04 | WATCHING — commit 89256e0d (#256 soft-delete fix): question soft-delete was OUTSIDE the try/finally boundary, so a throw after the DB commit stranded the deleted question, poisoning downstream specs. Fixed by moving delete inside try block so the finally-block restore always runs. **Pattern relation:** mirrors code-style.md §7 E2E hermiticity rule (afterEach cleanup) and row 71 "State-flip tests must assert precondition" (setup completeness for reactivation/isolation tests). **New refinement:** the DELETE or UPDATE that mutates shared seed state must occur INSIDE the try block whose finally restores it — not before the try or after the try opens. Root cause: if the test body throws after the mutation but before control reaches the finally, cleanup doesn't run. This is a distinct mechanism from "hermiticity restoration exists" (row 69 count=2); it's about the **timing/scoping** of the mutation relative to the try/finally boundary. Watch for recurrence — on 2nd occurrence in a distinct red-team spec (different RPC/feature), promote to code-style.md §7 as a sub-rule of E2E hermiticity: "Shared seed mutations must occur inside the try block of the test cleanup's try/finally boundary, not before or after." |
| Red-team RPC output-contract assertions under-asserted (positive paths assert existence but not field values) | 4 | 2026-06-13 | RULE CANDIDATE (count 4 — codified via PR-G/#742 §7 "RPC Output Contract") — **Occurrences:** (1) #736 complete_overdue_exam, (2) #557 complete_empty_exam, (3) PR-A #256/#257 session-replay specs, (4) #818 `rpc-cross-tenant-isolation.spec.ts` `expectWithinTimeSubmitContract` helper missing `correct_count`/`results` fields. In (4): semantic-reviewer + test-writer both flagged; orchestrator had initially noted `expired === false` in the contract but migration read confirmed the key is absent from the RPC return — deviation was correct; guard applied. §7 rule remains in place; **positive signal (count 4 + #869 2026-06-19):** the rule is being applied but each new RPC spec batch still reveals new fields not initially covered. Rule is working; gap is depth-of-reading at spec authoring time. **Second positive signal — #869 (5ff40d42, 2026-06-19):** `session-replay.spec.ts` two-fixture non-vacuity design applied on first pass; impl-critic, semantic-reviewer, and test-writer all approved with zero rework on the output-contract dimension. Rule is now being followed proactively. Do not re-promote. |
| Shared test-infra helpers (setup.ts, helpers/*.ts) exceed 200-line utility cap (wrongly assumed under .test.ts 500-line exemption) | 2 | 2026-06-06 | RULE CANDIDATE — code-reviewer agent suppression #5 exempts `.test.ts` files up to 500 lines, but setup.ts and helpers/*.ts are NOT test files — they are shared utility files subject to the 200-line utility cap. Occurrences: quiz-session-storage.ts (prior) + setup.ts >200 (#775, deferred to issue #778). Proposal: clarify code-reviewer agent def suppression #5: "Do NOT flag `.test.ts`/`.spec.ts` files for line limits (500-line relaxed limit applies). DO flag shared test-infra files (`setup.ts`, `helpers/*.ts`, `seed.ts`) that exceed 200 lines — these are utility files, not test files." |
| Missing negative/failure-path test when positive-path test exists (side-effect asymmetry) | 1 | 2026-06-06 | WATCHING — #379 cycle (8d0a758d): semantic-reviewer found 2 of 4 auth actions had positive audit-event tests + best-effort success tests but no 'audit-not-called-on-failure' negative test. Fixed by adding failure-path counterpart to complete symmetry. Distinct from row 73 (UI presence/absence — that is asserting a banner is absent without checking guarded content renders). This is specifically: when a function has a side-effect (audit log, email, state mutation), the test suite must include BOTH (a) positive path: side-effect fired on success, AND (b) negative path: side-effect NOT fired on failure. One without the other leaves half the contract untested. First occurrence; watch for recurrence across different side-effect types. |
| `packages/db/src/types.ts` not manually updated after RPC return-shape change | 1 | 2026-06-06 | WATCHING — #471 commit 3fd9f6e9: migration dropped `answered_count` from `get_session_reports` RETURNS TABLE; impl-critic caught that `packages/db/src/types.ts` still listed the column (the file is hand-maintained, not auto-generated in CI). Root cause: plan focused on the migration + TS consumer (reports.ts) but omitted the generated-types file from the "files affected" list. Fix: plan validation must enumerate `packages/db/src/types.ts` as an affected file whenever a migration changes any RPC's RETURNS TABLE columns. First occurrence; watch for recurrence. On 2nd, propose to CLAUDE.md or agent-workflow.md plan-validation checklist: "If the plan changes a migration's RETURNS TABLE signature, add `packages/db/src/types.ts` to the affected-files list." |
| Next.js framework overrides cache-control headers on routed responses (assert framework-guaranteed behavior, not framework-overridden behavior) | 1 | 2026-06-07 | WATCHING — commit b004f206 (#781 token-refresh anti-cache): the `force-token-refresh` pass-through path could not carry `no-store` cache-control headers because Next.js routing overrides them on forwarded responses; only synthetic redirect/403 exits (which construct a fresh NextResponse via `forwardAntiCacheHeaders`) reliably carry the headers. Test-design lesson: assertions about HTTP response headers must be scoped to headers that are guaranteed by the framework's routing layer, not potentially overridden by it. Specifically, the test asserted the pass-through response carried `no-store` — this was framework-overridden and the assertion was invalid. Fix: assert `no-store` only on the synthetic redirect/error exits; document in test comment why pass-through omits the header. First occurrence; watch for recurrence where a test incorrectly asserts a header or behavior that the framework silently overrides. On 2nd, propose note to code-style.md §7 or agent-test-writer guidance: "Before asserting a Next.js response header, verify whether the framework guarantees it survives the routing pipeline — Next.js may strip or override cache-control on routed (forwarded) responses." |
| Vector-ID renumber-on-merge (attack-vector letter IDs shift when a feature branch forks before sibling vectors merge) | 1 | 2026-06-07 | WATCHING — commit b004f206 (#788 record_auth_event): red-team spec vectors were numbered CN–CT at branch-time; after the #379 cycle merged (which included CK→CN), the identifiers shifted and the new spec's CN–CT collided with the post-merge numbering. Pattern: when a red-team spec is authored on a feature branch while a sibling PR with new vectors is in-flight, the final vector IDs must be reconciled at merge-time against the then-current attack-surface.md allocation. Prior occurrence: #379 cycle CK→CN rename. First log of this class; the two events are in the same related spec family (#379/#788), so they do not yet constitute two DISTINCT mechanisms (same spec author, same feature branch context). Watch for recurrence in a different RPC family's spec. On 2nd distinct occurrence, propose agent-red-team.md guidance: "At merge time, resolve any vector-letter conflicts introduced by sibling PRs that merged between branch-cut and merge-commit; update attack-surface.md atomically with the merge." |
| Subagent "verified clean" via `pnpm test` (vitest/esbuild) but skipped `pnpm check-types` (tsc) — type error merged | 2 | 2026-06-14 | RULE CANDIDATE — PROMOTED to live table row "Vitest passes / tsc fails on test file" (archive row 397). (1) #789/60047c2c: subagent typed metadata as `Record<string, unknown>` where RPC param expects `Json`; ran pnpm test, not check-types; pre-commit tsc caught it. (2) #533 cycle: test-writer added prop `isExam` absent from test helper `Required<>` type; vitest green (esbuild strips types), tsc failed pre-commit. Both: esbuild transpiles-and-runs, tsc strict-mode is the catch gate. Propose: delegation DONE WHEN must include BOTH `pnpm test` AND `pnpm check-types`. |
| Semantic-reviewer asserts non-existent project convention (`import 'server-only'` in all lib/ helpers touching server clients) | 1 | 2026-06-07 | WATCHING — #789 commit e9c2a36b (wire 4 auth actions + split toggle-student-status): semantic-reviewer SUGGESTION claimed every helper in lib/ touching server clients must carry `import 'server-only'`. Verified against codebase: `server-only` appears 0 times anywhere in the project; many sibling adminClient-importing modules have no such guard; the claimed convention does not exist. Marked FALSE POSITIVE, SKIPPED. Distinct from tracker-archive row 158 (`server-only` import removed due to Vitest/jsdom resolution failure — that row is about removing an existing guard; this is about the reviewer inventing a convention that was never in the codebase). On 2nd occurrence of semantic-reviewer asserting a zero-evidence codebase convention, add to cross-agent-lessons FP catalog with note: "Semantic-reviewer may assert the existence of project conventions that do not exist — always grep for 0 or 1 usages before accepting a 'must add X everywhere' claim." |
| Doc CI cadence understated — workflow `on:` triggers include PR/push, not just scheduled run | 1 | 2026-06-08 | WATCHING — commit 0e1fbfba (issue #109, Decision 40 Socket.dev): impl-critic found "weekly" description of CodeQL in docs/decisions.md understated (CodeQL also runs on PR/push per workflow `on:` triggers); 1 SUGGESTION (SonarCloud also runs on push). Both APPLIED before commit. Root: doc authors describe CI tools by their scheduled cadence and omit the push/PR triggers. First occurrence. On 2nd: propose a doc-authoring note: when documenting a CI tool, grep the workflow file's `on:` block for all trigger types, not just the scheduled ones. |
| Semantic-reviewer proposes rename/fix targeting a file without reading that file's purpose | 1 | 2026-06-08 | WATCHING — commit 0e1fbfba (issue #109): semantic-reviewer SUGGESTION flagged plan.md reference to `health-monitoring.yml` as stale and recommended renaming it to `agent-health.yml`. Validated as FALSE POSITIVE: `agent-health.yml` is a completely different workflow ("Agent System Health"); `health-monitoring.yml` has no git history (never existed — the plan.md reference was itself the stale artifact, deferred to issue #804). Root: reviewer proposed a destructive rename to a target file without reading that file to verify its purpose. Distinct from row 354 (asserting a universal convention) and row 332 (comparing against a stale migration baseline) — this is proposing a rename to a file the reviewer did not read. On 2nd occurrence: add to cross-agent-lessons FP catalog: "Before accepting a reviewer suggestion to rename or replace a file, read the target file and verify it is not already a different artifact." |
| File rename/move breaks hardcoded path in `.github/workflows/**` (code-style.md §9 rename-grep scope omits workflows) | 1 | 2026-06-08 | WATCHING — commit 13dd115a (#810, issue #326): PR #704 native-subagent-memory migration moved `red-team/attack-surface.md → topics/attack-surface.md` and renamed `learner/patterns.md → learner/MEMORY.md`, but `agent-health.yml` hardcoded both old paths. Stale for ~9 days (2026-05-30 → 2026-06-08), producing 37 phantom orphan-spec findings + vacuously-green checks. Root: code-style.md §9 rename-grep checklist targets `docs/*.md`, `.claude/rules/*.md`, MEMORY.md, agent-memory files — omits `.github/workflows/**`. Not the same as the plan.md `health-monitoring.yml` stale artifact (issue #804) — that file never existed; this is a file that moved and a workflow that tracked the old path. Secondary finding: xargs shell-parsing failed on unbalanced backtick/apostrophe in Status column, silently skipping rows; replaced with sed trim helper. One-off shell-parsing issue, not tracked as a separate row. On 2nd occurrence (another workflow path going stale after a rename): propose adding `.github/workflows/**` to the §9 rename-grep checklist in code-style.md. Also note: post-#704 path references may still lurk in other un-grepped locations — when any file is moved/renamed under `.claude/agent-memory/`, grep `.github/workflows/` proactively. |

| CR-local correctness nit SKIP'd as hypothetical, then cloud CR (ASSERTIVE) re-raised it as CHANGES_REQUESTED — forced extra CI cycle | 1 | 2026-06-09 | WATCHING — PR #811 (commit 22614058, issue #326 agent-health.yml stale-path fix): CR-local round 4 flagged stemmer regex `s/(ing|s)$//` mangling double-s words (e.g., "class" → "clas"). Triaged as SKIP-with-reason ("hypothetical — no current section heading ends in 'ss'; gold-plating an advisory heuristic"). Cloud CR (ASSERTIVE profile) on the PR raised the SAME finding as CHANGES_REQUESTED — a blocking review state that prevents auto-merge. This forced a fix commit (s/(ing|s)$// → s/ing$//; class/address/process test added) + re-push + full pipeline re-run (impl-critic, code-reviewer, semantic-reviewer, learner, coderabbit-sync) + a new CI cycle. Fix was <5 lines and behavior-preserving: impl-critic + per-commit reviewers confirmed identical output on all current section names. **Positive signal:** verification was fast precisely because the fix was small — pre-push cost was minimal, post-push cost was an extra CI cycle. **Root cause:** on auto-merge branches, a CHANGES_REQUESTED from cloud CR costs significantly more than applying the nit pre-push. The SKIP calculus that is correct for pure aesthetics ("different but equivalent shape") is incorrect for edge-case correctness findings that the ASSERTIVE profile re-raises as blocking. **Distinction from existing CR-local FP rows:** (1) row "CR-local false positives on Postgres CREATE OR REPLACE migration chain" (count=2) is about CR-local generating incorrect findings on migration chains — not about correctly-identified nits being SKIPped; (2) the stemmer finding was a REAL edge case, not a false positive. On 2nd occurrence (a CR-local correctness nit SKIPped pre-push, then cloud CR blocks the PR): propose adding the following to `agent-coderabbit-local.md` § Apply-vs-Defer — "Raise the SKIP bar on auto-merge branches: for correctness findings (edge-case correctness, not pure aesthetics), cloud CR ASSERTIVE will re-raise them as CHANGES_REQUESTED. If the fix is <10 lines and behavior-preserving, apply pre-push — the cost of applying is lower than the cost of a blocked PR + new commit + CI re-run." |
| Mechanical helper extraction introduces fidelity loss (scope drop or error-contract change) | 1 | 2026-06-09 | WATCHING — #796 (commits b95360f0 + 5e38c487): two distinct extraction failures in one cycle: **(1) impl-critic:** `cleanupFixtures` dropped `student_id` scope when consolidating per-spec cleanup into a shared helper — the original per-spec version deleted by `(studentId, questionId)`, the extracted shared version deleted by `questionId` alone, creating a cross-student deletion hazard; fixed before first commit by keying the DELETE on a composite `studentId::questionId` string. **(2) semantic-reviewer ISSUE:** `server-action afterAll` block changed error-handling from swallow (original used `console.error` + `return`) to throw during extraction into a shared cleanup helper, a behavioral divergence in a "pure move" refactor — the shared helper propagated the throw, which would abort downstream afterAll cleanup steps; fixed in 5e38c487 by wrapping in try/catch. Root cause for both: extraction mechanically copies a code block without auditing the extracted block's contract against the extraction target's requirements. Related to WATCHING row "Dead parameter forwarded when extracting handler" (count=1, 2026-06-01, #698/#666) — same root. **Dimensions of extraction fidelity to audit when extracting any helper:** (a) ownership-scope predicates preserved (all WHERE / key columns); (b) error-handling semantics preserved (swallow vs throw vs propagate vs log); (c) all parameters actually used in the extracted body (no dead params carried over). On 2nd distinct occurrence across a different commit/feature: consolidate with "Dead parameter forwarded" into a unified "extraction fidelity" RULE CANDIDATE; propose code-style.md §3 note or CLAUDE.md refactoring discipline section. |
| doc-updater rewrites historical milestone record instead of adding new entry (rule-adherence miss) | 1 | 2026-06-09 | WATCHING — #796 cycle (commits b95360f0 + 5e38c487): doc-updater initially rewrote the plan.md sprint milestone heading "Test hermeticity & infra — #796 split 3 oversized hub specs → 39 specs" to replace "39" with "41", directly violating the existing `agent-doc-updater.md` rule: "Exclude historical milestone records — a phase-completion entry (e.g. `docs/plan.md` 'Phase 5B-6 COMPLETE — N specs as delivered') documents the count at a point in time and must NOT be rewritten." Orchestrator caught it, restored the milestone verbatim, and directed doc-updater to add a new "→ 41 specs" entry under the correct "Test hermeticity & infra" heading instead. **This is a rule-adherence miss, NOT a new rule — the rule already exists and is explicit.** Root: doc-updater did not distinguish between a current-state literal it should update and a historical record it must not touch; the two look syntactically identical ("→ N specs") but differ semantically by whether the heading is a milestone/phase-complete entry. On 2nd occurrence: propose an explicit self-audit CHECK item to the doc-updater agent prompt: "Before editing any plan.md line containing a numeric spec count, check whether the line is a phase-completion / sprint-milestone record (contains COMPLETE, delivered, as-of, or is under a COMPLETE sprint heading) — if so, do NOT edit it; add a new entry instead." Cannot edit agent def file directly — propose to orchestrator. |
| Plan/Explore subagents ran against stale feature branch, producing wrong max-ID for shared sequential resource | 1 | 2026-06-09 | WATCHING — #326 (commits 202c6fa8 + 6f60ba2f): Explore + plan-critic subagents ran against fix/367 (predated PRs #802/#796/#817 on master). They concluded matrix max = CW, allocated CX–DC as free. At execution time, reading the live attack-surface.md on the working branch: #802 had already consumed CX–DG on master; renumbered to DH–DM. Root: branch was cut off current master but the Explore context included the wrong working tree (fix/367 instead of master). Distinct from "Vector-ID renumber-on-merge" (archive row 352): that row is about sibling PRs merging between branch-cut and commit on the same base branch — a merge-time reconciliation issue. This is about Explore running on the wrong branch entirely before the work branch was even created. First occurrence of this specific mechanism. On 2nd occurrence: propose a delegation-template note or CLAUDE.md note: "When a plan step allocates sequential IDs or version numbers from a shared file (attack-surface.md, migration numbering), the Explore subagent must read the file from the current MASTER working tree — not the feature branch — immediately before allocation. Verify by checking `git rev-parse --abbrev-ref HEAD` in the Explore subagent." |
| Sub-vector group reuses a 2-letter prefix already allocated to a different vector (namespace collision on sub-labels) | 1 | 2026-06-14 | WATCHING — #825 (1a068277 → impl-critic caught pre-commit): new sub-vector group for VFR RT DQ results-spec was labeled DB1/DB2/DB3; DB was already an allocated vector ID (internal-exam double-redemption, a completely different attack class). Impl-critic caught the collision; renamed to DR1/DR2/DR3 (DR row existed and these were sub-tests of it). Distinct from "Red-team spec self-labels vector mnemonic colliding with existing matrix ID" (live-table, count=3) — that row covers NEW top-level sequential IDs allocated past the wrong max; this is a 2-letter sub-label prefix that reuses an existing far-away vector's abbreviation. Also distinct from "Vector-ID renumber-on-merge" (archive row 352 — sibling PRs shifting the max while in-flight). Root: when grouping test cases under a sub-label scheme (DR1/DR2 etc.), the 2-letter prefix must be verified not to conflict with ANY existing vector abbreviation in attack-surface.md — not just the immediately preceding ones. On 2nd occurrence: propose adding to agent-red-team.md guidance — "Before labeling sub-tests with a 2-letter prefix (e.g. DR1/DR2), grep attack-surface.md for the prefix as a standalone vector ID to confirm no existing allocation conflicts." |
| Vector-label rename sweep used overly narrow grep scope (missed "mirror of X" / "vs X" label forms) | 1 | 2026-06-09 | WATCHING — #326 cycle (commit 202c6fa8): re-lettering of stale self-label BI → DM used grep scoped to "Vector BI". Post-commit semantic-reviewer found title "Stale token replay (mirror of BI)" which survived the sweep unchanged. Root: attack-surface.md cross-references an ID in multiple forms — "Vector X" (canonical), "mirror of X" (variant), "vs X" (comparison), and bare parenthetical "(X)" — but the rename grep targeted only the canonical form "Vector X". The variant forms are used in sub-vector titles and cross-reference prose. First occurrence of this specific mechanism. On 2nd occurrence: propose adding to the red-team agent or redteam skill: "When re-lettering a vector ID X, grep attack-surface.md for ALL label-reference forms: 'Vector X', 'mirror of X', 'vs X', '(X)', and trailing ' X)' — patch every form in a single sweep before committing." |
| Doc-sync: semantically-paired RPC detail sections not co-updated (cross-ref audit catches primary, misses paired sibling) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A cycle (commits 25bc9d62 + 7c296580): PR-level semantic sweep caught complete_overdue_exam_session detail section stale after mig 102 widened the mode guard; fix committed; doc-updater then found complete_empty_exam_session (the semantically paired RPC) equally stale — missed in the first fix because the cross-reference audit rule scopes to "the referenced section + summary tables" but not to parallel sibling sections covering the same mechanism. Both required separate fix commits. Root: cross-ref audit in agent-doc-updater.md is section-scoped; sibling RPCs in the same feature/migration family must be co-audited. On 2nd occurrence (different RPC pair, different PR): propose adding to agent-doc-updater.md "DO" — "after updating a detail section for an RPC, grep database.md for all RPCs sharing the same migration-level feature (mode guard, table, schema change) and co-update them in the same commit." |
| §15 carve-out comment dropped in CREATE OR REPLACE rewrite (security rule annotation lost in migration chain) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A cycle (CR local round 1, commit 2aed7c66 area): security.md §15 carve-out comment existed in mig 075 batch_submit_quiz, was dropped when mig 078 rewrote the function via CREATE OR REPLACE. The comment documented the immutable-column exception. CR local caught the missing annotation in 095c. Root: CREATE OR REPLACE rewrites copy function logic but authors don't grep for annotations/comments in the predecessor definition. On 2nd occurrence: propose adding to CLAUDE.md migration checklist — "when writing a CREATE OR REPLACE that supersedes a prior migration, grep the previous definition for any security-rule annotation comments (§N, security.md, etc.) and forward them to the new definition." |
| Loose error-message matching in tests (`includes('check')`) instead of asserting exact Postgres error code | 1 | 2026-06-10 | WATCHING — VFR RT Phase A cycle (impl-critic pre-commit e179093b): test-writer used `errMsg.includes('check')` to assert a CHECK constraint violation; impl-critic required `expect(error?.code).toBe('23514')`. Root: string-includes on error.message is fragile (Postgres message text is version-dependent; 'check' substring matches multiple error classes). Error code (SQLSTATE) is the stable contract. Second test batch (bdfae0dc) used the correct pattern first time (positive signal). On 2nd occurrence: propose note to test-writer/MEMORY.md — "Postgres error assertions must use `expect(error?.code).toBe('<SQLSTATE>')`, never `error.message.includes(substring)`. Common codes: 23514 check_violation, 23502 not_null_violation, 23505 unique_violation, 42501 RLS WITH CHECK, P0001 RAISE EXCEPTION." |
| Constraint/function verbatim-quote in docs not synced when constraint edited in migration (separate grep-and-update required) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A cycle (commits fcef799b + 9da1e647): mig 094 and mig 095 CHECKs each tightened; verbatim quoted constraint text in docs/database.md AND design.md stale in both cases; both caught by doc-updater post-commit rather than planned alongside the code change. Two distinct migrations within the same session — counts as 1 occurrence (same mechanism, same PR). On 2nd occurrence in a DIFFERENT PR: propose adding to code-style.md §9 or CLAUDE.md migration checklist — "when editing a CHECK constraint, RAISE EXCEPTION literal, or SQL function signature, grep docs/ and specs/ for the old verbatim text before committing and update it atomically." |
| Identical type union declared in N Server Action files instead of extracted to lib/ | 2 | 2026-06-07 | WATCHING (count 2) — (1) c6700161 PR #7: `QuestionFilter` duped across component+hooks → extracted to `lib/queries/quiz.ts`; (2) 19a0f1bb PR #797: `ActionResult` union byte-identical in 16 SA files → extracted to `lib/action-result.ts`. Both required large retroactive sweeps. Pattern: shared types declared locally at first use, not extracted until a sweep forces it. On 3rd commit: propose code-style.md §4 rule "extract shared SA result types to lib/ on 2nd reuse." Note: this is count=2 WATCHING, not yet RULE CANDIDATE — same-file-group repeats; needs a 3rd in a distinct feature area. |
| Subagent creates stray agent-memory dir in wrong cwd (apps/web/ instead of repo root) | 1 | 2026-06-07 | WATCHING — #797 cycle: plan-critic subagent cd'd into apps/web → created `apps/web/.claude/agent-memory/plan-critic/` (stray dir cleaned up). Root: subagents inherit cwd from invocation context, not CLAUDE.md root. On 2nd occurrence: propose to delegation template CONSTRAINTS section: "All file writes must use absolute paths rooted at the repo root (/media/sasha/T7/101.Claude Projects/01.LMS Plus/)." |
| Budget-curation copies live tracker rows to archive but does not delete the originals (copy-not-cut) | 1 | 2026-06-07 | WATCHING — #792 dedup cycle (b9099dd3): 2026-06-07 budget-curation relocated active rows to tracker-archive.md but left originals in the live table, creating 4 duplicate pairs. Fix: cut-not-copy — after pasting verbatim into archive, delete the source rows from the live table in the same edit. This is the exact mistake we guard against. On 2nd occurrence: add a curation checklist note to the budget-curation block in MEMORY.md: "STEP 3 MANDATORY: delete the pasted rows from the live table before saving." |
| Red-team matrix row description over-claims actor the test does not actually exercise | 1 | 2026-06-09 | WATCHING — #802 commit 8173a0b6: DA row described "admin-voided code" but the test seeds voided_by as a student; the RPC guard checks only voided_at presence (not who set it), so the test is valid but the description was misleading. Fixed in 8173a0b6. Root: matrix description written for the most common production path, not the actual test seed. On 2nd: log to cross-agent-lessons — matrix descriptions must reflect what the test actually seeds/exercises. |
| Doc-sync agent duplicates existing heading / RPC summary row instead of updating in place | 1 | 2026-06-10 | WATCHING — VFR RT Phase A cycle (f452eedb): doc agent run in report-only mode due to working-tree entanglement; follow-up doc agent wrote new content without reading the existing target section — introduced (a) duplicate heading in plan.md; (b) duplicate RPC summary row in database.md. Both caught by impl-critic pre-commit. Root cause: agent wrote new content without reading the existing target section to check for an existing entry. Distinct from "Docs agent fabricating plausible-but-wrong details" (hallucination from summary; this is structural duplication from not reading the target). On 2nd occurrence: propose note to agent-doc-updater.md "DO" — "before adding a heading or RPC summary row, grep the target file for the proposed heading/RPC name and update in place if found." |
| Test-writer agent generates cleanup/restore mutation without `{ error }` destructure (CR-local pitfall #3 in agent-generated code) | 2 | 2026-06-10 | RULE CANDIDATE — (1) 13fa0249: finally-block restore UPDATE without `{ error }` (§5); orchestrator caught. (2) 50c81b94 (#838 cycle): same agent error, different test (mig 104 soft-deleted-caller), same finally-block path missed. Root: cleanup code treated as boilerplate during generation; §5 not applied there. Both in same session/branch but different commits and different tests — meets distinct-mechanism bar at count=2. Propose note to test-writer/MEMORY.md: "every cleanup/restore mutation in generated tests must destructure `{ data, error }` per code-style.md §5 — the cleanup path is not exempt." |
| Integration-test count numeric literal in plan.md goes stale on each test-adding commit | 3 | 2026-06-10 | RULE CANDIDATE (count 3) — **(1)** VFR RT Phase A prior sub-cycle (plan.md 115→118→121): doc-updater caught drift post-commit. **(2)** 13fa0249 (2026-06-10): 121→125 caught post-commit by doc-updater. **(3)** b13d2c3a/899f440b (2026-06-10): 125→126→127 caught post-commit — 3rd occurrence in the same session. Same mechanism all three times: committer adds tests, does NOT update plan.md count in the same commit, doc-updater catches it after. **At count=3, the pattern is structural, not accidental.** The agent-doc-updater.md rule (proposed at count=2) catches it post-hoc; the correct fix is for the implementer to update plan.md in the same commit as the test files. **Strengthen the proposal:** add to CLAUDE.md workflow or agent-doc-updater.md note — "when a commit adds test files, run `pnpm test --run 2>&1 | tail -1` to get the suite count and update it atomically in the same commit; do not rely on doc-updater post-hoc catch." |
| New RPC/mode variant cloned from blueprint RPC omits supporting schema objects (partial unique index, trigger, constraint) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A (b13d2c3a/899f440b): `start_vfr_rt_exam_session` was modeled on `start_internal_exam_session`. Both `mock_exam` and `internal_exam` modes had a concurrency guard implemented as: (a) a partial unique index on `quiz_sessions(student_id) WHERE status='active' AND session_type=<mode>`; (b) a `unique_violation` (`23505`) handler in the RPC body. The vfr_rt variant cloned the function body but did NOT carry the supporting partial unique index. Caught by CR local (external), NOT by plan validation, impl-critic, or semantic-reviewer in the original Phase A cycle — the internal agents checked the function body but not the schema inventory for the blueprint. Root: `agent-workflow.md § Sibling file audit` covers helper functions and seed functions, but the mandate to "grep migrations for partial unique indexes and triggers referencing the blueprint table/function" is absent. The plan validator checked the function body shape but not whether each sibling's supporting schema objects were inventoried. On 2nd distinct occurrence (different RPC family): propose extending `agent-workflow.md § Sibling file audit` — "When cloning an RPC from a blueprint/sibling, grep migrations for partial unique indexes, triggers, and constraints referencing the blueprint's table — verify each is carried to the new variant's migration, not just the function body." |
| CR local suggests wrong fix rejected on a documented architectural decision | 2 | 2026-06-10 | RULE CANDIDATE — **(1)** VFR RT Phase A session earlier: CR local suggested adding `correct_count` at the question level (per-question aggregate), contradicting the explicitly-documented architectural decision that `correct_count` is row-level / per-blank (informational-only for dialog_fill). Documented in migs 100/102/103 and HANDOVER as "2nd occurrence this week." **(2)** b13d2c3a/899f440b cycle: CR suggestion repeated. Both rejected on documented grounds. Root: CR lacks access to the architectural decision record about row-level-only design; it proposes "obviously useful" derived fields without knowing the deliberate informational-only choice. Pattern: CR suggests a structural change that looks like a missing derived field but contradicts a decision we made. Distinct from "CR-local false positives on Postgres CREATE OR REPLACE migration chain" (that is about schema-chain misreading, not architectural-decision conflicts). On 3rd distinct suggestion in a different RPC family: propose adding a note to `.coderabbit.yaml` path-scope for `supabase/migrations/**` context — include a brief mention of the informational-only row-level constraint model. |
| CR local flags §15 carve-out application as a violation (carve-out misapplication false positive) | 1 | 2026-06-10 | WATCHING — VFR RT Phase A #833 (b13d2c3a/899f440b cycle): CR finding 5 flagged `start_vfr_rt_exam_session` caller-supplied `p_question_ids` as violating security.md §15 carve-out premise (the §15 carve-out allows question_ids from an immutable write-once column — quiz_sessions.config; CR argued the new RPC's question IDs come from the caller, not from an immutable column). Deferred to issue #833 as a genuine API-shape design question (gates Phase B, not a blocker now). **Root:** §15 carve-out is a narrow documented exception; CR reads the surface-level pattern ("question_ids passed in") and applies the rule without reading whether the exception's premise holds. Internal agents also missed this in both the original Phase A cycle AND the #831 fix cycle (the fix touched the exact `--§15 carve-out--` comment block and no agent flagged the misapplication premise). **Distinct from:** "§15 carve-out comment dropped in CREATE OR REPLACE rewrite" (tracker row: annotation lost in rewrite; different mechanism). **On 2nd occurrence:** add to cross-agent-lessons FP catalog: "Before accepting a finding that §15 carve-out is violated, verify the carve-out premise — specifically, are the question_ids sourced from an immutable write-once column (session config), not from the caller? The carve-out is narrow; agents conflate 'question_ids present' with 'caller-supplied question_ids violate §15'." |
| Doc migration-range footer literal stale across multiple doc surfaces (database.md + decisions.md) | 1 | 2026-06-10 | WATCHING — #838 cycle (37106063 + cd927b08): docs/database.md had a "migrations covered: 094–103" footer literal; doc-updater caught it stale (→094–104) in round 1. docs/decisions.md had the same literal drift, caught by doc-updater in round 2. Two different doc surfaces, same PR, same mechanism — counts as 1 occurrence. Root: when a new migration is added, docs with a "last migration: N" or "migrations covered: NNN–NNN" range footer are not included in the standard doc-sync scope. Distinct from "Red-team spec-count prose drift" (that is about grep-able spec-count numerics in ≥3 surfaces; this is a range literal in 2 surfaces). On 2nd occurrence in a different PR: propose adding to agent-doc-updater.md — "when a commit adds a migration, grep docs/database.md AND docs/decisions.md for any migration-range footer literal (e.g. `Migrations covered:`, `last migration:`, `094–1`) and update the upper bound atomically in the same commit." |
| Mode/role whitelist evaluated mechanically (does it reject X?) without evaluating admitted values against output sensitivity | 1 | 2026-06-10 | WATCHING — fce9a871 (#830 cycle): internal agents reviewed the `submit_quiz_answer` whitelist change (mig 095b/104) across 4 prior review rounds and verified it rejected `vfr_rt`. Nobody asked: "should exam-mode sessions (which the whitelist admits) be allowed to call an RPC that returns `is_correct` + `correct_option_id` — i.e., is this RPC's output safe to return to an in-progress exam caller?" Cloud CodeRabbit caught the answer-oracle gap. Root: whitelist reviews check rejected values (correct) but not whether each admitted value is compatible with the RPC's output contract (the gap). Applicable when ANY mode/role whitelist guards an RPC that returns sensitive output (correct answers, scores, pass/fail). On 2nd occurrence: propose adding to semantic-reviewer or code-style.md §7 — "when reviewing a mode/role whitelist on a data-returning RPC, evaluate each admitted value against the RPC's output sensitivity, not only the excluded values." |
| Mechanism-pin suggestion applied then reversed in the next cycle (premature pin churn) | 1 | 2026-06-10 | WATCHING — fce9a871/#830 cycle: in the previous cycle an `actor_role` NOT NULL assertion was applied to a test at CR suggestion (mechanism-pin). One cycle later, the explicit `active_user` gate landed in the same RPC, making the mechanism-level assertion redundant and it was reverted. One commit's churn. Root: a SUGGESTION-level mechanism-pin applied just before a mechanism change is imminent is inherently unstable. No clear prevention signal (the gate was not visible at pin time). Single occurrence only; do not elevate. On 2nd: look for a pattern of applying mechanism-level assertions in the same session as a pending mechanism change. |
| Explore/research subagent reports stale "latest" function definition (CREATE OR REPLACE chain tail missed) | 1 | 2026-06-10 | WATCHING — docs/697 cycle (commits 3260e2d9 + 903095ed): Explore subagent reported 20260430000012 as the latest batch_submit_quiz migration with "no redefinition after 078"; actual latest is 20260601000001 (mig 091). impl-critic's independent Pre-Flag Verification caught it. Distinct from "Semantic reviewer stale-baseline false positive" (that is a reviewer missing the chain) and "CR-local false positives on CREATE OR REPLACE migration chain" (that is an external CLI). This is the planning/Explore phase — before any code is written. On 2nd occurrence: propose adding to delegation template CONTEXT: "For any migration-citing spec, include the result of `ls supabase/migrations/ | grep <function_name>` so the Explore subagent reports the full chain, not the first match." |
| Quoted exact-string literal written independently in design.md and tasks.md drifts on copy (casing / wording mismatch) | 1 | 2026-06-10 | WATCHING — docs/697 cycle (commits 3260e2d9 + 903095ed): guard-message literal 'Session...' written as 'session...' in tasks.md A.9b while design.md and the actual RPC used capital S. impl-critic (round 1) caught it in design.md; the fix was applied but the string was independently written in tasks.md without the same correction — semantic-reviewer caught the surviving mismatch post-commit (903095ed). Distinct from "Spec-doc literal counts drifting" (that is about numeric derived values, not quoted string contracts). On 2nd occurrence: propose a spec-authoring note — "when writing a quoted string that must match an RPC RAISE or error literal, define it once in design.md and reference it in tasks.md rather than writing it twice." |
| Shell script parsing LLM output for a gate decision using exact-string match (fails open on prose-wrapped token) | 1 | 2026-06-10 | WATCHING — issue #832 (commits f7ec4a11 + 7e803759): run-security-auditor.sh compared stripped last line for exact equality with "BLOCKED"; model emitted "BLOCKED: Fix the HIGH finding before pushing." — fell through to unconditional "Push approved." fix: parse_verdict() blocks on BLOCKED prefix at any line start, approves only on lone APPROVED token, fails CLOSED when neither present. Also adds --parse-verdict stdin entry point so the parser is testable without the claude CLI; 5 pinning test cases (empty, APPROVED-then-BLOCKED ordering, CRLF ×2, indented APPROVED) cover 12/12 green. Root: LLM output is prose; gates must match tokens, not exact lines. On 2nd occurrence (any gate script re-introducing exact-equality on LLM output): propose a CLAUDE.md or agent-workflow.md note: "Any shell gate on LLM verdict must (a) match a token at start-of-line not exact full-line equality, (b) require an explicit APPROVED token to pass (fail-closed default), (c) have a test script covering empty, prose-wrapped token, and reversed-ordering transcripts." |
| CLAUDE.md QA-pipeline section drifts when lefthook.yml changes (new gate added or existing gate description stale) | 2 | 2026-06-20 | RULE CANDIDATE — (1) #833/#840 cycle (1309878c): CLAUDE.md §QA-pipeline claimed "pre-commit: biome lint/format + type-check + unit tests" but lefthook.yml intentionally omits unit tests from pre-commit ("fast gates only; full suite in CI"); caught by test-writer. (2) #925 Phase 3 (ad9857d9): doc-updater found CLAUDE.md §QA-pipeline pre-commit description went stale when a new lefthook guard was added; doc not updated in the same commit as the lefthook change; fixed in C4 (cc0fd0df). Common root: lefthook.yml changes do not trigger a CLAUDE.md QA-pipeline audit. Proposed rule: any commit touching lefthook.yml must also audit CLAUDE.md §QA-pipeline for accuracy; add to CLAUDE.md or agent-doc-updater.md. |
| Blueprint RPC function annotation (STABLE/VOLATILE/IMMUTABLE) carried verbatim into cloned variant without verifying correctness | 1 | 2026-06-11 | WATCHING — #833/#840 cycle (1309878c): `get_vfr_rt_exam_results` was cloned from `get_vfr_rt_exam_questions` (mig 099b). The `ORDER BY random()` RPC had been annotated STABLE (copied from 099b), but STABLE is incorrect for any function calling random() — it implies the function returns identical output given identical inputs in the same transaction. Semantic-reviewer caught it pre-commit; fix changed annotation to VOLATILE in 1309878c. Root: RPC cloning copies the function signature and volatility annotation along with the body; the annotation may have been correct for the source RPC but wrong for the target (different body, different side effects). On 2nd occurrence (a cloned RPC ships with a wrong volatility annotation): propose adding to CLAUDE.md migration checklist or agent-workflow.md Sibling file audit — "When cloning a Postgres function, re-evaluate the VOLATILITY annotation (STABLE / VOLATILE / IMMUTABLE) independently for the new function body; do not assume the source's annotation is correct for the clone." |
| Migration-writer subagent produced a NOT NULL violation in a test fixture (explanation_text: null in schema requiring NOT NULL) | 1 | 2026-06-11 | WATCHING — #833/#840 cycle: a migration-writer subagent generated a test fixture with explanation_text: null against a schema column declared NOT NULL. Caught by running the suite (runtime error, not type-check). Root: subagent did not read the column constraints before generating the fixture shape. Single occurrence; relates to existing row "Test fixture shape mismatch (wrong/missing field in fixture object)" (count=2, RULE CANDIDATE) but is a distinct mechanism (NOT NULL vs wrong-field-name). On 2nd occurrence: propose adding to test-writer/MEMORY.md — "when generating fixtures for a table with NOT NULL columns, read the CREATE TABLE migration before emitting null for any field." |
| impl-critic used static `grep 'it('` count as integration-test baseline instead of vitest runtime count | 1 | 2026-06-11 | FALSE POSITIVE (impl-critic) — #833/#840 cycle: impl-critic claimed plan.md integration-count baseline of 136 was wrong because `grep 'it('` on the test files returned 121; vitest runtime count was 144 (after +8 from the session). The static grep undercounts because it misses dynamically generated test cases and test.each() permutations. Convention: the authoritative count is the vitest runtime output, not a static grep. This false ISSUE invalidated a plan.md count update that was correct. Root: impl-critic used a mechanical grep-count shortcut to validate a runtime-count claim. Do not re-raise this; static grep != vitest count. |
| Newly-published transitive-dep advisory blocks pre-push `pnpm audit` hook mid-unrelated-run; fix = bounded pnpm override pin | 1 | 2026-06-13 | WATCHING — chore/esbuild-0281-audit-fix (f02a3a87): GHSA-gv7w-rqvm-qjhr (esbuild <0.28.1, high severity) published while an unrelated red-team-spec auto-merge batch (#851) was mid-run. `pnpm audit --audit-level=high` in the pre-push Lefthook hook started failing repo-wide, blocking ALL pushes. Fix: added bounded override `esbuild >=0.28.1 <0.29` to root package.json, mirroring 5 existing security pins (undici, zod, next, fast-uri, postcss). Agents all clean; 1 semantic SUGGESTION (add a JSON comment) skipped — package.json is strict JSON (no comments allowed). Context: 5 prior override pins confirm this is an established maintenance pattern, not a one-off. Root: transitive deps can publish advisories asynchronously to any active dev work; the audit hook has no per-package scope, so any high advisory blocks everything. On 2nd occurrence (another transitive-dep advisory interrupts work mid-session): log a durable note in cross-agent-lessons — "the pnpm override-pin pattern is the expected remedy; the bounded version range `>=fixed <next-major` prevents over-pinning; the override section in root package.json is the canonical location." Do NOT propose a rule change (count=1; the existing overrides are sufficient precedent — future occurrences just follow the pattern). |
| Stale in-file spec file-header test-count after adding a test case | 1 | 2026-06-13 | WATCHING — #851, 680c77e4: rpc-cross-tenant-isolation.spec.ts had a `// 13 tests` (or similar) file-header comment; adding Vector DX left the count stale at 13 (should be 14). Caught by the PR-level semantic sweep (commit 680c77e4 is a docs-only fix). Root: the per-commit doc-updater's "repeated-numeric-literal" rule covers external doc surfaces (tech.md, decisions.md); it does NOT scan in-file `// N tests` header comments inside the spec itself. The file-header comment is a separate surface. Distinct from "Red-team spec-count prose drift across multiple doc surfaces" (that rule covers tech.md ×3 + decisions.md; this is the spec file's own header). Distinct from "Integration-test count in plan.md goes stale" (that is about plan.md; this is about the spec file header). On 2nd occurrence (different spec file or PR): propose adding to agent-doc-updater.md or code-style.md §7 — "when adding a test case to a Playwright spec that contains a file-header test-count comment (`// N tests` or similar), update the count in the same commit." |
| Race/concurrency test accesses `data?.[0]` (or similar first-element index) without a prior `.toHaveLength()` guard | 1 | 2026-06-13 | WATCHING — #842 (b8109233): integration test for concurrent `complete_quiz_session` accessed `winner.data?.[0]` without first asserting `expect(winner.data).toHaveLength(1)`. Semantic-reviewer ISSUE: if the RPC returns null or empty, the failure manifests as an opaque `Cannot read properties of undefined` rather than a diagnostic message. Fix: add `toHaveLength` assertion before indexing. Distinct from "TS strict mode requires `!` non-null assertion on array index" (archive row 163/269 — that pattern is about TypeScript TS2532 compiler errors; this is about a runtime diagnostic-quality gap in test assertions for concurrency/race specs). Related: §7 throw-guard suggestion (replace bare `toHaveLength(1)` on a `winners` array with a throwing guard that prints all RPC outcomes — also applied). On 2nd occurrence (different spec or RPC family): propose adding to code-style.md §7 "Red-Team Isolation/Negative Assertions" or integration test rules — "When accessing `data[0]` in a concurrency or race integration test, assert `expect(data).toHaveLength(1)` first so a regression produces a diagnostic error rather than an opaque undefined-access." |
| attack-surface.md DW/gap-row Notes stale after the documented gap is resolved by a test in a different PR | 1 | 2026-06-13 | WATCHING — #842 (b8109233): the DW-row Notes in attack-surface.md still said "no new spec at any layer" after the concurrent-completion test for `complete_quiz_session` was added in commit b8109233 — red-team agent caught it in the same cycle and the fix was applied in-commit. Root: the DW-row was written when the gap existed; when a different PR (or a later commit) closes the gap, attack-surface.md Notes are not automatically updated. Distinct from "Red-team spec-count prose drift" (that pattern is about numeric count literals in external docs — tech.md, decisions.md; this is about prose Notes inside the attack-surface.md matrix itself). On 2nd occurrence (a gap-row Notes field still says "no spec" after a spec ships in another PR/commit): propose a pre-push step or red-team memory note — "Before pushing any integration or red-team test commit, scan attack-surface.md for rows whose Notes field records a gap that your new test covers, and update the Notes to reflect RESOLVED state." |
| Plan cites superseded RLS policy (DROP POLICY + CREATE POLICY chain) instead of binding latest definition | 1 | 2026-06-13 | WATCHING — #849 cycle: plan cited `flagged_questions` RLS USING clause from mig 043 (`deleted_at IS NULL` present) but the BINDING policy is mig 20260323000050 which DROPS the mig-043 policy and creates a new ownership-only policy (no deleted_at filter). plan-critic caught it. Root: orchestrator looked up the table's RLS policy in an early migration without tracing the DROP POLICY + CREATE POLICY chain forward. Mechanism is the same family as "Semantic reviewer stale-baseline false positive" (trace the chain to latest) and "Explore/research subagent reports stale 'latest' function definition" — but this is the ORCHESTRATOR'S OWN PLAN, not a reviewer false-positive. The existing "Pre-Flag Verification" rules in agent-critic.md / semantic-reviewer.md cover reviewers; the orchestrator's plan-phase citation is not explicitly covered. On 2nd occurrence (plan cites superseded RLS or function definition caught by plan-critic or impl-critic): propose adding to agent-workflow.md Plan Validation impact-analysis step — "When citing an RLS policy guard in a plan, trace the full DROP POLICY + CREATE POLICY chain in migrations to the latest definition — the first migration that adds the policy is not authoritative if later migrations drop and recreate it." |
| Asymmetric E2E `afterAll` guard variable initialization (victim var lacks '' initializer vs attacker var) | 1 | 2026-06-13 | WATCHING — #849 6e258f6a: semantic-reviewer flagged that `victimUserId` lacked a `''` initializer (declared as `let victimUserId: string` while `attackerUserId = ''` had one). If beforeAll throws before assigning victimUserId, the afterAll soft-delete filter sees `undefined` → wrong scope. Defensive fix: all guard-scope variables in beforeAll/afterAll pairs must have a consistent '' initializer (string) or null (if nullable). Distinct from "Test cleanup: throw inside finally block / multi-block afterAll without error accumulator" (that is about execution ordering on throw; this is about initialization symmetry preventing undefined-filter in cleanup). Also distinct from "vacuous test" (archive row 102 — that is about empty assertion tables). On 2nd occurrence (different spec): propose adding to code-style.md §7 E2E Hermiticity — "Sentinel variables used in afterAll/afterEach soft-delete filters (userId, orgId, sessionId) must be initialized to '' or null so that a beforeAll failure does not cause the cleanup filter to pass undefined/uninitialized to the DELETE/UPDATE WHERE clause." |
| `as unknown as T` cast without runtime guard in test contract helper / integration test (§5) | 4 | 2026-06-20 | RULE CANDIDATE — (1) #818 (test/818-strengthen-redteam-assertions): impl-critic caught cast in `expectWithinTimeSubmitContract` without typeof/Array.isArray guard before consuming value; (2) #845 (test/845-vfr-rt-results-answers-array): semantic-reviewer caught `AnswerRow[]` cast in integration test without Array.isArray guard before forEach — applied fix; (3) #925 PR fixes (03cb2fa7, 2026-06-20): CodeRabbit caught unguarded `as string` cast on an RPC result field in the new app-layer integration test (same §5 principle, simpler cast form); (4) #925 Phase 2 / PR #930 (665c0bdf, 2026-06-20): CR cloud caught `sessionId as string` in `fixtures.ts` — paired only with a truthy check (no `typeof`), fixed with `typeof sessionId === 'string'` guard. All four caught by post-commit/CR agents; all in test/integration files. Root: §5 rule applied to production code; test helpers and integration test assertions treated as exempt. At count=4: Phase-4 rule promotion note pending in HANDOVER — propose adding explicit note to code-style.md §5 cast-guard section: "§5 cast-without-runtime-guard applies equally to test helper functions and integration test assertions — test files are NOT exempt." Repo-wide test-file sweep also queued. |
| Single-concern sequential DB-seed/infra helpers exceeding 30-line function cap | 3 | 2026-06-19 | RULE CANDIDATE — three distinct file families, all WARNING/non-blocking: (1) #844 (8be57963): 4 integration-test seed helpers in vfr-rt-helpers.ts (insertShortAnswerQuestion 33L, insertDialogFillQuestion 32L, insertMcQuestion 35L, getRtRefs 33L) — sequential DB inserts, no branching; (2) #869 (redteam E2E spec cycle): `submitAndReplay` E2E helper, 46L — sequential exam-answer submit + replay steps; (3) #902 (redteam/902-record-code-emailed-e2e): `seedCode` E2E helper, 38L — sequential DB insert + auth setup steps. All three: single-concern, linear flow (sequential awaits), no branching logic; code-reviewer classified each as WARNING/non-blocking. Proposed code-style.md §3 exception clause (blocked pending orchestrator/user approval alongside #903 §-promotion work): "Single-concern sequential DB seed/setup helpers in test infrastructure (linear flow, no branching logic — just sequential awaits) are exempt from the 30-line function cap; document with a JSDoc `// Test infra: sequential DB seed — linear flow, no branching` comment." This is a narrow named exception for linear-flow infra code, identical in spirit to the existing 4-param infrastructure utility exception. NOTE: edit to code-style.md is PROPOSED ONLY — orchestrator applies it, not the learner agent. |
| Vitest passes / tsc fails on test file (esbuild strips types at build; tsc strict-mode catches shape mismatches) | 2 | 2026-06-14 | RULE CANDIDATE — (1) #789 cycle (archive row 353, 60047c2c): subagent ran `pnpm test` but omitted `pnpm check-types`; type error in a Server Action file merged (Record<string,unknown> vs Json param). (2) #533 cycle: test-writer-added test passed prop `isExam` absent from test helper's `Required<>` type; vitest passed at runtime (esbuild transpiles, strips types), pre-commit tsc caught the failure before commit. Root in both: Vitest/esbuild transpiles-and-runs with types stripped; tsc strict-mode is the only gate that catches type-shape mismatches in test files, fixture helpers, and test assertions. Distinct from "Test fixture shape mismatch (wrong/missing field in fixture object)" (that is about runtime fixture shape — this is about TypeScript type-checking of props/params during compilation). Both occurrences are distinct PRs, distinct file families, distinct type errors — count qualifies for RULE CANDIDATE. Propose adding to agent-workflow.md delegation template DONE WHEN section: "BOTH `pnpm test` (vitest green) AND `pnpm check-types` (tsc clean) must pass — esbuild strips types so vitest alone does not catch type errors." |
| Loading-state flag scope mismatch (session-level flag wired to per-answer spinner) | 1 | 2026-06-14 | WATCHING — #533 (e89cad75, semantic-reviewer ISSUE): session-level `submitting` flag (from `useQuizSubmit`) was wired to the per-answer "Submit Answer" spinner; in study mode the spinner never fires because `submitting` gates the session-submit flow, not the per-answer RPC. Fix: added a distinct `answering` boolean scoped to the individual per-answer RPC call. Distinct from "Missing `setSubmitting(true)` before async call in form/button handler" (archive row 108/299 — that is about *when* to set the flag: before vs. after the await; this is about *which flag* to use: wrong source/scope). Distinct from "useTransition + manual loading state hybrid fragility" (archive row 43/289 — that is about two parallel state mechanisms for the same operation; this is about a single mechanism pointed at the wrong scope). Pattern: multi-step UIs with both session-level and item-level async operations (quiz with per-answer + session submit; exam with per-answer + session complete) can easily cross-wire flags between scopes. On 2nd occurrence (a different per-item spinner or per-item disabled guard driven by a session-level flag): propose adding to code-style.md §6 React state rules — "Each loading spinner must be driven by a flag scoped to its own async operation; a session-level submitting flag must not gate a per-answer or per-item indicator (they are separate RPCs with separate in-flight states)." |
| Full test suite catches cross-component regression invisible to per-commit targeted test run | 1 | 2026-06-14 | WATCHING — #533 (03582906): `disabled={s.submitting \|\| s.answering}` blocked answering the NEXT question while the prior answer's RPC was in flight; the bug was introduced in commit e89cad75 (per-answer spinner fix) and caught only by quiz-session.test.tsx in the full `pnpm test` suite run, not by per-commit targeted tests on changed files. Root: targeted test runs (only run tests for files in the diff) miss regressions in consumer files not touched in the commit. The full suite runs cross-component flows (quiz-session orchestrating quiz-controls, option buttons, useQuizSession hook together) that targeted runs skip. Distinct from "Subagent verified clean via pnpm test but skipped pnpm check-types" (that is about a different gate being omitted — tsc; this is about test scope breadth, not a missing gate). Distinct from "CLAUDE.md QA-pipeline section claims pre-commit runs unit tests" (that is about a docs drift for what pre-commit actually runs). On 2nd occurrence (a targeted-run-clean commit introduces a regression only the full suite catches): propose adding to CLAUDE.md §Workflow or agent-workflow.md § Post-Implementation Pipeline — "before push, run the full test suite (`pnpm test`), not only tests for changed files; cross-component regressions (consumers of changed hooks/components) are invisible to targeted file-scoped runs." |
| `<LoadingButton loadingText>` with icon child hides icon during loading (children replaced by loadingText) | 1 | 2026-06-14 | WATCHING — #533 (2e0465b4, semantic-reviewer): LoadingButton with a `loadingText` prop replaces the entire `children` slot with the loading text during loading; any icon passed as a child is hidden. Semantic-reviewer flagged this on the sweep of shadcn Button consumers onto LoadingButton. Fix per callsite: either omit the icon when loadingText is set, or confirm the icon-hidden state is intentional (loading state visually replaces icon). First occurrence. Specific to the LoadingButton component introduced in this PR. On 2nd occurrence at a different callsite (different icon + loadingText combination where icon-hidden is unintentional): propose a CLAUDE.md or code-style.md §2 component-usage note — "`LoadingButton` with `loadingText` replaces all children during loading; do not pass icons as children unless the icon-hidden loading state is intentional and reviewed." |
| Code-reviewer false-positive: flags pre-existing over-limit file whose over-limit lines are not in the diff | 1 | 2026-06-14 | WATCHING — 4dea699a (#885/#887): code-reviewer emitted 4 WARNING false-positives — finish-quiz-dialog.tsx (302L), use-quiz-submit.ts (90L), use-quiz-state.ts (84L) are all over their §1 caps on master baseline, but 4dea699a added ZERO lines to those files' line counts (the diff touched logic inside them but did not add net lines that worsened the cap). The agent-code-reviewer.md suppression rule explicitly states "Only flag violations introduced or worsened by lines in the diff — do not flag pre-existing violations on lines not touched by this commit." The reviewer applied the rule to the file-was-touched level (file appears in the diff) rather than the over-limit-lines-were-added level. Distinct from "Pre-existing file-size violation surfaced when commit adds lines to over-limit file" (that pattern IS a genuine flag — commit worsens the count; this is a false positive — commit does not worsen the count). On 2nd occurrence (same FP class — touched but not worsened): propose adding a concrete example to agent-code-reviewer.md clarifying the distinction — a file touched by the diff but with no net-additive lines to its over-limit section is NOT flagged. |
| Verbose inline comment pushing file over line cap (defense-in-depth prose in component body) | 1 | 2026-06-14 | WATCHING — 8a6704a8 (#885/#886): quiz-session.tsx (pre-existing 155L, over the 150-line React component cap) grew to 161L; the keepMounted fix itself was ~3 lines, but a 3-line explanatory comment ("// Keep mounted during RPC so the spinner stays visible — dismounting during in-flight XHR causes...") added further lines. Semantic-reviewer suggested a trimmed comment; orchestrator applied and reduced to 157L. Still over cap; split deferred to #887. Root: multi-line explanatory prose in a component body counts toward the line cap just as code does, but authors often add comments freely without accounting for cap pressure. Distinct from "Biome auto-format expanding compact code past file-size limit" (that is Biome reformatting at commit time; this is hand-written prose during authoring). On 2nd occurrence (different component, comment-caused cap breach): propose a code-style.md §1 note — "Explanatory comments count toward file line limits; prefer a JSDoc comment on the function or a `// see docs/decisions.md §N` pointer over multi-line inline prose when the file is at or near its cap." |
| Shared component a11y/DOM contract change (alt text, aria roles) breaks un-mocked consumer tests + drifted vi.mock stubs simultaneously | 1 | 2026-06-14 | WATCHING — #863 (8b7c5de6): ZoomableImage alt prop changed from descriptive to presentational (alt="" aria-hidden=true) to fix a11y double-announce under an aria-labelled link. This broke (a) question-card and feedback-panel test files that rendered ZoomableImage un-mocked and asserted `getByRole('img',{name})` — test assertions depended on the component's old aria-visible alt; (b) report-question-row's vi.mock of ZoomableImage had drifted from the real component's new prop interface. Both failures were in consumer test files, not the changed component's own test. Targeted per-file test run missed them; only pnpm test (full suite) surfaced them. Root: when a shared visual component's a11y/DOM contract changes, consumers that render it un-mocked carry implicit assertions on that contract, and vi.mock stubs may not be updated to match. Two failure modes, one root cause, one commit = count=1. Distinct from archive row 129 "Test mock type becoming stale after production type refactor" (that was a TypeScript type annotation on a mock interface; this is a behaviorally-observable DOM/a11y contract break in rendered assertions). On 2nd occurrence (different shared component or PR): propose agent-test-writer.md note — "When changing a shared component's a11y/DOM contract (alt text, aria role, aria-hidden, aria-label), grep for all vi.mock stubs of that component AND for test files that render it without mocking; update both in the same commit." |
| Targeted per-file test run misses consumer regressions caught by full suite | 1 | 2026-06-14 | WATCHING — #863 (8b7c5de6): after changing ZoomableImage's a11y contract, test runner was scoped to the changed component's own test file — which passed. pnpm test (full suite) revealed 2 consumer tests (question-card.test.tsx, feedback-panel.test.tsx) that rendered ZoomableImage un-mocked and asserted `getByRole('img',{name})` — assertions depended on the old alt text that was now presentational. Per-file scope is always sufficient for isolated unit changes; it is insufficient when a change in a shared component propagates DOM-observable behavior changes to consumers. Root: there is no per-commit convention for when to run full suite vs. targeted; the default is targeted. On 2nd occurrence where a shared-component contract change causes downstream consumer test failures not caught by targeted run: propose adding a note to agent-workflow.md or agent-test-writer.md — "After any change to a shared UI component's a11y/DOM contract (alt, aria, role), run pnpm test (full suite) rather than a targeted per-file run to surface consumer regressions." |
| RPC body/param change not matched by same-commit database.md + COMMENT ON FUNCTION + types.ts sweep | 1 | 2026-06-14 | WATCHING — #890 PR feat/quiz-ux-batch-860-865: get_random_quiz_questions COALESCE p_count clamp was added to the migration but (a) database.md L2251/L2256 param+returns descriptions, (b) the COMMENT ON FUNCTION in the same migration, and (c) types.ts `GetRandomQuizQuestionsArgs` (p_has_image field) were all initially missed; caught post-commit by internal agents. Root: three distinct co-update surfaces exist for any RPC body/signature change, and the implementing agent only updated the migration body. Distinct from "doc-updater hallucination" (fabricating wrong claims without reading source — archive rows 333+) and "doc-updater duplicates heading" (structural duplication — archive row 373). This is a scope-of-update gap: correct migration, stale metadata in 3 other locations. On 2nd occurrence (different RPC, different PR): propose adding to agent-doc-updater.md DO — "When any migration changes an RPC body OR signature (params, return type), the same commit must update: (1) database.md RPC entry (param + returns lines), (2) the COMMENT ON FUNCTION in the migration, (3) the matching TypeScript Args/Result type in types.ts." |
| CR-local surfaces a MAJOR safety bug that all internal agents missed (NULL input → uncapped LIMIT) | 1 | 2026-06-14 | WATCHING — #890 PR feat/quiz-ux-batch-860-865 round 2: CR-local flagged `p_count` IS NULL → `LIMIT NULL` → uncapped full-table return (MAJOR severity). Internal agents (code-reviewer, semantic-reviewer, impl-critic) all approved the migration without catching it. Fix: added `COALESCE(p_count, 100)` clamp as a NULL guard. This is a reinforcing signal for the existing CR-local value note in `cross-agent-lessons.md` and the "Common Pitfalls" section in `agent-coderabbit-local.md`. Distinct mechanism from the existing CR-local catch list (§5 select-id, cast guard, silent failure paths, helper-in-loop, CREATE OR REPLACE chain FP). On 2nd occurrence (CR-local catches a safety MAJOR that internal agents all missed): propose adding this NULL-input-uncapped-LIMIT sub-pattern to the Common Pitfalls list in agent-coderabbit-local.md — "NULL RPC param passed directly to LIMIT/OFFSET/COUNT without COALESCE → uncapped full-table return; internal agents do not check plpgsql function bodies for missing null-guard on limit expressions." |
| CR suggests fix that conflicts with an active Biome rule (false positive class) | 1 | 2026-06-14 | WATCHING — #890 PR feat/quiz-ux-batch-860-865: CR suggested destructuring the `deps` array in a useEffect to satisfy `useExhaustiveDependencies`. The suggested destructure was itself flagged by Biome's `useExhaustiveDependencies` rule (the suppression comment in the file intentionally uses the full prop reference to avoid a stale-closure; destructuring would change the dep identity). This is a false positive where CR's suggested fix produces a Biome rule violation. Distinct from "CR-local false positives on CREATE OR REPLACE migration chain" (that is a scope/chain-tracing FP). Distinct from "CR local suggests wrong fix rejected on a documented architectural decision" (that is a known architectural constraint in code; this is a conflict between CR's suggested code and an active linter rule). On 2nd occurrence (CR suggests a fix that Biome itself rejects): propose noting in agent-coderabbit-local.md Handling Results DO — "Before applying any CR suggestion that adds or removes React hook dependency array members, verify the result passes `pnpm lint` (Biome useExhaustiveDependencies) — CR does not run Biome and may suggest forms the linter rejects." |
| Component file pushed over its 150-line cap by an additive doc-comment expansion | 1 | 2026-06-17 | WATCHING — #890 final cleanup cycle (feat/quiz-ux-batch-860-865 commits 170c5b6c+): quiz-session.tsx was exactly at the 150-line component cap; a code-reviewer-noted comment for a `q` shortcut was initially written as 3 lines, pushing the file to 152L. code-reviewer WATCH caught it; fix = condense comment to 1 line → 150L. Root: author accounted for logic lines but not doc-comment expansion when the file is already at the cap. Distinct from "Biome auto-format expanding compact code past file-size limit" (that is formatter-induced expansion; this is author-added comment text). Distinct from "Pre-existing file-size violation surfaced when commit adds lines" (that row is about pre-existing over-limit state; here the file was exactly at cap and newly-added comment text crossed it). On 2nd occurrence (different file or PR): propose noting in agent-code-reviewer.md — "When a file is exactly at its line cap, any additive comment expansion is a WATCH item; instruct authors to prefer terse inline comments when the file is at cap." Do NOT promote to rule at count=1. |
| DB/caller-supplied value interpolated into HTML/SVG/XML template string without escaping | 2 | 2026-06-19 | RULE CANDIDATE → code-style.md §5 — (1) #890: `uploadSampleImage` in `seed-quiz-setup-eval.ts`, SVG template, `label`/`color` params unescaped → fixed with `escapeSvgAttr()` helper. (2) #901/feat/internal-exam-code-email: `internalExamCodeEmail()` in `lib/email/templates/internal-exam-code.ts` interpolated `studentName`, `subjectName`, `code`, `expiresAt` (all DB values) directly into HTML body string → semantic-reviewer SUGGESTION → fixed with `esc()` helper (replace &/</>/" entities). Both: function builds HTML/SVG via template literals, treats DB-derived params as safe without escaping. Rule: "Any function that builds an HTML, SVG, or XML string via template literals must escape caller-supplied parameters with an HTML entity escape helper (`esc()` / `escapeSvgAttr()` or equivalent) before interpolation — even when current call sites are server-trusted." |
| Server Action result `error` field typed `string` instead of closed domain-literal union | 1 | 2026-06-19 | WATCHING — #901/feat/internal-exam-code-email PR-sweep: semantic-reviewer flagged `SendCodeEmailResult.error: string` as brittle (future handler might route or display `error` without knowing its range — any leaked SDK message would compile fine); fixed to a 4-literal union (`'Invalid input' | 'Code not found' | 'Code is no longer active' | 'Failed to send email'`). The send-code-email.ts JSDoc explains the rationale: a literal union prevents silent internal-message leaks at the type level. Distinct from "Raw internal/third-party error.message exposed through exported result type" (that is the runtime leak — a message that has already arrived; this is the type-level structural gap that would permit such a leak even after the runtime fix). Note: sibling actions `void-code.ts` and `issue-code.ts` still use `error: string` (pre-existing; not in this commit's diff). On 2nd occurrence (a new Server Action ships with `error: string` on a result type that will be displayed to users): propose adding a code-style.md §5 note — "Server Action result types that include a user-displayable `error` field must type it as a closed literal union, not `string`; this prevents a future code change from silently routing an internal SDK/DB error string to the UI." |
| Rebased CREATE-OR-REPLACE migration header claims "VERBATIM, ONLY change is X" but body incorporates extra fixes (stale comment) | 1 | 2026-06-19 | WATCHING — PR #856 rebase (commits 80f5f9ed + 66d09624): both migration comments opened with "VERBATIM from latest definition, ONLY change is [X]" but the rebase absorbed additional bug-fixes (guard additions, cleaned-up whitespace, minor corrections) into the body, making the header claim inaccurate. Two occurrences in the same PR/rebase batch = 1 distinct mechanism per the same-PR rule. This is a documentation-accuracy issue, not a security gap. Distinct from "Semantic reviewer stale-baseline false positive" (that is a reviewer mistake from comparing against the wrong predecessor; this is an authoring-time header comment not updated to reflect the actual body). Distinct from "Stale `why` annotations on test payloads after guard mechanism change" (that is post-authoring drift; this is wrong at commit time). On 2nd occurrence (different PR rebase or different set of RPC migrations with the same stale-header pattern): propose adding a migration authoring note — "When rebasing a CREATE OR REPLACE migration that opens with a 'VERBATIM from latest definition, ONLY change is [X]' comment, verify the comment remains accurate by diffing the final body against the prior migration before committing — a rebase that absorbs extra fixes silently invalidates the VERBATIM claim." |
| Mode/enum widening misses hard-coded consumers outside the central constants file | 1 | 2026-06-19 | WATCHING — VFR RT Phase B plan-critic (commit 0dedd3c4 cycle): widening the VFR RT mode set required updating (1) quiz-session-validators.ts mode whitelist, (2) result-summary.tsx hard-coded cast, (3) reports-utils.ts second MODE_LABELS map — none of which appeared in the initial plan; plan-critic caught all 3 before execution. Three consumer sites in one commit = 1 occurrence by the same-commit rule. Related to "Partial fix to sibling file group" (count=11 in live tracker) but distinct: that row tracks POST-COMMIT misses; this row tracks PRE-PLAN misses identified via plan-critic. The root cause is identical (central constants file updated, scattered consumers not swept). On 2nd occurrence where plan-critic catches missed consumers on a mode/enum widening: propose adding to agent-workflow.md Plan Validation "Pattern scan" step — "when a plan widens a mode/enum/whitelist constant, grep ALL files for hard-coded uses of the old constant set before approving the plan: storage validators, type-cast expressions, sibling label maps, RPC whitelists, type unions." |
| App-layer query filters non-existent column (schema-contract bug escaping all pre-commit gates) | 1 | 2026-06-20 | WATCHING → STRUCTURAL FIX IN PROGRESS (#925) — VFR RT Phase 1 (`344c12d1` / fix `5f392339`): `getRtSubjectData()` called `.is('deleted_at', null)` on `easa_subjects`. Column does not exist — table defined in mig 001 with no soft-delete. Passed: Vitest (mocked chain ignores `.is()` calls), tsc (query builder accepts any string column), Biome, 3 plan-critic rounds (Sonnet + Opus), Sonnet AND Opus impl-critic. Caught ONLY by semantic-reviewer post-commit. Broke `/app/vfr-rt` at runtime. Root: `lib/queries/*` and Server Actions had zero integration coverage; mocked unit tests structurally cannot detect schema-contract violations. Filed as P0 issue #925. Deep fix: commit `6d9d055e` (#925 Phase 0, 2026-06-20) establishes the app-layer integration test tier — real Supabase client, service-role seeding, RLS verified per-test. The new tier ITSELF immediately surfaced real schema/RLS behavior during authoring: `getTopicsWithSubtopics` filters parts by org-scoped questionCount>0, so a fresh test org sees zero parts until seeded (completely invisible to mocks — confirms #925 thesis). The original `.is('deleted_at', null)` bug NOW FAILS the tier (`column easa_subjects.deleted_at does not exist`). Column-existence guard rule + formal policy promotion deferred to #925 Phase 3/4 (column-existence grep convention). On 2nd occurrence BEFORE Phase 3 ships: promote to code-style.md §5 note immediately — "When a lib/ query or Server Action filters on a `deleted_at` (or any) column, grep the table's CREATE TABLE migration to confirm the column exists — mocked Vitest tests structurally cannot surface schema-contract violations. A table without `deleted_at` is hard-delete-by-design; do not add a soft-delete filter." |
| Code comment / fix annotation cites wrong defining migration (mentions a referencing migration, not the defining one) | 1 | 2026-06-20 | WATCHING — VFR RT Phase 1 fix (`5f392339`): the fix commit's inline comment on `getRtSubjectData()` originally cited "mig 067" as the source of truth for `easa_subjects` lacking `deleted_at`. Mig 067 is an RPC fix that incidentally references the column's absence; the DEFINING migration is mig 001 (the original CREATE TABLE). The wrong citation was corrected in-session before the commit landed. Root: when writing an explanatory comment to justify a schema fact, the path of least resistance is citing the most recently-visited migration in the session, not the canonical defining migration. Distinct from "doc-updater agent fabricates plausible-but-wrong claims" (that is hallucination of false facts; this is a correct fact cited to an indirect source). Distinct from "semantic-reviewer stale-baseline false positive" (that is a reviewer using the wrong migration to evaluate correctness; this is a developer writing a comment citing the wrong migration). Distinct from "Rebased CREATE-OR-REPLACE migration header comments stale" (stale authoring-time header; this is a wrong source citation in a code comment). On 2nd occurrence (different wrong-migration citation in a code comment or doc annotation): propose adding to CLAUDE.md migration checklist or agent-workflow.md delegation template — "When citing a migration number in a code comment or fix annotation to justify a schema fact (e.g. 'column does not exist'), grep `CREATE TABLE <tablename>` to find the defining migration — do not cite a migration that merely references or modifies the table." |
| Integration test harness cookie-jar scope (signInAs in beforeAll vs. beforeEach) | 1 | 2026-06-20 | WATCHING — #925 Phase 0 (`6d9d055e`, 2026-06-20): semantic-reviewer (Opus, S1): the new app-layer integration test harness in `packages/db/src/__integration__/helpers/` resets the auth cookie jar via `beforeEach`. As a result, `signInAs(student)` must be called inside each `it()`, not in a shared `beforeAll`. The harness's own tests do this correctly. The risk: a future developer writing tests against this harness may naturally call `signInAs` in `beforeAll` (the common pattern in E2E/Playwright), then observe RLS-filtered empty results with no error signal — a confusing silent failure. No current test is wrong. Planned mitigation: add a JSDoc comment to `signInAs` or the harness README in Phase 2 when the first mutation-lifecycle tests land. Distinct from "Integration-test count in plan.md goes stale" (that is a doc-drift pattern). On 2nd occurrence (a different integration test file that calls signInAs in beforeAll and then sees unexpectedly empty results due to cookie reset): promote to test-writer/MEMORY.md note — "In the app-layer integration test harness, signInAs() must be called inside each it() block, not in beforeAll() — the cookie jar resets per beforeEach(), so a beforeAll()-scoped sign-in is cleared before the first test runs, producing silent RLS-empty results." |
| Integration test next/navigation redirect mock shape mismatch (plain Error vs. isRedirectError-tagged throw) | 1 | 2026-06-20 | WATCHING — #925 Phase 0 (`6d9d055e`, 2026-06-20): semantic-reviewer (Opus, S2): the new app-layer integration test harness mocks `next/navigation` so that `redirect()` and `notFound()` throw a plain `new Error('redirect: ...')`. This is not the real tagged-error shape that Next.js uses internally — `isRedirectError(err)` returns false for plain Errors, so Server Actions that use the rethrow-redirect pattern (`if (isRedirectError(e)) throw e`) would not propagate the mock redirect correctly, producing a false test result. Phase 0 tests cover only read-only queries (no Server Actions that redirect), so no test is currently wrong. Planned fix: when the first redirecting Server Action integration test lands in Phase 2, align the mock to use the real tagged-error constructor from `next/dist/client/components/redirect-error`. Related to the existing archive row at line 165 ("Semantic-reviewer false positive: `isRedirectError` re-throw rule misapplied to client-side Server Action callers") — distinct mechanism (that is a false positive on the detection rule; this is a mock-shape issue in test infrastructure). On 2nd occurrence (a test for a redirecting Server Action where the redirect is not caught or asserted because the mock shape is wrong): promote to test-writer/MEMORY.md note — "When mocking next/navigation in an integration test for a Server Action that may call redirect() or notFound(), throw using the real tagged-error shape from next/dist/client/components/redirect-error, not a plain Error — isRedirectError() returns false for plain Errors and the rethrow-redirect pattern silently swallows the call." |
| App-layer integration test vacuous in tier-specific ways (RLS shadows helper filter; shared beforeAll makes isolation test one-sided) | 2 | 2026-06-21 | PROMOTED → code-style.md §7 "Integration-Test Negative Assertions Must Be Reachable" (#925 Phase 4, squash 6c1c3275). Count=1 (#925 Phase 1, branch-local 21d86dc5 + fix 4e462cf0, in squash fb2921c6): two sub-mechanisms both first occurrences. (A) Plan-critic CRITICAL: negative test for a disabled exam_config row vacuous because student RLS already enforces enabled=true AND deleted_at IS NULL exclusion before the helper's own filter runs. (B) Semantic-reviewer ISSUE post-commit: count-isolation test ("org A sees 3 rows, not 6") vacuous because shared beforeAll seeds both orgs, so the functional test already implicitly verifies isolation. Count=2 (#925 Phase 1 Commit 2, branch-local ba6a378e + fleet-fix 2aaf5a03, in squash fb2921c6): non-vacuous "both-perspectives" fix applied across all integration tests in the same batch ("strengthen session-dependent integration tests" commit verified via git). Cross-commit basis confirmed; promoted to code-style.md §7 in Phase 4 commit 6c1c3275. |
| Integration-support helpers absent from Sonar new-code-coverage gate (unit-lcov-invisible) | 1 | 2026-06-20 | WATCHING — #925 PR fixes (0cf61137, 2026-06-20): Sonar new-code-coverage gate failed because `apps/web/lib/integration-support/harness.ts` and `fixtures.ts` were not in `coverage.exclusions` in `sonar-project.properties`. These files cannot be reached by the unit test runner (they require a real Supabase stack), so they show 0% coverage in the unit LCOV report, and Sonar flags them as new code with insufficient coverage. Root cause is the same as the existing `packages/db/src/__integration__/**` exclusion: integration-tier support helpers are structurally unreachable by unit tests — they must be excluded from the coverage gate, not the new-code-quality gate. Semantic-reviewer SUGGESTION proposed also adding to `sonar.exclusions` (not just `coverage.exclusions`) to suppress non-coverage Sonar warnings on these files — orchestrator SKIPPED with reason: mirrors the coverage-only precedent of packages/db/__integration__; applying would be asymmetric without a demonstrated need. On 2nd occurrence (a new integration-tier support helper in a different package or directory misses the exclusion and causes a gate failure): promote proposal — add integration-support globs to BOTH `coverage.exclusions` (so coverage gate ignores them) AND `sonar.exclusions` (so issue-density gate also ignores them) consistently across both the existing packages/db and the new apps/web paths. |
| Plan places new tooling/guard script under root `scripts/` which is gitignored | 1 | 2026-06-20 | WATCHING — #925 Phase 3 (ad9857d9, 2026-06-20): plan-critic CRITICAL (coverage round, pre-execution): a new static-analysis guard script was initially planned to live at `scripts/check-integration-imports.ts`. But `.gitignore:48` lists `scripts/` as an ignored directory, so the file could never be committed, tracked, or run in CI. Relocated to `.claude/hooks/` before any code was written. Root: the plan author (orchestrator) did not check `.gitignore` before choosing a directory for a new tooling file. Lesson: before placing any new tooling/utility file in a root-level directory, grep `.gitignore` for that directory name. On 2nd occurrence (a plan chooses another gitignored directory for a committed file): promote to agent-workflow.md Plan Validation step — "Before proposing a new file path in a plan, verify the target directory is not listed in .gitignore. Root-level directories like `scripts/` may be ignored by project convention even when they exist." |
| Semantic-reviewer suggests removing a valid Supabase JS chain-boundary marker based on an invalid API shape | 1 | 2026-06-20 | FALSE POSITIVE / WATCHING — #925 Phase 3 (ad9857d9, 2026-06-20): semantic-reviewer ISSUE: claimed that `.rpc(` as a chain-boundary marker in the guard script would produce a false negative for the shape `.from('x').rpc('fn').is('deleted_at', null)`. Suggested removing `.rpc(` from the boundary set. Validated as a false positive by the orchestrator: (1) `.from('table').rpc('fn')` is invalid Supabase JS — `.rpc()` must be called on the Supabase client root, not chained after `.from()`; the shape does not exist in valid code. (2) The suggested fix would CREATE a real false positive for two adjacent newline-separated statements where `.rpc(` ends one expression and `.is(` begins the next. Skipped with reason: the `.rpc(` boundary is correct; a clarifying code comment was added to the guard script documenting why this shape is unreachable. On 2nd occurrence (semantic-reviewer flags removal of a boundary/guard based on an invalid API shape): log as a systematic false-positive class — semantic-reviewer may suggest removing guards based on theoretical shapes that are not valid in the actual API/SDK. |
| Durable file (rule/doc/decision) cites branch-local pre-squash commit hash as provenance | 1 | 2026-06-21 | WATCHING — #925 Phase 4 (f85a8c8e + semantic-reviewer ISSUE, 6c1c3275 fixes, 2026-06-21): code-style.md and decisions.md (Decision 46) initially cited branch-local pre-squash hashes 21d86dc5, 4e462cf0, ba6a378e, 2aaf5a03, 03cb2fa7 as provenance for the integration-tier rules. Post-squash-merge these hashes dangle — `git show <hash>` fails on a fresh clone or after GC. Semantic-reviewer caught it as an ISSUE in the first post-commit pass. Plan-critic's own tracker had also flagged the sibling "plans bake stale self-cited learner counts/commit-hashes" pattern. Fix (C3): replaced all five branch-local hashes with the reachable squash commit (fb2921c6 for Phase 0-1, f4c76c83 for Phase 2) or PR number (#925 Phase 0, #927, #930) at each citation site. Root: when authoring a rule or doc update during a feature branch, the natural citations are the visible branch commits — none of which survive squash. Lesson: in any durable committed file (rule, doc, decision), cite the reachable squash commit or PR number, never a branch-local pre-squash hash. On 2nd occurrence: add to agent-workflow.md or CLAUDE.md — "When citing commit provenance in a durable committed file (code-style.md, decisions.md, security.md, CLAUDE.md), use the reachable squash-merge commit hash or PR number — branch-local pre-squash hashes dangle after merge." |
| `.single<T>()` bare-access vs `!` flagged after explicit `if (error) throw` guard | 1 | 2026-06-21 | WATCHING — #918 (64dbb921/9f6c5440, 2026-06-21): semantic-reviewer (SUGGESTION) and impl-critic both flagged bare `sessRow.id` after a `.single<{id:string}>()` call with an explicit `if (error) throw` guard, suggesting `sessRow!.id` to match the `qRow!.options` pattern at L103 of the same file. Validated as a FALSE POSITIVE / SKIP: (1) tsc accepts bare `sessRow.id` without `!` because the TypeScript type narrowing from `.single<{id:string}>()` marks the result non-nullable when the error path has returned; (2) adding `!` without a comment would violate code-style.md §5 "No Non-Null Assertions Without Comment"; (3) the `qRow!.options` precedent in the same file exists because that expression's nullable channel is different — it is a distinct context. Two reviewer flags on the same artifact = ONE learner occurrence. On 2nd cross-commit occurrence: consider a one-line guidance note in code-style.md §5 or test-writer/MEMORY.md — "After `if (error) throw`, bare access of a `.single<T>()` result is tsc-clean and preferred over an uncommented `!`; only add `!` if the surrounding file convention requires it AND a justifying comment is present." |
| Practice-mode sessions need no per-test cleanup (no unique-active index); exam-mode sessions do | 1 | 2026-06-21 | WATCHING — #918 (64dbb921, 2026-06-21): impl-critic flagged missing try/finally cleanup on the quick_quiz positive test — validated as a FALSE POSITIVE. Practice modes (smart_review, quick_quiz) have no partial-unique active-session index in the DB, so creating duplicate concurrent sessions does not produce a constraint error; afterAll cleanup is sufficient and matches the 4 existing practice-mode positive tests in the same file. Exam modes (mock_exam, internal_exam, vfr_rt) DO have a unique partial index on `(student_id, mode) WHERE ended_at IS NULL`, so each test must clean up before the next test starts (or use per-test isolation). This is a structural difference in DB constraint design — not a test hygiene failure. On 2nd occurrence (impl-critic or test-writer generates the wrong cleanup pattern for one of these modes): add a durable note to test-writer/MEMORY.md — "Practice-mode integration tests (smart_review/quick_quiz) may share afterAll cleanup; exam-mode tests (mock_exam/internal_exam/vfr_rt) require per-test isolation because of the unique partial index on (student_id, mode) WHERE ended_at IS NULL." |
| Feature-mode lifecycle test omits mode-specific anti-cheat/override branch (e.g. partial-submission → passed=false) | 1 | 2026-06-21 | WATCHING — #931 (0719746a/3e8bacf1, 2026-06-21): mock_exam lifecycle integration test initially covered PASS (all correct) + FAIL (all wrong) paths but omitted the partial-submission branch (answered_count < total_questions → anti-cheat override → passed=false regardless of score). Test-writer flagged the gap; the third test was added in the same commit. Root: lifecycle test authors naturally model the happy-path and the worst-case, but miss the mode-specific override branch that fires on incomplete submission. The partial-submission branch is a security/anti-cheat rule, not just a product edge-case — omitting it leaves the override untested. Distinct from the general lifecycle integration test rule (code-style.md §7 "Lifecycle Integration Test for New Feature Modes") — that rule requires at least one test covering the full lifecycle; this is a sub-rule about coverage WITHIN that lifecycle test for mode-specific override branches. On 2nd occurrence (a lifecycle test for a different exam mode misses the anti-cheat/partial-submission branch): add to code-style.md §7 lifecycle note — "Lifecycle tests for exam modes (mock_exam, internal_exam, vfr_rt) must cover the anti-cheat/override branch: a test where answered_count < total_questions, asserting passed=false regardless of per-question correctness." |
| `REVOKE EXECUTE … FROM PUBLIC, anon, authenticated` to restrict internal SECURITY DEFINER helpers to a single authz boundary | 2 | 2026-06-21 | RULE CANDIDATE — VFR RT Phase 2 (Decision 47, 2026-06-21): mig 120's internal `_grade_record_mc/_short_answer/_dialog_fill` helpers REVOKE EXECUTE immediately after CREATE so only the postgres-owned dispatcher (`batch_submit_quiz`) can call them; the helpers TRUST their p_student_id/p_session_id/p_org_id args (no internal auth.uid() guard), so a direct authenticated call would forge graded rows. **(occ 2) #952 refinement — `REVOKE … FROM PUBLIC` ALONE IS INSUFFICIENT.** `CREATE FUNCTION` grants EXECUTE to PUBLIC, but Supabase ALSO grants EXECUTE on new public functions to `anon`/`authenticated` via `ALTER DEFAULT PRIVILEGES` — a SEPARATE grant that survives `REVOKE … FROM PUBLIC`. The first cut shipped `FROM PUBLIC` only; CI's signature-valid revoke test caught an authenticated student's direct call reaching the helper body (P0001), proving `authenticated` retained EXECUTE. Fix: `REVOKE EXECUTE ON FUNCTION <fn>(...) FROM PUBLIC, anon, authenticated;` (no explicit `GRANT … TO service_role` — service_role is the trusted backend, not attacker-facing, and keeps EXECUTE implicitly). Note also: local Supabase stacks LACK the default-priv anon/authenticated grant, so this gap is INVISIBLE locally (REVOKE FROM PUBLIC suffices locally) and only CI reproduces it — CI is authoritative for grant-model tests. The revoke negative test must use SIGNATURE-VALID args (empty `{}` → PGRST202 from overload-resolution before the EXECUTE check = vacuous, code-style §7). Promotion (orchestrator-deferred to learner curation) → security.md rule 7 note: "internal plpgsql helpers callable only by other SECURITY DEFINER RPCs must `REVOKE EXECUTE … FROM PUBLIC, anon, authenticated` immediately after CREATE — `FROM PUBLIC` alone leaves Supabase's default anon/authenticated grant intact." |
| Hand-editing `packages/db/src/types.ts` produces drift from `supabase gen types` output (wrong nullability, missing blocks) | 1 | 2026-06-21 | WATCHING — PR #952 fix commit 8f0f75df (2026-06-21): `types.ts` was hand-edited during Phase 2 VFR RT work to add `| null` to `blanks_safe`, `dialog_template`, and `options` fields (nullability at the app layer is guarded in the consumer, but the generator emits these columns as NON-null in the RETURNS TABLE type); the `check_non_mc_answer` block was also missed. CR cloud flagged the nullability discrepancy; regenerating against the live DB (migs 118–121 applied) confirmed the generator emits all columns non-null and includes the missing block. CR's own suggestion to make `lo_reference: string | null` was correctly SKIPPED because it would also diverge from the generator. Root: hand-edits applied the developer's runtime intuition (callers get nulls at runtime for non-MC questions) rather than reflecting the DB-level RETURNS TABLE declaration (which is non-null at the type level; nullability is a caller concern). Distinct from archive row 350 ("`packages/db/src/types.ts` not manually updated after RPC return-shape change"): that row is about the file NOT being updated when a migration changes a signature; this row is about the file being updated but with values that diverge from what the generator would produce. Same root: `types.ts` is treated as hand-maintained rather than generator-authoritative. On 2nd occurrence (any hand-edit to `types.ts` that diverges from `supabase gen types` output): propose CLAUDE.md plan-validation note — "After editing `packages/db/src/types.ts`, run `supabase gen types typescript --local` and diff the output against the hand edit. Manual edits to RPC return-type nullability drift from the generator — the generator is authoritative for the column's RETURNS TABLE declaration; caller-side null-safety is handled by the consumer, not the type file." |
