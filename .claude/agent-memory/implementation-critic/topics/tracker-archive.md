# implementation-critic memory — tracker archive

> Older findings relocated verbatim from MEMORY.md on 2026-06-07 to keep the auto-injected index under the 25 KB budget. No information lost; the live MEMORY.md retains the recurring-deviation tracker, durable knowledge, and false-positive guards. Newest entries stay live; these (two dated spec-notes + the full "Positive patterns" approval log, pre-2026-06-07) are preserved here for reference.

## Notes on user_consents-isolation spec (2026-06-04)

- **`user_consents` no-update/no-delete policies confirmed.** Mig 20260327000057 adds both `user_consents_no_update USING (false)` and `user_consents_no_delete USING (false)`. The spec comment says "no UPDATE/DELETE" — correct; service-role hard-delete works because service role bypasses RLS. No false positive.
- **`document_type` IN constraint and `document_version` char_length CHECK** — `'terms_of_service'` (16 chars) is valid for `document_type`; `'redteam-x-1.0'` (13 chars) and `'redteam-y-forged'` (16 chars) are both within the 1–20 `char_length` window. Seed is valid.
- **42501 on INSERT WITH CHECK(false)** — confirmed correct. `WITH CHECK (false)` always rejects authenticated-client inserts with PostgREST error `42501`. (Contrast: FOR DELETE USING false is a 0-row no-op, not a 42501. The distinction is correctly applied here — this is an INSERT, not a DELETE.) See durable rule: "never assert 42501 on a DELETE" — its counterpart is "always assert 42501 on WITH CHECK(false) INSERT".
- **AL migration citation error (ISSUE):** `docs/security.md` AL note cites `20260411000003` as the migration containing the `IF EXISTS` duplicate-session guard. The original `20260411000003` (initial `start_exam_session`) has NO such guard. The guard was added in `20260411000005` ("Fix 3: Add duplicate active exam session guard"). The migration citation in security.md is incorrect.

## Notes on GDPR warnings field (#684)

- **`section` key = payload key, not DB table name.** `queryResults` uses `'flagged_questions'` (payload key), not `'active_flagged_questions'` (DB view name). This is correct per Decision 39 ("section matches a payload key"). Do not flag this as a mismatch.
- **`makePageErrorChain` `state.head` reuse is safe.** Each `.select()` call resets `state.head` before `.then()` fires; the shared mutable state correctly simulates count→page sequence even though both `getCount` and `getPage` call `from('quiz_sessions')` and get the same proxy object. Verified correct — do not flag as a false positive.
- **flagged_questions null-drop is correctly NOT a warning.** It's a post-read row filter on a successful read, not a failed sub-read. Confirmed defensible scope boundary. The `collectSectionWarnings` path does capture `flagsResult.error` separately.

## Positive patterns

- **#379 record_auth_event APPROVED with 1 ISSUE (2026-06-06).** RPC: SECURITY DEFINER + SET search_path + auth.uid() null-check + cached role pattern (mig 078 precedent) all present. Forgery guards correct: `user.password_changed` forces resource_id=actor; admin events check v_role='admin' AND resource in caller org. Client correctness: all 4 call sites use user-context supabase (not adminClient). Best-effort consistency confirmed across all 4 actions. ISSUE: toggle-student-status.test.ts missing "still succeeds when audit write fails" test (the other 3 test files have it). SUGGESTION: record_auth_event undocumented in database.md (record_login has a full section).
- **#684 GDPR follow-up fix APPROVED clean (2026-06-06).** Phase-2 guard (`sessionsResult.error ? [] : sessionsResult.data.map(...)`) correct — semantically sound (skip phase-2 when session IDs can't be trusted) and forward-proof (protects `.map()` against null). `warnings: []` added to MOCK_PAYLOAD in both component tests — required, prevents TypeError on `.length`. `stringContaining('incomplete')` assertion valid — production message contains the substring. `expect(mockToastSuccess).not.toHaveBeenCalled()` outside `waitFor` is safe (settled after `mockToastWarning` assertion inside). `onOpenChange(false)` correctly asserted in dialog test for both warning and clean paths. DataExportCard has no dialog-close assertion (correct — it's a card). 4 new collect-user-data.test.ts tests are non-vacuous: skip-phase2, no-duplicates, clean-sections-omitted, stable-order all exercise distinct paths and pin distinct invariants.
- **Red-team batch-submit-time-limit spec APPROVED clean (PR-D, 2026-06-04).** Direct-insert mock_exam session pattern (no exam_config_id needed — batch_submit reads pass_mark from session config JSONB, not exam_configs table). `expired: true` key is unique to the expiry branch — non-vacuous pin confirmed. Hermeticity: soft-delete afterEach with try/finally + .is('deleted_at', null) + .select('id'). Immutable quiz_session_answers left in place (consistent with session-replay.spec.ts precedent). Stale `as string` cast survived the #636 guard sweep at audit-completeness.spec.ts:296 — residual cosmetic, TypeScript already narrows correctly after the guard. score_percentage `toBe` vs `toEqual` mixed pattern across sibling specs — both appear in approved CI-passing specs; not a correctness issue.
- **Migration metadata-key rename via CREATE OR REPLACE copy (#570, 2026-06-01).** The plan specified: full byte-copy of the latest migration, change only the header comment + the two metadata keys, mirror to both dirs. Implementation was exact — diff 078→082 showed only 3 lines changed, mirror diff was empty. Zero-deviation execution on a "surgical change to a large SQL function" plan. The minimal-change plan approach (vs in-place edit of 078) is a sound strategy for audit-sensitive RPCs; reinforces that the migration copy approach is the right pattern for this class of fix.
- **Red-team spec for complete_overdue_exam_session APPROVED clean (PR2, 2026-06-04).** All 4 error message assertions matched the latest CREATE OR REPLACE definition (mig 20260429000008). Timing math correct (120s > 60 + 30s grace). Seed validity confirmed (quick_quiz in CHECK constraint, deleted_at on quiz_sessions, config JSONB is schema-unconstrained). Hermeticity sound (try/finally + .is('deleted_at', null) filter + .select('id') observability). Pattern match to sibling specs correct. Zero deviations found after full CREATE OR REPLACE chain trace.
- **Red-team spec for complete_empty_exam_session APPROVED clean (PR3, 2026-06-04).** All error message assertions correct (AO/AR → 'session not found or not accessible', AP → 'session is not an exam', soft-deleted-user → 'user not found or inactive'). AQ idempotent path confirmed: RPC re-reads real values (COALESCE(v_score,0)/COALESCE(v_passed,false)) and does NOT UPDATE row on idempotent branch. Early-submit positive test confirmed: complete_empty has NO 'session is not overdue' guard — only complete_overdue does; v_overdue is computed post-UPDATE for audit routing only. Seed validity confirmed: correct_count NOT NULL DEFAULT 0, score_percentage NUMERIC(5,2) NULL, passed BOOLEAN NULL — all nullable/defaulted correctly. Hermeticity identical to sibling spec pattern.
- **A10 error-disclosure spec APPROVED clean (2026-06-06).** All 5 RPC probes verified against latest CREATE OR REPLACE chain: `start_internal_exam_session` → `p_code text` (mig 20260606000003, raises `code_not_found`); `void_internal_exam_code` → `p_code_id uuid` (mig 20260601000003, raises `code_not_found`); `complete_overdue/empty_exam_session` → `p_session_id uuid` (mig 20260429000008, raises `session not found or not accessible`); `get_report_correct_options` → `p_session_id uuid` (mig 20260316231503, raises `Session not found, not owned, or not completed`). Spec count 37 confirmed by `ls *.spec.ts | wc -l`. `DISCLOSURE_PATTERNS` correctly excludes `permission denied`. No afterEach needed (read-only). Non-vacuous: asserts `error != null` before message shape.
- **Red-team spec for deleteComment cross-user IDOR APPROVED clean (PR4, 2026-06-04).** Core semantics verified: `FOR DELETE USING` violation is a 0-row no-op (200 OK, no error, `deleted.length === 0`) — NOT 42501. 42501 is only raised for `FOR INSERT/UPDATE WITH CHECK` violations. Issue #711 acceptance criteria citing 42501 for DELETE IDOR was wrong; no-op assertion is correct. Seed valid (body 31 chars within CHECK 1–2000; question filtered active/non-deleted/in-org). Hard-delete cleanup correct (mig 049 hard-delete table). Positive test non-vacuous (owner can delete own comment). Hermeticity sound (createdCommentIds Set + afterAll hard-delete + positive test removes its own ID from set before afterAll). Durable rule: never assert 42501 on a DELETE operation — only on INSERT/UPDATE WITH CHECK violations.
- **PR-B red-team 3-spec batch APPROVED clean (2026-06-04).** rpc-admin-dashboard-students (#685), rpc-report (#253), exam-config-distributions-rls (#730) + unit-test Vector O all verified. Error strings byte-matched. is_admin() confirmed role='admin' only (instructor=false). RETURNS TABLE RPC → data null on error, array on success. Quiz_sessions schema confirmed (passed NULL, score_percentage NUMERIC NULL, correct_count NOT NULL DEFAULT 0, mode 'quick_quiz' valid). Partial-unique index scope confirmed (WHERE deleted_at IS NULL → afterEach hard-delete clears slot before next beforeEach). RLS WITH CHECK confirmed → 42501 on INSERT referencing soft-deleted parent. buildReportQuestions options.map projects exactly {id,text} → `correct` key never leaks. Zero deviations found.
- **#611 quiz-session-score-forgery REVISE (2026-06-05).** afterAll hard-deleted quiz_sessions row; sibling specs soft-delete (docs/database.md matrix: quiz_sessions = Yes soft-delete). JSDoc at top says "re-GRANTed only UPDATE (deleted_at)" but actual GRANT covers 11 columns (all non-scoring, non-PK). Both are ISSUE-class. Column list math verified: 16 total − 4 scoring − 1 PK = 11 — GRANT is correct.
- **PR-C red-team audit spec extensions APPROVED clean (2026-06-04).** AA audit-read-isolation: RLS `audit_read_own USING (actor_id = auth.uid())` confirmed; seed columns match DDL NOT NULL constraints; no DELETE policy (USING false); `.contains('metadata', {...})` is the correct PostgREST-js JSONB containment filter (`cs.{...}` wire encoding). #728 complete_empty metadata lock: latest CREATE OR REPLACE (mig 20260428000005) emits `exam.completed` with `answered_count/correct_count/reason='completed with no answers'` within grace; no collision — new test uses its own sessionId + testStart fence. `admin` var confirmed in scope (line 31 of spec). Zero deviations found.
- **Hub A unauthenticated-path spec APPROVED with 1 ISSUE (2026-06-04).** AG/AN error messages verified ('not authenticated' lowercase, /not authenticated/i matches). W-1/W-2 consent RPCs error is 'Not authenticated' (capital N) — /not authenticated/i is case-insensitive, correct match. CA param names exact (5 params: p_subject_id/p_topic_ids/p_subtopic_ids/p_count/p_filters; 4 params for counts). ISSUE: SECURITY INVOKER CA functions GRANT TO authenticated only — anon callers get permission-denied error, NOT empty-set, because GRANT TO authenticated without explicit anon GRANT does not allow anon access. Existing get_question_counts precedent (GRANT TO authenticated, test passes empty-set) establishes that the empty-set expectation can work — but only if the runtime Supabase instance grants anon EXECUTE. Since GRANT TO authenticated is explicit-only and no anon grant exists in any migration, the tests may fail with permission error. The safe fix is `expect(error).toBeNull() OR (error?.code === '42501' || data.length === 0)` for the CA tests, or confirm in CI that anon can call these. Tracked 2026-06-04. [SUPERSEDED: this ISSUE was a FALSE POSITIVE — Postgres grants EXECUTE to PUBLIC by default, so anon ∈ PUBLIC can call SECURITY INVOKER functions and RLS empties the result. See learner postgres-security-invoker-rls-pattern.]
- **Hub B cross-tenant isolation APPROVED clean (2026-06-04).** 4 new tests (AH/CB-1/CB-2/AK). AH: latest start_exam_session (mig 20260428000004) resolves v_org_id from auth.uid()'s users row and uses it as the filter key for exam_configs — the cross-org caller resolves to other-org which has no config, hitting the v_config_id IS NULL → 'no exam configuration found' error. CB-1/CB-2: SECURITY INVOKER + tenant_isolation RLS on questions confirmed → 0 rows for other-org on egmont's subjectId. AK: latest exam_config_distributions RLS (mig 20260601000002) is admin-only (`is_admin() AND same-org EXISTS`), no student policy → student crossOrgClient SELECT returns 0 rows total (unscoped query is correct — tests full table isolation). Non-vacuity sound (pickSubjectWithQuestions + ensureExamConfig guarantees egmont data). Hermeticity: ensureExamConfig is idempotent and matches quiz-session-config-injection precedent (no cleanup required for pre-existing or created configs).
- **Trigger-vs-RLS ordering for BEFORE triggers on quiz_sessions (PR-A, 2026-06-04).** For UPDATE: USING is evaluated on the OLD row (row visibility). BEFORE ROW trigger fires next. WITH CHECK is evaluated AFTER the trigger, on the NEW row. If the trigger raises, the transaction aborts before WITH CHECK runs. So a student UPDATE on any frozen column (including student_id/organization_id) surfaces the trigger's RAISE message, NOT a 42501 from WITH CHECK. The `expect(error?.message).toContain('immutable')` assertion in the parametrized #615 tests is definitively correct. Durable rule: when a BEFORE trigger raises on a frozen column, WITH CHECK never runs — do not question trigger-vs-RLS ordering on BEFORE triggers.
- **batch_submit §15 carve-out confirmed (PR-A, 2026-06-04).** The `_batch_questions` temp table in `batch_submit_quiz` (latest def: mig 20260601000001) fetches `WHERE q.id = ANY(v_session_question_ids)` with NO `deleted_at IS NULL`. This matches security.md §15 — write-once config.question_ids, read at session start, only fetch by those IDs. A mid-session soft-deleted question IS still in `_batch_questions`. All 3 answers land; count === 3 assertion is correct.
- **code-style.md §7 red-team rules PR (2026-06-04) APPROVED clean.** Two new rules added: (1) "Non-Vacuous" promoted at count=3 (BZ1 confirmed: spec explicitly seeds `createCrossOrgUser()` + asserts `rows.length > 0` before `not.toContain`); (2) "RPC Output Contract" promoted at count=2 (PR #736 = complete_overdue / issue #557 = complete_empty — mixed PR-vs-issue citation is the established convention in learner memory; both reference the correct events). YAML mirrors both rules without inventing new ones. No contradiction with existing §7 rules.
- **PR 743 #257 concurrent batch_submit output-contract assertions APPROVED (2026-06-04).** `typeof d.score_percentage === 'number'` is sound: batch_submit_quiz RETURNS jsonb (not a table type), so PostgREST transmits the JSONB payload as-is through JSON; `numeric(5,2)` inside `jsonb_build_object` becomes a JSON number, not a string — consistent with how the pre-existing test at line 147-148 already uses `typeof originalScore === 'number'`. Idempotent path (FOR UPDATE loser hits `IF v_ended_at IS NOT NULL`) reads `v_answered` from `quiz_session_answers` (winner's rows = 3) and `v_correct_count/v_score/v_passed` from the winner's already-committed column values — so cross-response scalar equality holds regardless of race outcome. `total_questions === 3` is a safe absolute assertion (seed enforces exactly 3 at beforeAll). Non-vacuity guard `answersA !== answersB` correct (pickLast ≠ pickFirst when ≥2 options). NOTE: the post-commit semantic-reviewer later caught that `typeof d.passed === 'boolean'` was wrong — `passed` is NULL for quick_quiz (only set for mock_exam/internal_exam); fixed to `expect(d.passed).toBeNull()`. Lesson: assert RPC field nullability per-mode, not just the happy-path type.
- **PR-E upsert_exam_config JSONB injection spec APPROVED clean (2026-06-04).** All 5 SQLSTATE assertions verified: (a/b) `::uuid` cast on non-UUID text → 22P02 correct; (c) PLACEHOLDER_TOPIC_ID is syntactically valid UUID so `::uuid` cast succeeds, then `::int` on 'abc' → 22P02 before FK checked — correct (Postgres evaluates VALUES expressions before constraint checks); (d) real topicId → FK passes → CHECK fires → 23514 correct; (e) missing key → `v_dist->>'topic_id'` is NULL → NULL::uuid is NULL (not error) → NOT NULL constraint → 23502 correct. Hermiticity confirmed: no EXCEPTION handler in RPC → unhandled exception rolls back full transaction via PostgREST per-call transaction boundary. Non-vacuity confirmed: auth guards raise P0001 (plain RAISE EXCEPTION without SQLSTATE); cast/constraint codes are structurally disjoint. No cleanup needed: all calls roll back atomically, seed config untouched. Durable rule: in a Postgres INTEGER VALUES clause, type-cast expression errors (22P02) fire before FK/CHECK/NOT NULL constraint checks — cast errors are expression-phase, constraints are constraint-phase.
- **PR-E BV2 privilege-escalation extension APPROVED clean (2026-06-04).** Vector BV2 (authenticated non-admin caller) added to upsert-exam-config-injection.spec.ts. Error message assertion `/admin access required/i` confirmed correct: mig 20260411000008 line 48-49 `IF v_role != 'admin' THEN RAISE EXCEPTION 'admin access required'` — only one CREATE OR REPLACE definition exists (dual-directory copy, no later override). Attacker role confirmed 'student' (default in upsertUser). Hermiticity confirmed: role guard fires before any config upsert or distribution DELETE — whole call raises immediately, nothing written, no cleanup needed. seedRedTeamAdmin + seedRedTeamUsers ordering: both call getEgmontOrgId independently (idempotent lookup), no conflict. P0001 message-match assertion pattern (`.toMatch(/admin access required/i)` not `.code`) is correct for RAISE EXCEPTION without SQLSTATE — no SQLSTATE means error.code is P0001 but error.message carries the human string; `.toMatch` is the right form. Non-vacuity confirmed: well-formed payload used so the ONLY rejection path is the role check.
- **rpc-cross-tenant.spec.ts egmontQuestionIds fix APPROVED clean (Hub B, 2026-06-04).** Root cause: arbitrary `easa_subjects` first-row seed (no guarantee of active questions) produced zero `egmontQuestionIds` in CI. Fix: derive `egmontSubjectId/TopicId/QuestionIds` from the same `pickSubjectWithQuestions`-guaranteed subject already used for `examSubjectId`. Key verifications: (1) removed `subjects/topics/topicIds` locals have no dangling references in the current file; (2) line-118 `topicIds` is a SEPARATE local scoped inside test "cannot SELECT questions" — not the removed beforeAll variable; (3) `egmontSubjectId/TopicId/QuestionIds` are used only at lines 88-90 (quick_quiz RPC) and line 308 (CB non-vacuity guard) — both legitimately want "any real egmont subject with active questions"; (4) questions table has `organization_id` + `subject_id` columns + indexes confirmed in initial_schema migration; (5) `{ error: qErr }` destructure satisfies code-style §5; (6) non-vacuity `expect(egmontQuestionIds.length).toBeGreaterThan(0)` in `beforeAll` restores the CI guard. Zero deviations found.

- **PR-5 auth-callback specs APPROVED with 1 ISSUE (2026-06-06).** Vector X (pkce-state.spec.ts): line number citations in spec comments verified correct against route.ts. Regression-guard weakness found (ISSUE): forged-code tests assert `/?error=invalid_code` which is produced by the PKCE failure path — if `next` handling were moved BEFORE the code exchange, the tests would still pass because the exchange still fails. The tests do NOT exercise the state where exchange succeeds but `next` is unsafe. The guard tests the CURRENT defense correctly, but would not catch a regression that bypasses the exchange. Vector V (login-complete-unauth.spec.ts): route.ts line numbers correct (15-17, 20); proxy.ts matcher line 126 correct; event_type 'student.login' confirmed in mig 047. Non-vacuity audit query checks `error` before asserting count. Clean. Vector W (rate-limiting.spec.ts): `attackerUserId` returned from `seedRedTeamUsers` confirmed. Event_type 'student.login' + 60s window confirmed in latest record_login definition (mig 047). Both admin queries throw on error (not vacuous). ISSUE: spec comment cites "line 28: event_type = 'student.login'" but actual line 31; "lines 27-30: rate-limit window" but window clause is on line 33 — comment line numbers are off by 3.

---

## Relocated from MEMORY.md on 2026-06-13 — per-PR implementation notes (keep the auto-injected index under 200 lines; no information lost)

## Notes on #789 recordAuthEvent wiring (commit 2)

- **Client-level mock intercepts helper correctly.** Tests for all 4 action files mock `supabase.rpc` at the Supabase client object level (`{ rpc: mockRpc }`). The `recordAuthEvent` helper receives the same client object and calls `supabase.rpc(...)` on it — so `mockRpc` is the intercepting mock. The helper runs for real but its I/O is fully captured. No `vi.mock('@/lib/audit/record-auth-event')` needed; client-level mock is correct and simpler.
- **Plan description of MEMORY.md change was inaccurate (benign).** Plan said "typo `VITAMIN_PASSWORD`→`VICTIM_PASSWORD` correction". Actual staged change: new positive-signal bullet for #789. `VICTIM_PASSWORD` was already correctly spelled in the file. Staged content is correct memory maintenance — this is a stale plan description, not a behavioral deviation. Do not re-flag in future commits.
- **`toggle-student-status-mutations.ts` in `actions/` dir does not require own test.** Code-style §7 requires tests for new files in `_hooks/`/`_utils/`/`lib/`. `actions/` is not in that list. Existing `toggle-student-status.test.ts` already covers both mutation helpers end-to-end via the module-level `@repo/db/admin` mock (which the mutations file imports). No test gap.

## Notes on VFR RT Phase A migrations commit (#697)

- **Large multi-migration commit (14 migrations): verbatim-copy diff approach.** For a commit where N migrations are verbatim copies with documented changes, run `diff <src> <dest>` to confirm ONLY the documented changes appear — do not read both bodies line by line. Verified clean on #697: 095b (one ON CONFLICT line), 095c (one ON CONFLICT line), 102 (DECLARE vars + mode guard + per-part block + CASE branches). All mirrors byte-identical confirmed by `diff` loop.
- **GDPR type widening for new nullable columns is a secondary change class.** When schema adds nullable columns to existing tables (`response_text`, `blank_index` on `quiz_session_answers`/`student_responses`), check 3 sites: (1) the SELECT string in the GDPR query, (2) the GdprExportPayload type in types.ts (must widen to `string | null`), (3) test fixture must add the new fields as `null`. All 3 aligned in #697.
- **tasks.md A.10 was completed but not checked in the staged diff.** A.10 artifacts (types.ts regen) ARE staged but the checkbox was not ticked. SUGGESTION class only — workflows item, not correctness.

## Notes on #835 fix — AnswerRow.selected_option_id nullable widening

- **`string | null` widening across three-layer chain is complete when all three are updated together.** AnswerRow (builder input) → QuizReportQuestion (builder output / query contract) → OptionsListProps (render input). All three must move atomically. `===` comparison with null evaluates false for all option ids, so `isSelected` and `isAnswered` derived booleans are runtime-safe without any additional null-guard. Pattern is correct — confirmed clean on this fix.
- **New test in builder.test.ts tests questionImageUrl passthrough, NOT the null-selectedOptionId path.** No dedicated `selected_option_id: null` builder test exists. Null path exercised only at component level (options-list.test.tsx). Non-vacuous because the builder is a pure passthrough (`selectedOptionId: answer.selected_option_id`) — TypeScript enforces it. Gap is SUGGESTION class only per the thin-wrapper rule.

## Notes on #835 report options-list refactor

- **Correct+selected double-render is intentional, not a bug.** `isCorrect && isSelected` renders the Check+"Correct" block first (from the `{isCorrect}` condition) and then appends "· Your answer" (from the `{isCorrect && isSelected}` condition). The `{isSelected && !isCorrect}` block is correctly guarded out. Behavior is correct — verified against test assertions in options-list.test.tsx.
- **ZoomableImage mock upgraded from `<span data-src>` to `<img src alt>`.** This enables `byRole('img', { name: 'Question illustration' })` queries and multi-image disambiguation by `alt` attribute in the explanation-image test. A pre-existing `data-testid="zoomable-image"` attribute is preserved, maintaining backward compat for any other tests using `getByTestId`.
- **`selected_option_id` nullable (mig 095) does NOT affect report builder.** `quiz-report-questions.ts` and `admin-quiz-report.ts` only serve quiz/internal-exam sessions (MC only); VFR RT sessions have a separate report path. `AnswerRow.selected_option_id: string` (non-null) is correct within that scope.

## Notes on #838 CR round-3 applies (legacy-mode whitelist + active-user gate)

- **Mig 104 'not authenticated' (space) vs sibling 'not_authenticated' (underscore)** is a pre-existing divergence inherited from the copy source (20260406000004) — the plan explicitly lists only 2 changes. Do not flag as a deviation when the copy source is the source of truth. Both forms remain in the codebase; flag at SUGGESTION only.
- **`forceEndSession` inline (not try/finally) in whitelist tests** is safe because (a) `start_vfr_rt_exam_session` resumes an existing active session idempotently — so a leaked active session becomes the input to the NEXT test's `startSession()`, not a blocker; (b) `cleanupTestData` in `afterAll` hard-deletes all sessions by `org_id`. Pattern is hermetic even without per-test try/finally.
- **#838 review was clean (0 CRITICAL, 0 ISSUE, 1 SUGGESTION)** — twin-file identity, guard placement, copy-source fidelity, test non-vacuity and hermiticity all verified correct.
- **Post-#838 fix-cycle commit APPROVED (0 CRITICAL, 0 ISSUE, 1 SUGGESTION).** Mig 104 deleted_at guard test: non-vacuous (rollback assertion `ended_at IS NULL`), hermetic (try/finally with `console.error` restore, `{ error: endErr }` on force-end). Doc edits accurate: 094–104 range confirmed (15 files), footer claim matches existing L717/L975 entries. plan.md 132→133 in both current-phase lines. SUGGESTION: finally-block UPDATEs lack `.select('id')` + zero-row log — acceptable for cleanup context per §5 established pattern, plan explicitly only required `{ error }` destructure.
- **PR #830 cloud-CR commit APPROVED (0 CRITICAL, 0 ISSUE, 0 SUGGESTION).** Guard ordering verified in both RPCs (auth → gate → session SELECT FOR UPDATE → whitelist → state checks). Twin-file identity confirmed. The `submit_quiz_answer` code block in database.md is intentionally pre-#838 (established pattern — prose bullets describe the current security model). Existing vfr_rt rejection tests remain non-vacuous with the narrowed whitelist (vfr_rt_exam NOT IN ('smart_review', 'quick_quiz')). Exam-session cleanup in new test uses `ended_at + deleted_at` UPDATE (soft-delete correct per docs/database.md soft-delete matrix). `forceEndSession` `.select('id')` + throw was only required for the named helper — inline finally-block cleanups follow the `console.error`-only pattern. plan.md both count lines updated (header + Phase A section). False positive not raised: code block stale is pre-existing, not introduced by this commit.

## Notes on agent-health.yml false-positive fix (#810)

- **`var=$(cmd)` is exempt from `set -e` in bash** — `set -euo pipefail` does NOT abort on a failing command substitution inside a variable assignment. The old `|| true` after `xargs 2>/dev/null` in Check 3 was therefore NOT load-bearing under `set -e`; removing it (replacing xargs with `trim()`) is safe. `sed` piped from `awk` exits 0 on well-formed input, so in practice these never fail.
- **`trim()` placement in Check 3** — defined at line 88 (before the while-loop at line 90), outside the loop. Correct hoisting. Do not flag as "redefined per iteration".
- **Check 4 security-auditor comment placement** — the explanatory comment about `-not -path '*/security-auditor/findings.md'` is placed inside the while-loop body (last line before `done`), but it describes the `find` command in the process substitution. Cosmetically misplaced — SUGGESTION class only.

## Notes on #823 fix-cycle round 4 (esbuild override + DO-block exact-one guard + doc-sync)

- **esbuild override pattern**: placing `"esbuild": ">=0.28.1"` in `pnpm.overrides` (without an upper bound, unlike undici/next/fast-uri/postcss) is intentional — esbuild's GHSA advisory covers all `>=0.17.0 <0.28.1`, so an open-ended `>=0.28.1` is correct. Do not flag the missing upper bound.
- **DO-block OR short-circuit with subquery count is correct**. `correct_option_id IS NULL OR 1 <> count(*)` catches both zero-correct rows (NULL from backfill) AND multi-correct rows (count ≠ 1). A row matching both conditions is counted once in the outer COUNT — correct. The subquery runs against the flags BEFORE step-3 strips them, so count is authoritative at execution time.
- **RPC summary batch_submit_quiz entry (line 712) was already present** in this commit: `reads from questions.correct_option_id for MC grading (mig 110b, #823)`. The tracker row (WATCHING) for the missing sibling update is resolved — update that row if promoting.
- **`LATERAL jsonb_array_elements` in `get_quiz_questions` at doc line 824 is NOT an unconverted grading path** — it shuffles/builds option text (no `correct` field involved). Do not flag as stale doc on future reviews.
- **`opt->>'id' = p_selected_option` membership checks at doc lines 936 and 1221 are option-ID existence checks**, not answer-key reads. The option list is still JSONB; only `correct` moved. Do not flag as missing conversion.

## Notes on #823 answer-key relocation (P0 security — correct_option_id)

- **DISTINCT ON order tiebreaker becomes irrelevant when column is scalar.** Old `get_report_correct_options` used `DISTINCT ON (sa.question_id) ORDER BY sa.question_id, opt.ord` — the `opt.ord` broke ties among multiple options marked `correct` in JSONB. New version uses `ORDER BY sa.question_id` only. This is safe because `q.correct_option_id` is now a single scalar; every duplicate `quiz_session_answers` row for a question maps to the same column value. Do NOT flag the dropped `opt.ord` tiebreaker as a deviation — it is meaningless for a scalar.
- **mc_key CTE simplification (get_vfr_rt_exam_results): removing DISTINCT ON + CROSS JOIN LATERAL is correct.** Old CTE had `DISTINCT ON (q.id)` + `CROSS JOIN LATERAL jsonb_array_elements` + `WHERE correct = true`. New CTE has simple JOIN + `WHERE q.question_type = 'multiple_choice'`. With a scalar `correct_option_id`, one row per MC question is guaranteed — no DISTINCT needed. Do NOT flag the removed DISTINCT ON as a missed dedup.
- **get_report_correct_options: absence of GRANT EXECUTE is correct.** This function has never had an explicit GRANT EXECUTE across its entire migration chain (migs 032–037). It uses the Postgres default PUBLIC grant. `CREATE OR REPLACE` preserves existing grants. Do NOT flag the missing GRANT in mig 112.
- **Trigger scoped `OF options` is intentional.** `BEFORE INSERT OR UPDATE OF options` fires on all INSERTs (options always set) and on UPDATEs that explicitly set options. An UPDATE that only sets `correct_option_id` does NOT fire the trigger — correct, because options is already clean. Do NOT flag this as a missed update path.
- **Backfill `LIMIT 1` without ORDER BY matches old scan `LIMIT 1` without ORDER BY.** Both are non-deterministic when multiple options have `correct: true` (data quality issue). Equivalence is preserved — the backfill picks the same arbitrary "first" that the old scorers would have picked.

## Notes on #796 spec-split (rpc-cross-tenant + audit-completeness)

- **`cleanupFixtures` flagged_questions filter is student_id-unscoped by design.** The helper uses `.in('question_id', ...)` without a student_id filter — intentionally broader than the original specs (which used `.eq('student_id', victimUserId)`). In the test DB this is fine because only redteam seed users exist; flagged as ISSUE in this review but may be an accepted trade-off. Track: if the plan explicitly accepts this, add to false positives.
- **`seedVictimResponses()` correctly belongs to the isolation spec, not the reports spec.** The BY/BE/Q/X/CL3 tests in rpc-cross-tenant-reports.spec.ts all seed their own fixture data; none need the mastery/streak/last-practiced responses that the probe-only tests need. Do not flag the omission as missing non-vacuity seeding.
- **force-token-refresh.ts helper is a Playwright E2E seam** — code-style §7 Vitest test requirement does NOT apply to `e2e/redteam/helpers/*.ts`. (Confirmed again for helpers/cleanup.ts and helpers/audit-helpers.ts, which DO have co-located .test.ts — correct pattern.)
## Notes on vfr-rt-spec-fixes (docs/697 branch, 2026-06-10)

- **Spec cites stale "latest" migration identifier for batch_submit_quiz.** design.md + tasks.md named `20260430000012` as the latest `batch_submit_quiz` body; actual latest is `20260601000001_align_batch_submit_audit_metadata_keys.sql`. Flagged as ISSUE. The "Pre-Flag Verification at implementation time" mitigation language was present, but the stale pointer is still misleading. Pattern: when a spec documents a "latest: <timestamp>" pointer for a function body, verify it is actually the most recent `CREATE OR REPLACE FUNCTION` in that chain before committing.
- **Spec-specified guard message casing doesn't match cited precedent.** design.md mig 103 wrote `'session not found, not owned, or not completed'` (lowercase s) and cited `get_report_correct_options` as the style precedent; actual precedent uses capital 'S'. Flagged as ISSUE. The test regex is `/i` (case-insensitive), so this won't cause a test failure, but the mismatch violates the design's own consistency claim. Pattern: when a spec says "same wording as X", grep X's RAISE string before committing.
- **discard.ts line count off by 1.** tasks.md said 104 lines; actual is 105. Flagged as SUGGESTION. The cap-exceeded message is accurate either way.

## Notes on redteam-e2e-coverage-batch (#784, #786, #788, #781)

- **Spec-count drift in steering + decisions on new-spec batch** — adding 2 new Playwright specs (rpc-record-auth-event.spec.ts + token-refresh-anti-cache.spec.ts) moves count from 37→39. `tech.md` has the count in 3 places (lines 85, 146, 183); `docs/decisions.md` Decision 27 has it once. None updated in the staged diff. This is a SUGGESTION (doc-updater handles it post-commit). The plan explicitly excluded `docs/*.md` schema edits from scope.
- **actor-liveness pre-check pattern confirmed.** rpc-record-auth-event.spec.ts beforeAll asserts all 3 RPC callers (attackerUserId, adminUserId, crossOrgAdminUserId) have `deleted_at IS NULL` before any test runs. This prevents gate-2 (`'user not found or inactive'`) from pre-empting gate-3 assertions (CN/CO/CP). Correct pattern — promote if reused elsewhere.
- **force-token-refresh.ts helper is a Playwright E2E seam, not a `_hooks/` util** — code-style §7 Vitest test requirement does NOT apply to `e2e/redteam/helpers/*.ts`. Do not flag missing co-located test for E2E helper files.
- **CL2 non-vacuity acknowledged in plan as control, not the proof.** CL2 asserts error null + empty array for cross-org user with no sessions. The vacuity concern is pre-acknowledged — CL3 is the non-vacuous ownership proof. Do not re-flag CL2 as vacuous.
- **CT user.deactivated: soft-delete target without `.is('deleted_at', null)` guard is acceptable.** The update at `audit-completeness.spec.ts:678` uses `.eq('id', victimUserId)` without `.is('deleted_at', null)`. If the victim was already soft-deleted from a cross-spec leak, the update succeeds idempotently and `softDeletedUserIds` still restores it. The test checks `!deleted?.length` as an existence guard. This pattern is acceptable — the service-role client doesn't enforce the soft-delete guard, and the afterEach restore uses `.not('deleted_at', 'is', null)` correctly.
- **RAISE-string casing verified for all 4 RPCs.** `record_auth_event` → `'not authenticated'` (lower); `get_session_reports` → `'Not authenticated'` (capital N); `void_internal_exam_code` → `'not_authenticated'` (snake). All anon assertions in new specs use `/not[ _]authenticated/i` or `/not authenticated/i` (case-insensitive) — safe against all three forms.

## Notes on #831 get_vfr_rt_exam_questions org-filter fix

- **NULL-org guard doubles as NULL-user guard — confirmed again.** `SELECT u.organization_id INTO v_caller_org_id ... WHERE u.id = v_caller AND u.deleted_at IS NULL; IF v_caller_org_id IS NULL THEN RAISE 'user_not_found_or_inactive'` preserves the old EXISTS gate exactly: soft-deleted user or unknown user → org returns NULL → guard fires. No behavioral regression. Pattern mirrors mig 099 lines 67–71 identically.
- **questions.organization_id is NOT NULL (verified in initial schema line 107).** The `AND q.organization_id = v_caller_org_id` filter is non-nullable on both sides — no null-equality trap possible.
- **adminUserId2 not pushed to the describe-scope `userIds` — correct by design.** It is explicitly passed as `userIds: [adminUserId2]` in its own `cleanupTestData` call. The first `cleanupTestData` call covers orgId + its own userIds (adminUserId + studentId). No orphan risk.

## Notes on PR #830 CR-local fixes (5 fixes, vfr_rt_exam race + test hardening)

- **uq_vfr_rt_exam_session_active is the ONLY unique index that can fire on a `mode='vfr_rt_exam'` INSERT.** The three existing partial unique indexes (`uq_active_exam_session` for `mock_exam`, `uq_internal_exam_session_active` for `internal_exam`, `uq_vfr_rt_exam_session_active` for `vfr_rt_exam`) each predicate on a mutually exclusive mode value. PK is `gen_random_uuid()` — no collision. Exception handler is therefore unambiguous: `unique_violation` in the INSERT sub-block can only be from `uq_vfr_rt_exam_session_active`. The RETURN inside EXCEPTION exits the function before the audit INSERT — no fall-through. Sound.
- **Fixture A correct_count = 34 verified from seed.** seedPool: 8 SA questions + 9 dialog_fill questions × 2 blanks each (18 quiz_session_answers rows) + 8 MC questions = 25 questions but 34 answer rows. Fixture A answers every blank with the canonical → all 34 rows `is_correct=true`. Fixture B answers SA all correct (8) + MC all correct (8) + all 9 DF questions with 'WRONG_XYZ' for BOTH blanks (18 wrong rows, 0 correct) = 16 correct_count. Pinned values match seeded data.
- **`v_resume record` type is safe for EXCEPTION re-read SELECT.** `v_resume` is declared as `record` in DECLARE (line 35 of mig 099). The EXCEPTION block SELECTs `qs.id, qs.config, qs.time_limit_seconds, qs.started_at INTO v_resume` — same 4 columns used in the RETURN. The happy-path step-5 idempotent-resume uses the SAME `v_resume` record (line 114+) with the same 4 fields. Type-safe; columns match existing use.
- **23505 test is hermetic post-cleanup.** Cleanup soft-deletes by `student_id + organization_id + subject_id + mode='vfr_rt_exam' + IS NULL ended_at + IS NULL deleted_at`. Both the directly-inserted first row AND any second-row attempt are soft-deleted in the finally block. The happy-path test after it calls the RPC which triggers its own fresh INSERT — not blocked by lingering active sessions.

## Notes on #326 attack-surface matrix registration (orphaned specs bookkeeping)

- **Bookkeeping-only diff is comment/title changes only — no assertion/logic changes.** The 3 spec files had ONLY `+` lines touching `/** ... */` doc comments, `test.describe(...)` titles, and `test(...)` titles. Zero assertion or logic changes confirmed by reading every `+` line in the diff.
- **Old V/BE–BI labels correctly left in their original matrix rows.** Those rows reference sibling specs (consent-gate.spec.ts V, quiz-draft-injection.spec.ts BI, rpc-cross-tenant-reports.spec.ts BE, session-replay.spec.ts BH, audit-event-forgery.spec.ts BF, session-replay.spec.ts BG) by filename — no edit to existing rows needed. Verified: grep of V/BE/BF/BG/BH/BI in the three renamed spec files returns zero hits post-rename.
- **Migration cite accuracy pattern for bookkeeping PRs.** When matrix Notes cite migration line numbers, the line-number ranges are stated as e.g. "L261-263" (the surrounding context) not exact lines. Verified: actual RAISE lines (262, 266, 288, 307) fall inside the cited ranges. This is acceptable precision for Notes fields — do not flag line-number-range claims unless the RAISE string itself is wrong.

## Notes on #833/#840 semantic-SUGGESTION fixes (mig 105 VOLATILE + cleanup.ts exam_configs + docs)

- **VOLATILE drop confirmed clean.** `STABLE` removed from both mirrors (byte-identical via `cmp`); no TS caller, no integration test asserts volatility category; no `pg_proc.provolatile` assertion anywhere in `__integration__/`. Function is in a not-yet-deployed migration — safe to change before first deploy.
- **`exam_configs` FK order correct.** `exam_configs` references `organizations(id)` — must be deleted BEFORE `organizations`. New line is at position 54, `organizations` delete is at position 56. `exam_config_distributions` is `ON DELETE CASCADE` from `exam_configs`, so no explicit delete needed in cleanup.ts. Confirmed no `exam_config_distributions` entry exists in the file.
- **`deleteOrLog` without `.select('id')` is the established cleanup.ts pattern.** All sibling calls (`questions`, `question_banks`, `quiz_sessions`, `users`, `organizations`) omit `.select('id')` chain — `deleteOrLog` accepts `PromiseLike<{ error }>`. The `.select('id')` + zero-row-log pattern only appears in `cleanupReferenceData` (different contract). New `exam_configs` line correctly matches sibling pattern; do not flag as §5 violation.
- **Mig 094 GRANT confirmed.** `explanation_text` + `explanation_image_url` appear on lines 143–144 of `094_question_type_enum_and_column.sql` GRANT block. The docs claims ("columns remain in the mig 094 column GRANT and are PostgREST-readable") are factually correct.
- **Line 2401 (pre-existing, outside diff) is not inconsistent.** It says "revealed only via `get_vfr_rt_exam_results`" in the context of the VFR RT RPC family, consistent with the updated wording "only here among the VFR RT RPCs". No conflict.

## Notes on #837 UI polish commit (question-table + calc-filter redesign)

- **`truncate` CSS class + `truncate()` function co-exist in question-table.tsx without conflict.** Line 77 uses Tailwind `truncate` class for CSS text-overflow; line 87 calls the imported `truncate(text, max)` function for topic name truncation. Both are valid — TypeScript distinguishes them. The `truncate` import is NOT dead. Do not flag as dead import on future review of this file.
- **`difficulty: 'medium'` in `makeQuestion()` fixture is intentional.** `QuestionRow.difficulty` is preserved (edit form reads it); only `QuestionFilters.difficulty` was removed. The fixture still satisfies the `QuestionRow` type. Not a stale test artifact.
- **`FilterToggle` extracted inside the component file, not to `_hooks/` or `_utils/`.** code-style §7 test requirement applies only to files in `_hooks/`/`_utils/`/`lib/`. An intra-file component helper does not require a co-located test file. Correct.
- **0 CRITICAL, 0 ISSUE, 0 SUGGESTION — APPROVED.** All 6 change areas clean: calc-badge shrink-0, difficulty column removal (both filter types + symmetrically from count+data queries + type), tooltip/full-text DOM approach, dialog overflow CSS, student calc toggle redesign (mutually-exclusive logic correct), terminology rename (label + tests aligned). No dangling symbol references found.

## Notes on #834 duration-formatter hours tier

- **Pure arithmetic + render-site change — no DB/RLS/auth surface.** All 5 boundary cases verified correct by arithmetic: `formatDurationMinutes` (0m, 59m, 60→1h 0m, 1629→27h 9m) and `formatDuration` seconds-level (59m59s, exactly 1h→1h 0m 0s, 27h 9m 43s). Both helpers use integer floor + modulo — no float-rounding risk.
- **Admin `clickable-session-row.tsx` already handled.** `session-table-helpers.ts` has its own hours-aware formatter (`'1h 0m'`, `'2h 30m'`) — was out of scope for this plan and correctly untouched.
- **Existing rendering tests (durationMinutes=15/20) continue to pass** — all fixture values are sub-60 min so `formatDurationMinutes` returns the unchanged `Xm` format.

## Notes on PR #856 CR triage (check_quiz_answer hardening + batch_submit replay fix)

- **`getSetCorrectOptionId: () => h.setCorrectOptionId` thunk is safe despite `h` defined on next line.** The thunk is captured by closure and only invoked inside `startTransition(async () => {...})`, which fires after the render cycle completes and `h` is defined. Do NOT flag this as an uninitialized reference — it reads `h` at call time (async callback), not at definition time (render). Memory note: this is the same "event-driven fetch, NOT useEffect" false-positive pattern already tracked.
- **`import-schema.ts` uniqueness refine (size===4) is load-bearing even with `id: z.enum(['a','b','c','d'])`.** The enum constrains each option's id to the valid set but does not prevent duplicates in the 4-element array. The `.refine` is the only guard against `['a','a','b','c']`. Plan point 3 correctly identified this gap.
- **`check_quiz_answer` guard order: auth → active-user → session SELECT (ended_at IS NULL) → mode guard → config guard → membership → question fetch.** Matches sibling `submit_quiz_answer` (mig 110) exactly. Confirmed byte-identical between packages/db/migrations/115_* and supabase/migrations/20260612000700_*. Do NOT flag the ordering deviation from batch_submit_quiz (which combines role+active-user into one SELECT for audit_events) — check_quiz_answer has no audit INSERT so the consolidated pattern is unnecessary.
- **batch_submit replay path: removing `AND q.deleted_at IS NULL` from the completed-session JOIN is the §15 carve-out applied to replay.** Without the fix, a question soft-deleted after session end would drop from replay results while still being counted in `answered_count` (from quiz_session_answers). The comment documents the divergence from the fresh-submit path correctly.
- **0 CRITICAL, 0 ISSUE, 0 SUGGESTION — APPROVED.** Both migration pairs byte-identical, all plan items implemented, guard ordering correct, hermiticity correct (try/finally with console.error in finally blocks), test titles behavior-first.

## Notes on #832 verdict parser fix (run-security-auditor.sh)

- **`set -euo pipefail` + non-zero function return in `if` condition is safe.** A bash function returning non-zero inside `if parse_verdict "$VAR"; then` does NOT trigger `set -e` — bash exempts the condition of `if`/`while`/`until`. Both call sites in run-security-auditor.sh are in `if` positions. Do not flag this pattern as a `set -e` hazard.
- **`${1:-}` guard is the correct `set -u` idiom for optional first argument.** Prevents unbound-variable abort when the hook is called with no args (lefthook path). Confirmed safe under `set -u` by live invocation.
- **Bash test scripts should use `set -u` only, NOT `set -e`.** The `|| actual=$?` exit-code capture pattern works correctly only when `set -e` is not active. If `set -e` were active, the `||` would still suppress the abort, but `set -u` only is the right posture for test scripts that intentionally invoke commands expected to fail.
- **Test false-pass risk via wrong path: exit 127 (file-not-found) ≠ expected exit 1 or 0 → FAIL.** If `$HOOK` pointed to a nonexistent file, `bash "$HOOK"` exits 127. Because all expected values are 0 or 1, every case would report FAIL — the test cannot silently pass on a bad hook path.
