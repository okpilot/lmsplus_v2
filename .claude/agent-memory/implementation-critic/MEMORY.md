# Agent Memory — implementation-critic

> Reviews staged changes against the approved plan before commit.
> This index holds durable recurring-deviation knowledge. Per-commit narrative lives in `git log`.

## Recurring-deviation tracker

| Pattern | First Seen | Count | Last Seen | Status (→ rule loc) |
|---|---|---|---|---|
| Zero-row no-op: UPDATE/DELETE missing `.select('id')` + `data?.length` check | 2026-04-10 | 6 | 2026-06-06 | PROMOTED → code-style.md §5. Recurs in prod (toggleExamConfig) AND test helpers (#622 afterEach, backdateSession, PR-A session-replay.spec.ts #256 question soft-delete + restore, PR-7 BE org-transfer). Still flag in new code. `cleanupReferenceData` split (#775 setup→cleanup.ts) confirmed clean: `.in().delete().select('id')` + conditional log already present in the staged diff. |
| Dead helper in test file → Biome `noUnusedVariables`/`noThenProperty` pre-commit fail | 2026-04-11 | 2 | 2026-05-27 | RULE CANDIDATE. queries.test.ts `buildChain`; PR-C `buildAnswersClient` (147 lines). Grep call sites for any large test helper before approving — delete if only self-referenced. |
| Error message refactor breaks paired test assertion regex | 2026-05-06 | 1 | 2026-05-06 | WATCHING. #628 `pickSubjectWithQuestions` dropped "in org" suffix; seed.test.ts:152 regex stale. Grep test files for old message substring when context strings (orgId, table name, filter clause) change. Note: #709 helper extraction kept error message byte-identical across all 4 call sites — no recurrence. |
| TRANSPORT_LAYER/payload-group loop applied to fewer RPCs than plan states | 2026-05-07 | 1 | 2026-05-07 | WATCHING. #108 `void_internal_exam_code` got DB_LAYER only. When plan documents a payload group across N RPCs, count loops in each describe block before approving. |
| Conditional redirect regression when helper return value discarded | 2026-04-14 | 1 | 2026-04-14 | WATCHING. `handleSubmitSession` discarded `discardQuizSession` result; `router.push` fires only on success → stranded user. Check callers of helpers that made an unconditional side-effect conditional. |
| Too-lenient INSERT rejection assertion (OR-branch allows vacuous pass) | 2026-05-31 | 2 | 2026-06-10 | RULE CANDIDATE. #314 flag-idor + server-action-unauthenticated used `expect(error !== null \|\| (data?.length ?? 0) === 0).toBe(true)`. Count=2: new integration test (feat/697-vfr-rt-phase-a, mig-094 CHECK tests) uses `isCheckViolation = errMsg.includes('23514') \|\| errMsg.includes('check') \|\| ...` boolean instead of established `expect(error?.code).toBe('23514')`. PostgrestError always has `.code` (SQLSTATE) on both `.from().insert()` and `.rpc()` — always assert it directly. |
| Hard DELETE on quiz_sessions in red-team afterAll/afterEach | 2026-06-05 | 1 | 2026-06-05 | WATCHING. #611 quiz-session-score-forgery.spec.ts afterAll used `.delete()` (hard DELETE) on quiz_sessions. Sibling specs (config-injection, rpc-cross-tenant, complete-overdue) all use soft-delete (`update({ deleted_at: ... })`). docs/database.md soft-delete matrix explicitly marks quiz_sessions as "Yes" (soft-delete only). Flag as ISSUE in future specs. |
| Thin-wrapper page-error tests: mock-dependency form is valid when SUT is pure pass-through | 2026-06-01 | 1 | 2026-06-01 | WATCHING. PR-A4 (#699) 7 fetchUser* functions are pure `return fetchAllRows(...)` — no mapping/filtering between fetchAllRows and the return. Mock-the-dependency (`vi.mock('@/lib/supabase-paginate')`) proves propagation completely. Non-vacuous because fetchAllRows is the ONLY code path. Do NOT flag these as "bypassing production logic" — there is no production logic to bypass. |
| New _hooks/ util extracted without co-located test | 2026-06-01 | 2 | 2026-06-20 | RULE CANDIDATE. PR-B1 (#565) quiz-recovery-handlers.ts (95 lines, 3 builder fns); VFR RT Phase 1 use-vfr-rt-start.ts (90 lines) — both added to _hooks/ without a .test.ts. code-style.md §7 requires tests for new utilities in _hooks/. Flag as ISSUE on all future _hooks/ util additions lacking a test. |
| #582 Readonly<Props> sweep: plan said "5 exist", reconciled to 3 | 2026-06-01 | 1 | 2026-06-01 | WATCHING. Issue originally stated 5 occurrences; 3 were the named `Readonly<Props>` pattern; 1 remaining is an inline `Readonly<{...}>` in `admin-internal-exam-report-header.tsx` which is a different idiom (not a named Props alias). The distinction matters: plan explicitly reconciled to 3 before execution. No false positive — reconciliation was correct. Track whether inline Readonly<{...}> should also be normalised. |
| Security.md doc bullet claims RPC performs capability it doesn't have | 2026-06-05 | 1 | 2026-06-05 | RESOLVED. AJ bullet (v1) claimed `upsert_exam_config` performs "audit logging" — RPC has no `INSERT INTO audit_events`. Fix verified: revised AJ bullet correctly describes soft-delete-aware lookup + conditional insert/update branch; index name `uq_exam_configs_org_subject_active` + 23505 rejection claim both confirmed against mig 20260411000007 + 20260411000008. Rule: when a doc bullet describes what an RPC "does", read the latest CREATE OR REPLACE definition before approving. |
| Doc describes trigger exemption/guard ordering that doesn't match the actual migration body | 2026-06-06 | 2 | 2026-06-06 | WATCHING. Batch A DB-hardening: (1) database.md describes mig 089 trigger as having `current_role != 'service_role'` exemption — actual trigger body is unconditional. (2) database.md validation contract and inline code block show `too_many_questions` BEFORE `no_questions_provided`, but actual mig 086 has the reverse (no_questions_provided first, then too_many_questions — matches the plan). #532 mig 092 round-1 comment L14 claimed "No admin-void path sets ended_at" — factually wrong; fixed in round-2 with `auth.uid() = NEW.student_id` guard and corrected comment listing all 5 ended_at-setting functions including void_internal_exam_code. When plan/migration comments make factual claims about which callers fire a trigger, grep for `SET ended_at` across all RPCs before approving. |
| Cross-org red-team Attack uses sentinel UUID because target org has no seeded questions | 2026-06-06 | 1 | 2026-06-06 | WATCHING. #625 Attack 1: attacker=egmont, target=redteam-other-org (no seeded questions) → always falls back to sentinel '00000000-...' → proves "unknown UUID rejected", NOT "cross-org org guard fires". Same code path as the mixed-valid test in #626. Non-vacuous fix: either throw if other-org has no questions, or flip attacker/victim perspective so the attacker is in the org without questions and uses IDs from the org that HAS questions (pattern used correctly by #623). |
| DB CHECK constraint violation from too-long document_version in test seed | 2026-06-06 | 1 | 2026-06-06 | WATCHING. PR-7 rpc-cross-tenant-x-1.0 (22 chars) exceeds BETWEEN 1 AND 20 on user_consents.document_version. Also dead constants E2E_CONSENT_GATE_TOS/PRIVACY_VERSION (21 chars each) in seed.ts. When seeding user_consents, always count chars against the 20-char constraint. |
| Doc new-section insertion duplicates existing heading/entry | 2026-06-10 | 1 | 2026-06-10 | WATCHING. VFR RT Phase A post-review fix: new plan.md section inserted caused duplicate `## Security & Test-Hardening Sprint` heading (lines 42+44). Separately, database.md RPC summary table got a second `complete_empty_exam_session` row. Both introduced by the doc-sync pass. When inserting a new section before an existing section in a doc, grep the target file for the first line of the existing section to verify it isn't duplicated after the insert. |
| plan.md integration-test count wrong: pre-existing wrong baseline propagated to new "now N" claim | 2026-06-11 | 2 | 2026-06-11 | FALSE POSITIVE (reconciled 2026-06-11 by orchestrator). The repo convention for the plan.md count literal is the VITEST RUNTIME total (`pnpm --filter @repo/db test:integration` summary line), NOT a static `it(` grep — `test.each`/parameterized tests make grep structurally undercount. Vitest reported 136 at the Phase A merge gate and 144 on this branch (+6/+2 deltas) — both plan.md claims were correct. Do NOT re-raise count findings from static grep; verify via the vitest run summary. Delta-checking remains useful. |
| Red-team results spec uses wrong vector ID sub-labels (DB2/DB3 instead of DR2/DR3) | 2026-06-14 | 1 | 2026-06-14 | WATCHING. #825 rpc-vfr-rt-results.spec.ts header and test titles use `DB2`/`DB3` — but attack-surface.md vector `DB` is the internal-exam double-redemption vector (start_internal_exam_session). The correct IDs for the results spec are `DR2`/`DR3` (the DR vector covers get_vfr_rt_exam_results). The attack-surface.md staging update correctly says DR2/DR3; the spec file uses different labels. When writing new red-team specs that cover sub-vectors of an existing matrix entry (e.g. DR), use the matrix vector ID (DR) as the prefix, not an arbitrary sub-label (DB). |
| Red-team non-vacuity read missing `enabled` filter that the RPC itself uses | 2026-06-14 | 1 | 2026-06-14 | WATCHING. #825 DN2 cross-org config non-vacuity check: the RPC SELECT filters `enabled = true` AND `deleted_at IS NULL`, but the test's cross-org absence proof only filters `organization_id + subject_id + deleted_at IS NULL` (no `enabled`). A disabled config in the attacker's org would make `crossOrgConfig` non-null → test assertion `expect(crossOrgConfig).toBeNull()` fails with a false positive. Non-vacuity reads that mirror an RPC's filter must use ALL the same predicates the RPC uses — not just a subset. |
| Namespace written to localStorage during quiz session but never in RT path — RT namespace cleared pre-start but session runner writes back to default key | 2026-06-20 | 1 | 2026-06-20 | WATCHING. VFR RT Phase 1: `use-vfr-rt-start.ts` reads/clears `RT_STORAGE_NAMESPACE`, but the quiz session runner (`useQuizPersistence` → `writeActiveSession`) always uses the NO-namespace (default `quiz-active-session:userId`) key. So the RT namespace is only used to guard the pre-start confirm — once the runner is active it checkpoints to the quiz key, not the RT key. Next checkpoint by an open quiz tab overwrites the RT session. Flag whenever a new storage namespace is introduced but the checkpoint write path doesn't thread it through to the persistence hook. |
| Fractional partial-credit SUM funneled through an `int` plpgsql variable → rounded before percentage computed | 2026-06-21 | 1 | 2026-06-21 | WATCHING. VFR RT Phase 2.3 mig 121 `batch_submit_quiz` dispatch: `SELECT sum(LEAST(correct_rows::numeric/total_blanks, 1.0)) INTO v_correct_count` where `v_correct_count int` (L33) → plpgsql implicit numeric→int cast ROUNDS the dialog_fill partial-credit sum BEFORE `v_score = round(v_correct_count::numeric/total*100,2)` (L258/261). MC/short-only = lossless (each fraction 0/1, sum integral) so MC byte-equivalent — but dialog_fill partial credit (e.g. 0.667) silently rounds to 1.0. Decision 47 documents partial credit as intended → true plan deviation, not accepted trade-off. Precedent `submit_vfr_rt_exam_answers` (mig 113) feeds `avg(LEAST(...))` into `numeric(5,2)` part vars, never an int. Fix: compute score from un-rounded numeric sum; round only for the `correct_count` INT column/return field. When a partial-credit/fractional aggregate is computed, verify the receiving variable type is `numeric` until after the percentage is derived. |
| Agent-memory curation: stub rows with `[full → topics/tracker-archive.md]` pointer require archive to already contain the full entry | 2026-06-22 | 1 | 2026-06-22 | WATCHING. #948 learner MEMORY.md: the second count=1 table had stub rows pointing to tracker-archive.md, but 9 of those stubs had NO corresponding full entry in the archive — the archive had slightly-differently-worded entries for 6 of them (fuzzy-match confirmed), plus 2 were PROMOTED rows in the main live table, and 1 (vi.spyOn) was in the live table under a different suffix. All content was conserved. Pre-check before flagging stub-row-with-no-archive-match: use fuzzy grep on the archive, check the live table, check different suffix forms. |

## Durable knowledge

- **CREATE OR REPLACE trace before flagging.** Before flagging a missing pattern (guard, search_path, auth check) on a Postgres function, trace the full `CREATE OR REPLACE FUNCTION` chain to the LATEST migration definition — the guard may already exist in a prior migration. Required pre-flag step per `agent-critic.md`. (Verified correct on #622 mig 076→077, #629 080→081, #108 void_internal_exam_code.)
- **Dual-directory migration invariant.** Numerically-numbered migrations (e.g. `081_*.sql`) must have a byte-identical copy in BOTH `packages/db/migrations/` and `supabase/migrations/`. EXCEPTION: timestamp-format migrations (`20260521000005_*` etc.) live in `supabase/migrations/` ONLY — confirmed across mastery RPC, dashboard-stats RPCs, filtered-pool RPCs, profile-stats RPC. Don't flag a missing packages/db counterpart for timestamp-format files.
- **Security §11 (multi-permissive RLS) self-scope is load-bearing.** Per-caller RPCs reading `student_responses` / `quiz_sessions` / `exam_configs` / `audit_events` must carry an explicit `<owner> = auth.uid()` predicate even when SECURITY INVOKER + RLS is present — RLS ORs the broader instructor/admin policy. The `auth.uid()` predicate is correct, not redundant; do not suggest removing it.
- **types.ts nullable-SQL-column convention.** Admin/student RPC entries in `packages/db` `types.ts` may type nullable SQL columns as non-nullable (e.g. `avg_score: number`) — this matches the established `get_admin_student_stats` pattern. Production query files use the `authRpc`/`rpc` wrapper with their own local Row type that captures nullability. Not a deviation; flag at most as SUGGESTION.
- **types.ts stale column after DROP+CREATE migration: ISSUE class when not in staged diff.** When a migration uses DROP FUNCTION + CREATE to change a RETURNS TABLE (removing a column), `packages/db/src/types.ts` must also be updated to remove the column from the Returns type. The file is auto-generated but committed — when it's not in the staged diff, flag as ISSUE. The risk is low when no consumer uses the Database type path (reports.ts uses a local RpcRow type), but the types.ts stale entry means `supabase gen types` would regenerate correctly while the committed file is wrong. First seen: #471 `get_session_reports` dropped `answered_count` column, types.ts line 1385 not updated.
- **`rpc`/`authRpc` wrapper contract.** Returns `{ data, error }` and never throws on query errors. `Promise.all([rpc(...), supabase.from(...)])` carries no unhandled-rejection risk. `fetchAllRows` return type guarantees `data: T[]` (never null) — `?? []` fallback after it is redundant.
- **Doc-only commits: mig comment vs guard line range.** The `L69-75` → `L73-75` correction on mig 117 is a valid finding class: L69-72 was the comment block, L73-75 is the IF/RAISE/END IF. When a citation spans a comment+code range, the code-only sub-range is more precise. Confirmed clean on #918 doc-fix commit.
- **Count/page filter symmetry (pagination).** When a read is split into a count query + paginated page query, the WHERE filters must be byte-identical between the two — a mismatch fetches wrong/excess rows. Verified clean repeatedly across #668 instances; keep it a checklist item.
- **Test title impl-detail leakage (code-style §7).** `it(...)` titles must not name internal helpers/types/validator branches (`forwards X to fetchAllRows`, `from FooOpts`, `typeof guard`). Public props, public SDK methods, and integration-boundary RPC names ARE permitted (they're contracts). Audit inline comments after a title rename — they often go stale.

## Durable knowledge — tooling/config

- **knip `ignoreDependencies` is workspace-scoped.** The correct knip schema uses `ignoreDependencies` inside the workspace key (not at top level) for per-workspace suppressions; `ignoreBinaries` + `ignore` live at top level. Verified clean on #325. Don't flag this pattern as incorrect.
- **`@repo/ui` is listed as a dep in `apps/web/package.json` even though no `@repo/ui` import exists in TypeScript source** — `packages/ui/src/index.ts` exports `{}` (Phase 5 placeholder). Ignoring it in knip is intentional; it is a forward-declared workspace dep. Do not flag as a dead dep.
- **Broad grep for component names (e.g. "Separator", "Progress", "Tooltip") returns false-positive file matches** when sibling components use same-named primitives from `@base-ui/react` directly (e.g., `SelectSeparator` in `select.tsx` uses `SelectPrimitive.Separator`, not the deleted `components/ui/separator.tsx`). Always verify import path, not just symbol name.
- **Tailwind v4 `@plugin` directive placement** — place after all `@import` lines, before `@custom-variant`/`@theme`. Verified correct on #325 (globals.css lines 1–6).

- **Dead mock branches in test helpers — ISSUE class, not cosmetic.** When a test mocks `mockFrom` with a per-table dispatcher, any `if (table === 'X')` branch for a table that the SUT no longer reads is dead code. The `throw new Error('Unexpected table: X')` guard at the bottom means the dead branch was written defensively to avoid that throw — but it also silently masks future regressions where the SUT unexpectedly reads the dead table. Remove dead branches so the guard fires if assumptions are violated. First seen: PR-A1 dashboard.test.ts:383 `easa_topics` branch — dashboard.ts has not read `easa_topics` since the mastery-RPC refactor.

## Notes on PR-A3 sweep pattern

- **Type-widening for DB-typed RPC columns (`n: number` in `get_question_counts`).** `exam-config/queries.ts` and `syllabus/queries.ts` apply `Number(row.n)` where the DB types.ts already declares `n: number`. No local Row type is declared; TypeScript sees a `number`. `Number(n)` on a `number` is a harmless identity but correct at runtime if PostgREST ever sends a string. Do NOT flag the missing local type widening for these files — the DB types are the contract, and the coercion is the defensive fix.
- **`(x != null ? Number(x) : null) ?? 0` is the correct pattern for nullable NUMERIC with a non-null fallback.** `quiz-report.ts` and `admin-quiz-report.ts` both use this two-step form. The inner ternary handles null→null; the outer `?? 0` is the pre-existing non-null default for `QuizReportSummary.scorePercentage: number`. Pattern is correct — verified PR-A3.

## Notes on PR-A2 sweep pattern

- **GROUP 1 real-error tests omit console.error assertion.** upsert-subject/topic/subtopic real-error tests assert `result.success === false` + `result.error` message but don't spy on/assert `console.error`. Production code does call it. Not flagged as ISSUE (plan only required the two behavioral outcomes); flagged as SUGGESTION. Pattern: when production code has a side-effect (logging), tests covering that path may not verify the side-effect. Consider whether this is a recurring blind spot.

## Durable knowledge — cached-role pattern in SECURITY DEFINER RPCs

- **Cached role variable prevents NOT NULL abort on delayed soft-delete.** When a SECURITY DEFINER RPC inserts into `audit_events` (actor_role TEXT NOT NULL), the actor's role must be fetched once into a local variable at authz time — not via repeated inline subqueries later. An inline `(SELECT u.role ... WHERE u.id = v_admin_id AND u.deleted_at IS NULL)` in the INSERT VALUES clause returns NULL if the admin is soft-deleted between the authz check and the audit insert, aborting the whole transaction on the NOT NULL constraint and rolling back an already-authorized mutation. Pattern established in mig 078 (batch_submit), applied to mig 084 (void_internal_exam_code) in PR #731. security.md §10 deleted_at filter is still required on the capturing SELECT — the fix does not relax it, it consolidates it.
- **NULL-org guard doubles as NULL-role guard when role is fetched on the same SELECT.** After `SELECT u.organization_id, u.role INTO v_admin_org, v_admin_role`, the `IF v_admin_org IS NULL` guard also ensures v_admin_role is non-null (the SELECT either populates both columns from a matching row or yields NULL for both). No separate v_admin_role IS NULL guard is needed.

## Notes on middleware anti-cache headers + cookie-orphan fix (#446)

- **Accepted trade-off on request-cookie propagation.** The old `response = NextResponse.next({ request })` in setAll propagated freshly-set request cookies into downstream Server Components. Removing that reassignment (mutate-in-place fix) loses that path. Plan explicitly accepted this: "that benefit was already lost (orphaned response), so this is strictly better." Do NOT re-flag request-cookie propagation loss in this codebase — it is a documented trade-off.
- **Duplicate not-throw test is a SUGGESTION-class finding, not ISSUE.** The added "does not throw when setAll is called without a headers argument" (middleware.test.ts new) is semantically identical to the pre-existing "writes set-cookie headers to the response via the cookies.setAll adapter". Duplicate test cases are not flagged by Biome (contrast: named helper functions which DO trigger noUnusedVariables). Log as SUGGESTION only.

## Positive patterns

- **VFR RT Phase 2 Task 2.2 (new check_non_mc_answer non-MC practice grader RPC): clean rule-11c sibling-guard clone + §15 membership-before-read ordering.** mig 119 + supabase mirror `20260621000200` byte-identical (`diff`). Genuinely NEW fn (CREATE OR REPLACE chain: only the staged files define it). Guard set mirrors LATEST check_quiz_answer (mig 117) EXACTLY: auth null → active-caller(users.deleted_at IS NULL) → owner+ended_at IS NULL+deleted_at(rule 11 multi-permissive scope) → practice whitelist IN('smart_review','quick_quiz') → config-shape explicit IS NULL → membership(=ANY frozen config.question_ids) → SET search_path=public + SECURITY DEFINER + GRANT EXECUTE only (no answer-key column-level grant). §15 CRITICAL invariant HOLDS: membership(L126) precedes the ONLY answer-key read(L138-153) — canonical_answer/blanks_config never touched for a non-member question. Grading byte-faithful to submit_vfr_rt(mig 20260619000300 L160-207): short normalize+canonical+synonyms w/ `v_norm<>''`; dialog per-blank `^\d{1,4}$`+blanks_config lookup `(b->>'index')::int`+NOT FOUND→invalid_blank_index; MC→unsupported_question_type; answer_type_mismatch payload guards both branches. blanks_config keys {index,canonical,synonyms} match schema mig 20260610000100. Return contract per plan; dialog top-level is_correct=all-blanks. 280≤300 lines, RAISE codes sanitized tokens. ONLY finding: docs/security.md §15 RPC-list entry for check_non_mc_answer not yet present — correctly DEFERRED to doc-sync pass (staged diff intentionally migration-only); the migration's own inline §15 comment is complete. SUGGESTION, not blocking. SCOPE-SPLIT RULE: when a commit is intentionally migration-only, the central-doc cross-reference (security.md §15 list, RPC summary table) belongs to the doc-sync pass — do not flag as missing-from-this-commit ISSUE. APPROVED 2026-06-21, 0 CRITICAL/ISSUE.
- **VFR RT Phase 2 Task 2.1 (extend get_quiz_questions for non-MC q-types): clean DROP+recreate + strip-precedent reuse + cast-guard.** mig 118 + supabase mirror `20260621000100` byte-identical (`diff`). DROP+CREATE (not OR REPLACE) for the RETURNS TABLE widen; 12 existing cols verbatim in mig-059 order + 3 new (question_type/dialog_template/blanks_safe). The phase-defeating LATERAL+GROUP BY (drops `options='[]'` non-MC rows) correctly REPLACED by mig-105's correlated-subquery `CASE WHEN multiple_choice THEN (SELECT jsonb_agg(... ORDER BY random()) FROM jsonb_array_elements(options)) ELSE NULL`. Strip logic byte-matches mig 105: dialog_template `regexp_replace('\{\{(\d+)\|[^}]*\}\}','{{\1}}','g')`, blanks_safe index-only. NO answer-key cols selected. All guards preserved (auth.uid null-check, SET search_path, SECURITY DEFINER, deleted_at+status filter, joins, GRANT). Caller `load-session-questions.ts`: wire-boundary `blanks_safe: unknown`, `Question` typed precisely, mapper uses `Array.isArray(q.blanks_safe) ? cast : null` (§5 cast-guard, not raw cast), no `any`. Unit test asserts dialog_fill pass-through + MC null. Pre-Flag chain latest = mig 20260327000059 (12-col LATERAL); staged mig is next in chain → genuine new behavior. APPROVED 2026-06-21, 0 findings.
- **#925 Phase 4 C3 (coderabbit-sync + fixes commit): clean hash replacement + YAML placement + accuracy fixes.** All 5 dangling pre-squash hashes (`21d86dc5`/`4e462cf0`/`ba6a378e`/`2aaf5a03`/`03cb2fa7`) removed from code-style.md; both replacement squash refs (`fb2921c6`/`f4c76c83`) confirmed reachable on master. `.coderabbit.yaml` YAML validates cleanly; new query-site instruction placed under `actions.ts` block (correct); cast-guard instruction placed under `**/*.test.{ts,tsx}` block (correct); no duplication with existing instructions. Decision 46 body edits accurate: "~73/~14" → "over 100/~30" and "3 plan-critic rounds" → "three plan-critic approval rounds" corrected; "lack the `deleted_at` column" qualifier added (verified: all 7 named tables confirmed column-absent; hard-delete-exception tables like `question_comments` that retain the column are correctly excluded from the list and the distinction pointer to database.md §3 is accurate). plan.md "this PR" → bare "Phase 4" self-reference removed. APPROVED 2026-06-21.
- **#925 Phase 4 C2 (docs-only commit): clean placement, preservation, and no count-literal drift.** decisions.md Decision 46 correctly numbered (45 was prior latest), placed after Decision 45 `---` before footer; footer prepends new entry preserving full prior chain. plan.md new section is first `##` after head `---`; packages/db integration-test count literals untouched. tech.md Unit/integration bullet appended (not replaced); only Biome version changed. No accidental edits outside planned scopes. APPROVED 2026-06-21.
- **#925 Phase 2 (quiz-action integration tests): clean paired-positive vacuity discipline + raw-coalescing guard.** 5 files (start/submit/check-answer/complete/batch-submit `.integration.test.ts`). Every cross-user/not-found negative paired with a positive proving the victim/own session resolves (non-vacuous). batch-submit raw `expect(result.passed).toBeNull()`/`expect(result.expired).toBe(false)` (NO in-test `?? null` coalescing) genuinely guards source batch-submit.ts:73-74 — the prior in-test coalescing vacuity bug was fixed before review. §7 lifecycle test (complete) drives start→submit×3→complete through the action layer (no fixtures) and asserts score reflects answers (66.67%, not echoed). All error strings asserted via exact `toBe`, verified byte-for-byte against action sources; seed.ts:124 confirms explanation_text non-null + image default-null so the truthy/null assertions are non-vacuous. Optional 'Question not found' RPC-error test absent — plan said "may," so compliant. APPROVED 2026-06-20, 2 SUGGESTIONs only.
- **#925 Phase 1 commit 1 (simple-read integration tests): clean count-isolation + RLS-vacuity reasoning.** quiz-subject-queries + exam-subjects `.integration.test.ts`. Count-isolation non-vacuity VERIFIED: `get_question_counts` is SECURITY INVOKER scoped by `questions` tenant_isolation RLS → two orgs seeding 3 Qs each under the SAME shared easa_* refs means a broken scope WOULD read 6; asserting exactly 3 is non-vacuous. Omitting the exam_configs `enabled:false` negative is CORRECT: `student_select_exam_configs` USING already filters `enabled=true AND deleted_at IS NULL`, shadowing the helper filter → such a negative is vacuous. signInAs INSIDE each it(); `.find()` not index; unique suffixed codes (S${suffix}, never 'RT'); exam_configs inserts cover all NOT-NULL no-default cols + satisfy CHECKs; 3-step (2×cleanupTestData + cleanupReferenceData) and 2-step accumulators with `errors.length===0` dependent-step guard. APPROVED 2026-06-20.
- **#925 Phase 0 app-layer DB integration tier: clean harness/plumbing.** New `*.integration.test.ts` ingestion fully gated: unit vitest exclude + base tsconfig exclude + dedicated tsconfig.integration.json/check-types:integration (mirrors @repo/db pattern). `@repo/db/test-helpers` re-export import-safe (setup/seed/cleanup import only @supabase/supabase-js, never vitest). `next/headers` cookie-jar mock hoist-safe (module-scoped fn + globalThis jar, no import capture), `beforeEach` resets jar → no auth bleed; one globalThis jar shared across createServerSupabaseClient() calls so signInAs session persists. afterAll = single internally-isolating cleanupTestData (deleteOrLog log-don't-throw) → §7 per-step-accumulator EXEMPT (correct). Reads migration-097 canonical RT subject (`code='RT'`/topic `P1_ACRONYMS`, verified present) instead of seeding easa_* → no cleanupReferenceData needed. CI exports env to $GITHUB_ENV (job-scoped) + reuses running container. APPROVED 2026-06-20, 2 SUGGESTIONs only.
- **#902 record-emailed E2E spec: clean hermiticity + non-vacuity pattern.** Per-step error-accumulator (2 independent try/catch/finally blocks, sets cleared in finally), soft-delete of both internal_exam_codes + quiz_sessions with `.select('id')` + conditional log, EB existence proof before cross-org attack, consumed/voided seeding correctly satisfies both pair-consistency CHECKs. APPROVED 2026-06-19.

- **#915 EC E2E redesign: clean discrimination-control pattern for transport-dependent actions.** Dropping audit-count assertions was correct — the audit row requires a successful email send (RESEND_API_KEY absent in CI under NODE_ENV=production, resend.ts:29 fails closed → ok:false → 'Failed to send email' toast). NEXT_PUBLIC_APP_URL IS set in CI (e2e.yml:134/142), so the early-return guard at send-code-email.ts:42 does NOT fire — the discrimination control deterministically reaches 'Failed to send email' in CI, not the stale-code guard message. Regex /Code emailed to student|Failed to send email/ is correct: covers both the email-configured (local) and CI-no-key outcomes. Non-vacuity is genuine: the active code MUST reach a post-guard terminal toast (one of those two), proving the button and action work. The stale-code negatives are proven state-caused, not broken-button. All imports clean after EMAILED_EVENT_TYPE + countEmailedAuditEvents removal. Voided seeding satisfies voided_pair_consistency CHECK (both voided_at+voided_by). afterEach uses `.select('id')` + zero-row conditional log per §5. APPROVED 2026-06-22, 0 CRITICAL/ISSUE.
- **#945 EJ soft-deleted-admin answer-key spec: clean vacuity + hermiticity.** Two rejection regexes (`/forbidden|user not found/i` for authoring, `/forbidden|caller has no organization/i` for report) are provably disjoint from both the positive-control regex (`/session not found|not in caller org|not completed/i`) and from each other — verified by regex test. 'not in caller org' substring in session-not-found message does NOT match `caller has no organization` regex. Dedicated throwaway admin distinct from EI's. Soft-delete asserts exactly 1 row. Restore in finally, assert outside. afterAll cascade-delete (single step, §7 exempt). No audit-count check correctly dropped (both RPCs are read-only). EJ is correctly the next sequential ID after EI. APPROVED 2026-06-21.
- **#864 has-image filter: clean mirror of calc-mode pattern.** DROP+CREATE signatures correctly targeted the 6-arg calc-mode overloads; all 3 function bodies carried SECURITY INVOKER / SET search_path / STABLE/VOLATILE / status+deleted_at / auth.uid() scoping verbatim from the prior definition; byte-identical two-dir mirror confirmed; imageMode threaded through both Zod schemas, both RPC wrappers, all 5 hooks, and the QuestionFilters component; test coverage mirrors calcMode coverage exactly. APPROVED 2026-06-14.
- **#862 keyboard shortcuts: stale-closure ref pattern applied correctly.** `optsRef.current = opts` + `highlightRef.current = highlightedIndex` set on every render (outside the once-attached `useEffect`); `onKeyDown` reads both via `.current`, so Enter always sees the live highlight. Hooks called before early return; `optionIds ?? []` safe fallback; `enabled === false` strict-equality guard correctly passes through when undefined. APPROVED 2026-06-14.

## False positives (do not re-raise)

- **Probe-gate keyed on allRows (pre-filter) is correct.** After the reports.ts probe-gate fix, `probeOutOfRangeTotal` is triggered only when `allRows.length === 0 && page > 1`. When `allRows` is non-empty but `rows` (post-filter) is empty, the code returns `totalCount: 0` without probing — this is correct (filtered page, not out-of-range). Do not flag `rows.length === 0 → totalCount: 0` path as "missing probe". Verified clean 2026-06-01.
- **Fix 1 probe + `count(*) OVER()` window function** — `p_limit: 1` on the probe RPC still returns the correct `total_count` because PostgreSQL evaluates the window function `count(*) OVER()` before applying the `LIMIT`/`OFFSET` clause; it counts all rows matching the WHERE predicate. Verified by reading migration `20260429000011_get_session_reports_exclude_internal_exam.sql` line 64. Do not flag probe-limit as "may return wrong total".
- **Fix 1 probe fires on page=2 empty** — `toHaveBeenCalledWith` in the "passes correct RPC parameters for page 2" test still passes because it asserts the first call's args; the probe's second call is unmocked (returns `{ data: [], error: null }`) and resolves with `totalCount: 0`. The test doesn't assert the result value, so no failure. Not a test correctness issue.
- **`getSessionReports` remaining body ~39 non-blank lines after `probeOutOfRangeTotal` extraction** — pre-existing auth/RPC/filter preamble (lines 59-92) cannot be meaningfully split further without artificial helpers; the extraction moved ~14 lines out. The code-reviewer BLOCKING finding was about the inline probe bloat; that bloat is now gone. Accept the residual >30 body lines as an orchestrator-pattern exception (auth + RPC call + filter + branch = one cohesive flow). Do not re-flag.



- `avg_score` / mastery RPCs return NULL (no COALESCE) for students with no sessions — intentional; app type is `number | null`, UI guards `!== null`.
- Hard DELETE on `exam_config_distributions` inside `upsert_exam_config` — intentional, documented in migration 043 + docs/database.md (ephemeral config table, same precedent as `quiz_drafts`).
- Adjacent conditional JSX guard blocks (`{canDismiss && (`) are not "duplicate buttons" — one state-driven trigger + one prop-guarded confirm-panel button are distinct.
- `_userId` / dropped-param on caller-scoped RPCs — the RPC is scoped via RLS + `auth.uid()`, so an unused student-id param is dead but harmless (SUGGESTION at most).
- Red-team seed `selected_option_id: 'a'` with `is_correct: true` — intentional; `get_student_mastery_stats` reads `sr.is_correct` directly, never re-derives correctness from `selected_option_id` vs the question's correct option. 'a' is cosmetic (#673 confirmed).
- Red-team spec with no `afterEach` is hermetic when each test seeds NEW unique rows and does not mutate shared beforeAll state — confirmed for rpc-void-internal-exam-code.spec.ts (#518/#638): every test creates fresh code+session rows; DD/DE (formerly BN-1/BN-2, renamed in #802) leave their code rows un-voided (RPC rejects before write); DF/DG/CD/positive (DF/DG formerly BO/BP) each touch only their own freshly-seeded rows. No shared seed mutation → no afterEach needed. Do not flag as a hermiticity violation.
- **try/finally hermiticity hardening for org-transfer tests (#768)**: moving seed-insert + transfer UPDATE inside `try` + declaring IDs as `let ... = null` before `try` is the correct pattern when a mid-test throw could strand shared state. Finally must use `console.error`, not `expect()`, to avoid masking original failure. Positive pattern — clean on #768.

## Topic pointers

- [tracker-archive](topics/tracker-archive.md) — older impl-critic findings (two dated spec-notes + the "Positive patterns" approval log, pre-2026-06-07)

## Notes on #533 loading-state commit 2 (answer-submit controls)

- **Clean commit — all plan items verified.** Both AnswerOptions callers (quiz-main-panel + session-answer-block) thread `submitting` correctly. `aria-busy={submitting || undefined}` correctly emits no attribute when idle (boolean `||` short-circuits to `undefined`, which React omits). `finish-quiz-dialog` `aria-busy` correctly placed on Save for Later only — the Submit Quiz button uses a text swap pattern (pre-existing). `SessionRunner` is unused (no callers beyond its own file) — no missed path. Internal-exam answer path goes through the same `session-answer-block.tsx` chain and IS covered. False positive guard: `finish-quiz-dialog.tsx` is 307 lines pre-existing; the diff adds only 7 lines to it — code-reviewer's file-size flag (if any) is on pre-existing bloat, not introduced by this commit.

## Notes on #533 Part C + regression fix (UNIT 1 + UNIT 2)

- **consent-form canSubmit still includes `!isPending` — LoadingButton double-disables, behavior preserved.** The `canSubmit = acceptedTos && acceptedPrivacy && !isPending` remains unchanged. With `loading={isPending}` AND `disabled={!canSubmit}`, LoadingButton internally computes `disabled={isPending || !canSubmit}`, which is functionally identical to the pre-change `disabled={!canSubmit}` (since `!canSubmit` is already true when `isPending`). Not a deviation — confirmed correct and non-redundant from an API-contract perspective.
- **issue-code-form `canSubmit` refactor (remove `&& !isPending`) is behavior-preserving.** `canSubmit` guards `handleSubmit` at line 41 (`if (!canSubmit) return`) AND the `type="submit"` button via `disabled={!canSubmit}`. Since `loading={isPending}` already disables the button during pending, removing `&& !isPending` from `canSubmit` removes a now-redundant gate. The `if (!canSubmit) return` guard inside `handleSubmit` still covers the double-submit case via the form's submit event. Clean.
- **Files that retain `Button` import alongside `LoadingButton` — correct.** All files that still render a Cancel/Close button (toggle-status-dialog, reset-password-dialog, export-student-dialog, void-code-dialog, code-entry-modal, config-form-dialog) correctly keep `Button` in scope. Files that converted their ONLY button (login-form, forgot-password-form, reset-password-form, consent-form, question-form-dialog, student-form-dialog, issue-code-form, change-password-form, data-export-card, edit-name-form) correctly removed the `Button` import.
- **quiz-main-panel `disabled={s.submitting}` revert is correct.** The previous `|| s.answering` blocked option-button clicks on the NEW question while the prior question's checkAnswer RPC was in flight, causing the "tracks answered count" test to fail (got 1, expected 2). `lockedRef` already prevents same-question double-submit. The option-area disable gate is correctly scoped to the session-level `s.submitting` (batch submit in flight), not the per-question `s.answering`.

## Notes on #533 CR fix commit (CR-1/CR-2/CR-3)

- **inFlightAnswers counter — nested try/finally pattern verified clean.** Outer try increments before the nested try block; outer finally always decrements via `Math.max(0, n-1)`. The inner catch for RPC errors returns `false` before success-path code, then falls to the outer finally. `handleAnswerError` still called with same args, `pendingQuestionIdRef.current.delete` still on success path, `onAnswerRecorded` still best-effort. No double-decrement possible; counter cannot go negative due to the `Math.max` floor.
- **ConfirmPanel `busy` prop pattern: `disabled={submitting}` retained, `aria-busy={busy||undefined}` scoped to own action.** Both call sites pass `busy={pendingAction === '<action>'}` correctly. The new test exercises `pendingAction==='save'` while the submit-anyway panel is open — confirms the attribute is absent when the wrong action is pending.
- **`h-7` is the genuine size="sm" class in button.tsx cva.** Confirmed at cva line 28: `sm: "h-7 ..."` (default is `h-8`, lg is `h-9`). The CR-3 assertion is non-vacuous.

## Notes on #789 recordAuthEvent wiring (commit 2)

- **Client-level mock intercepts helper correctly.** Tests for all 4 action files mock `supabase.rpc` at the Supabase client object level (`{ rpc: mockRpc }`). The `recordAuthEvent` helper receives the same client object and calls `supabase.rpc(...)` on it — so `mockRpc` is the intercepting mock. The helper runs for real but its I/O is fully captured. No `vi.mock('@/lib/audit/record-auth-event')` needed; client-level mock is correct and simpler.
- **Plan description of MEMORY.md change was inaccurate (benign).** Plan said "typo `VITAMIN_PASSWORD`→`VICTIM_PASSWORD` correction". Actual staged change: new positive-signal bullet for #789. `VICTIM_PASSWORD` was already correctly spelled in the file. Staged content is correct memory maintenance — this is a stale plan description, not a behavioral deviation. Do not re-flag in future commits.
- **`toggle-student-status-mutations.ts` in `actions/` dir does not require own test.** Code-style §7 requires tests for new files in `_hooks/`/`_utils/`/`lib/`. `actions/` is not in that list. Existing `toggle-student-status.test.ts` already covers both mutation helpers end-to-end via the module-level `@repo/db/admin` mock (which the mutations file imports). No test gap.

## Notes on VFR RT Phase A migrations commit (#697)

- **Large multi-migration commit (14 migrations): verbatim-copy diff approach.** For a commit where N migrations are verbatim copies with documented changes, run `diff <src> <dest>` to confirm ONLY the documented changes appear — do not read both bodies line by line. Verified clean on #697: 095b (one ON CONFLICT line), 095c (one ON CONFLICT line), 102 (DECLARE vars + mode guard + per-part block + CASE branches). All mirrors byte-identical confirmed by `diff` loop.
- **GDPR type widening for new nullable columns is a secondary change class.** When schema adds nullable columns to existing tables (`response_text`, `blank_index` on `quiz_session_answers`/`student_responses`), check 3 sites: (1) the SELECT string in the GDPR query, (2) the GdprExportPayload type in types.ts (must widen to `string | null`), (3) test fixture must add the new fields as `null`. All 3 aligned in #697.
- **tasks.md A.10 was completed but not checked in the staged diff.** A.10 artifacts (types.ts regen) ARE staged but the checkbox was not ticked. SUGGESTION class only — workflows item, not correctness.

## Notes on #838 CR round-3 applies (legacy-mode whitelist + active-user gate)

- **Mig 104 'not authenticated' (space) vs sibling 'not_authenticated' (underscore)** is a pre-existing divergence inherited from the copy source (20260406000004) — the plan explicitly lists only 2 changes. Do not flag as a deviation when the copy source is the source of truth. Both forms remain in the codebase; flag at SUGGESTION only.
- **`forceEndSession` inline (not try/finally) in whitelist tests** is safe because (a) `start_vfr_rt_exam_session` resumes an existing active session idempotently — so a leaked active session becomes the input to the NEXT test's `startSession()`, not a blocker; (b) `cleanupTestData` in `afterAll` hard-deletes all sessions by `org_id`. Pattern is hermetic even without per-test try/finally.
- **#838 review was clean (0 CRITICAL, 0 ISSUE, 1 SUGGESTION)** — twin-file identity, guard placement, copy-source fidelity, test non-vacuity and hermiticity all verified correct.
- **Post-#838 fix-cycle commit APPROVED (0 CRITICAL, 0 ISSUE, 1 SUGGESTION).** Mig 104 deleted_at guard test: non-vacuous (rollback assertion `ended_at IS NULL`), hermetic (try/finally with `console.error` restore, `{ error: endErr }` on force-end). Doc edits accurate: 094–104 range confirmed (15 files), footer claim matches existing L717/L975 entries. plan.md 132→133 in both current-phase lines. SUGGESTION: finally-block UPDATEs lack `.select('id')` + zero-row log — acceptable for cleanup context per §5 established pattern, plan explicitly only required `{ error }` destructure.
- **PR #830 cloud-CR commit APPROVED (0 CRITICAL, 0 ISSUE, 0 SUGGESTION).** Guard ordering verified in both RPCs (auth → gate → session SELECT FOR UPDATE → whitelist → state checks). Twin-file identity confirmed. The `submit_quiz_answer` code block in database.md is intentionally pre-#838 (established pattern — prose bullets describe the current security model). Existing vfr_rt rejection tests remain non-vacuous with the narrowed whitelist (vfr_rt_exam NOT IN ('smart_review', 'quick_quiz')). Exam-session cleanup in new test uses `ended_at + deleted_at` UPDATE (soft-delete correct per docs/database.md soft-delete matrix). `forceEndSession` `.select('id')` + throw was only required for the named helper — inline finally-block cleanups follow the `console.error`-only pattern. plan.md both count lines updated (header + Phase A section). False positive not raised: code block stale is pre-existing, not introduced by this commit.

## Notes on agent-health.yml false-positive fix (#810)

- **`var=$(cmd)` is exempt from `set -e` in bash** — `set -euo pipefail` does NOT abort on a failing command substitution inside a variable assignment. The old `|| true` after `xargs 2>/dev/null` in Check 3 was therefore NOT load-bearing under `set -e`; removing it (replacing xargs with `trim()`) is safe. `sed` piped from `awk` exits 0 on well-formed input, so in practice these never fail.
- **`trim()` placement in Check 3** — defined at line 88 (before the while-loop at line 90), outside the loop. Correct hoisting. Do not flag as "redefined per iteration".
- **Check 4 security-auditor comment placement** — the explanatory comment about `-not -path '*/security-auditor/findings.md'` is placed inside the while-loop body (last line before `done`), but it describes the `find` command in the process substitution. Cosmetically misplaced — SUGGESTION class only.

## Notes on #796 spec-split (rpc-cross-tenant + audit-completeness)

- **`cleanupFixtures` flagged_questions filter is student_id-unscoped by design.** The helper uses `.in('question_id', ...)` without a student_id filter — intentionally broader than the original specs (which used `.eq('student_id', victimUserId)`). In the test DB this is fine because only redteam seed users exist; flagged as ISSUE in this review but may be an accepted trade-off. Track: if the plan explicitly accepts this, add to false positives.
- **`seedVictimResponses()` correctly belongs to the isolation spec, not the reports spec.** The BY/BE/Q/X/CL3 tests in rpc-cross-tenant-reports.spec.ts all seed their own fixture data; none need the mastery/streak/last-practiced responses that the probe-only tests need. Do not flag the omission as missing non-vacuity seeding.
- **force-token-refresh.ts helper is a Playwright E2E seam** — code-style §7 Vitest test requirement does NOT apply to `e2e/redteam/helpers/*.ts`. (Confirmed again for helpers/cleanup.ts and helpers/audit-helpers.ts, which DO have co-located .test.ts — correct pattern.)
## Notes on vfr-rt-spec-fixes (docs/697 branch, 2026-06-10)

- **Spec cites stale "latest" migration identifier for batch_submit_quiz.** design.md + tasks.md named `20260430000012` as the latest `batch_submit_quiz` body; actual latest is `20260601000001_align_batch_submit_audit_metadata_keys.sql`. Flagged as ISSUE. The "Pre-Flag Verification at implementation time" mitigation language was present, but the stale pointer is still misleading. Pattern: when a spec documents a "latest: <timestamp>" pointer for a function body, verify it is actually the most recent `CREATE OR REPLACE FUNCTION` in that chain before committing.
- **Spec-specified guard message casing doesn't match cited precedent.** design.md mig 103 wrote `'session not found, not owned, or not completed'` (lowercase s) and cited `get_report_correct_options` as the style precedent; actual precedent uses capital 'S'. Flagged as ISSUE. The test regex is `/i` (case-insensitive), so this won't cause a test failure, but the mismatch violates the design's own consistency claim. Pattern: when a spec says "same wording as X", grep X's RAISE string before committing.
- **discard.ts line count off by 1.** tasks.md said 104 lines; actual is 105. Flagged as SUGGESTION. The cap-exceeded message is accurate either way.

## Notes on redteam-e2e-coverage-batch (#784, #786, #788, #781)

- **Spec-count drift in steering + decisions on new-spec batch** — adding 2 new Playwright specs (rpc-record-auth-event.spec.ts + token-refresh-anti-cache.spec.ts) moves count from 37→39. `tech.md` has the count in 3 places (lines 85, 146, 183); `docs/decisions.md` Decision 27 has it once. None updated in the staged diff. This is a SUGGESTION (doc-updater handles it post-commit). The plan explicitly excluded `docs/*.md` schema edits from scope.
- **actor-liveness pre-check pattern confirmed.** rpc-record-auth-event.spec.ts beforeAll asserts all 3 RPC callers (attackerUserId, adminUserId, crossOrgAdminUserId) have `deleted_at IS NULL` before any test runs. This prevents gate-2 (`'user not found or inactive'`) from pre-empting gate-3 assertions (CN/CO/CP). Correct pattern — promote if reused elsewhere.
- **force-token-refresh.ts helper is a Playwright E2E seam, not a `_hooks/` util** — code-style §7 Vitest test requirement does NOT apply to `e2e/redteam/helpers/*.ts`. Do not flag missing co-located test for E2E helper files.
- **CL2 non-vacuity acknowledged in plan as control, not the proof.** CL2 asserts error null + empty array for cross-org user with no sessions. The vacuity concern is pre-acknowledged — CL3 is the non-vacuous ownership proof. Do not re-flag CL2 as vacuous.
- **CT user.deactivated: soft-delete target without `.is('deleted_at', null)` guard is acceptable.** The update at `audit-completeness.spec.ts:678` uses `.eq('id', victimUserId)` without `.is('deleted_at', null)`. If the victim was already soft-deleted from a cross-spec leak, the update succeeds idempotently and `softDeletedUserIds` still restores it. The test checks `!deleted?.length` as an existence guard. This pattern is acceptable — the service-role client doesn't enforce the soft-delete guard, and the afterEach restore uses `.not('deleted_at', 'is', null)` correctly.
- **RAISE-string casing verified for all 4 RPCs.** `record_auth_event` → `'not authenticated'` (lower); `get_session_reports` → `'Not authenticated'` (capital N); `void_internal_exam_code` → `'not_authenticated'` (snake). All anon assertions in new specs use `/not[ _]authenticated/i` or `/not authenticated/i` (case-insensitive) — safe against all three forms.

## Notes on #831 get_vfr_rt_exam_questions org-filter fix

- **NULL-org guard doubles as NULL-user guard — confirmed again.** `SELECT u.organization_id INTO v_caller_org_id ... WHERE u.id = v_caller AND u.deleted_at IS NULL; IF v_caller_org_id IS NULL THEN RAISE 'user_not_found_or_inactive'` preserves the old EXISTS gate exactly: soft-deleted user or unknown user → org returns NULL → guard fires. No behavioral regression. Pattern mirrors mig 099 lines 67–71 identically.
- **questions.organization_id is NOT NULL (verified in initial schema line 107).** The `AND q.organization_id = v_caller_org_id` filter is non-nullable on both sides — no null-equality trap possible.
- **adminUserId2 not pushed to the describe-scope `userIds` — correct by design.** It is explicitly passed as `userIds: [adminUserId2]` in its own `cleanupTestData` call. The first `cleanupTestData` call covers orgId + its own userIds (adminUserId + studentId). No orphan risk.

## Notes on #869 batch_submit_quiz idempotent-replay output-contract test

- **`score_percentage` serialization via jsonb RETURNS is a number, not a string.** `batch_submit_quiz` RETURNS jsonb; PostgREST passes the raw JSON blob. `v_score numeric(5,2)` embedded in `jsonb_build_object` serializes as a JSON number (e.g., `100` for `100.00`). `toBe(100)` and `toBe(0)` are correct strict-equality assertions. Existing spec line 338 (`typeof d.score_percentage === 'number'`) confirms the wire type. Do NOT flag as a BIGINT/NUMERIC string-serialization issue — that pattern applies to column-level reads (SELECT), not to values embedded in a RETURNS jsonb function. APPROVED 2026-06-19.
- **`quick_quiz` leaves `passed` NULL** — confirmed in mig 095c: the pass-mark gating block (L247–257) only runs for `mock_exam` OR `internal_exam`. `v_passed boolean` defaults to NULL, never assigned for `quick_quiz`. Both fixture assertions `toBeNull()` are correct.

## Notes on PR #830 CR-local fixes (5 fixes, vfr_rt_exam race + test hardening)

- **uq_vfr_rt_exam_session_active is the ONLY unique index that can fire on a `mode='vfr_rt_exam'` INSERT.** The three existing partial unique indexes (`uq_active_exam_session` for `mock_exam`, `uq_internal_exam_session_active` for `internal_exam`, `uq_vfr_rt_exam_session_active` for `vfr_rt_exam`) each predicate on a mutually exclusive mode value. PK is `gen_random_uuid()` — no collision. Exception handler is therefore unambiguous: `unique_violation` in the INSERT sub-block can only be from `uq_vfr_rt_exam_session_active`. The RETURN inside EXCEPTION exits the function before the audit INSERT — no fall-through. Sound.
- **Fixture A correct_count = 34 verified from seed.** seedPool: 8 SA questions + 9 dialog_fill questions × 2 blanks each (18 quiz_session_answers rows) + 8 MC questions = 25 questions but 34 answer rows. Fixture A answers every blank with the canonical → all 34 rows `is_correct=true`. Fixture B answers SA all correct (8) + MC all correct (8) + all 9 DF questions with 'WRONG_XYZ' for BOTH blanks (18 wrong rows, 0 correct) = 16 correct_count. Pinned values match seeded data.
- **`v_resume record` type is safe for EXCEPTION re-read SELECT.** `v_resume` is declared as `record` in DECLARE (line 35 of mig 099). The EXCEPTION block SELECTs `qs.id, qs.config, qs.time_limit_seconds, qs.started_at INTO v_resume` — same 4 columns used in the RETURN. The happy-path step-5 idempotent-resume uses the SAME `v_resume` record (line 114+) with the same 4 fields. Type-safe; columns match existing use.
- **23505 test is hermetic post-cleanup.** Cleanup soft-deletes by `student_id + organization_id + subject_id + mode='vfr_rt_exam' + IS NULL ended_at + IS NULL deleted_at`. Both the directly-inserted first row AND any second-row attempt are soft-deleted in the finally block. The happy-path test after it calls the RPC which triggers its own fresh INSERT — not blocked by lingering active sessions.

## Notes on #326 attack-surface matrix registration (orphaned specs bookkeeping)

- **Bookkeeping-only diff is comment/title changes only — no assertion/logic changes.** The 3 spec files had ONLY `+` lines touching `/** ... */` doc comments, `test.describe(...)` titles, and `test(...)` titles. Zero assertion or logic changes confirmed by reading every `+` line in the diff.
- **Old V/BE–BI labels correctly left in their original matrix rows.** Those rows reference sibling specs (consent-gate.spec.ts V, quiz-draft-injection.spec.ts BI, rpc-cross-tenant-reports.spec.ts BE, session-replay.spec.ts BH, audit-event-forgery.spec.ts BF, session-replay.spec.ts BG) by filename — no edit to existing rows needed. Verified: grep of V/BE/BF/BG/BH/BI in the three renamed spec files returns zero hits post-rename.
- **Migration cite accuracy pattern for bookkeeping PRs.** When matrix Notes cite migration line numbers, the line-number ranges are stated as e.g. "L261-263" (the surrounding context) not exact lines. Verified: actual RAISE lines (262, 266, 288, 307) fall inside the cited ranges. This is acceptable precision for Notes fields — do not flag line-number-range claims unless the RAISE string itself is wrong.

## Notes on #833/#840 semantic-SUGGESTION fixes (mig 105 VOLATILE + cleanup.ts exam_configs + docs)

- **VOLATILE drop confirmed clean.** `STABLE` removed from both mirrors (byte-identical via `cmp`); no TS caller, no integration test asserts volatility category; no `pg_proc.provolatile` assertion anywhere in `__integration__/`. Function is in a not-yet-deployed migration — safe to change before first deploy.
- **`exam_configs` FK order correct.** `exam_configs` references `organizations(id)` — must be deleted BEFORE `organizations`. New line is at position 54, `organizations` delete is at position 56. `exam_config_distributions` is `ON DELETE CASCADE` from `exam_configs`, so no explicit delete needed in cleanup.ts. Confirmed no `exam_config_distributions` entry exists in the file.
- **`deleteOrLog` without `.select('id')` is the established cleanup.ts pattern.** All sibling calls (`questions`, `question_banks`, `quiz_sessions`, `users`, `organizations`) omit `.select('id')` chain — `deleteOrLog` accepts `PromiseLike<{ error }>`. The `.select('id')` + zero-row-log pattern only appears in `cleanupReferenceData` (different contract). New `exam_configs` line correctly matches sibling pattern; do not flag as §5 violation.
- **Mig 094 GRANT confirmed.** `explanation_text` + `explanation_image_url` appear on lines 143–144 of `094_question_type_enum_and_column.sql` GRANT block. The docs claims ("columns remain in the mig 094 column GRANT and are PostgREST-readable") are factually correct.
- **Line 2401 (pre-existing, outside diff) is not inconsistent.** It says "revealed only via `get_vfr_rt_exam_results`" in the context of the VFR RT RPC family, consistent with the updated wording "only here among the VFR RT RPCs". No conflict.

## Notes on #832 verdict parser fix (run-security-auditor.sh)

- **`set -euo pipefail` + non-zero function return in `if` condition is safe.** A bash function returning non-zero inside `if parse_verdict "$VAR"; then` does NOT trigger `set -e` — bash exempts the condition of `if`/`while`/`until`. Both call sites in run-security-auditor.sh are in `if` positions. Do not flag this pattern as a `set -e` hazard.
- **`${1:-}` guard is the correct `set -u` idiom for optional first argument.** Prevents unbound-variable abort when the hook is called with no args (lefthook path). Confirmed safe under `set -u` by live invocation.
- **Bash test scripts should use `set -u` only, NOT `set -e`.** The `|| actual=$?` exit-code capture pattern works correctly only when `set -e` is not active. If `set -e` were active, the `||` would still suppress the abort, but `set -u` only is the right posture for test scripts that intentionally invoke commands expected to fail.
- **Test false-pass risk via wrong path: exit 127 (file-not-found) ≠ expected exit 1 or 0 → FAIL.** If `$HOOK` pointed to a nonexistent file, `bash "$HOOK"` exits 127. Because all expected values are 0 or 1, every case would report FAIL — the test cannot silently pass on a bad hook path.
