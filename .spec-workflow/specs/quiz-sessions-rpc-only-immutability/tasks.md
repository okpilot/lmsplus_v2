# Tasks Document — Quiz Sessions RPC-Only Immutability

> **STATUS: SUPERSEDED (2026-05-28).** This spec proposed a single migration `080_quiz_sessions_full_immutability.sql` consolidating: trigger column extension (R1), `discard_quiz_session` RPC (R2), and write-handshake on 4 completion RPCs (R3). The work was instead split into smaller, separately-shipped pieces:
>
> - Mig `079_quiz_sessions_immutable_columns.sql` — initial trigger (10 columns) — **shipped**.
> - Mig `080_start_quiz_session_harden_input.sql` — unrelated hardening; reused the slot — **shipped**.
> - Mig `081_start_quiz_session_mode_whitelist.sql` — closes #629 (Vector BU) — **shipped** (see `redteam-quiz-session-bugs` spec PR 1).
> - The remaining trigger-column extension (R1, the 5 mutable score columns) is now tracked under `redteam-quiz-session-bugs` PR 2 / **issue #611** as mig `082_quiz_sessions_immutable_score_columns.sql` — **pending**.
> - The `discard_quiz_session` RPC + write-handshake (R2/R3) are not currently planned — `discard.ts` Server Action remains a direct UPDATE under RLS. Revisit only if a new red-team vector reopens the case.
>
> Tasks 1–8 below are NOT to be executed. See `redteam-quiz-session-bugs/tasks.md` for the live successor work.

- [ ] 1. Author migration `080_quiz_sessions_full_immutability.sql` (numbered) and mirror as `20260505000001_quiz_sessions_full_immutability.sql` (timestamped)
  - Files: `packages/db/migrations/080_quiz_sessions_full_immutability.sql`, `supabase/migrations/20260505000001_quiz_sessions_full_immutability.sql`
  - Sections in execution order: (1) `DROP TRIGGER trg_quiz_sessions_immutable_columns ON quiz_sessions;` (2) `CREATE OR REPLACE FUNCTION quiz_sessions_protect_immutable_columns()` with handshake bypass at top, service-role bypass second, then 15 column checks (10 existing verbatim + 5 new for `ended_at`, `correct_count`, `score_percentage`, `passed`, `deleted_at` — RAISE messages end "is RPC-only after row creation"); (3) re-`CREATE TRIGGER trg_quiz_sessions_immutable_columns BEFORE UPDATE OF <15 cols>` and `COMMENT ON TRIGGER` updated to cite #611; (4) `DROP POLICY students_update_sessions ON quiz_sessions;` (no replacement); (5) `CREATE OR REPLACE FUNCTION discard_quiz_session(p_session_id uuid) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = public` with the body sequence from design.md §"Component 3"; (6) `GRANT EXECUTE ON FUNCTION discard_quiz_session(uuid) TO authenticated;` (7) `CREATE OR REPLACE FUNCTION` re-emit each of `complete_quiz_session`, `complete_empty_exam_session`, `complete_overdue_exam_session`, `batch_submit_quiz` with the LATEST body verbatim, inserting one new line `PERFORM set_config('app.quiz_sessions_writable', 'true', true);` immediately before the existing `UPDATE quiz_sessions ...` statement and ZERO other changes. The two files must be byte-identical other than the filename comment header.
  - Purpose: Single migration applying all schema/function changes for the RPC-only architecture
  - _Leverage: `packages/db/migrations/079_quiz_sessions_immutable_columns.sql` (trigger blueprint), `packages/db/migrations/049_complete_empty_exam_session.sql` + revisions in `055` (RPC blueprint), `packages/db/migrations/063_extend_overdue_for_internal_exam.sql` (audit-events INSERT pattern with `actor_role` subquery filtering `deleted_at IS NULL` per security.md rule 10)_
  - _Requirements: R1 (block all client UPDATEs), R2 (discard_quiz_session RPC), R3 (handshake on legitimate RPCs)_
  - _Prompt: Implement the task for spec quiz-sessions-rpc-only-immutability, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Senior PostgreSQL/plpgsql developer | Task: Author the dual-source migration files implementing the RPC-only immutability architecture per design.md §"Component 1" and §"Component 2" and §"Component 3", grounded in the existing trigger pattern at packages/db/migrations/079_quiz_sessions_immutable_columns.sql and the SECURITY DEFINER RPC blueprint at packages/db/migrations/049_complete_empty_exam_session.sql. The 4 RPCs to be patched (complete_quiz_session, complete_empty_exam_session, complete_overdue_exam_session, batch_submit_quiz) must each be located via the FULL Pre-Flag Verification rule (.claude/rules/agent-critic.md): trace BOTH packages/db/migrations/*.sql AND supabase/migrations/*.sql for every CREATE OR REPLACE FUNCTION of the named function, take whichever has the highest timestamp/number, and copy that body verbatim. Specifically (verified by plan-critic): batch_submit_quiz LATEST = packages/db/migrations/078_active_user_gate_batch_submit.sql; complete_overdue_exam_session and complete_empty_exam_session LATEST = packages/db/migrations/063_extend_overdue_for_internal_exam.sql; complete_quiz_session LATEST = supabase/migrations/20260406000004_populate_last_active_at.sql (un-mirrored to packages/db; this file IS the source of truth for that function and includes a `last_active_at` stamp on users that MUST be preserved verbatim in mig 080). After locating each, copy the body verbatim into mig 080 with one new line `PERFORM set_config('app.quiz_sessions_writable', 'true', true);` inserted immediately before each UPDATE quiz_sessions statement, and zero other changes. Note on Postgres semantics (clarification, not behavior change): `set_config(name, value, true)` — third arg = `is_local` (transaction-scoped, clears on COMMIT/ROLLBACK). `current_setting(name, true)` — second arg = `missing_ok` (returns NULL when var is unset; NULL = 'true' is NULL-falsy so unset correctly evaluates to bypass-not-granted). Both are intentional. | Restrictions: Do NOT modify migration 079 — create new migration. Do NOT alter any column or behavior of the 4 patched RPCs other than inserting the handshake. Do NOT introduce SECURITY DEFINER on the trigger function. Do NOT use session-level set_config (must pass `true` for LOCAL = transaction-scoped). Do NOT add features beyond the 7 sections listed; spec scope is exact. The audit-event INSERT inside discard_quiz_session MUST filter `WHERE id = v_student_id AND deleted_at IS NULL` on the actor_role subquery (security.md rule 10). For complete_quiz_session specifically, do NOT regress the `UPDATE users SET last_active_at = now()` stamp introduced by 20260406000004 — copy it verbatim. | Success: Migration parses and applies cleanly; trigger column list = 15 columns; trigger function body has handshake bypass on line 1 of the IF chain; discard_quiz_session has SECURITY DEFINER + auth.uid() check + SET search_path = public + soft-delete filter on users SELECT + idempotent return on already-discarded; 4 patched RPCs each have exactly one new PERFORM set_config line and no other diff vs their prior latest definition (specifically: complete_quiz_session retains the last_active_at stamp from 20260406000004). Set the task to in-progress in tasks.md before starting; log via log-implementation tool after completion; mark complete only after success._

- [ ] 2. Apply migration locally, regenerate TypeScript types, run pre-existing test suites
  - Files: `packages/db/src/types.ts` (regenerated)
  - Operations: `npx supabase db reset` (clean apply of all migrations including 080) → `npx supabase gen types typescript --linked > packages/db/src/types.ts` → `pnpm test` (all unit tests must still pass; expect discard.test.ts to fail until task 4 lands; that's acceptable for this task) → `pnpm check-types` (no TS errors after type regen) → spot-check that `Database['public']['Functions']['discard_quiz_session']` exists in the regenerated types
  - Purpose: Verify the migration applies and the new RPC is reachable from typed client code; baseline type regen so subsequent tasks can use the typed RPC name
  - _Leverage: existing `pnpm` scripts in package.json; existing `supabase` CLI configuration_
  - _Requirements: R3 (functions accessible to authenticated callers)_
  - _Prompt: Implement the task for spec quiz-sessions-rpc-only-immutability, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend operations engineer | Task: Apply migration 080 locally via `npx supabase db reset` and regenerate `packages/db/src/types.ts` via `npx supabase gen types typescript --linked`, then run `pnpm test` and `pnpm check-types` to confirm baseline before the application-layer refactor begins. Discard tests are expected to fail at this point (mocks don't yet match new RPC shape) — note the failure count, do NOT modify discard.test.ts in this task. | Restrictions: Do NOT edit production code. Do NOT modify discard.test.ts. Do NOT commit; this task only verifies migration applies. | Success: `supabase db reset` exits 0; types.ts contains `discard_quiz_session` with `Args: { p_session_id: string }` and a `Returns` shape; `pnpm check-types` exits 0; `pnpm test` failures (if any) are limited to discard.test.ts. Set task in-progress before; log with log-implementation; mark complete only after verification._

- [ ] 3. Refactor `discardQuiz` Server Action to call the new RPC
  - File: `apps/web/app/app/quiz/actions/discard.ts`
  - Replace the current pre-fetch + `.from().update()` + optional `.from().delete()` chain with a single `supabase.rpc('discard_quiz_session', { p_session_id })` call. Simplify Zod schema: remove `draftId` (RPC handles draft cleanup via `quiz_drafts.UNIQUE(student_id)`). Map RPC error codes to existing user-visible strings per design.md "Error mapping" table. Preserve auth check + try/catch + console.error pattern (code-style.md §5).
  - Purpose: Make the Server Action a thin wrapper around the RPC; remove all direct PostgREST writes against `quiz_sessions` and `quiz_drafts` from app code
  - _Leverage: existing `apps/web/app/app/quiz/actions/discard.ts` for auth + Zod + error-handling skeleton; `apps/web/app/app/quiz/actions/check_answer.ts` (or another RPC-calling Server Action) as the `.rpc()` reference pattern_
  - _Requirements: R4 (Server Action calls RPC); R4.3 (error mapping); R4.4 (sanitized error messages)_
  - _Prompt: Implement the task for spec quiz-sessions-rpc-only-immutability, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Next.js full-stack developer | Task: Refactor apps/web/app/app/quiz/actions/discard.ts per design.md §"Component 4". The Server Action becomes a thin wrapper: auth check → Zod parse `{ sessionId }` → `supabase.rpc('discard_quiz_session', { p_session_id: sessionId })` → error mapping per design table → return. Preserve the function signature `discardQuiz(raw: unknown)` and the success/failure return shape so callers (discardQuiz.test.ts and any UI components) need no signature change. | Restrictions: Do NOT add any direct PostgREST UPDATE or DELETE on quiz_sessions or quiz_drafts. Do NOT change the function signature. Do NOT change user-facing error string `'cannot_discard_internal_exam'` (existing UI/test contract). Do NOT remove the auth check (defense in depth — RPC also checks). Do NOT use any types — narrow with `unknown` and Zod per CLAUDE.md NEVER DO. The error mapping must read the RPC's raised exception MESSAGE field, not just `error.code` — Postgres RAISE 'foo' surfaces as error.message in supabase-js. | Success: discard.ts is ≤ 60 lines; uses `supabase.rpc('discard_quiz_session', ...)` exactly once; preserves the four user-facing error strings from the design's mapping table; type-check passes; biome passes. Set task in-progress; log with log-implementation; mark complete only after success._

- [ ] 4. Update `discard.test.ts` to mock the RPC instead of the UPDATE/DELETE chain
  - File: `apps/web/app/app/quiz/actions/discard.test.ts`
  - Switch mocks from `vi.hoisted` `.from().update()` chain to `.rpc()`. Cover the 7 test cases enumerated in design.md §"Unit Testing" (auth fail, Zod fail, RPC success non-idempotent, RPC success idempotent, internal_exam_not_discardable, not_authorized, generic error with console.error). Test names follow the behavior-first rule (code-style.md §7) — describe what the user observes, not the implementation method.
  - Purpose: Tests align with the new RPC contract; existing UI/integration assertions still hold
  - _Leverage: existing `apps/web/app/app/quiz/actions/discard.test.ts` for the test skeleton; project's `vi.hoisted` + `buildChain` mock pattern documented in `.claude/agent-memory/test-writer/patterns.md`; another Server Action test that calls `.rpc()` (e.g., `apps/web/app/app/quiz/actions/check_answer.test.ts` if it exists)_
  - _Requirements: R4 (Server Action behavior); design §"Unit Testing"_
  - _Prompt: Implement the task for spec quiz-sessions-rpc-only-immutability, first run spec-workflow-guide to get the workflow guide then implement the task: Role: TypeScript test engineer (Vitest) | Task: REPLACE the existing test suite in apps/web/app/app/quiz/actions/discard.test.ts entirely with the 7 new cases from design.md §"Unit Testing" plus a regression case for mock_exam (see below). Mock supabase.rpc('discard_quiz_session', ...) per the patterns in .claude/agent-memory/test-writer/patterns.md. Test titles must describe user-observable behavior. The existing file has tests at lines ~70, ~119, ~202, ~264 that depend on `draftId` being in the Zod schema and on the direct `quiz_drafts.delete()` chain — both of those behaviors are removed by Task 3 and the tests CANNOT be carried over. Specifically REMOVE the existing tests `'returns invalid-input error when draftId is present but not a UUID'`, `'deletes the draft and returns success when draftId is provided'`, `'uses hard DELETE (not soft-delete) for draft cleanup'`. Replace, do not append. The mock_exam regression intent at line ~119 of the existing file (asserting that mock_exam mode IS discardable, only internal_exam blocks discard) MUST be preserved as a new behavior-first test (e.g., "returns success when a mock_exam session is discarded"). | Restrictions: Do NOT keep any test that asserts on the old `.from('quiz_drafts').delete()` chain or on the `draftId` Zod field. Do NOT test implementation details (function names, RPC arg shapes — those are contract details that belong in test bodies via `.toHaveBeenCalledWith`, not in `it(...)` titles per code-style.md §7 disallowed patterns). Do NOT add e2e or integration tests here — that lives in task 5. Do NOT mock the trigger or migration — only the supabase client. | Success: 7 design-mandated cases + 1 mock_exam regression case = 8 tests total, all pass via `pnpm --filter @repo/web test discard.test.ts`; no test asserts `draftId` or `quiz_drafts.delete()`; test titles pass the behavior-first lint rule from code-style.md §7; vi.resetAllMocks() in beforeEach. Set task in-progress; log with log-implementation; mark complete only after success._

- [ ] 5. Add red-team spec covering Vectors BL / BM / BN
  - File: `apps/web/e2e/redteam/quiz-session-mutable-columns.spec.ts` (NEW)
  - Mirror the structure of `apps/web/e2e/redteam/quiz-session-config-injection.spec.ts`. Single `test.describe`, `beforeEach` seeds attacker user + active session + captures original frozen snapshot via admin client, three attack tests (BL: `correct_count` + `ended_at` forge; BM: `passed = true` forge; BN: `score_percentage = 100` forge), each asserts the UPDATE is rejected and the row is unchanged via deep-equal. `afterEach` cleans up via admin (per the e2e hermiticity rule, code-style.md §7). Reuse `seedRedTeamUsers`, `ensureExamConfig`, `createAuthenticatedClient`, `getAdminClient` helpers already present in the redteam folder.
  - Purpose: Defense-in-depth coverage proving the trigger blocks the three forgery vectors at the DB layer
  - _Leverage: `apps/web/e2e/redteam/quiz-session-config-injection.spec.ts` (template); `apps/web/e2e/redteam/helpers/seed.ts`, `apps/web/e2e/redteam/helpers/redteam-client.ts`, `apps/web/e2e/helpers/supabase.ts` (existing helpers)_
  - _Requirements: R5 (red-team coverage)_
  - _Prompt: Implement the task for spec quiz-sessions-rpc-only-immutability, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Security/Playwright engineer | Task: Author apps/web/e2e/redteam/quiz-session-mutable-columns.spec.ts mirroring the LIFECYCLE pattern of apps/web/e2e/redteam/quiz-session-config-injection.spec.ts (beforeEach/afterEach, attacker client setup, deep-equal helper). Three attack cases per design.md §"End-to-End Testing". | CRITICAL DEVIATION FROM SIBLING SPEC: do NOT copy the sibling's `FROZEN_COLUMNS_SELECT` constant — its 10-column scope (`config, total_questions, mode, time_limit_seconds, started_at, organization_id, student_id, subject_id, topic_id, created_at`) does NOT include the BL/BM/BN attack columns. The new spec MUST define its own selector covering all 15 frozen columns OR at minimum the 5 new ones (`correct_count`, `ended_at`, `passed`, `score_percentage`, `deleted_at`). A 10-column selector would produce false-green deep-equal results because the actual attack columns aren't in the snapshot. Recommended: define `FROZEN_COLUMNS_SELECT_NEW = 'correct_count, ended_at, passed, score_percentage, deleted_at'` (or all 15) in the new file. The local `assertSessionStillLocked` helper deep-equals using whichever selector this spec uses, so the selector choice IS the coverage. | Restrictions: Do NOT promote assertSessionStillLocked into a shared helper module in this task — out of scope, file as a SUGGESTION issue if desired. Do NOT skip the afterEach cleanup (e2e hermiticity rule, code-style.md §7). Do NOT use admin client to write the attacker's session — start it via the legitimate `start_exam_session` flow as in the sibling spec. Do NOT mock anything in Playwright — these run against a real local Supabase. Do NOT reuse the sibling spec's 10-column FROZEN_COLUMNS_SELECT — that is a documented false-green risk for THIS spec's attacks. | Success: `pnpm --filter @repo/web e2e:redteam` runs and all three new attacks pass; the existing config-injection spec still passes (no regression); the new spec's deep-equal selector includes at minimum the 5 attack-target columns. Set task in-progress; log with log-implementation; mark complete only after success._

- [ ] 6. Update `docs/database.md` for new immutability state, RPC summary, and soft-delete matrix
  - File: `docs/database.md`
  - Update §3 immutability matrix `quiz_sessions` row: 15 frozen, 0 mutable, dual mig IDs (`079` / `20260502000001` and `080` / `20260505000001`). Add `discard_quiz_session` row to the RPC summary table, citing the new mig. Update the soft-delete matrix entry for `quiz_sessions` to note that soft-delete is now via `discard_quiz_session` RPC (not direct client UPDATE). Update any prose paragraphs that reference the old `students_update_sessions` policy (now dropped) — find via `grep -n "students_update_sessions" docs/database.md`. Update the Cross-Reference Audit Rule scope: any structural cross-reference added must trigger a re-audit of the entire affected section per `.claude/rules/agent-doc-updater.md`.
  - Purpose: Doc reflects current schema state; future readers don't reintroduce a vulnerability via stale docs
  - _Leverage: `docs/database.md` current state; `.claude/rules/agent-doc-updater.md` Cross-Reference Audit Rule; the new migration 080 as source of truth_
  - _Requirements: R6.1, R6.2, R6.3_
  - _Prompt: Implement the task for spec quiz-sessions-rpc-only-immutability, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Technical writer with database expertise | Task: Update docs/database.md per design.md §"Documentation" requirements. Update §3 immutability matrix quiz_sessions row to reflect 15 frozen / 0 mutable + dual mig IDs. Add discard_quiz_session to the RPC summary index. Update soft-delete matrix entry. Apply the Cross-Reference Audit Rule (.claude/rules/agent-doc-updater.md) — read the entire affected section, scan summary tables, and update any prose that references the dropped students_update_sessions policy. | Restrictions: Do NOT add forward-looking statements ("we might…", "in the future…") per .claude/rules/agent-doc-updater.md NEVER. Do NOT edit other unrelated sections. Do NOT remove existing rows; only add the discard_quiz_session row and update the quiz_sessions row. The dual-mig-ID format must follow the convention set by PR #612 / commit 52d6c80 (`079` / `20260502000001` style). | Success: `grep -n "students_update_sessions" docs/database.md` returns no results OR returns only documented historical context (e.g., "(removed in mig 080)"); the immutability matrix row for quiz_sessions shows 15 columns frozen and cites both migration pairs; the RPC summary table has the discard row. Set task in-progress; log with log-implementation; mark complete only after success._

- [ ] 7. Update `docs/security.md` and `docs/decisions.md` for the new pattern
  - Files: `docs/security.md`, `docs/decisions.md`
  - In `docs/security.md`: add a new subsection (or extend an existing one) documenting the session-variable trigger handshake pattern as the canonical "RPC writes through immutability trigger" mechanism. State the three rules: (a) the bypass uses LOCAL set_config (transaction-scoped); (b) the trigger preserves the service-role bypass for admin client; (c) any new RPC writing the protected table must include the `PERFORM set_config(...)` line immediately before its UPDATE. In `docs/decisions.md`: add a new architectural decision entry titled "quiz_sessions writes are RPC-only via SECURITY DEFINER + session-variable trigger handshake" with date `2026-05-05`, citing issue #611, summarizing the rationale (defense in depth, exam-score forgery prevention, Vectors BL/BM/BN closure).
  - Purpose: Establish the handshake pattern as documented project convention so future contributors apply it consistently when extending immutability protection to other tables
  - _Leverage: `docs/security.md` existing §15 (SECURITY DEFINER soft-delete carve-out) for tone and structure; `docs/decisions.md` existing entries for format_
  - _Requirements: R6.4 (security.md handshake pattern doc); R6.5 (decisions.md entry)_
  - _Prompt: Implement the task for spec quiz-sessions-rpc-only-immutability, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Technical writer with security/architecture focus | Task: Update docs/security.md to document the session-variable trigger handshake pattern as a new project-wide pattern, and add an entry to docs/decisions.md recording the architectural decision per design.md §"Steering Document Alignment". | Restrictions: Do NOT edit .spec-workflow/steering/*.md — that requires the spec-workflow MCP approval flow per .claude/rules/agent-doc-updater.md (deferred to a separate ticket if drift is found, similar to issue #553). Do NOT add speculative content. Keep entries terse and scannable. | Success: docs/security.md has a clear subsection naming the pattern, the LOCAL flag, the service-role bypass preservation, and the requirement that new RPCs include the PERFORM line; docs/decisions.md has a dated entry citing #611. Set task in-progress; log with log-implementation; mark complete only after success._

- [ ] 8. Final pre-commit verification: run all checks locally
  - Operations: `pnpm test` (all green) → `pnpm check-types` (zero errors) → `pnpm check` (biome lint + format) → `pnpm --filter @repo/web e2e:redteam` (red-team pass, including new spec) → `pnpm --filter @repo/web e2e` (full E2E pass — verifies handshake didn't break legitimate completion paths)
  - Purpose: Final gate before commit; mirrors the CI suite that will run on push
  - _Leverage: existing pnpm scripts; lefthook pre-commit hook (will also run automatically on commit)_
  - _Requirements: All — final integration check_
  - _Prompt: Implement the task for spec quiz-sessions-rpc-only-immutability, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Build/release engineer | Task: Run the full pre-commit verification suite. If anything fails, identify the failing test/check, surface it to the orchestrator (do NOT fix it — fixing belongs in the originating task and may need a critic round). | Restrictions: Do NOT push, commit, or otherwise mutate git state. Do NOT modify any source file in this task — verification only. Do NOT skip any check, even if previous tasks claimed green. Do NOT run `pnpm dev` or any long-running watch process. | Success: All 5 commands (pnpm test, pnpm check-types, pnpm check, pnpm --filter @repo/web e2e:redteam, pnpm --filter @repo/web e2e) exit 0 with no test failures and no lint errors. Set task in-progress; log with log-implementation; mark complete only after all checks green._
