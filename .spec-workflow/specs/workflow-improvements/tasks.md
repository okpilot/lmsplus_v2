# Tasks Document

- [x] 1. Create plan-critic agent definition and critic handling rules
  - File: `.claude/agents/plan-critic.md`, `.claude/rules/agent-critic.md`
  - Create the plan-critic agent definition following the existing agent format (YAML frontmatter with name/description/model, then markdown with mission, inputs, checks, output format, DO NOT, memory sections). Model: sonnet. Create the critic handling rules file with header (model/trigger/blocking info), Purpose, Severity Levels table (reusing CRITICAL/ISSUE/SUGGESTION/GOOD from semantic-reviewer), DO/NEVER sections covering: plan-critic findings block execution at ISSUE/CRITICAL, revision cap of 1 round, single-file under 10 lines skips plan-critic, pre-commit critics do not replace post-commit agents.
  - Purpose: Establish the plan-critic agent that reviews validated plans against the codebase before execution, catching wrong assumptions and pattern violations at the cheapest possible point (R1 acceptance criteria 1-3, 7-8).
  - _Leverage: `.claude/agents/code-reviewer.md` (format template), `.claude/rules/agent-semantic-reviewer.md` (severity levels to reuse), design.md R1 section (output format spec, skip conditions, revision cap)_
  - _Requirements: R1_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Agent definition author | Task: Create two files. (1) `.claude/agents/plan-critic.md` -- a plan-critic agent definition using the exact format from `.claude/agents/code-reviewer.md` (YAML frontmatter: name, description, model claude-sonnet-4-20250514). Mission: review validated plans against the codebase before execution. Inputs: validated plan text + source files from plan's "Files to change" and "Files affected". Checks: wrong assumptions about function signatures/return types, missed callers/importers, incorrect fallback values, pattern violations vs codebase conventions, security surface gaps. Output format: use the PLAN-CRITIC REVIEW template from design.md. DO NOT: modify the plan itself, execute code, check style (that's code-reviewer's job), run for single-file changes under 10 lines. (2) `.claude/rules/agent-critic.md` -- handling rules for both plan-critic and implementation-critic. Header: "Model: sonnet | Trigger: pre-commit | Blocking: on CRITICAL/ISSUE". Purpose section. Severity Levels table reusing CRITICAL/ISSUE/SUGGESTION/GOOD from agent-semantic-reviewer.md (do not redefine, reference). Handling Results with DO/NEVER sections. DO: run plan-critic on every multi-file plan, fix all ISSUE/CRITICAL before proceeding, respect 1-round revision cap for plan-critic, respect 2-round cap for implementation-critic, treat SUGGESTION as non-blocking. NEVER: skip implementation-critic even for small changes, loop more than the revision cap, let critics modify code or plans directly, replace post-commit agents with pre-commit critics. | Restrictions: Follow existing file format exactly, do not invent new severity levels, do not duplicate content from agent-semantic-reviewer.md | Success: Both files created, format matches existing agents, severity levels reference (not duplicate) semantic-reviewer, revision caps documented, skip conditions documented_

- [x] 2. Create implementation-critic agent definition
  - File: `.claude/agents/implementation-critic.md`
  - Create the implementation-critic agent definition following the existing agent format. Model: sonnet. Mission: review staged changes (pre-commit) against the validated plan and requirements. Inputs: `git diff --staged`, validated plan, requirements. Output format uses the IMPLEMENTATION REVIEW template from design.md. Revision flow: ISSUE triggers implementer revision (max 2 rounds), CRITICAL triggers orchestrator intervention. No skip condition -- even single-file changes get implementation review.
  - Purpose: Establish the implementation-critic agent that catches logic errors, missed requirements, and plan deviations before commit (R1 acceptance criteria 4-6).
  - _Leverage: `.claude/agents/code-reviewer.md` (format template), `.claude/rules/agent-critic.md` (created in task 1, references handling rules), design.md R1 implementation-critic section (output format, revision flow)_
  - _Requirements: R1_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Agent definition author | Task: Create `.claude/agents/implementation-critic.md` using the exact format from `.claude/agents/code-reviewer.md` (YAML frontmatter: name, description, model claude-sonnet-4-20250514). Mission: review staged changes against the validated plan and requirements before commit. Inputs: `git diff --staged`, the validated plan, requirements (from spec if exists, or plan output). Checks: deviations from approved plan (wrong fallback values, missing error paths, changed function signatures), logic errors (off-by-one, missing null checks, wrong comparison operators), missed requirements (plan items not implemented), pattern violations (diverges from how similar code is written elsewhere). Output format: use the IMPLEMENTATION REVIEW template from design.md with severity counts header, findings with severity/file/line/suggestion, and verdict. DO NOT: modify code directly, check style (code-reviewer's job), review files outside the staged diff, run tests (test-writer's job). Memory: `.claude/agent-memory/implementation-critic/patterns.md`. | Restrictions: Follow existing agent file format exactly, no skip condition (this agent always runs), reference `.claude/rules/agent-critic.md` for handling rules | Success: File created, format matches existing agents, inputs clearly specified, output template matches design.md, revision flow documented (ISSUE -> implementer revises, CRITICAL -> orchestrator intervenes, 2-round cap)_

- [x] 3. Add critic integration steps to agent-workflow.md
  - File: `.claude/rules/agent-workflow.md`
  - Insert two new sections: (1) "Plan-Critic Review" between the "Validated plan" step and "User approves" in the Plan Validation Pipeline section -- add to the pipeline diagram, describe the review step, revision loop, and skip condition. (2) "Pre-Commit Implementation Review" between "Execute" and "git commit" in the Post-Implementation Pipeline -- update the pipeline diagram, describe the review step, revision flow with 2-round cap, and orchestrator escalation. Update DO/NEVER lists in both pipeline sections with critic-related entries.
  - Purpose: Wire the critic agents into the existing orchestrator pipeline so they run at the correct points (R1 acceptance criteria 1-8).
  - _Leverage: `.claude/rules/agent-workflow.md` (existing pipeline diagrams and DO/NEVER lists to extend), `.claude/rules/agent-critic.md` (created in task 1, referenced for handling rules), design.md updated workflow pipeline and R1 section_
  - _Requirements: R1_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Workflow rule author | Task: Modify `.claude/rules/agent-workflow.md` to add critic integration. Two changes: (1) In the Plan Validation Pipeline section, after the "Validated plan" line in the ASCII pipeline diagram, insert a "Plan-critic review" step before "User approves". Add a narrative subsection "Plan-Critic Review" describing: when it runs (after plan validation, before user approval), what it receives (validated plan + source files), revision loop (1 round max -- if revised plan still has ISSUE/CRITICAL, orchestrator resolves directly), skip condition (single-file changes under 10 lines), timeout handling (proceed with warning if over 60s for 10 or fewer files, 120s for more than 10 files). Add to the Plan Validation DO list: "Run plan-critic on every multi-file plan before user approval". Add to NEVER: "Skip plan-critic for multi-file changes" and "Proceed to execution with unresolved plan-critic ISSUE/CRITICAL findings". (2) In the Post-Implementation Pipeline section, update the ASCII diagram to show an "Implementation-critic review" step between "Execute (subagents implement)" and "git commit". Add a narrative subsection "Pre-Commit Implementation Review" describing: when it runs (after subagent implementation, before git commit), what it receives (git diff --staged, validated plan, requirements), revision flow (ISSUE -> implementer revises, CRITICAL -> orchestrator intervenes, 2-round cap then orchestrator takes over), no skip condition, timeout (proceed with warning if over 90s for under 500 line diff). Add to Orchestrator Role DO: "Run implementation-critic on staged changes before every commit". Add to NEVER: "Skip implementation-critic, even for small changes" and "Allow more than 2 revision rounds between critic and implementer". | Restrictions: Do not rewrite existing sections -- insert new content at the correct positions, preserve all existing pipeline steps and DO/NEVER entries, keep ASCII diagram style consistent | Success: Both pipeline diagrams updated, narrative subsections added, DO/NEVER lists extended, skip conditions and timeout handling documented_

- [x] 4. Add spec usage rules to agent-workflow.md
  - File: `.claude/rules/agent-workflow.md`
  - Add a new section "Spec Artifact Rules" after the Plan Validation Pipeline section. Content: when to create a spec (3+ files or new architectural pattern), when NOT to (bug fixes, single-file, under 3 files), spec lifecycle (created during planning, updated during implementation, committed with branch), spec-as-context rule (spec is source of truth, not chat history), deviation rule (approved specs require explicit update for material changes), MCP fallback (manual file creation if spec-workflow MCP unavailable).
  - Purpose: Document when and how to use the spec-workflow MCP for persistent planning artifacts (R2 acceptance criteria 1-6).
  - _Leverage: `.claude/rules/agent-workflow.md` (insertion point after Plan Validation Pipeline), design.md R2 section (full content specification), `.spec-workflow/templates/` (existing template structure for fallback reference)_
  - _Requirements: R2_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Workflow rule author | Task: Modify `.claude/rules/agent-workflow.md` to add a new section "Spec Artifact Rules" after the Plan Validation Pipeline section (before the "Post-Implementation Pipeline Order" section). Content from design.md R2: (1) "When to create a spec" -- features spanning 3+ files OR introducing a new architectural pattern. Created via spec-workflow MCP tools. (2) "When NOT to create a spec" -- bug fixes, single-file refactors, changes under 3 files. Existing plan validation pipeline is sufficient. (3) "Spec lifecycle" -- created during planning -> updated during implementation -> committed with feature branch -> serves as session resume context. (4) "Spec-as-context rule" -- when a spec exists, the orchestrator references it (not chat history) as source of truth for requirements and plan. (5) "Deviation rule" -- after spec reaches "approved" status, material approach changes require updating the spec and noting the deviation. (6) "MCP fallback" -- if spec-workflow MCP is unavailable, write spec files manually to `.spec-workflow/specs/<name>/` using the template structure. Add a DO entry: "Create a spec for any feature spanning 3+ files or introducing a new pattern". Add a NEVER entry: "Make material changes to an approved spec's approach without updating the spec". | Restrictions: Insert as a new section, do not modify existing sections, keep formatting consistent with the rest of the file | Success: New section added with all 6 rules, DO/NEVER entries added, threshold (3 files) clearly stated, fallback documented_

- [x] 5. Add interview phase to agent-workflow.md
  - File: `.claude/rules/agent-workflow.md`
  - Insert a new step "Requirement Interview" in the Plan Validation Pipeline between "Root cause check" and "Draft plan". Include the interview template (3 question categories: scope boundaries, behavioral ambiguities, priority trade-offs), auto-skip conditions (single-file bug fix with clear root cause, user says "skip interview", zero ambiguities identified), answer incorporation rule (answers feed into plan draft and spec if one exists), default behavior (on for multi-file changes, skippable). Update the pipeline diagram.
  - Purpose: Surface requirement ambiguities as explicit questions before implementation, preventing misunderstandings discovered in review (R3 acceptance criteria 1-5).
  - _Leverage: `.claude/rules/agent-workflow.md` (pipeline diagram and Root cause check section as insertion point), design.md R3 section (interview template, auto-skip conditions, answer incorporation rules)_
  - _Requirements: R3_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Workflow rule author | Task: Modify `.claude/rules/agent-workflow.md` to add a "Requirement Interview" step. In the Plan Validation Pipeline ASCII diagram, insert between "Root cause check" and "Draft plan": "Requirement interview (if multi-file)". Add a new subsection "Requirement Interview (runs AFTER root cause check)" with: (1) Interview template -- 3-5 questions covering: Scope boundaries ("Is X in scope or out of scope?"), Behavioral ambiguities ("When Y happens, should the system do A or B?"), Priority trade-offs ("The full solution involves X, Y, Z. Are all must-have, or can Z be deferred?"). (2) Auto-skip conditions (interview skipped when ANY apply): single-file bug fix with clear reproduction path and single root cause; user explicitly says "skip interview"; orchestrator identifies zero ambiguities (must state "No ambiguities identified" and proceed). (3) Answer incorporation: answers feed into plan draft; if a spec exists (R2), answers recorded in spec requirements section. (4) Default behavior: on for multi-file changes, skippable but not skipped silently -- orchestrator either asks questions or explicitly states no ambiguities. Add to Plan Validation DO: "Run the interview for every multi-file change unless auto-skip conditions apply". Add to NEVER: "Skip the interview silently -- always state 'No ambiguities identified' or present questions". | Restrictions: Insert into existing pipeline, do not rewrite the root cause check section, keep the interview lightweight (3-5 questions max) | Success: Pipeline diagram updated, interview subsection added with template and skip conditions, DO/NEVER entries added, answers link to spec when one exists_

- [x] 6. Add task persistence rules to agent-workflow.md
  - File: `.claude/rules/agent-workflow.md`
  - Add a new section "Task Persistence" after the Orchestrator Role section. Content: when to create tasks (5+ discrete steps), task lifecycle (pending -> in_progress -> completed via TaskCreate/TaskUpdate), session resume protocol (check TaskList before exploring), completion reporting, threshold (below 5 steps is optional), fallback (session summary if TaskCreate/TaskUpdate unavailable).
  - Purpose: Enable multi-step work tracking that survives session restarts (R4 acceptance criteria 1-5).
  - _Leverage: `.claude/rules/agent-workflow.md` (Orchestrator Role section as insertion point), design.md R4 section (task lifecycle, thresholds, fallback rules)_
  - _Requirements: R4_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Workflow rule author | Task: Modify `.claude/rules/agent-workflow.md` to add a new section "Task Persistence" after the Orchestrator Role section (before the "Proactive Engineering Guidance" section). Content: (1) "When to create tasks" -- features with 5+ discrete implementation steps. Use TaskCreate for each step with clear title. (2) "Task lifecycle" -- pending (created during planning) -> in_progress (set via TaskUpdate when work begins) -> completed (set via TaskUpdate when step passes review). (3) "Session resume protocol" -- when a new session begins and developer asks to resume, run TaskList before exploring the codebase. Outstanding tasks provide starting context. (4) "Completion reporting" -- when all tasks for a feature are completed, report summary to developer. (5) "Threshold" -- below 5 steps, task creation is optional (orchestrator's discretion). (6) "Fallback" -- if TaskCreate/TaskUpdate unavailable, fall back to tracking in session summary. Add to Orchestrator Role DO: "Create tasks via TaskCreate for features with 5+ steps". Add to NEVER: "Start a new session on in-progress work without checking TaskList first". | Restrictions: Insert as a new section, do not modify existing sections, keep rules concise | Success: New section added with lifecycle, threshold, resume protocol, fallback, DO/NEVER entries added_

- [x] 7. Add delegation protocol to agent-workflow.md
  - File: `.claude/rules/agent-workflow.md`
  - Add a new section "Delegation Protocol" after the "Proactive Engineering Guidance" section. Content: the 5-section template (TASK, OBJECTIVE, DONE WHEN, CONSTRAINTS, CONTEXT), litmus test ("Could this agent execute end-to-end without a follow-up question?"), parallel dispatch rule (each prompt self-contained), failure logging format, post-commit agent integration note (existing agent definitions serve as CONSTRAINTS/CONTEXT, template supplements with TASK/OBJECTIVE/DONE WHEN).
  - Purpose: Standardize subagent prompts so agents receive unambiguous instructions and execute without follow-up (R5 acceptance criteria 1-5).
  - _Leverage: `.claude/rules/agent-workflow.md` (Proactive Engineering Guidance section as insertion point), design.md R5 section (template, litmus test, failure log format, post-commit integration)_
  - _Requirements: R5_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Workflow rule author | Task: Modify `.claude/rules/agent-workflow.md` to add a new section "Delegation Protocol" after the "Proactive Engineering Guidance" section (before the closing "Per-agent rules" reference line). Content: (1) Template -- 5 required sections for every subagent prompt: TASK (action verb + scope), OBJECTIVE (why it matters, connects to user's goal), DONE WHEN (measurable exit criteria), CONSTRAINTS (what NOT to do, file boundaries, line limits, security rules), CONTEXT (file paths, type signatures, patterns to follow, related tests). Format as a code block. (2) Litmus test -- before dispatching any subagent: "Could this agent execute end-to-end without a follow-up question?" If no, add missing context. (3) Parallel dispatch rule -- when multiple subagents launch in parallel, each prompt is self-contained, no dependencies on sibling outputs. (4) Failure logging -- if a subagent returns indicating missing context, log as: "DELEGATION FAILURE -- [agent type] -- [timestamp] / Missing: [what] / Fix: [what to include next time]". (5) Post-commit agent integration -- for post-commit agents, existing agent definition files serve as CONSTRAINTS and CONTEXT; the delegation template supplements (TASK, OBJECTIVE, DONE WHEN) but does not duplicate. Add DO: "Use the 5-section delegation template for every subagent prompt" and "Log delegation failures and improve future prompts". Add NEVER: "Dispatch a subagent without all 5 template sections" and "Duplicate agent definition content in delegation prompts". | Restrictions: Insert as a new section, do not modify existing sections, keep the template concise (constructible in under 30 seconds) | Success: New section with template code block, litmus test, parallel rule, failure log format, post-commit integration note, DO/NEVER entries_

- [x] 8. Add steering drift detection to doc-updater
  - File: `.claude/rules/agent-doc-updater.md`, `.claude/agents/doc-updater.md`
  - In agent-doc-updater.md: add a new section "Steering Document Drift Detection" after the "File Rename Protocol" section. Define the DRIFT finding type (non-blocking, reported to orchestrator), severity escalation (CRITICAL if contradicts security rules), what the agent checks (code contradicting steering doc statements, new patterns not documented), what it does NOT do (edit steering docs), orchestrator decision tree (intentional drift -> update doc, unintentional -> fix code), skip condition (no `.spec-workflow/steering/` directory). Add a new row to the Key Documents table for `.spec-workflow/steering/*.md`. In doc-updater.md agent definition: add a new check to the review checklist after existing doc sync checks -- read steering docs, compare diff, report contradictions as DRIFT findings.
  - Purpose: Detect when committed code drifts from steering documents so architectural decisions stay aligned with implementation (R6 acceptance criteria 1-6).
  - _Leverage: `.claude/rules/agent-doc-updater.md` (File Rename Protocol section and Key Documents table as insertion points), `.claude/agents/doc-updater.md` (review checklist to extend), design.md R6 section (DRIFT finding type, severity escalation, decision tree, skip condition)_
  - _Requirements: R6_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Agent rule and definition author | Task: Modify two files for steering drift detection. (1) `.claude/rules/agent-doc-updater.md` -- add section "Steering Document Drift Detection" after "File Rename Protocol" (before the closing line). Content: New finding type DRIFT (non-blocking, reported to orchestrator for decision). Severity escalation: if drift contradicts `docs/security.md` or `.claude/rules/security.md`, elevate to CRITICAL. What agent checks: after normal doc sync, compare commit diff against each file in `.spec-workflow/steering/` (product.md, tech.md, structure.md) for code contradicting steering doc statements and new patterns not documented. What agent does NOT do: edit steering docs (require developer approval). Orchestrator decision tree: intentional drift (code correct, doc outdated) -> update steering doc via spec-workflow MCP; unintentional drift (doc correct, code wrong) -> treat as ISSUE, fix code same session. Skip condition: if `.spec-workflow/steering/` does not exist or is empty, skip without error. Add DO entries: "Report DRIFT findings with specific steering doc reference and contradicting code", "Elevate to CRITICAL when drift contradicts security rules". Add NEVER entries: "Edit steering documents directly", "Skip drift check when steering docs exist". Add row to Key Documents table: `.spec-workflow/steering/*.md` | Code change contradicts a steering doc statement (DRIFT finding, not an update -- doc-updater reports only). (2) `.claude/agents/doc-updater.md` -- after the existing review checklist items, add a new check: "Steering drift check: (a) read each file in `.spec-workflow/steering/` if directory exists, (b) compare commit diff against statements in each steering doc, (c) report contradictions as DRIFT findings with: specific steering doc and section, contradicting code file + line, suggested resolution (update doc or fix code)." | Restrictions: Do not rewrite existing content, insert new sections/items at correct positions, keep DRIFT as non-blocking (except security escalation) | Success: Both files updated, DRIFT finding type defined, security escalation rule documented, decision tree documented, skip condition documented, Key Documents table extended, agent definition has new checklist item_

- [x] 9. Update CLAUDE.md workflow section
  - File: `CLAUDE.md`
  - Update the existing 9-step workflow in the "Your workflow for any non-trivial task" section to reflect all new steps: interview phase (after root cause, before plan), plan-critic (after validation, before user approval), spec creation (for 3+ file changes), implementation-critic (after execution, before commit), task persistence (after post-commit agents). Update the numbered workflow steps. Add references to new agent rule files in the "Per-agent rules" list. Update the "Key docs" section if needed.
  - Purpose: Ensure CLAUDE.md accurately reflects the updated pipeline so all agents and the orchestrator follow the same workflow (R1-R6 integration).
  - _Leverage: `CLAUDE.md` (existing 9-step workflow to update), `.claude/rules/agent-workflow.md` (source of truth for updated pipeline after tasks 3-7), design.md summary of file changes_
  - _Requirements: R1, R2, R3, R4, R5, R6_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Documentation author | Task: Modify `CLAUDE.md` to reflect the updated workflow. Changes: (1) Update the "Your workflow for any non-trivial task" numbered list from 9 steps to include new steps. The updated flow should be: 1. Explore, 2. Root cause check, 3. Interview (R3 -- present clarifying questions or state "no ambiguities"), 4. Create spec if 3+ files (R2), 5. Draft plan, 6. Validate plan, 7. Plan-critic review (R1 -- skip for single-file under 10 lines), 8. User approves, 9. Execute (subagents implement), 10. Implementation-critic review (R1 -- always runs), 11. Commit, 12. Post-commit agents (4 in parallel), 13. Read results + fix, 14. Update task status (R4), 15. Learner. (2) In the "Per-agent rules" bullet list under "Key docs", add references to `.claude/rules/agent-critic.md`. (3) In the "Post-commit review" section, add a note that pre-commit critics (plan-critic, implementation-critic) run BEFORE commit and do not replace post-commit agents. (4) In the "When NOT to use subagents" list, add: "Plan-critic and implementation-critic -- these are invoked as part of the pipeline, not as ad-hoc subagents". | Restrictions: Do not rewrite unrelated sections, preserve all existing content, keep the workflow list concise (one line per step), update the "Last updated" comment at the bottom of modified sections | Success: Workflow steps updated to reflect all 6 requirements, new agent rules referenced, pre-commit vs post-commit distinction clear, no existing content lost_

- [x] 10. Verification pass -- read all files end-to-end
  - File: All 7 files modified/created in tasks 1-9
  - Read every file touched by this spec end-to-end. Verify: (1) No contradictions between agent-workflow.md pipeline and CLAUDE.md workflow steps. (2) agent-critic.md handling rules match plan-critic.md and implementation-critic.md definitions (severity levels, revision caps, skip conditions). (3) agent-doc-updater.md drift section matches doc-updater.md agent definition checklist. (4) All cross-references are correct (file paths, section names). (5) No duplicate content between files. (6) Formatting is consistent with existing files (headers, tables, code blocks, DO/NEVER style). Dry-run: mentally walk through a multi-file feature change using the updated pipeline to confirm all steps connect.
  - Purpose: Catch inconsistencies between files before committing, since this spec touches 7 files that must be internally consistent.
  - _Leverage: All files created/modified in tasks 1-9, design.md (source of truth for intended behavior), requirements.md (acceptance criteria to verify against)_
  - _Requirements: R1, R2, R3, R4, R5, R6_
  - _Prompt: Implement the task for spec workflow-improvements, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Quality reviewer | Task: Read all 7 files modified or created by this spec and verify internal consistency. Files to read end-to-end: `.claude/agents/plan-critic.md`, `.claude/agents/implementation-critic.md`, `.claude/rules/agent-critic.md`, `.claude/rules/agent-workflow.md`, `.claude/rules/agent-doc-updater.md`, `.claude/agents/doc-updater.md`, `CLAUDE.md`. Checks: (1) Pipeline consistency -- the workflow steps in CLAUDE.md must match the pipeline in agent-workflow.md. Count the steps, verify the order, confirm skip conditions match. (2) Critic consistency -- agent-critic.md severity levels must match plan-critic.md and implementation-critic.md output formats. Revision caps (1 round plan, 2 rounds implementation) must be stated consistently in all 3 files. (3) Drift detection consistency -- agent-doc-updater.md DRIFT section must match doc-updater.md checklist item. Security escalation rule must appear in both. Skip condition must appear in both. (4) Cross-references -- every file path mentioned must be correct. Section names referenced from one file must exist in the target file. (5) No duplication -- critic severity levels are referenced, not redefined. Delegation template exists in one place only. (6) Formatting -- headers, tables, code blocks, DO/NEVER follow the style of pre-existing content. (7) Dry-run: walk through a hypothetical 5-file feature change and verify every pipeline step is reachable and documented. Report any inconsistencies found. | Restrictions: Read only -- do not modify files. Report findings as a checklist with PASS/FAIL per check. If any FAIL, describe what is inconsistent and which files to fix. | Success: All 7 checks pass, or all inconsistencies are identified with specific file + section references for fixing_
