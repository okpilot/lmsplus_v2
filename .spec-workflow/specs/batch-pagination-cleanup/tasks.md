# Tasks Document — batch-pagination-cleanup

- [ ] 1. Move PaginationBar to shared location + add entityLabel prop
  - Files: `apps/web/app/app/_components/pagination-bar.tsx` (NEW — moved), `apps/web/app/app/_components/pagination-bar.test.tsx` (NEW ��� moved), `apps/web/app/app/admin/questions/_components/questions-page-shell.tsx` (update import), `apps/web/app/app/admin/questions/_components/questions-content.tsx` (update import)
  - Move `pagination-bar.tsx` + `pagination-bar.test.tsx` from `admin/questions/_components/` to `app/app/_components/`
  - Add optional `entityLabel?: string` prop (default: `"questions"`). Update "Showing X–Y of Z questions" to use it.
  - Update all import paths in admin questions to `@/app/app/_components/pagination-bar`
  - Run existing pagination-bar tests to confirm they pass
  - Purpose: Enable pagination reuse across features
  - _Leverage: `apps/web/app/app/admin/questions/_components/pagination-bar.tsx`, `apps/web/app/app/admin/questions/_components/pagination-bar.test.tsx`_
  - _Requirements: NFR — Reusable pagination, Consistent pattern_
  - _Prompt: Implement the task for spec batch-pagination-cleanup, first run spec-workflow-guide to get the workflow guide then implement the task: | Role: Frontend developer specializing in React component refactoring | Task: Move PaginationBar component and its tests from `apps/web/app/app/admin/questions/_components/` to `apps/web/app/app/_components/`. Add an optional `entityLabel?: string` prop (defaulting to `"questions"`) and use it in the "Showing X–Y of Z" text (line 41). Update import paths in `questions-page-shell.tsx` and `questions-content.tsx`. Run `pnpm test -- --run pagination-bar` to verify. | Restrictions: Do NOT change any pagination logic or page number algorithms. Do NOT modify admin questions behavior. Only move files, add the prop, and update imports. | _Leverage: Read the current `pagination-bar.tsx` and its test file before making changes. Check `questions-content.tsx` for PAGE_SIZE import from this file._ | Success: (1) PaginationBar renders "sessions" when entityLabel="sessions" is passed, (2) PaginationBar renders "questions" when no entityLabel is passed, (3) all existing pagination-bar tests pass at new location, (4) admin questions page still works (imports updated). Mark task as [-] in tasks.md before starting, log implementation, then mark [x] when complete._

- [ ] 2. Extract parsePageParam to shared utility
  - Files: `apps/web/lib/utils/parse-page-param.ts` (NEW), `apps/web/app/app/admin/questions/page.tsx` (update to import), `apps/web/lib/utils/parse-page-param.test.ts` (NEW)
  - Extract `parsePageParam` function from admin questions `page.tsx` (lines 10-13) into a shared utility
  - Signature: `parsePageParam(value: string | string[] | undefined): number` — returns positive integer, defaults to 1
  - Update admin questions page.tsx to import from new location
  - Write co-located unit test covering: valid number, zero, negative, float, undefined, string array, NaN
  - Purpose: Reusable page param parsing for all paginated pages
  - _Leverage: `apps/web/app/app/admin/questions/page.tsx` lines 10-13_
  - _Requirements: NFR — Consistent pattern_
  - _Prompt: Implement the task for spec batch-pagination-cleanup, first run spec-workflow-guide to get the workflow guide then implement the task: | Role: TypeScript utility developer | Task: Extract `parsePageParam` from `apps/web/app/app/admin/questions/page.tsx` (lines 10-13) into `apps/web/lib/utils/parse-page-param.ts`. Signature: `(value: string | string[] | undefined) => number`. Returns positive integer >= 1. Write co-located test at `parse-page-param.test.ts` covering: valid ("3" → 3), zero ("0" → 1), negative ("-1" → 1), float ("2.5" → 1), undefined (→ 1), string array (["2"] → use first element), NaN ("abc" → 1). Update admin questions page.tsx to import from new location. Run `pnpm test -- --run parse-page-param`. | Restrictions: Do NOT change the parsing logic itself. Do NOT modify admin questions behavior. Keep the function pure (no side effects). | Success: (1) utility exports and works correctly, (2) admin questions page.tsx uses the import, (3) all tests pass. Mark task as [-] in tasks.md before starting, log implementation, then mark [x] when complete._

- [ ] 3. Reports page — server-side pagination + sort (#470)
  - Files: `apps/web/lib/queries/reports.ts` (REWRITE), `apps/web/lib/queries/reports.test.ts` (REWRITE), `apps/web/app/app/reports/page.tsx` (MODIFY), `apps/web/app/app/reports/_components/reports-content.tsx` (MODIFY), `apps/web/app/app/reports/_components/reports-list.tsx` (MODIFY), `apps/web/app/app/reports/_components/session-table.tsx` (MODIFY — sort toggles), `apps/web/app/app/reports/_components/session-card.tsx` (check if changes needed)
  - Replace `getAllSessions()` with `getSessionReports({ page, sort, dir })` returning `{ ok: true, sessions, totalCount } | { ok: false, error }`. Use `.range(from, to)` + `{ count: 'exact' }`. PAGE_SIZE = 10. Server-side `.order()`.
  - Rewrite `reports.test.ts` — current tests use `.rejects.toThrow()`, new function returns discriminated union
  - Update `page.tsx` to parse `?page`, `?sort`, `?dir` from searchParams using `parsePageParam` utility
  - Update `reports-content.tsx` to call new query, calculate totalPages, redirect if out of range
  - Update `reports-list.tsx` — remove client-side sort state (useState), sort toggles update URL params via `router.replace()`, add PaginationBar with `entityLabel="sessions"`
  - Purpose: Server-side paginated + sorted reports list (Decision 34)
  - _Leverage: `apps/web/app/app/admin/questions/queries.ts` (pagination pattern), `apps/web/app/app/admin/questions/_components/questions-content.tsx` (redirect pattern), shared `PaginationBar`, shared `parsePageParam`_
  - _Requirements: R1 (all acceptance criteria)_
  - _Prompt: Implement the task for spec batch-pagination-cleanup, first run spec-workflow-guide to get the workflow guide then implement the task: | Role: Full-stack Next.js developer | Task: Convert the reports listing page from client-side full-load to server-side paginated + sorted. Read the design doc at `.spec-workflow/specs/batch-pagination-cleanup/design.md` sections 2-5 for full details. Read the existing admin questions pagination pattern in `queries.ts`, `questions-content.tsx`, and `page.tsx` as your template. Key changes: (1) Replace `getAllSessions()` in `reports.ts` with `getSessionReports({ page, sort, dir })` using `.range()` + `{ count: 'exact' }` + `.order()`, PAGE_SIZE=10, discriminated union return. (2) Rewrite `reports.test.ts` — change `.rejects.toThrow()` to `{ ok: false }` assertions. (3) Parse URL params in `page.tsx` using shared `parsePageParam` from `@/lib/utils/parse-page-param`. Add `parseSortParams` inline. (4) Update `reports-content.tsx` with redirect for out-of-range pages. (5) Convert `reports-list.tsx` from client-side sort (useState) to URL-driven sort toggles that reset page to 1. Add PaginationBar with `entityLabel="sessions"`. Import PaginationBar from `@/app/app/_components/pagination-bar`. Run `pnpm test -- --run reports` to verify. | Restrictions: Do NOT change session-table.tsx or session-card.tsx display logic beyond what's needed for sort toggles. Maintain mobile responsive layout. Sort column mapping: 'date' → 'started_at', 'score' → 'score_percentage', 'subject' → join subject name. Keep parallel subject name + answer count resolution but scope to the 10 sessions on the page. Page file must stay under 80 lines. | Success: (1) Reports page shows 10 sessions per page, (2) sort toggles change URL params and re-fetch, (3) PaginationBar shows "Showing 1–10 of N sessions", (4) out-of-range page redirects, (5) all tests pass. Mark task as [-] in tasks.md before starting, log implementation, then mark [x] when complete._

- [ ] 4. Quiz report — server-side paginated question breakdown (#469)
  - Files: `apps/web/lib/queries/quiz-report.ts` (REWRITE), `apps/web/lib/queries/quiz-report.test.ts` (REWRITE), `apps/web/app/app/quiz/report/page.tsx` (MODIFY), `apps/web/app/app/quiz/report/_components/report-card.tsx` (MODIFY), `apps/web/app/app/quiz/report/_components/result-summary.tsx` (MODIFY), `apps/web/app/app/quiz/report/_components/question-breakdown.tsx` (MODIFY), `apps/web/app/app/quiz/report/_components/report-card.test.tsx` (UPDATE), `apps/web/app/app/quiz/report/_components/result-summary.test.tsx` (UPDATE)
  - Split `getQuizReport()` into `getQuizReportSummary(sessionId)` + `getQuizReportQuestions({ sessionId, page })`. Delete `QuizReportData` type, replace with `QuizReportSummary`. Questions function returns `{ ok: true, questions, totalCount } | { ok: false, error }`.
  - Update `page.tsx` to parse `?page` alongside `?session`
  - Update `report-card.tsx` to accept separate summary + questions + pagination props
  - Update `result-summary.tsx` prop type from `QuizReportData` to `QuizReportSummary`
  - Replace `question-breakdown.tsx` client-side PAGE_SIZE/showAll with server-side pagination + PaginationBar
  - Update test files for new types and split functions
  - Purpose: Server-side paginated question breakdown (Decision 34)
  - _Leverage: `apps/web/app/app/admin/questions/queries.ts` (pagination pattern), shared `PaginationBar`, shared `parsePageParam`, `get_report_correct_options` RPC_
  - _Requirements: R2 (all acceptance criteria)_
  - _Prompt: Implement the task for spec batch-pagination-cleanup, first run spec-workflow-guide to get the workflow guide then implement the task: | Role: Full-stack Next.js developer | Task: Convert the quiz report question breakdown from client-side expand/collapse to server-side pagination. Read the design doc at `.spec-workflow/specs/batch-pagination-cleanup/design.md` sections 6-10 for full details. Key changes: (1) Split `getQuizReport()` in `quiz-report.ts` into `getQuizReportSummary(sessionId)` (returns session metadata without questions) and `getQuizReportQuestions({ sessionId, page })` (returns paginated questions with `.range()` + `{ count: 'exact' }` on `quiz_session_answers`, PAGE_SIZE=10). Delete `QuizReportData` type, create `QuizReportSummary`. `get_report_correct_options` RPC still called once per page (returns all correct options for session — small enough). (2) Update `page.tsx` to parse `?page` param. Call both query functions. (3) Restructure `report-card.tsx` to accept `summary: QuizReportSummary` + `questions: QuizReportQuestion[]` + `page/totalCount/pageSize` as props. (4) Update `result-summary.tsx` prop from `QuizReportData` to `QuizReportSummary`. (5) Replace `question-breakdown.tsx` client-side toggle with PaginationBar. Import from `@/app/app/_components/pagination-bar`. (6) Update all test files — fixtures, types, assertions. Run `pnpm test -- --run quiz-report` to verify. | Restrictions: Do NOT change how correct answers are resolved (must use `get_report_correct_options` RPC). Do NOT expose `options[].correct` field. Session summary stats come from the session row, NOT from iterating questions. Keep the `ended_at` guard that prevents mid-session answer exposure. Page file must stay under 80 lines. | Success: (1) Quiz report shows 10 questions per page, (2) summary (score, time, subject) stays visible across pages, (3) PaginationBar shows "Showing 1–10 of N questions", (4) out-of-range page redirects, (5) all tests pass. Mark task as [-] in tasks.md before starting, log implementation, then mark [x] when complete._

- [ ] 5. Migrate flagged_questions reads to active_flagged_questions view (#467)
  - Files: `apps/web/app/app/quiz/actions/flag.ts` (MODIFY reads), `apps/web/lib/queries/quiz.ts` (MODIFY filterFlagged), `apps/web/app/app/quiz/actions/filter-helpers.ts` (MODIFY applyFilters), `apps/web/lib/gdpr/collect-user-data.ts` (MODIFY collectUserData), `apps/web/app/app/quiz/actions/flag.test.ts` (UPDATE mocks), `apps/web/app/app/quiz/actions/filter-helpers.test.ts` (UPDATE mocks), `apps/web/lib/gdpr/collect-user-data.test.ts` (UPDATE mocks)
  - For each READ query: replace `.from('flagged_questions')` with `.from('active_flagged_questions')`, remove `.is('deleted_at', null)`. Cast result to table Row type with safety comment.
  - Write operations (`unflagQuestion`, `flagQuestion`) stay on `flagged_questions` base table
  - `toggleFlag()` has a READ (existence check, line ~33) that moves to view, and write paths that stay on table — split carefully
  - Update test mocks: READ assertions/keys → `active_flagged_questions`, WRITE assertions → keep `flagged_questions`
  - Purpose: Centralize soft-delete filtering in the DB view
  - _Leverage: Migration 051 (`active_flagged_questions` view with `security_invoker = true`), `packages/db/src/types.ts` view type_
  - _Requirements: R3 (all acceptance criteria)_
  - _Prompt: Implement the task for spec batch-pagination-cleanup, first run spec-workflow-guide to get the workflow guide then implement the task: | Role: Backend TypeScript developer with Supabase expertise | Task: Migrate all flagged_questions READ queries to use the `active_flagged_questions` view. Read the design doc at `.spec-workflow/specs/batch-pagination-cleanup/design.md` section 11 for full details. Source files to modify: (1) `flag.ts` — `toggleFlag()` existence check (line ~33, `.from('flagged_questions').select('student_id')...is('deleted_at', null)`) → `.from('active_flagged_questions').select('student_id')` (remove `.is('deleted_at', null)`). `getFlaggedIds()` same pattern. KEEP `unflagQuestion` and `flagQuestion` on `flagged_questions` base table. (2) `quiz.ts` `filterFlagged()` — same swap. (3) `filter-helpers.ts` `applyFilters()` — same swap. (4) `collect-user-data.ts` `collectUserData()` — same swap. For type safety: cast view results with comment: `// View columns typed nullable (Postgres artifact); safe to cast — underlying table has NOT NULL constraints`. Use `as Tables<'flagged_questions'>[]` or similar. Update ALL test files: `flag.test.ts` (READ assertion mocks → `active_flagged_questions`, WRITE assertions stay `flagged_questions`), `filter-helpers.test.ts` (mock key rename), `collect-user-data.test.ts` (mock key rename). Run `pnpm test -- --run flag --run filter-helpers --run collect-user-data` to verify. | Restrictions: Do NOT modify write operations (upsert, soft-delete update). Do NOT modify the view definition or migration. Do NOT add runtime null guards for the type cast — the underlying NOT NULL constraints make it safe. Do NOT modify RLS policies. | Success: (1) Zero `.from('flagged_questions')` calls remain on READ paths, (2) all `.is('deleted_at', null)` removed from flagged read paths, (3) write paths still use base table, (4) all tests pass. Mark task as [-] in tasks.md before starting, log implementation, then mark [x] when complete._

- [ ] 6. Add unique constraint on question_banks(organization_id) (#358)
  - Files: `supabase/migrations/YYYYMMDDHHMMSS_unique_bank_per_org.sql` (NEW)
  - Create new migration adding `ALTER TABLE question_banks ADD CONSTRAINT question_banks_organization_id_key UNIQUE (organization_id);`
  - No application code changes — `insertQuestion`'s `.limit(1).single()` becomes deterministic by definition
  - Purpose: Enforce 1:1 org-bank invariant at DB level
  - _Leverage: Existing migration pattern in `supabase/migrations/`_
  - _Requirements: R4 (all acceptance criteria)_
  - _Prompt: Implement the task for spec batch-pagination-cleanup, first run spec-workflow-guide to get the workflow guide then implement the task: | Role: Database migration developer | Task: Create a new migration file at `supabase/migrations/` with timestamp format `YYYYMMDDHHMMSS_unique_bank_per_org.sql`. Content: `ALTER TABLE question_banks ADD CONSTRAINT question_banks_organization_id_key UNIQUE (organization_id);` with a header comment explaining the purpose (enforces 1:1 org-bank invariant, makes `.limit(1).single()` in insertQuestion deterministic). Use `IF NOT EXISTS` pattern if PostgreSQL supports it for ADD CONSTRAINT, otherwise use a DO block with exception handling for idempotency. Check the latest migration timestamp in `supabase/migrations/` to generate the correct next timestamp. | Restrictions: Do NOT modify any existing migration files. Do NOT modify `insertQuestion` application code. Do NOT add any other schema changes. | Success: (1) Migration file created with correct timestamp, (2) constraint is idempotent (safe to re-run), (3) no application code changes needed. Mark task as [-] in tasks.md before starting, log implementation, then mark [x] when complete._

- [ ] 7. Close already-resolved issues (#365, #364, #363)
  - No files changed
  - Close issues with comment referencing PR #463 as the fixing change
  - Purpose: Board hygiene
  - _Requirements: R5_
  - _Prompt: Implement the task for spec batch-pagination-cleanup, first run spec-workflow-guide to get the workflow guide then implement the task: | Role: Project maintainer | Task: Close 3 GitHub issues that were already resolved by PR #463. Run: `gh issue close 365 --repo okpilot/lmsplus_v2 --comment "Resolved by PR #463 — getQuestionsList now returns discriminated {ok, error} union instead of collapsing errors to empty array."` then `gh issue close 364 --repo okpilot/lmsplus_v2 --comment "Resolved by PR #463 — replaced questions.length === 100 check with exact totalCount from Supabase { count: 'exact' }."` then `gh issue close 363 --repo okpilot/lmsplus_v2 --comment "Resolved by PR #463 — question-filters.tsx refactored from 169 to 73 lines during pagination implementation."` | Restrictions: Do NOT modify any code. Only close issues with comments. | Success: All 3 issues closed with explanatory comments. Mark task as [-] in tasks.md before starting, log implementation, then mark [x] when complete._
